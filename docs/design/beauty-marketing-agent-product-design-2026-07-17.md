# 美业宣发经营 Agent：产品设计与决策日志（合并权威版）

Generated by `/office-hours` on 2026-07-17; 2026-07-17 同日将 `ai-native-context-decision-log-2026-07-17.md` 全文并入第二部分（原文件删除，条目编号不变）

> **现行权威入口（2026-08-19）**：本文的时序裁决以后置 accepted 决策为准。下方「2026-07-24 当前产品合同」保留为历史快照；当其与 D-170～D-178 或本文末的 D-178 权威勘误冲突时，以下方「2026-08-19 现行产品合同摘要」、D-170～D-178 原决策与勘误为准。

Last synchronized: 2026-07-24（D-101～D-109：单店单人、双创作主线、四类输出编译、模型原生视频、图片领域合同、模型装配后台、Skills 与商品化计费；D-110：收敛执行合同——装配门先行、单一门槛序列、修复即建设、LPGS 并入收档；D-111：Day-0 与条件缺失＝双入口分流+意图驱动渐进引导，场景反推先行纪律；D-112：Agent 介入边界＝确定性骨架+段内智能、LLM 出判断状态机执行，含 platform 双字段合同；D-113：事实槽 LLM 满足度判断、AgentKit 双参照登记、择优语义收窄为默认单主候选；D-114：定制创作主容器＝agent 流式对话+AGUI 三层结构，含 ContentIntent v1 定位；D-115：ImageIntent v1——三操作商家语言、七类语义 slot、精确文字校验门、OutputPlan 两型；D-116：图文合一 NotePlan v1——页级计划先行、双风格可配草稿、确认卡超时放行、拟人化交付语言合同；D-117：MarketingIdentity 建档向导+生成自由/软提示/发布收口——过程零内容限制、AI 生成提醒标识、风控唯一收口在发布确认，三动作分离、执行绑 revision；D-118：四类输出编排分级——copy/image/image_text_note/video 确认，轻输出＝五段退化执行非旁路，图文多阶段编排，候选策略分型；D-119：客户资产录入交互合同——五步范式（示例卡/行业推荐/一句话+拍照代填/AI 编排/逐项确认）、双轨同链、录入永不前置；D-120：多格式资产解析管线——解析统一 MinerU 不建独立 VLM 通道、交互同步+执行服务端、单件同步批量异步、四层表结构 ContextBundle 只吃确认层；D-121：内容工作区——个性资产容器与引用上下文入口、与租户严格分离、套餐一等维度 1/2/3+加油包；D-122：HITL 编排容错原则——允许 LLM 幻觉、允许工具微错、允许人介入修正，流程恒前进；D-123：商品数字框架 v1——内部成本基准+大冗余定价、文案/图片/视频三桶额度与三类加油包、视频 3/6/9 亮点定位+代运营引流、数字＝运营参数线下核算后台手填、缺额提醒超额报停；D-124：D-102 六项付费收口门槛归属——R 门=注册邮件/单一商品目录/前台去工程语言，E 门=支付真实闭环/删除匿名化/取消退款补单，试点期支付零开发以兑换码+人工开通承接；D-125：Landing 两阶段——先按现有框架诚实内容填充随 R 门，精细窗口专项后置，功能完整性与全流程体验测试优先；D-126：Dashboard 工作台首页（非 Landing）轻推荐+Day-0 示例店——热态一天一主推荐点击预填充 Composer、v1 依据=确定性规则+行业先验受控配置，冷态三行业示例店 platform_sample 隔离、示例任务真实执行扣试用额；D-127：开发路线＝原地换壳保核——不重开不整体重写、退役面真删默认（compose/烧录链、槽位残留、换壳后旧 IA 页）、Pro Studio 行原为 FREEZE 仅留入口（已由 **D-170 RETIRE 全量退役** 取代，见 2026-08-01 追加）、前端沿在途 Composer 主干完成换壳＋旧交互面整批下线无双轨期、新能力按 ADR-0019 纵切净新建、god module 触碰时拆不开专项重构票、拆票前置＝逐模块归桶判定矩阵；D-128：拆票前四项细化口径——v1 内容变体平台＝小红书/抖音/视频号三平台、朋友圈＝交付目标不做变体主体（M-01 产品面依据）、注册试点期＝邮箱+兑换码+运营代开手机号短信挂 E 门评估、产品命名挂 R 门前专项、示例任务产物允许导出判断与正式使用一致+试用状态额度＝后台人工动态开关；D-129：拆票前组件选型收口——MinerU＝官方精准 API＋数据出域温柔文字免责不设门槛（自托管挂 E 门同批）、文案与编排判断位默认 LLM＝DeepSeek（默认型号＝deepseek-v4-pro，供给目录运营可换，官方文档全量镜像入 references）、基础组件优先以 MkFast 模板与 Cloudflare 既有能力为实现基础（邮件＝Resend、对象存储＝R2）＋同日补充三项：图/视频默认型号＝seedream-5-pro/seedance-2 系·供给通道默认＝方舟直连 tuzi relay 容灾、exactText 校验＝既有多模态 VLM 零新件、Recipe/Skills 载体＝PG＋提示词 Langfuse（后台表单渲染组件悬置已由 D-130 解除）；D-130：前端组件供给基准＝HeroUI Pro V3（用户专属授权）——REBUILD/NEW 面优先取用·AI Chat 组件族对 D-114 对话流·D-107 后台表单＝其组件族组装、KEEP 桶不为换库专项迁移触碰时换、门店橱窗 DESIGN.md 仍为唯一视觉权威组件经 token 适配（主题拍板＝Glass）、专属授权镜像 references/repos/herouipro-v3 不入库不分发、Landing 页例外保持现状仅改文案与前后链接（专项优化随 D-125 阶段二）其余页面组件与动效均优先取用·模板起点 template-chat＝工作区交互页/template-dashboard＝运营后台）；D-131：拆票组织——前端相关票独立成组由用户指定 Agent 与模型执行（已登记＝Opus）·分组不破 ADR-0019 纵切（接缝合同与属主显式化、旅程收口不许组内假绿）·前端组票面自包含（DESIGN.md＋ADR-0014＋D-130 上下文包）；D-132：凭证与运营供给单门——凭证项与运营项归并为单一供给门类不散落各票：开票前产出统一供给清单（docs/ops/provisioning-manifest.md）由用户一次性补齐、票面只按清单键引用不各自索取，开发与 CI 默认档一律 fixture/种子值走绿缺凭证不阻塞不假红、真凭证到位后按 live_verified 门径一次核销，运营数字以种子样例值验收真值运营期后台手填）
2026-07-27 追加：D-133～D-138 补录（原始拍板在 Orca run 收口阶段，来源 `.scratch/orca-run-2026-07-25/`）；D-139～D-149＝产品规划落地差距审计 12 项拍板（审计报告 `docs/reviews/product-plan-implementation-gap-review-2026-07-27.md`，7 路只读审计：首页入口/成品结果/资产身份/商业化/对话流/HeroUI 采用/Harness 编排）；D-150＝流程决策「完成语义锚定旅程」——消费者证明关票门/接缝对称防线/假绿治理/支线旅程硬门，根治「后端建满前台不接」复发机制，约束此后全部开票。
2026-08-01 追加：**D-170 Pro Studio 全量退役（RETIRE）**——取代 D-127 FREEZE「只留入口」、D-110③ keep-code、D-103 画布保留假设、D-099 parity 现行入口、D-075/077/084/092 入口族现行声张、ADR-0012 作为现行法；实施权威＝`docs/specs/pro-studio-retirement-spec-2026-08-01.md`；主线仅定制创作＋自由创作薄路径，无无限画布接替。
Branch: `main`
Repo: `legacy-origin-a/legacy-web-repo`
Status: ACCEPTED PRODUCT DIRECTION, RESEARCH OPEN
Mode: Startup
Supersedes as product authority: `bin-main-design-20260717-130101.md`
执行权威: Wave 1 验证与执行合同见 `/Users/bin/.gstack/projects/legacy-origin-a-legacy-web-repo/bin-main-design-20260717-183658.md`（已批准）与验证手册 `references/analysis/beauty-marketing-validation-2026-07-17/VALIDATION-PLAYBOOK.md`；量化指标命名、硬失败清单、进场节奏与本文冲突时，以 Week 0 预登记口径为准（另见 D-026）。
Preserves from prior design: 已核验的 ContentPackage、异步恢复、唯一写入边界和发布安全结论仍是实现约束，不再决定前台产品结构。

本文分两部分：第一部分为产品设计正文；第二部分为决策日志（D-001 起）与待验证、待拍板清单。

# 第一部分：产品设计

## Decision Summary

产品主框架锁定为面向**单店单人**的 AI-native 美业宣发内容创作 SaaS。核心价值是把门店事实、品牌/IP、素材与平台/行业打法编译为低学习成本、可直接使用的文案、图片、图文笔记和视频；持续曝光、承接流量与到店引流是内容的业务目标，但产品不扩张为店务或经营管理系统。

本文沿用用户提出的“广告 + 到店引流”作为商业目标，但“广告”在首个发布范围中指广义门店宣发，不默认等同于付费媒体投放。首发聚焦自然/自有内容、微信私域交接和可导出的本地营销物料；只有账号、预算、平台能力都经过真实验证时，付费投放才作为绑定精确成品的一次性外部动作出现，否则入口隐藏并提供人工交接。

### 2026-08-19 现行产品合同摘要（D-170～D-178）

- **创作表面**：Pro Studio/无限画布已全量退役（D-170）；主线是定制创作 + 模型原生的自由创作薄路径。XHS 专项在图文轴的 Recipe 层，不新增一级导航（D-171）。
- **会话与执行**：`AgentThread` 是跨 Work 的长期会话根；Agent Session Harness 与 DBOS Production Make Harness 只通过冻结 `ExecutionPlanSnapshot` 交接，计划以 plan-as-data 表达，五阶段只保留 trace taxonomy（D-178）。
- **计费**：商家侧唯一口径是周期积分 + 加油包，支付提供方为 Waffo Pancake；三桶额度与 Creem 已退役（D-172）。
- **首次出活与上下文**：Day-0 首 token 预算为最多 3 次显式操作（D-173）；行业从门店档案属性读取（D-174）；自由创作不注入门店/项目事实，但仍保留权利与资质硬门（D-175）。
- **失败与工具**：试点前不建设失败任务原地重跑，先保证诚实失败与可退回出口（D-176）；面向商家的 standalone tools 与 Recipe Studio 独立页已退役（D-177）。
- **权威勘误**：D-178 历史正文中的 D-088 引用错位；Thread 禁令的正确 supersede 目标为 D-046，Recent 投影另引用 D-097，D-088 视频重生的新任务/一次用户用量语义不变。完整留痕见本文 D-178 后的「D-178 权威勘误」。

> **历史快照提示**：下方「2026-07-24 当前产品合同」为当日快照，不再承担现行摘要职责；不改写其原文是为保留决策演进审计轨迹。

### 2026-07-24 当前产品合同

- 一个账号只对应一个个人门店工作区和一个实际操作者；首发不做成员邀请、多人协作、角色切换、多工作区或席位计费（D-102）。（此处「工作区/多工作区」＝租户/协作/席位口径，仍不做；D-121「内容工作区」为独立概念——账号 1:N、按套餐 1/2/3＋加油包、配前台切换器，与租户严格分离，不在此否定之列。）
- 创作中心只有两条用户主线：默认的**定制创作**负责自动编排门店上下文、行业/平台 Recipe 与 Skills；次入口**自由创作**只调用用户显式选择的模型原生能力，不隐式注入整套门店资料，也不另建 Agent/Harness（D-103）。
- 定制创作共享稳定 `ContentIntent`，随后分为文案、图片、图文笔记和视频四类输出编译器；Kickart 只作为定制视频的控制节点参考，不是全部媒介的统一流水线（D-104）。
- 视频主线直接交付模型原生 `VideoArtifact`；本地 FFmpeg 拼接、字幕/BGM/水印烧录和弱剪辑器退出创作主链，需要专业剪辑时交接剪映等外部工具（D-105）。
- 图片统一为 `generate / edit / reference_transform` 三个底层操作，套图是 `OutputPlan`，图文笔记是 `NotePlan + 文案分支 + 图片分支 + 一致性评估`（D-106）。
- 内部后台负责模型装配、Skills/Recipe 和发布运行；`Workflow + Skill + 输入输出与评测合同` 以前台“创作能力包”呈现，仍由 `CreationRecipeVersion` 作为唯一顶层发布聚合（D-107～D-108）。
- 用户购买套餐与文案/图片/视频三桶额度及对应加油包（文案包/图片包/视频包；三桶体系由 D-123 扩展自 D-109 两桶）；公开模型名与尺寸、比例、时长、数量等创作规格可见，Token、供应商单价、路由、重试和毛利不可见（D-109）。
- 收敛执行唯一权威：装配门（全新商家注册→默认栈→首次出活的 required 旅程）先行，随后 M 合入门 → R 试点/发布门 → E 发布工程门；R 级修复一律直接落 D-101～D-109 目标形态（修复即建设）；LPGS 十六票并入收档；裁决元原则=D-101～D-109 是更细化场景感受后的决定，与更早决策、正文旧口径或评审/研究建议冲突时以 D-101～D-109 为准（D-110）。
- Day-0 与「启动条件缺失」一律用双入口产品行为解决：定制创作流内引导补充（意图驱动、可跳过）、满足线=本次意图能用上任何一类经营表达资产（门店/产品/团购/品牌/个人 IP 等），不满足自然路由自由创作先出活并显式告知；不用数据层方案（不播合成身份、不改模糊可选）；功能决策场景反推先行、技术合同服从（D-111）。
- Agent 介入边界：确定性骨架＋段内智能——五段 Harness 仍是 DBOS 确定性状态机，段内为受限 agent loop（控制 LLM 理解/规划/allowlist 内调工具/自检），阶段分支由 LLM 出结构化判断信号、Workflow 按预置条件边执行转移；场景规则退位给 LLM 判断＋确定性兜底，硬编码只保四类底线（事实真实/权利授权/产品额度/外部发布确认）；platform 拆为 contentPackagePlatform×distributionTarget 双字段，用户选择进服务端签名合同（D-112）。
- 事实槽由「Recipe 声明结构化事实需求＋ContextBundle 已确认事实第一等满足＋LLM 满足度判断＋expiry/权利硬校验兜底」承接，上传走建档链不直接满足槽；AgentKit 轻量版/seq 版与 Kickart 并列为参考实现（借组合模式不引 runtime，四宫格仅可选形式）；④段默认交付 1 个主候选，「择」仅限用户品味选择与质量门有界重试，不建多场景通用「优」判据（D-113）。
- 定制创作主容器＝agent 流式对话＋AGUI 三层结构：对话流主线（意图/引导补问卡/计划卡流式/HITL 确认卡/额度卡/任务卡/成品预览卡）、对象工作区纵深（成品卡展开进入，结果中心=放大镜）、任务卡时间桥（长任务可离场+通知拉回）；「提交→跳转结果页」模式被取代，SSE/token 流/审批卡/任务中心重组为对话内嵌；自由创作保持参数面板式不 agent 化；ContentIntent＝①②段编译产物非用户输入，image_text_note 为 outputKind 一等值（D-114）。
- 图片领域用户语言：自由创作三任务语言入口（生成新图/修改这张图/用几张图合成一张→generate/edit/reference_transform，不暴露工程术语）；七类语义 slot（人物/作品案例/门店场景/产品/品牌元素/风格参考/构图参考，案例图 edit 不变量=美业命门）；exactText[] 一等字段＋生成后 OCR/VLM 精确文字校验门（「398 生成成 389」=事实事故非质量瑕疵）；OutputPlan 两型 single/set，页序语义归 NotePlan（D-115）。
- 图文合一 NotePlan v1：页级计划先行（pageRole 可扩展枚举 cover/pain_scene/solution_show/work_case/price_offer/cta_guide，页结构由语义决定不固定张数）→图片分支并行、文字依赖串行，五维一致性评估回炉，单页重生成=1 图点，ImageTextNote 全包=R-01 试点验收对象；意图确认卡=建议补充+默认值+超时自动继续（补 D-111，秒数为运营参数，引导永不变成新阻断）；默认双风格草稿（暂定干货科普+种草叙事，配图仅跟随选中版本），风格集合为后台受控配置可随时重排不锁死（补 D-113，判断权在用户）；全链对客文案（阶段宣告/确认卡/任务总结/失败申报）=拟人化交付陈述层——给经验不足商家内容确定性（策略依据/趋势对齐/使用导购）以支撑价值认可，禁止工程语言与 LLM 原始输出直接暴露（D-116）。
- MarketingIdentity 建档=对话式向导+AI 结构化草案（低门槛入口→意图分流→一句话背景+可选参考文件→AI 扩展全字段→校对保存），字段带 provenance（user/account_import/document/ai_suggestion）与显式用途说明，AI 推断项默认 unconfirmed；**生成自由+软提示+发布收口**：生成过程零内容限制（允许适当美化、多角度演绎，不因事实未登记拦截），规避项=AI 生成提醒标识（「内容由 AI 生成，请注意甄别真实性」类，对齐 D-039 AIGC 标识义务），风控唯一收口=外部发布确认门（D-112），内容真实性责任在商家、平台不承担全流程风控；「创建人设/设为默认/本次会话选择」三动作显式分离，人设投影缓存键含 identity revision；执行绑定 identity revision 非可变整数 id；「事实真实」底线口径收窄=忠实性（商家已提供的信息不得生成错，D-115 校验门维持）+红线硬停（D-025 准入层维持），演绎性内容不属硬门（D-117）。
- 四类输出形式确认＝`copy`（纯文本，朋友圈/小红书/短视频脚本等载体格式为 contentPackagePlatform×风格集合作用于 copy 的模板维度）/`image`/`image_text_note`/`video`（与 D-101/D-104 四类输出编译器一一对应）；编排分级＝纯文本「人设+载体模板拼接→单次 LLM 调用」秒级流式、纯图片 ImageIntent 单次模型调用、图文多阶段编排（NotePlan 先行→文字定稿→页级并行→五维评估）、视频模型原生直出；候选策略分型＝copy/image/video 默认 1 主候选（D-113）、image_text_note 默认双风格（D-116）；**轻输出＝五段 Harness 退化执行而非旁路**——③④段可退化为模板拼接+单调用，但共享①②段注入（人设/事实槽/平台）、额度门、AI 生成软提示、交付合同与 durable 载体，禁止为轻链路另立旁路系统，段位重量封装在各输出编译器内部（D-118）。
- 客户资产录入＝**五步范式**（推广绘文人设向导到全资产）：看样子（每类行业示例卡可换一换）→选推荐（行业先验清单勾选，admin-config 受控配置）→说一句（一句话＋**拍照/上传代填**：价目表→项目清单、团购截图→套餐草案、门头照→门店档案）→AI 编排（provenance 草案：user/photo_extract/ai_suggestion）→逐项确认（AI 推断默认 unconfirmed；素材 VLM 自动分类四 slot＋权利确认软提示不阻断）；覆盖门店/项目/团购/品牌/个人 IP（D-117 特例）/素材库六类存量 schema；**双轨入口同一建档链**（流内缺什么补什么＝D-111 最小录入＋资产页向导式录入容器），禁止第二套录入真相；**录入永不前置**（D-043 ≤2 击出活合同不动，Day-0 冷态＝示例资产）（D-119）。
- 多格式资产解析管线＝五段（接收→解析→结构化→确认→入库）：原件先落 OwnedAsset；解析段＝**统一 MinerU**（格式归一器非语义理解器；照片/截图/PDF/office/复杂表格全走精准 API，网页 URL 走 MinerU-HTML，当前免费、Token 90 天轮换、50 文件/分钟；不为解析建独立 VLM 通道——体验提升功能不过度部署）、纯视觉图四 slot 分类＝生成链既有多模态模型一次调用、敏感证件 v1 手填不自动解析；结构化段＝LLM 出领域草案；**交互同步＋执行服务端**（上传→解析动画→草案即现→当场确认，第三方 key/计量/重试/审计全在服务端，解析任务走同一 DBOS 载体）；**单件同步、批量异步**（批量走队列+任务卡+批量确认）；四层表结构 OwnedAsset→ParsedDocument（可重解析）→AssetDraft→领域表，**ContextBundle/workflow 只读确认层**（D-113 一致）；**手动输入容灾**＝解析失败/回填不准时一键转手填（provenance=user，同 schema 同确认层，下游引用无差别，永不阻断）；MinerU 自部署为规模化迁移路径预留（D-120）。
- 内容工作区＝**个性资产容器与引用上下文入口**：账号 1:N 工作区，一个工作区与一个店铺或品牌 1:1 关联、承载独立一套个性资产（D-119 六类）；**与租户严格分离**（不是权限/安全边界，现有代码 workspace/ws 语义按租户理解不变，实施须新对象命名切割）；创作时 ContextBundle 从当前工作区解析、录入入当前工作区、跨工作区资产不串；**套餐一等维度**＝初级 1 个/中级 2 个/高级 3 个＋加油包单买（档位命名按 D-123 统一；数字框架见 D-123）；默认 1 工作区＝D-101 单店单人体验不变，前台仅工作区切换器一处显现（D-121）。
- HITL 编排容错原则（纲领）：**允许 LLM 幻觉、允许工具微小错误、允许人介入修正，流程恒前进**——不为追求零错误在过程中设阻断；人的介入位是修正点不是审批墙（可介入可不介入，不介入有默认值/超时放行/容灾轨）；设计检验三问＝是否为零错误阻塞流程？介入位是否可跳过？错误是否有人工修正轨且产物同 schema 无损进入后续引用？既有落点＝D-096 partial 诚实交付/D-112 单阻塞审批/D-116 超时放行/D-117 生成自由发布收口/D-119 逐项确认/D-120 手动容灾（D-122）。
- 商品数字框架 v1：内部成本基准（文案≈0.1 元/次、图≈0.5 元/张、图文＝1 文案配 1～6 图、视频≤15s≤15 元/条，对内参考动态调整）＋对客定价留**非常大冗余**（内部核算参考线＝综合毛利 ≥70%）；**高频主力＝文案/图片/图文笔记，视频＝高成本亮点**＝套餐内定数（初级 3/中级 6/高级 9 条/月）＋视频专项加油包标高定价＋**代运营/陪跑线下服务引流钩子**；加油包三类＝文案包/图片包/视频包，后台自由定价；商家感知合同＝付款→得到次数/额度、每次消耗→扣减、**缺额提醒、超额报停**；**全部数字＝运营参数**（运营线下核算→后台手填，支持促销与客户运营灵活调整），前期不建成本管控开发兜底（D-123）。
- D-102 六项公开付费收口的门槛归属：**R 门（真实商家试点前）＝①注册/验证/找回邮件＋④单一商品目录＋⑤前台去工程语言**；**E 门（公开收款前）＝②支付 Provider 真实 checkout/webhook/续费/取消＋③删除/匿名化＋⑥取消/退款/人工补单闭环**；试点期支付零开发＝兑换码＋人工开通＋人工补单承接（D-045 既有能力），收款链路集中 E 门一次做完（D-124）。
- Landing 页两阶段：**阶段一（随 R 门）＝现有框架诚实内容填充**——能力宣称只写已拍板合同（不宣称代发布全平台/智能代运营）、pricing 从单一商品目录同源取数（不硬编码）、定位语＝行业特征×个人 IP 宣发工具平台、AI 生成软提示一处诚实交代，不新增代运营区块；**阶段二＝Landing 精细窗口专项后置**（视觉/转化/文案打磨与代运营入口取舍，立项在 R 门试点后）；优先级纪律＝**功能完整性与全流程体验测试先于 Landing 精细化**（D-125）。
- **Dashboard 工作台首页**（登录后主界面，非 Landing 营销页——Landing 归 D-125）轻推荐＋Day-0 示例店＝同一首页的热/冷态：**热态＝一天一个主推荐**（为什么今天适合发＋用了本店什么＋希望顾客做什么三要素，非机会流），**点击＝预填充 Composer 非自动执行**（额度在「生成」处消耗），v1 推荐依据＝**确定性规则＋行业先验**（admin-config 受控配置，不建推荐算法不接实时热点），文案走 D-116 拟人化合同；**冷态＝三行业示例店**（美发/美甲/皮肤管理各一套示例资产，运营维护，provenance=platform_sample **永不进入真实 ContextBundle**，真实资产录入即替换展示位），**示例任务＝真实执行扣试用额度**（不建免费演示模式第二链路）；「1 候选执行确认」由 D-113 收编无需新决策（D-126）。
- 开发路线＝**原地换壳保核，不重开、不整体重写**（D-111～D-126 重画交付面后的路线重审结论；「无客户无数据负担」的正确兑现＝敢删代码、敢破坏旧合同、不留兼容垫片，而非从零重长已对的东西）：骨架**保留+扩展**（SaaS 骨架/编排执行内核/模型供给链/交付结果面）；退役面**真删默认**（compose/烧录链、槽位表单残留、换壳完成后的旧 IA 页面——git rm 真删，git 可恢复，不注释封存）；**Pro Studio 全量退役（RETIRE）**（D-170 取代 D-127 FREEZE「只留入口」；无新开发/售卖/入口/adoption；无无限画布接替；实施见 `docs/specs/pro-studio-retirement-spec-2026-08-01.md`）；前端沿在途 Composer 主干候选**完成换壳**（M-01/M-02 合同断裂先修）＋**旧交互面整批下线、无新旧双轨期**（接受中间态部分旧页短暂不可用）；新能力（解析管线/四类编译器/三桶扩展/首页挂载/Recipe Studio/Skills）按 ADR-0019 **纵切净新建**；god module（model-supply ≈8 万行/operations ≈5.7 万行）收敛＝**触碰时拆**，不开专项大重构票；拆票前置＝逐模块 KEEP/EXTEND/RETIRE/FREEZE/REBUILD/NEW **归桶判定矩阵**，禁止在 RETIRE 桶续命、在 REBUILD 桶打补丁（D-127；Pro Studio 行由 D-170 升格为 RETIRE）。
- 拆票前四项细化口径：**v1 内容变体平台集合＝小红书/抖音/视频号三平台**（contentPackagePlatform），**朋友圈（wechat_moments）＝交付目标**（distributionTarget，导出＋assisted 拿图文去发，不做平台化变体引擎主体——私域转发面无平台规则引擎，M-01 修复的产品面依据）；**注册通道试点期＝邮箱＋兑换码＋运营代开账号**（R 门①原样建成，D-045 承接零新开发），**手机号短信登录挂 E 门评估**（与部署中国化同批触发点不提前建）；**产品对外名称挂「R 门前专项」**（配合 D-125 Landing 阶段一填充一并定名，不阻塞拆票，R 门收口前必须完成）；**示例任务产物允许导出、判断口径与正式使用完全一致**（不为示例建特殊限制或阉割链路），**试用状态与试用额度＝后台人工动态开关/调整**（D-123 运营参数口径延伸，滥用应对＝运营手动收紧非预建防滥用开发）（D-128）。
- 拆票前组件选型收口：**MinerU 部署形态＝官方精准 API（验证期）**，上传环节数据出域提示＝**温柔文字免责、不设门槛不阻断**（对齐 D-117 软提示精神），自托管（开源需 GPU）＝规模化迁移路径**挂 E 门同批评估**；**文案生成与内部编排判断位 LLM 默认供应商＝DeepSeek**（OpenAI 兼容接口直走 D-035 AI SDK 现行 API，默认型号＝deepseek-v4-pro（v4-flash 轻档备选），catalog 运营参数后台可换＝D-044 机制，需视觉输入的判断位除外；官方 API 文档全量镜像 `references/analysis/deepseek-api-docs-2026-07-24/`，实施票引用接入细节一律指向镜像不凭模型记忆）；**基础组件选型总原则＝优先以 MkFast 模板与 Cloudflare 既有能力为实现基础**——邮件＝模板内置 Resend（R 门①沿用，中国送达率挂 E 门）、对象存储验证期后端＝R2（抽象层已建成只填 env）；其余栈决策（DBOS/AI SDK/Langfuse+promptfoo/admin-config/PG+Drizzle/图视频供给双通道）经逐项扫描均已闭合，实施 Agent 无组件选型临场发明空间；同日补充三项拍板：**图默认型号＝seedream-5-pro 系、视频默认型号＝seedance-2 系，供给通道默认＝火山方舟直连（tuzi relay 容灾备选）**，档位随 D-123 运营核算可调；**exactText 校验＝生成链既有多模态模型 VLM 校验**（零新件不引独立 OCR，误报率票内验收）；**Recipe/Skills 定义载体＝PG 既有库，提示词模板＝Langfuse**（D-037 口径落定）；后台动态表单渲染组件悬置已由 D-130 解除（＝HeroUI 表单组件族组装）（D-129）。
- 前端组件供给基准＝**HeroUI Pro V3（用户专属授权）**：**REBUILD 换壳面与 NEW 净新建面优先取用**（`@ag-ui/pro` 1.0.0-beta.7，HeroUI v3＋Tailwind v4＋React 19＋React Aria，与现栈 React 19.2/Tailwind 4.1.18 零升级对齐；60+ 生产组件含 AI Chat 场景族——chat-conversation/prompt-input/chain-of-thought/markdown/code-block，正对 D-114 对话流主容器；D-107 后台动态表单＝其表单组件族组装，D-129 悬置解除）；**KEEP 桶存量组件不为换库专项迁移**（触碰时换，无双组件库统一化运动）；**「门店橱窗」DESIGN.md 仍为唯一视觉权威**，组件一律经主题 token 适配接入（**主题拍板＝Glass**，Brutalism/Mouve 不启用）；授权＝用户专属，镜像 `references/repos/herouipro-v3/` gitignore **不入库不对外分发**，组件按需复制进 app 属授权范围内使用，升级＝重拉镜像锁版本；**Landing 页例外**＝不受组件基准约束、保持现状仅改文案与前后链接（D-125 阶段一范围，专项优化＝阶段二），其余页面**组件与动效**均优先取用（动效随库内 Motion 体系）；模板起点＝template-chat→工作区交互页、template-dashboard→运营后台可视化（D-130）。
- 拆票组织＝**前端相关票独立成组（frontend lane），由用户指定的 Agent 与模型执行**（已登记＝Claude Opus，model:'opus'）：REBUILD 换壳面/HeroUI 组件接入/模板起点落地/前台交互面统一归组，非前端票不混入；**分组不破纵切**——ADR-0019 票面三要素不变，前后端同旅程拆分时接缝合同与属主显式写明（三进三出/HTTP+SSE 为接缝语言），旅程收口验收落前端票或专设联调票，不允许前端组内部自闭环不接真后端的假绿；**前端组票面自包含**＝每票附上下文包指引（DESIGN.md 门店橱窗 token 合同＋ADR-0014 交互架构＋D-130 组件基准/镜像路径/模板起点＋对应接缝合同），指定 Agent 无需读全决策日志即可执行且不越界（D-131）。
- 凭证与运营供给单门＝**开票前统一供给清单、票面只引用不索取、fixture 默认档恒绿、live 门一次核销**：外部凭证（模型/渠道/解析/邮件/存储等 API key 与账号开通）与运营供给项（套餐三桶与加油包数字、试用开关、兑换码规则、示例店素材、行业先验配置、对外名称等）不散落各票各自设门——统一登记入 `docs/ops/provisioning-manifest.md`，用户一次性补齐后票面按清单键引用；开发与 CI 默认档一律 fixture/种子值走绿，缺凭证不阻塞开发、不制造假红；真凭证到位后按既有 live_verified 门径一次核销（D-069/D-098 先例）；运营数字用种子样例值验收、真值运营期后台手填（D-123 口径）。教训依据＝装配失守（.env 占位 key）与历轮「重复索证→测试卡死」（D-132）。

下图只描述默认的定制创作主链；自由创作从显式 Prompt、参考素材和模型参数直接进入模型原生请求映射，再复用同一任务、结果、资产、用量与交付基础设施：

```mermaid
flowchart LR
  A["经营目标或流量机会"] --> B["Agent 确定宣发任务"]
  B --> C["编译本店事实、IP、素材、行业配方与平台机制"]
  C --> D["交付完整宣发成品包"]
  D --> E["品味选择或就地纠偏"]
  E --> F["多平台版本、宣传物料、发布或导出"]
  F --> G["关联咨询、预约、买券、核销、到店信号"]
  G --> H["沉淀本店偏好、栏目和可再创作资产"]
  H --> B

  C -. "仅命中特定受限素材时" .-> R["事实、权利或专业条件门"]
  R -. "通过或换安全方案" .-> D
```



## Problem Statement

现有方案不断强化 Provider、任务、版本、审计、授权、恢复和多角色兜底，却没有先回答门店每天为什么做内容。结果是工程对象投影到前台，用户面对表格、窗口、角色交接和参数，而不是一个会主动完成宣发任务的 Agent。

美业门店需要的不是“完成一条内容生产流水线”，而是：

- 日常持续展示项目、服务、环境、产品与专业能力；
- 把平台热点、同城话题、节日季节和流行结构与本店真实信息结合，获得流量；
- 长期经营品牌 IP、老板/主理人 IP 和专业人员 IP；
- 为团购、上新、限时活动和本地到店制作转化内容；
- 高频制作封面、海报、项目卡、服务流程卡、价格/优惠卡、预约引导卡与私域物料；
- 发布后低负担关联内容与咨询、预约、买券、核销或到店等观察信号，并据此提出下一轮可验证建议。

本项目的既有竞品研究和试点手册本来就支持这条主线。跑偏来自对 Phorest/Fresha 局部素材模式的过度外推，不是行业证据不足。

## Demand Evidence Already in the Workspace

| 本地证据 | 已经说明的用户结果 |
| --- | --- |
| `references/creatok/reports/creatok-productization-architecture-gap-analysis.md` | 项目套图、案例卡、流程卡、价格/优惠卡、预约引导卡；场景包包括案例种草、团购优惠、专业科普、顾客口碑、同城引流 |
| `references/creatok/reports/creatok-function-breakdown.md` | 核心转化是咨询、加微、预约、团购券、到店；趋势/灵感、参考结构、本店上下文和差异化改写是可复用产品模式 |
| `references/analysis/15-pilot-playbook.md` | 试点设计把验证链路设为选题、素材、平台、发布和私信/预约/买券/核销/到店记录；真实执行仍待开展 |
| `references/benchmark/ui-adaptation-study-2026-07-08/` | 示例起步、业务目的分类、Auto 默认、最少素材、成品优先、视觉预设和基于结果再创作（竞品常称“做同款”）是降门槛共性 |
| `references/analysis/10-graphic-renderer-selection.md` | P0 已把封面、价格卡、长图等稳定营销物料作为产品输出 |
| `references/analysis/vozeb-borrowing-report-2026-07-15.md` | 热门结构只作内部参考，发布保持原创或授权；优秀生成结果可晋升为本店资产 |

这些证据足以修正产品框架，但不能替代真实门店频次、付费价值和转化关联研究。

## Target User and Complete Launch Shape

首要用户仍是生活美容、美发、美甲美睫、皮肤管理等单店的实际内容操作者。可能是店主、主理人、店长、员工或内容运营，但这些只是现实身份，不映射为首发系统角色。首发账号边界固定为 `一个账号 = 一个个人门店工作区 = 一个实际操作者`，由同一个人完成目标选择、成品判断和发布交接；成员、多人组织、角色切换、多工作区与席位计费均不进入首发。（此处「多工作区」＝租户/协作/席位口径；D-121「内容工作区」为账号内 1:N 的内容容器与套餐维度，属独立概念，不在此否定之列。）医疗美容不计入默认首发范围，只作为独立研究探针；未来仅在商家资质、受监管内容策略、素材权利、平台账号和发布审查门全部通过后按商家条件启用。

定制创作必须完整包含五个宣发入口，共用同一个上下文与资产编译内核；自由创作作为同一创作中心的次入口，不被包装成第六个美业场景：

1. 日常项目/服务曝光。
2. 热点、同城与节点借势。
3. 品牌 IP 与个人 IP 经营。
4. 促销、团购与到店转化。
5. 日常宣传物料。

研发可以内部按依赖顺序完成，但不能把某个只有生成、没有修改、发布/导出、复用或恢复的半产品交给用户。

### 五类入口的首个完整发布范围

五类入口共享一个最小 MarketingPackage（ContentPackage 在宣发语境下的产品称谓，仍是唯一成品聚合，不引入第二聚合；见 ADR-0011）：一个可直接采用的主推荐、所选平台的成品、必要视觉/口播/拍摄建议、明确 CTA、事实与权利状态、快捷修改、保存/导出、异步恢复和基于此再创作。备选方向按需展开，不把选稿变成必经步骤。

自由创作的单项文案、图片或视频仍进入同一个 canonical ContentPackage 结果信封，但可以只包含一个 deliverable，并以“作品/生成结果”呈现；实现需要增加等价的结果类型判别，不在此锁死具体字段名。两条主线之间的转入/转出必须保留来源与 revision 血缘。

| 可见入口 | 首发必须完整交付 | 失败或缺料时的诚实降级 |
| --- | --- | --- |
| 日常项目/服务曝光 | 本店项目主张、证据素材、封面/图文成品、可选口播或拍摄清单、咨询/预约 CTA | 缺顾客案例时改用环境、流程、产品、专业解释或安全模板，不伪造案例 |
| 热点、同城与节点借势 | 有来源和时效的机会卡、本店原创角度、匹配事实/素材、平台成品、机会过期提示 | 热点过期或相关性不足时转成常青搜索/项目内容，不强行蹭热点 |
| 品牌 IP 与个人 IP | 明确表达身份、栏目锚点、身份一致的成品、系列续写入口、下一条建议 | 身份资产不足时先使用品牌官方口吻；个人 IP 未确认时不代言或编造经历 |
| 促销、团购与到店转化 | 已核验价格/权益/期限、价格或优惠卡、平台成品、预约/买券/到店 CTA | 无权威价格或承接能力时不写具体优惠，改成项目介绍或补充事实任务 |
| 日常宣传物料 | 选定用途的成套模板、明确尺寸、可替换文字/素材、确定性导出和复用入口 | 真实素材不足时使用品牌安全占位或文字版，不把未渲染模板伪装成成品 |

当前锁定的公开内容变体是小红书、抖音和微信视频号。按 D-027 与 D-105，抖音与微信视频号变体的「平台成品」包含由具备所需多模态能力的上游模型原生生成并持久化的视频成片；通过能力门验收后才按「已验证」放行，不再以内建 FFmpeg 创意拼装补齐。按 D-028，口播稿、视频脚本、拍摄清单属于文案层一等交付物：用户从文案意图进入时它们本身就是完整交付；仅当用户要成片而成片能力未过验收时，「辅助完成」标签作用于成片能力本身，此时交付脚本+拍摄清单供商家自拍自剪，不将文案交付表述为降级。点评/美团的项目、团购和履约信息可以作为权威经营输入，朋友圈与线下物料可以作为导出成品；在账号级能力、规则和发布链路未验证前，不宣称对点评/美团或朋友圈自动发布。任何可见入口只有满足上表完整合同后才对用户开放，未完成入口先隐藏。

## Confirmed Premises

1. 内容是经营手段，广告曝光与到店引流才是产品目标。
2. 宣发任务和流量机会比店内岗位更适合作为稳定产品入口。
3. 个性化不是一份“人设表”，而是门店事实、品牌/IP、素材、历史选择和当前经营信号的动态组合。
4. 行业资产不是提示词市场，而是场景、平台、Hook、物料、CTA、合规和缺料回退配方。
5. 六维编排只存在于后台，前台不让用户填写六张表。
6. 人只处理主观品味、临时纠偏、异常事实、资产晋升和不可逆动作。
7. 顾客授权只在当前成品使用特定顾客素材时触发，不是每条内容的起点。
8. 内容与线索可以做相关性记录，首期不伪造严格因果归因，不承诺播放量、涨粉、GMV 或到店量。

## Approaches Considered

### Approach A: 业务场景货架

以五类宣发任务和一批场景模板作为首页。用户主动选择项目种草、团购海报、热点借势或 IP 栏目，再由系统自动填充本店资产。

- Effort: M
- Risk: Low
- Pros: 最快摆脱模型工具市场；入口清楚；容易验证场景频率。
- Cons: 仍然是用户找功能；不会主动发现今天值得发什么；个性化复利较弱。

### Approach B: 主动宣发 Agent，选择

首页由“今天值得发什么”的机会流和一句话 Composer 组成。Agent 结合当前经营目标、可验证信号、平台机会和本店资产，主动交付少量完整成品；场景货架作为显式起点和兜底。（2026-07-24 D-126 修订：v1 Dashboard 首页热态＝一天一个轻推荐、非机会流，点击预填充 Composer、成品在「生成」处产出并消耗额度；机会流为热点能力门验证后的激活态。）

- Effort: L
- Risk: Medium
- Pros: 最符合 AI 原生；能把个性化资产、行业资产和时机组合起来；既支持主动建议，也支持用户直接下任务。
- Cons: 机会质量、事实时效与热点相关性必须可解释；错误主动建议比普通模板更伤信任。

### Approach C: 通用聊天/画布加多人审批

用自由聊天、参数面板或画布覆盖所有任务，再配置角色、审批和工作流。

- Effort: XL
- Risk: High
- Pros: 表面上灵活，适合少量专业操作者。
- Cons: 把复杂度留给门店；无法形成美业任务入口；容易继续围绕工程对象打转；已被本轮产品纠偏否决。

Recommendation: 选择 Approach B，同时用 Approach A 的任务货架做可见兜底。它既保留清楚入口，又让 Agent 真正承担机会发现、上下文编译和成品交付。

## Product Shape

### 首页只做三件事

1. **今天值得发什么**：先给一个有本店依据、可直接采用的主推荐，说明“为什么适合现在、用了本店什么、希望顾客做什么”；用户需要时再展开不超过两个明显不同的备选。（2026-07-24 D-126 修订：主推荐＝轻推荐卡，点击＝预填充 Composer 非自动执行，成品在「生成」处产出并消耗额度，首页不自动生成完整成品预览。）
2. **一句话下任务**：例如“把新团购做一套能发的”“用小林的口吻讲这个热点”“把这条做成我们店版本”。公开可访问或已授权的链接可以直接解析；否则使用截图/OCR、粘贴文本、选择既有素材或授权导入，解析失败不阻塞任务。
3. **五类任务入口**：项目/服务曝光、跟热点、做 IP、活动/团购、宣传物料。入口是示例，不是要求用户先选工作流。

不在首页展示模型、Provider、节点、工作流、角色泳道、ContextBundle 字段或完整门店表格。

五类入口的交互形态是 Composer 的场景上下文切换（参照小云雀场景模式：点击后推荐词与工具集合整体切换，不做页面跳转，也不弹字段表单——结构化输入按 D-031 融入对话流）。

**Day-0 零资产状态（D-029）**：首屏与 Day-N 是同一工作界面，没有独立引导页或前置建档向导。冷态下 Composer 与五类入口照常可用；示例橱窗展示明确标注的示例门店成品（支持基于此再创作与反向学习，示例内容与用户事实账本物理隔离，绝不混入 store_facts；从示例店发起的示例任务＝真实执行、扣试用额度，见 D-126）；「今天值得发什么」区域显示诚实冷态（示例推荐或建店邀请），随事实账本积累点亮为真主推荐，不做伪个性化。资产进入以流内轨为默认：第一次任务缺什么才要什么，用户给的主页/价目表/照片在任务流中提取为事实与素材候选，此后只有增量补足；集中建档另有资产页向导式录入容器（D-119 双轨同链、同范式同 schema，两轨均非前置门槛）。

### 热点机会卡最小合同

> 注（2026-07-24，D-126）：本节为热点能力经「热点与同城机会」能力门验证升级后的**激活态**契约。v1 Dashboard 首页热态＝确定性规则＋行业先验的一天一个轻推荐，不接实时热点抓取；热点在 v1 仅作为用户提供的输入或辅助能力，不是首页核心构成。

热点机会不能只是一句“这个正火”。每张可见机会卡至少包含：

- 来源与来源类型，以及采集时间和过期时间；
- 适用平台、地域/同城范围和目标受众；
- 为什么与本店相关，以及匹配了哪些项目、活动、素材或 IP；
- 可复用的是流量机制、问题或内容结构，不是待复制的受保护表达；
- 预期承接动作，以及机会失效或本店相关性不足时的常青内容降级方案。

只有仍在有效期、来源可追溯且能解释本店相关性的机会进入首页。来源不可验证、已经过期或只能靠复制原内容成立的机会不展示。

### 最少输入

用户可从任一入口开始：

- 说一句经营目标；
- 选择 Agent 推荐机会；
- 粘贴公开可访问或已授权的平台热点、参考内容、团购或门店主页；
- 选择一个项目/活动或上传最少素材；
- 对历史成品点击“基于此再创作”。

链接不可访问、需要登录或解析失败时，自动切换到截图/OCR、粘贴文本、手动选取或授权导入，不把国内平台链接识别写成既成能力。已有事实自动读取。关键事实缺失、冲突、过期或用途升级时，一次只问一个阻塞问题。非关键素材缺口先使用安全回退交付成品，再提示可选补料。

### 成品优先

第一次结果不是一段聊天文本，也不要求先做选稿。Agent 先给一个最推荐、可直接采用的完整宣发方向；只有用户主动查看或主观方向确有明显分歧时，才展开备选（不超过两个；专业解释型、体验种草型、活动转化型只是方向示例，不是固定三选）。每个方向至少说明：

- 为什么发；
- 谁在表达；
- 使用了哪些本店事实和素材；
- 适合哪些平台；
- 希望顾客做什么；
- 当前事实、权利与合规状态；
- 可以直接得到哪些图文、口播、套图或宣传物料。

### 快捷修改

用户在成品上直接执行：

- 更像主理人本人 / 换专业人员身份；
- 不要太促销 / 加强优惠和到店动作；
- 换成本店另一组素材；
- 改成小红书、抖音或微信视频号；将点评/美团事实用于改写，或导出朋友圈/线下版本；
- 做成海报、项目套图、口播或预约引导卡；
- 保存为系列 / 基于此再创作，再换一个项目。

所有普通修改默认只影响当前任务。只有用户明确“以后都这样”或确认资产晋升时，才进入长期偏好、栏目或模板。

### 承接与结果产品面

每个 ContentPackage 或活动包都绑定内容/活动 ID、选定 CTA 和承接终点。发布或导出后，用户可以补记发布地址与状态；已验证平台能力可以自动回流，未验证时只提供低负担人工记录。

前台用一排可选 chips 记录私信、加微、预约、买券、核销和到店，数量、时间或备注均可选，不要求维护 CRM 表格。结果按来源分三级：

1. **已验证信号**：来自经过验证的平台、交易或预约接口。
2. **门店记录信号**：由经营者点击或补记的真实观察。
3. **推断相关性**：只说明时间和内容上的关联，永不写成因果事实。

复盘使用一条清楚的结果阶梯：已发布 → 获得注意 → 发生咨询 → 预约/买券 → 核销/到店。周复盘只回答“发了什么、观察到什么、下一轮验证什么”，并允许一键续做、换 CTA 或停止某个系列，不用虚假的自动归因驱动决策。

## Context and Asset Orchestration

后台使用六维宣发坐标：

| 维度 | 核心问题 |
| --- | --- |
| 宣发任务 | 为什么做这次内容 |
| 流量机会 | 为什么是现在、为什么是这个角度 |
| 表达身份 | 谁在说、凭什么可信 |
| 平台机制 | 在哪里被看见、需要什么容器和结构 |
| 门店事实/素材 | 什么让内容属于这家店且可证明 |
| 转化动作 | 看完希望顾客做什么 |

编译关系：

```text
个性化资产
  门店 + 服务/产品 + 价格/团购 + 品牌/IP + 真实素材 + 历史采用 + 账号/承接

加

行业资产
  美业场景 + 品类知识 + 平台结构 + 热点借势 + IP 栏目 + 物料模板 + CTA + 合规规则

加

本次机会
  经营目标 + 当前节点 + 可验证平台/经营信号

等于

本店本次可直接使用的宣发成品包
```

优先级固定为：本次明确指令 > 当前有效经营事实 > 已确认门店/IP 资产 > 已确认偏好 > 行业/平台配方 > 模型通用知识。价格、优惠、资质、团购、人员经历和顾客案例不能被低优先级内容覆盖。

团购订单、预约、核销、时段空档和平台表现不能被假定为自动接通。只有账号级验证接口、带来源的导入/截图提取、聚合统计或用户确认信号可以进入当前 ContextBundle；未接通的数据不伪装成实时信号。

## 资产进入、纠错与生命周期

冷启动不要求先建完一套门店档案。用户可以给门店主页、价目表、团购截图、历史发布、品牌手册或手机相册，也可以先说一个任务；Agent 抽取候选事实、项目、素材和表达身份，在成品旁用一句话摘要预览“本次理解了什么”。用户就地改错或替换即可，主流程不出现大表格。

所有会影响成品真实性的资产保留来源、适用门店/身份、生效时间、过期时间和版本。价格、活动、团购、资质和承接方式到期时，只让相关成品或发布批准失效；不要求用户重新填写整套资料。

品牌和个人 IP 是独立资产，不是语气下拉框。个人 IP 至少记录所有者、真实身份与专业边界、允许的平台/场景、表达样本、肖像/声音/历史内容授权、生效期、离店或撤销后的处理；品牌 IP 则记录品牌主张、禁忌、视觉与可复用栏目。员工离开、授权撤销或运营人变化时，停止新的身份生成并使待发布引用失效，历史内容按真实权限进入处置任务。

每个主推荐被动显示少量“为什么用了这些”chips，例如项目、活动、IP、素材、平台与 CTA；用户可一键换项目、换身份、换素材或纠正事实。修改默认只作用于本次，只有明确确认“更新门店资料”“以后都这样”或“保存为系列”才进入长期资产。

## 定制创作 Workflow Orchestration

后台工作流可以很重，但前台只看到白话事件和成品：

1. 解析经营目标和主要宣发任务。
2. 发现并解释可用流量机会。
3. 编译本次最小 ContextBundle。
4. 选择行业场景、平台、热点、CTA 和物料配方。
5. 按相关性、质量、时效、权利和身份选择本店素材。
6. 生成平台中立的核心主张、证据和转化钩子。
7. 生成多平台变体、套图、口播、拍摄清单和宣传物料。
8. 只在命中时执行事实、权利、合规和账号能力门。
9. 展示一个主推荐，备选按需展开，支持就地修改。
10. 发布或导出后关联结果信号，并提议偏好或资产晋升。

所有长任务可离开、恢复、重试和回到精确版本。用户不需要守在工作流页面，也不看到 Job、Trace 或 Provider 路由。

## Human-in-the-loop

| 介入类型 | 触发条件 | 交互 | 默认作用域 |
| --- | --- | --- | --- |
| 品味选择 | 主推荐不合适，或用户主动查看其他合理方向 | 直接采用主推荐，或展开备选后选择/混搭 | 当前版本 |
| 临时纠偏 | 当前成品与本次意图不符 | 就地自然语言修改、diff、undo；普通修改不要求批准 | 当前任务 |
| 事实/权利冲突 | 价格、活动、身份、来源、时效或素材用途缺失/冲突 | 单张确认卡，展示来源和影响 | 对应事实或素材 revision |
| 资产晋升 | 用户要求基于此再创作/续写系列，或稳定模式值得复用 | 预览固定项、变量槽、作用域、来源与权利 | 经确认的最窄资产范围 |
| 外部行动批准 | 公开发布、投放、扣费、账号权限变化 | 绑定精确成品、账号、平台、时间、费用 | 一次性动作 |

上下文摘要可以用一行 chips 被动展示，但不是审批步骤。首发只有当前账号本人，不出现成员责任帽子或多级审核；外部发布责任人只属于一次性交接信息，不升级为成员系统。

## Conditional Rights Gate

只有当前成品使用可识别顾客肖像、声音、评价、聊天记录、before/after、服务记录或其他受限素材时，才计算主体、素材、用途、平台、期限和付费投放范围。

权利不足时按顺序处理：换安全素材、匿名化/裁切、请求补充授权、放弃该表达。一个案例素材不可用不能阻塞整个宣发任务。Phorest/Fresha 只作为这类窄场景的产品行为参考，不再用于证明行业默认岗位链。

## Success Criteria

### 产品结果

- `time_to_evaluable_package`：从一句话、一个机会或一份最少素材到出现可判断完整成品包的时间（按含/不含 `queue_wait` 双口径报告）。
- `time_to_usable_package`：到用户确认可发布/导出的时间（同双口径）。
- `package_completeness_gap`：用户认为成品包中多余项与缺失项的记录。
- `publishable_package_rate`：首轮成品直接采用或小改后可用的比例（由验证手册「直接采用/小改/大改/拒绝」分类计算）。
- `opportunity_adoption_rate`：被展示的热点/节点机会中，用户直接采用或小改采用的比例，并按过期/不相关原因回收。
- `store_asset_match_accuracy`：项目、活动、IP、素材、平台和 CTA 与本店事实匹配正确的比例。
- `multi_format_completion_rate`：一次任务所需平台内容与宣传物料是否成包交付。
- `material_export_success_rate`：使用真实门店素材的必交付物能否按目标尺寸稳定导出。
- `publish_or_handoff_success_rate`：自动发布、手机交接或导出包是否到达正确账号/渠道并记录状态。
- `reuse_rate`：基于此再创作、系列续写和本店模板的复用比例。
- `lead_signal_capture_rate`：已发布内容能否低负担关联咨询、预约、买券、核销或到店信号。
- `merchant_signal_entry_time`：经营者补记一次真实观察信号所需时间与放弃率。
- `published_package_with_observed_signal_rate`：已发布成品在预先声明的观察窗内至少关联到一个注意、咨询、预约/买券或核销/到店信号的比例，按已验证、门店记录、推断相关性分层。
- `observed_attention_to_intent_progression`：同一内容/活动 ID 下，从注意信号进入私信、加微或问价等意向信号的观察分布；只描述共现进阶，不声称内容导致转化。
- `observed_intent_to_store_progression`：同一内容/活动 ID 下，从咨询进入预约/买券，再到核销/到店的观察分布，分别报告来源完整度与未知项，不用缺失数据补零或做因果归因。

### 守门指标

前 7 项与验证手册的硬失败清单逐一对应，阈值固定为 0，预登记后不得修改：

- 跨门店、跨 IP 事实或素材串线次数为 0。
- 关键经营事实无来源使用次数为 0。
- 未授权主体素材进入公开链路次数为 0。
- 冒用个人身份（未授权代言、编造经历或使用未登记表达身份）次数为 0。
- 过期或错误价格/权益泄漏次数为 0。
- 错误版本外发（导出或外发非放行最新版本）次数为 0。
- 未经一次性批准的公开/付费动作次数为 0（应确认的高风险动作确认召回率为 100%）。
- 临时纠偏被错误沉淀为长期偏好的比例为 0（产品自加守门，不在手册 7 项内；检测手段见 D-032 推论二的三层检测）。

这些指标不能替代真实经营效果。产品可以记录内容与线索的关联，不在首期宣称严格因果归因。

## Release Capability Gates

每项依赖外部数据或账号的能力都只有三种前台状态：**已验证**（当前账号/数据源真实跑通）、**辅助完成**（用户给截图、文本或人工交接即可完成）、**不可用**（隐藏能力，不展示假按钮）。“架构上支持”或“有模板代码”不等于已验证。

### “已验证”的统一证据合同

任何能力从“辅助完成/不可用”升级为“已验证”前，必须在版本化验证计划中登记：能力与账号/数据源范围、负责人、样本构成、最低样本量、通过阈值、关键错误零容忍项、证据制品路径、验证时间、有效期和重新验证触发器。不同能力不共用一个拍脑袋数字；如果最低样本量或阈值尚未确定，该能力就是开放放行阻塞项，不能标成“已验证”。

证据包至少包含可复跑用例、脱敏运行记录、真实回执或导出物、失败样本和负责人结论。跨门店串线、无来源关键事实、权利越界、版本错发等关键错误阈值固定为 0；其他成功率和耗时阈值由对应能力计划在测试前锁定，不能看完结果后修改。平台规则/接口、账号权限、模型/模板主版本、数据来源或安全策略变化时立即失效并重新验证；正常有效期也必须逐能力声明，过期自动降回“辅助完成”或“不可用”。

| 能力门 | 发布前必须留下的真实证据 | 未通过时的产品行为 |
| --- | --- | --- |
| 热点与同城机会 | 来源覆盖、采集时间、过期判断、本店相关性解释，以及真实任务中的采用/拒绝原因 | 改用用户提供的热点、节日节点或常青场景；不展示伪实时热榜 |
| 链接摄取 | 在公开可访问和已授权样本上验证解析成功率、字段正确性与登录边界 | 自动切换截图/OCR、粘贴文本、手动选择或授权导入 |
| 结果信号 | 明确每个信号的来源、更新时间、账号范围与人工补记耗时 | 只显示门店可记录的观察，不生成自动转化报表 |
| 宣传物料渲染 | 用真实商家照片、中文长短文本和首发尺寸验证裁切、排版、可编辑替换和确定性导出 | 未通过合同的物料入口隐藏，或交付明确标记的文字/素材包 |
| 发布与交接 | 真实账号的精确版本、目标渠道、结果回执和导出/手机交接降级成功记录 | 只提供可校验的发布包，不宣称自动发布 |
| 生成与恢复 | 记录真实任务的耗时、成本、成功率、取消、重试、离开后恢复和精确版本一致性 | 显示真实异步状态与预计范围；超出预算或能力时停止并给出可恢复结果 |
| 事实、IP 与权利 | 用过期价格、身份撤销、素材用途变化和受监管场景做回放测试 | 换安全事实/素材/品牌身份，或在唯一阻塞点请求确认 |

现有渲染研究只验证过有限模板，且没有完成真实商家素材的完整验收，因此它是候选实现证据，不是“宣传物料已可发布”的证明。每个可见入口必须通过对应能力门和完整包合同后才能上线。

## Research Plan

下一轮不再观察假定的“服务结束到发布”流水线。应连续回放和观察真实宣发任务：

```text
为什么要发 / 为什么现在发
→ 选题和热点从哪里来
→ 使用了哪些门店、项目、IP 与素材
→ 需要哪些平台内容和宣传物料
→ 哪些地方被改、为什么不敢发
→ 如何发布或导出
→ 是否出现咨询、预约、买券、核销、到店信号
→ 什么值得基于此再创作或沉淀为系列
```

进场按已批准的 Wave 1 执行合同（见 D-026）分两波：第一波先覆盖 4 至 7 家有直接关系的首发范围门店，Week 1 末启动转介绍招募第二波，扩展至 8 至 12 家；每家走 2 至 4 周真实内容日历（wave 2 门店各自顺延完整观察窗），分开记录美发、美甲美睫和生活美容/皮肤管理，并覆盖店主自营、员工兼任、连锁集中运营与代运营协作。医疗美容只设独立研究探针，不混入首发频次、采用率或放行结论。任务频率与入口排序可用这段观察校准；产品能否脱离陪跑、资产是否产生复利在同一窗口内延长观察——总研究时长以 wave 1 启动起算、上限 10 周，到期未凑齐样本按实际样本出「证据不足」结论，需要更长周期时另行立项。Week 2 起至少保留一组无前台实时协助的自助对照样本（后台 WOZ 拦截队列照常运转并全量计量）。人数和周期是研究计划，不是行业结论。

优先测试六个原型：

1. “今天值得发什么”。
2. “热点 × 本店”。
3. “一事多用”的完整宣发包。
4. “基于此再创作 / 续写系列”。
5. “给一张价目表或主页就开始”的资产进入与就地纠错。
6. “发布后十秒补记 + 周复盘”的承接与结果产品面。

## Constraints and Non-goals

- 不做通用 AI 工具市场或模型目录首页。
- 不做要求商家配置节点、角色泳道或多级审批的工作流产品。
- 不做成员邀请、多人协作、角色切换、多工作区、席位计费或商家侧 RBAC 配置。
- 不做店务管理或经营管理系统：预约管理、收银、库存、会员/CRM、员工排班、财务报表不进产品范围；经营信号只用于校准宣发（D-030）。
- 不用六维后台模型制造六张前台表格。
- 不把平台热点抓取结果直接复制为公开内容；只借结构与机会，成品保持原创或有授权。
- 不假定团购、预约、核销、账号发布或平台数据已接通；能力按账号和真实验证启用。
- 首发的“广告”不包含未经验证的付费媒体投放；自然/自有宣发之外的投放必须先通过账号、预算、平台与回执能力门。
- 医疗美容不进入默认首发；研究不得被解释为已经通过资质、受监管内容或平台发布放行。
- 不用顾客案例或 before/after 作为所有美业内容的默认素材。
- 不用曝光、互动或线索相关性包装成确定 GMV 或到店因果。
- 不为了产品重构引入第二套 Agent、工作流或记忆真相源。
- 不把本地 FFmpeg 拼接、字幕/BGM/水印烧录或弱剪辑器作为视频创作能力；复杂后期交给专业外部编辑工具（D-105）。

## Implementation Boundary

后续工程的价值集中在 ContextBundle 编译、资产匹配、平台/物料配方、事实与权利来源、可恢复任务、版本审计和反馈学习。现有 AI SDK、PostgreSQL、pg-boss 与 ContentPackage 继续作为底座。（编排层 durable 载体按 D-034：DBOS Transact 进 PoC 定案制，pg-boss 收窄为存量队列，共存不硬迁。）旧设计只继承四项白名单约束：ContentPackage 是唯一用户成品事实且 Product Core 是唯一物理写入入口；长任务可持久恢复；来源、事实、权利、合规/AIGC 和账号能力按版本追溯；真实外部动作绑定精确成品、账号、平台、时间与费用并一次性确认。旧视频/画布/节点/角色 UI 和工作流形态不继承。

前端只需要机会卡（v1 按 D-126＝一天一个确定性主推荐卡，非实时热点机会流）、任务入口、Composer、一个主推荐与按需备选、资产就地纠错、快捷修改、条件确认、异步收件箱、发布/导出、结果回流/周复盘和基于此再创作入口。任何新增工程对象必须说明它如何减少用户输入、提高成品可用率、完成到店承接或保护一次真实外部动作，否则不能进入主流程。

## Open Questions

1. 五类入口的真实周频次、排序与付费价值。
2. “今天值得发什么”应多主动，以及怎样解释机会来源才不打扰。
3. 热点与本店的相关性门槛、过期速度和原创改写边界。
4. 品牌、主理人和专业人员 IP 在不同品类与平台中的稳定表达资产。
5. 五类入口的首发完整包在真实任务中哪些部分被采用、删减或要求补充。
6. 哪些转化信号可合法、低负担回流，哪些只能人工备注。
7. 顾客案例类内容的真实占比，以及授权和安全替代的完成率。

## The Assignment

拿 8 至 12 家候选门店最近 30 天的实际发布、活动、热点跟进和宣传物料做任务回放，先按五类宣发任务标注“为什么发、用了什么、谁在表达、发到哪里、希望用户做什么、后来发生什么”。用真实任务分布校准首页入口、行业配方和最小成品包，不再用岗位假设补全行业事实。

## What I noticed about how you think

- 你把产品目标说得很清楚：“广告 + 到店引流”。这直接否定了把内容生产当最终价值的工程视角。
- 你列出的“日常项目/服务曝光、平台热点 + 自身信息、品牌 IP / 个人 IP、日常宣传物料”不是零散功能，而是完整的宣发任务体系。
- 你反复强调“工程要藏在后端，前端交互要简单优雅”。这意味着上下文、工作流和资产模型只有在减少输入、先给成品和支持快捷纠偏时才有产品价值。
- 你指出错误链路是“纯想当然”，因此本文把所有组织、频次和角色判断重新降为待验证，把已存在的本地产品证据放回主位。

# 第二部分：决策日志（原《AI 原生经营上下文与资产编排决策日志》全文并入）

- 日期：2026-07-17
- 状态：Working
- 作用：持续记录产品方向中的关键决定、依据、影响和待验证项，作为后续设计稿与复盘的恢复入口。
- 条目默认日期：2026-07-17
- 条目默认状态：`accepted_design_default`，表示已进入设计基线，不等于已承诺生产实现
- 当前产品设计基线：本文第一部分
- 执行权威：`/Users/bin/.gstack/projects/legacy-origin-a-legacy-web-repo/bin-main-design-20260717-183658.md`（Wave 1 验证与执行合同，已批准；入账见 D-026）
- 前一版工程设计快照：[`raw/design-baseline-bin-main-design-20260717-130101.md`](../../references/analysis/hitl-research-2026-07-17/raw/design-baseline-bin-main-design-20260717-130101.md)
- 本机 gstack 源路径：`/Users/bin/.gstack/projects/legacy-origin-a-legacy-web-repo/bin-main-design-20260717-162033.md`（产品设计）、`bin-main-design-20260717-183658.md`（执行合同）
- 相关调研：[`references/analysis/hitl-research-2026-07-17/`](../../references/analysis/hitl-research-2026-07-17/)、[`references/analysis/beauty-marketing-validation-2026-07-17/`](../../references/analysis/beauty-marketing-validation-2026-07-17/)

## 记录规则

只记录会改变产品结构、用户旅程、上下文边界、工作流或验收方式的决定。D-001 至 D-019 使用紧凑格式，日期和状态继承文件头；未单列的证据边界与待验证项统一收敛到文末“当前待验证”，`Supersedes` 未写表示无。D-020 起使用文末完整模板。讨论过程、实现细节和临时想法不写入决定正文。

## D-001 产品不是生成工具，而是门店经营内容 Agent

- 决定：产品主叙事从“选择能力并生成内容”改为“Agent 理解本店经营状态，主动建议并完成值得做的内容”。
- 原因：竞品已经普遍具备示例、场景、自动参数、异步任务和“做同款”类能力；本产品的差异必须来自持续理解门店事实、真实素材、经营目标和历史反馈。
- 影响：正文优先描述本周经营循环和用户结果；模型、Provider、任务状态和对象结构退到后台与附录。
- 证据边界：竞品共性已有本地实探；真实门店持续使用仍未验证。

## D-002 结构化内核保留，但不再一比一投影为前端表单

- 决定：门店、服务、价格、活动、团购、品牌、IP、素材权利、平台和合规仍使用结构化、版本化事实；前端默认只展示意图、匹配摘要、必要确认和成品。
- 原因：价格、授权、资质和发布事实必须可追溯，但要求用户逐字段维护会把 Agent 退化成表单后的执行器。
- 影响：完整表格只保留在二级管理或纠错入口；主流程采用 Agent 自动提取、匹配、轻确认和可逆修改。

## D-003 个性化资产升级为“经营上下文”

- 决定：Agent 的经营上下文聚合稳定身份、当前经营事实、表达身份、真实证据资产、已确认内容经验和即时信号；它是 ContextBundle 的编译输入视图，不是新的存储或真相源。AI memory 仅负责偏好观察、候选抽取和召回。
- 原因：静态门店档案不足以决定“现在为什么发、用什么素材、由谁表达、希望顾客做什么”。
- 影响：用户上传主页、价目表、团购截图、历史帖子或新素材后，系统提取事实候选、偏好候选和素材候选并进入各自的处理通道；有效权威事实直接版本化，只有冲突或低可信事实才确认，偏好与资产仍需明确晋升。
- 证据边界：团购订单、预约和核销的自动同步尚未完成真实账号验证，近期只能作为有来源、有时效的导入或人工确认信号。

## D-004 行业模板升级为“场景策略配方”

- 决定：行业资产不只是视觉模板或提示词，而是包含触发条件、适用品类、经营目标、素材槽、推荐表达身份、平台结构、CTA、合规边界、缺料回退和质量标准的版本化配方。
- 原因：同一“团购促销”在美甲、美发、生活美容和医美中的素材、表达身份、风险和转化动作不同。
- 影响：行业配方只提供结构和默认值，不能覆盖本店价格、授权、资质或身份事实。
- 证据边界：首批场景是候选集，真实频次和品类差异仍需门店任务观察验证。

## D-005 每次任务先编译不可变 ContextBundle

- 决定：每次生成前，由后台上下文规划器把本次意图、有效门店事实、服务或 Offer、表达身份、行业配方、授权素材、平台规则、历史经验、即时信号和未决问题编译为不可变上下文快照。
- 原因：避免把整个数据库或聊天历史直接塞给模型，并保证内容可以解释、复盘和追溯。
- 影响：ContentPackage 记录 ContextBundle 引用、关键决策和来源；后续修改产生 derived-from 与 delta，不篡改历史快照。

## D-006 Agent 的自主、确认和禁止猜测边界

- 决定：
  - Agent 自动决定可逆事项：场景候选、素材组合、Hook、平台格式、技术参数、候选排序和低风险改写。
  - 有效且来源权威的价格、优惠、有效期、套餐内容、表达身份、顾客授权和资质由系统自动读取；只有缺失、冲突、过期、撤权或用途升级时，才由对应责任人确认。
  - 实际扣费、公开发布、投放和外部发送始终使用绑定精确版本的一次性批准。
  - Agent 永不猜测：顾客评价、订单归因、库存或名额、医疗效果、官方认证、授权状态和“必然合规”。
  - 修订（2026-07-24，D-117/D-122）：上列「永不猜测」不再作为生成期硬拦截清单。生成期零内容限制、允许演绎（D-117）；收口分派＝忠实性（商家已提供的信息不得生成错）由 D-115 校验门承接、医疗效果红线由 D-025 硬停、素材/身份权利与授权状态由 D-022 与 D-112 权利门承接、外部发布由 D-112 发布确认门收口；演绎性内容（含场景与体验叙事）不属硬门，AI 生成软提示随成品交付。下方「事实缺失插确认卡」触发仅对忠实性关键事实保留。
- 原因：AI 原生不等于无边界自动化；价值来自少问、问对和不重复问。
- 影响：主流程只在事实缺失、冲突、过期、撤权或用途升级时插入一张事实确认卡；外部副作用则在最后可逆时刻插入一次性批准卡。

## D-007 工作流负责重执行，前端只呈现用户事件

- 决定：后台使用可恢复工作流执行“理解意图、规划上下文、检索匹配、规则过滤、必要确认、生成评估、成品交付、发布交接和学习”；前端只接收白话阶段、确认请求、候选和结果。
- 原因：Job、重试、幂等、Provider 路由、账本和回滚是工程价值，但不是商家任务语言。
- 影响：禁止把 Trace、状态机、模型目录或 ContextBundle 字段直接暴露到主流程。

## D-008 决策与学习必须可回溯、可纠正

- 决定：产品记录本次为何选择项目、Offer、IP、场景和素材，以及用户的采用、修改、拒绝、发布和复用行为；长期偏好必须可查看、纠正、撤回。
- 原因：持续个性化需要学习，但一次性指令不能静默升级为永久事实。
- 影响：区分本次指令、软偏好和硬事实；用户明确“以后都这样”并确认作用域后才形成长期偏好，重复行为达到阈值时只生成待确认候选。

## D-009 Human-in-the-loop 是选择性控制系统，不是审批链

- 决定：HITL 按“判断是否必须由人完成”切入，而不是按工作流页面逐步确认。控制梯度为：可逆选择直接执行，临时纠偏就地修改，稳定重复后提议沉淀，事实冲突才阻塞确认，外部副作用前单次授权。
- 原因：逐步确认会把 Agent 退化成向导；完全不确认又会混淆品味、事实和授权，导致失控感。
- 影响：同一时刻最多出现一个需要用户处理的节点（受 D-032 推论一限定：作用域按任务；异步收件箱聚合多任务待办、呈现层一次置顶一个）；非阻塞建议允许忽略，后台继续推进。

## D-010 人的输入按语义分流，并采用最窄默认作用域

| 决策类型 | 典型表达 | 默认作用域 | 是否自动沉淀 | 交互方式 |
| --- | --- | --- | --- | --- |
| 主观喜好选择 | “喜欢 B 的开头”“这张更好看” | 当前版本 | 只记录弱信号，不改变默认值 | 同屏 2–3 个可比较候选，推荐项优先（受 D-023 限定：默认先给一个主推荐，比较仅在展开备选或主观分歧时出现） |
| 临时纠偏 | “这条别写价格”“这次换小林口吻” | 当前任务或派生版本 | 否 | 就地修改，明确“仅本次”，支持差异与撤销 |
| 事实纠错 | “199 已下架，现在是 239”“小林不是老板娘” | 对应 Store、Offer、Persona、Rights 或 Qualification 的新版本 | 只能经事实确认写入 | 展示旧值、新值、来源、生效期及影响范围 |
| 长期偏好 | “以后小红书都少一点促销感” | 默认限定到门店 × IP × 场景 × 平台中的最窄适用组合 | 重复行为只生成候选，确认后生效 | 修改完成后再给作用域 chips，不在修改前阻塞 |
| 可复用资产 | “以后团购都按这个三段结构做” | 门店资产库中的版本化配方 | 只生成候选，预览确认后入库 | 展示固定部分、变量槽、适用场景、来源和权利 |
| 不可逆批准 | 发布、投放、外部发送、明确扣费 | 精确动作的一次性授权 | 永不学习、永不复用 | Just-in-time 展示目标、版本、账号、时间、费用和风险 |

- 决定：默认作用域遵循“当前版本 < 当前任务 < 本次活动 < 场景 × IP × 平台 < 门店默认”。系统总是先选最窄作用域，扩大范围必须由用户明确表达或确认。
- 原因：“改一下”通常只是在救当前成品，不代表商家建立了一条长期品牌规则。
- 影响：禁止用一个模糊的“记住我的选择”复选框代替作用域；禁止把价格、授权、资质或身份纠错归类为审美偏好。

## D-011 临时修改默认不学习，长期记忆采用“观察—提议—确认”

- 决定：一次选择或修改只产生 `DecisionEvent` 和当前版本 delta；系统可以用它调整本次候选，但不能静默改写长期默认。
- 决定：用户明确说“以后、默认、都这样”时可立即生成 `PreferenceCandidate`；同一语义在至少 3 个独立任务中重复出现时，也只提议记住，不自动生效。阈值是首版待验证默认，不是永久业务规则。
- 决定：正式偏好必须保存适用范围、正反例、证据事件、确认人、确认时间、可撤销状态和替代关系；软偏好随时间衰减，事实不按行为衰减，只能过期、撤权或产生新版本。
- 原因：稳定个性化来自跨任务一致性，而不是把偶然修改过拟合成永久规则。
- 影响：记忆提议采用非阻塞卡片，例如：“你最近 3 次都弱化了强促销词，要设为‘小林 IP × 小红书团购’默认吗？”操作只保留“记住”“仅这次”“调整范围”。

## D-012 事实纠错与不可逆授权必须独立于偏好学习

- 决定：价格、套餐、活动有效期、订单或预约信号、人物身份、素材权利和资质属于有来源、有时效的经营事实；发现冲突时更新事实版本并重新编译 ContextBundle，不能从内容修改中反推。
- 决定：公开发布、定时发布、投放、外部发送、高成本生成和用途升级使用一次性 `ApprovalReceipt`。授权绑定精确的内容版本、账号、平台、时间、费用和用途；任一关键字段变化，旧授权立即失效。
- 原因：事实确认在回答“什么是真的”，批准在回答“是否允许执行这一次动作”，两者都不是“用户喜欢什么”。
- 影响：已发布内容不被历史回写；事实纠错默认影响未来内容，可提示是否同步更新尚未发布的草稿。用户也不能通过确认绕过法律、平台或证据硬红线，系统只能提供安全替代方案。

## D-013 Human-in-the-loop 的前端切入节点固定为七类

1. **上下文摘要**：事实无冲突时只展示一行“本次将使用”的可点击 chips，不要求确认。
2. **流内事实确认**：仅在缺失、冲突、过期、撤权或用途升级时插入一张轻确认卡，一次只问一个阻塞问题。
3. **候选比较**（受 D-023 限定，仅在用户展开备选或主观方向确有分歧时出现，默认先给一个主推荐）：在 Hook、结构、画面和口吻等主观判断处给 2–3 个差异清楚的成品候选，允许混搭，不再二次确认“是否采用”。
4. **就地纠偏**：通过自然语言、动态快捷动作、直接编辑完成，默认标注“仅这版”，提供 diff、undo 和恢复 AI 版。
5. **记忆提议**：在用户已得到结果之后非阻塞出现，说明观察证据和推荐作用域；每次修改后不得追问“是否记住”。
6. **资产化预览**：明确要求做成同款，或结构、素材组合被稳定复用后，预览固定结构、变量槽、场景、来源、权利和示例，再保存为版本化资产。
7. **外部行动批准**：只在真正产生公开或付费副作用前显示流内审阅面板，按钮直接描述动作，例如“确认发布到抖音账号 A”。

- 影响：主流程组件只使用上下文 chips、流内确认卡、候选比较、就地编辑、作用域 chips 和非阻塞提议卡；资产化与发布审阅可以进入侧边抽屉。普通采用、修改和偏好操作不使用模态框，完成后提供持久可找回的撤销入口。

## D-014 可复用资产沉淀的是稳定结构，不是整篇成品

- 决定：可资产化对象包括场景变体、表达卡、内容结构、视觉布局、素材槽组合、Hook/CTA 模块和复用配方；临时价格、日期、顾客信息、订单数据和单次活动信号必须抽成变量或剔除。
- 决定：出现明确“保存成同款/模板”的意图，或跨任务重复采用、发布后再次复用时，系统生成 `ReusableAssetCandidate`；用户在预览固定项、变量槽、适用范围、来源与权利后确认，才形成不可变 `AssetRevision`。
- 原因：整篇保存会把过期经营事实和顾客信息带入未来任务；只保存抽象 prompt 又失去行业素材槽、IP 和合规边界。
- 影响：“少一点广告感”通常是软偏好，不单独包装成模板；“老板娘日常分享的三图团购结构”才是可复用配方。后续基于此再创作时由新 ContextBundle 填充当前事实和素材，而不是复制旧文案。

## D-015 后台使用决策账本连接上下文、记忆和授权

- 决定：最小领域对象拆分为 `DecisionEvent`、`PreferenceCandidate/Preference`、`ReusableAssetCandidate/AssetRevision` 和 `ApprovalReceipt`，禁止用一个笼统的“用户反馈”字段混装。
- 决定：每条记录至少包含决策类型、触发位置、输入与前后差异、默认及最终作用域、证据事件、ContextBundle 与 ContentPackage 版本、操作者、时间、可逆性、替代关系和受影响对象。
- 原因：后台必须能回答“这次为什么这样做、为什么认为这是偏好、谁把它提升为长期规则、哪个批准允许了哪一次行动”。
- 影响：旧 ContextBundle 和 DecisionTrace 永不原地改写；纠正、撤销和规则升级都追加新事件或新版本。前端只显示白话解释和影响范围，不暴露对象模型。

## D-016 现阶段不引入第二套 Agent、工作流或记忆运行时

- 决定：继续以现有 AI SDK、PostgreSQL、pg-boss、ContentPackage 版本体系和 Operations 任务为主底座（受 D-034 限定：五段式新工作流的 durable 载体主选 DBOS Transact、PoC 定案制，pg-boss 收窄为存量队列）；把视频的 pause/select/resume、抖音的 snapshot invalidation 和飞书的 immutable intent confirmation 上收为通用 HITL 领域能力。
- 决定：Temporal、Inngest、Trigger.dev、Cloudflare Workflows、LangGraph、Mastra、OpenAI Agents SDK、Letta 和 Zep 不进入当前主链路。外部组件只能在明确替换对象、唯一真相源和回退方案后做隔离 spike。
- 原因：现有工程已经具备结构化提案、持久任务、版本、人工等待、幂等与高风险确认的可复用基础；当前首要缺口是统一的决策语义，已知实现断点还包括 creation assistant 决定未写回服务端，以及人工等待与 `recoverable` 故障语义混用。它们适合在现有边界内闭环，而不是为此引入第三个运行时。通用框架仍需自建版本绑定、作用域、权利、批准和对账，并会制造双写。
- 影响：近期工程重点是 `DecisionEvent / PreferenceCandidate / AssetRevision / ApprovalReceipt / ContextBundle` 领域边界和现有断点闭环，不是框架迁移。建造顺序服从 D-026 执行合同：48 小时薄壳范围先行，D-015 完整决策账本按回放 `intervention_log` 证据分期落地；本条与 D-015 定义领域边界，不构成立即全量建造的排期承诺。
- 证据边界：该判断基于 2026-07-17 当前代码和依赖快照；如果跨服务补偿、跨月等待、工作流升级或 Core 运行时迁移成为被测瓶颈，需要重新评估 Temporal 或 Cloudflare Workflows。

## D-017 经营事实、人类决定和 AI 记忆使用三条独立真相链

- 决定：Store、Offer、Rights、Qualification、订单、预约和价格等经营事实只允许权威接口或经权限校验的人写入版本化领域表；`DecisionEvent` 记录人的选择、纠正、撤回、偏好晋升和批准；AI memory 只负责观察、召回、排序和生成 `PreferenceCandidate`。
- 决定：语义相似度只能在合法作用域内排序；不能决定事实有效性、偏好作用域或授权。检索不能自动强化偏好，重复行为不能自动升级为事实。
- 原因：通用记忆组件会压缩语义，也可能把推测写成断言；偏好、事实和批准的有效期、来源及责任完全不同。
- 影响：当前状态保存在普通领域表，关键决定与来源保存 append-only ledger。Undo 追加补偿事件，supersede 链接新旧版本，不全量重写 Event Sourcing。
- 证据边界：LangMem、Mem0、Graphiti 等只可做候选抽取或检索 spike；采用前必须通过本项目的 false-memory、跨门店泄漏和未知关键事实 abstention 测试。

## D-018（已废止）曾将特定案例素材协作错误外推为美业默认工作流

- 状态：`superseded`（错误外推；由 D-020、D-021 和 D-022 取代）
- 原决定：默认工作流为“技师现场采集 → 顾客逐次授权 → Agent 异步生产 → 技师校验专业事实 → 店长校验经营事实并策展 → 账号责任人一次性发布”，并围绕该流程建模责任帽子、逐次授权和逐层校验。
- 废止原因：Phorest 的预约分享权限、Fresha 的作品上传与策展仅能证明特定产品能力和权利边界，不能推导出中国美业门店的通用内容生产组织链。原决定把条件性的“顾客案例素材”支线误升为产品主链，偏离了美业内容的广告曝光与到店引流目标。
- 历史中仍有效的边界：涉及可识别顾客的公开素材仍需可追溯授权；个人职人 IP、门店 IP、品牌资产、个人账号和商家账号仍需记录所有者、运营人、授权来源、可解绑性和离职处置。这些是条件门和权属能力，不是默认岗位流水线。
- 影响：禁止后续设计继续以该人员链作为首页信息架构、任务编排、组件设计或验收主线。

## D-019 前端继续使用领域化工作台，只补 HITL 业务组件

- 决定：继续复用 AI SDK 7 与现有 Base UI/shadcn、Sonner、ToggleGroup、Drawer，不整体接入 assistant-ui、CopilotKit 或 ChatKit。assistant-ui 和 AG-UI 只作为 Tool UI、interrupt 和协议模式参考。
- 决定：首批领域组件固定为 `ContextSummaryChips`、`SubjectiveChoiceCard`、`InlineCorrectionBar`、`FactConflictCard`、`MemoryProposalCard`、`AssetPromotionCard`、`IrreversibleApprovalCard`、`NeedsDecisionTask`、`UndoAction` 和 `MobilePublishHandoff`。
- 原因：现有创建界面已是“文档时间线 + 领域卡片”，且候选比较、版本、任务流插槽和移动端结构都存在。完整 Agent UI 框架会引入第二套消息、线程和状态，却仍不能提供本项目的事实、作用域、资产化和权利语义。
- 影响：前端只提交结构化决定，不拥有长期记忆和批准真相；异步恢复以持久工作流、任务事件和 ContentPackage revision 为准，不以聊天流为准。
- 证据边界：如果未来出现独立跨页面 Agent 对话中心、第二种 Agent runtime 或第二客户端，再做 assistant-ui/AG-UI 隔离评估。

## D-020 美业产品主链以广告曝光与到店引流为中心

- 日期：2026-07-17
- 状态：`accepted_design_default`
- 决定：产品接受“当前经营目标”或“当前流量机会”两类起点，主链为“经营目标/流量机会 → 确定宣发任务 → Agent 编译门店、项目/服务、活动/团购、品牌/IP、真实素材与平台约束 → 交付可直接使用的多平台成品包 → 快捷改稿或基于此再创作 → 发布/导出 → 回收咨询、加微、预约、团购券、核销和到店等观察信号”。首批宣发任务以日常项目/服务展示曝光、平台热点结合本店信息的借势曝光、品牌 IP/个人 IP 持续表达、促销团购和日常宣传物料制作为核心。“广告”在首发范围内指广义门店宣发，不默认包含付费媒体投放。
- 原因：美业内容不以完成一条店内岗位交接为价值，而以持续获得目标顾客曝光、建立品牌或专业人设、承接促销与流量机会，并推动线下到店为核心经营结果。
- 影响：首页、创建入口、主动建议、异步任务和验收指标都围绕“今天值得做什么宣发、现在有什么流量机会、成品如何承接到店动作”组织；岗位、任务队列、ContextBundle 和 Provider 实现只是后台能力。
- 证据边界：宣发任务的真实频次、不同品类的流量机会差异，以及内容对到店的贡献归因仍需门店任务日志和平台/经营数据验证。
- 待验证：用户是否更常从经营目标、热点机会、可用素材还是预期成品类型起步；哪类主动建议真正促成发布与到店转化。
- Supersedes：D-018 中的默认岗位工作流。

## D-021 行业资产按宣发决策维度编排，不按岗位流水线组织

- 日期：2026-07-17
- 状态：`accepted_design_default`
- 决定：行业资产的编排坐标固定为“宣发任务 × 流量机会 × 表达身份 × 平台机制 × 门店事实/素材 × 转化动作”。这六维是后台检索、匹配、编译和评估的结构，不是要求用户逐项填写的六张表。Agent 应从已有经营上下文、当前信号和用户一句话意图中自动补齐，前端仅呈现场景入口、匹配理由、必要缺口和成品。
- 原因：同一个项目既可以用于日常曝光、热点借势、IP 栏目或限时促销，也会因平台、表达身份、现有素材和 CTA 而产生完全不同的成品。岗位流转无法表达这种组合关系。
- 影响：场景策略配方、素材标注、平台适配、IP 表达卡、CTA 与效果回流都通过这六维索引关联；前端交互仍遵循“示例起步、最少素材、参数自动、成品优先、快捷修改、基于此再创作”。
- 证据边界：六维是当前设计编排模型，不代表每一维都需要产生独立实体，也不代表已穷尽各美业品类的实际内容打法。
- 待验证：六维是否足以解释高频宣发任务和再创作复用；哪些维度可以可靠自动推断，哪些必须在缺失时向用户追问。
- Supersedes：D-018 的责任帽子主链组织方式。

## D-022 顾客授权和专业核验是绑定素材或事实的条件门

- 日期：2026-07-17
- 状态：`accepted_design_default`
- 决定：人工介入挂在“当前成品真正引用的素材、事实或外部动作”上，不挂在预设人员顺序上。只有内容引用可识别顾客、顾客评价、前后对比等受限素材时，才启动对应用途和平台范围的权利校验或授权；只有内容包含未有权威来源覆盖的专业操作、禁忌、效果或其他高风险专业事实时，才请有能力的责任人核验。已有有效授权和权威事实时自动通过；缺失、冲突、过期、撤权或用途升级时才阻塞。
- 原因：权利和专业事实必须可追溯，但它们并不存在于每一个宣发任务中。将条件门设计为逐层人工审批，会为日常项目曝光、热点借势、IP 表达和宣传物料制造无意义的摩擦。
- 影响：`MediaConsentGate` 与专业事实核验保留为按需触发的后台能力；其触发条件、来源、用途、有效期和失效行为进入 ContextBundle 与 DecisionTrace。默认界面不显示顾客或技师交接步骤，只在条件门被触发时呈现一张必要确认卡。
- 证据边界：何种素材、表达和平台用途需要何种授权，以及何种专业主张需要资质或人工复核，必须在上线前完成本地法律、品类和平台规则审查。
- 待验证：顾客案例类内容在真实任务中的占比、现有授权获取方式、撤权流程，以及专业事实核验的必要触发集合。
- Supersedes：D-018 中的逐次授权与技师逐层核验默认流程。

## D-023 可见入口必须交付完整成品包，默认先给一个主推荐

- 日期：2026-07-17
- 状态：`accepted_design_default`
- 决定：对外保留五类宣发入口：日常项目/服务曝光、热点/同城/节点借势、品牌/个人 IP、促销/团购/到店转化、日常宣传物料。每个可见入口必须完成主推荐成品、目标平台或用途变体、必要物料、CTA、事实/权利状态、快捷修改、发布/导出、异步恢复和基于此再创作；未达到合同的入口先隐藏。第一次结果默认给一个可直接采用的主推荐，备选只在用户主动查看或主观方向确有分歧时展开。
- 原因：强制三选一仍把模型的不确定性转嫁给用户；只交一段文案或一个未渲染模板则继续制造半产品。门店需要的是一个敢用的完整结果和最短纠偏路径。
- 影响：此前 D4 的固定“3 选 1”候选政策被本决定取代。当前公开内容变体锁定小红书、抖音和微信视频号；点评/美团先作为经营事实输入，朋友圈和线下物料作为导出用途，未验证账号能力前不宣称自动发布。
- 证据边界：五类入口的真实频次和各自最小必交付物仍需真实任务验证；完整合同可按证据调整，但不能用公开半产品代替。
- 待验证：主推荐直接采用率、备选展开率、各入口成品包删减/补充项和真实素材导出成功率。
- Supersedes：前一版工程设计会话（`bin-main-design-20260717-130101.md`）的固定「3 选 1」候选政策（该会话的问题编号 D4，非本日志 D-004）。本决定同时限定 D-010 表首行与 D-013 第 3 条：候选比较仅在展开备选或主观分歧时出现。不改变 ContentPackage 单一采用事实。

## D-024 外部能力与经营结果必须按证据级别诚实呈现

- 日期：2026-07-17
- 状态：`accepted_design_default`
- 决定：热点、链接解析、发布、订单/预约/核销、结果回流和付费投放等外部能力按“已验证、辅助完成、不可用”三态呈现。结果信号区分已验证接口、门店人工记录和推断相关性；只记录“发布后观察到或关联到什么”，不将时间关联写成内容导致咨询、预约、买券、核销或到店。
- 原因：未验证能力如果以正常按钮、实时数据或自动归因出现，会直接破坏商家对经营事实和 Agent 建议的信任。
- 影响：链接不可访问时回退到截图/OCR、粘贴文本或授权导入；热点必须带来源、采集/过期时间和本店相关性；发布后提供低负担信号 chips 与周复盘。付费投放只有账号、预算、平台和回执真实验证后才出现一次性动作。
- 证据边界：相关性记录用于形成下一轮验证建议，不证明内容的因果增量或平台算法效果。
- 待验证：各能力的真实成功率、辅助完成耗时、结果信号来源完整度和经营者补记负担。
- Supersedes：任何把链接抓取、平台回流、付费投放或严格归因写成当前默认能力的旧表述。

## D-025 医疗美容不计入默认首发，只作为独立研究探针

- 日期：2026-07-17
- 状态：`accepted_design_default`
- 决定：默认首发面向美发、美甲美睫、生活美容和皮肤管理等本地门店。医疗美容样本可以用于识别受监管差异，但不混入首发任务频次、采用率、成功指标或能力放行结论；未来只有商家资质、受监管内容策略、素材权利、平台账号和发布审查门全部通过后，才按商家条件启用。
- 原因：医疗美容的主体资质、功效表达、案例素材和广告审查边界显著不同。把它混进普通美业首发会让产品要么过度限制日常生美，要么低估医疗内容风险。
- 影响：五类宣发入口的默认合同先由非医疗门店验证；医疗美容保持独立策略、证据包和 release gate，不能用“已做行业研究”代替准入。
- 证据边界：本决定只定义产品范围，不构成法律意见；具体准入条件需本地法律、品类和平台规则复核。
- 待验证：是否存在值得单独产品化的医疗美容楔子，以及其资质、内容、案例、发布和结果回流的最小安全合同。
- Supersedes：任何把医疗美容样本默认计入普通美业首发或通用放行的旧表述。

## D-026 Wave 1 验证执行合同：薄壳 + WOZ 拦截操作台 + 两波进场

- 日期：2026-07-17
- 状态：`accepted`（已批准执行；来源：`bin-main-design-20260717-183658.md`，三轮评审后 9/10 定稿。本条为入账记录，细节以该文档与 Week 0 预登记文档为准）
- 决定：C 级门店任务回放采用「48 小时薄壳 + WOZ 拦截操作台」：商家看到的前台交互必须与目标产品一致，WOZ 只藏在后台。薄壳范围 = creation-assistant 采纳/编辑写回 ContentPackage、`awaiting_quality_review` 改普通等待态、单店事实账本（store_facts + assets）、预登记口径事件日志、WOZ 拦截队列、两层硬失败门（自动阻断 + 人工强制清单）、chips 信号补记、基于此再创作、导出交付。
- 决定：进场分两波：第一波 4–7 家直接关系门店；Week 1 末启动转介绍招募，扩至 8–12 家；wave 2 门店各自顺延完整 2–4 周观察窗；跨店共性结论在 8–12 家凑齐且 wave 2 完成至少 2 周回放后判定；总研究时长以 wave 1 启动起算、上限 10 周，到期按实际样本出「证据不足」结论。
- 决定：硬规则：Week 0 基线访谈零工程依赖；薄壳未就绪只顺延、永不降级为微信人工交付；「≥80% 冷读检验点」为暂定值，Week 0 预登记文档锁定全部阈值、分子/分母定义与统计口径后不得修改；7 项硬失败阈值为 0（跨店串线、无来源关键事实、未授权素材、冒用个人身份、错误价格/权益、错误版本外发、未经一次性批准的公开/付费动作）；研究期间不执行任何平台写操作。
- 原因：这是唯一同时满足「前台一致」「工程冻结线」「立即进店」三个约束的方案；量化指标名（`time_to_evaluable_package` / `time_to_usable_package` / `package_completeness_gap` / `queue_wait` 双口径）与验证手册逐一对齐。
- 影响：本文所有量化指标命名、硬失败清单、进场节奏以本执行合同与预登记口径为准；D-016 的工程建造顺序服从薄壳先行；第一部分 Research Plan 已按本条改写。
- 证据边界：执行合同本身已批准；样本量与阈值数字在 Week 0 预登记时最终锁定。
- 待验证：见验证手册与「当前待验证」清单。
- Supersedes：第一部分 Research Plan 原「直接 8–12 家起步、复利观察 8–12 周」的单波口径。

## D-027 视频成片进入首发合同，接通与验收为薄壳后第一工程优先级

- 日期：2026-07-17
- 状态：`accepted`（2026-07-24 实现路径由 D-105 修订；“视频成片是交付物”继续有效）
- 决定：视频成片是抖音与微信视频号变体的首发必交付物，不是「可选口播或拍摄清单」可替代的项。目标管线为模型目录与薄 Provider Adapter 调用具备所需多模态能力的上游模型，取得 provider-native `VideoArtifact` 后及时持久化、验收并进入 ContentPackage；现有 `ffmpeg composer` 与 `model.composed-video` 只属于待退役代码现状，不再是目标创作链。视频能力仍按预登记证据合同补办六题验收（单片成本、端到端时延、可用质量率、开关行为、来源记录），列为 48 小时薄壳之后的第一工程优先级，目标在 Wave 1 首场回放前可用。
- 决定：对外放行仍走能力门三态：验收证据通过前，成片能力不得标「已验证」，「辅助完成」标签只作用于「成片自动生成」这一能力；此时视频平台变体交付口播稿/视频脚本+拍摄清单（按 D-028 属文案层一等交付物，供商家自拍自剪，不构成文案交付的降级）。Wave 1 回放期间 WOZ 使用真管线出成片；视频任务频次、采用率与成片质量信号进入 Week 0 预登记。
- 原因：三个锁定公开平台中两个（抖音、视频号）为视频优先，无成片则平台变体合同不成立；现有 Provider 抽象、耐久任务和真实探针可以复用，但历史 FFmpeg 投入不能成为继续保留本地创意合成的产品理由。
- 影响：本条在 D-016/D-026 的建造顺序上为视频开一个显式例外：视频接通与验收补办不等待 `intervention_log` 证据，位列薄壳后第一位（D-034 后与 DBOS PoC spike 并列第一优先批次）；其余 Stage 2 项仍按 `intervention_log` 排序。第一部分五类入口表的「平台成品」定义按本条更新。ADR-0008 中动态目录、耐久任务、来源和真实能力门继续有效；首帧→片段→FFmpeg 合成、水印/字幕/BGM 创意烧录等路径由 D-105 取代。
- 证据边界：六题验收数字尚未按预登记口径落盘；「模型未接通」为 2026-07-17 观察运行（`docs/evidence/contentpackage/ticket-06-08/observed-run-20260717/`）的状态快照。
- 待验证：单片成本、端到端时延、可用质量率是否达到预登记阈值；视频任务在真实门店回放中的频次与采用率。
- Supersedes：本文此前「成片地位待拍板」条目；ADR-0008 中被合并版权威取代的范围坐标（详见其 2026-07-17 修订节）。

## D-028 文案与成片是两层独立交付物，组合由入口意图决定

- 日期：2026-07-17
- 状态：`accepted`
- 决定：文案生成（LLM 完成）与视频成片（具备所需能力的视频模型原生生成并持久化）是两层独立交付物。文案层交付物包括朋友圈文案、图文笔记文案、口播稿、视频脚本、拍摄清单等，各自独立存在、独立验收，是全值交付，不是任何能力的降级形态。
- 决定：是否组合由用户的入口意图决定：从文案意图进入（如「写一条朋友圈文案」「出一份口播稿」）时，交付合同就是该文案本身，不强制附带成片；从成品包或视频意图进入时，成片合同生效，视频脚本/分镜作为成片管线的输入组成部分。
- 决定：能力门三态标签只作用于能力，不作用于交付物类别：「成片自动生成」未验收时标「辅助完成」并交付脚本+拍摄清单；文案生成能力按其自身证据独立标注。验收指标分账：文案类任务不进成片指标（六题验收、`multi_format_completion_rate` 的视频分量）的分母，反之亦然。
- 原因：口播稿/视频脚本由 LLM 完成、成片由视频模型完成，二者的成本、时延、质量门与失败模式完全不同；混装会把文案交付误标为降级、污染成片指标分母，而用户经常只需要文案（发朋友圈、自己拍摄）。
- 影响：MarketingPackage 内容物按任务意图裁剪；第一部分五类入口表中「口播或拍摄清单」均按本条理解为文案层交付物；D-027 的降级表述已按本条修正；ADR-0008 的 2026-07-17 修订节同步修正。
- 证据边界：文案与成片的组合率、各文案类型的真实需求频次仍需 Wave 1 回放验证。
- 待验证：从文案意图直接进入的任务占比；视频脚本被用于成片管线与商家自拍的比例。
- Supersedes：D-027 初稿中「口播+拍摄清单为成片诚实降级」的表述。

## D-029 Day-0 首屏与 Day-N 同界面：示例冷态 + 流内资产积累

- 日期：2026-07-17
- 状态：`accepted`
- 决定：产品没有独立引导页或前置建档向导。Day-0 首屏 = Day-N 的同一工作界面：中心 Composer（一句话任务 + 全模态上传）+ 五类场景入口（Composer 上下文切换，非页面跳转）+ 明确标注的示例橱窗（预置示例门店成品，支持基于此再创作与查看创作过程式反向学习；只读、不计量、可隐藏）。
- 决定：「今天值得发什么」区域冷态诚实呈现（示例推荐或建店邀请），随事实账本积累点亮为真主推荐；任何情况下不做未使用本店事实的伪个性化推荐。
- 决定：资产录入是偶发行为，不占首屏主位：第一次任务缺什么才要什么（D-006），用户提供的主页/价目表/相册在任务流中提取为事实与素材候选（D-003），此后仅增量补足；零资产不阻塞首次生成，缺料走五类入口表的诚实降级。示例门店内容与用户事实账本物理隔离（跨店串线 = 0 的边界情形）。
- 原因：内容生产是 Web 端高频核心，资产登记是一次录入 + 增量补足的偶发行为，不配拥有首屏主位；小云雀登录态实探证据（首页即工作界面、场景 = Composer 上下文切换、示例即引导、资产流内积累、参数全自动）验证了成熟同类产品的同构做法。
- 影响：第一部分「首页只做三件事」已补 Day-0 状态定义与场景模式句；前台骨架方向定为原型 B（Composer 主轴），原型 A（机会流）是同一屏在事实账本点亮后的热态；薄壳前台按此实现。
- 证据边界：`references/benchmark/interaction-study-2026-07-07/interaction-patterns-xyq-creatok.md` 与其 `sources/xyq-2026-07-17-live.md`（登录态实探）；示例橱窗具体内容与冷态文案未定稿。
- 待验证：冷态到首个真主推荐的转化路径；示例“基于此再创作”对首任务完成率的影响。
- 修订（2026-07-24，D-126）：示例橱窗「不计量」限于展示/浏览/预填充；从示例店发起的「基于此再创作」＝示例任务，走真实生成链路并扣试用额度，不建免费演示模式第二链路。
- Supersedes：本文此前「Day-0 零资产首屏待拍板」条目。

## D-030 定位边界：行业特征 × 个人 IP 资产的高效宣发工具平台，不是店务或经营管理系统

- 日期：2026-07-17
- 状态：`accepted`
- 决定：产品定位 = 以美业行业特征（场景配方、平台机制、品类知识）为基础，结合个人 IP 资产，快捷高效满足门店日常宣传物料与日常宣发需求的工具平台。行业资产与品牌/个人 IP 资产是个性化复利的两大支柱，门店事实是真实性底座。
- 决定：本文语境中「经营」仅指宣发的目标与信号来源（为什么发、发了之后观察到什么），不扩张为管理功能面。店务管理与经营管理范畴——预约管理、收银、库存、会员/CRM、员工排班、财务报表等——一律不进产品范围；结果信号 chips 与周复盘只用于校准宣发，不构成管理台账。
- 决定：新增功能的准入检验：必须回答「是否让一次宣发任务更快、更好、更像本店」；答不上的不进主流程（与第一部分 Implementation Boundary 的工程对象准入检验同构）。
- 原因：用户拍板（2026-07-17）：核心不是门店店务管理或美业日常经营管理；「经营循环」语言若不设边界，会自然长出管理后台功能，稀释高频宣发主轴。
- 影响：PRODUCT.md 定位与 Anti-references 同步补充；第一部分 Constraints 增加不做店务/经营管理条目；后续功能评审以本条为准入门。2026-07-17 全仓扫描结论：无硬冲突；历史 p0-spec 的「手工线索台账」（状态流水线 + 金额 + 联系方式）以本文「承接与结果产品面」口径为准（chips + 三级来源、不要求 CRM 表格），台账中金额/联系方式类字段在 Week 0 预登记时做隐私与必要性复核；历史 P2 条目（多门店管理、代运营工作台）启动前须按本条准入检验复核。
- 证据边界：本条不否定结果信号阶梯（D-024）与资产沉淀（D-014）——它们服务宣发校准，属工具平台范围。
- 待验证：无（定位声明）。
- Supersedes：无（澄清并约束既有「宣发经营循环」表述的语义范围）。

## D-031 前台无槽位填表：结构化输入融入对话流，以生成式 UI 承载

- 日期：2026-07-17
- 状态：`accepted`
- 决定：主流程禁止传统 SaaS 槽位填表——不做字段表单 modal、结构化启动向导、多字段必填页。场景入口点击后只切换 Composer 上下文（推荐词与工具集合），不弹表单；小云雀的「领域字段表单 modal」模式明确不抄这一层。
- 决定：原本需要结构化输入的内容全部融入 Chat box 对话流，以生成式 UI 元素承载：上下文 chips（本次将使用）、单问阻塞确认卡（一次只问一个）、就地纠偏条、候选卡、作用域 chips、资产化预览卡——即 D-019 已定的领域组件集。交互协议参照 AG-UI / assistant-ui 的 Tool UI 与 interrupt 模式，维持 D-019「只参考模式、不整体接入框架」的边界。
- 决定：字段与槽位概念仅存在于后台（配方素材槽、资产变量槽、事实账本字段、ContextBundle）。前台呈现的是 Agent 已自动补齐的结果加可点改的 chips，不是等用户填的空位；缺关键事实时在对话流内单问，非关键缺口用安全回退先交付。
- 决定：「一键代理」快捷按钮是本范式的标准微交互——把完整的后台 agent 能力收口为单个按钮或 chip，用户极简操作、agent 藏后端（参照 CreatOK「优化提示词」按钮与小云雀「创意助手」开关）。本产品对应物：Composer 意图增强（一句话自动补全为完整 brief，可预览可改）、快捷修改 chips、基于此再创作、平台一键转换。
- 原因：用户拍板（2026-07-17）：槽位填表不符合简易便捷的客群需求与 AI 原生趋势。本条是 D-002（结构化内核不投影为表单）与 D-021（六维不投影为六张表）在交互层的收口：任何结构化输入都不得以表单形态出现。
- 影响：小云雀范式提炼第 3 条在本产品的对应物为「对话内生成式 UI」而非字段表单；D-013 七类 HITL 节点的呈现容器全部为对话流内元素（资产化与发布审阅可进侧边抽屉，仍非表单页）；薄壳 WOZ 前台同样不做表单；D-010 表中各「交互方式」按本条口径实现。
- 证据边界：AG-UI/assistant-ui 模式的适配成本见 hitl 调研；「优化提示词/创意助手」一键代理按钮证据见 `references/benchmark/interaction-study-2026-07-07/interaction-patterns-xyq-creatok.md`；生成式 UI 元素的具体形态在薄壳与原型中迭代。
- 待验证：对话流内单问与 chips 纠偏能否覆盖全部结构化输入场景；何种缺口用户更愿意用上传而非对话补齐。
- Supersedes：无（收紧 D-002/D-013/D-019 的呈现口径）。

## D-032 Agent Workflow 编排总纲：三层架构、确定性主干、三进三出前后端合同

- 日期：2026-07-17
- 状态：`accepted`
- 决定：后台编排采用三层架构。**对话层**（Composer + 对话流，AI SDK token 流式）承接意图、即时文案与生成式 UI 交互，是用户感知到的「Agent」；**编排层**：每个宣发任务 = 一条独立可恢复工作流——编译不可变 ContextBundle → 配方/素材匹配 → 生成（文案层/媒体层，按 D-028 分层）→ 条件门（命中才停）→ 交付主推荐 → 纠偏循环 → 一次性批准外发 → 学习旁路（只写候选）；**能力层**（LLM/图/视频模型 API、必要的资产校验/交付归一化与发布适配器），可替换、不含业务真相。FFmpeg 创意拼装不再属于能力层目标合同（D-105）。
- 决定：编排层为**确定性主干 + 智能节点**：步骤顺序固定、可审计、可回放，AI 智能封装在节点内（意图理解、匹配、生成、评估都是 LLM 完成）。不采用自由 agent 循环（流程不可回放，「为什么用了这些」与硬失败 = 0 无法承诺），不采用图编排框架（D-016 已否：第二真相源 + 双写）。等待（人工确认、慢模型）为持久挂起态，不占资源，答复即续，恢复带版本围栏（事实已变则重新编译）。
- 决定：前后端合同为**三进三出**：后台给前台只有——白话进度事件、需要用户的一个问题（生成式 UI 卡）、成品版本（ContentPackage revision）；前台给后台只有——意图（一句话 + 场景上下文 + 素材）、结构化决定（进决策账本）、信号补记（chips）。对话流是投影，真相在 ContentPackage 与三条账本（D-017）；前台形态迭代不改后台合同。
- 决定：技术栈确认不换底座（AI SDK + PostgreSQL + pg-boss + ContentPackage，D-016）（编排层载体落实见 D-034：DBOS 进 PoC 定案制，pg-boss 收窄为存量队列）；本轮纠偏改变的是建造优先级：①token 流式会话层升为一等公民；②D-019 生成式 UI 组件集优先级提前；③视频管线接通与验收为薄壳后第一优先（D-027；D-034 将 DBOS PoC spike 与之并列同批）。
- 推论一（阻塞作用域，收编原待拍板第 1 项）：每任务独立挂起/恢复 ⇒ 阻塞节点作用域**按任务**——每任务最多一个阻塞节点；异步收件箱聚合各任务待办，呈现层一次置顶一个当前待办。全局互斥锁与运行模型相悖，不做。D-009 的「同一时刻最多一个待处理节点」按本条口径理解。
- 推论二（学习旁路检测，收编原待拍板第 2 项）：学习旁路只写候选、生效必过确认卡 ⇒ 「临时纠偏错误沉淀 = 0」按三层检测：**结构阻断**（未确认候选在架构上无法生效）+ **自动评测**（`BeautyPreferenceMemoryEval` 领域评测台：false promotion、事实污染、abstention、supersede 后旧值不复现等回归；评测台通过是 Stage 2 偏好学习能力上线的放行门）+ **运行审计**（晋升链路全留痕、偏好视图可查可撤、`false_persistence_rate` 与短期撤回率进周复盘；Wave 1 期间 WOZ 抽查晋升链路，未经确认的晋升记硬失败）。
- 原因：由产品体验合同的五条硬性质自上而下推导——即时对话性、长任务耐久性、任务独立性、可审计性、学习不越权；产品定架构、架构定栈，不从技术细节反推。hitl 调研佐证：被调研运行时（Cloudflare/assistant-ui/OpenAI Agents/AG-UI/LangGraph/Temporal/Inngest/飞书/抖音）的等待模型全部为 per-run/per-instance，「一次一个」仅指单张卡内一个问题；CreatOK 零 agent 框架跑通同类产品是确定性主干的同行佐证。
- 影响：待拍板清单清空；D-009 加受限标注；守门指标交叉引用改指本条推论二；薄壳与 Stage 2 建设按三个优先级调整执行。
- 证据边界：`references/analysis/hitl-research-2026-07-17/`（等待模型、评测台、晋升链路、abstention 证据）；三层架构的模块切分在工程 spec 细化，本条只锁形态与合同。
- 待验证：三进三出合同能否覆盖全部前台交互场景；编排层步骤粒度是否需按品类微调。
- Supersedes：无（收敛 D-005/D-007/D-009/D-011/D-013/D-015/D-016/D-017 为统一编排总纲，不改变其各自决定）。

## D-033 Task 统一交互单元与编排层 Harness 五段式

- 日期：2026-07-17
- 状态：`accepted`（2026-07-17 用户确认）
- 决定：客户的每一次交互定义为一个 **Task**：起点 = 客户需求（模糊输入），终点 = 匹配的执行 API 返回结果回到平台。Task 驱动一条 D-032 编排层工作流实例；其全部产物（文字/图文/视频/物料/修改）打包为集合体 = ContentPackage（不新增第二聚合，ADR-0011 不变）；修改类交互 = 派生 Task，产出 derived revision。
- 决定：编排层 Harness 固定五段：①**意图正名**（LLM 节点：模糊一句话 → 结构化任务声明——五类任务类型 + 交付物层 D-028 + 隐含约束；缺口按 D-031 对话流单问）②**上下文注入**（确定性节点：六维编译 D-021 从三池取料——行业资产池/个人与门店资产池/本次信号池 → 不可变 ContextBundle D-005，优先级链既定）③**Brief 编译**（LLM 节点，核心增值点：ContextBundle → 每执行单元的完整 brief——文案创作指令/图像 prompt+参考图+参数/视频分镜+首帧；把用户写不出的「200 字专业 brief」自动化）④**执行与择优**（混合：模型目录自动路由、N→1 候选评估（ad_video_gen 评估抽卡范式；2026-07-24 受 D-113 限定＝④段默认交付 1 主候选、不做默认抽卡，N→1 仅为质量门失败的有界重试与可选评分参考）、七项硬失败门自动拦截——字段引账本/素材出授权库/身份出登记资产）⑤**回装与交付**（产物组装回 ContentPackage revision，主推荐 + 按需备选 D-023，chips 可解释）。
- 决定：Harness 质量机制四件：事实 grounding（结构化字段只引账本）、双层评估（执行单元质量评分 + 成品包合同校验，五类入口最小合同即校验器）、全程可解释（每段选择依据落 DecisionTrace）、信号回流（采用/修改/拒绝进学习旁路，只写候选）。
- 决定：Harness 建造顺序由 Wave 1 证据驱动：WOZ 拦截操作台即「人肉 Harness」——五段中任何一段不足都由 WOZ 人工补位并被 `intervention_log` 按分钟计量；哪段先自动化、自动化到什么程度按回放数据排序（与 D-026 Stage 2 原则一致），不做纸上完美设计。
- 原因：用户拍板 Task 抽象（2026-07-17）；五段式是 D-005/D-021/D-023/D-028/D-031/D-032 在编排层内部的解剖与命名，不引入新对象；ad_video_gen 三件套（状态黑板/人话进度/评估抽卡）与「用户手写 200 字 brief = 产品要自动生成的目标物」调研结论为 harness 范式证据。
- 影响：工程 spec 的编排层模块按五段切分；意图正名与 Brief 编译为 LLM 节点，上下文注入与硬失败门为确定性节点，执行择优为混合节点；WOZ 操作台的拦截分类按五段打标，使 `intervention_log` 直接映射到 harness 建造项。
- 证据边界：`references/benchmark/ai-native-journey-study-2026-07-08/`（ad_video_gen 拆解）、`references/benchmark/interaction-study-2026-07-07/interaction-patterns-xyq-creatok.md` §3（brief 缺口）、`references/analysis/hitl-research-2026-07-17/`。
- 待验证：意图正名对五类任务 + 交付物的判准率；六维中哪些可靠自动推断、哪些必须单问；Brief 编译质量对 `publishable_package_rate` 的贡献度；N→1 择优的成本收益比（按 D-113 已收窄为默认单主候选，此项降级为可选评分范式的成本收益）。
- Supersedes：无（D-032 编排层的内部细化）。

## D-034 编排层 durable 载体：DBOS 进 PoC 定案制，pg-boss 共存过渡

- 日期：2026-07-17
- 状态：`accepted`（2026-07-17 用户拍板采纳 11 号简报主推荐；载体最终锁定绑 PoC 通过）
- 决定：五段式 Harness（D-033）的 durable 执行载体主选 **DBOS Transact（TypeScript）**：纯 npm 库（MIT）嵌入单 Node 服务，workflow/step checkpoint、挂起等待（`recv`/`setEvent`）、进度流（`writeStream`/`readStream`）、队列全落现有 PostgreSQL，不新增常驻服务；`DBOSClient` 供 Workers 壳无 executor 即 enqueue/读流/审批。
- 决定：**定案形态 = PoC 门**：2-3 天 PoC 六题全过才最终锁定——①五段最小骨架（含 ContextBundle 冻结与⑤段 OCC 条件写）；②kill -9 崩溃恢复断点续跑、不重放已完成 step；③挂起 48h 等审批 + 恢复 + 版本隔离行为；④`writeStream`→SSE→前端进度通路；⑤与现有 Drizzle/pg 连接池及 migration 体系共存；⑥bundler 禁打包/launch 前注册等部署链约束的实际代价。任一硬伤转 **Inngest**（强备选）评估，前置 = SSPL 自用边界法务确认（不 fork 服务端、不透出 dashboard、不转售编排能力）。
- 决定：**pg-boss 共存不硬迁**：五段式新工作流走 DBOS，存量 pg-boss job 原地不动，稳定后按数据再评估合并（交叉验证已推翻「DBOS Queues = pg-boss 超集可直接替代」）。
- 决定：出局与范式吸收：CF Workflows（无 self-host 无逃逸路径、部署版本语义无官方背书）与 Trigger.dev（平台非库、自托管 8 服务且无 checkpoint）出局主线；照抄 CF Agents SDK「对话壳⇄durable 核、进度更新亦检查点化」双层结构与 Trigger.dev waitpoint 前端直连审批、metadata 进度推送两项范式。
- 原因：四候选深调 + Codex 对抗交叉验证后，DBOS 是唯一「验证期最快（零新增服务）× 平移最便宜（迁中国云≈换 PG 连接串）」两头都占的候选，合 D-032「栈不换」与验证期原则；r01 交叉验证削弱「直接替代 pg-boss/迁移极窄」两断言，故定案绑 PoC 而非直接锁定。
- 影响：编排层实现从「pg-boss 上自建状态机」改为「DBOS workflow + 五段 step」；三进三出合同的白话进度事件由 `writeStream`→SSE 承载；工程序列在薄壳后插入 PoC spike（与视频接通并列第一优先批次）。
- 证据边界：`references/analysis/harness-research-2026-07-17/`01-04 号报告 + `xcheck/`r01-r04 裁定；源码镜像 `references/repos/harness-2026-07-17/`。
- 待验证：PoC 六题；at-least-once 语义下④段视频提交/模型调用的幂等键设计。
- Supersedes：无（D-032 编排层载体的落实；pg-boss 角色由「队列即编排」收窄为「存量队列」）。

## D-035 ①③结构化节点：AI SDK 现行 API 起步，BAML 触发式升级

- 日期：2026-07-17
- 状态：`accepted`（2026-07-17 用户拍板采纳）
- 决定：①意图正名、③Brief 编译两个 LLM 结构化节点以 Vercel AI SDK **现行**结构化输出 API 实现（注意 `generateObject` 已 deprecated，按 ai@7 现行 API 书写，不抄旧文档），各自封装在独立非流式函数边界内；调用外层埋点记录首过 schema 校验率、repair 调用率、retry 触发率、嵌套字段完整率。
- 决定：BAML 为登记在案的**触发式升级路径**而非默认件：WOZ 期建 50-100 条真实输入标注 eval 集（覆盖模糊表达/方言/缺字段），先跑出目标中国模型的基线首过校验率，**用基线数据反推迁移阈值再登记触发点**（不预设 92%/97%——交叉验证判该数字无证据链）；触发时仅迁触发的节点，另一节点留 AI SDK。
- 决定：BAML 引入前置核查 = 其发布物内部 Apache-2.0/MIT 许可冲突（r07 实锤）须上游解决或法务确认可接受。
- 原因：①③封在函数边界后切换成本可控（非「极低」，r07 修正口径），验证期零新增件优先；BAML 的 SAP 宽容解析对弱模型增益机制真实，但 benchmark 为近两年前 BFCL v1 厂商自测，对中国模型实际增益以自建 eval 集实测为准。
- 影响：五段式工程实现①③节点先行落地无阻塞；eval 集建设并入 WOZ 期任务清单。
- 证据边界：`references/analysis/harness-research-2026-07-17/07-baml-vs-aisdk.md` + `xcheck/r07-xcheck.md`。
- 待验证：目标中国模型上的基线首过校验率与阈值标定；BAML 许可冲突上游状态。
- Supersedes：无。

## D-036 评估与审计三件分工：Langfuse + promptfoo + 纯 Vitest

- 日期：2026-07-17
- 状态：`accepted`（2026-07-17 用户拍板采纳）
- 决定：**Langfuse 自托管**承载线上 trace/回放/实验/prompt 版本（全部所需功能在 MIT OSS 免费版；映射 = Task=trace、五段=五个语义 span、`LangfuseGuardrail`=红线门禁、`LangfuseEvaluator`=择优——两原语已解包实锤）；**promptfoo** 承载七条硬红线回归 + red-team 对抗的 CI 阻塞门（policy 插件官方示例即红线模板），并为④段 N→1 择优判分器提供范式；**BeautyPreferenceMemoryEval**（D-032 推论二的放行门）用**纯 Vitest 自写 runner** 跑多轮对话→管线→记忆状态硬等式断言（`false_persistence_rate===0` 等），结果导出 Langfuse datasets 做基线看板与漂移监控（05/06 号报告分歧的融合裁决）。
- 决定：合规边界 = **Langfuse 非 system of record**（数据在 ClickHouse 且受 TTL 影响）：红线门禁与合规留痕**同步双写自建 PG 审计表**，以 traceId 外键关联富回放。运维认账：自托管强制 PG17+ClickHouse+Redis+S3 四件套。
- 附带纠偏：CF AI Gateway 实有 Custom Provider（r04 推翻「中国模型不可接」），验证期可选小 spike 真机试火山接入白捡网关重试/日志，非必需项。
- 原因：三件各任其职不重叠（离线阻塞门/线上观测与数据集/有状态硬等式测试），evalite 出局（单人项目停滞）；Langfuse 被 ClickHouse 收购（2026-01）资金面稳。
- 影响：DecisionTrace 的落库查看方案定型；七红线回归与 BeautyPreferenceMemoryEval 的承载定型；promptfoo/Vitest 进 CI，Langfuse 进部署清单。
- 证据边界：`references/analysis/harness-research-2026-07-17/05-langfuse.md`、`06-eval-harness.md` + `xcheck/`r05/r06 裁定。
- 待验证：火山/豆包经 openai 兼容覆盖接 promptfoo 的真机验证；Langfuse trace 与自建 PG 审计表的双写一致性方案。
- Supersedes：无（D-032 可审计性与推论二的承载落实）。

## D-037 非代码可变层：扩展存量 admin-config 自建薄后台，提示词 Langfuse prompt management 先行

- 日期：2026-07-17
- 状态：`accepted`（2026-07-17 用户拍板采纳，提示词承载用户点名 Langfuse 先行）
- 决定：Harness 骨架固定在代码；常变层（提示词/模型择优参数/④段策略顺序）数据化外置，承载 = **扩展存量 `apps/core/src/p1/admin-config/`**（已有 global/workspace 双作用域、追加式修订、CAS apply/回滚、actor/reason/correlationId 审计、secret 拒绝——r09 交叉验证实锤，不另建配置系统）：新增强类型 Harness artifact + apply 前 candidate→eval→approve→publish 门（发布强制过 promptfoo 门禁）+ 面向运营的结构化表单；React Flow 只读 DAG viewer **缓建**。（2026-07-24 D-101 修订末句：首版仍不引入 React Flow；真实复杂分支触发后可作为受控编辑视图，不再永久限定为只读，但不得成为运行时。）
- 决定：提示词承载 = **Langfuse prompt management 先行**（零新增件、与观测/eval 天然一体、label 发布流 + 版本回滚够用）；发布时钉住不可变 prompt 版本/content hash。
- 决定：**Mastra Port 口径更新**（细化决策 B「推迟 + Port 预留」）：条件打开至「Mastra Agent runtime 与调优/观测集成边界」，绑四项 spike gate（editor db 源迁移/回滚实测、Simple Auth 生产可用性、①③ Agent 化改造成本、版本钉扎与 Langfuse 打通），触发点 = 运营对提示词编辑体验提出超出 Langfuse 的要求；**不为 Editor 提前把编排改跑 Mastra workflow**。
- 决定：可视化平台终裁：**Dify 不当运行时**（Python 双语言、崩溃续跑仅到暂停点、多租户许可禁令）；「子流引擎」路线（TS 主干经版本 pin API 调③④）技术可行但绑双前置（法务确认我方 SaaS 客户不构成 Dify tenant + 接受崩溃丢在途 run），**搁置为战术选项**，触发点 = 运营可视化画布成为硬需求；范式照抄（草稿→发布→版本 pin 三态、GraphRuntimeState 暂停/恢复契约、火山/豆包视频插件接入形态）。Flowise/Langflow 排除进生产后端（Langflow KEV+勒索实证）；n8n 内部运营自动化在免费线内可用（客户可见内嵌按官方四类边界个案判）；Windmill 仅范式参考。
- 原因：用户给定前提（骨架不常变、常变三类、非代码人员后台、维护简易）下，「代码骨架 + 数据化常变层」是唯一同时守住确定性主干（D-032）与运营可改性的路线；存量 admin-config 使成本低于自建估算（2-3 人周的上限）；12-Factor Agents（own-your-prompts/own-your-control-flow）为行业先例佐证。
- 影响：运营后台 = admin-config 扩展 + Langfuse prompt 面板两个入口；WOZ 期提示词迭代直接走 Langfuse 版本化，不等自建后台。
- 证据边界：`references/analysis/harness-research-2026-07-17/08-visual-orchestration-platforms.md`（Dify 段待 r08 复核收尾）、`09-visual-light-and-build-own.md`、`10-mastra-studio.md` + `xcheck/`r09/r10 裁定。
- 待验证：admin-config 扩展的 Harness artifact 方向已由 D-101 锁定为固定五阶段、阶段内受控积木与 Recipe compiler；首批 StageType/config Schema、原子发布和运营可用性仍待验证。Mastra 四项 spike gate 与 Dify 双前置继续按 D-101 的更窄触发条件挂起。
- Supersedes：决策 B 的 Mastra Port 口径（细化非推翻）。

## D-038 Harness 工程统一约束五条

- 日期：2026-07-17
- 状态：`accepted`（2026-07-17 用户拍板采纳）
- 决定：与载体选择解耦、无论 D-034 PoC 结果如何均生效的五条工程约束——①五段 step 内核写成 runtime 无关纯函数，载体只当外壳，换载体不动内核；②step 一律按 at-least-once 设计业务幂等（无任何候选给 exactly-once）；③大产物（视频/图）走对象存储，编排状态与 trace 只存引用；④⑤回装段 revision fencing 一律业务层 OCC 条件写（载体只给版本隔离不防数据覆盖）；⑤发布 SOP：新版本部署须有 in-flight 实例排空/版本粘滞策略，纳入 CI 清单。
- 原因：四份载体报告 + 交叉验证的一致共识；把「换得起载体」写成结构性约束是对 D-034 PoC 定案制的配套保险。
- 影响：五段式工程 spec 的 step 接口签名按纯函数约定书写；对象存储引用与幂等键进入数据模型设计。
- 证据边界：同 D-034。
- 待验证：无（约束性决定）。
- Supersedes：无。

## D-039 合规义务层承载：09 章义务清单并入 Week 0 预登记文档

- 日期：2026-07-18
- 状态：accepted
- 决定：合集-v1.5 09 合规章的 17 条我方法定义务清单（含法规依据与三档生效时点）、滥用拦截 3/5 细则、Gate 0 时点表与分层验收指标表，并入 Wave 1 Week 0 预登记文档（该文档按 D-026 阻塞 Week 1 进场；合规义务作为其组成部分同批落盘，同受「开始回放后不得修改」约束）。迁入时做三处校正：①AIGC 口径按 ADR-0008 2026-07-11 修订节与 p0-spec §8/§9（创作阶段开关 + provenance 如实记录 + 法务后审；红线 = 伪造/绕过强制 provider/platform provenance，而非「关闭可选产品侧开关」）；②Preflight 触发点收窄为发布/公开交付阶段（ADR-0004 2026-07-11 修订 + CONTEXT.md 活边界：发布审查不得回移进普通创作/导出）；③医美条目挂 D-025 前提（不入默认首发、独立研究探针），原「P0 落点」列整列标注为 2026-07-07 历史坐标、按现行架构重标。
- 原因：合集-v1.5 正式退役后合规义务层出现权威真空（p0-spec 已历史化，Wave 1 执行合同与 VALIDATION-PLAYBOOK 零承载）；法定义务不随产品转向失效，且 09 §2 多条义务「验证期即须满足」，与 WOZ 封闭试点直接相关；预登记文档是 Week 1 前必然产出的强约束载体，无需新增独立权威文档。
- 影响：Week 0 预登记文档新增「合规义务」组成部分；预登记落盘前的临时口径 = 合集 line 7 的 2026-07-11 横幅 + 退役横幅第 5 点；合集退役横幅第 5 点「去向待拍板」措辞更新为指向本条。
- 证据边界：真空清单与活载体盘点见 `docs/reviews/doc-consistency-review-2026-07-18.md` §5（2026-07-18 一致性复核 escalate 项，对抗核验员全仓实证：Gate 0 三件套/数据卫生/医美 SOP/AIGC 口径已有活载体，真空集中在清单整体+滥用细则+DPA+未成年人+PIA+投诉举报+用户协议标识条款+分层验收指标表）。
- 待验证：预登记文档中合规验收指标的具体阈值与 09 §9 分层指标表的映射方式。
- Supersedes：合集-v1.5 09 章作为合规义务现行承载的残余地位（其历史拍板记录地位不变）。

## D-040 阶段重排：功能完善的产品开发阶段，合规与运营执行整体置后

- 日期：2026-07-18
- 状态：`accepted`（2026-07-18 用户拍板）
- 决定：当前阶段定性为**功能完善的产品开发阶段**：优先按本文档产品设计部分的全量功能完成开发——对话层（Composer、Day-0 冷态、生成式 UI、异步收件箱）、编排层（五段式 Harness、durable 载体、条件门、纠偏循环、学习旁路候选链）、能力层（模型目录、模型原生视频管线接通、资产持久化与发布/导出适配；本地 FFmpeg 创意拼装按 D-105 退役）、五类入口完整成品合同、资产进入与生命周期、承接与结果产品面，以及配套观测/评估基建（Langfuse、promptfoo、BeautyPreferenceMemoryEval）。
- 决定：合规层与运营层的执行项整体置后，触发点 = 全量功能开发完成、重启验证/试点时：Week 0 预登记文档落盘（含 D-039 合规义务清单迁入）、门店两波进场与回放窗（D-026 运营部分）、WOZ 拦截操作台与 intervention_log 人工运营、eval 集运营采集（与待验证 17 的 2026-07-17 拍板同一口径）。「已验证」证据合同的登记仪式（版本化验证计划/预登记锁定）同样顺延——开发期各能力验收只作工程质量检查与数据留存，不走预登记流程、不冻结验收矩阵。D-026/D-039 内容与约束不变，仅执行时点顺延；「预登记阻塞 Week 1 进场」在运营重启时恢复生效。
- 决定：边界澄清——置后的是流程与文档执行，不是结构性护栏：七项硬失败门、条件权利门（D-022）、provenance 如实记录、审计双写（D-036）、promptfoo 红线 CI 门、能力门三态与证据合同（D-024）属功能正确性组成部分，随功能同批建。D-034 PoC 定案制与 D-038 五条工程约束不受影响。
- 决定：D-033「Harness 建造顺序由 Wave 1 回放数据排序、WOZ 人肉补位」在本阶段暂停适用，五段式按功能全集直接建造；WOZ 操作台随运营层置后（其拦截语义由 HITL 节点与异步收件箱作为产品功能承载）。
- 原因：用户判断（2026-07-18）：此前多处顺序编排把运营与合规排为发布前硬门槛，但产品仍在打磨阶段、功能随时迭代，过早加入运营/合规的测试与投入会随功能迭代反复作废，属于浪费；反而整体排在功能完成之后更好。饱和开发资源先完成完整功能落地；验证与合规流程按生效时点排期，绑定运营重启而非开发期。
- 影响：工程范围以全量功能开发规格承载（docs/specs/ 与 GitHub issue，`ready-for-agent`）；本文档 Research Plan 与 Success Criteria 中依赖门店回放的指标采集随运营重启执行，指标定义不变。
- 证据边界：无新增调研；本条为阶段排期决定。
- 待验证：无（排期性决定）。
- Supersedes：无（D-026/D-039 执行时点顺延；D-033 建造顺序条在本阶段暂停适用，内容不变）。

## D-041 编排层 durable 载体定案：DBOS Transact 锁定（PoC 七面全过）

- 日期：2026-07-18
- 状态：`accepted`（T04 PoC 主会话实测定案）
- 决定：D-034 的 PoC 门已过，**DBOS Transact（TypeScript，@dbos-inc/dbos-sdk 4.23.6）锁定为五段式 Harness 的 durable 执行载体**。七个验证面（六题+审计双写）全部通过：①五段骨架含 ContextBundle 冻结与⑤段 OCC 条件写跑通；②kill -9 断点续跑、已完成 step 零重放、in-flight step 重跑由效果键幂等兜底；③挂起审批跨进程死亡存活、答复即续、版本围栏触发重编译；④writeStream→SSE 进度通路真实可用；⑤与 pg-boss/自研迁移锁/业务连接池同库共存、DBOS 表全部隔离在独立 system DB（业务库 schema 零污染、闲时 2 连接）；⑥tsx 直跑无 bundler 冲突、launch 前注册即可、applicationVersion 自动指纹+恢复按版本作用域（印证 D-038 ⑤ 发布 SOP 必要性）；⑦审计先行同事务、审计写失败=任务硬停、Langfuse 失败=降级+补偿队列扫尾、确定性审计 id 重放幂等。Inngest 备选评估随定案解除挂起。
- 决定：架构姿势固化——system DB 独立分库（`*_dbos_sys`）；workflowID=Task ID（重复投递天然去重，实测重投已完成任务零重跑）；五段新 kind 只进 DBOS、存量 pg-boss 原地不动（D-034 共存口径不变）；效果键按 `wf:{id}:s{n}:{unit}:{candidate}` 粒度派生。
- 发现与随迁（进对应工程票）：官方 datasource 适配器仅 knex/drizzle、无裸 pg——exactly-once 事务步骤在本栈不可用也不需要（D-038 ② at-least-once+业务幂等实测足够），禁止为此引入 knex/drizzle；SSE 终态帧须挂 handle 完成后读（closeStream 早于返回有竞态）；conflict/error 提前退出路径的 outbox 靠补偿器扫尾（补偿器为必备常驻件）；DBOS status.error 不透出细节，错误细节以自建审计表为权威（合 D-036）。
- 证据边界：PoC 代码 `apps/core/src/p1/harness-poc/`（THROWAWAY）；场景证据与报告 `.scratch/dbos-poc-2026-07-18/REPORT.md`。
- 待验证：无新增（promptfoo×火山半天 spike 仍按待验证 18 独立执行）。
- Supersedes：D-034 的「定案形态 = PoC 门」条件态（载体由主选转锁定，其余决定不变）。

## D-042 UIUX 评审三拍板：暗色转正、套组收进对话流、全量修复范围（impeccable critique）

- 日期：2026-07-19
- 状态：`accepted`（2026-07-19 用户拍板）
- 背景：双 agent 隔离设计评审（设计总监视角 + 确定性检测器，互不可见后合成）对产品 Web 应用全面评审，健康分 30/40（Good）。核心结论：工作台首屏材质系统实测达标（壳级玻璃逐像素吻合、玫瑰金 ≤5%、问候语对比 9-12:1），诚实纪律成立；欠账集中在次级面——/pricing 整页 mkfast 模板遗留皮肤（模板橙 hue38 + Bricolage 字体，双路独立实锤）、门店/素材内页页头对比度实测 1.1-1.9:1 硬失败、内容库卡片白字压浅渐变不可读。
- 决定①：**暗色主题纳入正式设计范围**——主应用保留双主题；暗色不再视为模板暗色继承，须按橱窗范式专门打磨暗色材质（白瓷/玻璃的暗色对应物需定义）；DESIGN.md 需补暗色条款（现行文本只给 Pro Studio 留深色影棚）。
- 决定②：**Composer「本次内容套组」模块清单收进对话流**——评审判其为 D-031 禁止的槽位表单回潮（7 项编号清单、6 项禁用带重复解释，嵌于唯一主轴）。执行：折叠/隐藏不可用模块，改为 chips + 单问确认卡，去编号脚手架。本条为 D-031 的执行重申，非新原则。
- 决定③：**本轮修复范围 = 全量 P1+P2 一次清**——三 P1（/pricing 品牌蒙皮+CTA 真实状态、内页页头对比度、内容卡可读性与去同构网格）+ 两 P2（套组瘦身按决定②、误导性持续状态文案「正在为你起草文案…」改真状态映射）。P3 与次要观察入 backlog 随批顺带。
- 证据边界：评审快照 `.impeccable/critique/2026-07-18T15-57-52Z__mkfast-template-main-src-product.md`（含 Nielsen 十项、认知负载、人格红旗、检测器 file:line 全量）；截图证据在会话 scratchpad。
- 待验证：/pricing 推荐档 CTA「不可用」需在配真实支付的环境复核是否为环境诚实态（当前栈 Stripe 假 key）。
- Supersedes：无（DESIGN.md 暗色条款修订随修复批落地；D-031 内容不变）。

## D-043 主路径折叠：五节点纪律执行到呈现层 + Day-0 体验合同入宪

- 日期：2026-07-19
- 状态：`accepted`（2026-07-19 用户确认修复方案后指示落盘拆票）
- 背景：真机截屏走查（fixture 栈全链实走，证据见 spec）确认：流式三候选、移动工作台、门店页、双端接力等终点体验已建成，但从「输入一句话」到「看到候选」隔着创作台设置页——Brief 四卡、模型五卡、额度确认、水印/AIGC 开关、素材授权跳转全部前置平铺，约 8 步；对标（小云雀 3 步 / CreatOK 2 步）的「简单」来自默认值折叠而非结构缺失。根因四条：工程合同直接投影成页面、每道门单独有依据但无人管叠加、WOZ 可见性泄漏进商家界面、「诚实」被实现成「全部前置可见」。这是 hitl 调研修正表（`references/analysis/hitl-research-2026-07-17/README.md`「对原设计方案的修正」）明令反转而未执行到呈现层的部分。
- 决定①：**Day-0 体验合同入宪**——主路径硬门槛：从输入一句话到看到流式候选 ≤2 次点击、0 个前置表单、0 次前置模型/额度确认；无冲突路径 0 张阻塞卡。写入产品验收合同并落 e2e 断言（真实计数点击），任何票向主路径新增前置节，先过此门。
- 决定②：**折叠执行方案**——Brief 由 core 在创建时以 AI 草稿自动确认并压成只展示已确认值的「本次将使用」chips（冲突才由 Harness 弹单问卡）；模型/额度从主路径移除（默认档自动带出报价，额度按钮旁被动展示，不足才阻塞）；水印与 AIGC 标识保留并收进更多设置，实际值进入执行合同，AIGC 标识关闭时必须明示产物烧录边界并如实留痕（2026-07-24 修订：视频不做我方像素烧录——AIGC 承接改为软提示＋发布提醒＋平台上传自动标识，水印/标识烧录开关随 D-105 退出创作主链，本句「产物烧录边界」口径由 D-117/D-118 取代）；没有领域语义、也不进入执行合同的整理标签死控件移除，不为保留 UI 形状伪造字段；「开始创作」直接进入 Harness 流式候选，创作台改为流中可展开抽屉；素材授权改为上传当下的一键确认，由系统生成结构化 evidence pointer，受限素材仍走结构化追问，素材库表单保留为补录/审计入口；桌面首屏补场景 chips（移动端「灵感场景」上桌面，点击换推荐词与套组预设，不跳页不弹表单）。
- 决定③：**确认合并口径**——普通生成在按钮旁明示面向用户的图片/视频额度与剩余量，点「生成」即视为确认（确认与执行合并一击，审计留痕语义不变）；只有额度不足、一次使用异常多产品次数或动作本身不可逆时才额外阻塞。用户不需要确认供应商单价、Token、按秒取整、fallback 或内部预计成本（D-109）。
- 证据边界：走查截屏与全链脚本（会话 scratchpad `shots/`，tour4/tour5 可复用为回归工具）；对标证据 `references/benchmark/interaction-study-2026-07-07/interaction-patterns-xyq-creatok.md` 模式 B/D；后端三进三出合同、审批/授权语义零改动。
- 待验证：折叠后 confirmation_precision 与 time_to_first_usable_draft 的实测变化（接入 Langfuse 四指标）。
- Supersedes：细化 D-012 的确认呈现边界；2026-07-24 再由 D-109 将技术式报价替换为产品额度预览。D-024 的外部能力三态诚实呈现完整有效；D-042 决定②在本决策下扩展为全主路径折叠。

## D-044 模型供给平台默认配置 + 试用套餐（LLM 网关口径）

- 日期：2026-07-19
- 状态：`accepted`（2026-07-19 用户拍板：「模型本来就应该我们在后台默认配置好；试用额度设试用套餐匹配用量」）
- 背景：真机走查实证新 workspace 五家模型供应全部「暂不可用 · 尚未完成可用性验证 · 文案条数可用 0」，生成键永久禁用——Day-0 完全无法出活，是与对标体感差距的最大单点（对标模式 D：技术参数全部可自动，注册即出活）。
- 决定①：**模型供给平台默认配置**——新 workspace 经 verified-workspace provisioning outbox 自动写入平台默认模型 preference，并引用平台全局激活证据；「未选择模型/尚未完成可用性验证」在 Day-0 主路径不可达。strict BYOK 配置始终覆盖平台默认档，平台路径不得读取租户凭据，平台密钥继续走 Secret Manager/KMS。默认档首期为平台指定的单一明确模型；这不否定 P1 模型供应规格允许的 LLM Auto 路由边界。
- 决定②：**试用套餐**——entitlement 层新增 `trial` 档（allowance 按条数、`fixed_days` period、expireDays 到期归零并落 `plan_expired`、`REGISTER_GIFT` 每租户一次）；workspace 开通通过可信内部授予命令与稳定幂等键自动发放，不复用受开发门控的 `checkout_plan`。AdminPlanControl 增一档可视化编辑。计量模型保持条数制（商家语言），不换积分币。
- 证据边界：admin/plans（AdminPlanControl，CAS+审计+回滚）与 admin/models（目录草稿→发布→变更历史 + 四运行时开关）后台骨架已存在且可视化，本决策为其补开通钩子与 trial 档，非新建后台；走查截屏（模型五卡全灰帧）。
- 待验证：trial 额度量与 expireDays 数值（挂定价测算，先用可配置默认值上线）。
- Supersedes：无（模型目录/激活域与 P1 模型供应规格的 LLM Auto 允许口径不变，仅新增平台默认供给与开通路径）。

## D-045 额度体系补课：流水语义 + 失败退额 + 兑换码 + 支付接缝（mkfast-app 对照采纳）

- 日期：2026-07-19
- 状态：`accepted`（2026-07-19 用户指认 mkfast-app 参考源码并确认落盘）
- 背景：`references/repos/mkfast-app`（同栈：TanStack Start + Cloudflare + Drizzle + Better Auth）自带完整积分体系，与我方 entitlement 域对照后判定：计量模型不换（条数制对商家更直白，配额双口径修复方向一致），平移四机制。
- 决定：①新建 workspace 级 grant-lot 批次账本与交易表，承载 REGISTER_GIFT/SUBSCRIPTION_RENEWAL/PURCHASE_PACKAGE/REDEMPTION_CODE/REFUND/USAGE/EXPIRE 的条数制语义，按 `expirationDate ASC NULLS LAST` 消耗；REFUND 以 `relatedTransactionId` 挂失败 USAGE，并补齐文案部分输出中断与视频外层失败的自动退条数；②**兑换码模块**按统一事务实现生成/批量/作废/核销，核销记录关联真实 grant transaction id，提供 platform admin 管理页与 `workspace.billing.manage` 商家兑换权限；③**支付接缝**由 Stripe/Creem webhook 先绑定 workspace 并归一化订阅生命周期事件，再以 payment/internal actor 进入 Foundation 结算；`plan.payment-mapping` 由 admin-config 热读，取消在周期末回落，legacy `apply_plan` 与 Foundation commerce 只保留一个写入真相；④mkfast-app 的 admin/prompts 提示词编辑页不采（与 D-037 Langfuse prompt management 重叠）。
- 证据边界：mkfast-app 镜像 `references/repos/mkfast-app`（浅克隆，入 .gitignore 不入库）只提供词汇与字段思想；其兑换三步无统一事务、悬空 `creditTransactionId`、按 createdAt 而非到期日 FIFO、过期后未清 remainingAmount 等缺陷不得复制。
- 待验证：webhook→套餐映射在真实支付环境的端到端（挂 GL-16 环境项）。
- 上游对照（2026-07-20 源码复核补充）：mkfast-app 三条语义可移植——①退款幂等 = `unique(relatedTransactionId, type)` 唯一索引 + 已存在退款即 no-op；②兑换核销原子 CAS = `lt(redemptionsCount, maxRedemptions)` 条件自增 + 零变更行判超发；③授予批次自带 remainingAmount/expirationDate 即 grant-lot 骨架。反面教训一条补入合同：上游授予路径（addCredits）自身无幂等键、去重全靠支付 webhook 层兜底，我方授予路径必须设独立幂等键。详见 docs/reviews/mkfast-mkimage-source-xref-2026-07-20.md。
- Supersedes：无（加油包、周期额度、CAS 后台语义不变，为其补流水完整性）。

## D-046 流内自由追问口：result 阶段常驻自由文本「调整方向」输入（聊天流三路复核裁决）

- 日期：2026-07-19
- 状态：`accepted`（2026-07-19 用户拍板，采纳三路对抗复核建议第 1 条）
- 背景：针对「为什么不做聊天流」做三路对抗复核（steelman 辩护 / 工程代码实证 / 合规任务形态），三路独立收敛于同一裁决。**thread-as-primary 维持不做**：聊天容器会静默破坏审批 CAS 恰一次消费、报价确认字段级失效、跨任务收件箱聚合、交付物版本化四项硬机制（合规路）；消息 log 变第二写入面与 DBOS durable 状态双 writer，重开「新旧投影矛盾」缺陷类（工程路）；竞品无人把自由聊天跑成主路径，小云雀 thread 实测亦为一次性提交容器（steelman 路自我否证）。但代码实证坐实一个真缺口：候选交付后 composer 整块 hidden（unified-creation-workbench.tsx:3071），「打磨表达」助手为固定种子单轮提议非自由文本，result 阶段仅结构化按钮（换一批/重试/派生）——「改得更活泼一点」类长尾意图无处可说。此即 D-031 待验证「chips 能否穷尽结构化输入场景」的实证答案：不能穷尽。
- 决定：**result 阶段保留常驻自由文本「调整方向」输入口**——自由文本喂入一次 revise turn，底层复用派生 Task/derived revision 机制（D-033），零新内核、不新增消息持久化实体；对话仍为输入层与叙述层，交付/批准/恢复真相仍以 workflow + ContentPackage revision + CAS 收件箱为准（D-019 边界不动）。落点并入 T4（GH #53，流中接管票为 result 阶段容器属主）。
- 附带裁决：②可见迭代血缘（「你说→我做→你改」流中时间线，底层 revision 链已存在仅呈现缺位）进 backlog；③单任务内时间线呈现语言已由 T4 承载，不另立项。thread 壳日后如需 = 「Task + 类型化帧」的只读投影（工程路判定现架构 thread-ready，推迟零锁定成本）；重新评估触发条件维持「Pro Studio 常驻多会话面板进排期时 spike assistant-ui vs 自建」不变。
- 验收（并入 T4）：result 阶段存在自由文本入口；提交产生 derived revision 且审计链完整；不新增消息持久化实体。
- Supersedes：无（D-019/D-031 边界维持；本决策为 D-031 该待验证项的实证补课）。

## D-047 下一轮功能补齐讨论范围与顺序

- 日期：2026-07-19
- 状态：`accepted`（2026-07-19 用户指定）
- 决定：下一轮产品开发讨论只聚焦已经识别的功能缺项，暂不展开运营层问题；讨论与后续拆解顺序固定为：①后端管理平台的可视化；②多渠道模型供应商的管理；③视频、图片生成功能的升级与完善；④用户旅程、交互体验与 UI 的升级完善。（2026-07-20 D-072 将第④项提前至第③项之前，其余不变。）
- 决定：本条只锁定讨论边界与顺序，不预先假定四个方向的具体产品形态、功能范围或实现方案；每个方向在逐一讨论中形成的关键决定，均在当轮即时追加为后续 D-XXX 决策，并同步写明证据边界、影响范围、待验证项与被替代决定，避免依赖会话上下文作为唯一真相。
- 原因：用户已经确认运营问题可以后补，当前优先补足明确的开发功能缺项；同时要求关键决定及时记录，降低长会话中的上下文丢失与膨胀风险。
- 影响：后续讨论从“后端管理平台的可视化”开始；运营验证、真实门店回放与合规执行继续遵循 D-040 的延期边界，不进入本轮四项功能讨论的默认范围。
- 证据边界：本条为用户指定的讨论议程与记录机制，不构成四个方向已完成需求定义或已批准开发。
- 待验证：四个方向各自的用户角色、核心任务、信息架构、权限边界、最小可交付范围与验收合同，按顺序逐项讨论后确定。
- Supersedes：无；延续 D-040 的阶段排序。

## D-048 后端管理平台定位：面向日常运营的一体化可视化控制台

- 日期：2026-07-19
- 状态：`accepted`（2026-07-19 用户拍板）
- 决定：后端管理平台不是单一配置页或纯技术监控面板，而是面向平台日常运营人员的一体化全功能控制台；产品范围同时包含平台总览、配置管理台与运维监控台，并允许从总览逐级下钻到具体业务对象、配置项和异常证据。
- 决定：交互默认采用可视化、点击、选择、开关和简易输入；运营人员的正常工作不依赖编辑代码、SQL、环境变量、原始 JSON 或命令行。可安全回滚、边界明确的日常动作可由控制台直接完成；复杂、高风险或必须代码级处理的事项，由系统在提醒或告警后生成可移交给技术人员的上下文与证据，不在运营界面内伪装成一键修复。
- 决定：复用现有模型、模板、集成、套餐、兑换码、用户、审计七个后台路由页（健康为审计页内区块，非独立页面）及既有 Foundation/P1 权限、CAS、审计和回滚能力（CAS/回滚=admin-config 修订链，审计=Foundation 层 `p1_command_audits` 命令审计），在其上补统一信息架构与总览/下钻闭环；不另建第二套管理运行时。成熟架构与组件只在许可证、维护状态、现有 React/TanStack/shadcn 栈兼容性和定制成本通过评估后局部吸收，不为采用框架而整体重写现有后台。
- 原因：用户明确后台主要供日常运营人员使用，要求兼具建设总览、平台控制、配置管理和运维监控，并尽量把操作收敛为可视化点击或简易输入；复杂维护仍由纯技术人员负责。
- 影响：第一项后续讨论需继续确定总览首页的信息层级、一级导航与对象模型、运营可执行动作白名单、告警到技术移交合同；第二项“多渠道模型供应商管理”作为该控制台内的核心能力域展开，而不是另起孤立后台。
- 证据边界：当前代码存在七个后台路由页（models/templates/integrations/plans/redemptions/users/audit，健康为 audit 页内 `AdminOperationsHealth` 区块），`/admin` 默认跳转模型页，尚未形成统一总览；本条确认产品定位，不代表总览指标、告警规则或完整页面清单已经拍板。
- 待验证：成熟开源架构/组件的许可证、维护活跃度、栈兼容性、可访问性、数据密度能力与接入成本；低风险运营动作和技术升级动作的具体分界。
- Supersedes：无；细化 D-044 所述既有后台骨架的产品化方向。

## D-049 后台主对象与作用域：以工作区/门店为中心

- 日期：2026-07-19
- 状态：`superseded`（2026-07-19 用户纠正概念边界，最终裁决见 D-051）
- 决定：一体化后台以**工作区/门店**作为一级业务对象，用户账号作为工作区的成员关系，不再以孤立用户账号作为客户全景。工作区详情聚合成员与角色、套餐与额度、账本与支付、任务与内容、渠道连接、模型用量与失败、告警和审计等该租户事实；平台级模型、模板、套餐定义、全局配置与系统健康继续保持平台全局作用域。
- 决定：所有后台页面和请求必须显式区分“平台全局”与“目标工作区”，界面持续显示当前作用域；禁止继续以管理员的第一个工作区作为隐式管理目标。进入工作区详情或通过显式工作区选择器完成下钻，任何写操作都需再次显示目标工作区、影响摘要和原因，并进入审计链。
- 决定：平台管理员查看其他工作区采用受审计的管理视图，不以无痕用户冒充作为默认机制；需要复现商家视角时，另行定义带显著横幅、时限、原因和完整审计的支持会话合同，未拍板前不实现 impersonation。
- 原因：套餐额度、支付、任务、内容、连接和模型消耗均天然属于租户边界；以工作区/门店为主对象能给运营人员完整上下文，也能消除现有部分后台能力隐式读取管理员首个工作区造成的错看、错改风险。
- 影响：第一版新增工作区列表与工作区详情；现有用户页改为平台账号入口并可反查所属工作区；总览、告警、审计与搜索结果均支持按工作区下钻。后端查询合同需接受显式 workspaceId 并做平台管理员授权与审计，不再依赖请求人默认工作区。
- 证据边界：当前代码已有用户、工作区成员、套餐、支付、任务与连接等分散事实，但尚无工作区管理界面；本条确认聚合与作用域模型，不代表详情页全部字段、动作或权限已经定案。
- 待验证：一个账号跨多个工作区时的默认排序与搜索体验；工作区合并、停用、所有权转移等低频高风险动作是否进入首版。
- Supersedes：本条整体由 D-051 替代，不再作为后台产品或开发范围依据。

## D-050 后台客户主对象纠正：账号是唯一用户身份，不建设门店/工作区管理

- 日期：2026-07-19
- 状态：`superseded`（2026-07-19 用户进一步纠正后台主对象，见 D-051；不建设门店/工作区管理的边界被 D-051 继承）
- 决定：本轮定义的是平台内部的开发者/运营管理后台。后台面向的客户主对象只有**注册用户账号**：用户以一个账号身份使用产品，并按需求选择不同等级套餐；套餐决定该账号可用的功能、服务数量、额度、并发、队列优先级及其他权益。后台围绕“账号 → 套餐/权益 → 用量/账本/支付 → 任务/内容 → 连接/异常/审计”形成全景与下钻。
- 决定：不建设门店管理、工作区管理、门店切换器、组织层或门店级 CRUD。`workspaceId` 若继续存在，只是现有系统内部的数据隔离、权限和 Core 调用键，不作为后台用户可见的业务对象、一级导航或运营选择项；后台通过账号关系在内部解析相关数据，页面统一使用用户可理解的账号与套餐语言。
- 决定：保留 D-048 的一体化平台总览、配置管理和运维监控定位；平台全局模型、供应商、模板、套餐定义、系统配置和健康属于控制台全局域，用户详情承载单账号的权益与运行事实。不得为了隐藏 workspace 概念而在本轮无必要地重写底层隔离模型，除非后续实证表明一账号对应多个内部 workspace 并造成数据歧义。
- 原因：用户明确产品中用户只有注册账号这一种身份，不需要平台替其管理门店；此前把底层 workspace 与门店业务对象等同并提升为后台主对象，属于概念错误，会制造无需求的管理层级和开发范围。
- 影响：第一版新增/改造对象应是平台总览、用户账号列表与账号详情，而不是 `/admin/workspaces`；现有用户页需聚合套餐、权益、用量、支付、任务、内容、连接、异常和审计。研究与组件选型改按账号中心后台评估。
- 证据边界：本条确定后台产品概念与展示边界；不直接决定现有 workspace 表、membership、触发器或 Core workspaceId 的迁移/删除，底层是否简化需另做数据模型与兼容性评估。
- 待验证：现有数据中一个账号与内部 workspace 的实际基数；账号封禁、套餐变更、额度补偿、任务重试等动作的分级权限与审计合同。
- Supersedes：本条由 D-051 替代；其中“用户只有注册账号身份、不建设门店/工作区管理”继续有效并被 D-051 收编。

## D-051 后台一级管理对象：整套系统能力与运行状态

- 日期：2026-07-19
- 状态：`accepted`（2026-07-19 用户纠正并拍板）
- 决定：一体化后台的一级管理与运维对象不是门店、工作区或用户账号，而是**本系统完整的后台功能与能力域**。用户账号、套餐和权益只是客户与商业化能力域中的一个模块；后台总览和导航首先按系统能力组织，并允许从能力异常或运行事实下钻到受影响用户、任务和具体记录。
- 决定：每项可管理能力都应提供统一的可视化管理合同，至少回答：①能力用途与当前可用状态；②当前配置、版本与生效范围；③供应商、模型、凭据、队列、数据库、存储和外部渠道等依赖；④调用量、成功/失败、延迟、额度与成本等运行事实；⑤最近变更、告警、影响范围、审计证据与下一步；⑥符合 D-048 边界的安全操作。复杂代码级修复继续生成技术移交信息，不包装为运营一键操作。
- 决定：首轮能力地图至少覆盖账号与认证、套餐/权益/支付/兑换码、模型供应商与路由/质量、文案/图片/视频/音频生成、Harness/任务/队列、ContentPackage/模板/素材/画布、渠道与工具集成、数据与存储、配置/密钥、观测/告警/审计。后台以能力目录与统一状态语义贯通现有独立页面，不为每个模块继续堆互不相识的孤岛页。
- 决定：用户对外只有注册账号这一种身份，套餐决定功能与服务数量；不建设门店/工作区管理。`workspaceId` 等底层隔离键不得进入一级信息架构，只能在技术证据或受控诊断详情中按需显示。
- 原因：用户明确重点管理和运维的是系统整个后台的功能。以账号为中心仍会遗漏模型、任务编排、队列、媒体生成、集成、存储、配置和观测等核心系统能力，无法形成真正的全功能控制台。
- 影响：`/admin` 默认入口应成为系统能力指挥台；新增能力目录/状态总览与统一异常入口，现有模型、模板、集成、套餐、兑换码、用户、审计七个路由页（含审计页内健康区块）按能力域重新编组。后续第二项“多渠道模型供应商管理”是 AI 能力域的专项下钻。
- 证据边界：本条锁定管理对象和统一能力合同，不代表每项能力的指标、动作白名单、告警阈值或页面布局已经定案；成熟组件研究需按 capability/module-centric 控制台重新校准。
- 待验证：当前后台模块与运行接口的完整能力盘点；哪些能力可以提供可逆操作，哪些只能观察与移交；跨能力依赖图是否需要 React Flow 等拓扑视图。
- Supersedes：D-049 全部内容与 D-050 的“账号为后台主对象”部分；保留 D-050 的单一账号身份和不建设门店/工作区管理边界；D-048 继续有效。

## D-052 Cloudflare 交叉运维边界：原生深诊断、产品化投影与安全控制三层分工

- 日期：2026-07-19
- 状态：`accepted`（2026-07-19 用户指定方向）
- 决定：因后期部署在 Cloudflare，一体化后台必须与 Cloudflare 原生运维能力交叉设计；在实施前把相关数据与动作明确划为三类：①只能或应该留在 Cloudflare Dashboard/Wrangler/技术观测工具中的原生深诊断；②可通过 Cloudflare 官方 API、GraphQL Analytics、Observability/OTel、Analytics Engine 或我方遥测投影到产品后台的只读状态；③经过最小权限、影响预览、可回滚/幂等与审计约束后，可由我方后台安全触发的控制动作。
- 决定：我方后台不复制 Cloudflare Dashboard，也不把原始基础设施指标直接交给日常运营人员；它负责将 Cloudflare 证据翻译为“受影响的系统能力、用户或任务、开始时间、最近变更、建议动作、负责人和技术证据链接”。Cloudflare 原生控制面继续是部署、账户/区域安全、原始日志/Trace 深查和基础设施配置的权威；我方 Product Core、审计表与业务账本继续是功能状态、任务、额度、成本和用户影响的权威。
- 决定：所有 Cloudflare 数据访问与控制调用只在服务端使用专用最小权限 API Token，浏览器不得接触账户级凭据；读取失败、采样、延迟、保留期、套餐限制和 API 限流必须显式显示为未知/过期，不得以最后一次缓存值伪装实时健康。跨控制台跳转携带经脱敏的时间范围、script/deployment、correlation/trace 和能力上下文，不携带用户原文、素材或密钥。
- 原因：Cloudflare 已拥有 Workers、网络、安全、部署、日志与资源级诊断能力，重复建设成本高且容易形成第二真相；但只把运营人员送进 Cloudflare 又无法解释业务影响，也无法与我方任务、模型、套餐和审计闭环关联。
- 影响：Cloudflare 交叉矩阵成为 D-051 能力目录和告警投影的输入；后台需要 Cloudflare 只读采集适配器、缓存/新鲜度合同、证据深链和技术移交 envelope。Cloudflare 写操作默认关闭，逐项通过安全动作评审后才开放。
- 证据边界：本条确认分层原则与真相边界；哪些数据只能在 Cloudflare 查看、哪些 API 可读、哪些动作可写，需按当前实际 Workers/R2/Hyperdrive/Observability 配置和 Cloudflare 最新官方文档形成逐项矩阵后再拍板。
- 待验证：Workers Analytics/Logs/Traces、deployments/versions、R2、Hyperdrive、Queues、WAF/安全、DNS/Zone、Secrets、账单/用量等数据的 API 可得性、计划限制、采样/保留期、延迟和最小权限；当前未使用的 Cloudflare 产品不得因调研自动进入架构。
- 上游对照（2026-07-20）：MkImage 管理任务详情「从持久化时间戳重建阶段时间线」（提交/轮询/供应商完成/R2 各阶段 + 相邻耗时差）是「把技术证据翻译成业务时间线」的现成参照件，可用于本条证据翻译层；其 Workflow instance id 仅作文本存展、不联动控制台，深链缺口反面印证本条自建方向。详见 docs/reviews/mkfast-mkimage-source-xref-2026-07-20.md。
- Supersedes：无；细化 D-048/D-051 的外部控制面与技术移交边界。

## D-053 Cloudflare 交叉运维 v1：只读投影，不持有控制面写权限

- 日期：2026-07-19
- 状态：`accepted`（2026-07-19 用户选择推荐方案）
- 决定：Cloudflare 交叉运维第一版只使用服务端专用的最小权限**只读 API Token**，将可获得的 Workers 部署与运行摘要、R2 和 Hyperdrive 状态/趋势及已启用的 Cloudflare 安全证据投影到我方系统能力后台；第一版不持有 Cloudflare 控制面写 Token，不从我方后台执行 Worker 发布/灰度/回滚、采样与 OTel destination 调整、DNS/WAF 变更、Secret/Binding 管理、R2 桶策略或任意对象操作、Hyperdrive 连接配置及账户账单操作。
- 决定：我方后台允许的交互限于刷新只读快照、触发我方健康探针、确认/指派内部告警、执行已有 Product Core 合同内的产品侧熔断/降级/幂等重试，以及携带脱敏时间范围、能力、deployment/script 和 correlation/trace 上下文打开 Cloudflare 技术台；这些产品侧动作不得伪装为 Cloudflare 资源已被修改。
- 决定：当前仓库未绑定 Cloudflare Queues，异步与 durable 任务事实来自 DBOS、pg-boss 和 PostgreSQL，后台不得展示虚构的 Cloudflare Queue 状态。外部 OTel/Grafana destination 尚未配置，只能作为后续技术深诊断候选；现有 Workers logs/traces 100% 采样和 Hyperdrive 全零占位 ID 必须以“配置风险/未就绪”呈现，不能宣称生产可用。
- 原因：只读投影足以完成运营可视化、业务影响解释和技术移交；将部署、密钥、网络、安全、存储或数据库连接写权限放进产品后台，会扩大账户级 blast radius，并与 CI/IaC/Cloudflare 原生控制面形成双写真相。
- 影响：Cloudflare adapter 首版只有查询、归一化、缓存、新鲜度和 deep-link 能力；权限、密钥管理和验收按 read-only 设计。Workers Observability Query API 虽用于查询，但官方当前要求 `Workers Observability Write`，因此首版明确排除；Workers 趋势使用 GraphQL Analytics，部署/版本和资源盘点使用只读 REST，日志与 Trace 明细留在 Cloudflare 原生技术台下钻。未来任何 Cloudflare 写动作均需单独决策，逐项证明必要性、最小权限、双人/二次确认、幂等/回滚和不可变审计，不由本条授权。
- 证据边界：本轮以 Open CLI 直接读取 Cloudflare 官方资料为主；Web Search fallback 仅用于定位少量已迁移的官方页面，核心事实均重新通过 Open CLI 读取官方页面核验。完整 A/B/C 数据与权限矩阵保存到 `references/analysis/cloudflare-admin-boundary-2026-07-19/`。具体数据集和保留/采样限制以该研究包及实施时刷新后的官方文档为准。
- 待验证：生产 Cloudflare 账户/Zone/Worker/R2/Hyperdrive 实际 ID 与计划能力；只读 token 的最终权限集合；后台刷新周期、缓存 TTL 和 Cloudflare API 失败时的 stale/unknown UX。
- Supersedes：将 D-052 的第三层“安全控制动作”在 v1 收窄为我方产品侧安全动作；Cloudflare 原生写控制全部延期，D-052 其他边界不变。

## D-054 后台信息架构：系统能力一级、技术依赖二级

- 日期：2026-07-19
- 状态：`accepted`（2026-07-19 用户选择推荐方案；2026-07-20 D-080 细化状态计算：各域自报+依赖查找表，不建图级严重度传播引擎）
- 决定：后台采用“能力 + 依赖”的两层信息架构。一级按日常管理可理解的系统能力域组织，初始包括账号与商业化、AI 供应与生成、任务编排、内容与资产、外部集成、运行与治理；二级从具体能力下钻到 Workers、Node Core、DBOS/pg-boss、PostgreSQL/Hyperdrive、R2、模型/渠道供应商等技术依赖和证据。技术组件不与业务能力混成同级扁平导航。
- 决定：能力状态由产品合同与其依赖共同计算，不以单项基础设施绿色直接推导“功能可用”。例如 Workers 正常但视频 Provider 不可用时，视频生成能力仍可为 blocked；某个备用 Provider 异常但主路由和恢复合同正常时，能力可为 degraded/attention 而非全局不可用。每个状态必须说明受影响功能、范围、新鲜度、原因和证据来源。
- 决定：能力与技术依赖是多对多映射，由统一能力目录/registry 维护引用，不在多个页面复制状态真相。同一个 R2、Hyperdrive 或 Provider 故障可以映射到多个受影响能力；从能力页进入依赖详情，也可从技术依赖反查受影响能力与任务。Cloudflare 只读投影按 D-053 进入依赖层，首页只呈现其产品化影响。
- 决定：一级能力详情继续遵循 D-051 的统一合同：可用性、配置/版本、依赖、用量/成本、任务/失败、变更/审计和安全操作；全局技术拓扑只作为技术人员的二级诊断视图，不作为默认首页，也不因存在 React Flow 候选而自动建设可编辑画布。
- 原因：日常管理需要先回答“哪项系统功能有问题”，技术维护再回答“哪个组件导致”；纯技术服务优先会把用户影响切碎，全部扁平又会丢失能力与依赖关系。
- 影响：`/admin` 总览按能力异常与待处理事项聚合；现有模型、模板、套餐、用户、审计、健康页面成为能力或依赖下钻。需要新增类型化的 capability registry、dependency reference、status projection 和 affected-capability 查询合同。
- 证据边界：本条确定两层结构与初始一级分组；各能力的最终名称、数量、依赖边、状态算法与页面排序需在全后台功能盘点后固化。
- 待验证：一项依赖同时影响多个能力时的去重和严重度合成；依赖图在表格/列表无法高效诊断时是否触发只读 React Flow spike。
- Supersedes：细化 D-051 的能力中心结构；D-037/D-053 的只读拓扑和 Cloudflare 边界保持不变。

## D-055 后台默认首页：异常与待办优先

- 日期：2026-07-19
- 状态：`accepted`（2026-07-19 用户选择推荐方案；2026-07-20 D-080 收窄首版为只读异常列表，负责人/确认/指派工作流延后）
- 决定：`/admin` 默认首页首先回答“现在有什么需要我处理”，按异常与待办优先组织第一屏；系统全景作为同页摘要和能力目录入口，技术拓扑继续放在二级诊断层，不把全部指标或技术节点平铺成默认首页。
- 决定：首页统一聚合 `blocked`、`degraded`、`attention`、`not_verified` 和长时间 `stale` 等需要判断的状态，至少覆盖能力故障、供应商/模型异常、任务积压或失败、依赖未就绪、配置与密钥风险、部署后回归及已接入的安全信号。排序综合严重度、受影响能力/任务/用户范围、持续时间和最近变化；相同根因只生成一条主事件并列出受影响能力，避免重复告警。
- 决定：每条异常或待办必须直接给出受影响能力与范围、开始/最近变化时间、证据来源与新鲜度、最近相关变更、当前负责人/确认状态，以及符合既有权限边界的下一步动作或技术台 handoff。没有异常时显示“当前无待处理异常”，随后展示能力全景与关键趋势，不用空白页或装饰性红绿大屏制造虚假价值。
- 原因：本后台的核心任务是管理和运维整套系统能力；日常打开首页最重要的是快速发现需要行动的问题，而不是从全量指标或技术拓扑中自行寻找异常。异常优先还能把 Cloudflare、Core、任务、模型供应商和业务影响统一到同一行动语言中。
- 影响：第一期新增统一 incident/attention projection、异常去重与排序、负责人/确认状态和来源深链；现有健康卡片与各能力页继续复用为下钻。系统全景和技术拓扑不会删除，但不阻塞异常首页首版。
- 证据边界：本条只拍板默认首页的信息优先级与最小异常合同；具体阈值、自动归因、通知渠道、SLA 和值班流程需结合真实运行数据另行验证，运营指标仍按 D-040 延后。
- 待验证：不同严重度的阈值与去重窗口；哪些事件需要人工确认/指派；全绿时最有价值的三到五个全景摘要；误报率和从首页到定位/处理的时间。
- 上游对照（2026-07-20）：MkFast/MkImage 的 `/admin` 首页均为直接重定向到对象列表页，无异常/待办概念——异常优先首页为真增量、无现成方案可抄；MkImage 任务页 StatCard（彩色图标底 + Skeleton 加载）可复用为「全绿时全景摘要」区块的轻量件。详见 docs/reviews/mkfast-mkimage-source-xref-2026-07-20.md。
- Supersedes：无；细化 D-051/D-054 的 `/admin` 默认入口与 D-052/D-053 的 Cloudflare 业务语义化投影。

## D-056 后台首个开发增量：先完成全能力横向骨架

- 日期：2026-07-19
- 状态：`accepted`（2026-07-19 用户选择推荐方案；2026-07-20 D-080 改写首个增量形态：合同先定+已插桩域做深+其余静态存根，与 D-068 供应纵向合并为同一增量）
- 决定：后端可视化的首个开发增量采用“全能力骨架”路线：先盘点并注册整套后台能力，统一能力标识、分组、负责人、状态语义、证据新鲜度、受影响范围、依赖引用和下钻入口；在此基础上交付 D-055 的异常首页。不会先把 AI 供应、任务、Cloudflare 等少数模块纵向做完，也不会先逐页重写现有七个管理页面。
- 决定：横向覆盖不等于为所有能力制作同等深度的指标和操作。每项能力首版必须至少提供真实的名称/用途、当前状态或明确的 `not_instrumented` / `not_verified`、证据时间、关键依赖、负责人和一个有效下钻；已有模型、模板、集成、套餐、兑换码、用户、审计/健康页面作为现成下钻复用。缺少数据时显示缺口和接入任务，禁止用零值、静态绿色或模拟数据伪装健康。
- 决定：首个逻辑包由 capability registry、统一 status/attention projection、`/admin` 异常首页、能力目录与既有页面重新编组组成；类型化配置改造、安全动作白名单、Cloudflare read broker、深度趋势、统一审计和只读拓扑按该骨架逐步接入。每个后续纵向包都必须回填同一 registry 与状态合同，不得再建平行首页或孤岛状态源。
- 原因：管理目标是整套系统能力。先做少数域会继续让其他后台功能不可见，逐页迁移又会推迟统一状态与异常入口；横向骨架能先建立全局真相、导航和扩展接口，同时最大化复用已实现页面。
- 影响：第一批开发票以 AP-01（统一信息架构/总览）和 AP-02（运行状态投影）为主，并把 AP-03 的动作与 handoff 合同只做到可扩展边界；后续 Cloudflare CF-01～CF-08、模型供应商专项、媒体生成和 UX/UI 改造均挂到同一能力目录。验收以“所有已盘点能力都有真实状态或显式数据缺口、有下钻、异常可聚合”衡量，不以图表数量衡量。
- 证据边界：本条决定首个开发增量的覆盖策略和最小完成合同，不表示每个能力的深度观测、阈值、写操作或页面已经纳入首版；能力清单需由代码、运行接口和最新设计文档共同盘点后冻结。
- 待验证：完整 capability inventory 及 owner；哪些现有接口足以直接形成状态；首批 `not_instrumented` 项及其接入优先级；异常投影在真实数据量下的去重与性能。
- Supersedes：无；细化 D-051/D-054/D-055 的首个实现顺序，并采用 `references/analysis/admin-platform-research-2026-07-19/` 的 AP-01/AP-02 建议。

## D-057 后台权限底座：能力权限合同先行，复杂角色管理延期

- 日期：2026-07-19
- 状态：`accepted`（2026-07-19 用户选择推荐方案）
- 决定：后台首版先建立服务端可执行的能力权限合同，不同期建设自定义角色编辑器。现有 `admin/user` 继续作为进入管理面的外层身份门；进入后台后，所有读取与操作再按稳定的 capability permission 校验，前端只根据授权呈现入口和动作，不能成为权限真相。
- 决定：首批权限域至少区分系统/能力查看、任务恢复动作、渠道/部署生命周期动作（隔离/排空/恢复）、运行/模型/模板配置发布与回滚、账号与商业化治理、供应商凭据治理、审计查看/导出。（「事件确认与指派」随 D-080 对 D-055 的收窄延后，出现第二运营角色时再引入。）高影响动作除权限外仍需目标与影响预览、原因、CAS/幂等或回滚合同及不可变审计；Cloudflare 写操作继续被 D-053 禁止，不能因新增权限键而获得授权。
- 决定：首期受信平台管理员可被赋予完整能力权限，以维持当前单一管理路径；权限映射必须在服务端集中解析并默认拒绝未知权限。未来需要多人分工时，再把这些既有权限组合为观察者、操作员、配置管理员、商业化管理员或自定义角色，不重写领域命令和审计合同。
- 原因：继续只用二元角色会把查看、告警处理、配置发布、商业化和凭据管理混成同一权限，随着后台覆盖整套系统能力会放大误操作与凭据暴露风险；现在直接建设完整 RBAC UI 又会抢占能力骨架和异常首页的首版范围。
- 影响：AP-01/AP-02 的所有查询和 AP-03 的动作合同需声明 required permission；现有 admin middleware 只负责粗粒度入口，Core/服务端负责细粒度授权。审计事件记录 actor、permission、target、reason、before/after 或结果、correlation 和时间；禁止仅靠隐藏按钮保护操作。
- 证据边界：本条确定授权模型和首批权限域，不决定组织架构、角色命名、授权 UI、审批流或双人复核。当前代码仅有二元 admin 门（前端 `adminRouteMiddleware` 角色校验 + Core `actor === 'admin' || adminActorIds` 判定，抽查管理写接口均过该门、未发现无鉴权写口），能力级权限键零存在，能力权限合同需从零建设而非补覆盖。
- 待验证：完整后台命令盘点及 permission matrix；凭据元数据与密钥值的读取边界；高影响商业化动作是否需要双人复核；首期管理员引导与权限缺失 UX。
- 上游对照（2026-07-20）：两项范式可直接沿用——①中间件双态（路由态 throw redirect / API 态 401-403，共享同一判定源），能力权限合同落地时保留「入口门 + 细粒度守卫」分层；②Better Auth `admin()` 插件的 ban/role/impersonation 原语复用不自写（impersonation 是否启用仍按 D-049 支持会话合同另行拍板）。MkImage force-fail 三件套（AlertDialog 确认 + 服务端状态前置守卫 + 幂等补偿）是受治理快捷动作的最小雏形，补权限键与审计写入即符合本条合同；上游能力级权限键零存在，印证合同从零建设的判断。详见 docs/reviews/mkfast-mkimage-source-xref-2026-07-20.md。
- Supersedes：无；细化 D-048 的简单操作/技术移交边界与 D-056 的首版能力骨架。

## D-058 多渠道模型供应：模型、交易方、执行渠道与部署分层

- 日期：2026-07-19
- 状态：`accepted`（2026-07-19 用户选择推荐方案）
- 决定：多渠道模型供应采用独立供应链分层，不把“供应商+模型+endpoint”扁平复制成彼此无关的模型，也不把供应真相全部交给外部网关。规范层依次为：① `CatalogModel` 表示制造商模型身份、稳定名称/版本、模态与能力；② `ProviderProfile` 表示实际 API 交易方/服务支持与合同边界；③ `ExecutionChannel` 表示官方直连、中转商、网关、区域/协议/账号与凭据归属；④ `Deployment` 将一个模型绑定到一个具体渠道，并承载 provider model/endpoint revision、生命周期、数据策略、价格、限流、激活证据与运行状态。
- 决定：同一 `CatalogModel` 可有多个 Deployment，同一 ExecutionChannel 也可承载多个模型；质量基线与用户可理解的模型能力归模型层，实际成本、额度/限流、延迟、错误、可用性和凭据风险归渠道部署层。管理界面可提供便于操作的扁平表格，但编辑、审计和 API 合同必须保留这些规范实体及引用关系，禁止通过复制模型行形成多份状态真相。
- 决定：路由最终选择并冻结的是 Deployment，而不只是模型名；RouteSnapshot 需保存模型、交易方、渠道、部署、凭据/策略/价格/endpoint 版本与候选顺序。Bifrost、LiteLLM 或其他网关若采用，只登记为一种 ExecutionChannel；Product Core 继续拥有能力目录、路由规则、幂等/回退、产品额度、供应商成本账本和审计真相。
- 原因：同一模型经官方直连、中转、网关或不同区域/账号调用时，制造商能力相同，但合同方、价格、限流、数据边界和故障域不同；扁平条目会重复模型和质量事实，网关全托管又会把产品级成本与审计真相外移。
- 影响：现有 `ProviderProfileRevision`、`ExecutionChannelRevision`、`PublishedDeployment` 与 `RouteSnapshot` 作为迁移基础继续演进；固定三凭据槽和单一 direct runtime 需改为按渠道/部署动态装配。后台至少提供模型、交易方/渠道、部署、凭据与路由五个关联视图，并能从模型反查所有渠道、从故障渠道反查受影响模型/能力/任务。
- 证据边界：当前代码已经出现上述实体与引用，但多处仍是 recorded 默认值、环境变量装配和原始 JSON 编辑；本条确认目标规范模型，不代表现有运行时已经支持任意渠道热接入或所有供应商自动发现。RouteSnapshot 目前无单一权威类型定义（存在 StrictByokRouteSnapshot、checkpoint 内嵌等分化形态），迁移第一步需统一其规范类型。
- 待验证：ProviderProfile 与合同/账单账号是否需进一步拆分；一个渠道多 endpoint/region 的建模粒度；供应商项目/子账号与凭据版本关系；历史扁平 catalog revision 的前向迁移和兼容读取。
- 上游对照（2026-07-20）：MkImage 的 ModelConfig 字段形状可作 CatalogModel 起点——业务 model id 与 provider model 字符串分离、providers[]/preferredProvider、supportedResolutions/Qualities/AspectRatios 能力收窄矩阵；需补 revision 化并把 provider 归属改挂 Deployment。其 ImageProviderAdapter=submit+poll 两方法异步 port（submit 返回 taskId 或内联结果二选一）是干净的最小切面，缺 cancel/usage/health 维度。四层分层与 RouteSnapshot 上游零对应。详见 docs/reviews/mkfast-mkimage-source-xref-2026-07-20.md。
- Supersedes：无；细化 D-044（及其引用的 P1 模型供应规格）的模型目录、平台默认供给与网关边界，并作为 D-051/D-056 的 AI 供应能力域规范。（2026-07-20 血缘勘误：原误引 D-029，该条实为 Day-0 同界面，与模型供应无关。）

## D-059 多渠道路由：版本化发布策略 + 运行时健康护栏

- 日期：2026-07-19
- 状态：`accepted`（2026-07-19 用户选择推荐方案）
- 决定：多渠道路由首版采用“版本化发布策略 + 运行时健康护栏”，不退化为只有一条不可感知故障的静态主备链，也不让系统持续学习并无审核改写权重。管理员按能力/operation 发布 RoutePolicy revision，声明硬约束、候选 Deployment、顺序/档位、最大尝试、成本边界与回退授权；candidate→simulate/eval→approve→publish→rollback 沿用现有修订、CAS、审计和影响预览合同。
- 决定：硬约束先于排序，至少包括能力/参数兼容、激活证据、数据类别与区域、凭据归属/状态、模型与渠道生命周期、套餐/额度及明确禁用项；未通过硬约束的 Deployment 永不因质量、价格或实时健康分数而被选中。首版排序由已发布策略和经门禁的质量基线决定，成本/延迟/可靠性只使用可核验、单位明确且有新鲜度的输入，不拿 recorded 占位价格当生产事实。
- 决定：运行时可对已发布候选叠加短期 `healthy / degraded / cooldown / circuit_open / unavailable` 状态，依据明确的 429、5xx、连接、配额与探针/运行证据暂时排除或降级候选；overlay 有原因、来源、开始/过期时间和审计，只影响新任务，不修改 RoutePolicy revision。自动回退只允许在 Provider 明确 `rejected_before_accept` 且下一候选仍满足全部硬约束时发生；一旦已接受或接受状态未知，进入查询/对账/人工恢复，不跨渠道盲目重提。
- 决定：每次执行继续冻结 RouteSnapshot，记录策略修订、全部允许候选及排序、实际 Deployment、交易方/渠道、凭据/endpoint/价格/政策版本、运行时排除原因和回退链。质量评测或运行指标只能生成策略候选/告警，未经发布门不能永久改变路由；前台继续使用平台默认供给与简单质量档位，不暴露 Provider 参数。
- 原因：固定优先级无法及时避开真实故障，完全动态调权又难以解释、回放和验收；发布策略提供长期意图，健康护栏只负责短期安全避障，能同时维持可用性与确定性审计。
- 影响：现有按 `qualityRank` 排序和文案两候选回退需升级为类型化 RoutePolicy + ephemeral health overlay；管理员路由模拟器需展示硬过滤、排序、实时排除、最大成本和接受态分支。媒体首版仍可固定 Deployment，但复用同一策略/健康合同，为后续安全多渠道候选做准备。
- 证据边界：当前代码已有 candidate planning、RouteSnapshot、route simulation、最多两次尝试及接受态保护，但自动选择主要覆盖文案、排序维度单一、健康 overlay 和动态渠道装配尚未完整实现。
- 待验证：首批能力策略档位及可比较成本单位；429/5xx 的窗口、冷却与半开探针（D-080：初始值直接采用 LiteLLM router `cooldown_time`/`allowed_fails` 与 Envoy outlier detection 成熟默认，以带出处的配置常量落地，真实流量积累后按发布门流程校准，不自行标定）；跨区域/跨交易方 fallback 是否需额外授权；媒体 Provider 对“已接受/未知”的准确映射和对账 API。
- Supersedes：无；细化 D-044 所指 P1 模型供应规格允许的 Auto 路由边界，并继承 D-058 的 Deployment 选择与快照规范。（2026-07-20 血缘勘误：原误引 D-029。）

## D-060 供应商凭据：独立 Credential Account + 写入式密钥版本

- 日期：2026-07-19
- 状态：`accepted`（2026-07-19 用户选择推荐方案；2026-07-20 D-080 收敛生命周期为 `pending→active→retired` 三态主干，tested 为激活前置门、draining 仅异步媒体子状态）
- 决定：供应商 API 凭据从固定槽和 Deployment 内嵌配置中独立为 `CredentialAccount`。它表示一个实际供应商/中转商账号、项目或子账号及其结算/限流边界，可绑定一个或多个 ExecutionChannel/Deployment；数据库只保存账号标签、ProviderProfile、项目/区域、凭据类型、scope、secret reference、版本、状态、来源、验证时间、有效期与可公开限额等元数据，密钥值始终写入 Secret Manager/KMS，产品 API 不提供读取、导出或回显原值。
- 决定：凭据轮换采用 `pending → tested → ready → active → draining → retired/revoked` 生命周期。新版本先做最小付费/无副作用连通与能力探针，再显式切换绑定；旧版本保留到已冻结任务完成或达到排空截止时间后撤销。任务 RouteSnapshot/Attempt 只记录 credential account/version/reference，不记录密钥；轮换不追改历史快照，也不能让运行中任务静默换凭据。
- 决定：平台凭据与用户 strict BYOK 继续严格隔离为不同 CredentialAccount scope，BYOK 失败不回退平台凭据，平台任务也不读取用户凭据。环境变量只作为迁移期 `env_fallback`，后台需持续显示风险并支持迁移到 vault；Cloudflare Worker Secrets 只管理 Worker 自身运行密钥，不替代 Node Core 与多供应商的 CredentialAccount 注册表。
- 决定：凭据管理使用 D-057 的独立高敏权限，查看元数据、写入/轮换、测试、激活、排空和撤销分别审计；路由/模型发布者默认不能读取或修改密钥。连接测试返回规范化结果和证据，不把上游响应、Authorization、完整 endpoint query 或账号敏感信息写入日志。
- 原因：一把供应商密钥无法表达多个项目、分账、限流隔离和无损轮换；每个 Deployment 复制一把又会放大重复、泄漏和撤销成本。独立账号实体符合供应链分层，也能支持官方直连与中转渠道的不同结算/风险边界。
- 影响：现有 `model.direct / ark.media / douyin.platform` 三固定槽迁移为动态 CredentialAccount registry 与 binding；Provider runtime 需通过服务端 secret broker 按冻结版本装配，不再依赖单一进程环境变量作为唯一真相。管理 UI 提供账号元数据、绑定关系、版本状态、探针与轮换排空，不展示 secret 值。
- 证据边界：本条确定目标凭据模型和安全生命周期；具体 Secret Manager 产品、热加载/缓存 TTL、双版本并存实现及各供应商支持的 key/project scope 需结合部署平台和官方合同验证。现状校准（2026-07-20 交叉复核）：当前主装配源为裸 `'env'`（`providerCredentialEffectiveSource` 默认值），迁移目标才是受监控的 `env_fallback`；strict BYOK 无回退隔离已有既存实现（`FoundationStrictByokLedger`：`byok_strict`+`byok-strict-no-fallback-v1`+`providerRetryDisabled:true`，走 endpointProfile 路径），迁移到 CredentialAccount scope 时以其为基线扩展，不从零重建。
- 待验证：现有 vault/env 凭据迁移；中转商 token 与官方 project/key 的元数据差异；撤销前如何确认异步媒体任务已排空；供应商不支持 key 级用量/限额时的本地归因精度。
- Supersedes：无；细化 D-044 的平台默认凭据/BYOK 隔离与 D-058 的渠道部署绑定。

## D-061 上游渠道边界：按真实供应商管理，New API / Sub2API 仅作技术指纹

- 日期：2026-07-19
- 状态：`accepted`（2026-07-19 用户纠偏确认）
- 决定：当前 New API / Sub2API 不是本产品准备自建、嵌入或交给其托管产品真相的网关；它们是部分上游模型供应商用于对外提供 API 的后台框架。另一类上游是模型厂商官方 API。平台处于这些上游渠道与平台注册用户之间，负责采购、验证、编排和分配模型能力。
- 决定：`ProviderProfile` 的一等对象必须是真实签约、充值、开票、提供支持和处理数据的上游运营方；`CredentialAccount` 表示我方在该运营方取得的账户/token/余额与限额边界；`ExecutionChannel` 表示具体 API 入口。New API / Sub2API 只作为 `gatewayFingerprint`、可见版本、协议特征与风险标签等渠道元数据，不能替代运营方身份，也不能据此推断真实上游模型、授权、SLA 或数据政策。
- 决定：平台注册用户不直接分配上游 token、上游账号或网关余额，而是分配我方产品内部的 `Entitlement + UsageAllowance + ConcurrencyPolicy + RoutePolicy`。每个用户请求由平台在获准的 Deployment 中选择和冻结实际上游渠道；上游余额、RPM/TPM、并发和故障状态属于供应侧约束，用户套餐额度与权益属于产品侧真相，两者分别记账并通过分配策略关联。
- 决定：官方 API 与中转供应商是可以同时存在的不同 ExecutionChannel；是否可承载某个用户/任务由能力验证、质量、成本、余额/限额、健康、数据等级、来源可信度和套餐政策共同决定，不按“New API 或 Sub2API”二选一，也不把框架类型直接暴露给普通用户。
- 原因：若把框架名当供应商，会丢失真实交易方、合同、充值余额和数据处理责任；若把上游账号直接分给用户，又会泄漏供应关系并造成额度、成本、退款和故障切换多套真相。平台必须拥有从供应采购到用户权益分配的中间控制层。
- 影响：供应商后台需新增渠道档案、账户/充值与余额、模型/能力映射、价格版本、限额、健康、质量、数据政策和对账；用户/套餐后台需通过内部能力池分配模型档位、模态额度、并发、优先级和允许策略。New API/Sub2API 的探测与兼容性测试作为渠道接入证据，不作为产品对象层级。
- 证据边界：本条确认上下游关系和真相所有权；当前各上游是否能返回真实 usage、余额、request ID、模型版本和可核对账单，需逐渠道盘点与实测。框架指纹可能被修改或隐藏，不能仅靠接口外观可靠识别。
- 待验证：用户分配首版按套餐统一池还是支持用户级覆盖；哪些套餐允许成本较高的官方通道；上游余额不足、限流和故障如何影响用户可见承诺；第三方供应商的来源证明、退款规则与数据协议。
- Supersedes：纠正将 New API / Sub2API 视为本产品拟自建网关或一等 Provider 的误读；不改变 D-058～D-060 的供应链分层、路由与凭据安全合同。

## D-062 注册用户模型选择：模型可选，实际供应渠道隐藏

- 日期：2026-07-19
- 状态：`accepted`（2026-07-19 用户选择推荐方案）
- 决定：平台注册用户可以选择内部 `CatalogModel`；产品也可在适合的任务中提供推荐模型或质量档，但普通用户不选择、不持有也不看到具体 ProviderProfile、官方/第三方 ExecutionChannel、New API/Sub2API 技术指纹、上游账号/token、余额、内部价格或 fallback 顺序。
- 决定：用户固定选择某个 CatalogModel 时，RoutePolicy 只在该模型已发布且符合套餐、operation、数据政策和区域约束的 Deployment 中选择实际渠道；用户选择 Auto/质量档时，才允许策略同时选择 CatalogModel 与 Deployment。两种路径都冻结 RouteSnapshot，后台授权角色可按任务审计实际交易方、渠道、价格和接受态证据。
- 决定：供应渠道切换不改变用户的模型选择与产品额度。某渠道降级、限流、余额不足或停用时，新任务可以在同一 CatalogModel 的获准 Deployment 间安全切换；若没有合规可用候选，应明确告知模型暂不可用或让用户改选，而不是静默换成另一个模型。跨数据等级或来源信任等级的切换仍需 RoutePolicy 预先授权。
- 原因：模型身份会影响用户对质量、风格和能力的预期，保留模型选择可建立可理解的控制感；隐藏渠道则使平台能在不泄漏采购关系和凭据的前提下做成本、健康和容灾管理，也避免把 New API/Sub2API 等上游实现耦合进用户体验。
- 影响：用户模型页继续以 CatalogModel 为选择对象，不新增渠道下拉框；供应后台需要为同一模型维护多条 Deployment、能力一致性证据和渠道策略。账号详情展示“可用模型/质量档与来源政策”，只在支持/审计权限下展示单次任务实际渠道。
- 证据边界：本条确定用户可见选择与后台渠道隐藏边界；哪些套餐开放哪些 CatalogModel、默认推荐模型、Auto 是否在首版开放，以及同名模型经第三方渠道的真实性证明仍需后续分配与准入决策。
- 待验证：用户是否真正需要看到模型版本；模型暂不可用时的推荐替代交互；企业客户是否需显式 `official_only` 或专属渠道合同；渠道隐藏是否需同时提供不暴露交易方的数据处理等级说明。
- 上游对照（2026-07-20 反衬证据）：MkImage 的「provider 隐藏」仅是构建期前端开关（VITE_SHOW_IMAGE_PROVIDER），客户端偏好仍可穿透选中 provider 且解析结果明文入库、requested/resolved 不分离、env override 可静默覆盖用户选择——正是本条要排除的装饰性隐藏形态，印证治理性隐藏 + RouteSnapshot 可审计为真增量而非过度设计。详见 docs/reviews/mkfast-mkimage-source-xref-2026-07-20.md。
- Supersedes：无；细化 D-044 的前台模型选择与平台默认供给，并继承 D-058/D-059/D-061 的 Deployment 路由和上游渠道边界。（2026-07-20 血缘勘误：原误引 D-029。）

## D-063 注册账号分配：套餐默认 + 有期限的账号级覆盖

- 日期：2026-07-19
- 状态：`accepted`（2026-07-19 用户选择推荐方案）
- 决定：注册账号的模型能力与资源分配采用“套餐默认 + 账号级覆盖”。版本化 `EntitlementPolicy` 绑定套餐，统一声明允许的 CatalogModel/质量档、文案/图片/视频/音频额度、并发、速率、队列优先级、可用 SupplyPool、超额规则和有效期；套餐发布后批量形成账号默认权益，不逐用户复制一份静态配置。
- 决定：`AccountAllocation` 只表达显式例外，包括活动赠送、客服补偿、企业合同、灰度准入、风控限制和临时封禁。覆盖分为 grant/restrict，必须记录目标能力、增量或上限、来源、原因、创建者、起止时间、审批/审计和回退状态；到期后自动回到当前套餐默认，不永久漂移成无法解释的用户特例。
- 决定：有效权益按“平台安全/合规硬限制 > 套餐 EntitlementPolicy > 账号级批准覆盖 > 活动/临时额度 > 请求级合法偏好”计算，并提供变更前后预览。硬限制不能被 grant 覆盖；限制项优先于赠送项。管理员不直接编辑数据库余额或覆盖历史用量，而是发布权益 revision 或追加不可变额度调整事件。
- 决定：后台可以从注册用户详情进入分配管理，但真实计费与任务隔离继续绑定服务账号/工作区；当一个登录身份属于多个工作区时，必须明确选择目标工作区，不能把覆盖静默应用到其全部组织。该工作区选择仅出现在账号详情→分配管理的下钻流中，不进入一级导航或全局切换器，不改变 D-050/D-051 的一级信息架构边界。D-051 的用户主对象体验不因此抹掉底层 workspace 隔离。
- 原因：套餐默认能让绝大多数账号一致、可批量升级和可预测验收；有期限覆盖保留商业与支持所需的灵活性，同时避免逐用户手工配置造成维护爆炸、陈旧授权和无法审计的额度差异。
- 影响：现有 trial/starter/growth/pro 套餐配置需扩展模型/质量档与 SupplyPool 权限；用户/工作区详情新增 EffectiveEntitlement、来源解释、覆盖创建/到期/撤销和影响预览。用量、供应成本和支付继续分账，覆盖只改变未来授权或追加调整事件。
- 证据边界：本条确定分配机制，不在当前讨论中确定具体套餐价格、每档额度或营销权益；这些数值仍属后续商业/运营配置。现有系统已有套餐额度与工作区账本，但尚无动态模型池权限和结构化账号覆盖闭环。
- 待验证：覆盖是否需要双人审批的阈值；企业账号的 dedicated pool；套餐降级时进行中任务与已预留额度的处理；批量调整的最大影响范围与撤销窗口。
- Supersedes：细化 D-051 的账号管理、D-057 的权限合同、D-061 的供应/产品双侧真相与 D-062 的用户模型选择；对 D-050「workspaceId 不作为运营选择项」在账号分配下钻流内开受限例外（仅该流内显式选择目标工作区，一级 IA 边界不变）。

## D-064 官方与第三方渠道切换：按数据等级显式授权

- 日期：2026-07-19
- 状态：`accepted`（2026-07-19 用户选择推荐方案）
- 决定：同一 CatalogModel 的官方 API 与第三方上游 Deployment 可以进入同一候选体系，但是否允许跨来源选择或 fallback 由版本化数据政策显式授权，不采用“永不跨来源”的绝对隔离，也不采用所有任务默认自由切换。
- 决定：每个任务在规划时确定 `dataClass`，每个 Deployment 绑定 `DataPolicyRevision`、来源信任等级、处理区域、留存/训练/子处理者合同和允许的数据类。RoutePolicy 先做硬过滤，再参与健康、质量与成本排序；低敏/公开数据可在已验证且策略授权的官方与第三方渠道间切换，`contains_face`、`pii`、`medical/health` 等受限数据只允许进入合同和技术证据均已批准的 Deployment。
- 决定：跨来源 fallback 仍受 D-059 的接受态保护；只有上游明确在接单前拒绝、下一候选满足同一 CatalogModel、operation、用户权益和全部数据硬约束时才允许。内容安全/合规拒绝不能通过换供应商绕过；已接单或接受状态未知时不重提。没有合规候选时失败关闭并给出可操作说明，不为提高成功率静默降低数据保护等级。
- 决定：普通用户仍遵循 D-062 看模型、不看渠道；后台 RouteSnapshot 必须保存实际交易方、来源种类、数据政策 revision、选择/排除理由和 fallback 证据。前台可展示不泄漏采购方的“数据处理等级/受保护通道”说明；企业合同若要求 `official_only`、特定区域或专属池，以套餐/账号硬约束表达。
- 原因：完全分池会损失同模型容灾和供给弹性，完全自由切换又会让人脸与个人信息经过未经批准的数据处理方。数据等级授权把决定建立在任务风险和真实合同上，而不是建立在框架名、模型名或单纯价格上。
- 影响：渠道注册必须补齐 DataPolicyRevision 和证据有效期；任务入口需要可靠的数据分类与人工纠错路径；路由模拟器需展示因数据政策排除的候选，用户/套餐后台可配置 official-only/region/dedicated-pool 等硬限制，但不能选择具体供应商 token。
- 证据边界：本条确定策略结构，不声称任何现有第三方上游已经获准处理人脸、PII 或健康数据；每家供应商仍需合同、数据流、留存和实际探针逐项准入。数据分类规则本身也需结合素材授权与后续合规评审验证。本条 `medical/health` 指内容数据的敏感度分类（含皮肤/健康信息的照片等），不等同 D-025 的医美品类边界；首轮不因存在该数据类而承接医美内容。
- 待验证：美业照片中 `contains_face` 与潜在健康/皮肤信息的精确分类；匿名化/裁剪后的降级规则；用户隐私告知与同意；区域要求；供应商合同变化后的批量隔离与进行中异步任务处置。
- Supersedes：无；细化 D-059 的跨交易方 fallback 待验证项，并继承 D-061/D-062 的供应渠道隐藏与用户模型选择边界。

## D-065 合规候选排序：质量与可靠性过门后再优化成本

- 日期：2026-07-19
- 状态：`accepted`（2026-07-19 用户选择推荐方案）
- 决定：当用户已选定 CatalogModel 且 D-064 允许多个 Deployment 时，默认路由不固定官方优先，也不采用最低报价优先，而使用“质量/可靠性门禁 → 健康与容量护栏 → 可对账成本优化”的分层选择。不同 operation/质量档发布各自 RoutePolicy revision，不能用一个全局权重覆盖文本、图片、视频和音频。
- 决定：候选首先通过能力 conformance、来源/模型映射可信度、数据政策、近期激活证据、版本化质量基线、成功率、p95 延迟、错误/接受态完整性等硬门槛；任一关键证据缺失、过期或低于阈值时排除或仅保留 canary，不因价格低而进入主流量。运行时再叠加 circuit、限流、余额/配额、并发和容量 headroom，避免把请求送到当前不可兑现的候选。
- 决定：只有剩余候选均能兑现同一用户承诺后，才按规范化供应成本、失败成本、延迟和集中度做可解释排序/配流。正式账单/结算明细优于上游 usage，observed usage 优于网关估算；只有 estimate 的渠道必须保留风险折扣和流量上限，不能用 New API/Sub2API 的内部倍率、余额或标价直接证明其真实最低成本。
- 决定：权重、阈值与流量比例沿用 D-059 的 candidate→simulate/eval→approve→publish→rollback，不由在线指标无审核永久自改；实时健康只做有期限 overlay。路由模拟与任务审计要展示硬门禁、证据新鲜度、成本来源、最终排序及未选原因。
- 原因：官方优先会忽略已验证第三方渠道的成本与容量价值，最低价优先会把协议失真、隐藏重试、失败成本和来源风险伪装成利润。先守住同一产品承诺，再优化供应成本，才能让用户模型选择稳定并使渠道采购具备可控毛利。
- 影响：需要建立按 Deployment/operation 的质量、成功率、p95、接受态、真实/估算成本和容量 read model；SupplyPool/RoutePolicy 管理页提供阈值、排序、流量上限与差分模拟；供应成本对账完成前只允许低比例 canary 或保守排序。
- 证据边界：本条确定排序原则，不预设各模型与模态的具体阈值、权重或最低毛利；这些数值需真实 probe/eval、运行指标和结算数据校准。当前代码主要按 `qualityRank` 排序，尚未完整实现这些输入。
- 待验证：不同 operation 的质量指标；成本统一单位和汇率/税费；失败/重复任务成本；渠道集中度上限；低样本新渠道 canary 比例；余额不可查询时的容量风险估计。
- Supersedes：无；闭合 D-059 的首批策略档位方向，并继承 D-064 的数据硬约束和 D-061 的供应成本/产品权益分离。

## D-066 上游供应账户隔离：共享池为默认，专属池为显式例外

- 日期：2026-07-19
- 状态：`accepted`（2026-07-19 用户选择推荐方案；2026-07-20 D-080 确认 SupplyPool 维持一等实体——专属池是代运营/陪跑高价值客户的核心交付服务，非边缘例外）
- 决定：平台持有的上游供应商账户/token 默认作为共享 `CredentialAccount + SupplyPool` 服务多个注册账号，不为每个用户创建或分发一个上游账户。套餐与账号级 Entitlement/Concurrency/Rate/QueuePolicy 在产品侧隔离使用权，供应侧则统一利用账号余额、RPM/TPM、并发与渠道容量。
- 决定：企业合同、指定区域、受限数据、专属账单、保底容量或强隔离需求通过显式 `DedicatedSupplyPool` 例外实现，绑定一个或多个专用 CredentialAccount/Deployment，并由 AccountAllocation 或合同权益授权。专属池是否允许回退共享池、共享池是否允许回退专属池均默认否，只有合同与数据政策明确授权才可发布。
- 决定：共享账户不等于共享或不可归因的账本。每次请求仍冻结 workspace/account、RouteSnapshot、CredentialAccount/version、supplier request/task ID、usage 与价格 revision，分别写产品用量账本与供应成本账本（ProductUsageLedger/ProviderCostLedger 为目标口径，即现有 GrantLot 额度账本与 `FoundationModelSupplyLedger.appendProviderCost` 的演进，复用其 schema，不另建重名账本）；上游只能提供账户总余额时，未能归因的对账差异保留为 supplier-level variance，不强摊给用户。
- 决定：共享池必须有防 noisy-neighbor 的账号/工作区并发、速率和公平排队；供应账户级限制、产品账号级限制和系统总容量分别建模。某一用户超额或异常不能耗尽整条供应渠道，也不能通过增加多个产品账号绕过上游共享限额。现状校准（2026-07-20）：产品账号侧 per-workspace 并发门与套餐级 queuePriority 已落地，本条新增的是供应账户级容量隔离与公平排队，不重复建设产品侧限流。
- 原因：逐用户上游开户会把充值、密钥、余额、轮换和回收成本线性放大；全局共享且无专属例外又无法承诺企业隔离与容量。共享池加显式专属池能兼顾供给利用率、可运营性和合同级隔离。
- 影响：CredentialAccount 增加 `poolScope=shared|dedicated`、容量/账单作用域与绑定关系；SupplyPool 管理页展示共享覆盖、专属消费者、容量占用和集中度；账号详情只展示获得的产品能力和是否处于专属供给合同，不展示上游 token。
- 证据边界：本条确定账户隔离模式，不预设现有任何供应商允许多租户转售或共享其账号；每个官方 API 与第三方上游的合同、限流作用域和转售许可仍需验证。当前系统尚未实现动态多账户池和跨实例公平调度。
- 待验证：共享池容量预留算法；企业专属池最低容量与空闲复用；恶意/异常流量隔离；账号级限流无法读取时的估算；供应商禁止转售或要求终端用户隔离时的替代方案。
- 上游对照（2026-07-20）：MkImage `credit_transaction` 批次账本（每笔授予带 remainingAmount+expirationDate、消费按批次扣减）印证 GrantLot 骨架方向；退款幂等唯一索引直接可抄（细节见 D-045 上游对照）。两条实施要求带入双账本票：①消费顺序按到期时间优先，不抄上游按 createdAt FIFO 导致批次白白过期的缺陷（与 D-045 已定 `expirationDate ASC NULLS LAST` 口径一致）；②授予/核销路径设独立幂等键，不抄上游仅退款幂等的单边设计。ProviderCostLedger 上游零对应（全仓无上游货币成本字段），印证自建。详见 docs/reviews/mkfast-mkimage-source-xref-2026-07-20.md。
- Supersedes：无；细化 D-060 的 CredentialAccount 绑定、D-063 的账号权益覆盖和 D-065 的容量路由输入。

## D-067 多渠道供应首个开发增量：官方 + 第三方 + 用户分配端到端闭环

- 日期：2026-07-19
- 状态：`superseded`（部分：仅「首增量只用同步文本」的范围被 D-068 取代，端到端闭环结构与 MP-01~MP-08 模块划分继续有效；保留首次裁决历史）
- 决定：多渠道模型供应管理的首个开发增量采用端到端最小闭环，不先交付只有页面和记录、运行时仍为单槽位的“供应商后台”，也不先交付继续依赖单一通道的用户分配。首个验收样本至少包含一条模型厂商官方 API 与一条真实第三方上游 API；后者可使用 New API/Sub2API，但我方只管理实际运营商渠道和技术指纹。
- 决定：闭环依次贯通：结构化 ProviderProfile/SupplyContract → CredentialAccount secret 写入/测试 → ExecutionChannel/endpoint revision → provider alias 映射 CatalogModel → operation conformance/数据政策/价格证据 → Deployment 审批发布 → SupplyPool/RoutePolicy revision → 套餐默认与测试账号覆盖 → 用户选择 CatalogModel 发起真实任务 → RouteSnapshot 与 ProductUsage/ProviderCost 分账 → 后台任务/渠道审计下钻。
- 决定：发布后 HTTP 与 Worker 必须读取同一 effective revision，新增、隔离、排空或停用渠道不依赖进程重启；让一条渠道进入 degraded/circuit_open 后，新任务仅在 D-059/D-064 允许且未接单的边界内切到另一条候选，进行中任务继续按冻结快照恢复或对账。无法证明接受态时不得以“容灾测试”为名重复提交。
- 决定：首个增量用同步文本/文案 operation 验证动态供给控制面，优先选择能形成同一 CatalogModel 两条 Deployment 的真实渠道；图片、视频、音频的 submit/poll/cancel/download、异步排空和多渠道 fallback 不在本增量伪装完成，统一进入第三项媒体生成功能专项。首轮也不包含自动充值、全供应商账单适配、在线自学习调权或自建 New API/Sub2API。
- 原因：当前真正缺口是目录、凭据、运行时、用户权益和账本之间没有动态闭环；只做控制台或只做分配都会继续产生“看得到但调用不到”的死配置。两类真实来源的一条薄主链能最早暴露协议、热装配、接受态与对账边界，并为后续横向扩展保留可信基线。
- 影响：首批票按 MP-01 动态供应实体/迁移、MP-02 通用凭据与 secret broker、MP-03 HTTP/Worker 版本化热装配、MP-04 官方/第三方 adapter conformance、MP-05 SupplyPool/RoutePolicy 发布、MP-06 套餐与 AccountAllocation、MP-07 任务审计与双账本、MP-08 端到端/故障注入验收拆分；后台页面只实现支撑这条主链所需的结构化表单、状态与影响预览。
- 证据边界：本条确定开发切片与验收路径，不预先指定具体官方厂商、第三方运营商或测试模型；真实凭据、供应合同与允许的低敏测试数据需在实施前确认。当前代码已有 Catalog/RouteSnapshot/探针/账本底座，但单 direct LLM 槽、固定凭据槽和重启生效不满足验收。
- 待验证：两条测试渠道能否提供同一模型身份；第三方隐藏 retry/usage/request ID 的透明度；热装配的缓存失效与滚动兼容；真实 Postgres/Worker 故障注入环境；最小付费探针预算。
- Supersedes：无；作为 D-058～D-066 的首个实现切片，并延续 D-056“先建立统一控制面合同、避免孤岛页面”的原则。

## D-068 多渠道供应首轮范围：文本、图片、视频三模态同时闭环

- 日期：2026-07-19
- 状态：`accepted`（2026-07-19 用户明确纠偏）
- 决定：多渠道模型供应管理的首轮开发仍采用 D-067 的端到端主链，但范围必须同时覆盖文本、图片和视频，不能先用同步文本验证后把图片/视频留到第二轮。音频未包含在本次明确范围中，继续作为后续独立能力；第三项“视频、图片生成功能升级”是在首轮接通真实供应控制面基础上继续完善产品能力，不承担补做本应在首轮完成的渠道接入。
- 决定：首轮 operation 至少覆盖产品现有真实主链中的文本生成/改写、图片生成/编辑和视频生成。三类任务都必须经过统一 ProviderProfile/SupplyContract/CredentialAccount/ExecutionChannel/Deployment、能力/数据/价格证据、SupplyPool/RoutePolicy、套餐与账号分配、RouteSnapshot、ProductUsageLedger/ProviderCostLedger 和后台审计，不允许媒体继续作为只靠环境变量、固定 provider mode 或目录 recorded 项的旁路。
- 决定：图片与视频在首轮必须实现并验收原生异步副作用合同，包括 submit、provider task ID、acceptance、recover/query、poll 或已验证 callback、cancel、download、自有资产持久化、供应商 URL TTL、排空、晚到终态、幂等与成本结算；Provider 接受或接受状态未知时禁止跨渠道盲目重提。每个准备宣称可生产的 operation 至少有一条 `live_verified` Deployment；若宣称多渠道容灾，则必须有两条独立且获准的 Deployment 并通过真实故障注入，单通道 operation 必须明确显示无 fallback。
- 决定：首轮整体必须同时纳入至少一条 `official_direct` 与一条 `upstream_reseller` 来源，验证两类渠道可以通过同一控制面采购、发布、分配和审计；不要求为凑数量而让每个模态接入不合格渠道。每个具体模态的供应商数量、同模型双 Deployment 可得性与数据合同在实施盘点后确定，但不能以此删掉该模态的端到端交付。
- 决定：D-067 的 MP-04 拆为 MP-04T 文本 adapter/conformance、MP-04I 图片 lifecycle/conformance、MP-04V 视频 lifecycle/conformance；MP-03 热装配、MP-05 路由发布、MP-06 用户分配、MP-07 双账本/审计、MP-08 故障注入必须同时覆盖三模态。验收账号需从后台获得三模态权益，分别完成真实任务并可追溯到冻结渠道、凭据/价格/政策 revision 与最终资产。
- 原因：文本、图片和视频都是产品当前核心交付面；若首轮只打通文本，媒体仍停留在固定环境变量、分散凭据和不可动态治理状态，多渠道后台会成为半成品，第三项升级也会被迫先返工供应底座。
- 影响：首轮范围、测试预算、真实凭据和故障注入复杂度上升，但三模态共用同一对象模型、发布流、账号分配和审计，不另建三套后台。开发拆票可并行，最终只能在三条主链共同通过后宣称多渠道供应首轮完成。
- 证据边界：当前代码已有 Ark/Tuzi 媒体 lifecycle、Volcengine TTS 及文本执行底座，但图片/视频供应覆盖、凭据后台和发布门禁不完整；本条是交付范围决定，不代表现有 recorded 目录项或 README 声称支持即可计为完成。
- 待验证：每个模态可用于验收的官方/第三方真实渠道与预算；图片编辑和视频取消的上游合同；素材含人脸时的 D-064 准入；长任务排空与凭据轮换；三模态共享 Pool 的容量/成本单位与后台呈现。
- 上游对照（2026-07-20）：MkImage Cloudflare Workflow 图片编排的步骤切分思路可参照——积分扣减为首个 durable 步骤、provider-completed 与 asset-persisted 分离（R2 落盘 non-fatal 单独重试）、失败补偿退额、ErrorCategory 错误分类；但其 step 为 impure（步内直接 DB 写 + 副作用），与 D-038 纯函数内核约束冲突，切分思路参照、执行模型不抄。其无 workflow 终止/取消入口的 force-fail 竞态缺口（存在半年未修），反面印证本条 cancel/排空/晚到终态合同的必要性。详见 docs/reviews/mkfast-mkimage-source-xref-2026-07-20.md。
- Supersedes：D-067 中“首个增量只用同步文本、媒体统一进入第三项专项”的范围；保留其端到端闭环、动态热装配、真实渠道与不自建 New API/Sub2API 的其余决定。

## D-069 首轮多渠道验收强度：三项核心操作各双渠道

- 日期：2026-07-19
- 状态：`accepted`（2026-07-19 用户选择推荐方案；2026-07-20 交叉复核提出「媒体双渠道故障注入移入下一增量」的收窄建议，被用户否决——三核心 operation 是主力生产功能，供应可行性未经验证则交付本身存疑，验收强度维持原样，见 D-080）
- 决定：D-068 首轮中的文本生成、图片生成、视频生成三项核心 operation 各至少有两条独立、真实、获准且 `live_verified` 的 Deployment；文本改写/适配、图片编辑等首轮次级 operation 至少有一条真实 `live_verified` Deployment。次级操作即使只有单通道也必须完成统一注册、分配、RouteSnapshot、账本和生命周期，不得退回环境变量旁路。
- 决定：“双渠道”不能用同一供应商账户的两个 token、同一 endpoint 的两个别名或同一网关内不可辨识的两个 channel 充数。至少区分两个可独立隔离的交易方或 ExecutionChannel 故障域，并记录底层模型制造商/区域是否仍共享；若两条渠道最终依赖同一官方模型厂商，只能声称渠道级容灾，不能声称上游制造商级容灾。
- 决定：双渠道验收至少对每个核心 operation 证明：两条候选均能独立完成真实任务和成本归因；用户选定同一 CatalogModel 时可在其两条合规 Deployment 间选路；主通道接单前失败后按策略切换；已接受/未知时不重提；隔离/排空一条后新任务无重启使用另一条；RouteSnapshot、异步 task/asset、usage 与 ProviderCost 可分别回放。
- 决定：不要求首轮把每一个可选 CatalogModel 都做成双渠道；每个核心 operation 至少有一个平台主推 CatalogModel 达到双渠道发布门。其余模型必须在用户选择页和后台明确标记 single-channel/no-fallback，不能借整体“三模态多渠道”口径掩盖单模型缺口。
- 原因：每模态只有一条通道只能证明统一目录接通，不能证明路由、隔离、故障切换和渠道隐藏；要求每个次级 operation 也双渠道又会把长尾协议覆盖变成首轮阻塞。核心双渠道、次级真实单渠道是能验证架构且不伪装范围的中间强度。
- 影响：MP-04T/MP-04I/MP-04V 各增加双渠道 conformance 与故障注入矩阵；后台能力页按 operation/CatalogModel 展示渠道数、独立故障域、数据等级覆盖和 fallback readiness；发布门阻止核心 operation 在少于两条合格 Deployment 时标记为 multi-channel ready。
- 证据边界：本条是验收数量和独立性口径，不指定具体供应商组合，也不保证两条渠道都能承载所有受限数据类。D-064 仍优先；若含人脸任务只有一条获准渠道，该数据等级必须显示 single-channel，而不能为满足数量降低准入。
- 待验证：三项主推 CatalogModel 的双渠道可得性；供应商之间是否共享底层上游或区域故障域；真实故障注入预算；图片/视频两渠道输出质量可比性与异步取消合同。
- 上游对照（2026-07-20 反面印证）：MkImage adapter 层零测试、无 provider 契约测试与故障注入，force-fail 竞态缺口长期未修——同栈产品「不验证供应可行性」的实际后果，佐证本条维持三核心 operation 双渠道 + 真实故障注入的验收强度（即 D-080 C5 否决收窄的裁决）。详见 docs/reviews/mkfast-mkimage-source-xref-2026-07-20.md。
- Supersedes：细化 D-068 对“每个生产 operation 至少一条、宣称容灾时两条”的开放口径；三项核心 operation 的双渠道不再可选。

## D-070 模型供应与网关控制中心：后台可视化的核心模块

- 日期：2026-07-19
- 状态：`accepted`（2026-07-19 用户纠正并拍板）
- 决定：多渠道模型供应与网关管理不是独立于后台可视化之外的技术附属页，而是 D-051 系统能力指挥台中的**核心一级能力模块**，统一命名为“模型供应与网关控制中心”。它在现有管理壳内管理全部 `ExecutionChannel`，同时覆盖官方直连、真实第三方中转、可选内部网关和不同区域/协议/账号；“网关”不等同于 Bifrost、Higress、New API 或 Sub2API 某一套软件。
- 决定：该模块必须形成总览与下钻闭环，至少可视化文本/图片/视频 operation readiness、核心模型双渠道覆盖、Provider/SupplyContract/CredentialAccount/Channel/Deployment/CatalogModel 关系、Pool/RoutePolicy 与生效 revision、数据等级、健康/容量/余额/限额/成本、同步 attempt 与异步媒体任务生命周期、受影响账号/任务、最近变更和统一审计。外部网关自带 Console 只能作为技术证据深链，不能成为日常管理主入口或第二业务真相。
- 决定：首轮提供受治理的快捷操作，包括连通与 conformance 探针、候选配置验证、路由模拟、发布/回滚、渠道隔离/恢复、停止接收新任务、排空、凭据轮换与撤销前影响检查、健康/余额刷新。所有动作必须调用 Product Core 类型化命令，满足 capability permission、目标与影响预览、原因、CAS/幂等、可逆/排空语义和不可变审计；不得暴露密钥原值、直写数据库、绕过发布门，或对 `accepted` / `acceptance_unknown` 的图片视频任务执行盲目重试。Cloudflare 控制面写操作继续受 D-053 禁止。
- 原因：供应商、凭据、渠道、路由、健康、容量与成本共同决定文本、图片和视频是否真实可交付；若只在外部网关控制台或分散配置页查看，运营人员无法理解产品影响，也无法在同一证据链中完成安全隔离、排空、恢复和回滚。
- 影响：后台信息架构必须把本模块放入 AI/创作供应能力的一级入口，并与系统总览异常、账号权益、任务、双账本和审计互相下钻；开源网关和低代码后台的控制台只用于借鉴信息架构与交互，不替代现有 TanStack 管理壳。后续开发票需同时交付可视化页面、Core command、权限、审计和三模态真实运行证据，不能只做网关配置 CRUD。
- 证据边界：开源组件比选见 `references/analysis/provider-control-plane-selection-2026-07-19/`；当前已有模型、凭据、激活探针、目录发布/回滚、路由模拟、质量和健康能力（分布在 models/integrations/audit 三个路由页内的区块，非各自独立页面），但尚未形成上述统一模块、动态渠道热装配和完整快捷动作矩阵。
- 待验证：模块一级页面是单一指挥台还是总览加多个任务页；快捷动作逐项的权限、确认强度、回滚/恢复时限和批量操作边界；三模态高密度运行时间线及关系视图是否需要 ECharts/React Flow；Higress/Bifrost 等技术 Console 的安全深链方式。
- 上游对照（2026-07-20）：两件交互零件可作本模块下钻页参照——①MkImage 任务详情版式（摘要卡 + 延迟四格拆解 + 时间戳重建时间线 + 错误徽章/原文折叠 + 产物预览）即三模态任务下钻详情模板；②高密度运行表骨架（多维 faceted 筛选 + 服务端分页排序 + nuqs URL 状态同步，刷新/分享保持筛选态）即 operation/渠道运行表模板。上游无「多渠道供应」概念（单层 provider 枚举），控制中心本体零对应。详见 docs/reviews/mkfast-mkimage-source-xref-2026-07-20.md。
- Supersedes：无；细化 D-048/D-051 的一体化能力后台、D-056 的首版骨架与 D-058～D-069 的多渠道供应控制面。

## D-071 多渠道供应控制面组件组合：自有核心 + 分层复用

- 日期：2026-07-20
- 状态：`accepted`（2026-07-20 用户确认推荐组合）
- 决定：多渠道模型供应管理正式采用“**自有 Product Core 控制面 + 原生后台核心模块 + 分层局部复用**”。Product Core 继续作为 ProviderProfile、SupplyContract、CredentialAccount 元数据、ExecutionChannel、Deployment、SupplyPool、RoutePolicy、RouteSnapshot、接受态、媒体生命周期、用户权益、产品用量、供应成本和审计的唯一真相；D-070“模型供应与网关控制中心”继续由现有 TanStack/shadcn 管理壳原生承载，不引入第二套业务控制台。
- 决定：官方 API 与真实第三方上游的原生 adapter 是生产执行基线。Bifrost 只保留为固定 Deployment、零跨 Deployment retry/fallback、关闭内容日志且可删除的隔离 PoC；Higress 只有在项目另行明确接受外部 Kubernetes 运行面后，才能做同时覆盖文本、图片、视频的条件性执行网关 PoC，当前不进入 P0 生产依赖。任何 PoC 均不得缩减 D-068/D-069 的三模态首轮范围，也不得接管跨渠道路由、接受态、媒体恢复或双账本。
- 决定：LiteLLM 只作 Provider 适配与价格目录对照；APISIX、Envoy AI Gateway、Kong、Portkey、Helicone 维持研究包中的 Reference/Deferred/Reject 边界。New API/Sub2API 仅作为真实第三方供应渠道的技术指纹和交互参考，不由我方部署或二次开发为产品底座。Refine、React-admin、Directus、Appsmith、ToolJet 不替代现有管理壳；OpenMeter、OpenFGA/OPA/Cedar、OpenBao/Infisical 仅在各自触发条件成立后重新评估（Casbin 同此档，完整候选见 policy-rbac.md；OPA/Cedar 的结构化 decision/reason-code 合同已作 P0 设计借鉴，仅独立运行时依赖延期），P0 保留现有双账本、类型化权限/策略和 SecretStore/AWS Secrets Manager。
- 原因：所有整套候选都无法同时覆盖已确认的文本/图片/视频双渠道、异步媒体副作用、`rejected_before_accept/accepted/acceptance_unknown`、动态渠道热装配、套餐/账号分配、凭据排空、双账本和统一审计；整体采用会形成第二路由、第二权限/额度或第二业务真相，并增加与 Cloudflare Workers 不匹配的常驻服务或 Kubernetes 运维面。
- 影响：首轮工程投入集中到现有 Core 的动态供应注册、热装配、三模态 adapter/conformance、发布/隔离/排空/恢复、原生控制中心、权限审计和真实故障注入；外部组件按固定端口、单向数据和可删除原则接入。后续选型不得仅因候选增加模型数量、图形控制台或自动 fallback 就改变这一边界，任何升级为生产依赖都需独立 ADR、真实 PoC 与退出演练。
- 证据边界：完整比选与官方来源见 `references/analysis/provider-control-plane-selection-2026-07-19/`，既有 Bifrost/LiteLLM/New API/Sub2API 深度研究见 `references/analysis/model-provider-management-2026-07-19/gateway-components.md`；本条锁定采用策略，不代表 Bifrost/Higress 已通过生产 PoC，也不代表 MP-01～MP-08 已开发完成。
- 待验证：Bifrost 固定 Deployment PoC 的协议、错误、usage、上游证据、升级和回滚门；是否存在足以承担 Kubernetes 运维成本的 Higress 三模态收益；各真实官方/第三方渠道的凭据、数据合同、预算和故障注入条件。
- Supersedes：无；把 `references/analysis/provider-control-plane-selection-2026-07-19/` 的推荐组合转为正式决策，并继承 D-058～D-070 的全部产品边界。

## D-072 下一轮讨论顺序调整：先统一用户旅程与 UI，再深化图片视频能力

- 日期：2026-07-20
- 状态：`accepted`（2026-07-20 用户明确调整优先级与对标范围）
- 决定：在后台可视化与多渠道模型供应的本轮讨论之后，下一项先讨论并收敛用户旅程、交互体验与 UI 升级，再回到图片、视频生成功能的能力升级与完善。当前阶段先形成产品旅程、页面职责、状态呈现和交互合同，不直接把竞品页面拆解等同于开发实现。
- 决定：图片与视频生成流程重点对标小云雀和 CreatOK；图文笔记、小红书内容与文案创作流程重点对标讯飞绘文。调研优先复用本地 Reference 与用户现有登录态，以 OpenCLI 做只读增量验证；只把可复核的新证据保存到 `references/`，不为重复事实重新抓取或重复写文档。
- 决定：所有产品的比较统一按“任务入口 → 意图/素材输入 → 默认值与高级配置 → 提交/成本确认 → 异步等待 → 结果交付 → 自由调整/结构化快捷修改 → 资产、历史与再利用”分段；结论必须区分“直接借鉴、结合本产品改造、明确不采用”。现行 D-029/D-031/D-043/D-046 继续作为我方边界：Day-0 与 Day-N 同界面、主路径不回退到多字段启动表单、默认值折叠、结果阶段常驻自由文本调整口。
- 原因：当前缺项不只是视觉样式，而是文本、图片和视频能力在同一产品中的入口、等待、交付、修改和资产复用尚需统一；先把用户旅程定清，才能避免后续媒体能力升级继续把工程参数、模型与状态直接投影成碎片化页面。
- 影响：本轮先产出当前产品真相、三款对标产品的增量证据、分阶段旅程矩阵、差距优先级和待拍板项；形成明确决策后再拆开发票。图片/视频能力升级仍是首轮产品范围，不因讨论顺序调整而推迟或降级，也不改变 D-068/D-069 对多渠道供应首轮三模态闭环的要求。
- 证据边界：只读页面走查不提交生成、发布、购买或授权动作，因此可以验证入口、配置、导航、可见状态和历史/资产结构，不能据此证明生成质量、真实成本、排队时延、失败恢复或发布成功率；这些需在后续获准的真实任务验收中补证。
- 待验证：我方当前真机从冷态到首个候选的实际点击与理解成本；三种内容旅程是否应共享一个 Composer 壳还是在结果/编辑阶段分化；视频大额确认与图片/文案默认直发的边界；移动端等待与结果修改；图文长内容编辑器和统一 ContentPackage 的衔接方式。
- Supersedes：仅调整 D-047 中第三、第四项的讨论先后——用户旅程/交互/UI 提前到图片视频能力升级之前；D-047 的四项范围及其余决定不变。

## D-073 用户旅程总结构：统一创作入口、三类成品工作区、共享底层合同

- 日期：2026-07-20
- 状态：`accepted`（2026-07-20 用户明确接受）
- 决定：产品采用“一个统一创作入口 + 三类成品工作区（文案/图文、图片、视频）+ 一套共享任务、历史、素材和调整合同”。文本、图片、视频均属于首轮完整产品范围；本决定只规定共同壳与分化边界，不将任何模态推迟到第二轮。
- 决定：统一“如何开始、如何等待、如何恢复、如何追溯”，由同一 Composer、Brief、Task/Result Shell、Recent/历史投影和来源血缘承接；分化“如何看结果、如何修改、如何交付”，分别提供文案/图文编辑与平台预览、图片大预览与套图调整、视频播放器与分镜/字幕/封面工作面。
- 决定：统一入口的首层语言以商家交付物和高频任务为主，不以模型品牌、模态术语或工作流节点为主；模型、比例、时长、规格、seed 和运行证据按需进入“本次设置/运行详情”。图片和视频必须在冷态首层可发现，但不因此建立三套孤立首页、历史或资产真相。
- 决定：现有 Task、Work、Job、Asset、ContentPackage、RouteSnapshot 及派生 revision 继续作为唯一业务合同；三类成品工作区只能消费和呈现这些 canonical 对象，不另建第二任务、第二内容、第二额度或第二发布状态。
- 原因：当前核心摩擦来自入口层分类争夺与结果层成品感不足，而不是底层缺少任务和血缘合同。共同壳可以降低开始、等待和恢复的学习成本，结果分化可以让图文、图片、视频获得符合媒介的编辑与交付体验，同时避免三套工具继续碎片化。
- 影响：后续 UI 票据先围绕首屏 Composer 信息架构、共享 Task/Result Shell 和三类成品工作区拆分；首轮验收必须同时覆盖文本、图片、视频的可发现、提交、等待、结果、调整、保存/交付和恢复，不以某一模态完成代替整轮完成。
- 证据边界：本决定基于当前代码/测试真相及 2026-07-20 对当前产品、小云雀、CreatOK、讯飞绘文的只读增量审计；尚未用真实付费任务验证图片/视频质量、排队、扣费、失败恢复和发布成功率，因此这些运行指标仍需后续真机验收。
- 待继续拍板：首屏 Composer 已由 D-074～D-084 关闭，条件 Brief 与高成本确认由 D-088/D-094 关闭，移动端轻编辑与三类结果动作由 D-085/D-087 关闭，平台交付边界与微合同由 D-086/D-096 关闭；本条 UI/用户旅程开放项已全部闭合。
- Supersedes：解决 D-072“是否共享一个 Composer 壳还是在结果/编辑阶段分化”的结构问题；D-029/D-031/D-043/D-046、D-068/D-069 和 D-072 其余边界继续有效。

## D-074 首屏 Composer 采用意图先行方向

- 日期：2026-07-20
- 状态：`accepted`（2026-07-20 用户在三种首屏概念中明确选择方案 1）
- 决定：冷态首屏采用“意图先行 Composer”。用户首先用一句自然语言表达今天想发布什么；系统在同一输入壳内推断并显示可修改的“平台 + 交付物”，同时保留 `+ 来源`、`本次设置`、预计总消耗和唯一有文字标签的主 CTA。大 Hero 与“今天值得发什么”大卡压缩为轻量问候和一条可解释推荐，Composer 必须在首屏完整可见。
- 决定：Composer 下方最多提供 4～6 个商家任务快捷入口，用于降低空白输入成本和保持文本、图片、视频可发现；快捷入口、场景、模板、推荐和基于此再创作只能补充上下文或提出替换预览，不得静默覆盖用户已有输入。示例与 Recent 继续位于其后，并按当前推断交付物过滤。
- 决定：不采用“交付物货架”作为首屏第一视觉中心，也不采用双栏可视 Brief 作为所有任务的冷态默认。前者只保留为快捷入口，后者只在多模态、高成本、低置信度、素材授权或合同冲突时原位条件展开；普通文案和低成本图片继续由主 CTA 合并确认并直接提交。
- 决定：概念图只锁定信息层级和交互主轴，不是最终像素、文案或新增导航依据；实现继续复用现有白瓷/玻璃、字体、状态色、焦点、明暗主题和真实素材授权体系，不从竞品或生成图另起视觉系统。
- 原因：意图先行保留了统一入口的最短路径，既能承接明确任务和模糊需求，也能把 Agent 推断隐藏在商家语言之后；将交付物快捷入口和条件 Brief 分别放在第二层与必要状态，能够兼顾三模态可发现、费用透明和复杂任务确认，而不让简单任务回到分类页或启动表单。
- 影响：首屏改造需要合并当前 CreationEntry 与后续 Record Composer 的重复启动阶段、补齐图片冷态一级入口、压缩 Hero/推荐、收敛四套分类、保护原始输入并统一桌面/移动提交 seam。首轮验收继续遵守普通路径到首个 token 不超过 2 次显式操作，高成本视频只额外一次确认。
- 证据边界：三张概念图及完整信息架构记录见 `references/analysis/user-journey-ui-benchmark-2026-07-20/composer-first-screen-decision.md`；视觉选择尚未证明真实意图推断准确率、报价时延、素材授权成功率或移动键盘体验，这些仍需交互原型与真实任务验收。
- 待继续拍板：首屏快捷任务已由 D-075/D-082 暂定，创作对口推断已由 D-081 取消，推荐证据抽屉与条件 Brief 已由 D-094 关闭；本条开放项已全部闭合。
- Supersedes：解决 D-073“先细化首屏 Composer 还是文案/图文工作区”的顺序及 D-072/D-073 的首屏结构开放项；D-029/D-031/D-043/D-046 和 D-073 的共享底层合同继续有效。

## D-075 首屏六个快捷任务与 Pro Studio 入口暂定

- 日期：2026-07-20
- 状态：`accepted_design_default`（六个任务由用户明确表示“暂定这几个”，Pro Studio 入口由用户明确补充；进入当前设计基线，但任务名称、排序和数量尚未最终锁定）
- 决定：首屏 Composer 下方暂定六个快捷任务：①从案例图写小红书；②朋友圈项目介绍；③项目/活动套图；④促销海报；⑤抖音项目成片；⑥旧内容换平台。六项分别覆盖图文/文案、图片、视频和已有内容复用，作为 D-074 意图先行 Composer 的空白输入辅助，不替代自然语言主入口。
- 决定：在六个交付物快捷任务之外增加“Pro Studio 无限画布”独立入口，用于复杂多素材编排、高阶精修、TTS/SFX 与受控画布 Agent。入口位于快捷任务之后、Recent/示例之前，以次级横条和“进入 Pro Studio”动作呈现；它是专业工作区入口，不是第七个交付物、不进入默认一级导航，也不与首屏“开始创作”争夺 primary。
- 决定：该入口统一进入现有 `/pro-studio` canonical gate，不直接绕过 entitlement、Owner 购买、激活等待或 Canvas SSO。已解锁工作区继续进入画布，未解锁工作区由现有入口页展示演示、购买资格或联系 Owner；主产品不得在未读取事实时伪装“已解锁”。
- 决定：点击快捷任务只添加或建议交付物、平台、来源和表达意图；用户已有输入时默认保留原文，任何替换必须先预览并可撤销。浏览、预填、切换和退出均不得创建 Work、Task、Job 或 ContentPackage。
- 决定：本清单暂不扩展为工具大全、场景墙或独立导航，也不为六项分别创建历史、素材或数据合同。后续可依据真实任务频次、首屏启动率、纠偏率和跨品类适用性调整名称、顺序、合并或替换，但调整不得破坏文本、图片、视频首轮可发现性。
- 原因：六项已经覆盖当前优先交付物，并能让首次用户理解系统不仅能写文案，也能生成套图、海报、视频和复用旧内容；Pro Studio 又是已经开发的高阶创作面，需要有明确但不干扰日常主路径的发现入口。暂定而非永久锁定，可以避免在缺少真实门店任务数据时过早固化分类。
- 影响：下一轮首屏设计使用“六个任务 + 一个独立专业空间入口”完成布局、文案和状态原型；开发票据需复用现有 Pro Studio entry/entitlement/SSO/adoption 合同，并把清单与“保留原文、零预填写入、交付物可纠偏”作为同一验收合同，不能只实现七个视觉按钮。
- 证据边界：快捷清单来自当前产品五类宣发入口、三款对标流程和本轮产品判断，尚未经过四类门店的真实任务频次与可理解性测试；Pro Studio 已有 `/pro-studio`、active/locked entitlement、Owner checkout、激活等待、Canvas SSO 和跨服务 E2E，但入口存在不等于所有部署已满足真实 host、恢复与公开销售门禁。
- 待继续拍板：真实任务数据达到什么条件后调整暂定清单。系统自动推断议题已由 D-081 关闭，移动端六项与 Pro Studio 展示层级已由 D-084 暂定。
- Supersedes：暂时关闭 D-074“首屏 4～6 个快捷任务清单”的开放项，但不把暂定清单升级为永久范围；D-074 其余待拍板项继续有效。

## D-076 首屏 Composer 四层架构正式确认

- 日期：2026-07-20
- 状态：`accepted`（用户结合小云雀四处界面批注明确提出层级，并确认修订概念图“这个架构就比较合理了”）
- 决定：D-074 的意图先行主轴不变，冷态首屏正式采用四层结构：①同一 Composer 内的 `文案 | 图文 | 视频` 创作对口切换；②一句话意图、`+ 来源`与动态模型/相关设置组成的主 Composer；③版本化快捷模板；④少量工具组合、Pro Studio 和“查看全部”。轻量推荐位于 Composer 之前，Recent/示例位于四层之后，均不得抢占 primary。
- 2026-07-24 D-103 修订：创作中心先区分默认“定制创作”和次级“自由创作”，但仍复用同一 Composer、对口、素材、结果与历史；它是 mode policy，不新增一级导航或第二首页。定制模式由 Recipe/Harness 自动编排，自由模式把公开模型与原生参数提升为主要设置。
- 决定：`文案 | 图文 | 视频`是面向用户的创作对口与推断纠偏器，不是底层 operation 枚举，也不是进入创作前的必选步骤。用户可直接输入，系统默认或推断当前对口（该推断部分已由 D-081 取代：创作对口只由用户显式选择）；用户手动选择后本次任务内不得被模板或后台默认静默改回。“图文”覆盖纯图片、海报/套图和图文笔记，精确结果继续由当前快捷模板或可改交付物 chip 表达，再编译为现有 `copy.generate`、`image.generate` 或共享父 Task 下的 child runs；D-073 的文案/图文、图片、视频三类成品工作区不因此合并。
- 决定：首屏设置行按当前创作模式、创作对口、模板、素材和模型能力动态匹配，只显示当前任务相关的产品模型、比例/数量/时长/参考等 3～5 个高频设置，其余进入“更多设置”。普通用户可以看见产品化 `CatalogModel` 名称，但不能看见官方/中转交易方、ProviderProfile、ExecutionChannel、Deployment、New API/Sub2API、凭据或 fallback 顺序；定制创作默认由系统推荐模型，自由创作由用户显式选择。模型、参数、数量或工具变化后必须重新取得服务端 `UserDebitPreview` 并冻结内部 quote revision（D-109）。
- 决定：D-075 六项正式解释为“快捷模板”而非六个孤立生成器。每个前台模板引用版本化 `CreationRecipeVersion`，至少包含交付物/平台、用户可见的上下文补丁、来源要求、模型 auto/fixed 策略、设置补丁、输出合同、报价策略和目标成品工作区；隐藏系统 prompt 只返回服务端 revision 引用，不下发浏览器。实际模型、参数、报价和路由仍以提交时冻结的 CreativeExecutionContract/RouteSnapshot 为准，模板声明不是执行事实。
- 决定：创作对口、模板、模型设置和工具选择统一遵循 `previewChange → commitChange → undoChange`。字段记录 `user | template | system` 来源和 dirty/revision；系统只能直接更新仍属 system 且未被用户修改的默认值。用户原文、显式模型、手改参数、素材/权利事实和已选工具始终受保护；冲突必须给出差异预览和一个明确解决动作，固定模型不可用时保留选择并阻塞提交，禁止静默跨 CatalogModel 回退。
- 原因：小云雀的清晰感来自“同一壳、分层承接”：先限定创作上下文，再在同一输入区动态匹配设置，模板降低空白成本，工具扩展复杂能力。把这四层改造成我方业务语义后，既能保持 D-074 的最短自然语言路径，也能让三类创作、模型选择和后续高级能力明确可发现。
- 影响：首屏实现需补正式创作模式/对口状态、版本化 CreationRecipeVersion、提交前可逆 patch、三模态目录查询、模型 parameter descriptors、产品额度预览与内部 quote fingerprint；不能继续把前端 `NAMED_PRESET_CONTRACTS`、`internalIntent` 替换或硬编码比例/时长当正式合同。桌面与移动复用同一状态控制器，移动端通过底部抽屉承载来源/模板/工具和更多设置。
- 证据边界：确认概念图与完整合同见 `references/analysis/user-journey-ui-benchmark-2026-07-20/composer-first-screen-decision.md`；当前代码已有三 operation、模型目录、模板版本、Pro Studio allowlist 与 canonical 对象，但尚未实现上述统一 Recipe、字段来源/撤销、三模态 Auto、parameter descriptors 或完整报价重算。本决定是产品与交互基线，不代表 UI 或运行合同已经开发完成。
- 待继续拍板：真实任务数据达到何种条件后调整六项清单。三项对口的自动推断议题已由 D-081 关闭，首批 Recipe 字段与移动端模板/工具容器分别由 D-082/D-084 暂定。
- Supersedes：细化 D-074 的首屏层级，并取代“所有模型信息都只能藏入本次设置”的绝对呈现口径（同时细化 D-043 决定②：模型默认档 Auto 与合并确认不变，可见 CatalogModel 名称由「更多设置」上移至首屏动态设置行，不构成前置确认）；D-074 的意图先行、唯一主 CTA、条件 Brief 与 D-073/D-075 的共享真相、成品分化和 Pro Studio 边界继续有效。

## D-077 创作工具目录采用两类入口、一套业务真相

- 日期：2026-07-20
- 状态：`accepted`（用户明确要求后期增加类似小云雀的创作工具快捷入口，可作为 Composer 模板或跳转后的单独工具，并要求实际操作记录参考）
- 决定：前台统一命名“创作工具”，底层只保留两类入口。`composer_recipe` 在当前 Composer 内预览并套用 CreationRecipeVersion，适合基于此再创作（D-085 取代旧称“做同款”）、节点营销、旧内容换平台等可由同一输入壳承接的任务；`standalone_tool` 打开聚焦弹层、独立页面或专业工作区，适合多尺寸适配、批量去背景、字幕擦除与 Pro Studio。`dialog | route | workspace`只是 standalone 的呈现容器，不是第三套运行时或业务真相。
- 决定：首页“工具组合”只显示当前创作对口、素材、模板、权益和运行可用性最相关的少量入口，并提供“查看全部”。完整目录使用“图文与发布、图片处理、视频处理、品牌资产、专业工作区”等商家任务/结果语言筛选，不照搬模型新品、沉浸式短片、短剧 Agent、会员或供应商技术分类，也不让工具卡墙长期占据首屏。
- 决定：后续新增工具必须以版本化 `CreativeToolEntry` 纳管，至少声明 entry kind、可见名称/说明、输入与来源要求、输出类型、能力/模型要求、权益与数据等级、报价策略、可用性、recipe revision 或 canonical route、目标结果工作区和审计版本。工具目录是控制面投影，实际执行继续落入现有 Task/Work/Job/Asset/ContentPackage/RouteSnapshot；任何工具不得自建历史、素材库、额度或发布状态。
- 决定：浏览目录、预览模板、选择工具、打开 gate 和返回均不得创建 Work、Task、Job 或 ContentPackage，也不得上传、生成、扣费或发布。`composer_recipe` 只有点击“开始创作”才写入；`standalone_tool` 只有在聚焦工具内明确提交/新建后才持久化。小云雀实测“点击自由画布即新增空项目”的副作用不照搬。跨入口携带上下文必须使用版本化来源/草稿 handoff，不能以 sessionStorage 或 URL 中散落 prompt 形成第二真相。
- 决定：Pro Studio 继续是 `standalone_tool: workspace` 的 canonical 样本：首页入口只到 `/pro-studio`，复用 entitlement、Owner checkout、激活等待和 Canvas SSO；复杂多素材编排、多分支、节点排序、TTS/SFX 和画布 Agent 不回填到普通 Composer。其产物仍经 adoption 进入唯一 ContentPackage revision。
- 原因：对小云雀三条入口的实际走查确认，“创作工具”可以共用一个目录却采用不同交付方式：爆款复刻原位重配 Composer，图片多尺寸延展打开聚焦弹层，自由画布进入独立项目工作区。吸收这种入口弹性，同时锁住我方 canonical 对象和零写入浏览边界，可以持续扩展能力而不重建工具平台式碎片化产品。
- 影响：首轮只需先建立 CreativeToolEntry 目录、推荐过滤、两类 dispatch 和共享 handoff/报价/审计接口；不建设任意第三方插件市场或通用工作流编排器。初步参考映射包括基于此再创作、节日活动套组、多平台尺寸适配、批量去背景、字幕修复、门店视觉规范和 Pro Studio，具体开发批次另行按真实需求与模型成熟度确定。
- 证据边界：实际操作记录和 14～17 号截图见 `references/analysis/user-journey-ui-benchmark-2026-07-20/xiaoyunque-creative-tools-entry-audit.md`；本轮没有提交任何生成、付费或发布，因此只确认入口与第一屏承接，不证明竞品工具质量、时延、失败恢复或资产回流。进入自由画布产生的一个空“未命名项目”已原样保留，未擅自删除。
- 待继续拍板：首批 standalone 工具与上下文 handoff 已由 D-092 关闭，完整目录分类、排序与搜索门槛已由 D-093 关闭，移动端目录容器与退出恢复已由 D-084 关闭；本条开放项已全部闭合。
- Supersedes：细化 D-075 的 Pro Studio 次级入口与 D-076 的工具组合层；不改变 D-073 的统一任务/历史/素材真相，也不授权建立插件市场或第二 Canvas Agent。

## D-078 创作入口与快捷模板纳入后台版本化可视配置

- 日期：2026-07-20
- 状态：`accepted`（用户明确三个创作入口与快捷模板均为暂定组合，可先按推荐方案完成开发，后续按实际运营需求频繁调整，并要求纳入后台可视化管理；2026-07-20 D-098 C3 收窄首轮：仅 Recipe+Surface 进完整发布生命周期与可视编辑器，Lens/Tool 静态引用起步）
- 决定：`文案 | 图文 | 视频` 三个创作对口、首页六个快捷模板、各对口精选模板和工具推荐作为首发设计默认值进入开发，不再永久硬编码在前端。推荐首发全局六项为：从案例图写小红书、朋友圈项目介绍、项目/活动套图、促销海报、抖音项目成片、旧内容换平台；切换对口后分别展示最多四个文案/图文 P0 或三个视频 P0，其余进入“查看全部模板”。
- 决定：后台既有管理壳的“内容能力”新增“创作入口与模板”结构化编辑器，承接标签、副文案、排序、默认策略（其中对口默认选中已由 D-081 取代：不得后台默认选中创作对口）、精选项、上下架、目录排序及桌面/移动预览，并采用 `草稿 → 预览 → 校验 → 发布 → 回滚`。首版只支持平台全局配置，不提前建设工作区/套餐/地区多层覆盖；后续只有在真实商业需求出现后才扩作用域。
- 决定：配置模型拆分为用户可见 `CreationLensDefinition`、版本化 `CreationRecipeVersion`、只负责前台编排的 `CreationSurfaceRevision` 和 D-077 已确认的 `CreativeToolEntry`。四者进入独立 Creation Experience Catalog 与原子发布聚合；Surface 只引用已发布 recipe/tool revision，不复制 prompt、模型、报价或路由字段。实现只复用现有 admin-config 的 append-only/CAS/actor/reason/audit 基础设施模式和官方模板的 preview/publish/retire 生命周期，不能把组合配置写入固定 key 单项生效的 `admin_config_revisions`，也不能塞入 body 为 CanvasDocument 的 `p1_template_versions`。与 D-037 边界对账：Creation Experience Catalog 属 D-037「新增强类型 artifact」一类，复用 admin-config 基础设施模式与 candidate→publish 门思路，仅因组合配置不适配固定 key 单项生效的 schema 形态而独立建目录，不构成第二套配置运行时。
- 决定：“可配置”不开放底层任意编排。当前 `文案 | 图文 | 视频` 是首个发布组合，后续允许在 2～4 个用户可见对口内重新编组，但必须合计覆盖文本、图片、视频三类首轮能力；`copy.generate | image.generate | video.generate`、三类成品工作区、模型目录、报价、RouteSnapshot 和 Task/Work/Job/Asset/ContentPackage 合同保持稳定。普通后台发布不能创造第四类 operation、把图文静默改成视频执行，或让任一首轮能力失去可发现入口。供应故障需要临时关闭时只能走高影响应急开关、明确显示原因并独立审计。
- 决定：每个 Composer 会话读取并冻结一个已发布 `CreationSurfaceRevision`，首次套用模板时冻结 `recipeRevision`；后台随后发布的新版本只影响新会话，不得静默改写用户正在编辑、已报价或已提交的任务。浏览、预览、排序、套用和返回继续遵守零业务写入，只有用户明确开始创作或在独立工具内明确提交后才持久化。
- 决定：首页普通工具首轮实现“基于此再创作”（composer recipe，D-085 取代旧称“做同款”）和“预设尺寸重排与导出”（standalone tool）两个窄切片；前者先修复当前退役复用命令，后者在智能裁剪/批量重试完成前不得包装成“一键智能多平台适配”。桌面最多 3 项、移动最多 2 项，不以不可用工具补位。“Pro Studio 无限画布”底层属于 standalone workspace，但前台继续作为独立“专业工作区”横条。后台启用不是运行可用性的充分条件，最终可用性必须同时满足已发布配置、能力目录、Provider live evidence、权益和素材/权利条件。批量去背景、字幕擦除等候选能力只有达到完整发布门后才能上架。
- 原因：入口、模板和工具陈列将随真实任务频次、模型成熟度与运营策略频繁变化，硬编码会把日常运营调整变成前端发版；但如果连 operation、模型和任务合同都开放为任意配置，又会破坏 D-073 的统一真相和执行安全。以版本化 Surface 编排稳定 Recipe/Tool 引用，可以同时获得调整速度、审计回滚和运行确定性。
- 影响：首轮开发需建立独立 Creation Experience Catalog，补四类对象的查询、草稿、校验、原子发布、历史和回滚，迁移当前三对口、六模板、两个普通工具和 Pro Studio 专业入口为首个 Surface revision，提供非 JSON 的可视化管理界面，并让 Composer/Task 冻结 surface、recipe、model、quote 和 route revisions。普通工具仅在可用性交集通过后激活；自动化必须覆盖排序、上下架、非法引用、并发发布冲突、回滚、三模态可发现、零写入浏览、移动恢复和键盘排序。
- 证据边界：推荐组合来自当前产品合同、三款竞品走查和美业任务判断，尚不是门店真实运营数据；本轮先保留事件 revision 以支持 exposure/select/apply/start/complete/correct/cancel 后续分析，但按用户要求暂不把运营指标看板纳入当前开发范围。完整方案见 `references/analysis/user-journey-ui-benchmark-2026-07-20/creation-surface-admin-configuration.md`。
- 待继续拍板：何时以真实任务数据调整首发组合。创作对口的自动推断议题已由 D-081 关闭，首批 Recipe 字段与复用关系已由 D-082 暂定，移动端“查看全部”容器已由 D-084 暂定。
- Supersedes：把 D-075 的“暂定清单”和 D-076 的三对口/模板层升级为可发布、可回滚的后台配置合同；细化 D-037 的承载边界（新增强类型 artifact 以独立 Catalog 落地，非另建配置系统）；不改变 D-073 的三模态首轮范围和统一业务真相，也不改变 D-077 的两类工具入口。

（编号说明：D-079 未使用（跳号）；D-080 已被后台/供应交叉复核处置占用，本块自 D-081 续号。2026-07-20 交叉复核 F5 标注。）

## D-081 创作对口只由用户显式选择

- 日期：2026-07-20
- 状态：`accepted`（用户明确纠偏：“三个创作入口不用自动推荐，是用户主动选择的”）
- 决定：`文案 | 图文 | 视频` 创作对口不做系统推荐、意图推断、素材推断、模板隐式带选或后台默认选中。新草稿冷态同时展示所有已发布对口且均未选中；用户可以先输入文字或添加来源，但提交前必须通过点击、键盘或模板差异确认主动选择一个对口。未选时主动作只提示“选择创作类型后继续”并把焦点移回对口组，不能代替用户选中。（2026-07-20 D-098 C6 对账：必选创作对口是模式选择器，不属于 D-031/D-043 所禁的字段填表或前置确认，占用 D-074 两次显式操作预算之一；模板卡点击一击两用，纯自由文本路径「选对口(1)+开始创作(2)」恰 2 击合规。）（2026-07-24 修订：定制创作主线已由 D-114 改为 agent 流式对话＋ContentIntent 编译 outputKind——本条 radiogroup 必选、未选阻塞、禁意图推断的合同仅在**自由创作**线存续（自由创作本以用户显式选择为本性，D-103/D-115）；定制创作部分被 D-114/D-116 取代，见 D-110 元原则。）
- 决定：用户选择后本地草稿记录 `source: user_explicit`，系统再加载该对口对应的快捷模板、允许来源、产品模型、参数 schema、工具推荐、服务端报价和目标结果工作区。模型、比例、时长等专业设置仍可在已选对口内部使用 Auto 默认，系统仍可建议平台和精确交付物，但任何这些结果都不得反向改变创作对口。
- 决定：冷态可以浏览 D-078 的跨模态六模板；要求特定对口的动作必须明确写成“选择图文并套用”“选择视频并套用”等，并把对口选择与 Recipe patch 作为一次可预览、可撤销的原子变更。已有对口与模板不一致时必须先确认，模板和后台发布不得静默切换。
- 决定：切换对口始终逐字保留原始输入、来源、上传素材和权利事实。若当前模板、显式模型、手改参数、工具、素材角色或报价不兼容，先展示“保留 / 暂存 / 改变”差异；确认前保持原对口，取消完全还原，确认后可撤销，切回时恢复该对口此前的设置。禁止以切换对口为由删除素材或覆盖用户字段。
- 决定：选择、切换、差异预览、取消和撤销均只改变版本化 Composer 草稿，不创建 Work、Task、Job、ContentPackage、额度预占或供应商请求。提交时冻结 lens、surface、recipe、model、quote 与 RouteSnapshot revisions；已提交任务不能原地换对口，需要改变时使用“基于当前内容新建创作”形成派生草稿。
- 决定：桌面和移动共享 `unselected → selected → switch_preview → frozen` 状态机。对口组使用可见标题、`radiogroup` 和必选语义，初始没有选中项；移动端三个入口完整可见且不依赖横向滚动，冲突预览只使用一个 bottom sheet，返回/取消后恢复原对口、草稿、滚动与焦点。读屏不得在输入过程中播报任何模式推断。
- 原因：创作对口直接决定成品媒介、模型目录、费用等级、输入要求和结果工作区，错误的自动推断会让用户在尚未确认输出类型时看到错误设置、报价甚至提交错误任务。由用户显式选择，同时只自动补齐对口内部的专业参数，可以把媒介控制权留给用户，又不要求用户理解底层模型和供应渠道。
- 影响：当前桌面工作台和移动端都默认初始化为 `copy.generate`，冷态 Picker 仍只有文案/视频且使用普通 `aria-pressed` 按钮，`primaryCreationOperations()` 也缺少图片（图片对口在桌面工作台 mode picker 已可达，缺口精确为冷态一级入口，勿重复建设桌面对口），切换 operation 会直接重置比例；首轮实现必须引入独立未选状态、三项 Lens picker、按对口保存的设置草稿、切换差异/撤销和未选提交阻塞，并修正对应单元、交互、移动与无障碍测试。
- 证据边界：小云雀证明用户显式选择后可在同一工作面同步切换输入要求、模型/参数、计价与工具推荐；CreatOK 证明模型/规格与价格随选择同面呈现（模板继承未实测）；讯飞绘文证明写作动作原位切换输入合同与示例（未展示价格/模型维度）。本轮只读走查没有证明竞品会保护已有输入或提供切换冲突撤销；我方后两项属于根据现有 canonical 草稿、报价和权利合同补足的产品安全边界，不冒充竞品实测。
- 待继续拍板：三个成品工作区的结果动作已由 D-085 关闭。首批 `CreationRecipeVersion` 字段与模板间复用关系已由 D-082 暂定，冷态六卡文案与差异确认已由 D-083 暂定。
- Supersedes：取代 D-074“系统推断交付物作为创作对口来源”、D-076“系统默认或推断当前对口”及 D-078“后台默认策略”中与创作对口选择冲突的部分；保留三者的统一 Composer、对口内 Auto 参数、模板版本化、报价重算、后台发布/回滚和三模态首轮范围。

## D-080 后台与供应决策交叉复核处置：五项收敛采纳，专属池与三核心双渠道验收维持

- 日期：2026-07-20
- 状态：`accepted`（2026-07-20 用户对六项收敛建议逐条拍板）
- 决定（C1 采纳）：D-055 首版收窄为只读异常列表——root-cause key 去重、严重度排序、受影响能力/范围、新鲜度、下钻与技术移交链接，由现有 pending-actions 投影与 OperationalMetric 合同组合，不新建 incident 持久化、负责人、确认或指派工作流；ack/assign 挂 D-040 运营执行恢复或出现第二运营角色时再引入。D-057 首批权限域同步移除「事件确认与指派」。核心前提=后台当前只有一套管理员角色权限（单人运营），用户确认此定义。
- 决定（C2 采纳）：D-054 能力状态由各域模块自报（沿用 `OperationalMetric` 的 known|unknown 诚实合同）；能力↔依赖映射为静态查找表，仅用于详情页展示 join 与「从依赖反查受影响能力」，不做图级严重度自动传播与合成；去重仅在 incident 源级。两层导航结构不变。
- 决定（C3 采纳）：D-056 首个开发增量改为「能力详情合同类型先定 → 已插桩域（模型供应/任务队列/权益额度）完整做深 → 其余域静态 manifest 存根（`not_instrumented`+owner+下钻现有页）→ 异常首页由真实上报域组合生长」；该骨架与 D-068 的 MP 纵向为**同一增量**，MP 产出直接回填同一 capability registry，消除两个「首个增量」的排序歧义（即闭合交叉复核 F6 项）。Cloudflare GraphQL analytics broker（CF-02/03）不进首切，先 deep-link+自有探针。
- 决定（C4 部分采纳）：D-060 凭据生命周期收敛为 `pending→active→retired` 三态主干；`tested` 不作独立状态、改为激活前置门（激活命令校验最近连通/能力探针证据），`draining` 仅作为承载异步媒体任务的渠道/凭据子状态。secret 只写 KMS 不回显、轮换不追改历史快照、BYOK 严格隔离等安全边界原样保留。**SupplyPool 维持一等实体、不推迟**：用户拍板——企业专属池是代运营/陪跑高价值客户的核心交付服务，属产品服务本体而非边缘例外；D-058 八实体口径与 D-066 共享/专属边界不变。
- 决定（C5 否决）：D-069 验收强度维持原样——文本、图片、视频三项核心 operation 各双渠道 `live_verified` + 真实故障注入。理由：三模态是主力生产功能，供应可行性未经验证则交付本身存疑；首轮周期与预算按此规划，不以降低验收换进度。
- 决定（C6 采纳）：D-059 健康护栏参数（冷却窗口/失败阈值/半开探针）初始值直接采用 LiteLLM router（`cooldown_time`/`allowed_fails`）与 Envoy outlier detection 成熟默认，以带出处的配置常量落地；真实流量积累后按发布门流程校准，不自行标定。
- 原因：2026-07-20 六路交叉复核（4 路代码实证 + 2 路最佳实践评审 + 主会话交叉裁决）结论：事实层零 P0、四个研究包引用全部忠实；最佳实践层方向全部成立，需收敛的集中在首轮铺开面（想象态工作流、传播引擎、全量存根 registry、六态凭据、参数自标定）。完整报告见 `docs/reviews/admin-supply-decisions-xcheck-2026-07-20.md`。
- 影响：D-054/D-055/D-056/D-057/D-059/D-060 按本条修订执行（各条状态行已加指针）；D-058/D-066/D-068/D-069 边界不变且经复核确认。同批已完成非决策性口径修正：七路由页与健康区块口径（D-048/D-051/D-070）、二元 admin 现状（D-057）、BYOK 既有基线与裸 env 现状（D-060）、账本实名映射（D-066）、workspace 选择受限例外（D-063）、D-029→D-044 血缘勘误（D-058/D-059/D-062）、RouteSnapshot 规范类型缺口（D-058）、产品侧限流已存在（D-066）、D-067 部分取代限定、D-047 顺序指针、D-064 数据类澄清、D-071 候选枚举补全。
- 证据边界：本条记录复核处置与用户裁决，不代表 AP/MP 拆票已按新口径重排；收敛后的首增量拆票需按 D-056/D-068 修订口径重新对齐。复核由 6 路 Opus 子 agent 完成，报告含全部 file:line 证据。
- 待验证：`ready` 灰度态与 ack/assign 的重新引入触发点；LiteLLM/Envoy 默认参数在真实流量下的首轮校准窗口；专属池（DedicatedSupplyPool）在代运营/陪跑服务合同中的容量与账单条款形态。
- Supersedes：收窄 D-055 首版范围与 D-057 权限域一项；改写 D-056 首个增量形态并闭合其与 D-067/D-068 的排序歧义；细化 D-054 状态计算、D-059 参数来源与 D-060 生命周期粒度；确认 D-058/D-066/D-069 维持原判。

## D-082 首批六张快捷卡片映射八个单对口 Recipe variant

- 日期：2026-07-20
- 状态：`accepted`（用户确认“暂定这个形式，后期可改”；表示首个发布版本按本条开发，不表示永久冻结）
- 决定：前台首发继续保持 D-075/D-078 的六张快捷卡片，底层发布八个具体 `CreationRecipeVersion`。从案例图写小红书、朋友圈项目介绍、项目/活动套图、促销海报、抖音项目成片各引用一个单对口 Recipe revision；“旧内容换平台”使用同一 `familyId` 下的文案、图文、视频三个 variant。
- 决定：“旧内容换平台”在 `CreationSurfaceRevision` 中只是按 lens 键控的一组有序 published revision 引用与展示信息，不新增第五套运行时对象。冷态点击后必须显示无默认值的 `文案 | 图文 | 视频` 选择，由用户主动确认后解析对应 variant；不得根据旧内容类型、目标平台或模型能力自动分流。
- 决定：每个 Recipe revision 至少冻结展示信息、唯一 lens/交付意图、事实要求、来源槽与授权策略、可见 Brief 默认、交付物/数量/比例/时长、目标载体、Workflow/Prompt/ModelPolicy/QuotePolicy revision 引用、结果工作区及 revision/audit/content hash。Recipe 不保存用户实际素材 ID、实际报价、Provider、Deployment、Credential、New API 或 Sub2API 字段。
- 决定：首发默认输出暂按专项方案实施：案例图写小红书为笔记+3:4 封面并复用真实案例图；朋友圈项目介绍为一条短文案；项目/活动套图默认四张 3:4；促销海报默认一张 3:4 且可改 1:1/9:16；抖音项目成片默认 15 秒 9:16 成片+封面+发布文案；旧内容换平台按用户选择的三个 variant 分别交付文案、图文包或视频包。
- 决定：首轮复用采用不可变 revision 引用，不建设任意节点编排、通用 RecipeFragment DSL 或运行时深层继承。后台可以“复制自已有 Recipe”辅助编辑，但 published Recipe 保存完整、可独立校验的输入/输出快照；共享 Workflow、Prompt、ModelPolicy、QuotePolicy 和 ResultWorkspace 的更新必须显式产生并发布新 revision。
- 决定：六张卡片、八个 variant、字段默认值与排序均属于首个发布组合，后期可以按真实运营需要在后台通过 `草稿 → 预览 → 校验 → 发布 → 回滚`调整。新版本只影响新草稿；已打开草稿、已冻结报价、已提交任务和历史回放继续使用原 surface/recipe/workflow/prompt/model/quote/route revisions。
- 原因：六张卡片维持简单的用户认知，八个单对口 variant 则避免一个 Recipe 同时承担三种媒介、三套来源与报价分支；完整快照配合不可变引用能获得可校验、可回滚和可回放的后端配置，同时保留后期调整空间。
- 影响：Creation Experience Catalog 与后台编辑器需支持具体 Recipe revision 和 Surface 内的 recipe collection 引用；前端套用改为服务端 `RecipePatchPreview`，保留用户原文并展示“保留/暂存/改变”，不能继续用 `selectedPreset.internalIntent` 替换 intent。Work/Task 需补 lens、surface、recipe、workflow、prompt revision 血缘；朋友圈目标需建模为 distribution/export target，不能冒充当前自动发布 `ContentPackagePlatform`。
- 证据边界：六项默认数量、篇幅、比例和时长是结合当前产品合同与竞品流程形成的首发设计默认，不冒充真实门店运营数据；用户明确允许后续调整，因此真实数据只用于发布新 revision，不追改历史事实。
- 待继续拍板：三个成品工作区的结果动作已由 D-085 关闭；取得真实运营数据后调整首发组合的触发门槛。冷态六卡副文案与跨对口确认已由 D-083 暂定。
- Supersedes：闭合 D-076、D-078、D-081 中“首批 Recipe 字段值与复用关系”的待拍板项；细化 D-078 的 Surface 引用边界，但不改变 D-081 的用户显式选择、D-073 的三模态首轮范围或 Product Core 的任务/报价/路由真相。

## D-083 六张快捷卡片冷态文案与仅冲突确认

- 日期：2026-07-20
- 状态：`accepted`（用户确认“后期可调”；表示采用首发默认，不表示文案、顺序或预览永久冻结）
- 决定：六张冷态卡片首发使用“标题 + 一行副文案 + 明确动作”。对应组合为：从案例图写小红书 /“用案例图生成笔记与封面”/“选择图文并套用”；朋友圈项目介绍 /“用项目资料生成朋友圈文案”/“选择文案并套用”；项目/活动套图 /“用项目或活动信息生成 4 张套图”/“选择图文并套用”；促销海报 /“用优惠和期限生成活动海报”/“选择图文并套用”；抖音项目成片 /“用案例素材生成 15 秒竖版成片”/“选择视频并套用”；旧内容换平台 /“选择旧内容，再决定改成哪种形式”/“选择创作形式”。
- 决定：前五张卡片明确写出目标对口，用户点击该动作本身即为 D-081 要求的主动选择。没有受保护字段冲突时，一击在本地草稿中完成选择与套用，不统一增加确认弹窗；已有文字、来源和上传素材始终保留，本身不构成冲突。
- 决定：同对口 Recipe 会覆盖用户手改模型/参数/模板/已确认报价，或用户已选另一对口时，必须先展示服务端 `RecipePatchPreview`，按实际差异列出“保留 / 暂存 / 改变”。主动作分别为“套用并更新设置”或“切换到{对口}并套用”；取消前保持原状态，确认后提供撤销。
- 决定：“旧内容换平台”打开来源、创作形式、目标载体的聚焦面板。冷态对口和目标载体均无默认值；若已有用户主动选择的对口，可以明确沿用该 user-owned 选择，但仍不得根据旧内容媒介、文字、目标平台或模型能力自动决定 variant。
- 决定：普通卡片 `title / summary / previewAsset` 来自 Recipe presentation，Surface 只控制 revision 引用、顺序、精选和上下架；collection 展示信息属于 Surface。lens 标识、`选择{对口}并套用`、冲突确认 CTA 和不可用原因根据真实合同生成，后台不能自由填写或关闭。后期文案、顺序、预览与默认值通过新 revision 调整。
- 决定：直接套用后 Composer 原位展示“已选择{对口}并套用‘{模板}’”和“撤销”，焦点进入第一个缺失输入；卡片整体使用单一 button 语义，动作不依赖 hover，最小触区 48×48px，状态变化只礼貌播报一次。浏览、套用、预览、取消和撤销继续零业务写入。
- 原因：对每张模板都增加确认会把明确的用户点击变成重复确认，而完全无确认又会在跨对口或覆盖手改设置时造成意外变化。以“受保护字段是否冲突”为门槛，既维持小云雀式快捷入口的低摩擦，也保护用户控制权和报价确定性。
- 影响：前端需要统一 Recipe apply state machine 与 `RecipePatchPreview`，不能把“是否已有输入”当确认条件；后台预览需覆盖冷态、同对口、跨对口和不可用状态，并校验展示文案与真实 lens/输出默认一致。事件携带 surface/recipe/action/lens revision，不记录隐藏 prompt 或用户敏感正文。
- 证据边界：具体副文案和数字是 D-082 首发 Recipe 默认的用户可见表达，不是运营效果承诺；用户允许后期调整，因此调整只发布新 revision，不追改已打开草稿、冻结报价、已提交任务或历史回放。
- 待继续拍板：三个成品工作区的结果动作已由 D-085 关闭；真实使用数据达到何种门槛后替换首发文案或排序。移动端六卡与“查看全部”容器已由 D-084 暂定。
- Supersedes：闭合 D-081/D-082 中“冷态六卡副文案与差异确认”的待拍板项；不改变 D-081 用户主动选择、D-082 六卡八 variant、D-078 后台版本化配置或零写入浏览边界。

## D-084 移动端六卡全展示与全屏创作目录

- 日期：2026-07-20
- 状态：`accepted`（用户选择方案 A，首发暂定、后期可调）
- 决定：移动端冷态在 Composer 之后以两列三行完整展示 D-082/D-083 六张快捷卡片，不使用横向轮播或“只露四张再展开”。320px 与 390px 均使用两列；200% 缩放或有效内容宽度低于 280px 时改为单列，不截断标题、副文案或动作。用户主动选择对口后，首页只显示该对口最多四张 P0 卡片，采用两列两行。
- 决定：卡片整体为单一 button，展示预览、标题、副文案和 `选择{对口}并套用` 等真实动作，不嵌套第二按钮、不依赖 hover，触区至少 48×48px。无冲突时一击本地套用，有冲突才进入 D-083 的 `RecipePatchPreview`；套用后聚焦首个缺失输入并提供撤销。
- 决定：移动首页明确分成“快捷模板 / 查看全部模板”和“创作工具 / 查看全部创作工具”两个区，普通工具最多两项，Pro Studio 继续是独立全宽专业工作区横条。模板、普通工具与 Pro Studio 不混成同一种卡片或一个语义含糊的“查看全部”。
- 决定：两个“查看全部”进入同一 canonical 全屏目录路由的不同 kind/tab，完整目录承接筛选、长列表、浏览器历史、深链与返回恢复；未选对口时默认查看全部，已有 user-owned 对口时可明确沿用为初始筛选，但筛选本身不得选择或切换 Composer 对口。目录状态只保存 kind、筛选、surface revision、return key 等非敏感引用，不把原文、素材授权、隐藏 prompt、Provider 或完整草稿写入 URL。
- 决定：完整目录不使用半高 bottom sheet。移动端全局同时只允许一个 bottom sheet，限于跨对口/手改字段冲突、“旧内容换平台”的来源/形式/载体选择和一步窄工具确认；禁止嵌套 sheet。系统返回、Escape、下滑关闭和取消必须恢复原草稿、目录/页面滚动及触发点焦点。
- 决定：后台可以通过新 Surface/Recipe/Tool revision 调整文案、预览、顺序、精选和上下架，并提供移动预览；移动布局断点、六卡完整可发现、模板/工具分区、用户显式选对口和安全动作合同由产品代码控制，普通运营配置不得切成横向隐藏、四卡摘要或默认选中。
- 原因：六张首发卡片承担文本、图片、视频和旧内容复用的一级可发现性；横向轨道会把大部分能力藏在手势之后，四卡摘要又与 D-083 冷态六卡不一致。两列全展示保留发现性，全屏目录适合增长后的筛选、返回和深链，Bottom Sheet 则专注短暂冲突确认。
- 影响：移动端应复用现有主壳安全区、Creation Catalog 查询、Button/Card 和路由恢复能力，但替换当前横向 Scene/Suggestion 条、默认 `copy.generate` 与直接覆盖 intent 的快捷入口。实现需新增稳定的 mobile Surface projection、全屏 catalog route 和单一 overlay 状态；不能把现有 `SceneVisualButton` 原样当成 D-084 交付；移动创作面当前并无 bottom sheet（80vh Drawer 为共享组件，仅用于 admin 用户详情与移动用户菜单），单一冲突 sheet 需新建而非改造既有创作面容器（2026-07-20 交叉复核校正）。
- 证据边界：小云雀、CreatOK、讯飞绘文的本轮移动端均未实测；本条移动布局是结合桌面竞品结构、我方 320/390px 现有测试、当前代码与无障碍边界形成的产品设计，不冒充竞品移动端复刻。具体卡高、图片比例和首屏露出量仍需同一交互原型在 320×720、390×844、横屏和 200% 缩放下验证。
- 待继续拍板：三个成品工作区的结果动作已由 D-085 关闭，完整目录首批分类与搜索门槛已由 D-093 关闭；真实使用数据达到何种门槛后替换首发文案、排序或精选项仍属后续运营校准，不阻塞开发。
- Supersedes：闭合 D-075/D-076/D-077/D-078/D-083 中移动端六卡、模板/工具容器与退出恢复的开放项；不改变 D-081 用户显式选对口、D-082 六卡八 variant、D-083 仅冲突确认或 D-077 两类工具入口。

## D-085 三类成品工作区采用共享动作语义与媒介专属工具

- 日期：2026-07-20
- 状态：`accepted`（2026-07-20 用户选择方案 B）
- 决定：文案/图文、图片、视频三个成品工作区采用“共享 Result Shell、共享动作语义、媒介专属工作面”。统一壳负责运行状态、采用、继续调整、交付、基于此再创作、版本历史和运行详情；文案/图文提供编辑、选区改写、事实来源与平台预览，图片提供大预览、主图/套图采用、排序、逐张替换和局部重生成，视频提供播放器、封面、字幕、分镜候选、逐镜调整和 Pro Studio 精修入口。不得为了视觉一致把三个工作区压成同一套僵硬按钮，也不得再建立第四套结果页。
- 决定：共享首层结构为“成品主视图 + 一个状态驱动主动作 + 继续调整 + 交付 + 更多 + 媒介专属快捷调整 + 常驻自然语言调整口”。候选态分别使用 `采用此版本`、`选为主图/加入套图/采用这组`、`使用此成片`；采用后主动作转为 `继续完善并交付`，已交付后转为 `基于此再创作`。当“交付”已经升为当前 primary 时不再重复显示同名 secondary。桌面同时最多一个 primary 与三个 secondary，移动端固定一个 primary 与“更多”。
- 决定：采用、保存素材、保存版本与发布是四个不同语义。采用把当前候选写入 canonical ContentPackage revision 或成品槽；“保存”只表示保存新 revision；图片/视频“保存为素材”只针对明确、持久、具备权利与 parent/source 血缘的不可变媒体版本，配方/系列复用才使用 `reuse-memory.ts` 的 `AssetRevision`；发布由“交付”能力面承接并绑定精确平台、账号、ContentPackage revision、ApprovalReceipt 和回执。未绑定、未验证或不支持自动发布的平台只显示复制、下载发布包、系统分享降级或 assisted 人工交接，不伪装自动发布成功。
- 决定：三个工作区都常驻“还想怎么改？”自由文本。自由文本与需要模型执行的结构化快捷修改继承 D-046，统一创建 derived Task + derived revision，保留 source/adopted/edit/derived-from/route/cost 审计链且不新增 message/thread 持久化实体；纯确定性人工改字、排序可直接创建带 expectedRevision/OCC 和审计证据的 ContentPackage/Asset derived revision。任何路径都不原地覆盖成品，也不能继续一个创建 Work、另一个只在客户端拼接字符串。图片、视频的修改或重新生成在提交前显示影响范围、公开模型、目标输出规格、将使用的图片/视频额度与预计余额；可靠完成时间可作状态提示，但不展示供应商费用公式或内部重试时长。逐张/模型原生局部修改不默认触发整套重跑，重新生成保留当前版本和完整血缘。
- 决定：对外统一使用“基于此再创作”，取代语义含糊且当前命令已断链的“做同款”。它只复用结构、风格、参数、槽位和用户明确选择的素材角色，重新注入当前门店事实、授权素材与报价，创建新草稿；不得修改源成品或复制旧价格、期限、顾客信息和活动事实。版本恢复同样追加新 revision，不删除后续历史。
- 决定：Result Shell 只投影现有 Task、Work、Job、Asset、ContentPackage 与 RouteSnapshot，不新增 Result 状态实体、第二历史、第二资产或第二发布真相。现有 `VideoWorkflow` 只可作为由上述 canonical 对象派生的视频流程读模型，不进入唯一真相集合；其命令必须落回明确的 Task/Job/Asset/ContentPackage revision。（注：VideoWorkflow 当前为带独立持久表与 OCC 的一等权威存储，降级为派生读模型属实质重构——真相迁往 canonical 对象并保留崩溃恢复幂等——拆票必须显式立项，不得按低成本改造排期；2026-07-20 交叉复核补注。）运行详情默认折叠，只显示实际产品模型、参数、报价、任务状态、来源、权利、合规、失败与退款证据；前台不显示 Provider、Deployment、Credential、New API/Sub2API 或 fallback 顺序。
- 决定：移动端 P0 必须支持查看、采用、自由文本轻改、复制/下载、系统分享或降级、采用到当前内容、保存新版本、把明确、持久、具备权利与 parent/source 血缘的单个不可变媒体版本保存为素材、基于此再创作、版本恢复和异步恢复/重试/取消；视频还需播放、轻量封面与字幕校对。移动端动作预算固定为一个 primary 与“更多”，不增加条件性第三按钮。精确图像布局、逐帧修剪、字幕位置/动画和复杂多轨交给桌面或 Pro Studio，交接时保留当前 revision、选中对象与未提交调整，不能用“请到桌面继续”替代基础成品动作。
- 原因：当前能力并非空白，而是候选采用、内容包编辑/版本、平台适配、导出发布和复用分散在多层，存在两套采用、两套自由调整、正式 `copy.adapt` 与客户端前缀占位、至少四套交付入口以及已退役的“做同款”命令。共享语义可以消除用户对“保存、采用、调整、交付”的猜测，媒介专属工作面又能保留图片逐张和视频逐镜等真实任务，不把产品降级成通用结果卡。
- 影响：首轮开发应先建立纯投影 Result Shell phase/action adapter，再把三个工作区接入统一采用/调整/交付/版本合同，随后补齐文案/图文成品预览、图片采用后套图编辑、视频完成态与移动端结果动作；现有 Harness candidate、ContentPackage revision/OCC/variant/export/ApprovalReceipt、媒体 Asset、VideoWorkflow 与 Light Composer 能力优先复用并收敛重复入口。详细 P0/P1、状态矩阵、移动与无障碍验收见 `references/analysis/user-journey-ui-benchmark-2026-07-20/result-workspace-action-contract.md`。
- 证据边界：当前代码和测试是主要事实源；小云雀旧视频结果可支持重新生成、提升画质和字幕处理方向，CreatOK 与讯飞绘文可支持任务历史、资产沉淀、文档编辑和平台预览方向，但 2026-07-20 未提交新的竞品生成任务，不能声称其当前账号已验证本文完整动作集。本文是我方产品合同，不是竞品界面复刻。
- 待继续拍板：“交付”面板首批组合已由 D-086 关闭，图片单图/套图采用最终文案已由 D-087 关闭，视频局部重生与完整视频重生的产品用量边界已由 D-088 关闭，Result Shell 与现有 ContentPackage detail 的迁移方式已由 D-089 关闭。
- Supersedes：闭合 D-073、D-081、D-082、D-083、D-084 中“三类结果统一动作命名和平台交付边界/三个成品工作区结果动作”的开放项；仅在前台命名上取代 D-077/D-078 的“做同款”为“基于此再创作”，内部 reuse/recipe 术语可保留但不得外显；不改变 D-073 三类成品工作区与 canonical 真相集合、D-046 常驻调整口、D-043 ContentPackage 真相、D-081 用户主动选择创作对口或 D-068/D-069 三模态首轮范围。

## D-086 “交付”面板采用能力感知的分层交付

- 日期：2026-07-20
- 状态：`accepted`（2026-07-20 用户选择方案 B；2026-07-20 D-098 C2 收窄首轮：自动发布 delivery attempt 状态机后置到首个平台过 live gate，首轮全建「拿到文件」+assisted 交接）
- 决定：文案/图文、图片、视频三个成品工作区共用同一个能力感知“交付”面板，并按用户结果而不是技术通道分成“拿到文件 / 交接到平台 / 直接发布”三组。复制、单项下载和完整发布包是所有适用成品的底线能力，只需 canonical/adopted revision 和对应权限，不要求发布 ApprovalReceipt；系统分享只在当前设备和 payload 真实支持时出现，不支持、用户取消或失败时明确降级为复制交接链接或下载，不把“已交给系统分享”写成“已发布”。
- 决定：“交接到平台”首发包含 assisted 人工交接。准备本地交接资料不产生“已交接”回执；只有内容真正通过系统分享或 assisted 发送给外部责任人/目标端时，才要求绑定精确目标平台、目标账号或责任人、ContentPackage revision、用途、时间、费用范围和一次性 ApprovalReceipt，并生成交接 receipt。状态使用“交接资料已准备”“已交接”“待人工发布”“已记录发布结果”等诚实语义。人工交接成功只证明资料已经交给后续责任人或目标端，不证明外部平台已经发布。
- 决定：“直接发布”不是固定按钮或后台开关。只有精确的平台账号授权版本、publishing adapter revision 与 publishing deployment/probe 达到 `automatic_verified`，且当前操作者具备 `publication.handoff` 权限、目标 revision 已取得 ApprovalReceipt 时才显示。提交时冻结平台、账号授权版本、ContentPackage revision、ApprovalReceipt、幂等 delivery attempt、adapter/deployment/capability revision、发布参数、用途、费用与有效期；成功必须返回可核验结果 receipt。未绑定、未验证、授权过期、能力探针失败或 adapter 恢复门未通过时，不显示或暗示自动发布，自动降级为完整发布包和 assisted 交接。按当前生产装配，首发 `automatic_verified` 平台数明确为 0；任何平台只能通过独立上线票据和真实账号验收后逐项开放。
- 决定：首批目标载体为小红书图文、抖音视频、微信视频号和朋友圈，但“首批目标载体”不等于“首批自动发布平台”。小红书包包含 `caption.txt`、封面、按发布顺序编号的图片、平台 checklist 与权利/AIGC/事实核对摘要；抖音和视频号包包含 `video.mp4`、`cover.jpg`、`caption.txt`、`subtitles.srt` 或 `subtitles.vtt`、平台 checklist 与权利/AIGC/事实核对摘要；朋友圈包含分段文案、图片/视频单项文件、系统分享和完整交接包，继续作为 distribution/export target，不为展示按钮而升级成 ContentPackagePlatform 或自动发布平台。
- 决定：`delivery_ready` 是 Result Shell 尚未创建 attempt 的可交付阶段；创建后的 delivery attempt 生命周期为 `delivering → delivered | partial_delivery | delivery_failed | delivery_unknown`。`delivery_unknown` 表示网络中断或外部响应不足以判断平台是否接受，只允许 query/reconcile，不允许盲目重发。首版一个 attempt 只面向一个平台/账号，平台内存在多个交付对象时，只有全部对象均有成功 receipt 才进入 `delivered`；部分成功必须逐对象展示成功、失败、未知和待人工确认，重试只创建针对已确认失败对象的新 attempt，并复用同一 adopted revision，不重复交付已成功对象，unknown 对象只核验。跨平台批量调度不进入首版。重试前重新校验平台账号授权版本、ApprovalReceipt 有效期、publishing probe 和能力快照；revision、账号、发布参数、用途或费用变化时必须重新批准。
- 决定：移动端继续遵守 D-085 的一个 primary 加“更多”预算。“交付”打开全高或接近全高的能力面，先显示当前最安全的下一步，再显示可用分组、费用/账号/平台摘要和状态；不支持的能力可以在需要解释时显示原因与替代路径，但不得以灰色“直接发布”制造能力已存在的错觉。键盘、读屏和焦点恢复必须区分下载完成、系统分享完成、资料已交接与平台已发布四种结果。
- 原因：当前产品已有平台 variant、导出、ApprovalReceipt、assisted 记录、成功 receipt 和失败重试等接缝，但它们分散在 ContentPackage detail、导出载体和移动交接路径中。按结果分层可以在不虚构平台能力的前提下完成三模态首轮交付，也允许未来某个平台和账号通过真实验收后原位升级为自动发布，不必重做结果工作区或另建一套发布首页。
- 影响：首轮开发先统一 delivery capability resolver、`publication.handoff` 权限映射、发布包 manifest、系统分享 fallback、canonical assisted handoff token/page（复用现有 `dashboard/handoff/$token` 页面范式——分享/下载/复制/人工回报四段已完整，替换数据源为 canonical delivery，不从零重做）、assisted receipt 和逐对象 delivery attempt 投影，再把三个 Result Workspace 与现有 ContentPackage detail 接入；平台自动发布只能按“平台账号授权版本 × publishing adapter revision × publishing deployment/probe”逐项通过独立 live gate 后开放。后台可管理发布包模板、目标载体、展示顺序和已发布 capability revision，但不能绕过代码级安全门、ApprovalReceipt、publishing live evidence 或回执合同。详细能力矩阵、包格式和验收边界见 `references/analysis/user-journey-ui-benchmark-2026-07-20/delivery-capability-contract.md`。
- 证据边界：小云雀、CreatOK 与讯飞绘文本轮未完成新的复制、下载、系统分享和真实平台发布闭环；竞品页面存在分享或发布入口不能证明交付成功。我方当前 image_text 已有确定性 ZIP 与 ExportReceipt，video 仅有单 MP4 导出，canonical Result/ContentPackage 尚未补齐统一复制、媒体直接下载、系统分享和完整视频包；现有 assisted 是绑定 revision/export receipt 的账本接缝，但可打开的 canonical handoff token/page 尚未完成；生产 publisher 对现有平台均未达到 `automatic_verified`。D-086 是目标产品合同，不代表这些缺项已经开发完成。
- 待继续拍板：发布包 manifest/ZIP、系统分享降级矩阵、assisted 责任角色与确认时限已由 D-096 关闭；各平台达到 `automatic_verified` 的逐平台上线票据继续按真实环境独立验证，不属于 UI 信息架构待确认项，也不阻塞复制、单项下载和完整发布包底座开发。
- Supersedes：关闭 D-085 的“交付面板首批平台与下载包格式、系统分享/assisted/自动发布首发组合”开放项；细化但不改变 D-028 文案/成片两层交付、D-043 ContentPackage 真相、D-060 模型供应凭据安全、D-069 三模态双渠道生成供应 live gate 或 D-085 Result Shell 动作预算。D-060/D-069 不构成任何平台发布能力证明；平台账号授权与 publishing adapter/deployment 必须独立验证。

## D-087 图片采用按成品角色使用明确动作，套图采用保留专属组装工作面

- 日期：2026-07-20
- 状态：`accepted`（2026-07-20 用户选择方案 B，并采纳方案 C 作为套图专属工作面）
- 决定：图片候选不统一显示含糊的“采用所选”。Result Shell 根据当前 Recipe 输出合同、交付物类型和明确目标槽位只显示一个角色化主动作：独立单图/海报使用“采用这张”，当前内容主图使用“选为主图”，图文封面使用“设为封面”，正在组装套图的单张候选使用“加入套图”，完整候选套图使用“采用这组”，替换已采用槽位使用“替换当前图片”。同一情境不得并列多个含义接近的采用按钮；如果主图与封面是同一角色，只显示当前平台/交付物使用的用户语言。
- 决定：动作完成反馈必须指明结果和位置：分别使用“已采用这张图片”“已设为主图”“已设为封面”“已加入套图，第 N 张”“已采用这组，共 N 张”“已替换，原版本仍可恢复”。不再使用“保存结果”“操作成功”等无法判断是否写入当前内容、素材库或版本历史的反馈。
- 决定：“采用这张”“选为主图”“设为封面”和“采用这组”把候选写入 canonical ContentPackage revision 或明确成品槽。首次采用没有现成 ContentPackage 时，命令绑定来源 Work/Task、workspace、Recipe revision 和稳定幂等键，以 create-if-absent 语义原子创建唯一 ContentPackage；已有包时才携带 expectedRevision/OCC 更新。丢响应重放必须返回同一包与 revision，不得创建第二个 ContentPackage。整组采用必须原子写入同一个 revision，冻结有序不可变媒体版本引用和来源。首版不新增独立 PrimaryImage/Cover 实体：image_text 当前 variant 的有序视觉列表第 1 张就是该 variant 的主图/封面，Recipe/deliverable 决定前台使用哪一个称谓；“设为封面”本质是创建一个把目标图移到第 1 位的新 ContentPackage revision。候选组装阶段的“加入套图”、移除、排序和设封面先修改可撤销的本地 working selection，不创建中间 ContentPackage revision；其中设封面反馈为“已设为本组封面，采用这组后生效”，并持续显示未采用状态。用户执行“采用这组”时才一次提交。已经采用后的增删、排序、设封面或替换属于确定性编辑，每次创建新的 derived revision，旧 revision 可恢复，不原地覆盖。
- 决定：套图专属工作面吸收方案 C，仅在交付物确为套图或用户明确进入组装时出现。它包含候选区、当前套图、角色/顺序、设封面、替换、移除与“采用这组”；独立海报、单图主图和封面不得被迫经过套图托盘。整组已有完整候选时可以直接“采用这组”，不要求逐张重复确认。
- 决定：继续调整按影响范围使用“调整这张”和“调整整组”，公共自然语言入口仍为“还想怎么改？”。模型调整继承 D-046，创建 derived Task、完整审计血缘和新的候选不可变媒体版本，不新增 message/thread 实体；确定性图像编辑同样生成新的候选不可变媒体版本，原媒体对象永不修改。首轮由新的 owned media asset id 加 parent/source lineage 承载版本节点，不复用 `reuse-memory.ts` 中表示可复用配方/系列的 `AssetRevision` 类型。新结果默认不替换当前 ContentPackage，用户必须通过“替换当前图片”或“采用这组”再以 OCC 写入。逐张调整不默认重跑整组；整组调整、比例变化或统一风格等会影响多张图片时，提交前必须显示影响范围、公开模型、目标输出规格、将使用的图片额度和完成后的预计余额，不展示供应商报价公式或内部重试时长。整组运行可保留部分成功的候选用于查看或重试失败项，但不得部分采用到 ContentPackage，再次“采用这组”仍须原子提交完整有效集合。
- 决定：“采用”和“保存到素材库”继续是两套独立语义。单张素材动作使用“保存到素材库”，多选使用“保存选中图片到素材库”，成功后显示“已在素材库”；只保存明确、持久、具备权利和 parent/source 血缘的不可变媒体版本，不复制 Provider 临时 URL。采用不得自动创建可复用素材，保存素材也不得改变当前 ContentPackage 的主图、封面或套图顺序。在不可变媒体版本与血缘落库前，这两个素材动作不得以正式能力上架。
- 决定：桌面继续遵守一个 primary、最多三个 secondary；移动端只保留一个 primary 与“更多”。移动端套图工作面支持选择、排序、设封面、移除、采用整组和撤销，但复杂自由布局交给桌面或 Pro Studio。图片选择态不能只靠边框颜色或勾选图标，读屏名称必须包含角色、顺序和是否已采用；完成后礼貌播报精确反馈并把焦点送回更新后的图片或套图摘要。
- 原因：当前代码能够通过 `adopt_into_content_package` 一次写入有序视觉 Asset 集合，也能把后续图片任务产物附加到已采用 ContentPackage，但 canonical 图片画廊仍只有预览，缺少角色槽、单图采用、整组采用、替换和排序合同。角色化语言让商家在提交前知道图片会放到哪里；套图专属工作面又能保留小红书/活动套图的顺序与整体审阅，不把单图任务拖入批量组装流程。
- 影响：首轮开发需为图片 Result Workspace 增加由 Recipe/deliverable 推导的 adoption target、可撤销 working selection、角色/顺序投影、原子 group adoption、slot replacement 和 adopted-state adapter。“加入套图/移除/采用前排序/设为本组封面”属于本地 working-selection typed intent/reducer，不调用 canonical 写命令；只有“采用这张/选为主图/采用这组/替换当前图片/已采用后设为封面”等提交动作编译为同一服务端 visual-adoption command，不建设多条写入链。现有 `adopt_into_content_package` 的有序 `visualAssetIds`、ContentPackage 版本/OCC 与后续 generation attachment 可复用，但必须补齐不可变媒体版本 parent/source 血缘和 image_text 非空、唯一、image-only 约束，不能继续用“附加整次任务产物”代替用户选择。详细语言、写入和验收合同见 `references/analysis/user-journey-ui-benchmark-2026-07-20/image-adoption-contract.md`。
- 证据边界：小云雀、CreatOK 与讯飞绘文本轮均未完成新的图片生成与采用闭环，不能声称其已验证本文中文动作。CreatOK 的角色化套图和讯飞绘文的逐图多版本/整批导出只支持对象层级方向；我方当前普通媒体也没有通用 revision 类型或一等候选组，`reuse-memory.ts` 的 `AssetRevision` 是可复用配方/系列而非图片二进制版本。D-087 是目标产品合同，不代表上述媒体版本、候选组或角色动作已经实现。
- 待继续拍板：Recipe 图片输出类型、目标槽位、套图门槛与 working selection 恢复边界已由 D-095 关闭；本条开放项已全部闭合。
- Supersedes：关闭 D-085 与 `result-workspace-action-contract.md` 中“图片单图/套图采用最终文案”开放项；细化 D-013 的可逆采用、D-043 ContentPackage 唯一真相和 D-085 的图片媒介专属工作面，不改变 D-046 模型调整 derived Task 合同或 D-084 移动端一个 primary 加“更多”的动作预算。

## D-088 视频局部重生与完整视频重生均为新的用户生成任务

- 日期：2026-07-20；2026-07-24 按 D-105/D-109 修订产品语言与执行边界
- 状态：`accepted`
- 决定：用户每次明确提交“局部重生/模型原生编辑”或“重新生成完整视频”都创建新的 derived Task/GenerationJob，并产生一次新的产品用量预占；它们不是对旧成片的免费确定性编辑。当前成片与历史 revision 始终保留，新结果作为候选版本出现，不自动覆盖已采用成片。
- 决定：前台只展示公开 CatalogModel、动作范围、比例/分辨率、目标成片时长、输出数量、本次将使用的视频额度和完成后的预计余额。用户不需要理解 `per_request`、`per_output_second`、最低消费、取整、Token、供应商单价或 fallback；这些只在后台 SupplierCost / Route / ProviderAttempt 快照中冻结和对账。
- 决定：一个用户主动任务只产生一次幂等的 ProductUsage 预占/结算。任务内部的 Planner、评估、ProviderAttempt、质量门重试、fallback、恢复和下载可以产生多笔供应成本，但不得静默重复扣用户额度；用户再次主动提交“再生成一次/换一版”才是新任务并再次使用相应产品次数。
- 决定：同一 supplier task 的轮询、恢复、下载、采用候选、纯确定性排序、复制文案和独立字幕文本修改不使用新的生成额度。任务最终未受理或系统失败时释放/退回一次；部分交付按能力包 `UsagePolicyRevision` 退回未交付部分，不把未知供应成本伪装为零。
- 决定：局部镜头或模型原生编辑结果可以作为独立 VideoAsset 候选，但本产品不再把它与其他片段本地拼接为完整成片。若用户需要新的完整视频，必须调用模型原生完整生成/编辑能力创建新任务；复杂剪辑交给外部专业工具，处理后的文件可重新上传为有血缘的新来源资产。只有完整 `VideoArtifact` 才能被采用进 ContentPackage 的视频成片槽。
- 决定：供应商按请求、输出秒数或其他规则的真实价格、usage、接受态、重试与退款证据继续由不可变 RouteSnapshot / ProviderCostSnapshot / ProviderCostLedger 承接；上游超产或系统重试造成的差额由平台成本承担，不对已确认的产品次数补扣。
- 原因：用户需要理解的是“这次会得到什么、使用几次视频额度、还剩多少”，不是供应商如何计费；同时 FFmpeg 创意拼装退场后，“整段重新合成”已经不是有效产品动作，必须改为模型原生的完整视频重生。
- 影响：视频 Result Workspace 统一使用 `UserDebitPreview -> reserve -> dispatch -> settle/refund` 产品合同，并把供应商技术计价留在后台；旧 `video-regeneration-billing-contract.md` 中面向用户展示按秒公式和本地合成的部分不再适用。
- 待继续拍板：各上游真实 usage 证据优先级、接受后取消/失败的供应成本退款矩阵，以及不同公开规格映射 1 次或多次视频额度的商品规则；均不得改变“系统重试不重复扣、用户主动重生是新任务”的主合同。
- Supersedes：关闭 D-085 的视频重生开放项；取代本条旧版“整段重新合成”和面向用户展示按秒/金额技术报价的口径；继续细化 D-059 的接受态重试边界、D-061/D-066 的双账本、D-065 的供应价格证据与 D-073/D-085 的视频工作区动作。

## D-089 新建独立结果中心，并将工作台与内容详情收敛为相邻但不竞争的入口

- 日期：2026-07-20
- 状态：`accepted`（2026-07-20 用户选择方案 C；2026-07-20 D-098 C1 改写首轮迁移形态：直接建新面+同轮退旧分支，灰度分桶/影子投影/renderer 回滚/Wave 1 兼容 panel 后置到 D-040 运营重启）
- 决定：新增独立、稳定的 Result Center 路由 `/dashboard/results/$workId`，作为文案/图文、图片、视频从运行、恢复、候选采用、继续调整到交付的主工作区。用户明确开始生成并取得 durable `workId` 后进入该路由（2026-07-24 D-114 修订：定制创作提交后不自动跳转，成品在对话流内以成品预览卡呈现、点击成品卡才进入本路由［放大镜］；独立路由与 ResultShellModel 投影/命令合同保留）；异步任务即使尚未完成也由结果中心承接进度、后台继续、恢复/核验、取消和失败处理，不等到成品完成才建立临时结果页。浏览器返回应回到原 Composer 草稿与触发位置，不能创建第二份草稿。Result Shell 运行阶段承载 ADR-0007 的 token 级流式候选呈现（文案/图文自首个 token 起流式渲染），阶段级 polite 聚合播报仅是其无障碍聚合层，二者并存不互替，不得以 Job/阶段级进度条替代 token 流式（2026-07-20 交叉复核 F1 补句）。
- 决定：新增的是独立页面和导航边界，不是新的业务实体或写入真相。Result Center 必须使用 D-085 的唯一纯投影 `ResultShellModel` 和统一 `ResultCommandAdapter`，状态仍从 Task、Work、Job、Asset、ContentPackage、RouteSnapshot、账本与交付回执推导；所有采用、调整、重生成、版本、交付和恢复动作继续落回既有 canonical command、幂等键、expectedRevision/OCC 与审计链。禁止新增 Result 表、Result status、第二历史、第二资产、第二发布记录或仅供新页面使用的平行命令。
- 决定：Result Center、Composer 与 ContentPackage detail 的职责明确分离。Composer 负责表达意图、选择对口、装配输入与提交；提交后只保留可恢复的运行/完成摘要和“查看结果”入口，不再长期维护完整结果编辑器。Result Center 负责结果处理和成品工作面。`/dashboard/content/$contentId` 继续作为内容库中的稳定档案、版本/来源/交付证据深链，但其与结果中心重叠的采用、自由调整、重生成和主动交付控件要逐步迁移或改为打开结果中心，不能形成第三套完整编辑面。历史包若没有可解析的来源 Work，先保留现有详情兼容能力，并明确标记为 legacy fallback；不得猜测或绑定“最新 Work”。
- 决定：`workId` 是结果中心主路径标识。路由可携带可分享的 `contentId`、不可变 `versionId`、`panel` 与语义化 `focusKey`，但服务端必须校验它们确实属于该 Work 的 lineage；不匹配时显示可恢复错误，不静默切到最新内容。阶段不写入 URL，而是按 canonical 对象重新投影。来自内容详情、历史、通知、任务和跨设备中继的入口应尽量解析到同一 `workId`；无法解析的 legacy ContentPackage 留在原详情页。跨设备只传已持久化状态，不伪装同步未保存编辑。
- 决定：返回与恢复至少保存来源路由、筛选、滚动、触发点焦点、当前 panel、选中对象、基线 revision 和未提交编辑键；未提交编辑按 `{workspaceKind, workId, baseRevisionId, surfaceVersion}` 隔离。刷新或发现 revision 已前进时提供恢复、对比或丢弃，禁止把旧草稿静默套到新 revision。显式 `workId` 或 `contentId` 失效时必须显示 not-found/recoverable 状态，不得回落到最近结果。
- 决定：迁移采用新路由 strangler，而不是一次性重写。Wave 0 先建立共享 target resolver、phase/action adapter、command adapter、路由恢复与 shadow projection；Wave 1 新结果中心以旧结果 body 作为临时兼容 panel，桌面和移动同时内部灰度；Wave 2～4 依次迁入文案/图文、图片、视频工作面；Wave 5 收敛历史、通知、任务、内容详情和设备中继入口；Wave 6 删除工作台完整结果分支及 ContentPackage detail 的重复主动动作。波次只表示实施顺序，文本、图片、视频与桌面、移动必须全部通过首轮发布门后才能对外宣称完成。
- 决定：灰度按账号稳定分桶，同一账号桌面与移动默认使用同一版本；一次页面会话中不得热切换 renderer。回滚只切回旧 renderer/旧入口，不做数据或 schema 回滚，因为新旧页面必须调用同一 canonical command。出现错目标/错版本写入、重复命令或扣费、未提交编辑丢失、旧深链失效、重定向循环、返回错误对象或键盘/读屏阻断时，立即关闭对应工作区或结果中心总开关。
- 决定：移动端结果中心继续遵守一个 primary 加“更多”、安全区、320px/390px/横屏/200% 缩放和唯一聚合 live region 合同。进入结果中心只在目标真正变化时聚焦结果标题；后台阶段变化使用 polite 聚合播报，不抢焦点。版本深链展示指定不可变版本并提示已有新版本，由用户显式切换。
- 原因：独立结果中心能让 Composer 保持轻量创作入口，让运行中与完成后的复杂结果处理拥有稳定深链和专属空间，也更适合承载三种媒介差异显著的工作面。代价是新增页面可能固化成第四套结果表面，因此必须用唯一投影/命令适配器、明确职责和旧结果面退场条件约束，而不能把当前工作台与 ContentPackageDetail 的逻辑复制到新路由。
- 影响：首轮需新增 canonical result route、ResultTargetResolver、共享 phase/action/command adapter、URL/return/draft 恢复合同和灰度开关；现有 `UnifiedCreationWorkbench`、`MobileActionBook`、`ContentLibrarySurface` 与 `ContentPackageDetail` 按上述波次拆除重复状态机和主动动作。详细迁移、路由、灰度、回滚与验收合同见 `references/analysis/user-journey-ui-benchmark-2026-07-20/result-center-migration-contract.md`。
- 证据边界：当前 `/dashboard` 已按 viewport 分成桌面 Workbench 与移动 ActionBook，两者各自维护结果状态和动作；`/dashboard/content/$contentId` 实际继续挂载完整内容库 surface，`ContentPackageDetail` 又承担编辑、版本、导出、审批、交付、重试、复用和回滚。D-089 是目标迁移合同，不代表独立结果路由、共享 adapter、精确恢复或旧入口退场已经开发完成。
- 待继续拍板：Result Center 的导航入口已由 D-090 关闭，legacy ContentPackage 没有来源 Work 时的兼容迁移已由 D-091 关闭；D-089 的迁移方式开放项已全部闭合。
- Supersedes：关闭 D-085 与 `result-workspace-action-contract.md` 中 Result Shell/ContentPackage detail 迁移方式的开放项；仅覆盖 D-085“不得再建立第四套结果页”的路由限制，允许新增一个独立 Result Center 页面，但不改变 D-085 的共享 Result Shell、三类媒介专属工作面、唯一 canonical 真相、动作预算、版本/OCC、交付和审计合同。

## D-090 结果中心不占用一级导航，作为稳定对象工作区从上下文入口进入

- 日期：2026-07-20
- 状态：`accepted`（2026-07-20 用户选择方案 B）
- 决定：Result Center 不增加“结果”一级导航，也不在“内容”下增加一个平行结果列表。商家一级导航继续保持“创作、内容、素材、门店”；结果中心是由明确 `workId` 定位的对象工作区，用户从创作提交/完成、最近创作、内容档案、任务状态、通知和可信深链进入，而不是先进入一个空泛结果首页再寻找对象。
- 决定：创作提交成功并取得 durable `workId` 后直接进入对应 Result Center（2026-07-24 D-114 修订：定制创作提交后不自动跳转，成品预览卡在对话流内呈现，点击成品卡才进入结果中心；本条其余入口/返回/状态文案合同不变）；如果用户选择离开并后台继续，创作首页“最近创作”、内容页相关记录、任务/通知入口都必须展示真实阶段与唯一下一动作，并链接同一个 `/dashboard/results/$workId`。入口文案按状态使用“查看进度”“处理当前问题”“继续调整”“继续交付”“查看结果”，不得统一写成含糊的“查看详情”。
- 决定：Result Center 路由不提供无 `workId` 的集合空态，不把 `/dashboard/results` 建成隐形第五个列表页。用户直接访问缺少或无权访问的目标时，分别返回缺少目标、not-found 或 forbidden；不得展示最近结果替代，也不得自动跳转到任意 Work。结果集合、搜索、筛选和批量管理继续由“内容”与任务/历史投影承接。
- 决定：返回规则按来源恢复。来自 Composer 的返回恢复原草稿、滚动和触发位置；来自最近创作、内容、任务或通知的返回恢复原筛选、分页、滚动和焦点。Result Center 页内的“回到创作”用于基于当前结果继续表达新意图，“返回”则回来源，两者不能合并成一个行为不稳定的按钮。
- 决定：桌面和移动使用同一入口语义。移动端可以在创作首页最近创作卡、系统通知或内容卡上提供一个明确主动作，但不得为提高曝光新增底部“结果”标签。后台可配置最近创作卡片数量、展示顺序和非关键文案，不能把一级导航、目标校验、状态动作映射或返回合同改成运营配置。
- 原因：结果中心是具体 Work 的处理空间，不是独立内容集合。新增一级“结果”会与“内容”、最近创作和任务历史争夺同一批对象，使用户难以判断“结果”和“内容”的区别；上下文入口既保留小云雀式的最近创作连续性，也能保持当前四项一级导航清晰。
- 影响：D-089 的 canonical result route 只实现对象详情路由；无需设计结果首页或第五个集合查询。首轮开发必须补齐 Composer、最近创作、内容、任务、通知与设备中继的入口矩阵、状态文案、可信 return key 和无目标错误页，并验证所有入口解析为同一 Work/revision。
- 证据边界：当前产品已有创作、内容、素材、门店一级结构，以及 Work/Task/ContentPackage 历史和详情入口，但各入口仍使用不同 query/path、粗粒度 `from` 和本地 stage；D-090 是目标导航合同，不代表入口收敛与精确返回已经实现。
- 待继续拍板：最近创作桌面/移动默认数量与通知阶段已由 D-097 关闭；后续可通过受控 Surface/通知策略 revision 调整，但不得改变“不设结果一级导航”和“结果集合归内容/任务投影”的边界。
- Supersedes：关闭 D-089“Result Center 是否提供常驻一级入口”的开放项；不改变 D-084 的模板/工具全屏目录、D-085 的结果动作、D-089 的独立对象路由或现有四项商家一级导航。

## D-091 无来源 Work 的旧 ContentPackage 按需创建受审计兼容 Work

- 日期：2026-07-20
- 状态：`accepted`（2026-07-20 用户选择方案 C；2026-07-20 D-098 C4 收窄首轮：只留 ResultTargetResolver 只读 legacy 分支，anchor 写路径与修复机器后置到真实 pre-lineage 内容出现）
- 决定：没有可解析来源 Work 的 legacy ContentPackage 不做全量预迁移。默认继续在原 ContentPackage detail 展示只读档案、不可变版本、来源、导出与交付回执；只有用户明确执行“继续调整”或需要进入 Result Center 完成主动交付时，系统才按需创建一个 compatibility Work anchor，并导航到 `/dashboard/results/$workId?contentId=$contentId`。仅查看、下载既有文件、复制既有成品或查看历史不得触发迁移。
- 决定：兼容 Work 创建必须是服务端幂等命令，稳定幂等键至少绑定 workspace、legacy ContentPackage id 与 migration contract revision；并发点击、丢响应重放或从不同入口触发都返回同一个 compatibility Work，不得为同一旧包制造多个 anchor。创建前再次校验 workspace ownership、操作者权限、当前 ContentPackage revision 与是否已存在有效 anchor；已存在时直接复用。
- 决定：compatibility Work 明确标记 `origin=legacy_import`、source ContentPackage、source revision、actor、reason、migration contract revision、createdAt 和审计事件。它只提供 Result Center 所需的稳定 target 与 lineage，不伪造原始 Recipe、prompt、模型、Provider、RouteSnapshot、报价、生成 Task/Job 或不存在的权利证据；缺失字段保持 unknown/not_recorded，并在运行详情诚实显示“旧内容未记录”。
- 决定：创建 compatibility Work 本身不调用模型、不请求上游、不创建 ProviderAttempt、不预占或扣减产品用量，也不生成新的 ContentPackage revision。进入结果中心后的查看和确定性本地操作继续按各自动作合同处理；用户随后明确提交 AI 调整、重新生成或模型原生完整视频重生时，才创建新的 derived Task/Job、更新 `UserDebitPreview` 并使用相应产品额度，来源血缘指回 legacy ContentPackage 当前精确 revision 与 compatibility Work。
- 决定：原 ContentPackage 及其 revisions、Asset 引用、ExportReceipt、ApprovalReceipt 和 DeliveryAttempt 仍是历史真相。compatibility Work 不复制这些对象，只引用精确 id/revision；ResultTargetResolver 必须验证引用关系。旧详情页在 anchor 创建后显示“继续在结果中心处理”，后续主动采用、AI 调整、重生成和交付动作只进入同一结果中心；原详情保留档案与回执深链，不再维持第二套主动命令。
- 决定：迁移失败必须保持原详情可读，并给出可重试、权限不足、版本已变化或数据不完整等明确原因；不得创建半成品 Work 后静默跳转。若 Work 已创建但响应丢失，恢复/重试通过幂等查询找回同一个 Work。compatibility Work 不允许由普通用户删除以断开历史；若发现错误绑定，只能由受权修复命令追加审计并重新绑定或撤销其可用状态，不能改写审计历史。
- 原因：全量生成兼容 Work 会为从未再次使用的旧内容制造大量伪运行对象，并容易让不存在的生成事实看起来真实；永久只读又会阻断旧内容进入统一结果中心继续产生价值。按需、幂等、无费用的 compatibility anchor 能在用户确有意图时补齐路由目标，同时保持旧内容历史和缺失证据诚实。
- 影响：首轮需新增 `ensure_legacy_content_work_anchor` 类型化命令、唯一约束/幂等 registry、ContentPackage→compatibility Work 映射查询、ResultTargetResolver 的 legacy 分支、旧详情入口与迁移失败/恢复 UI。后台审计可按 `legacy_import` 查看数量、失败和错误绑定，但不得提供无条件全量迁移按钮。
- 证据边界：当前已有 ContentPackage 迁移与 legacy read-only 投影基础，但不存在 D-091 所定义的 Result Center compatibility Work 合同；本条是目标迁移边界，不代表命令、唯一约束、映射查询或 UI 已实现。
- 待继续拍板：compatibility Work 的长期保留期与错误绑定修复权限；这些属于实现和治理细化，不改变按需创建、幂等、无生成费用、不伪造历史和单一结果入口的主合同。
- Supersedes：关闭 D-089 中 legacy ContentPackage 无来源 Work 的最后开放项；不改变 D-043 ContentPackage 唯一内容真相、D-085 Result Shell 纯投影、D-088 新生成独立计费、D-089 独立结果中心或 D-090 上下文入口边界。

## D-092 首批 standalone 工具采用四项均衡组合与显式上下文 handoff

- 日期：2026-07-20
- 状态：`accepted`（用户六项统一选择方案 B）
- 决定：首批 standalone 工具固定为多平台尺寸重排与导出、批量去背景、视频字幕擦除与修复、Pro Studio 无限画布。前两项使用聚焦 dialog/route，字幕修复使用独立 route，Pro Studio 继续使用 canonical workspace gate。基于此再创作、节点营销和旧内容换平台继续是 Composer Recipe；首尾帧生视频继续是视频 Composer 输入模式；品牌 VI、详情长图和短剧工具在能力与需求通过独立发布门后再上架。
- 决定：独立工具允许从明确的 Work、ContentPackage 或 Asset revision 显式带入上下文，入口必须显示“带入当前内容/素材”及对象摘要；只传稳定 id/revision、角色和最小设置，不把完整 prompt、供应商信息或敏感草稿写入 URL。打开、预览和返回零业务写入，用户在工具内明确提交后才创建 canonical Task/Work/Job、报价、账本与结果。
- 决定：首页普通工具仍遵守桌面最多三项、移动最多两项，Pro Studio 为独立专业横条；不能为了凑数展示尚无真实运行能力的条目。能力真实存在但套餐未解锁时可显示锁定原因，能力未通过发布门时必须隐藏。
- 原因：四项覆盖图片批处理、视频清洁、多平台交付和高级工作区，同时不把 Composer 模式重复包装成工具；显式 typed handoff 保留连续性，又避免打开工具即写入或形成第二草稿真相。
- 影响：首轮建立四个 CreativeToolEntry revision、typed ToolHandoff、零写入打开/返回、统一报价与结果 adoption；六项联合合同（汇总级，各项深度在 D-092~D-097 条内）见 `references/analysis/user-journey-ui-benchmark-2026-07-20/ui-journey-six-open-items-contract.md`。
- Supersedes：关闭 D-077 的首批 standalone 清单与上下文携带开放项；细化 D-078 的两个普通首页切片，不改变工具必须通过可用性交集才能上架的发布门。

## D-093 完整目录采用双 tab、任务语言分类与十二项搜索门槛

- 日期：2026-07-20
- 状态：`accepted`（用户六项统一选择方案 B；2026-07-20 D-098 C5 收窄首轮：只建 published-visible 计数+12 项门槛闸，搜索索引/匹配随任一 tab 真实跨线再建）
- 决定：模板与工具共用一个 canonical 全屏目录路由，使用 `模板 | 工具` 两个 tab；入口决定初始 tab，切换 tab 不选择创作对口、不创建任务。模板分类为精选、文案、图文、视频、内容复用；工具分类为全部、图片处理、视频处理、发布与适配、专业工作区。分类内默认使用后台已发布的人工顺序，不做不可解释的个性化重排。
- 决定：某个 tab 的已发布可见条目达到 12 项时显示搜索；少于 12 项只显示分类与筛选，避免空洞搜索框。搜索匹配已发布名称、副文案、用户可见标签和任务关键词，不检索 hidden prompt、Provider 或内部 operation；URL 仅保存 kind/tab、分类、查询词、Surface revision 与 return key。
- 决定：移动端继续使用全屏目录，不降级为 Bottom Sheet；未发布或能力未过门的条目不参与数量、分类或搜索。返回恢复原 tab、筛选、查询、滚动和焦点。
- 原因：双 tab 保持模板与工具语义分离，十二项门槛避免小目录过度设计，又给增长后的查找留出确定触发点。
- 影响：首轮需实现统一 catalog route、两套分类、published-visible 计数、条件搜索和返回恢复；不建设独立模板页与工具页两套目录运行时。
- Supersedes：关闭 D-077/D-084 的目录分类、排序与搜索门槛开放项。

## D-094 普通任务直接开始，高成本复杂或高风险任务进入条件 Brief

- 日期：2026-07-20
- 状态：`accepted`（用户六项统一选择方案 B）
- 决定：简单文案、单图和自由创作的标准单次视频，在事实、权利、来源与产品额度均完整时继续直接开始，不统一增加 Brief。以下任一条件命中时显示紧凑条件 Brief：定制视频需要先确认 Storyboard/ReferenceIntentPlan；多交付物或跨平台组合；图片数量超过四张；使用顾客案例、前后对比、评价等受限素材；价格、期限、效果、资质等高风险事实缺失/冲突；本次产品额度达到 UsagePolicy 的额外确认门槛；公开模型、范围、关键规格或额度变化使原确认失效。
- 决定：条件 Brief 只汇总目标成品、平台、来源/权利、关键事实、公开模型与设置、影响范围、将使用的图片/视频额度、完成后预计余额和待确认项；可靠完成时间可作非计费状态提示，不展示供应商单价、按秒公式或内部重试时长。确认绑定精确 draft/recipe/model/usage-policy/internal-quote/source revisions，发生变化必须重新确认；取消返回原 Composer 且不丢输入。
- 决定：只有系统建议、来源提取或派生事实实际参与当前草稿时显示证据抽屉。最少展示来源名称与类型、被使用的事实及应用位置、更新时间/新鲜度、授权/权利状态、不确定/冲突和待确认项；不得暴露 Provider、hidden prompt 或内部路由。
- 原因：条件 Brief 保留简单任务的短路径，同时给高成本、高风险和复杂组合一个可解释确认面；证据抽屉只在有证据可说明时出现，避免装饰性“AI 依据”。
- 影响：首轮需实现服务端 Brief trigger projection、QuotePolicy 门槛引用、revision 绑定、证据摘要与取消恢复；触发规则属于代码/版本化安全策略，普通运营配置不得关闭视频、权利或高风险事实门。
- Supersedes：关闭 D-074 的条件 Brief 和证据抽屉开放项；D-081 的用户主动选择创作对口继续有效。

## D-095 图片采用稳定输出类型和目标槽，两张起进入套图并支持显式跨设备草稿

- 日期：2026-07-20
- 状态：`accepted`（用户六项统一选择方案 B）
- 决定：首批 Recipe 图片输出类型为 `single_image | ordered_image_set`，采用目标槽为 `standalone | primary | cover | gallery`。它们是 Recipe/adoption 合同字段，不新增图片角色实体；小红书/朋友圈套图使用有序集合，第 1 张为当前交付物的封面/首图，海报和单主图使用 `single_image`。
- 决定：交付物预期两张及以上时默认进入套图工作面，支持排序、设封面、替换、移除和采用整组；用户仍可明确切换为单图处理。采用前 working selection 不创建 ContentPackage revision，整组采用继续按 D-087 原子提交。
- 决定：未采用 working selection 在同一设备自动恢复七天；跨设备不同步隐式本地选择。用户明确点击“保存草稿”后，才把 typed selection 作为 Work draft revision 保存，不创建 ContentPackage revision、不调用模型、不扣费；恢复时绑定 base revision，漂移后必须比较、丢弃或重新应用。
- 原因：两张即产生顺序和封面问题，进入套图比到三张或四张才切换更一致；本机自动恢复降低误关损失，显式保存才跨设备则避免每次拖动都制造服务端写入和冲突。
- 影响：首轮补 Recipe 枚举、adoption target resolver、两张门槛、本地七天恢复、显式 Work draft selection 与 stale 处理。
- Supersedes：关闭 D-087 的图片角色、套图门槛和 working selection 恢复开放项；不改变其原子采用、素材分离和不可变媒体血缘。

## D-096 交付采用 v1 manifest、确定性 ZIP、文件优先分享与双责任角色

- 日期：2026-07-20
- 状态：`accepted`（用户六项统一选择方案 B）
- 决定：完整发布包使用 `beauty-delivery-manifest/v1`，至少记录文件角色、顺序、MIME、大小、校验和、目标平台、ContentPackage revision、权利/AIGC/事实摘要和生成时间；不得包含 Provider、Credential、hidden prompt 或敏感内部路由。ZIP 名称为 `{门店}-{内容类型}-{平台}-{YYYYMMDD}-{短revision}.zip`，对非法字符、空名称、重名和长度做确定性清理。
- 决定：系统分享按能力降级。纯文案优先分享文本；图片/视频在设备 `canShare`、数量和体积满足时优先分享文件；不满足时分享一次性交接链接，再降级为下载。默认不同时发送文件与链接，避免接收者拿到两个版本；取消分享不记已交付，系统接收分享也不等于平台已发布。
- 决定：assisted 首发责任角色只设“当前账号本人发布”和“指定外部发布责任人”，不因此建设完整成员/RBAC。交接后 24 小时未确认则提醒并保持待确认，一次性交接链接默认 72 小时失效；“已交接”与“已发布”继续分离，后者必须有外部结果回执或人工记录。
- 原因：版本化 manifest 与确定性包名保证可核验交付；文件优先、链接和下载分级覆盖设备差异；双角色足以承接当前单账号产品，又不提前建设组织权限系统。
- 影响：首轮需实现 manifest schema/validator、确定性打包、Web Share 能力矩阵、一次性链接、24 小时提醒、72 小时失效和 assisted receipt 状态。
- Supersedes：关闭 D-086 的 manifest、ZIP、系统分享 payload、assisted 责任角色和确认时限开放项；不把任何平台升级为 `automatic_verified`。

## D-097 最近创作默认桌面六条移动四条，只通知关键可行动状态

- 日期：2026-07-20
- 状态：`accepted`（用户六项统一选择方案 B；2026-07-20 D-098 C2/C7：站内通知实现为引用式投影、四类无源事件与 D-086 逐对象交付投影同批建设；浏览器系统通知后置到真实用户阶段）
- 决定：创作首页最近创作默认桌面显示六条、移动显示四条，按最近有效活动排序；每项显示媒介、标题/任务摘要、真实 phase、更新时间与一个状态驱动下一动作，其余进入内容或任务历史。后台可发布数量和非关键文案 revision，但桌面不得超过六条、移动不得超过四条来挤占 Composer 主轴。
- 决定：站内通知只覆盖结果可用、需要选择/补确认、`acceptance_unknown` 需恢复核验、任务最终失败、交付部分成功/失败/未知、交付完成。普通排队、轮询进度、自动保存和确定性排序不通知。浏览器系统通知只用于用户主动授权的后台长任务，且复用同一通知事件，不重复制造第二条状态真相。
- 决定：最近创作卡与通知必须链接精确 `/dashboard/results/$workId` 及可验证 revision/panel；失效时显示 recoverable error，不打开最近结果代替。点击后标记通知已读不改变业务 phase；返回恢复原页面、滚动与焦点。
- 原因：六/四条与已确认首页密度相容，关键状态通知支持长任务恢复而不制造轮询噪声；精确深链继续遵守 D-089/D-090 的对象工作区边界。
- 影响：首轮需补 Recent projection、有效活动排序、六/四响应式上限、通知事件映射、系统通知授权与精确 result deep link。
- Supersedes：关闭 D-090 的最近创作数量和通知阶段开放项；不新增“结果”一级导航。

## D-098 UI/用户旅程决策交叉复核处置：七项收敛全部采纳

- 日期：2026-07-20
- 状态：`accepted`（2026-07-20 用户对七项收敛建议一次性全部采纳）
- 决定（C1 采纳）：D-089 首轮直接建 Result Center 新面（Wave 0 基建 + 三媒介工作面直接建），并同轮退场旧 workbench 结果分支与 ContentPackageDetail 重复主动动作；按账号分桶灰度、shadow projection、新旧 renderer 共存与“回滚到旧 renderer”、Wave 1 旧结果 body 兼容 panel 整体后置到 D-040 运营执行恢复（首批真实用户上线、需要不中断生产切换时）。独立结果中心、唯一投影/命令适配器、三媒介工作面等产品边界不变。
- 决定（C2 采纳）：D-086 自动发布 delivery attempt 状态机（`delivering→partial_delivery/delivery_unknown` 逐对象部分交付、unknown 只 reconcile、重试前重校授权/probe）后置到首个平台通过独立 live gate 升为 `automatic_verified` 时建设；首轮交付面=「拿到文件」（manifest/v1+确定性 ZIP+系统分享矩阵+一次性链接）与 assisted 交接及其 receipt 状态全建。D-097 站内通知实现为引用式投影（复用 pending-actions 收件箱、Task 状态与 deliveryEvents，不建独立 Notification 表）；「结果可用 / acceptance_unknown 需恢复核验 / 交付部分成功 / 交付完成」四类事件源与 D-086 逐对象交付投影同批建设。
- 决定（C3 采纳）：D-078 首轮只有 `CreationRecipeVersion` 与 `CreationSurfaceRevision` 进完整 `草稿→预览→校验→发布→回滚` 生命周期与可视编辑器；`CreationLensDefinition` 降为静态枚举（作为 Surface 内有序引用，D-081 已把对口钉死为三项），`CreativeToolEntry` 保留 typed schema 但作静态注册表种子；原子发布聚合收窄为 Recipe+Surface。出现对口在 2～4 内重新编组或第二批工具频繁上下架的真实运营需求时再升级为可发布对象。
- 决定（C4 采纳）：D-091 首轮只实现 ResultTargetResolver 的只读 legacy 分支（“历史档案”默认态）；`ensure_legacy_content_work_anchor` 幂等命令、唯一约束 registry、受权修复命令与 migration contract revision 后置到真实存在无来源 Work 的内容出现时。
- 决定（C5 采纳）：D-093 首轮只建 published-visible 计数与 12 项门槛闸；搜索索引与匹配实现待任一 tab 真实跨过 12 项再建（门槛逻辑本身即触发器）。
- 决定（C6 采纳）：确认 Day-0 点击计数口径——模板卡点击一击两用（选对口+套用，即 D-083 口径）；纯自由文本路径「点选对口(1)+开始创作(2)」恰 2 次显式操作，均符合 D-043 ≤2 击合同（零余量）。必选创作对口是模式选择器（radiogroup），不属于 D-031/D-043 所禁的字段填表或前置确认，占用 D-074 两次显式操作预算之一。V1（#60）e2e 断言按此口径重立基线，解除阻塞。（2026-07-24：本口径以定制创作 radiogroup 为前提；D-114 改对话流主容器后，定制创作线的 Day-0 计数与 e2e 基线须按「一句话意图＋确认卡超时放行」重立——D-043 ≤2 击产品合同不变，仅计数模型换算；自由创作线维持本口径。）
- 决定（C7 采纳）：D-097 浏览器系统通知（Web Notifications 授权+Service Worker）后置到真实用户阶段；站内通知矩阵保留首轮。
- 原因：2026-07-20 七路交叉复核（3 路代码实证 + 1 路引用忠实性 + 1 路一致性对抗 + 1 路铺开面评审 + 1 路票包对齐 + 主会话引证抽验与交叉裁决）结论：事实层零 P0、引用零 P0/P1、一致性零硬矛盾；需收敛的集中在「为尚不存在的现实建迁移/发布安全机器」与「把结构性钉死的小对象升级为全生命周期配置」两类首轮铺开面。完整报告见 `docs/reviews/ui-journey-decisions-xcheck-2026-07-20.md`。
- 影响：D-078/D-086/D-089/D-091/D-093/D-097 按本条修订执行（各条状态行已加指针）；#50-#60 票包按报告 §6 裁定执行（T4 工作台结果区、T6 场景 chips 为被取代面勿再加码；#60 按 C6 口径解除阻塞；T1/T2/Td 增补随新实现票落地）。同批已完成非决策性口径修正：ADR-0007 token 流式落点补句（D-089）、D-076↔D-043 与 D-078↔D-037 对账、D-081 桌面图片可达性精确化与竞品归因分家、D-084 Drawer 指向校正、D-085 VideoWorkflow 实质重构立项句与“至少四套交付入口”口径、D-086 handoff 页范式复用句、D-092 措辞降格、D-079 跳号标注、待拍板指针更新、「抖音项目成片」四处统一。
- 证据边界：本条记录复核处置与用户裁决。重大前提勘误：#50-#60 已实现并合入 main（`57abba8`/`3811050`/`0429e8d`/`d66a9be`/`e6348c3`，主会话抽验），存续裁定=5 durable/3 addendum/2 superseded/1 blocked（已由 C6 解除），详见报告 §6。
- 待验证：C1/C2/C4 后置件的解锁触发点实际到来时间（运营重启 / 首平台 live gate / 真实 pre-lineage 内容）；C6 口径在交互原型中的实测点击成本（D-074 待验证项继续有效）。
- Supersedes：收窄 D-078 首轮配置对象范围、D-086 首轮交付状态机、D-091 首轮写路径、D-093 首轮搜索实现、D-097 浏览器通知时点；改写 D-089 首轮迁移形态（直接建+同轮退旧，灰度机器后置）；细化 D-081/D-043 的点击计数与前置门语义对账。产品边界（独立结果中心、能力感知分层交付、版本化创作配置、按需兼容锚、双 tab 目录、通知矩阵、用户显式选对口）全部不变。

## D-099 Pro Studio 画布按上游对标基线重做（含三项范围拍板）

- 日期：2026-07-22
- 状态：accepted（2026-07-24 D-103：归入自由创作的高级画布面，不构成第三条产品主线）
- 决定：Pro Studio 画布按 `docs/evidence/pro-studio/upstream-parity-gap-baseline-2026-07-22.md` 逐行重做（G01–G48 行级核销索引，全量修复或票内记录豁免；⤴8 项 SaaS 增强不得回退）。实施口径=「挂载而非重写」：优先挂载 vendor 已拷 `mount-exact` 组件，组合根等按「移植（ports）+ 适配层替换 runtime 调用」处理（本机 Agent 桥仍 ⛔），并回写 copy-manifest/ports-manifest。**功能票（K2–K7）前置一层底座票 K1**（manifest/port 治理、生产组件白名单、BackendPort vNext 合同冻结、批量账本合同拍板、宿主 UI runtime、跨包属主协调）。三项范围拍板：① 用户侧模型选择**开放**（ModelPicker 接我方 catalog，D-044 平台默认降级为缺省值而非唯一值）；② zip 数据导出**要做**（与 adoption 并存：adoption=成品出口，zip=创作过程资产带走权，走独立 `pro-studio-canvas-export/v1` manifest，媒体从对象存储服务端收集）；③ Agent 对话式外壳**拆独立票**（上游 assistant 对话式 UI 包住既有三段确认/凭据/审计内核，流式对齐 ADR-0007，不并入本次画布重做主线）。
- 与 P0/P1 的边界：Pro Studio 继续以 `AdvancedCanvasProjectRevision + GenerationCheckpoint` 驱动节点级 GenerationJob，不进入 Composer 的 `CreationSubmissionCoordinator`、营销 `CreationExecutionSnapshot` 或五阶段 Harness；两线共享 Product Core 的 Catalog/Quote/Route/Provider/Usage/Cost/OwnedAsset/Capability/Audit 合同。batch 仅为 UI 聚合确认，每 item 一个 job/reservation；ContentPackage 仍只在显式 adoption 时通过唯一 revision port 写入。Canvas ZIP 虽使用独立 manifest，仍逐 Asset 执行工作区访问、私下取回资格与 export policy。
- 2026-07-24 产品归属：Pro Studio 是自由创作下的高级交互面；自由创作的基础路径仍是模型原生薄调用，不要求用户进入画布。Pro Studio 可保留画布草稿/Checkpoint，但不得自建第二套模型目录、用户额度、供应成本、OwnedAsset、结果历史或采用真相。
- 原因：K01–K11 验收只验"import 了 VozebCanvas+若干交互可走通"，无并排对标条款，导致上游 canvas 目录 46 候选拷 34（+8 util 计 42 行 manifest）、生产仅挂载 1 个视口组件、其余 exact-copy UI 未成生产可达组件，用户可见层全为自建简版——"验收不验体验"根因复发（同 ADR-0010 五根因）。修复靠验收基线逐行核销+并排走查，不靠重申原则。
- 影响：新发 Pro Studio 重做票包（K1 底座 → K2 组合根/交互 → K3 精修 → K4 生成 UX → K5 资源 → K6 工程管理/导出 → K7 对标验收，A1 Agent 外壳独立线）；spec=`docs/specs/pro-studio-parity-rework-spec-2026-07-22.md`（经两路 Codex 决策保真+代码现实复核与一路确认复核，3 P0+多项 P1 全采纳）；`pro-studio-kernel-integration-spec` 的 K03 结果被本条修订（其余 K 票结论不重开）；copy-manifest 扩容需按票补 A2/A3 逐文件证据，并新增 ports-manifest 与 A2/A3 derivative addendum。
- 主线关联：`docs/specs/beauty-marketing-agent-p0-remediation-spec-2026-07-22.md` 拥有共享执行/账本/存储/ContentPackage 不变量，`docs/specs/beauty-marketing-agent-p1-productization-spec-2026-07-22.md` 拥有 adoption 后成品、Pro Studio 来源 Asset 治理与 export eligibility 投影；二者不得接管画布工程或节点级顶层编排。
- 证据边界：基线由双路 Opus 源码盘点合成（上游 vozeb@a2c52c7 全组件 vs 当前挂载链），行级证据在基线文档内；未做真机并排走查（列为 K7 验收动作而非本条前提）。
- 待验证：上游共享控件（ImageSettingsPanel/VideoSettingsPanel/AudioSettingsPanel/ModelPicker/PromptSelectDialog，位于 `web/src/components/`）是否全部在 A2/A3 授权范围内可拷（K1 内核清点时判定，超范围则等价重建该控件层）；批量账本 A（聚合报价）/B（宿主 fan-out）由 K1 设计拍板。
- Supersedes：修订 `kernel-integration-v1-acceptance-2026-07-19.md` 的 K03 pass 结论（其"import 即挂载"判据作废，已取证行为事实保留为回归基线）；D-044 供给默认从"唯一供给面"收窄为"缺省值"（仅 Pro Studio 范围）。

## D-100 Provider 发布门改为三模态官方主渠道真实连通

- 日期：2026-07-22
- 状态：`accepted`（用户确认“只要保证可以真实连通即可”，并要求按建议修改）
- 决定：当前 release candidate 不再要求文案、图片、视频具备同 CatalogModel 双渠道。发布硬门改为每种媒介至少一条 `official_direct` Deployment 完成一次真实生产 Adapter 调用：文案取得有效输出与 token/费用证据；图片和视频完成 submit、跨实例/重建后 recover、poll 到 completed、download 非空媒体，并记录 provider task、结果 SHA-256、run nonce、时间与实际费用。
- 决定：通过上述门的能力可标 `live_verified`，但无第二条合格 Deployment 时必须同时投影 `single_channel / no_fallback`、`dualChannelReady=false`，不得宣称 multi-channel ready、自动回退或独立故障域。Token 校验、HTTP 200、recorded/fake、fixture 与单测不能替代真实生成。
- 决定：同 CatalogModel 双渠道映射、独立账号/端点故障域、secondary probes、完整 transport fault injector 与故障切换矩阵保留为非阻塞增强；只有未来声称 multi-channel ready 或自动回退时才重新成为硬门。成本帽、secret 保护、受保护环境、当前 commit 绑定和证据有效期继续强制。
- 原因：当前目标是证明三模态官方供应能力真实可调用并能取得可用结果；尚未采购/批准 text/image 同模型第二渠道，不应让不存在的冗余阻塞单渠道发布，也不能用伪映射制造双渠道声明。
- 影响：修订 D-069 与 D-080 C5 的当前发布阈值；#119/#128/#146/#147、Provider live workflow、P0 release gate、admin supply spec 与 handoff 按单渠道真实连通验收。现有双渠道矩阵和 `multi-channel ready` 负向门继续保留。
- 证据边界：本条只完成产品约束与代码门禁修改，不构成真实 Provider 已连通。关闭 G-LIVE 仍需受保护环境中的三次真实生成证据。
- 待验证：官方 Ark 文案、Seedream 图片、Seedance 视频凭据与成本配置就绪后，执行 `primary_connectivity` workflow，并确认三条 probe 均为 `live_verified`、`blockedChecks=[]`、`skippedOperations=[]`。
- Supersedes：仅替换 D-069、D-080 C5 中“三核心双渠道为当前发布硬门”的部分；不替换其供应实体、凭据治理、诚实标签、独立故障域判定和多渠道声明约束。

## D-101 非技术运营采用固定五阶段与受控积木的 Recipe Studio

- 日期：2026-07-24
- 状态：`accepted`（用户明确确认方案 B）
- 决定：内部后台采用“**固定五阶段 Harness + 阶段内受控积木 + 编译发布**”，不建设任意 DAG、任意脚本或第二 Workflow 运行时。外层继续复用当前代码定义、DBOS 执行的 `intent_naming -> context_injection -> brief_compilation -> execution_selection -> assembly_delivery` 五阶段；运营人员只能在开发预先声明的阶段槽、积木类型、条件类型和参数范围内调优或重新组合，不能改变五阶段顺序、绕过系统硬门或直接修改 durable 运行语义。
- 决定：后台作者体验分为三个任务面。①“局部调优”用结构化中文表单编辑 Prompt、instruction/reference 型 Skill、Context selector、ModelRecipeProfile、候选策略、Eval 阈值、输出合同和公开默认值；②“流程调整”用五阶段有序卡片启停可选积木、在同阶段合法范围内排序、绑定已发布依赖，并选择来源不足补问、质量不足有界重试等预置条件；③“测试与发布”完成变更影响预览、脱敏编译结果、golden cases、candidate/production 对比、内部试跑、发布、production label 切换和回滚。普通运营不填写原始 JSON、revision ID、Provider、Credential、polling 或原生字段名。
- 决定：运营可无代码完成的范围包括 Prompt / Skill / Profile 内容与引用、Context 选择规则、同一 API Family 下的模型与参数更新、候选数、质量权重、阈值、有限重试和阶段内组合。新增 Stage/Node/Tool/side-effect 类型、跨阶段拓扑或无界循环、新输出载体或用户状态机、新认证/签名/multipart/streaming/callback/polling/cancel、数据表/权限/密钥/外部写入、新扣费/结算/幂等/补偿语义、脚本型 Skill/sandbox/任意网络，以及事实、权利、安全、额度、发布或 ContentPackage 唯一写入硬门变化，必须进入开发评审和应用发版。后台必须明确显示“本次变化无需开发 / 高影响需平台确认 / 需要开发”，不能允许运营用 JSON 绕过边界。
- 决定：开发维护代码型 `StageTypeRegistry`，至少冻结 stage type、实现 revision、允许阶段、输入/输出/config Schema、Prompt/Skill/Tool 允许范围、条件类型、副作用等级和 edit policy；运营保存声明式、不可变 `WorkflowRecipeRevision`。`RecipeCompiler` 解析全部依赖、校验 Schema/阶段语法/权限/能力兼容/成本与用量包络，生成 `CompiledExecutionPlan` 和 `CompilationReceipt`；DBOS 只执行已映射到代码注册表的已发布计划，不在运行时解释运营提交的任意流程图。
- 决定：`CreationRecipeVersion` 继续作为前台“创作能力包”的唯一顶层发布聚合，引用不可变 Workflow、Skill、Prompt、ModelRecipeProfile、Eval、OutputContract 与 UsagePolicy revision；`CreationSurfaceRevision` 只负责卡片陈列、排序、精选和上下架。依赖 revision 不复制内容，也不新建平行 `CapabilityPack` 实体、客户应用发布生命周期或运行时；Recipe 发布原子钉住全部依赖，已开始任务继续按冻结 revision 完成，新发布只影响新 Composer 会话。出现跨多个能力包独立发布/回滚的高频证据前，不扩大依赖对象的顶层发布面。
- 决定：首版编辑器采用有序阶段卡片、侧栏表单和业务条件选择器；不把 React Flow 作为首版依赖。只有真实能力包出现难以由有序卡片表达的并行或条件分支时，才允许增加受 `StageTypeRegistry` 与服务端 compiler 双重约束的图形编辑视图；图形视图保存的仍是同一个 WorkflowRecipe revision，不能成为执行引擎或第二状态真相。
- 决定：发布链固定为 `draft -> preview -> compile -> schema/policy/eval gates -> internal test -> published revision -> production label`，回滚只把 routing label 切回上一条不可变已发布 revision。发布硬门至少覆盖未知字段/节点/依赖为零、事实/权利/安全/额度违规为零、required Skill 执行、planner-selected Skill 误选率、输出合同完整、相对 production 的质量回归、延迟/供应成本/用户用量包络，以及所有依赖和 `workflowImplementationRevision` / `adapterImplementationRevision` / Schema dialect / validator revision 可解析并进入 ExecutionSnapshot 或 CompilationReceipt。
- 原因：当前代码已经有不可变 `CreationRecipeVersion`、Recipe/Surface 草稿预览发布回滚、五阶段 `runHarnessWorkflow()`、DBOS 恢复与幂等、Langfuse Prompt 冻结和 admin-config/Catalog 审计基础；真正缺口是 Recipe 后台只编辑标题、摘要、Prompt ref 与 auto/fixed model，其他编排字段原样复制，`workflowRevisionRef` 尚未驱动运行时，Skill 领域对象为空，ExecutionSnapshot 也未冻结完整 Workflow/Skill/Profile/Capability/实现 lineage。整体迁移 Dify/Mastra/n8n 会重复运行时和状态真相；只做固定表单又不能满足运营重新组合的需求，方案 B 是在现有可靠内核上补最薄作者层。
- 影响：扩展现有 Creation Experience Catalog 与后台“内容能力”而非新建通用低代码产品；首批需要定义 `StageTypeRegistry`、`WorkflowRecipeRevision`、`RecipeCompiler`、`CompiledExecutionPlan`、`CompilationReceipt`、SkillBinding、动态表单 Schema 和完整执行快照，并把当前 Recipe 编辑器中原样复制的 context/settings/source/output/workflow 字段改成受约束的结构化作者面。首批可用性验收要求非技术运营不看代码或 raw JSON，能完成一次局部调优、一次阶段内重新组合、candidate/production 对比、内部发布与回滚。
- 证据边界：代码事实核对基于 2026-07-24 当前 `packages/contracts/src/creation-experience.ts`、`apps/core/src/p1/creation-experience/`、`apps/core/src/p1/harness/`、`apps/core/src/p1/execution-spine/creation-execution-snapshot.ts` 与 `mkfast-template-main/src/p1/admin-creation-experience-control.tsx` / `admin-model-control.tsx`；完整分析保存在 `/Users/bin/.gstack/projects/legacy-origin-a-legacy-web-repo/office-hours-gap-analysis-checkpoint-20260724.md` §20。本条确认产品与架构边界，不代表 compiler、Skill Catalog、动态表单或运营可用性已经实现。
- 待验证：首批阶段积木的精确目录和每项 config Schema；Recipe dependency revision 的原子发布事务；五阶段现有代码如何消费 CompiledExecutionPlan 而不破坏 DBOS 恢复；ExecutionSnapshot v2 与旧任务兼容；5～10 个真实美业能力包能否覆盖在受控语法内；非技术运营的 15 分钟局部调优、30 分钟复制/重组/内部发布目标；何时出现足以解锁 React Flow 编辑视图的真实分支复杂度。
- Supersedes：细化 D-037 的“代码骨架 + 数据化常变层”，将其 React Flow“只读 viewer”绝对限制修订为“首版不用、真实复杂分支触发后可作受限编辑视图”；细化 D-078 的“可配置不开放底层任意编排”和 D-082 的“不建设任意节点 / 通用 DSL”；继续遵守 D-098 C3 的 `CreationRecipeVersion + CreationSurfaceRevision` 顶层发布收敛，只为 Recipe 内部依赖补不可变 revision 与编译合同，不恢复四类对象全面独立发布。

> D-102～D-109 来源于 2026-07-24 Office Hours 持续检查点 §§10～19；以下只收录用户已明确给出的产品边界、纠偏和已闭合合同，候选库、未确认实现工具与商品数字继续保留在各条“待验证”中。

## D-102 首发产品锁定单店单人，MK Fast 只需补齐公开付费闭环

- 日期：2026-07-24
- 状态：`accepted`（用户明确要求只考虑单店单人，并要求先定义产品、再定义商品）
- 决定：首发账号合同固定为 `一个账号 = 一个个人门店工作区 = 一个实际操作者`。现实中的店主、主理人、店长或员工不映射为不同系统角色；首发不做成员邀请、成员列表、角色切换、团队审批、多工作区切换、席位管理或按席位计费，也不把这些列为公开上线阻塞项。（「多工作区切换」指租户/席位口径；D-121 内容工作区的前台切换器为独立概念，不在此否定之列。）
- 决定：目标用户是有高频宣发引流需求、但缺少 AI 创作能力和工具经验的美业商家。核心任务是用最少输入获得可直接修改、导出或发布交接的宣发物料；核心成功指标是 `time_to_usable_package` 与直接采用/小改采用率，不是模型调用、对象数量或测试数量。
- 决定：MK Fast / 当前 SaaS 基础足以承接单店单人的账户、个人工作区、内部 default-deny 权限、管理员用户控制、支付 Provider 抽象、Entitlement 与 Usage 账本，也足以作为封闭试用底座；不重写账户、权限和支付，不为首发补企业组织能力。
- 决定：当前默认配置不足以直接宣称“公开付费上线”。公开试用/收费前必须真实跑通：①注册邮件、邮箱验证与找回密码；②唯一支付 Provider 的 checkout、签名 webhook、续费、取消与 customer portal；③账号删除后门店内容、素材、对象存储和依法保留记录的删除/匿名化；④营销页、订单、支付与权益共用的单一商品目录；⑤商家前台去除角色、Workspace、API Key、Provider、技术账本等工程语言；⑥取消、退款/争议、人工补单/补权益与客服处理的最小闭环。
- 决定：商家 2FA、企业审计、复杂风控、发票、国内支付渠道、自动退款和复杂财务对账可按真实风险与目标市场后置；平台管理员与供应商凭据仍使用现有 recent-auth 和最小权限边界。
- 决定：产品先定义用户、任务、最少输入、上下文/Recipe、交付物、修改/采用/导出/复用；商品随后再定义套餐、次数、价格、加购、优惠、有效期和退款。技术账本不得反向决定用户买到什么。
- 原因：单店单人已经覆盖当前最窄付费楔子；继续建设多人组织会增加权限、导航和商品复杂度，却不提高一次宣发任务的可用结果。
- 影响：首发前台只呈现当前账号本人；原“多人组织通过责任帽子扩展”等正文口径由本条修订。内部 workspace 与权限隔离可以保留，但不得投影成商家要理解的组织产品。
- 证据边界：账户、工作区、权限、支付与权益的可复用判断基于 2026-07-24 当前代码和定向测试；邮件、真实支付、删除与公开商品闭环尚未取得生产环境端到端证据。
- 待验证：生产邮件与支付配置、删除/匿名化清单、唯一商品目录映射、人工取消/退款/补权益 SOP，以及首次注册到第一次可用结果的全链体验。
- Supersedes：第一部分 Target User 与 HITL 中的首发多人/责任帽子扩展口径；不取消内部 workspace 数据隔离、平台管理员权限或一次性外部发布责任人交接。

## D-103 创作中心采用“定制创作 + 自由创作”两条主线

- 日期：2026-07-24
- 状态：`accepted`（用户明确给出两条主线及其技术边界）
- 决定：**定制创作**是默认主线和核心差异化。系统自动组合门店、项目/商品、品牌、个人 IP、素材、历史偏好与当前任务，再叠加美业场景和目标平台 Recipe，经 Harness、Context Resolver、Prompt/Brief Compiler 和受控 Workflow 交付完整 MarketingPackage。目标是稳定、低学习成本的标准成品，内部可理解为约 6～8 分质量带；真实评测前不把该数字作为对外承诺。
- 决定：**自由创作**是同一创作中心内的清晰次入口。用户显式选择当前可用的公开 CatalogModel，提供 Prompt、参考素材和必要原生参数，系统只做 Capability 校验、Request Mapping、报价/用量、调用、等待、落库和展示；默认不读取隐含门店、IP、平台 Recipe 或历史偏好，也不新建第二套 Agent/Harness。它既服务熟悉模型的用户，也服务只要一个简单文案、图片或视频的最小任务。
- 决定：两条主线共享 Auth、个人 Workspace、Entitlement/Usage、CatalogModel、Provider routing、AI SDK/媒体 Adapter、DBOS 异步与恢复、资产存储、结果历史、血缘、成本、观测、安全、权利和交付；不共享同等深度的创作编排，也不实现成两套 API、任务、账本、素材库或结果中心。
- 决定：内部继续使用一个 canonical ContentPackage 结果信封。定制创作交付多部件 MarketingPackage，自由创作允许只含一个 deliverable 的最小作品包；实现必须有等价的结果类型判别，但本条不锁死 `packageKind` 等具体字段名。
- 决定：自由创作结果可带血缘地“作为素材/参考进入定制创作”；定制创作中的单个文案、图片或视频可“进入自由创作继续精修”。转换必须冻结来源对象、revision、实际使用的显式上下文和新任务血缘；从定制转入自由时要明确显示携带了哪些资料。
- 决定：定制创作默认由系统择模；自由创作展示动态公开模型名、能力、关键参数和产品用量，但不展示 Provider、Channel、Credential、网关、内部价格或 fallback。自由创作不是模型供应商商城，也不成为第二个首页。
- 决定：现有 Pro Studio 如果继续保留，只能视为自由创作下的高级画布/交互面，不构成第三条产品主线；它可保留自己的画布草稿与 checkpoint，但生成任务、资产、用量、供应成本和显式采用后的 ContentPackage 仍服从共享 canonical 合同。与 D-099 的精确页面及运行边界需在实施规格中对账。
- 原因：小白用户需要系统替他编排，熟练用户和简单任务需要模型原生能力；把两者压成一个流程会让前者过于工程化、后者过度编排。
- 影响：创作入口需增加明确 mode policy、两套 Context/Compiler/Output policy 和一套共享结果封装；D-043 的“主路径隐藏模型”只约束默认定制创作，自由创作按本条展示公开模型与必要参数。
- 证据边界：当前代码已有统一 Composer、copy/image/video lens、Recipe、CatalogModel、Quote 与提交基础，但尚无正式 `customized/free` mode、自由创作显式上下文隔离合同或双向转换闭环。
- 待验证：两条入口的命名与层级、自由创作的最小基础字段、切换模型时参数保留/失效提示、结果类型判别、双向转换与 Pro Studio 收编方式。
- Supersedes：任何把创作产品限定为单一深编排路径、把自由创作等同于 Pro Studio 或把 Pro Studio 视为第三条产品主线的旧解释；不改变 D-030“不做通用 AI 工具市场”的边界。

## D-104 定制创作采用稳定主干与四类输出编译器，Kickart 只直接参考视频线

- 日期：2026-07-24
- 状态：`accepted`（用户明确纠偏 Kickart/Seedance 的适用范围，并要求复刻控制框架而替换模型）
- 决定：Kickart 是**定制视频创作**的成熟参考形态，不是文案、图片和图文笔记的统一实现。借用的是来源进入、多模态理解、创意方向、故事板、参考意图编排、模型原生生成、质量/事实/权利检查、局部修订、交付和回执等管控节点；不把 Kickart、自研 VLM、固定模型或其 SaaS/API 变成业务依赖。
- 决定：定制创作统一主线止于稳定的 canonical `ContentIntent`：`用户意图 -> 门店/项目/品牌/IP/素材上下文 -> 行业与平台 Recipe -> 事实/权利/CTA 约束 -> ContentIntent`。从此处分为四类输出编译器：
  - `copy`：模型专属文案 Prompt/Profile -> `TextArtifact`；
  - `image`：`ImageIntent` + 图片模型专属 Prompt/参考/参数 -> `ImageArtifact`；
  - `image_text_note`：`NotePlan` 协调文案与图片分支 -> 图文笔记 ContentPackage；
  - `video`：`Storyboard + ReferenceIntentPlan + VideoModelRecipeProfile` -> 模型原生 `VideoArtifact`。
- 决定：四类输出共享 Task、ContextBundle、行业/平台 Recipe、事实与权利、模型目录、用量、异步恢复、结果历史、血缘和 ContentPackage；不共享同一套最终 Prompt、参考素材策略、原生参数或质量评测。
- 决定：视频参考素材必须保留原始图片、视频、音频及其语义角色。前置理解/控制 LLM 负责识别“每份素材控制人物、项目、场景、动作、运镜、结构、风格、节奏或声音中的什么”，再编译成模型原生请求；不能只把多模态素材总结成文字后丢弃原始输入。
- 决定：每个进入生产的模型/variant 维护独立且版本化的 `Capability Contract + Official Playbook + ModelRecipeProfile + Golden Cases/Eval`。稳定 Workflow 只依赖领域 artifact 和 capability requirement；模型专属 Prompt、reference mapping 与参数留在 Profile/Request Mapper。
- 决定：规划文件不锁死某个模型产品名或子型号。只有满足同一 capability、Profile 和 golden eval 的 deployment 才可互换；每次执行必须冻结实际 Provider、Channel、model ID/revision、Profile、Capability、Price 与输出血缘。
- 决定：用户控制节点保持极简：来源、模型与评分等自动留痕；关键事实/权利/能力/质量失败时条件阻塞；高成本批量媒体前可展示内容计划与产品额度；成品后允许局部修订；公开发布和额外扣减继续绑定精确 revision 确认。
- 原因：模型原生多模态能力已经承担生成本身，产品差异化在于美业意图、上下文、参考角色和平台结构如何被正确编译，而不是自研一个通用媒体流水线。
- 影响：五阶段 Harness 继续作为稳定外壳，但必须产出可检查的领域 artifact；模型更新主要修改 Profile/Capability/Eval，不重画 Workflow。文案、图片和图文笔记按各自官方最佳实践与美业样例独立调优。
- 证据边界：Kickart 官方产品页证明了电商视频的产品化控制节点；它不证明美业 Recipe、任意模型替换或四类输出已经在当前代码中实现。当前代码已有五阶段和媒体 Port，但 `CreativeDirectionSet`、完整 `ReferenceIntentPlan`、四类模型专属 Profile 与真实择优评测仍不完整。
- 待验证：首发四类输出的精确 canonical intent Schema、首批能力包的 artifact 边界、各模型官方资料版本、美业 golden cases、控制 LLM 与生成模型的组合质量及最小局部重跑范围。
- Supersedes：任何把 Kickart 视频链泛化为全部媒介、把一个 generic Prompt 用于所有模型、或把具体供应商模型写成长期产品真相的旧口径。

## D-105 FFmpeg 创意拼装退出视频主链，复杂后期交给专业外部工具

- 日期：2026-07-24
- 状态：`accepted`（用户明确纠偏）
- 决定：视频定制创作的交付终点是供应商模型原生生成、及时持久化并通过质量门的 `VideoArtifact`，不是本地 `ComposedVideo`。参考图片、参考视频、参考音频、首尾帧和用户实拍素材应直接交给满足能力合同的多模态视频模型。
- 决定：新视频任务不再经过 `model.composed-video`、固定“首帧 -> 多片段 -> concat”或本地字幕/BGM/品牌水印/AIGC 标识烧录；产品不建设时间线、片段拼接、混音、烧录或弱化版剪辑器，也不把 `compositionEvidence` 当作视频完成的核心真相。
- 决定：需要复杂剪辑时，产品交付模型成片、原始参考素材、脚本与字幕文本，用户在剪映等专业工具处理后可重新上传为有来源和权利声明的新 revision 资产；除非以后有明确商品需求与官方集成，不保存或模拟第三方剪辑工程。
- 决定：`ffprobe`/FFmpeg 仍可作为不可见技术工具承接播放可用性检查、临时 URL 下载后的媒体探测、确有平台证据的容器/编码归一化和封面抽取；它们属于 `AssetValidation` / `DeliveryNormalization`，不得出现在用户 Workflow、故事板、能力包卖点或创作计费中。
- 决定：模型原生编辑、延展、局部重生、音频和参考能力优先；模型不支持的复杂后期不得用本地拼接伪装为模型已完成。局部视频片段可以作为独立 Asset，但只有完整模型原生 `VideoArtifact` 才能进入 ContentPackage 的成片槽。
- 原因：在具备多模态参考与完整视频生成的模型已可用后，本地简单拼接只会重复模型能力并制造大量非差异化代码；真正专业的二次剪辑也无法由弱 FFmpeg 流水线替代。
- 影响：`composition-runtime.ts`、`ffmpeg-composition-port.ts`、`composed-video-workflow.ts`、相关 integration tests 与 `VideoCompositionEvidence` 进入后续退役/迁移清单；本条只修改产品与目标架构，不在设计同步阶段直接删除代码。视频工作区中的“重新合成整段”统一改为“重新生成完整视频”或模型原生编辑。
- 证据边界：现有 FFmpeg 管线和测试是当前代码事实，不再构成产品需求证据；是否仍需编码归一化必须由真实目标平台兼容失败证明。
- 待验证：目标视频模型的参考角色、编辑/延展、音频和完整成片能力；外部剪辑交付包内容；各目标平台是否需要技术归一化；旧 composed-video 任务和历史证据的只读兼容/迁移策略。
- Supersedes：D-027 中 FFmpeg composer/composed-video 为目标首发管线的部分；D-028 的“合成管线”实现表述；D-032/D-040 的能力层 FFmpeg 创意合成；ADR-0008 的首帧→片段→FFmpeg concat/BGM/字幕/水印路径与 ADR-0006 的 compose worker 产品地位。成片是首发交付、耐久任务、来源/权利/AIGC 如实记录和能力门验收仍有效。

## D-106 图片采用三种底层操作，套图与图文笔记由计划层组织

- 日期：2026-07-24
- 状态：`accepted`（用户确认图片领域收敛方案）
- 决定：首发图片领域合同固定为三种底层操作：① `image.generate` 从意图生成图片；② `image.edit` 只改变指定内容并保护身份、事实、版式或案例证据；③ `image.reference_transform` 为多份参考分配角色后组合、换场景或风格迁移。
- 决定：成套组图是 `OutputPlan`，不是第四种底层操作。满足原生组图能力的模型可以一次生成；不满足时由页序计划、逐页生成、上一页/关键参考回灌和一致性评估完成，不能把普通批量生图冒充叙事连贯组图。
- 决定：共享 canonical `ImageIntent` 至少表达 operation、用途、主体、场景、构图、精确文字、各参考素材语义 slot、changes、invariants、事实/权利约束和 `OutputPlan`。不同语义 slot 独立声明数量、MIME、大小、组合限制，再由模型 Profile 映射到供应商字段。
- 决定：图文笔记是组合产品：先生成 `NotePlan`（核心主张、页数/页序、每页角色与事实证据、文案层级、统一视觉规则、CTA），再由 Copy Compiler 与 Image Compiler 围绕同一计划生成，经过逐页与整体一致性评估后组装为 ImageTextNote ContentPackage；不得后置随机配图。
- 决定：定制创作由系统根据经营意图自动构造 `ImageIntent/NotePlan`；自由创作由用户直接选择三种操作之一并提供显式 Prompt、参考素材和模型参数。两条主线共享图片 Adapter、资产、任务、用量和历史，不共享 Harness 深度。
- 决定：共享的是 `ImageIntent`，不是一个跨模型通用 Prompt。每个 deployment 使用独立 `ImageModelRecipeProfile` 编译 Prompt、参考角色顺序和原生参数；OpenAI 图片模型与火山 Seedream 的当前官方示例只是首批 Profile 样本，不是长期商品锁名。
- 决定：首轮美业评测至少覆盖人物/技师身份、品牌与门店风格、中文标题/价格/权益/CTA、参考角色不混淆、局部编辑保护真实案例、五页笔记页序/人物/配色/主张一致，以及可用率、返工、成本、时延和实际模型/Profile/Price 血缘。
- 原因：图片生成、局部编辑、多参考转换的输入约束与 Prompt 方法不同；组图和图文笔记又是输出计划问题，把它们压成一个 generic prompt 或一个“批量”开关无法稳定交付。
- 影响：图片 Brief、CapabilityManifest、Profile、自由创作表单、图片 Result Workspace 与图文笔记 assembler 均按该领域合同收敛；模型公开名动态生成，实际 deployment revision 在执行快照中冻结。
- 证据边界：官方文档只证明模型能力与建议用法，不证明本产品美业质量；当前图片 Brief、Ark Adapter 和 Catalog 尚未完整表达三种操作、语义 slot、组图、输出数量与 Profile revision。
- 待验证：`ImageIntent` 精确 Schema、首批 OutputPlan 类型、模型原生组图与逐页策略的选择门、中文文字与一致性阈值、两条主线的高级参数集合。
- Supersedes：任何把成套组图定义为第四种图片操作、把批量生成等同连贯组图，或让不同图片模型共用同一最终 Prompt 的旧实现假设。

## D-107 后台以模型装配与发布为主线，不建设通用工作流平台

- 日期：2026-07-24
- 状态：`accepted_product_boundary`（用户明确把快速适配频繁模型/供应商更新设为 Harness 与 Workflow 管理核心；具体 UI 库仍待 spike）
- 决定：后台要解决的核心不是“每换模型重画 Workflow”，而是把稳定业务 Workflow 与高频变化的供应商输入、Prompt/参考最佳实践、价格、生命周期和评测分开。目标编译链为：`稳定 Workflow + 版本化 CapabilityManifest + 版本化 ModelRecipeProfile + Golden Eval -> 供应商原生请求`。
- 决定：内部后台分为三个任务面：①模型接入/更新，管理 API Family、Channel、Credential、Deployment、Capability、原生参数/素材 slot、Price 和 Profile；② Skills/Recipe Studio，管理受控 Workflow、Context、Prompt、Skill、Profile、Eval、Output/Usage 合同；③发布与运行，管理当前 revision、shadow/canary/production、健康/成本/质量、Compilation/Invocation/Provider Receipt 和回滚。首发可由同一内部管理员操作，不因此新增复杂 RBAC。
- 决定：`ProviderDriver` 按 API Family 承担认证/签名、HTTP/SDK/multipart/streaming、sync/polling/callback/cancel、错误归一化和临时输出持久化；同一 API Family 下的新模型、字段、枚举、默认值、Prompt 最佳实践、参考顺序、价格和路由通常只修改 Manifest/Profile/Eval/Price，不部署应用代码。
- 决定：新增认证/签名/传输或生命周期、新产品级 operation、新用户交互/输出载体、新权限/外部副作用、新账本/幂等/补偿语义必须由开发修改 Driver、领域或 Workflow 代码。后台必须明确显示“无需开发 / 高影响确认 / 需要开发”，不能用 raw JSON 绕过。
- 决定：三层事实分离并通过不可变 revision 引用连接：
  - `Model Supply Catalog`：ProviderDriver、Deployment、CapabilityManifest、Price/Route；
  - `Model Assembly`：semantic slot mapping、ModelRecipeProfile、Prompt/reference compilation、golden eval binding；
  - `Creation Experience Catalog`：WorkflowRecipe、CreationRecipeVersion、CreationSurfaceRevision。
- 决定：模型与 Recipe 的作者界面使用强类型 Schema、中文结构化表单、脱敏 native request 预览和 candidate/production 对比，替换原始 Catalog JSON 与手填 revision ID。具体 JSON Schema validator 和 form renderer 在配对 spike 后确定，不在本条锁死 RJSF、JSON Forms 或其他库。
- 决定：发布时统一生成 `CompilationReceipt/ExecutionSnapshot`，至少冻结 Context、Workflow、Prompt blocks/hash、Skill、CapabilityManifest、ModelRecipeProfile、Schema/validator、实际 Provider/Channel/model、Price、`workflowImplementationRevision` 与 `adapterImplementationRevision`，保证重试、恢复、成本、质量与回滚可追溯。
- 决定：不把 Dify、Mastra、n8n、LangGraph 或 Temporal 作为新的生产主运行时；只有现有受控 Studio 的作者体验或表达能力被真实需求证明不足时，才按 D-037/D-101 的触发门另行评估。
- 原因：当前系统的执行与供应控制面已经很厚，真正缺少的是运营可用的模型装配、语义映射、评测和发布闭环；再引入通用低代码运行时会制造第二状态真相。
- 影响：扩展现有 Model Supply、admin-config 与 Creation Experience Catalog，收敛重复 supply/runtime truth；模型更新的首要验收从“加了一个目录项”改为“Manifest/Profile/Golden Cases 可发布并能由真实任务反查”。
- 证据边界：2026-07-24 当前代码已有 Catalog、Deployment、凭证、探针、路由、五阶段 Harness、DBOS、Langfuse 与 Recipe revision 基础；动态参数 Schema、语义 slot mapping、统一 Compilation Receipt 和端到端装配向导仍未实现。
- 待验证：首个真实 API Family 的 Manifest/Profile schema、表单渲染库（D-130 结项＝HeroUI 组件族组装，不引 JSON-Schema 表单框架）、字段兼容/迁移规则、同 Family 无代码更新时间、新 Driver 的最小代码面、完整 revision 冻结与回滚。
- Supersedes：任何把模型更新等同 Workflow 重画、把原始 JSON 控制台视为运营装配台、或默认引入第二生产 Workflow 运行时的旧方案；细化 D-037、D-076、D-078、D-082 与 D-101。

## D-108 Skills 是一等领域能力，并以前台“创作能力包”与 Workflow 共生

- 日期：2026-07-24
- 状态：`accepted`（用户明确纠偏 Skills 为 AI-native Agent 必备能力，并确认 Skill + Workflow 前台显性化）
- 决定：职责固定为：Context 提供本次真实事实/素材/身份/权利；Workflow 决定阶段、状态、恢复、门禁和交付；Skill 提供某类专业任务怎样理解、规划、转换或评估；Tool 执行具体 API/脚本/外部动作；Model 完成理解或生成；Harness 在当前 stage 的 allowlist、预算、权限和副作用边界内连接五者。Skill 不拥有 Task 状态、账本、发布权限或 ContentPackage 写入。
- 决定：兼容开放 Agent Skills 文件包：`SKILL.md` 加可选 `scripts/`、`references/`、`assets/`；平台另附版本化扩展合同（暂名 `meiye.skill.json`），强制声明 revision/contentHash、输入输出 Schema、Context scope、allowed Tools、side-effect class、模型能力、execution mode、调用/时间/成本预算、eval、兼容和 fallback。开放格式负责可发现知识，平台合同负责生产安全。
- 决定：canonical `SkillRevision` 是产品真相；不同 Provider/Channel 的原生上传 ID 和版本只是 `SkillDeployment` 映射。支持 `provider_native`、`harness_native`、`prompt_materialized` 三种执行方式，但首发只上少量平台审核的 instruction/reference 型 Skills；provider-native deployment、受审 scripts、sandbox 和复杂 Tool-orchestrating Skills 按真实证据逐步开放。
- 决定：`WorkflowRecipe` 只保存不可变 `SkillBinding`，至少支持 `required / planner_selected / user_selected / disabled`。LLM 只能从当前 stage allowlist 中选择，默认不得递归发现任意 Skill；Skill 只能读取声明 Context、使用允许 Tool、在预算内调用，输出先过 Schema/质量门，再由 Workflow 组装，不能直接扣费、发布、改账号或写 ContentPackage。
- 决定：控制 LLM 负责 Context 理解、Skill 发现/选择、受控 Tool 调用和 `NotePlan/ImageIntent/VideoIntent` 等结构化规划；图片/视频生成模型只接收 `ModelRecipeProfile` 编译后的原生 Prompt、参考素材和参数，不假设媒体生成模型会运行 Agent Skills。运行时分别冻结 controller route、SkillInvocationReceipts 和 generation route。
- 决定：Skill 在后台治理、在前台产品化表达。每个 Skill 有 `presentationPolicy = backend_only | explainable | user_selectable`：定制创作自动绑定 required/planner-selected，只显示 1～3 个白话“本次优化”chips；自由创作默认 `selectedSkillRefs=[]`，用户可从少量审核后的“创作增强/能力包”主动添加，并明确标记结果已增强。前台不接收 SKILL.md、scripts、provider skill ID、隐藏 Prompt，也不开放互联网 Skill 市场。
- 决定：适用于特定场景的 `Workflow + Skills + 输入/输出 + Eval + Usage` 在前台统一称为**创作能力包**。内部不新增平行 `CapabilityPack` 运行时实体：`CreationRecipeVersion` 是能力包的不可变顶层发布合同，`CreationSurfaceRevision` 只负责快捷入口/模板的陈列、排序、精选和上下架；浏览、预览和套用只修改 Composer draft，用户明确开始创作后才冻结执行。
- 决定：事实来源、肖像/声音/案例权利、受监管红线、价格权益、输出完整性、产品额度、外部发布批准、Provider receipt、资产血缘、幂等和 Tool allowlist 不能只写在 Skill 中，必须由 Context truth、Harness policy、Workflow gate 和确定性 validator 强制。
- 决定：每次 Skill 内部模型或 Tool 调用都是独立 child effect，携带稳定幂等键、budget reservation、provider receipt、usage/cost、settlement 和 retry/acceptance 状态；`SkillInvocationReceipt` 只做聚合展示，不能到 Skill 结束才补一笔总账。
- 原因：只有 Workflow 会把专业打法继续硬编码进节点/Prompt；只有 Skills 又缺少耐久状态、质量、计费和交付。两者共生才能既稳定交付，又快速迭代行业、平台和模型用法。
- 影响：SkillCatalog、SkillRevision、SkillBinding、SkillDeployment、SkillInvocationReceipt 从第一版领域合同起为一等对象，并接入 D-101 Recipe Studio 的 author/bind/eval/publish 流；不因此建设第四运行时、开放市场或任意代码平台。
- 证据边界：当前 AI SDK 依赖已具备 Skills provider 接口，代码也有 Tool allowlist/Schema/有限 Agent loop 参考；商家创作运行时尚无正式 SkillCatalog、Binding、Deployment 或 InvocationReceipt。开发工具目录中的 `skills/*/SKILL.md` 不是产品 Skill Catalog。
- 待验证：首发 Skill 清单、扩展 manifest 精确 Schema、三种执行方式的质量/成本差、controller model × skill 触发/抑制 eval、前台 chips/能力包命名与非技术运营作者体验。
- Supersedes：任何把 Skill 等同 Prompt 别名、可选插件、Provider 原生 ID、开放市场或独立运行时的旧解释；细化 D-101 中 Skill 依赖和前台创作能力包合同。

## D-109 商品采用套餐、图片/视频额度与加油包，供应成本只在后台核算

- 日期：2026-07-24
- 状态：`accepted`（用户明确给出计费、促销与可见信息边界）
- > 2026-07-24 修订（D-123）：本条「首版只出售图片/视频两桶、文案不单独商品化」口径已由 D-123 行使「除非证明需要单独商品化」预留条款，升级为**文案/图片/视频三桶＋文案包/图片包/视频包三类加油包**；本条其余合同（一任务一预占/失败全退/四账本/供应细节不可见/促销 GrantLot）完整有效。建 SKU/额度桶以 D-123 为准。
- 决定：普通商家购买的是平台套餐与稳定产品权益，不是 Token、供应商算力或某个固定上游。首版用户侧只保留：套餐等级、本周期图片额度/视频额度、已购图片/视频加油包、活动赠送额度与到期日，以及本次使用多少、完成后预计剩多少。
- 决定：模型公开名称与直接影响创作用途/产品用量的规格必须可见，包括当前支持的操作、尺寸/分辨率、比例、目标成片时长、输出数量、必要参考素材能力和质量档；用户看到的是平台发布的 CatalogModel 与产品能力说明，不是实际 Provider、Channel、Deployment、Credential、网关或 fallback 顺序。
- 决定：前台不展示 Token、供应商单价、平台毛利、上游按请求/秒数/Token 的公式、最低消费/取整、内部模型/评估/重试次数或技术式成本 Quote。目标“10 秒视频”是用户创作规格，应显示；“供应商按 5 秒一档收费”是后台成本事实，不显示。可靠完成时间可作非计费状态提示，但不是用户必须理解的报价项。
- 决定：首版只出售两种可认知用量：图片额度与视频额度。文案、标题、脚本、平台适配、能力包规划和包内封面文案默认作为套餐/主交付的附属能力；后台可继续记录 copy/audio 资源用于成本、限流和滥用保护，除非真实数据证明需要单独商品化。（2026-07-24：D-123 已行使本预留条款，文案升为第三额度桶。）
- 决定：每个 `CreationRecipeVersion` / 创作能力包引用不可变 `UsagePolicyRevision`，在提交前把当前公开模型与规格映射为稳定的图片/视频次数。高规格输出可以明确使用多次额度，但不能临时透传供应商价格；任何会改变用量档的模型或参数变化必须实时更新 `UserDebitPreview`。
- 决定：一个用户主动任务只产生一笔产品权益预占。内部 Planner、LLM、图片/视频调用、评估、质量淘汰、fallback、系统重试和同 supplier task 恢复分别进入 ProviderCost Ledger，不重复扣用户；最终未受理/失败全额退回，部分交付退回未交付部分，用户主动“再生成一次/换一版”才创建新任务并再次使用额度。采用、下载、排序、复制和无需模型的确定性编辑不扣生成额度。
- 决定：运营促销、召回、首购奖励和人工补偿使用独立 `PromotionalGrant/GrantLot`，记录活动 revision、资源、数量、有效期、来源、幂等键、预算上限与原因；不通过修改已发布套餐或历史订单来临时增量。首版只补最小赠额命令与活动批次视图，不建设 CRM/营销自动化平台。
- 决定：周期套餐额度随订阅周期刷新且不默认累计；付费加油包默认长期有效或采用清晰的较长有效期；活动赠送必须显示到期日；消耗按最早到期优先。具体有效期、套餐名、价格、次数、加油包数量、高规格倍数与毛利缓冲均留到真实成本和商品定义阶段，不在本条拍数字。
- 决定：后台保持四条不可混写的真相：`ProductCatalogRevision`（商品与公开售价）、Payment/Order Ledger（实收/退款/周期）、Entitlement/Usage（授予/预占/结算/退回/到期）、ProviderCost Ledger（供应商、模型、Token/秒数、重试与真实成本）。上游价格变化不改写已购权益，运营赠送不改写套餐或支付记录。
- 决定：D-044/D-045 的 trial、条数制、grant-lot、失败退额、兑换码和支付接缝继续有效；正式公开商品必须先收敛营销页、支付与权益的单一目录，并通过真实 checkout/webhook。自动加购可后置，首版优先手动购买图片/视频加油包。
- 原因：目标用户需要可预测的“我还能做多少张图/多少条视频”，不需要承担快速变化的供应商定价与失败模式；套餐与赠额又能为促销运营保留空间。
- 影响：面向用户的 `CreativeQuote` 收敛为 `UserDebitPreview`；D-043 的视频大额技术报价、D-087 的预计费用/时长、D-088 的按秒/最低消费/取整公开说明不再适用。后台 QuotePolicy、RouteSnapshot 与 ProviderCostSnapshot 继续完整保留用于成本和毛利。
- 证据边界：现有代码已有资源分桶、套餐/加购、reserve/commit/refund/expire/compensate、GrantLot、兑换码、支付生命周期和供应成本基础；公开商品目录、真实加油包支付、运营赠额命令与去工程化用量页尚未形成完整闭环。外部平台只验证“套餐 + 充值/加油包 + 约等于产量”是可行市场语言，不复制其当前价格数字。
- 待验证：正式套餐/加油包命名、价格、图片/视频次数、高规格倍数、有效期、目标毛利与安全余量；真实供应组合的质量通过率、失败/重试放大与用户任务结构；一次完整购买、预占、交付/退款与客服补偿闭环。
- Supersedes：D-043/D-087/D-088 面向普通用户展示技术式金额、按秒公式、最低消费、取整或供应侧预计成本/时长的部分；不改变用户主动新生成会再次使用额度、系统重试不重复扣、产品用量与供应成本分账的有效原则。

## D-110 收敛执行合同：装配门先行、单一门槛序列、修复即建设、LPGS 并入收档

- 日期：2026-07-24
- 状态：`accepted`（用户三项拍板：LPGS 并入收档、装配门第 0 步、修复即建设；并确认裁决元原则）
- 决定：**决策优先级元原则**：D-101～D-109 是更细化场景感受后的决定，与更早决策、第一部分正文旧口径或任何评审/研究建议冲突时，以 D-101～D-109 为准；本条及后续一切执行规格按此原则裁决。`comprehensive-development-review-2026-07-24`（评审基线 D-100）的 M/R/E/S/P findings 编号体系被采纳为验收追踪单位，但其修复方案凡与 D-101～D-109 目标形态冲突，必须先按新合同校准再实施。
- 决定：**装配门为第 0 步**（先于 M 合入门）：①默认 dev profile 补齐 `HARNESS_DBOS_SYSTEM_DATABASE_URL` 装配与模型激活证据的开发态方案，关闭「默认栈 Composer 提交 404」（`main.ts:287/1460/1657` 门控链 → `server.ts:2368` 404）与「boot 抛 `HARNESS_LIVE_MODEL_REQUIRED_ERROR`」（`structured-model-runtime.ts:22`）两道串联环境门；②注册钩子接线 workspace provisioning（trial + 平台默认供给），关闭 D-044 断崖①（`consumeWorkspaceProvisioning` 当前除自身文件外全仓零调用点，`onCreateUser` 只做 newsletter 订阅）；③「全新商家注册→默认栈→首次出活」e2e 升为 required gate，装配/激活类问题从走查反复发现改为制度性拦截（07-18 已诊断 harness 未激活、07-19 已拍 D-044，六天后 `.env` 仍零条 HARNESS 行——根因即缺此门）。验收=干净环境按文档启动四服务、注册新账号、提交一条文案任务、结果中心可见成品。
- 决定：**统一执行序列**：装配门（第 0 步）→ M-01～M-06 合入门 → R-01～R-08 试点/发布门 → E-01 发布工程门；S/P 治理与 D-101～D-109 新建设（Recipe Studio 编译链、Skills 五件套、计费三件套、四类输出编译器）挂对应门后排入；当前未提交的 Composer 提交链收敛代码（20 文件，+656/-336，路由冻结重构+提交门简化+前端提交链闭环）继续完成并按 M 门验收合入。四条并行线（决策/评审/开发/LPGS）自本条起收口为本序列单一权威。
- 决定：**修复即建设**：所有 R 级修复直接按 D-101～D-109 目标形态实施，不先修旧形态再迁移。首个应用：R-01 媒体完整包直接按 D-104/D-106（四类输出编译器、NotePlan、图文合一）口径实施，不补旧完整包字段（当前 copy lens 产 0 图、image lens 产空正文单图，「图文」无合一交付物是结构缺口，设计答案即 NotePlan）；每项 R 修复动工前，实施规格先对照 D-101～D-109 校准一次。
- 决定：**LPGS 处置**：`.wayfinder/map-lightweight-personalized-generation-spine.md` 与十六票并入收档，作为收缩研究输入保留，不再作为独立决策权威逐票推进；lpgs-08 的退役清单与首发收缩边界吸收进本条退役面。四处口径分歧按元原则裁决（经核对，D-101～D-109 无一支持 LPGS 相抵表述）：①Langfuse 保持 D-036 角色，明文补运行时约束「Langfuse 不可用不得阻断生成主链」（审计表为权威、`HARNESS_BUILTIN_PROMPTS` fallback 已存在，与待验证 18 原口径一致）；②DBOS 维持编排层 durable 载体——D-041 定案 + D-101 明文「DBOS 执行五阶段/已编译计划」双重确认，LPGS「DBOS 仅候选承载异步视频」表述废弃，收缩对象是「DBOS 之上不叠第二运行时」；③Pro Studio 吸收「移出首发投入焦点」，代码与 entitlement 付费墙保留不删，收编方式按 D-103 挂自由创作主线验证后；④Skills 一等地位不变（D-108 自身即最新决策），五件套建设排入 D-101 Recipe Studio 同批次，不提前于装配门/合入门。
- 决定：**退役清单启动**（标准：删除后主链无感；本阶段只登记排期，不在设计同步阶段删代码，与 D-105 口径一致）：立即批=graphile-worker 对照适配器（`p1/job-runtime/` 内 graphile 四文件+依赖，main/job-worker 零引用，README 自证 comparison adapter）、`provider-conformance/` 32 文件 + `z2-accept/` 4 文件孤岛（无 main wiring、非 test 消费者为零）、CreativeBrief 重复声明（`operations/types.ts:575-603` 逐字拷贝 `contracts/uiux.ts:86-113`，改 re-export）；条件批=legacy 只读投影尾巴（`legacy-content-package-delivery-projection.ts`/`legacy-inflight-decision.ts`/`legacy-mapper.ts`，读侧排空后）、supply-registry 迁移脚手架（`dual-read.ts`/`env-fallback-monitor.ts`/`expand.ts`，wired 稳定后）、FFmpeg 视频链（`composition-runtime.ts`/`ffmpeg-composition-port.ts`/`composed-video-workflow.ts` 及关联测试与 `VideoCompositionEvidence`，按 D-105，待模型原生 `VideoArtifact` 链验收后）。
- 原因：四路只读探查与同日全面 review 交叉证实：「几十万行完成度低」的根因不是架构烂——上轮「同一语义多套事实/模块各自为战」诊断经逐组核实大部分不成立（Brief 四类为分阶段流水线非重复、RouteSnapshot 四形态有活跃 canonical 归一层、写侧 cutover 已完成且有静态测试锁、DBOS+pg-boss 分层非竞争、供给两目录为职责分区），主链为真实完整实现（五阶段真实分化、context 真实数据源、OCC 幂等落库，非 mock）——而是①装配失守：两道环境门+Day-0 供给钩子零调用，同类问题反复诊断从未制度性关闭；②接缝合同断裂：2/8 admission、Context 事实与 Recipe 来源槽断裂、媒体单 Asset 空正文、图文无合一 lens、Pro Studio 绕唯一写口、media fixed 与 copy auto 准入口径不一；③验收单位错位：对象/合同/单测绿灯≠旅程绿灯，严格 Day-0 spec 存在但不在 required PR journey；④多线并行无单一执行权威。
- 影响：后续开发排期一律以本条序列为准；review 的 M/R/E/S/P 编号为验收追踪单位（方案按元原则校准）；「注册→首次出活」旅程成为第一 required gate；LPGS 地图与票据收档；R-01 实施规格按 D-104/D-106 重写；checkpoint §9 路线 C（收缩主干并退休重复层）由本条转正为正式决策；D-102～D-109 各条待验证不受影响继续有效。
- 证据边界：诊断基于 2026-07-24 四路只读探查（主链逐跳追踪、八组重复事实核实、D-101～D-109 合同对象盘点、前端完成度与 e2e 覆盖）+ `docs/reviews/comprehensive-development-review-2026-07-24.md`（评审固定点 `main@f2b8c3aa`，基线 D-100）+ 当前 `.env` 与 git 状态（HEAD `f1ea53c9` + 20 文件未提交）。本条确认执行序列与裁决边界，不代表装配门已实现；R-06 双账本、D-045 支付→权益接缝等仍需真 PG/真闭环证据。
- 待验证：装配门 required gate 的 CI 形态与验收脚本；模型激活证据的开发态最小方案（探针 vs fixture 边界）；退役立即批执行后全量测试基线不回归；每项 R 修复对照 D-101～D-109 的校准规格产出；M-01 的 8/8 admission 方案（`contentPackagePlatform` 与 `distributionTarget` 拆分）；装配门关闭后「注册→首次出活」的实测时长。
- Supersedes：checkpoint §8「同一语义多套事实」诊断按核实结果修正、§9 路线 C 由本条转正；LPGS lpgs-00/lpgs-08 两票内容被吸收、wayfinder 流程作为独立决策权威的地位终止；不改写 D-036/D-041/D-103/D-108 及 D-101～D-109 任何已拍板内容。

## D-111 Day-0 与条件缺失的解法：双入口分流与意图驱动渐进引导

- 日期：2026-07-24
- 状态：`accepted`（用户给出双入口场景方案，经两轮修正后确认；并确立「场景反推先行」决策纪律）
- 决定：Day-0 与「启动条件缺失」类问题一律用 D-103 双入口的**产品行为**解决，不用数据层方案——不播种合成身份记录、不把必填字段改为模糊可选、不伪造普通品牌身份。**定制创作入口**：启动条件不满足时流内引导补充（对话式、每次 1～2 项、可跳过，D-031 口径），不生硬报错；满足后顺利进入后台调用并明确提示使用了哪些资料；引导后仍不满足或用户跳过，**自然路由到自由创作**先出活，显式告知「本次按通用模式生成」并给升级预告（补充后更像你的店），不静默降级（D-024）。**自由创作入口**：直接进入，零经营上下文要求，参数只按所选模型调用文档的约束保留默认值或由客户调整后调用（D-103 原合同）。
- 决定：**定制创作满足线**＝本次意图能有意义地用上客户**任何一类经营表达资产**——门店、产品/项目、团购/活动、品牌、个人 IP、素材、历史偏好——即 ContextBundle 对本次意图非空且有增益；不收窄为「门店」单类（客户表达需求覆盖门店、产品、团购、品牌、个人 IP 等多维，均服务宣发引流目的）。行业类目为通用最小增益单元，优先从创作意图推断，推断不出才轻问。
- 决定：**引导跟着意图走**，不设固定字段清单：意图指向团购→引导团购要素（项目/价格档/有效期）；指向个人 IP→引导 IP 要素（人设/口吻/身份）；指向新品→产品要素；行业已含于意图时直接以行业 Recipe 起步。
- 决定：**非必须项回退语义**：营销身份缺失→门店官方中性口吻（文案口吻回退，非数据实体）；素材缺失→纯文案或引导；项目/价格事实缺失→不引用具体数字；历史偏好缺失→无个性化。
- 决定：**F-01/M-03 修复方向**按本条重写：composer admission 不把营销身份作为硬性冻结必填字段；「查询失败/为空/未选择」三态显式区分；多身份显式选择并记忆默认、移动端身份/素材入口恢复随 M 门修复。
- 原因：Day-0 阻断（F-01 合入必修、M-03）此前的候选方案全部从「怎么满足技术合同」出发（播种记录/判别联合/放宽必填），是「只考虑技术实现和判断、没有结合实际场景反推」的脱节复现——与 M-02 要求上传文本文件、图文拆两次提交同一病根。正确顺序是**场景反推先行、技术合同服从**；决策相关功能时必须先给用户旅程与用户感知判断，再谈代码实现。
- 影响：装配门 provisioning 播种清单收敛为两样：trial 权益+平台默认供给（不播身份）；admission/gate 启动条件按「意图×经营上下文增益」分流逻辑重写；结果侧「已使用资料」提示与路由告知话术进入 Composer 交付面；本条与 D-110「修复即建设」并轨——后续所有 M/R 修复规格先做用户旅程/感知判断再定技术方案，作为校准纪律。
- 证据边界：基于 2026-07-24 chain-trace/ux-reach 探查（WIP 将身份设为硬冻结必填、onCreateUser 零播种）与 review M-03、agent-team F-01；分流逻辑与引导交互尚无代码与真实用户证据。
- 待验证：意图→资产类别的识别准确率（错误引导比不引导更伤感知）；「有增益」判定的实现形态（规则 vs 控制 LLM）；引导跳过率与补充转化率；路由告知话术的用户理解度；多身份默认记忆交互细节。
- Supersedes：agent-team 评审 F-01 的「identity 改可选+gate 放行空身份」修复建议（属模糊可选字段反模式）；本场此前讨论的「服务端播种中性身份记录」与「neutral|registered 判别联合」两个数据层方案（均不采用）；不改写 D-103/D-029/D-031/D-024/D-043，是其在 Day-0 场景的合成应用。

## D-112 Agent 介入边界：确定性骨架＋段内智能，LLM 出判断、状态机执行转移（含 platform 双字段合同）

- 日期：2026-07-24
- 状态：`accepted`（用户确认「确定性骨架＋段内 Agent」边界；A-2 platform 拆分并入本条）
- 决定：产品向 Agent 方向演进的架构边界固定为**确定性骨架＋段内 Agent**：五段 Harness 仍是 DBOS 确定性状态机（恢复、计费、审计、硬门的锚点）；每段内部为受限 agent loop——控制 LLM 理解、规划、在 allowlist 与预算内调用工具、自检，直至产出合格 artifact 或触发分支信号；阶段间分支（来源不足→补问、无增益→路由自由创作、质量不足→有界重试等）由 **LLM 产生结构化判断信号、Workflow 按 D-101 预置条件边执行转移**；对话层对用户完全 agentic（自由输入、追问、就地纠偏，D-032 原文）。本条是 D-032/D-108 的兑现与边界明确，不是新架构。
- 决定：「LLM 作为 workflow 大脑」三职责裁决：①**判断分流中枢＝控制 LLM，成立并立即建设**——D-111 的意图→资产类别引导与「有增益」判定由此拍定为 LLM 判断实现（原「规则 vs 控制 LLM」待验证项闭合）；②**工具调用中心＝段内成立**（D-108 已定形态：Context 理解、Skill 发现/选择、受控 Tool 调用、结构化规划）；③**运行时自由编排流程走向＝不采用**——五段序是内容生产自然序，重排无场景收益；事实/权利/合规/额度硬门不可交由「LLM 记得检查」；durable 恢复与一任务一预占依赖确定的步骤边界。
- 决定：**场景规则退位机制**：admission/gate 中的场景适配规则逐步退位给「控制 LLM 判断＋确定性校验兜底」；硬编码只保四类不可让步底线——事实真实、权利授权、产品额度、外部发布确认。M/R 修复按 D-110「修复即建设」直接落 LLM 判断形态（首例：M-02 事实槽不再写 MIME 匹配规则，改为「Recipe 声明结构化事实需求＋LLM 判断 ContextBundle 满足度＋确定性校验兜底」）。
- 决定：**platform 双字段合同**（M-01 修复方案）：拆分 `contentPackagePlatform`（内容结构语义，值域扩展为内容变体全集：`xiaohongshu / douyin / video_account / wechat_moments / offline_material / generic`，复用类继承来源或 generic）与 `distributionTarget`（分发语义：`export / manual_copy / assisted_handoff / publish:<platform>`，`publish:*` 仅对应平台过能力门后开放）。商家 UI 保持单一「发到哪/用在哪」选择，LLM 负责把商家表述映射到双字段，内部关注点分离不暴露给用户；能否代发前置展示（可代发/生成后导出复制），不做提交后报错。三条实施纪律：①用户确认的平台、交付物、模型设置进提交 Body→服务端签名 preview/quote→admission 冻结，杜绝静默覆盖；②同一 Recipe validator 覆盖发布时、提交时与全部正式 seed，8/8 seed 合同测试进 M 门验收；③平台/交付物变化影响用量档时实时更新 `UserDebitPreview`（D-109）。
- 决定：**建设时点**：装配门批次不变（接线非智能）；控制 LLM 判断节点提前进 M 门批次——D-111 分流判定、①段意图理解（自由文本 intent 字段已在未提交 WIP，顺势建）、M-02 事实匹配；③段规划（NotePlan/ImageIntent/Storyboard）随 R-01 修复即建设路径。
- 决定：**护栏三条**：每个 LLM 判断节点必须有确定性 fallback（超时/失败→保守路径：引导或路由自由创作，不硬失败）；判断输出全部过 Schema 校验（D-035 结构化输出基建）；判断质量以 golden cases 验收（D-036 promptfoo 基建）。不引入第二套 agent runtime（D-016 不动），控制 LLM 全部走现有 AI SDK 调用。
- 原因：需求清晰化后确认产品从生成工具向 Agent 演进（D-001 原点）；「灵活性受限、上下文组合机械、补差式填坑」的根源不是确定性主干，而是已设计的智能节点（①意图理解、②上下文组合判断、③规划）全部未建，只能用规则和表单补位、越补越机械；「LLM 出判断、状态机执行」在保留审计/恢复/计费锚点的同时把场景长尾交给模型。参照：Claude Code 对用户 agentic 而内部 harness 确定；Kickart 控制节点固定＋理解生成智能（D-104）。
- 影响：M-01/M-02 修复规格按本条重写；D-111「有增益」判定实现形态闭合为控制 LLM；admission 场景规则冻结新增（此后只加四类底线，其余进 LLM 判断层）；Landing 诚实能力（review 7.1）与 `distributionTarget` 能力门同源对齐；交付面（D-086）直接消费 `distributionTarget`，不再从 platform 推断。
- 证据边界：三职责裁决基于本场四路探查与 review 证据推理；段内 agent loop 的时延/成本/质量尚无本产品实测；自由文本 intent 字段存在于未提交 WIP（`composer-submission-client.ts`），未合入。
- 待验证：控制 LLM 判断节点的时延与 token 成本包络（判断类节点是否用更小模型——D-129 后＝默认 deepseek-v4-pro、轻档可切 v4-flash，catalog 层运营可换）；意图→双字段映射与分流判定的 golden cases 首过率；段内 loop 的预算上限与中断恢复语义；8/8 seed 合同测试通过后六卡真机旅程；LLM 判断与四类硬底线冲突时的仲裁顺序（底线恒胜）。
- Supersedes：任何「LLM 运行时自由编排五段/任意重排流程」的方案表述；M-01 的单字段平台合同与「UI 选择不进提交体」现状；checkpoint §5.2 对 WorkflowAgent「通常不需要」的表述细化为本条三职责边界；不改写 D-016/D-032/D-101/D-108/D-111，是其合成与兑现。

## D-113 事实槽 LLM 满足度判断、AgentKit 双参照登记与择优语义收窄

- 日期：2026-07-24
- 状态：`accepted`（用户确认；含对四宫格与抽卡择优的两处用户纠偏）
- 决定：**M-02 事实槽方案**（D-112 场景规则退位首例）：Recipe 来源槽从 MIME text 上传槽改为**结构化事实需求**声明（factTypes 对齐 StoreFact ledger 既有分类，不发明第二套分类法）；ContextBundle 内已确认 StoreFact 为**第一等满足来源**（revision/provenance/expiry 冻结进 snapshot），上传资产与来源 ContentPackage 降为补充来源；LLM 判断满足度（满足/部分满足/不满足）产出分支信号；事实的 expiry/撤销/权利标记走确定性硬校验（「事实真实」底线，LLM 不可放行过期撤销事实）；**部分满足＝LLM 按意图判断事实关键性**——对本次意图是核心事实（如团购推广之价格）→轻问补齐并顺手入 ledger，可省略事实（如日常种草之价格）→直接生成不引用并在结果侧提示，判断失败走引导保守路径；价目表截图/团购链接等上传走「资产→事实提取→用户确认→入 StoreFact ledger」建档链，不直接满足 Recipe 槽；正式 8 Recipe × ContextBundle 集成测试进 M 门（替换自造 Recipe 测试）。
- 决定：**AgentKit 双参照登记**（与 Kickart 并列为 D-104/R-01 参考实现）：`bytedance/agentkit-samples/ad_video_gen`（轻量版）＝最小生成链路基准——控制 LLM 理解规划＋一次 image_generate（四宫格故事参考图）＋一次 video_generate（reference_images 直出 15s 成片），零拼接零后处理；控制台模板 `ad_video_gen_seq`（生产完整版）＝生产形态参照——营销策划/图片生成/视频生成/抽卡评估/视频合成五环节，平台分层 Identity／Runtime／工具三类（内置 image·video·websearch＋MCP 化 video_clip＋自实现函数）／Observability／TOS。三参照分工：Kickart＝产品化控制节点参照，轻量版＝最小链路参照，seq 版＝完整形态参照。**借组合模式，不引 AgentKit/VeADK runtime**（D-016/D-112）；美业场景替换＝电商 4 段故事→美业行业 Recipe 故事结构（Recipe Studio 数据化承载），供应商替换＝D-107 CapabilityManifest＋ModelRecipeProfile 机制。
- 决定：**四宫格 Storyboard 仅为视频多模态参考的可选形式之一，不作固定项**——ReferenceIntentPlan 保持参考形式开放（单图/多图/宫格/参考视频/音频及各自语义角色，D-104 原则），四宫格作为「单图内多分镜自一致」技巧登记入候选形式库。
- 决定：**择优语义收窄**：④段 `execution_selection` 默认交付 **1 个主候选**，不做默认抽卡；「择」仅在两处显式发生——**用户品味选择/就地纠偏**（D-023/D-104，「优」的判断权在商家，系统只过硬门保「可用」不预判「最优」）与**质量门失败的有界重试**（D-101 预置条件，纠错语义非抽卡）；不建设多场景通用「优」判据；用户主动「换一版」＝新任务再次使用额度（D-109）。P-02 修复方向同此收窄（copy 默认 3 候选串行评分改为 1 主候选＋备选按需展开）。理由：抽卡评估是字节以消耗填补单次输出落差的**运营选择**，其成立前提——单一电商场景「优」判据可归纳＋平台承受内部多次生成成本——本产品均不具备；五类任务×四类输出的多场景强行归纳极可能「择而不优」；且按 D-109 内部淘汰不重复扣用户，任何内部多候选直接侵蚀毛利。本产品路线＝首次质量靠个性化上下文＋行业 Recipe 编译（D-111 增益逻辑），兜底靠就地纠偏，以编译质量降落差而非以消耗填落差。
- 决定：**D-105 前提显式化**（观察登记，不修改 D-105 任何决定）：首发成片时长≤单段模型上限（美业短视频 15 秒内主流，前提成立）；将来真实需求要求多镜头长视频时，参照 seq 版「合成＝受控工具节点（如 MCP 化 video_clip）」形态重新评估，不回创意拼装老路。
- 原因：M-02 是「合同视角压过场景视角」的标本（商家已确认的事实被要求以文本文件重新上传）；AgentKit 两版示例与 D-104/D-105/D-108/D-112 高度同构，验证本产品分层与模型原生直出路线是业界生产形态而非过度设计；但其抽卡环节属运营性选择，须按本产品场景结构与计费合同独立裁决而非照搬。
- 影响：M-02 修复规格按本条重写；R-01/视频线实施规格引用三参照；P-02 修复目标形态确定；④段实现按收窄语义建设；ReferenceIntentPlan Schema 保持形式开放。
- 证据边界：AgentKit 信息来自 GitHub README 与火山控制台模板页（opencli 实读）及公开架构图；其抽卡判据与 video_clip 工具合同未见源码；「美业多场景『优』判据不可归纳」是产品判断，未经实验证伪。
- 待验证：LLM 事实满足度判断的 golden cases 首过率；轻问补齐的用户接受度与入 ledger 转化率；四宫格形式在美业素材（真人/案例）上的适用性与权利边界；当前代际单段模型的实际可用时长；1 主候选交付后的用户「换一版」率（检验首次质量路线成立性）。
- Supersedes：本场早前「抽卡评估印证④段生产必要性」的表述（修正为运营性选择不照搬）；P-02 所指默认 3 候选现状的目标形态；不改写 D-023/D-033/D-101/D-104/D-105/D-109/D-112，是其应用与细化。

## D-114 定制创作主容器：agent 流式对话＋AGUI 三层结构（含 ContentIntent v1 定位）

- 日期：2026-07-24
- 状态：`accepted`
- 决定：**交互容器判断方法论**：由任务的时间结构与对象生命周期决定，不由「agent 应有的样子」的审美决定——秒级、多轮、上下文连续的片段→对话流；分钟级、用户会离场的任务→任务卡＋通知；持久、需回访管理的对象→对象工作区。
- 决定：**定制创作主容器换为 agent 流式对话＋AGUI，三层结构**：①**对话流主线**——意图输入、引导补问卡（D-111）、计划卡（NotePlan/Storyboard 等③段规划产物流式分块吐出，D-024 诚实呈现的最佳形态）、HITL 确认卡（D-013 七类节点）、额度预览卡（UserDebitPreview）、任务进度卡、成品预览卡（采用/导出/调整入口）全部发生在会话流内；②**对象工作区纵深**——成品卡点开进入完整工作区（变体、交付面、历史），结果中心保留，D-089「相邻不竞争」明确为「对话流是主线、工作区是放大镜」；③**任务卡时间桥**——分钟级长任务（视频等）在对话内嵌可离场任务卡，复用 SSE/异步任务中心/收件箱，完成后通知拉回。
- 决定：**自由创作不换**：保持「模型直调＋参数面板＋生成」形态（D-103），不强行 agent 化；两条主线共享成品卡与对象工作区。
- 决定：**被取代与保留边界**：被取代＝当前「Composer 提交→navigate 跳转结果页」的断裂模式；保留并重组＝SSE/token 流（ADR-0007）、result-center-shell、审批卡、任务中心——从独立页面重组为对话内嵌卡＋展开面，基建不推倒。
- 决定：**ContentIntent v1 定位四原则**（B-1 并入）：①ContentIntent 是①②段的编译产物而非用户输入结构，不可变冻结进 snapshot，永不投影为前端表单（D-031）；②它是四类输出编译器的分叉点，只装共享语义——objective、outputKind、主体经营资产引用、contentPackagePlatform×distributionTarget（D-112）、contextBundleRef、recipeRef、事实/权利/CTA 约束集、素材引用＋语义角色、表达身份（或中性回退）——四类不共享的细节（图片 slot、storyboard、页序）留给下游 ImageIntent/NotePlan/Storyboard（D-104 共享边界）；③`image_text_note` 为 outputKind 一等值（四值 `copy / image / image_text_note / video` 与四类编译器一一对应），修「lens 单交付物」结构缺口——图文是一次成品不是两次提交；④objective＝五类宣发任务枚举（日常曝光/热点借势/IP 运营/物料制作/承接转化，D-020）＋商家原话自由描述并存，LLM 归类、归不进就近归类＋原话兜底，不硬造第六类。字段语义锁定、字段名开放（D-103 先例）。
- 原因：D-111（引导对话）＋D-112（段内智能）拍定后，①②段的澄清、分流、告知天然是对话形态，「提交→跳转」会打断引导流；流式中间产物让商家看到「系统用了我的什么、怎么想的」；火山 AgentKit Runtime 调试页（用户提供截图：流式结构块＋工具卡＋逐 shot 评级＋内嵌成品）验证了体验价值，但它是单会话演示型调试台，缺多任务并行、离场恢复、资产管理、额度门与审批流，不可照搬；成品管理是对象语义，塞对话流会成滚动灾难——故三层分治。
- 影响：R-01/NotePlan/视频线的呈现层按本条实施（修复即建设，避免按旧跳转模式做完再返工交互层）；前端 Composer 演进为持续会话容器＋AGUI 卡片系统；会话（session）成为一等容器；Track B 各 Schema 的呈现合同以卡片为载体。
- 证据边界：三层结构基于旅程推演与既有基建盘点（SSE/token 流/任务中心/收件箱已建，ux-reach 探查证实）；会话容器改造与卡片系统的工程量未估；火山截图为 Runtime 调试页，非生产 SaaS 证据。
- 待验证：多任务同会话的信息密度与滚动体验；成品卡→工作区展开的转场语义；移动端对话流＋卡片可用性；计划卡流式分块的中断/恢复表现；ContentIntent v1 字段集对正式 8 Recipe 的覆盖检验。
- Supersedes：当前「提交→跳转结果页」交互模式（含 D-089/D-090「提交成功即进入 Result Center」的导航时点——改为对话流内成品卡点击进入，结果中心路由与投影/命令合同保留）；取代 D-081/D-098 C6 关于定制创作对口为必选 radiogroup 模式选择器的部分（自由创作线存续）；D-089 结果中心与工作台「相邻不竞争」表述细化为「主线/放大镜」；不改写 D-031/D-032/D-043/D-046/D-085/D-103/D-111/D-112/D-113，是其在交互层的合成落位。

## D-115 ImageIntent v1：三操作商家语言、七类语义 slot、精确文字校验门与 OutputPlan 两型

- 日期：2026-07-24
- 状态：`accepted`
- 决定：**三操作的商家语言**：定制创作中操作由系统自动选择，商家无感（D-111）；自由创作入口用任务语言三入口——「生成新图」「修改这张图」「用几张图合成一张」，分别精确映射 `image.generate / image.edit / image.reference_transform`，不暴露工程术语，无第四入口；自由创作 v1 不开放套图（OutputPlan 复杂度不暴露给裸模型直调）。
- 决定：**语义 slot v1 七类**（从美业五类任务真实素材反推）：`subject_person`（人物/技师，绑权利）、`work_case`（作品案例：甲面/发型/皮肤前后对比，绑真实性——edit 的 invariants 是美业命门，改了甲面即假案例）、`store_scene`（门店环境）、`product`（产品/项目实拍）、`brand_element`（logo/品牌色/字体）、`style_ref`（仅风格不复制内容）、`composition_ref`（构图参考）；每 slot 独立声明数量/格式/组合限制（D-106），由 ImageModelRecipeProfile 映射到各模型原生参考字段。
- 决定：**精确文字一等字段＋生成后文字校验门**：`exactText[]` 进 ImageIntent 一等字段，声明必须逐字精确的文字（价格/日期/权益条款）与允许模型发挥的文字（氛围文案）；生成后对 exactText 做 OCR/VLM 确定性对照，不匹配→有界重试（D-101 纠错语义）或降级为「图不带价格文字＋文案层给价格」；LLM 不可放行文字错误的图——「398 生成成 389」是事实事故而非质量瑕疵，此门是四类硬底线「事实真实」在图片上的落点；中文文字渲染能力进 CapabilityManifest 作为选模型硬能力轴（Seedream 系为首批 Profile 样本）。
- 决定：**OutputPlan v1 两型**：`single`（默认）/ `set`（套图：张数＋页序＋每页角色＋一致性要求）；图文笔记页序归 NotePlan 管，OutputPlan 只服务纯套图场景，NotePlan 内部引用 OutputPlan 承载图片分支，两者不重复建模页序语义。
- 原因：`reference_transform` 类词汇商家不可懂，任务语言入口消除理解成本；美业图片刚需是中文价格/权益文字上图，文字错误直接构成虚假宣传风险；slot 集合从真实素材类型反推而非从模型 API 反推（场景反推先行，D-111 纪律）。
- 影响：自由创作图片面按三入口实施；图片 Brief/CapabilityManifest/ImageModelRecipeProfile 按七 slot＋exactText 收敛；文字校验门进四类硬底线实现清单；B-3 NotePlan 引用 OutputPlan set 型。
- 证据边界：七类 slot 与两型 OutputPlan 为产品判断，未经真实任务覆盖率检验；OCR/VLM 对照的实现精度未验；Seedream 中文渲染强为官方宣称＋行业共识，未在美业样例实测。
- 待验证：slot 七类在真实任务中的覆盖率与缺类；exactText 校验方案已结（D-129 补充＝生成链既有多模态模型 VLM 校验，零新件），误报率票内验收；文字渲染失败降级路径的用户接受度；自由创作三入口的商家理解度实测；set 型一致性要求的评估口径。
- Supersedes：细化 D-106，不改写其任何决定；自由创作图片入口的任何术语化呈现方案。

## D-116 图文合一 NotePlan v1：页级计划先行、双风格可配草稿、确认卡超时放行与拟人化交付语言

- 日期：2026-07-24
- 状态：`accepted`（Office Hours Track B-3 收口；讯飞绘文真机走查后拍板）
- 决定：**NotePlan v1＝`image_text_note` 的编译产物**（ContentIntent 之下、D-114 计划卡的图文形态）：整篇主题锚＋页数组，每页＝`{pageRole, pagePurpose, imageIntent（引用 D-115）, textBlock（标题/正文/图内文字 exactText[]）, 依赖边}`；pageRole 初始枚举 `cover/pain_scene/solution_show/work_case/price_offer/cta_guide`，**可扩展不锁死**——页结构由语义决定，不固定张数（绘文死结构「1 封面＋4 内页」仅作参照）。
- 决定：**执行序＝plan-first**：先编译 NotePlan 全量呈现（计划卡流式，D-114）→图片分支页间并行执行→**文字依赖串行**：图内 exactText 与标题先定稿再进图，防止文字返工带动图片重做。
- 决定：**五维一致性评估**：整篇交付前按「主题连贯/视觉风格统一/信息不重复/pageRole 覆盖合理/图文互指一致」五维走 LLM 判定（D-112 判断中枢语义），不合格页回炉（有界重试，D-101 纠错语义）。
- 决定：**单页重生成＝1 张图点**（D-109 计费口径），不重扣整篇；纯文字层单页改写不扣图点。
- 决定：**ImageTextNote 全包＝R-01 试点门验收对象**（D-110 修复即建设：R-01 直接按本条目标形态实施，不做旧模式过渡版）。
- 决定：**意图确认卡＝建议性补充＋默认值＋超时自动继续**（补 D-111）：定制创作确认卡展示建议补充维度与将采用的默认值，带手动「继续」键与**超时自动放行**——引导永不变成新阻断；超时秒数为运营参数（绘文实测 30s 倒计时为参考基线），不硬编码。与 D-112 硬门合成的两条安全边界：①用户开始编辑卡片即暂停倒计时（防绘文实测「调整未完成被自动继续」缺陷）；②预计消耗超出可用额度或本轮涉及外部副作用时不自动继续，回落 D-112 额度/发布硬门显式确认。
- 决定：**默认双风格草稿＋风格集合受控可配**（补 D-113）：图文笔记默认产出两版风格草稿——**暂定「干货科普版」＋「种草叙事版」**——交付陈述给出版本定位导购（如「干货版适合收藏立专业人设、种草版适合互动转化」），用户自选后才进配图段（文案成本×2、图成本×1）。**风格集合不锁死为固定功能**：风格定义（名称/文体特征/结构模板/适用平台）为后台受控配置数据（复用 D-037 admin-config 存量机制：CAS＋审计＋回滚），可随内容平台风格/限制/爆款的动态演化增删改与重新编排。与 D-113 不冲突：其反对的是同模型抽卡＋系统择优；此处为跨风格定位各出一版＋用户自选，判断权在用户不在系统。
- 决定：**拟人化交付语言合同**：五段 Harness 全链对客文案（阶段宣告/进度/确认卡/任务总结/部分失败申报）是编排的一等输出面，按「拟人化交付陈述」标准产出——任务总结须含内容策略说明、版本定位导购、使用建议；失败申报诚实并给降级路径（绘文「图片未成功响应＋文案完整＋自备图建议」为活样本）；**禁止工程语言、内部对象名、LLM 原始反馈直接暴露给客户**。该层的产品机理：给经验不足的商家**内容确定性**（策略依据/趋势对齐/使用导购），从而支撑对产品价值的认可——有效运营技巧的产品化，与既有「商家语言泄漏」修复同一纪律并上升为编排层合同。
- 原因：绘文真机走查（`references/analysis/huiwen-tuwen-walkthrough-2026-07-24/REPORT.md`）实测印证 plan-first、exactText 场景、双容器交互、partial 诚实交付四项设计方向；其「跨模型双版＋用户自选」绕开「什么是优、怎么择」难题（D-113 曾裁决的风险），且美业商家干货（立专业人设）与种草（带转化）双需求真实存在；其确认卡超时放行解决「引导变阻断」隐患；其任务总结的拟人化陈述被用户点名为编排关注点与价值感知机制。
- 影响：B-3 关闭；NotePlan 进入 ContentIntent v1 编译层；D-111 补超时放行条款；D-113 补跨风格双版默认条款；D-115 ImageIntent 被 NotePlan 页级引用；风格集合进 admin-config 配置面；R-01 验收对象锁定为 ImageTextNote 全包；拟人化交付语言进五段 Harness 实现清单与评测面（可作 promptfoo 检查项）。
- 证据边界：绘文管线实测双样本取证——本会话走查 taskId=18210804（快速模式故障样本，`references/analysis/huiwen-tuwen-walkthrough-2026-07-24/`）＋Codex 深度走查 taskId=18210820（深度模式成功样本，SSE 协议/API 合同/成品审计/积分目录，`references/analysis/turbodesk-article-creation-flow-2026-07-24/`）；「加入到对话」＝Candidate→ArticleDocument 显式采纳门已由 Codex 证实（我方 adopt_harness_candidate OCC 为同语义更强实现）；双风格文案成本×2 的用户接受度未验。
- 待验证：超时秒数与美业用户操作节奏匹配（WOZ 期标定）；双风格默认是否构成 Day-0 选择负担（R 门观察）；风格配置 schema 细化（名称/结构模板/平台适配字段与编排界面)；五维评估判定 prompt 与回炉阈值；拟人化陈述评测口径（进七红线还是独立检查项）；图文任务整篇首次配图的计价口径（绘文参照＝按一次文案计价、自动配图 0 点促销、独立生图才计图价，进 Track C／D-109 数字框架输入）。
- Supersedes：无推翻——为 D-111/D-113/D-114/D-115 的补充条款与 B-3 提案收口。

## D-117 MarketingIdentity 建档交互与生成-发布责任边界

- 日期：2026-07-24
- 状态：`accepted`（Office Hours 人设对标拆解收口；第 2 条为用户两次现场拍板——生成自由+软提示+发布提醒，过程零限制）
- 决定：**建档交互＝对话式向导＋AI 结构化草案＋逐项确认**：MarketingIdentity 建档采用绘文验证过的交互范式——低门槛入口（不阻断首次创作，可跳过）→意图分流（个人 IP/产品服务/品牌等，与 D-111 资产类别对齐）→一句话背景＋可选参考文件→AI 十几秒扩展为结构化草案（名称/受众/定位/内容方向等全字段）→用户校对保存。叠加我方差异化：每个 AI 生成字段带 provenance（`user|account_import|document|ai_suggestion`），AI 推断的受众细分、专业能力声明默认 `unconfirmed` 待商家逐项确认；每字段带显式用途说明（绘文同款产品语言：哪个字段影响选题/口吻/深度）。
- 决定：**生成自由、软提示、发布收口——平台不承担全流程风控**（用户拍板）：①生成过程**零内容限制**——允许适当美化、多角度演绎、场景与体验叙事，不做生成期事实阻断/逐句核验，不因「事实未登记」拦截生成（商家自有信息量不足以支撑丰富内容输出，演绎是产品力的一部分）；②规避项＝**AI 生成软提示**（「内容由 AI 生成，请注意甄别真实性」类标识，绘文同款已足够，对齐 D-039 AIGC 标识义务；视频成片不做我方像素烧录——各发布平台上传时自动执行标识动作，既有烧录基建随 D-105 退役，见 D-118 裁决）；③风控唯一收口＝**发布提醒与发布审核**（D-112 四硬底线之「外部发布确认」门）：要不要发、发哪版由商家确认，内容真实性责任在商家；平台不做过多兜底。
- 决定：**三动作显式分离**：「创建人设」「设为默认」「本次会话选择」为三个独立动作（绘文「保存即隐式改默认＋临时选择不持久＋切换后专属选题缓存不失效」三状态混淆为反面教材）；人设相关投影（专属选题等）缓存键须含 identity revision，切换即失效。
- 决定：**执行绑定 identity revision**：创作执行冻结身份精确 revision（我方 `CreationExecutionSnapshot.identity` 存量形态确认不动），不用可变整数 id（绘文 `styleId` 复用人设 id 且无 revision 为反面教材）。
- 原因：绘文人设双证据包（本会话 `references/analysis/huiwen-tuwen-walkthrough-2026-07-24/persona-deep-dive.md`＋Codex `references/analysis/turbodesk-article-creation-flow-2026-07-24/05-persona-system-deep-dive.md`，任务 18210911 控制变量实证人设注入选题/检索/正文/图片上游全链）；其「人设幻觉经营事实」（开店十年/本店在推 TWG/抽奖送试用装）恰恰展示了演绎生成能力，我方以 AI 标识＋发布确认承接而非生成期阻断——与 D-025 医美「线下筛选＋线上纯提醒＋红线硬停」、「不过度泛化合规边界」既有反馈、验证期护栏成本≈0 原则一脉相承；过度事实约束会掐死内容输出丰富度，行业通行做法（绘文一行小字）已构成合规规避项。
- 影响：Codex 05 §16「四条硬规则」（无 store_fact 不得「本店在售」等）**不采纳为硬门**，降级为生成质量偏好（已登记真实事实优先注入，缺失时自由演绎）；本会话调研文档中「硬阻断」定性按本条修正；AI 生成软提示进成品交付面与导出物；发布确认卡为风控唯一收口；建档向导进 MarketingIdentity 建档链实施规格（R 门批次，修复即建设）。
- 证据边界：绘文建档/应用全链双走查取证；「从已有账号反推建档」分支未实测；演绎内容的商家实际审改行为未观察；AI 提示的法律充分性未经法务复核（D-040 合规置后，触发点＝功能完成重启验证）。
- 待验证：已有账号反推建档（美业商家常有现成店号，可砍 Day-0 建档成本）；AI 生成软提示的呈现位（成品卡/导出物/发布确认卡）；演绎内容商家审改率（WOZ 期观察，反哺「生成质量偏好」调优）；建档向导字段集与存量 `MarketingIdentity/expressionSamples/professionalBoundaries` schema 的映射细化。
- Supersedes：细化 D-112「事实真实」底线适用口径——收窄为「忠实性」（商家已提供的信息不得生成错，D-115 exactText/价格校验门属此、维持不动）与「红线硬停」（D-025 准入层、维持不动），**演绎性内容不属硬门范畴**；同时收窄 D-006「永不猜测」列表的生成期适用口径（D-006 处已加分派标注）；Codex 05 §16 硬阻断建议不采纳；不推翻 D-111～D-116。

## D-118 四类输出编译器的编排分级合同：轻输出退化执行、重输出多阶段编排、退化不旁路

- 日期：2026-07-24
- 状态：`accepted`（用户确认四分法与轻重判断后落盘）
- 决定：**四类输出形式确认**：面向客户的输出结果形式为四种，与 D-101/D-104 四类输出编译器一一对应——`copy`（纯文本：朋友圈文案/小红书文案/短视频脚本等）、`image`（纯图片）、`image_text_note`（图文结合）、`video`（视频）。「载体格式」不是新对象＝`contentPackagePlatform`（D-112）×风格集合（D-116 后台受控配置）作用于 copy 编译器的输出模板维度。
- 决定：**编排分级**：①纯文本＝人设＋载体模板拼接→**单次 LLM 调用**（无 plan、无 fan-out、无评估回炉；秒级 token 流式直出）；②纯图片＝ImageIntent 编译→**单次模型调用**（D-115 三操作/七 slot/exactText 门；十秒级）；③图文结合＝**多阶段编排**（D-116：NotePlan 先行→文字依赖定稿→页级图片并行→五维一致性评估回炉；分钟级，任务卡时间桥）；④视频＝模型原生**单调用直出**为主（D-105；分钟级＋离场恢复）。**AIGC 标识不做我方像素烧录**（2026-07-24 用户裁决：保持生成自由＋软提示＋发布收口原则——AI 生成软提示（D-117）＋发布确认提醒承接；各发布平台在上传素材时自动执行标识动作，标识烧录由平台侧完成，不由我方环节执行；D-105 烧录退役口径完整成立，与其无冲突）。候选策略分型：copy/image/video 默认 1 主候选（D-113），image_text_note 默认双风格草稿（D-116）。
- 决定：**退化执行，不是旁路**：轻输出（copy/image）是五段 Harness 的**退化执行**——③④段退化为「模板拼接＋单调用」，但仍在同一骨架进出：共享①②段（意图正名＋上下文注入：人设 D-117／事实槽 D-113／平台 D-112）、额度门、AI 生成软提示、交付合同、DBOS durable 载体与 SSE 投影。**禁止为轻链路另立旁路系统**；段位重量差异封装在各输出编译器内部，骨架与进出合同统一。
- 原因：用户确认四分法与轻重判断（纯文本/纯图片＝拼接＋LLM 可实现；图文＝绘文式多模型编排）；「几十万行完成度低」的病根＝模块各自为战（D-110 诊断），轻链路开旁路会再造第二套真相；绘文反面教材＝快速模式（其轻链路）恰是故障重灾区（假进度 3/3 稳定复现，见 `references/analysis/huiwen-tuwen-walkthrough-2026-07-24/REPORT.md` §10.1 第 6 条），**轻不等于可以不 durable**。
- 影响：四类输出编译器实施规格按分级表执行；copy 编译器实现成本＝prompt 模板＋人设注入＋单调用（轻量级，可先行接通验证骨架）；image_text_note 按 D-116 NotePlan 全量，R-01 为重链路验收对象；Track C 计费框架按分级对应（文案点／图点／文案×风格数＋图点／视频点，绘文积分目录为参照：`references/analysis/turbodesk-article-creation-flow-2026-07-24/02-observed-api-contracts.md` §14）。
- 证据边界：分级判断基于绘文双走查与已有决策合成；「秒级/十秒级」时延为预估，未在我方栈实测；退化执行的工程声明形态（编译器如何声明段位重量）未定义 schema。
- 待验证：退化执行的编译器声明形态（静态声明 vs 运行时判定）；copy 轻链路实测时延与 token 流式体验；载体格式模板集初始清单（朋友圈/小红书文案/短视频脚本之外的边界）；各发布平台 AIGC 自动标识的覆盖差异实测（抖音/小红书/视频号上传时的自动标识策略，发布提醒文案按平台差异校准）。
- Supersedes：无推翻——为 D-101/D-104 四类输出编译器的编排分级细化，合成 D-111～D-117 各合同在四种输出形式上的投影。

## D-119 客户资产录入交互合同：五步范式、双轨同链、拍照代填

- 日期：2026-07-24
- 状态：`accepted`
- 决定：**五步录入范式**（绘文人设向导四要素＋D-117 确认步，推广到全部客户资产）：①**看样子**——每类资产先展示行业示例卡（可换一换），让商家先看到「录完是什么样、能帮我干什么」（D-116 内容确定性机理在录入侧的应用）；②**选推荐**——美业行业先验清单（常见项目/套餐结构/赛道）推荐勾选少打字，清单为运营配置数据（admin-config 受控机制，与 D-116 风格集合同机制，不写死代码）；③**说一句**——一句话描述＋可拍照/上传：**价目表拍照→项目清单草案、团购截图→套餐草案、门头照→门店档案**（绘文「添加参考文件」的美业化；商家不愿打字但都有价目表和相册）；④**AI 编排**——扩展为结构化草案，字段带 provenance（`user|photo_extract|ai_suggestion`）；⑤**逐项确认**——预填表单校对，AI 推断默认 `unconfirmed`（D-117 同款）；素材类由 VLM 自动分类（work_case/store_scene/product/subject_person，对应 D-115 slot）＋生成描述＋**权利确认软提示不阻断**（客照肖像授权勾选，D-117 口径）。
- 决定：**资产类别覆盖六类**：门店（StoreProfile/StoreFact）、服务项目/产品、团购/优惠（Campaign fact）、品牌元素（brand_element）、个人 IP（MarketingIdentity——D-117 建档向导为本合同在该类的特例）、素材库（OwnedAsset）；存量 schema 承载，本合同新建的是**录入交互层**。
- 决定：**双轨入口、同一建档链**：①流内轨——定制创作中缺什么补什么（D-111 引导补充＝最小录入，产物直接入 ContextBundle）；②资产页轨——集中建档与管理（资产页从展示容器升级为**向导式录入容器**）。两轨同范式/同 schema/同 provenance 机制，禁止第二套录入真相（D-118「不旁路」同原则）。
- 决定：**录入永不前置**：所有录入均为可跳过的增益动作，不构成首击门槛（D-043 ≤2 击出活合同不动）；Day-0 冷态展示由示例资产承担。
- 原因：绘文人设向导实测验证「模板展示＋推荐＋LLM 引导＋LLM 编排」范式的低门槛建档效果（`references/analysis/huiwen-tuwen-walkthrough-2026-07-24/persona-deep-dive.md`＋`references/analysis/turbodesk-article-creation-flow-2026-07-24/05-persona-system-deep-dive.md`）；Day-0 断崖的另一半＝资产录入无交互层（D-043 诊断）；生硬填表是既往脱节病根（M-02 类「要求上传文本文件」）；用户点名「不是生硬的填表式，而是模板展示加推荐加 LLM 引导，然后 LLM 编排」。
- 影响：资产页升级为向导式录入容器；价目表/截图 VLM 提取进建档链实施规格；行业先验清单进 admin-config 配置面；D-113「上传走建档链不直接满足槽」的建档链获得交互层定义；素材权利确认按 D-117 软提示口径执行。
- 证据边界：范式源自绘文人设单类资产实测，推广到六类为产品判断；VLM 提取价目表/截图的准确率未实测；行业先验清单覆盖度未建；示例资产卡素材来源未定。
- 待验证：VLM 价目表/团购截图提取准确率；行业先验清单初始覆盖（建议美发/美甲/皮肤管理三品类起步）；示例资产卡素材来源；流内最小录入与资产页完整建档的字段深度分层（哪些字段流内必问、哪些资产页补全）。
- Supersedes：无推翻——D-117 建档交互成为本合同在 MarketingIdentity 类的特例；细化 D-111 引导补充的录入侧承接与 D-043 资产页形态定义。

## D-120 多格式资产解析管线：三件套引擎、单件同步批量异步、四层表结构

- 日期：2026-07-24
- 状态：`accepted`（D-119「拍照代填」的引擎室；用户提出 MinerU 候选与前台/后台之问后拆解拍板）
- 定位总纲（用户强调）：**MinerU 只是客户体验提升工具，不是流程核心卡点**——它是录入的可选加速器：不可用/不精准时手动输入无损承接（容灾条），实施上不作为关键路径依赖、不为它过度部署，架构上永不允许「解析服务挂了导致录入或创作不可用」。
- 决定：**管线五段**：接收（统一上传入口，**原件先落 OwnedAsset**——object key＋sha256＋权利标注，provenance 之根，可重解析不重传）→解析（格式归一）→结构化（语义提取）→确认（D-119 第⑤步逐项确认，provenance=`photo_extract`）→入库（写入领域表）。上传物形态谱三类：文档型（价目表照片/团购截图/PDF/Excel/执照）、纯视觉型（门头/环境/作品案例/客照——不走文档解析，走 VLM 分类）、链接文本型（团购链接抓取有平台风控，v1 引导「截图代链接」）。
- 决定：**解析引擎统一 MinerU，不为解析建独立 VLM 通道**（2026-07-24 用户修订：解析是体验提升功能非核心功能，不做过多部署；API 文档实读全文见 `references/analysis/mineru-api-notes-2026-07-24.md`）：**文档型输入（照片/截图/PDF/office 全系/复杂表格）全部走 MinerU 精准解析 API**（生产模式：token＋JSON 输出＋批量≤200；Agent 轻量版仅限开发调试——IP 限频不可控且无 JSON）；网页 URL（大众点评/店铺页）→`model_version=MinerU-HTML`（反爬页可达性待实测，兜底＝截图上传）；纯视觉图四 slot 分类（D-119）＝生成链**既有多模态模型的一次调用**（非新通道非新部署）；敏感证件类（营业执照等）v1 **不做自动解析、手填**（同时规避第三方数据边界问题）。关键参数基线：图片/扫描件必须显式 `is_ocr=true`（默认 false 是坑）、`model_version=vlm`（官方推荐）、`enable_table=true`、美业场景 `enable_formula=false` 省耗；callback（SHA256 checksum＋失败重推 5 次）与 DBOS 任务步对接，`extract_progress` 做真实进度条（对齐 D-114 白话进度，不做绘文式假进度）；每账号每天 1000 页最高优先级（超出降级排队，验证期≈500+ 商家建档/天足够）。结构化段＝LLM 把中间表示转领域对象草案（「项目：头皮护理 SPA/398/60min」）——此步 MinerU 不可替代 LLM。
- 决定：**交互同步＋执行服务端**（「前台还是后台」为假二选）：交互上同步——上传→解析中动画（秒到十秒级）→草案卡即时呈现→当场逐项确认（异步会把 D-119 第④→⑤步打断，草案无人确认则 ContextBundle 永远吃不到事实，用户旅程先行纪律）；执行上服务端——第三方 API key/计量/重试/审计全在服务端，前端永不直调；解析任务走**同一 DBOS durable 载体**（D-118 不旁路延伸：有外部调用与失败重试的任务不做裸调）。
- 决定：**单件同步、批量异步**：单件（一张价目表/一个截图）同步走完五段；批量（一次 50 张素材照）走后台队列＋任务卡时间桥（D-114）＋完成后「批量确认」界面一屏勾完。
- 决定：**四层表结构**：`OwnedAsset`（原件层）→`ParsedDocument`（解析层：中间 Markdown/JSON＋解析器版本＋sourceAssetId——独立成表理由：解析器升级可重跑原件、审计可回放「AI 读出什么 vs 商家确认什么」）→`AssetDraft`（草案层：领域草案＋provenance＋unconfirmed 字段集）→领域表（确认层：StoreFact/项目/Campaign fact/OwnedAsset 分类标注）。**ContextBundle 与后续 workflow 只读确认层**（D-113「已确认事实第一等满足」的存储面落点）——调用面天然干净。
- 决定：**数据边界**：敏感证件类（营业执照等）v1 不做自动解析、手填（既不送第三方解析 API，也不建我方 VLM 解析通道——与上方解析引擎段及修订①一致）；MinerU 开源自部署（需 GPU）登记为规模化后的迁移路径，触发点绑用户量不预设时间（验证期用最快栈原则）。
- 原因：用户指出 D-119 的前置条件——多格式零散上传物需结构化入库供 workflow 调用，并提出 mineru.net API 候选与前台/后台之问；拆解结论＝MinerU 只覆盖解析段文档型输入，完整引擎需三件套；同步交互是确认环节不断裂的前提；服务端执行是 key 安全/计量/审计的前提。
- 决定：**手动输入容灾（2026-07-24 用户补充）**：解析失败（MinerU API 错误/超时/限流）或识别回填不精准时，**手动输入始终可用**——五步范式的④AI 编排段可整体跳过，直接进⑤逐项确认表单手填（provenance=`user`）；手填产物与解析产物**同 schema、同确认层**，下游 ContextBundle/workflow 引用无差别，客户流程体验完整不断裂。交互要求：解析中动画常带「跳过，直接手动填写」出口；解析失败按 D-116 拟人化陈述诚实申报＋一键转手填，永不阻断。AI 代填是加速器不是必经点，手动路径＝范式保底轨。
- 决定：**解析管线＝共享基础设施（ParseService），非资产录入专属**：输入路由＋Provider 调用＋ParsedDocument 落库做成一个服务，除资产录入外的复用环节（按优先级）——①D-117/D-119 建档向导「添加参考文件」（品牌手册/旧文案→表达样例与品牌信息提取，零新建复用）；②**平台页面反推建档**（商家大众点评/美团店铺页 URL→MinerU-HTML→建档草案，「拍照代填」升级「链接代填」，反爬可达性待实测）；③admin 侧风格模板导入（爆款结构文档→D-116 风格集合配置草案）；④行业知识库（远期，D-113 事实检索源）。
- 影响：D-119「拍照代填」获得引擎定义；解析 Provider 进 D-107 CapabilityManifest 机制（新增 document_parse 能力轴）；解析消耗的计费归属（平台承担 vs 建档配额）挂 Track C；ParsedDocument/AssetDraft 两新表进领域模型；批量确认界面进资产页实施规格；MinerU 进参考项目索引。
- 证据边界：MinerU 能力/参数/配额基于官方 API 文档实读（`references/analysis/mineru-api-notes-2026-07-24.md`，原文快照留存），未在美业价目表样本实测精度；VLM 直读单照准确率未与 MinerU 对比实测；计费价格表与数据留存条款需登录 API 管理页核（笔记§六未核清单）。
- 待验证：MinerU（vlm 模型）在美业真实价目表/团购截图样本上的提取精度验收（不达标再议补充方案）；数据留存条款（隐私政策未取到，法务批次；敏感证件已改手填规避）；MinerU-HTML 对美团/抖音/大众点评反爬页可达性（HTML 100 个/天限额下「链接代填」可行性）；解析计费归属（Track C——注：MinerU 当前**免费无商业化收费计划**，登录态实核，解析段近零成本）；批量确认的一屏交互形态；免费政策变化监测＋自部署预案。运维硬要求（登录态实核）：**Token 有效期 90 天不可续期、每账号最多 5 个**——轮换热切换机制随接入实现；限流合同＝提交 50 文件/分钟、5000 文件/天（HTML 100/天）、查询 1000 次/分钟。
- Supersedes：无推翻——为 D-119 第③④步的引擎与存储合同；细化 D-113 建档链的存储面（四层）与 D-107 Provider 机制的能力轴扩展。（2026-07-24 同日两次用户修订：①解析统一 MinerU 不建独立 VLM 通道、敏感证件手填；②新增手动输入容灾条。）

## D-121 内容工作区：个性资产容器、引用上下文入口与套餐计费维度

- 日期：2026-07-24
- 状态：`accepted`
- 决定：**内容工作区定义**：每个账号拥有 N 个内容工作区；一个工作区与**一个店铺或一个品牌 1:1 关联**，承载该经营主体的独立一套个性资产（D-119 六类：门店/项目/团购/品牌元素/个人 IP/素材库）。工作区＝**纯粹的内容存储与引用上下文入口**，不承担其他职责。
- 决定：**与租户严格分离**：内容工作区**不是**租户/权限/安全边界——租户＝账号级安全与计费主体（现有代码 `workspace/ws_*` 语义按租户理解，保持不动）；内容工作区是租户之下的内容组织单元（账号 1:N）。实施时必须以新对象命名切割两概念（候选名 `ContentWorkspace`/`BrandSpace`，定名归实施），合同层禁止复用现有 workspaceId 承载本概念（绘文 styleId 复用人设 id 的语义混淆为反面教材）。
- 决定：**引用作用域**：创作时 ContextBundle 从**当前工作区**解析（人设挂载、事实槽满足、素材引用全部以工作区为边界）；D-119 录入与 D-120 解析产物归属当前工作区；**跨工作区资产不互通**（不串上下文）。
- 决定：**套餐一等计费维度**：工作区数量进套餐结构——**初级套餐 1 个、中级套餐 2 个、高级套餐 3 个，可单独购买加油包扩展**（档位命名经 D-123 统一）；多品牌/多内容背景需求通过购买承接。（数字为方向基线，精确档位与定价进 Track C／D-109 数字框架。）
- 决定：**与 D-101 单店单人兼容**：默认 1 工作区＝单店单人体验完全不变；多工作区是商业化扩展而非产品复杂化——前台交互仅「工作区切换器」一处显现，Day-0 用户无感。
- 原因：资产录入（D-119/D-120）需要明确的归属容器与额度边界；绘文以「人设最多 3 个」做付费维度（`references/analysis/huiwen-tuwen-walkthrough-2026-07-24/persona-deep-dive.md` 商业面），用户升维为「资产上下文空间」切法——一个工作区内人设/门店/素材是一套整体，跨品牌开新工作区，比按单类资产计数更干净且天然防上下文串味。
- 影响：D-119 录入/D-120 解析/D-113 ContextBundle 合同均增加工作区作用域；D-109 套餐结构获得第一个结构性维度（1/2/3＋加油包）；Composer 增加轻量工作区切换器；领域表挂工作区外键；数据模型新增 ContentWorkspace 对象。
- 证据边界：1/2/3 档位为用户方向拍板，未经定价测算；「一工作区一店铺/品牌」的粒度未经多品牌商家访谈验证；与现有租户代码的切割成本未盘。
- 待验证：与现有 workspace（租户语义）的命名与迁移切割方案；单工作区内多平台（小红书＋公众号）人设变体是否够用（绘文一人设一平台为参照）；加油包定价与购买流；工作区切换器交互位；多工作区商家的真实占比（R 门观察，决定该维度的商业权重）。
- Supersedes：无推翻——为 D-101 单店单人的商业化扩展维度；D-109 计费框架的结构性输入；D-119/D-120 的归属容器定义。（档位命名后经 D-123 统一为初级/中级/高级，数字 1/2/3 不变。）

## D-122 HITL 编排容错原则：三允许、流程恒前进

- 日期：2026-07-24
- 状态：`accepted`（用户对 D-116～D-121 系列的纲领提炼）
- 决定：Human-in-the-loop workflow 编排的核心容错原则——**允许 LLM 幻觉、允许工具微小错误、允许人介入修正，以推进流程顺利进行**。三允许并列成立：不为追求零幻觉/零错误在过程中设阻断（那会掐死流程与产品力）；人的介入位是**修正点不是审批墙**——可介入、可不介入，不介入时有默认值/超时放行/容灾轨承接，流程恒前进。
- 既有落点（本原则的合同化实例）：D-096 partial 诚实交付（能力感知交付 attempt 状态机逐对象诚实展示；D-109 部分交付退回未交付部分同族）、D-112 单阻塞审批（唯一硬阻塞＝外部发布确认）、D-116 确认卡超时放行、D-117 生成自由＋发布收口（允许演绎，人审在发布）、D-119 逐项确认（AI 代填＋人校对）、D-120 手动输入容灾（工具错→人修正，产物无损承接）。
- 决定：**设计检验三问**——后续任何 workflow/功能设计以此检查：①是否为追求零错误而阻塞流程？②人的介入位是否可跳过（默认值/超时/容灾）？③错误发生时是否有人工修正轨、且修正产物同 schema 无损进入后续引用？任一为否须重设计。
- 影响：进 workflow 编排与功能 spec 的评审检查项；与 D-110「修复即建设」并列为执行纪律；后续评审/复刻建议中「为消灭错误而加阻断」类方案默认按本原则降级处理（与 D-117 对 Codex 硬规则的裁决同口径）。
- 证据边界：原则提炼自本日绘文对标与 D-116～D-121 拍板过程；三问检验的实际拦截效果待后续 spec 评审实践验证。
- 待验证：三问进哪个评审环节（spec 模板 checklist vs 评审 prompt）；「微小错误」与「不可接受错误」的边界案例积累（忠实性/红线之外是否有第三类）。
- Supersedes：无——纲领提炼，不改变任何既有合同。

## D-123 商品数字框架 v1：内部成本基准、三桶额度、视频亮点定位、数字＝运营参数

- 日期：2026-07-24
- 状态：`accepted`（用户四段拍板：①内部成本基准与大冗余原则；②套餐组合灵活可调＋运营线下核算手填＋不做成本管控开发兜底；③高频主力/视频亮点＋代运营引流的产品运营逻辑；④视频档位 3/6/9 定数＋三类加油包＋缺额提醒/超额报停）
- 决定：**内部成本基准**（对内核算参考，动态调整，永不进前台，符合 D-109 供应细节不可见）：文案 ≈0.1 元/次输出、图片 ≈0.5 元/张、图文混编＝1 次文案配 1～6 张图、视频（≤15 秒）≤15 元/条。对客定价与套餐定价必须留**非常大的冗余**保证容差——内部重试、质量淘汰、高规格倍数、供应商波动按 D-109 均不重复扣用户，全部吃进我方容差；内部核算参考线＝综合毛利率 ≥70%（参考线，非系统强制门槛）。
- 决定：**高频主力×亮点分层（产品运营逻辑）**：文案、图片、图文笔记＝高频生成主力服务内容（低成本、足量供给、撑日常价值感）；视频天然极高成本，定位为**亮点功能**——套餐内只配小额度、以**视频专项加油包**补充，加油包价格标高一点影响不大；**视频同时是代运营服务的引流钩子**：客户想获得更多视频生成服务与额度时，转化为线下陪跑或代运营服务。SaaS 工具→线下服务的转化漏斗为既定产品运营逻辑；产品面只需「视频额度用尽→引导」位，不建设代运营功能模块。
- 决定：**视频档位定数**：初级 3 条/月、中级 6 条/月、高级 9 条/月，超出一律以视频加油包形式补充。
- 决定：**额度三桶＋三类加油包**：套餐额度＝文案/图片/视频三桶（行使 D-109「除非证明需要单独商品化」预留条款：文案升为第三桶，套餐内从宽配置、文案包兜底）；加油包分**文案包/图片包/视频包**三类，**后台自由定价**（视频包按引流定位可显著标高）。
- 决定：**计费运行时合同（商家感知面）**：前台感知只有「付了对应款项→得到对应次数/额度」；租户管理面＝每次消耗扣减对应次数/额度；**缺额提醒**（低额度阈值提醒）＋**超额报停**（额度不足时任务不可发起）——为 D-109 一任务一预占/失败全退/`UserDebitPreview` 合同的商家语言表述，不新增计费机制。
- 决定：**数字＝运营参数，不做成本管控开发兜底**：全部套餐组合（档位价格、三桶额度、工作区数、加油包规格与价格、促销组合）为后台可调配置，**由运营人员线下核算后手填进后台**（`ProductCatalogRevision`＋既有 admin-config CAS/审计/回滚承载，D-037/D-109）；支持客户运营与阶段性促销活动的灵活调整（促销走 D-109 `PromotionalGrant/GrantLot`，不改已发布套餐）；**前期不为严格成本管控做过多开发兜底**——不建毛利守门、成本告警、自动核价系统，`ProviderCost Ledger` 只记录、供线下核算取数。
- 初始参考值（供运营首次线下核算的起点；除视频 3/6/9 为拍板数外均为可调参考，毛利按成本上限口径估算）：试用（注册赠一次性）＝文案 20＋图 10＋视频 1（成本 ≈22 元＝获客成本）；初级 199 元/月/1 工作区＝文案 100＋图 40＋视频 3（成本 ≈75 元，毛利 ≈62%）；中级 399 元/月/2 工作区＝文案 300＋图 100＋视频 6（成本 ≈170 元，毛利 ≈57%）；高级 699 元/月/3 工作区＝文案 600＋图 180＋视频 9（成本 ≈285 元，毛利 ≈59%）；加油包参考＝文案包 100 次/29 元、图片包 50 张/89 元、视频包 3 条/149 元或 5 条/249 元（视频包可再标高）。主打档需回 ≥70% 参考线时，由运营上调档位价（如 299/599/999）或下调图额度，核算权在运营。
- 影响：D-109 额度桶体系扩展为三桶、加油包三类（其余合同——预占/回退/四账本/供应细节不可见/促销 GrantLot——完整有效）；档位命名统一为**初级/中级/高级**（对应 D-121 工作区 1/2/3）；D-118「Track C 计费框架按分级对应」获得数字基线；D-116 待验证「图文首次配图计价」获输入＝绘文参照（按一次文案计价＋自动配图 0 点促销）进运营核算选项；缺额提醒＝轻量运行时要求（阈值提醒挂额度预览面）；D-102 ④单一商品目录承载三桶×档位×加油包 SKU。
- 证据边界：成本数字＝用户 2026-07-24 口头基准（无供应商合同/实测账单佐证，声明动态调整）；视频 3/6/9、三类加油包、缺额提醒/超额报停、运营手填与代运营引流逻辑＝用户直接拍板；毛利 ≥70% 参考线与档位价/文案图额度参考值＝我方核算建议（运营参数，非拍板数）；绘文积分目录（`turbodesk-article-creation-flow-2026-07-24/02` §14）为外部参照不复制数字。
- 待验证：视频真实成本按时长/规格分档实测（15 元为 ≤15s 上限口径）；缺额提醒阈值；文案包真实购买需求（套餐内从宽后可能长尾）；「视频额度用尽→代运营引导」的产品位形态与转化话术（R 门观察）；促销组合在 Payment/Entitlement 账本的表达细节；档位价格锚（199/399/699 vs 299/599/999）由运营首次核算定。
- Supersedes：D-109「首版只出售图片额度与视频额度两种可认知用量、文案默认不单独商品化」条款（行使其预留升级为三桶三包；D-109 其余全部合同有效）；D-121「初始/高级/最高级」命名统一为「初级/中级/高级」（工作区 1/2/3 数字不变）。

## D-124 D-102 六项付费收口的门槛归属：R 三项、E 三项，试点期支付零开发

- 日期：2026-07-24
- 状态：`accepted`（用户确认按建议分配落盘）
- 补充（2026-07-24，D-128）：R 门①注册通道现实校准——试点期＝邮箱＋兑换码＋运营代开账号；手机号短信登录挂 E 门评估，不提前建。
- 决定：**R 门（真实商家试点前必须闭合）三项**：①注册/验证/找回邮件（真实商家须能自主进入产品）；④单一商品目录（试点所见价格/套餐/额度必须同源，前台数字从 `ProductCatalogRevision` 取数，直接承接 D-123 运营手填）；⑤前台去工程语言（试点第一眼不见工程语言，对齐 D-105/D-109 既有合同）。
- 决定：**E 门（公开发布收款前必须闭合）三项**：②支付 Provider 真实 checkout/webhook/续费/取消/portal；③删除/匿名化（合规义务按 D-040 置后逻辑，试点期人工处理承接）；⑥取消/退款/人工补单闭环（有真实收款才有退款场景）。
- 决定：**试点期支付零开发原则**：试点期收款（如发生）走 D-045 既有兑换码＋人工开通＋人工补单承接，与 D-123「运营手填、不做开发兜底」同精神；真实收款链路集中在 E 门一次性建成，不分散建设、不提前建设。
- 影响：D-110 门槛序列获得 D-102 六项的验收挂载（R 门验收清单＝①④⑤，E 门验收清单＝②③⑥）；D-045 兑换码/支付接缝在试点期的角色明确为过渡承接非最终形态；R 门验收脚本须把①④⑤纳入。
- 证据边界：分配依据＝D-040 置后原则＋D-110 序列＋D-045 兑换码能力已在代码中（mkfast-app 对照）；试点商家对注册邮件链路的实际需求强度、试点期是否出现主动付费诉求均未实测。
- 待验证：①邮件服务商选型已结（D-129＝模板内置 Resend），送达率挂 E 门；试点期出现「试点商家要求正式付费」时是否提前触发②部分能力；③人工删除流程的响应时效承诺口径；⑥人工补单的审计留痕格式（沿用 GrantLot 幂等键）。
- Supersedes：无——D-102（六项内容）与 D-110（门槛序列）的应用细化，不改二者。

## D-125 Landing 页两阶段：先诚实填充随 R 门，精细窗口专项后置

- 日期：2026-07-24
- 状态：`accepted`（用户拍板：先按现有框架做内容填充，专项精细调整后置，功能完整性与流程体验测试优先）
- 决定：**阶段一（当前，随 R 门批次）＝现有框架内容填充**：不动版式结构，把模板皮占位内容替换为诚实内容，四原则执行——①能力宣称只写已拍板合同（四类输出/五步资产录入/人设/工作区；不宣称全平台代发布、智能代运营等未拍板或三态做不到的能力，对齐 D-024）；②pricing 区从单一商品目录同源取数（初级/中级/高级、三桶额度、加油包，不硬编码，承接 D-123/D-124 ④）；③定位语＝行业特征×个人 IP 宣发工具平台，非店务经管系统；④AI 生成软提示口径一处诚实交代（D-117/D-039）。阶段一不新增代运营区块。
- 决定：**阶段二（后续专项）＝Landing 精细窗口专项**：Landing 本身是非常好的获客窗口，值得专门细化（视觉、转化、文案打磨）；单独立项、不在当前批次；代运营服务入口的取舍（工具定位纯度 vs 服务心智预建）留到该专项一并定。
- 决定：**优先级纪律**：功能完整性与后续体验的全流程测试先于 Landing 精细化——窗口再好，背后流程不完整即为空转化。
- 影响：Landing 内容填充进 R 门批次（与 D-124 ⑤前台去工程语言同批验收）；07-19 critique 的 pricing 模板皮 P1 债在阶段一以内容替换部分偿还，视觉层整改留阶段二；阶段二立项条件＝R 门试点跑通后。
- 证据边界：现有 Landing/pricing 为 mkfast 模板皮（07-19 impeccable critique 实锤 P1）；代运营转化数据为零，入口取舍无数据支撑故后置。
- 补充（2026-07-24，D-130）：Landing 页不受前端组件基准（HeroUI Pro V3）约束——阶段一保持现状，仅改文案与前后链接；组件/视觉专项优化归阶段二。
- 待验证：阶段一填充后 Landing 与实际产品能力的一致性走查；阶段二立项触发点（R 门试点后或真实获客需求出现时）；代运营入口取舍（阶段二定）。
- Supersedes：无——D-102 ⑤与 D-124 的 Landing 面应用细化。

## D-126 Dashboard 首页轻推荐与 Day-0 示例店：同一工作台首页的热态与冷态

- 日期：2026-07-24
- 状态：`accepted`（用户整体确认建议方案，含示例任务真实执行扣试用额；用户补充澄清范围＝Dashboard 首页）
- 范围澄清：本条「首页」＝**Dashboard 工作台首页**（登录后主界面，即评审 P-01 所指主页），**不是 Landing 营销页**（Landing 归 D-125）；轻推荐卡与示例店预览均挂载于登录后工作台。
- 决定：**轻推荐内容合同（热态）**：一天一个主推荐，非机会流；三要素＝为什么今天适合发（周几/节气/平台节奏等确定性依据）＋用了本店什么（点名引用当前工作区哪项资产）＋希望顾客做什么（CTA 意图）；**点击＝预填充 Composer，非自动执行**（对齐 D-043 ≤2 击与 D-112 确认合并，额度在「生成」处才消耗）；推荐文案走 D-116 拟人化交付合同（「周五晚是美甲预约高峰，把这周新款发出去正合适」，禁工程语言）。
- 决定：**v1 推荐依据＝确定性规则＋行业先验**：admin-config 受控配置、运营可调可换；不建推荐算法、不接实时热点抓取——与 D-116 风格集合、D-119 行业清单同一受控配置机制，对齐 D-123「前期不做开发兜底」口径。
- 决定：**Day-0 示例店（冷态）**：三行业起步（美发/美甲/皮肤管理，D-119 三品类）各配一套示例资产（门店档案＋2～3 个项目＋1 个团购＋若干素材），运营后台维护；示例资产 **provenance=platform_sample，永不进入真实 ContextBundle 引用**——仅作 D-119 五步「看样子」的素材源与首次演示底料；用户真实资产一录入即替换展示位，防示例内容泄入真实生成。
- 决定：**示例任务＝真实执行**：新商家从示例店「试着做一条」走真实生成链路、扣试用额度（D-123 试用档＝获客成本），第一次体验即真链路真产物；不建免费演示模式第二链路（D-119 双轨同链、D-118 禁旁路同一精神）。
- 决定：P-01 修复方向按本条执行；「1 候选执行确认」确认由 D-113 收编，无需新决策。
- 影响：`TodayRecommendationCard`/`ExampleStorePreview` 由未挂载组件转正为生产功能，实施规格以本条为准；首页构成＝热态轻推荐卡＋继续上次工作/冷态示例店预览（P-01 五要素全覆盖）；行业先验受控配置扩展为「行业清单＋示例店资产＋推荐规则」三件套同机制；R 门验收含 Day-0 首跑真实链路。
- 证据边界：两组件代码存在未挂载＝评审 P-01 实锤；三行业与示例资产构成＝方向拍板，具体素材由运营制作；试用额度数字引用 D-123 参考值（运营参数）。
- 待验证：推荐规则 v1 起步规则集（周几×行业×平台节奏）；示例店素材制作与权利链（平台自制素材授权）；platform_sample 隔离的实现位置（ContextBundle 解析层过滤）；示例任务产物是否允许导出（已拍板允许、判断与正式使用一致，试用状态额度＝后台人工动态开关——D-128 结项）；推荐卡点击→Composer 预填的字段映射。
- Supersedes：无——P-01 修复方向定案；D-043/D-111/D-119/D-123 的首页面应用细化。

## D-127 开发路线：原地换壳保核——不重开、分层处置、真删默认

- 日期：2026-07-24
- 状态：`accepted`（三个分歧点用户逐条拍板：①退役面真删成立；②换壳即下线无双轨期＝正常开发节奏；③god module 收敛采纳「触碰时拆」建议；同日用户补充画布冻结细则——全冻结仅留入口、接入形态悬置、后续视情况解锁，见 FREEZE 桶）
- 背景：D-111～D-126 重画交付面后，重审「重新开发 vs 原有基础增删改」。现状＝无客户、无数据负担，可零迁移成本推进。实测盘点（2026-07-24）：全库约 55 万行 TS（core ≈30.8 万＋web ≈24.2 万）、650 个测试文件；新决策批点名否定的存量代码不足 3%（compose/烧录链＋槽位表单残留）；ADR-0013～0018 全部「凝结自现状」＝新决策批亲手确认后端内核有效；MinerU 足迹＝0（新增面净新建，与旧码零冲突）；今日推荐卡/示例店组件存在但全库零 import（ADR-0019「装配失守非架构错误」实锤）。
- 决定：**不重开 repo、不整体重写**。「无客户无数据负担」的正确兑现＝敢删代码、敢破坏旧合同、不留兼容垫片——它降低激进手术的成本，不使「从零重长已对的东西」变得划算。D-110「修复即建设」深化不推翻。
- 决定：**分层处置（模块归桶）**：
  - **KEEP+EXTEND**：SaaS 骨架（注册/租户/计费权益/审计/admin-config，≈3.3 万行）、编排执行内核（五段 harness/执行脊柱/job 运行时，≈3.2 万行）、模型供给链（model-supply/supply-registry/integrations，≈12.2 万行）、交付结果面（creation-experience/result-delivery/product，≈2.9 万行）。
  - **RETIRE（真删默认）**：ffmpeg compose/烧录链（D-105/D-118，video ≈2.5k 行＋27 文件散点）、槽位表单残留（D-031/D-114）、换壳完成后的旧 IA 页面——默认动作＝**git rm 真删**（git 可恢复），不注释封存、不留兼容垫片。
  - **FREEZE（冻结不删）**：Pro Studio（core ≈2 万行＋canvas 面）**全部功能冻结，只保留入口**——入口在现有框架内定位＝**自由创作模式的高阶版**（ADR-0012 降级口径细化）；具体接入形态与功能**当前悬置不判**，实施 Agent 不得自行设计画布接入或恢复画布投入；解锁触发＝已确认决策（D-111～D-127 票包）完成后视情况评估，不预设时点。冻结期不投入、不删除。
  - **REBUILD（换壳）**：前端交互壳沿在途 Composer 主干候选完成换壳（M-01/M-02 合同断裂先修）；**旧交互面换壳完成即整批下线删除，不做新旧双轨并存期**，接受中间态部分旧页短暂不可用。
  - **NEW（净新建）**：解析管线（MinerU）、四类输出编译器、三桶计费扩展、Dashboard 首页挂载、Recipe Studio、Skills——按 ADR-0019 纵切新建。
- 决定：**god module 收敛＝触碰时拆**：model-supply（≈8 万行）/operations（≈5.7 万行）不开专项大重构票（重写陷阱变体）；功能票触碰哪块、拆哪块，拆分随票交付随票验收。
- 影响：D-111～D-126 拆票前置＝逐模块归桶判定矩阵细化到子模块粒度（执行线完成），每张票动工前先归桶——**禁止在 RETIRE 桶上续命、在 REBUILD 桶上打补丁**；退役执行并入 D-110 退役批次机制；重开新 repo 选项关闭（重开第一步仍是装 MkFast＝重长注册/计费/审计等已对资产，且清零 650 测试文件的验证资产与 CI 管线）。
- 证据边界：行数/文件数＝2026-07-24 实测盘点；「底座强、病在收口与接线」＝07-18 评审与 D-100 评审（`docs/reviews/comprehensive-development-review-2026-07-24.md`）两次独立定性；重写陷阱成本大头＝验证非编写＝闲鱼项目 10-agent 评审独立同结论；M-01/M-02＝D-100 评审实锤（8 个正式 Recipe 仅 2 个可进新主干）。
- 待验证：归桶判定矩阵已完成并经用户终审批准（2026-07-25，`docs/specs/bucket-disposition-matrix-2026-07-25.md`，223 行终裁——RETIRE 四批获准，1A 立即删 36 行可开退役票）；旧 IA 页面清单与下线批次已由矩阵 REBUILD 桶与 RETIRE 1D 批落定；god module 首拆点随首批功能票确定；画布解锁评估（触发＝已确认决策完成后视情况，接入形态与功能届时再判）。
- Supersedes：无——D-110 开发路线的深化；与 ADR-0019 纵切纪律配套执行。

## D-128 拆票前四项细化口径：v1 平台集合、注册通道、命名时点、示例产物导出

- 日期：2026-07-24
- 状态：`accepted`（四项用户逐条拍板：①②采纳建议；③同意挂 R 门前专项；④允许并补充试用管控口径）
- 决定：**v1 内容变体平台集合＝小红书/抖音/视频号三平台**（contentPackagePlatform）；**朋友圈（wechat_moments）＝交付目标（distributionTarget）**，走导出＋assisted 交付（商家拿图文去发），**不作为平台化变体引擎主体**——朋友圈是私域转发面，无平台规则引擎可言，做变体主体属过度工程；存量涉朋友圈/无平台产物的 Recipe 按此口径改造或退役。此为 M-01 修复（8 正式 Recipe 仅 2 过新 admission）的产品面依据；工程面依 D-112 platform 双字段合同执行。
- 决定：**注册通道现实校准（D-124 R 门①补充，非重开）**：美业商家几乎不用邮箱为现实前提；试点期＝邮箱注册（R 门①原样建成）＋兑换码＋运营代开账号（D-045 承接，零新开发，试点商家由运营线下带入）；**手机号短信登录挂 E 门评估**——依赖国内短信服务商与实名，与部署中国化同批触发点，不提前建。
- 决定：**产品对外名称挂「R 门前专项」**：配合 Landing 阶段一内容填充（D-125）一并定名；不阻塞拆票与开发；R 门收口前必须完成（R 门⑤前台去工程语言的前置输入，当前对外面为 mkfast 占位）。
- 决定：**示例任务产物允许导出**：判断口径与正式使用完全一致，不为示例任务建特殊导出限制或阉割链路（D-119 双轨同链、D-118 禁旁路同一精神）；**试用状态与试用额度＝后台人工动态开关/调整**（admin-config 承载，D-123「数字＝运营参数」口径的延伸）；滥用应对＝运营手动收紧，不预建防滥用开发。
- 影响：D-126 待验证「示例任务产物是否允许导出」结项；M-01 修复票获得产品面依据（三平台 admission＋朋友圈改交付目标）；R 门验收清单不因注册通道扩项；「命名专项」进 R 门前清单；ADR-0013（platform 双字段句）与 ADR-0016（R/E 门归属段）同步标注。
- 证据边界：M-01 现状＝D-100 评审实锤；「商家不用邮箱」＝行业常识判断，试点期由运营代开对冲，未实测；朋友圈导出物的转发顺手度未走查。
- 待验证：朋友圈 Recipe 改造后的交付体验走查（导出物是否够顺手转发）；手机号登录 E 门评估触发；命名专项启动（R 门前）；试用开关的后台操作面（admin-config 现有 CAS/审计是否够用）。
- Supersedes：无——D-112/D-124/D-125/D-126 的应用细化与结项。

## D-129 拆票前组件选型收口：MinerU 部署形态、默认 LLM 供应商与 MkFast/CF 优先原则

- 日期：2026-07-24
- 状态：`accepted`（同日三批拍板：①MinerU 官方 API＋免责口径；②DeepSeek 默认供应商＋官方文档镜像指令；③A/B 快速确认＝「ab 都优先以 MkFast 以及 Cloudflare 的功能为实现基础」）
- 背景：D-111～D-128 新功能批对照已锁栈决策（D-034～D-041、ADR-0013～0019）逐项扫描：无「换栈」成分——执行编排（DBOS）、LLM 调用（AI SDK 现行 API）、观测评估（Langfuse+promptfoo+Vitest）、运营参数（admin-config）、数据与前端（托管 PG+Drizzle／mkfast Vite/TanStack+Better Auth）、图视频模型供给（catalog 已注册 Seedream 4.5/5.0-pro＋Seedance 1.5-pro/2.0，方舟直连＋tuzi relay 双通道）全部闭合；真正开放的选型仅本条四项，一次收口。
- 决定：**MinerU 部署形态＝官方精准 API（验证期）**。数据出域提示＝**温柔文字免责提示：不设门槛、不阻断、不加授权勾选**（对齐 D-117/D-122「提示不拦路」精神；修正评审建议中的「授权提示」表述——用户裁决只要文字免责不要门槛）。自托管（开源需 GPU、异构运行时脚印）＝规模化迁移路径，**挂 E 门同批评估**（与中国化部署触发点天然重合，数据出域一并解决）。ADR-0017 既有「自部署为规模化迁移路径」口径就此转正式拍板。
- 决定：**文案生成与内部编排判断位 LLM 默认供应商＝DeepSeek**（用户原话「默认先选」）。覆盖位＝copy 输出编译器的生成调用＋五段 Harness 全部文本判断位（①意图正名/③Brief 编译/结构化判断信号/事实槽满足度/评估回炉等）；**需视觉输入的判断位除外**（按 catalog 多模态位配置，与 ADR-0017 纯视觉图四 slot 分类同理）。OpenAI 兼容接口直走 D-035 AI SDK 现行 API；JSON 输出/Function Calling 满足 ADR-0013 结构化判断信号合同。**默认型号拍板＝deepseek-v4-pro**（用户指定，两名并存故明确到型号；deepseek-v4-flash＝轻档备选）；型号＝supply-registry catalog 运营参数，后台可换（D-044 机制；拉取当日现役即此二者，1M 上下文、默认思考模式、OpenAI 与 Anthropic 双格式端点——以镜像 pricing 页为准，勿凭模型记忆写旧型号名）；现状 catalog 无 deepseek 条目（仅 BYOK 测试样例）、默认档为 fixture/llm-openai 占位——注册与替换为供给票内工作。
- 决定：**DeepSeek 官方 API 文档全量镜像入库**：`references/analysis/deepseek-api-docs-2026-07-24/`（源＝https://api-docs.deepseek.com/zh-cn/ 全站页面枚举拉取，INDEX.md 含逐页源 URL 对照与拉取记录）；实施票引用 DeepSeek 接入细节（参数/限流/错误码/缓存/思考模式）一律指向镜像文件，不凭模型记忆。
- 决定：**基础组件选型总原则＝优先以 MkFast 模板与 Cloudflare 既有能力为实现基础**：邮件服务商＝模板内置 Resend（mail+newsletter 双通道现成，R 门①沿用，中国送达率挂 E 门，试点期运营代开可绕——D-128）；对象存储验证期后端＝**Cloudflare R2**（runtime-truth objectStorage 抽象与 env mode 门已建成，只填后端配置，本地盘仅 dev fallback）。该原则同时约束后续同类小选型：模板或 CF 有现成能力的，不引第三方新件。
- 补充（同日，用户逐条裁决：1 按建议、2 同意、3 同意、4 悬置）：**图默认型号＝seedream-5-pro 系、视频默认型号＝seedance-2 系**（对齐 v4-pro 质量优先逻辑，档位数字随 D-123 运营核算可调），**供给通道默认＝火山方舟直连，tuzi relay＝容灾备选**（官方一手优先，同为 catalog 运营参数可切）；**exactText 精确文字校验器＝生成链既有多模态模型 VLM 校验**（零新件，对齐 ADR-0017「不为解析建独立 VLM 通道」精神，不引 PaddleOCR 等独立 OCR，误报率票内验收——D-115 待验证「方案选型」就此结项）；**Recipe/Skills 定义载体＝PG 既有库**（KEEP 桶，与 Recipe 现存储同库），提示词模板＝Langfuse prompt mgmt（D-037 既定口径落定，写进拆票 spec）；**后台动态表单渲染组件（D-107）＝悬置**——用户将单独给定组件及应用建议，落定前实施 Agent 不得自行引入表单框架（已解除：D-130＝HeroUI Pro V3 表单组件族组装）。
- 影响：拆票口径——实施 Agent 无组件选型临场发明空间；ADR-0017（凝结来源行＋解析引擎路由句＋运维行）与 ADR-0013（默认供应商段）同步标注；重申 D-115 模型原生文字渲染残余风险＝票内验收，**不预建 HTML→图片渲染兜底**（satori/puppeteer 等不得自行引入）。
- 证据边界：DeepSeek 判断位输出质量未在本项目 eval 集实测（D-036 评测台跑通后验）；MinerU 限流 50 文件/分钟、5000 文件/天为验证期够用判断，未压测；Resend 中国送达率未实测（试点期不依赖）。
- 待验证：DeepSeek 结构化判断信号在五段 Harness 的实测通过率（评测门）；MinerU 免责提示文案定稿（归 D-124 R 门⑤前台去工程语言一并出）；R2 env 配置进装配门票清单；exactText VLM 校验误报率（票内验收）；后台动态表单渲染组件（D-130 结项＝HeroUI Pro V3 组件族组装）。
- Supersedes：无——D-035/D-044/D-120/ADR-0017 的应用收口。

## D-130 前端组件供给基准：HeroUI Pro V3（用户专属授权）

- 日期：2026-07-24
- 状态：`accepted`（用户指定组件并给出应用口径「所有的前端组件可以优先使用 Hero UI Pro 的 V3 版本」，附专属授权仓；D-129 第 4 项悬置就此解除）
- 背景：D-129 遗漏扫描第 4 项（D-107 后台动态表单渲染组件）悬置待用户指定；用户给出专属授权仓 `Agile-Avocation/herouipro-v3` 并把口径放大到全前端组件优先供给源。实探：`@ag-ui/pro` 1.0.0-beta.7（HeroUI Pro React v3），基于 @heroui/react ≥3.2.2＋**Tailwind CSS v4**＋**React 19**＋React Aria Components（无障碍）＋Motion＋Recharts＋TipTap；60+ 生产组件（图表/DataGrid/看板/侧边栏/Sheet/**AI Chat 场景族**——chat-conversation/prompt-input/chain-of-thought/chat-attachment/chat-loader/markdown/code-block）；内置 Brutalism/Glass/Mouve 三主题；Compound Component API。**现栈版本完美对齐**：mkfast web＝React 19.2＋Tailwind 4.1.18＋Vite 7，零升级前置。
- 决定：**前端组件优先供给源＝HeroUI Pro V3**。适用面＝**REBUILD 换壳面与 NEW 净新建面**优先取用；D-114 对话流主容器优先评估 AI Chat 场景组件族；**D-107 后台动态表单渲染＝HeroUI 表单组件族组装**（不引 JSON-Schema 表单框架，D-129 悬置解除）。
- 决定：**KEEP 桶存量组件不为换库而专项迁移**——触碰时换（对齐 D-127「god module 触碰时拆」同款纪律），不开专项换库票、无双组件库统一化运动；存量 shadcn 系与 HeroUI 过渡期并存合法。
- 决定：**「门店橱窗」视觉系统（DESIGN.md）仍为唯一视觉权威**：HeroUI 组件一律经主题 token 适配接入（**主题拍板＝Glass**——用户指定，三内置主题中选定，Brutalism/Mouve 不启用；Glass 与磨砂玻璃语言同向），不得原样拖入破坏视觉一致性。
- 决定：**授权与镜像纪律**：授权＝用户专属（Pro 付费授权），本地镜像 `references/repos/herouipro-v3/`＝复制源与升级对齐点，**gitignore 不入库、不对外分发**（同 mkfast-app/vozeb 先例）；组件按需复制进 app 代码库属授权范围内产品使用；升级＝重拉镜像锁版本（当前 beta.7，历史版本按分支切换）。
- 补充（同日）：**Landing 页例外**——不受组件基准约束，保持现状形态，仅改相关文案与前后链接（属 D-125 阶段一诚实填充范围）；组件/视觉专项优化后置（D-125 阶段二触发）。其余页面优先使用 HeroUI Pro V3 的**组件与动效**（动效随组件库 Motion 体系，不另引动画库）。**模板起点指派**（用户同日补充）：`template/template-chat`＝工作区交互页（D-114 对话流主容器/定制创作工作区）起点参照；`template/template-dashboard`＝运营后台可视化（admin 管理面）起点参照；仓内另有 template-email/template-finances，未指派用途不擅自启用。
- 影响：ADR-0014 补组件供给基准段；DESIGN.md 补组件供给注记；D-125 补 Landing 例外行；D-129/D-107 悬置项结项——D-101～D-130 批次内再无悬空选型；.gitignore 增 herouipro-v3 行。
- 证据边界：实读 README＋package.json＋src 结构（81 个 tsx；60+ 组件为 README 口径）；beta.7 稳定性随上游 beta 节奏（锁版本对冲）；Pro 授权条款全文未细读（分发边界按「不入库不分发」保守执行）；Glass 主题与门店橱窗 token 实际贴合度未实测。
- 待验证：Glass 主题（已拍板）→门店橱窗 token 适配 spike（换壳首票内）；chat 组件族与 AGUI 三层卡片流（D-114）映射走查；DataGrid/表单族在 admin-config 后台（D-107）的组装验证；beta 升级节奏与锁版本策略。
- Supersedes：无——D-129 第 4 项悬置的解除与 D-127 REBUILD 桶的组件供给细化。

## D-131 拆票组织：前端相关票独立成组，由指定 Agent 与模型执行

- 日期：2026-07-24
- 状态：`accepted`（用户指令「拆票的时候，把所有前端相关的作为一组。我要用指定的 Agent 和模型来进行前端的开发」；指定 Agent 与模型名称待用户届时给定）
- 背景：D-130 确立 HeroUI Pro V3 组件供给基准后，用户决定前端开发由专属指定的 Agent 与模型承担；拆票组织需与 ADR-0019 纵切纪律兼容。先例＝07-18 四线 worktree 并行的 WT-4 frontend 独立 lane（成功合回 main）。
- 决定：**票包组织增加「前端组」（frontend lane）**：所有前端相关票（REBUILD 换壳面、HeroUI 组件接入、模板起点落地、前台交互面）统一归组，由用户指定的 Agent 与模型执行；非前端票不混入该组。
- 决定：**分组不破纵切（ADR-0019 不变）**：每票仍带旅程描述与旅程级验收命令；前后端同旅程拆分时，接缝合同与属主显式写明（三进三出/HTTP+SSE 合同为接缝语言），旅程收口验收落在前端票或专设联调票；**不允许「前端组内部自闭环、不接真后端」的假绿**。
- 决定：**前端组票面自包含**：每票附前端开发上下文包指引——DESIGN.md（门店橱窗 token 合同）＋ADR-0014（交互架构）＋D-130（组件基准、镜像路径 `references/repos/herouipro-v3/`、模板起点）＋对应接缝合同，使指定 Agent 无需读全决策日志即可执行且不越界。
- 补充（同日）：**指定登记＝前端组执行 Agent＝Claude Opus**（用户指令「指定前端 Agent 为 opus」；编排中以 model:'opus' 子 agent 承载，版本随现役 Opus 代际——当前 Opus 4.8）。
- 影响：拆票阶段产出结构＝前端组＋其余分组（随归桶矩阵定）；ADR-0019 补拆票组织标注；指定 Agent 已登记（Opus），票包说明直接引用。
- 证据边界：WT-4 frontend lane 先例实证可行；指定 Agent/模型能力未知（票面自包含即为对冲）。
- 待验证：前端组与其余组的接缝票排序（归桶矩阵后）；上下文包充分性（首票试运行校验，执行 Agent＝Opus 已登记）。
- Supersedes：无——ADR-0019 拆票组织的细化与 D-127 REBUILD 桶的执行安排。

## D-132 凭证与运营供给单门：清单先行、票面引用、fixture 恒绿、live 一次核销

- 日期：2026-07-25
- 状态：`accepted`（用户指令「把凭证项以及运营项的单独门槛归为一类，避免又出现之前重复去索取凭证、卡死在测试环节」＋「在完整开票前，先列出清晰的需要的凭证清单，我把清单里面需要的内容补充完整，然后再引入到对应的票当中」）
- 背景：历轮教训——根 `.env` 长期占位 key（set-via-docs）且零 HARNESS 行导致装配失守；持久层/供应商用例被 env 门控大面积 skip 造成本地绿≠真绿；开发过程中多票各自向用户索取凭证、互不知情重复索取，测试环节因等凭证卡死。D-110 装配门解决「接线」，本条解决「供给的组织方式」。
- 决定：**凭证项与运营项归并为单一供给门类**，不散落各票：外部凭证（模型/渠道/解析/邮件/存储等 API key 与账号开通）与运营供给项（套餐三桶与加油包数字、试用开关、兑换码规则、示例店素材、行业先验配置、对外名称等）统一登记入供给清单 `docs/ops/provisioning-manifest.md`。
- 决定：**清单先行**：完整开票前产出清单，用户一次性集中补齐（凭证真值只进本地 `.env`/密管，永不入库；清单只记键名、用途、状态、消费票）；补齐后各票票面按清单键**引用**清单项，任何票不得单独向用户发起凭证索取。
- 决定：**fixture 默认档恒绿**：每条外部集成必须有无凭证可跑的 fixture/种子值默认档，开发与 CI 默认档不因缺凭证阻塞或假红；真凭证到位后按既有 `live_verified` 门径（D-069/D-098 先例）一次核销，live 核销归供给门票不进各功能票 DoD。
- 决定：**运营数字不进开发依赖**：D-123 运营参数（价格/额度/试用）以种子样例值完成开发与验收，真值运营期后台手填。
- 影响：票包批次 0 增加「供给清单登记票」；票面模板增加「供给依赖（引用清单键）」项；spec（issue #194）Implementation/Testing Decisions 增补对应条款；`.env.example` 成为清单键名的权威词表（DeepSeek/MinerU 为净新增键，由对应票定义后回写清单）。
- 证据边界：清单初版由决策层枚举（D-129 组件选型为主源），完备性以「开票时逐票核对供给依赖是否都有清单键」verify；漏项发现时补清单不开新索取通道。
- Supersedes：无——D-110 装配门与 D-123「数字＝运营参数」的组织化延伸；废止历轮「各票自行向用户要 key」的默认做法。

## D-133 原生视频编辑面：接受暗面，验证期不建（补录）

- 日期：2026-07-26（拍板）／2026-07-27（补录入本日志，原始记录在 `.scratch/orca-run-2026-07-25/STATUS.json` 与 closeout 材料）
- 状态：`accepted`（用户拍板 OI-74 方向 3）
- 决定：新原生视频作品无编辑/重生成面，只保下载/交付；旧作品只读可见＋confirm 显式不可用（T23 r2 A 案即终态）；原生编辑面重落地无限期后置至需求验证；T38 §E 三条前置转为「若未来重落地」条件项；本 run 不再为此开票。
- 原因：T23 退役 FFmpeg 链一并扫掉了在线 D-105 原生视频读/编辑/重生成面（归桶矩阵 §1C 无一行授权），须在「补建」与「显式接受暗面」间拍板。
- Supersedes：无（D-105/D-127 的执行细化）。

### D-133 修订（2026-07-28）：收紧为「平台零视频编辑」，四条残留一并真退役

- 日期：2026-07-28
- 状态：`accepted`（用户拍板）
- **原则（本条为总纲，优先于下方逐项）**：**平台不提供任何视频编辑功能。所有编辑处理由上游 API 渠道供应商完成，我方只作为输入与输出的接收方。**
- 起因：07-28 主控核查发现 D-133 原措辞（「新原生视频作品无编辑/重生成面，只保下载/交付」）与实际代码不符。`mkfast-template-main/src/product/results/video/video-worksurface.tsx`（21KB）挂载于 `routes/dashboard/results_/$workId.tsx:73`，且 07-27 的后续票把镜头级能力建了回来而 D-133 措辞从未更新。实测四态：`full_compose` 整片重合成真退役（静态门四处禁复活＋DB `CHECK (scope = 'shot')`）；**`shot` 单镜重生成保留且计费**（`videoRegenScopes = ['shot']`／`videoBillableScopes = ['shot']`）；**独立字幕资产文字编辑保留且免费**（`subtitle_text_edit` 在 `videoWorksurfaceFreeActions`）；烧录字幕改动不可用。危害＝文档说「没有」而实际「有且扣商家额度」，读此条规划新功能者会做错决定。
- 决定：**上述四态全部收敛为真退役**。逐项处置按下表，判据＝「是否改变媒体字节或隐含我方持有拼接权」：

| 动作 | 性质 | 处置 |
|---|---|---|
| `poll`／`recover`／`download_supplier_task` | 查上游任务状态、重连、取产物 | **保留**——接收方行为 |
| `play_control` | 播放/暂停/拖动 | **保留**——观看，不改字节 |
| `adopt_candidate`／`select_shot_candidate`／`deterministic_sort` | 在已收到的产物中挑选 | **保留**——选择非编辑 |
| `subtitle_text_edit` | 改字幕文字 | **退役** |
| `cover_select` | 选封面，含 `frameTimeSeconds` 抽帧 | **退役**——抽帧＝视频处理 |
| **shot regen**（唯一计费动作） | 单镜重生成 | **退役** |
| `subtitle_toggle` | 字幕显示开关 | **退役**（2026-07-28 用户补充拍板，无待定） |

- 原因（shot regen 单列）：单镜重生成虽由上游算力完成，但「重生成这一个镜头」这一产品概念**隐含成片由镜头拼接而成、且拼接权在我方**——正是 FFmpeg 链的思路残留。`full_compose` 已退而 `shot` 保留在逻辑上不自洽：**无拼接能力即不应有局部替换入口**。要改则整条重新生成，我方不管局部。
- 影响（连带，须随退役票一并处理）：
  1. `videoBillableScopes` 退空——视频工作面将无任何计费动作，视频桶消耗点只剩初次生成，D-123 三桶口径需复核是否受影响。
  2. `apps/core/src/p1/model-supply/video-regeneration.ts` 与 `video-regeneration-postgres.ts` 整块退役候选；DB 约束 `CHECK (scope = 'shot')` 随表处置。
  3. 静态门 `video-full-compose-retirement.static.test.ts` 现断言 `videoRegenScopes = ['shot']` **必须存在**，退役后该门须**反转**（先红后绿），否则它会替旧形态背书。
  4. `VideoCoverSource` 的 `'frame'` 分支随 `cover_select` 退役；`'authorized_image'`（用已授权图片当封面）属素材选择非视频处理，去留随边界裁定。
  5. **与 D-138 合流**：抽帧退役后封面只能来自上游产出或已授权图片，因此交付 checklist 必须改口为「封面可在抖音发布页自选或自动生成」——即 D-138 原定却未落地的那句指引。两条决策在此处并为同一张票。
- 证据边界：以上均为 07-28 主控只读实测（源码 + 静态门断言），未改产品代码；`buildVideoFullDeliveryPackage`（`apps/core/src/p1/result-delivery/delivery-package.ts:475`）中 cover 与 subtitles 均为可选入参且生产调用点带收据校验，说明二者「有时有」，其产出条件未追到底。
- 待验证：①（已结）`subtitle_toggle` 用户补充拍板＝退役，无条件。②（已结）见下方「封面来源裁定」。③三桶计费口径是否因视频计费动作归零而需要调整。

#### 封面来源裁定（2026-07-28，用户拍板，结 ②）

- **事实（用户提供）**：上游 API 渠道供应商**没有单独导出封面的配置能力**。要拿到封面，必须由我方对成片做抽帧或截图处理。
- **裁定**：封面因此**归类为视频编辑**，同时也属发布相关内容，**一并冻结/退役**——`cover_select`、`VideoCoverSource` 的 `'frame'` 分支、`frameTimeSeconds` 全部退役。**产品明确不提供封面。**
- **实测补充（主控 07-28，本条是本轮最值得记的发现）**：抽帧代码**从来就不存在**。`selectCoverFrame`（`mkfast-template-main/src/product/results/video/video-worksurface-model.ts:637-644`）只把 `frameTimeSeconds` 写进状态，`posterUrl` 仍取 `composedCandidate.posterUrl`（上游产物或种子 fixture `/seed/video/video-poster-vertical.webp`）；全仓无 canvas／`toBlob`／任何图像产出路径；`frameTimeSeconds` 在 `apps/core` 与 `packages/contracts` **零命中**，即商家的封面选择从未离开浏览器。
- **形态定性**：这不是「做了但实现不了」，而是**「做了、看着能用、实际是空操作」**——商家点「选这一帧当封面」，UI 收下操作、给出反馈，却无任何产出、也不向后端传递。比前者更隐蔽，因为它连报错都不会有。此形态应作为**假绿清单的新类目**：不是测试为未完成功能背书，而是**UI 为不存在的能力背书**。
- **连带（交付面，不受 D-155 冻结约束——属诚实性修正）**：既然产品不提供封面，交付 checklist 的抖音段 `- [ ] 上传 video.mp4 与封面`（`apps/core/src/p1/result-delivery/delivery-package.ts:178`）在指使商家去找一个我方从不产出的东西，**必须改口**为「封面可在抖音发布页自选或自动生成」——即 D-138 原定却未落地的那句指引。同批修正 `delivery-package.ts:194` 的 `- [ ] 确认成片、封面、字幕属于同一 revision`：`revision` 是工程词汇且该文件在交付 ZIP 内商家可见，违反 D-116。`buildVideoFullDeliveryPackage` 的可选 `cover` 入参与其收据校验随之复核（既然无产出路径，该分支应确认为死码后处置）。

## D-155 发布相关功能开发整体停止，延后专题讨论

> **时序交叉引用（2026-08-19）**：D-155 现行处置以同日后置修订「归档移出主干」为准；下方「既有代码原地保留不删」仍保留为初始 accepted 文本的审计记录，不再是现行实施指令。

- 日期：2026-07-28
- 状态：`accepted`（用户拍板）
- 决定：**所有「发布」相关的功能开发全部停止，延后到后续专题讨论，不作为现阶段重点。** 冻结面下方明确划界；冻结期内不新增、不扩建、不为其接线，既有代码原地保留不删（避免删了又要建的往复）。
- 原因（用户原话要点）：各平台的发布功能目前仍处在**流程规划阶段**，并未真正落地开发；但前期的交叉建设已经产生了「一部分功能做了、却又实现不了」的中间态。继续在这个中间态上加东西，只会持续制造无法兑现的产品面。
- **冻结面（不做）**：
  - `distributionTarget` 的三个发布目标 `publish:xiaohongshu` / `publish:douyin` / `publish:video_account`，及 Composer 内与之配套的签名预览文案与渲染分支。
  - `apps/core/src/p1/integrations/` 下的平台代发链路：`douyin-oauth-lifecycle.ts`、`douyin-publish-polling.ts`、`douyin-observe-sync.ts`、`confirm_douyin_publish` 命令、`douyin_publish_jobs` 表。
  - 平台账号绑定 / OAuth / BYOK 凭据用于代发的部分。
  - 抖音封面与字幕的产品化（D-138 的「后议」由本条统一承接，不再单独跟踪）。
- **不在冻结面（继续做）**：
  - **交付与导出**——交付包、确定性 ZIP、平台核对清单、`assisted_handoff` / `manual_copy` / `export`。这些是「把成品交到商家手上、由商家自己发」，是当前形态的主链路，与代发无关。
  - **商家自报发布结果的记账**——`record_publish` / 结果 chips / 三级信号 / 周复盘。**这是承接闭环（内容→到店引流）的组成部分，不是代发能力**；冻结它等于同时冻结产品目标本身的度量。主控在此显式划出该边界，若与用户本意不符须回头修订本条。
- 影响：
  - `docs/specs/reshell-and-extend-dev-spec-2026-07-25.md` Out of Scope 已有「平台代发真实账号（`publish:*` 能力门）——触发点到达时另开批次」，本条与之一致并**扩大**：不仅不建代发，连规划期的接线与配套产品面也停。
  - D-138（抖音包封面/字幕维持后议＋顺手加指引）**并入本条**：其中「顺手加指引」一项因与 D-133 修订合流仍需执行（抽帧退役后封面来源改变，交付 checklist 必须改口），但**属交付面文案修正，不属发布功能开发**，不受本冻结约束。
  - 冻结期内若发现既有发布相关代码正在对商家做出无法兑现的承诺（如可见文案宣称代发），**按诚实性缺陷即修**，修的方向是撤回承诺而非补齐能力。
- 证据边界（2026-07-28 主控只读实测）：`composer-home.tsx:2401` 与 `composer-question-timeout.ts:34` 均注明 **v1 的 Composer 只签 `export` / `assisted_handoff`**，因此 `composer-signed-preview.ts:46-48` 的「生成后发布到小红书/抖音/视频号」三句当前**商家不可达**——承诺尚未真正上屏，但渲染分支（`composer-home.tsx:2405`）是活的。`p1/integrations/` 下代发基建（OAuth 生命周期／发布轮询／观测同步／`douyin_publish_jobs` 表）体量可观且已入库。
- 待验证：解冻触发点未定——专题讨论时一并确定（候选锚点：真实试点商家提出代发需求、或平台账号能力门具备真实验证条件）。届时须先做一次代发面存量盘点，判断哪些中间态可续用、哪些应先退役再重建。

### D-155 修订（2026-07-28 同日）：冻结面代码**归档移出主干**，不再「原地保留」

- 起因：本条原写「既有代码原地保留不删（避免删了又要建的往复）」。同日用户提出维护成本诉求后重新权衡——`apps/core/src/p1/integrations/` 实测 **10,338 行生产代码**（主控 07-28 计量，已排除 test/fixture），是当前唯一一块「整块无用且体量过万行」的代码。
- 决定：**冻结面代码整块归档移出主干**（归档到 `references/` 或独立长期分支，保留完整历史与可取回性），主干删除。解冻时从归档取回。
- 原因：冻结代码不是零成本——它仍要通过 typecheck、仍出现在每次 grep 与全仓扫描里、仍会让读代码的人以为代发能力存在（本轮反向扫描已实证这类误导的代价）。**维护一万行死代码的持续成本，高于将来从归档取回的一次性成本。**
- 边界：归档 ≠ 丢弃。取回路径必须在归档时写明并可执行；`douyin_publish_jobs` 等已入库表的处置随归档票单独判断（表与代码的生命周期不同）。
- 前置：动手前须先确认 `p1/integrations/` 内**没有非发布用途的活代码**（该目录含 `feishu-*`、`byok.ts`、`secret-store.ts`、`provider-credential-runtime.ts` 等，未必全属代发面）。**误删活代码的代价远高于晚删。**

## D-158 场景知识从代码转为渐进披露的数据（方向，**形态已由 D-159/D-161/D-162 裁定**）

- 日期：2026-07-28；**2026-07-29 更新状态**
- 状态：`accepted`——**方向不变，形态已定**
- **裁定结果（2026-07-29）**：
  - **待验证① Agent Skills 规格的独立可采用性 → 成立。** `agentskills/agentskills`，Apache-2.0，独立组织归属，约 36–44 家客户端实现（含 OpenAI Codex／Gemini CLI／Cursor／VS Code／GitHub Copilot／Letta／Spring AI）；**规范明文不强制 skill 存文件系统**，故落 PG 合规。形态由 **D-159④**（跟格式与机制、不跟分发模式）与 **D-161①**（治理走 sidecar、退三个自造枚举）确定。
  - **待验证② 与 Langfuse（D-149）如何分工、避免第三套知识载体 → 由 D-162① 裁定**（四层载体分工表 ＋ 一处实存冲突的处置）。**该冲突是实存的，非假想**：Skill 契约当前带有内联提示词字段，与 D-149 的 14 位点承载重叠。
- 背景：用户提出维护成本诉求——代码量大、后期难维护。主控计量后确认**编排层不是大头**：`p1/harness` 生产代码 15,539 行，而 web `src/product` 54,033 行、`p1/model-supply` 46,095 行、`p1/operations` 33,667 行。三类成因中只有一类与框架有关（见下）。
- 事实（07-28 主控计量，生产代码，已排除 test/fixture/vendor）：代码规模的三个成因——① **god module／拆分问题**（`model-supply/foundation-module.ts` 6,235 行、`model-supply/index.ts` 5,547 行、`composer/composer-home.tsx` 2,794 行，三文件 14.5k）**任何框架都不治**；② **不可压缩的领域复杂度**（provider 适配器、目录、计费、证据门）换框架只是换个地方写；③ **框架能吸收的管道**（session 状态机／流式／工具分发／上下文组装／compaction）——**我们目前几乎没有**，因为仍是固定五段式 pipeline 而非 agent loop。
- 由此得出的关键判断：**pi 的 SDK 能吸收本项目现有代码约等于零**，它省的是「将来要写的」而非「已经写的」。**不能指望换框架来减负。**
- 决定（方向）：把散在代码里的**场景知识转为数据**——场景配方、行业先验、合规红线、平台规则、口吻模板，现分散于 `note-plan-compiler.ts`(465)、`policy-gates.ts`(487)、`output-compiler.ts`(456) 与 Langfuse 提示词，**每加一个场景要改代码**；目标形态是**加场景＝加一个知识文件**，运营可做，代码不增长。
- 为何采「渐进披露」而非普通配置化：知识条目一多必然撑爆上下文。pi 的 Skill 机制（**只有 description 常驻上下文，完整内容按需读入**）正解此题，而这正是「更多场景／更多渠道／更多工具」必然撞上的墙。**采其形状不必引其实现**——前提是 Agent Skills 为可独立采用的规格，此点由调研 03 号裁定，未裁定前不锁形态。
- 影响：本条是**唯一同时满足「减代码」与「服务更多场景诉求」的方向**，其余两条成因中，god module 拆分**按用户裁决排到下一轮**（当前并行工作已满），领域复杂度不可压缩。
- 待验证：①Agent Skills 规格的独立可采用性（调研 03）；②知识转数据后，既有 promptfoo 红线门与 Langfuse 提示词承载（D-149）如何分工，避免出现第三套知识载体。

## D-134 流程裁决：T39/T40 退出 Orca 车道（补录）

- 日期：2026-07-26／2026-07-27 补录
- 状态：`accepted`
- 决定：收官两票 T39（R 门收口）/T40（E-01）退出 Orca 车道，由 Agent Team 队友在原 worktree 执行（exec-t39 等），协调者亲验合入。纯流程决策，不改产品合同。

## D-135 套餐容器合同：显式延后＋绑触发点（补录）

- 日期：2026-07-27
- 状态：`accepted`（用户拍板 OI-50 方向 2）
- 决定：D-121 内容工作区的套餐容器合同（schema/投影/加油包绑定）**不建**，显式延后；触发点＝首个真实多工作区或加油包购买需求出现，或定价页真要卖它那一刻；反向护栏测试原样保留做防腐层。
- 共同取向（D-135~D-138）：验证期不给想象功能修路，约束按生效时点排期，触发点写死以防遗忘。

## D-136 导航格局：四项 nav 合同不动，IA 收口等试点（补录）

- 日期：2026-07-27
- 状态：`accepted`（用户拍板 OI-54 方向 1 主＋方向 3 保底）
- 决定：一级导航四项合同（创作/内容/素材/门店）不动，试点期拿行为数据；若冷启动商家找不到素材/身份面，则首页加入口卡片（不碰被 spec 钉住的 nav）；整套 IA 收口等试点反馈，与 OI-63（D-137）同场。

## D-137 旧壳链：钦定只读历史岛（补录）

- 日期：2026-07-27
- 状态：`accepted`（用户拍板 OI-63 方向 1）
- 决定：`/dashboard/jobs_/$jobId` 与 canonical 历史页遗留岛钦定为「只读历史岛」——补历史定位文案、不再投资、不进删除批；正式去留并入 D-136 试点后 IA 收口一起裁；与 D-133「旧作品只读可见」承诺一致，不收回可见性。

## D-138 抖音包封面/字幕：维持后议＋顺手加指引（补录）

- 日期：2026-07-27
- 状态：`accepted`（用户拍板 OI-77 方向 1＋顺手做 2）
- 决定：维持后议（试点驱动）；交付 checklist 加「封面与字幕可在抖音发布页自选/自动生成」纯文案指引，随下一张碰 checklist 的票捎带不单开票；不动视频编译链。

## D-139 五类场景入口：正式改约为输出类型轴

- 日期：2026-07-27
- 状态：`accepted`（产品规划落地差距审计 12 项拍板之一；审计报告 `docs/reviews/product-plan-implementation-gap-review-2026-07-27.md`）
- 决定：创作入口分类**转正为输出类型轴**（文案/图文/视频 lens＋配方卡目录）；设计文档一部「首页三件事之五类任务入口＋Composer 场景上下文切换」交互合同废止。五类宣发任务（项目曝光/热点/IP/活动团购/物料）保留为需求分类语言与配方卡命名/分组维度，不再是前台结构轴。
- 原因：场景轴已被物理退役（Z1/#105）且无票认领；用户裁决零开发接受现状。
- 影响：一部 Product Shape 相关段落以本条为准；D-104 四类输出编译器口径不变。
- Supersedes：一部「首页只做三件事」第 3 条与「五类入口交互形态」段（该二段保留为历史文本）。

## D-140 五步录入 UI：属主裁定＝前端 lane 立即开票，与建档链投影同批

- 日期：2026-07-27
- 状态：`accepted`
- 决定：D-119 五步录入 UI 立即开票，属主＝前端 lane（D-131 票面自包含）；与建档链投影修复（商家确认事实→ContextBundle，审计 P0-1，先做数据流复核再接线）**同批收口**。范围含：上传 PUT 端点透出 web、五步 UI 消费 `asset_intake_experience`、provenance 前台渲染、字段级逐项确认、权利确认改软提示（对齐契约 `blocking:false`）。
- 原因：T24/T33 票面互指属主致执行真空，两票验收各自全绿掩盖。

## D-141 加油包购买面：显式延后绑 E 门＋死链即修

- 日期：2026-07-27
- 状态：`accepted`
- 决定：加油包前台购买面绑 E 门触发点（与真实 checkout 同批，D-124 收款集中口径不变）；额度不足「查看套餐」死链**立即**改指兑换码入口＋联系运营（一行级修复入接线批）。

## D-142 MarketingIdentity AI 向导：补建，排 P1

- 日期：2026-07-27
- 状态：`accepted`（2026-07-28 修订，见下）
- 决定：补建 D-117 主体（一句话背景＋可选参考文件→AI 扩展全字段→校对保存），identity schema 补 provenance/unconfirmed；排 P1 不挤占接线批。附带缺陷单独修（不属本决策选项）：6 个合规字段前端静默硬编码写入商家档案——改为显式展示确认或默认最保守值。

### 修订（2026-07-28，主控）：「扩展全字段」收缩为「扩展全部**表达类**字段」

原文的「AI 扩展全字段」与本决策自己的合规立场冲突，据此修订口径：

- **AI 可扩展**：展示名称、归属、品牌主张/真实职业、专业边界、表达样例、禁说事项、画面原则、栏目锚点等**表达类**字段。
- **AI 不得触碰，只能由商家回答**：`sourceRef`（授权证明）、`allowedPlatforms`、`allowedScenes`、`portraitAuthorization`、`voiceAuthorization` 五个**授权类**字段。这五项的 provenance 必须显式为 `user`，省略即拒（契约级硬不变量，非前端约束）。

理由：附带缺陷那条（6 个合规字段前端静默硬编码）已于 2026-07-28 修复并合入 main（`179e3d5c`，W12①）——把「前端替商家全开四平台五场景」改成了商家必答。若 AI 可以扩展这五个字段，等于换一条路把刚拆掉的东西装回去：商家仍然没做过授权声明，只是代答者从硬编码常量换成了模型。**授权范围是商家对外承担责任的边界，不是可以由推断填充的内容。**

实施记录：W12① = `179e3d5c`（已合 main）；W12② AI 向导 = #242 名下在办。

## D-143 商品数字种子基准：按 D-123 原文对齐

- 日期：2026-07-27
- 状态：`accepted`
- 决定：种子基准＝D-123 原文（视频定数初级 3/中级 6/高级 9 条月，档位名初级/中级/高级），登记 `docs/ops/provisioning-manifest.md`；三处不一致代码与公开定价页对齐 admin-config 单一真相（公开页同源取数入接线批，消除 Landing ¥399 vs pricing ¥499 矛盾）；真值运营期后台手填（D-123/D-132 口径不变）。

## D-144 leads CRM 台账：退役真删

- 日期：2026-07-27
- 状态：`accepted`
- 决定：`leads.tsx`＋详情页＋`update_lead` 命令链退役真删（D-127 真删默认）；若库中存在真实迁移期数据，先降只读再进下一删除批；结果账本归一到 ContentPackage `resultSignals` 三级信号（D-030「不做 CRM」边界重申）。

## D-145 任务卡时间桥：修把手＋主动拉回延后绑触发点

- 日期：2026-07-27
- 状态：`accepted`
- 决定：把手丢失缺陷（Composer 挂载不查在飞任务、关标签页丢 sessionStorage 把手）入接线批直接修——后端重放能力已完好；主动拉回通知显式延后，触发点＝连接桥（飞书/企微）建成或试点反馈证明需要；不建 web push/Service Worker（PRODUCT.md「警惕无载体的想象功能」）。

## D-146 HeroUI Pro V3：全量扩容替换批

- 日期：2026-07-27
- 状态：`accepted`
- 决定：按 D-130 全量兑现——`components.json` 按面扩容（AI Chat 族 chat-conversation/chain-of-thought/markdown/code-block、后台表单族 cell-*/data-grid/native-select 等、可视化族 kpi/图表件全进）；对话流容器与卡片族替换为库件；template-chat/template-dashboard 重对齐；Motion 体系进产品面；token 桥三破口修复（surface 半透明/玫瑰金表达/porcelain 双定义）＋桥测试补 HeroUI 侧断言。约束：对话流替换须 e2e 门全程在位；D-107「后台动态表单＝HeroUI 表单族组装」随批兑现。
- 原因：审计确认取用率 12/65（真用 8），AI Chat 五件仅 1 件，两模板名义起点，Motion 零使用；用户裁决全量兑现而非改约。

## D-147 Harness 智能层：接＋删同批，旁路先删

- 日期：2026-07-27
- 状态：`accepted`
- 决定：事实槽内核接入③段（bundle 分层消费，`layer:'current_fact'` 语义生效）与⑤段正则场景引擎（`marketing-scene-policy.ts`）退位**同批**成 P1 票（二者功能耦合，不可只删不接）；`p1/copy/stream` 旁路删除＋`StructuredCandidateScorer` 死注入清除**立即先行**（纯删零依赖，D-118 禁旁路口径）；`OUTPUT_COMPILER_CONTRACTS` 随批接线或删除。附带：③④段补确定性 fallback（D-122 流程恒前进）。

## D-148 durable 载体口径：ADR-0013 修订为分层＋④段事件化成票

- 日期：2026-07-27
- 状态：`accepted`
- 决定：ADR-0013「DBOS 唯一 durable 载体」修订为**分层口径**——DBOS＝五段编排控制流权威、pg-boss＝子任务执行层、outbox＝投递层，边界显式写入 ADR；④段 DBOS step 内自旋轮询（最长 150s/300s）改 `DBOS.recv` 事件化，单独成工程票；不做全迁 DBOS 的载体统一运动（验证期性价比裁决）。

## D-149 提示词承载：现在补齐 Langfuse

- 日期：2026-07-27
- 状态：`accepted`
- 决定：忠实 D-037 补齐——建提示词推送脚本、请求时钉扎版本（锁定拉取而非事后收据）、14 个 LLM 位点全量入 Langfuse prompt management、失败不静默（启动校验配置，缺失显式降级告警）；Langfuse outbox 补 dead-letter 状态与 attempt 上限同批。
- 原因：审计确认仅 2/14 位点入库且钉扎方向反；用户裁决按原合同补齐而非改约。

## D-150 完成语义锚定旅程：消费者证明关票门、接缝对称防线、假绿治理、支线旅程硬门（流程决策）

- 日期：2026-07-27
- 状态：`accepted`
- 背景：07-27 产品差距审计（`docs/reviews/product-plan-implementation-gap-review-2026-07-27.md`）确认「后端建满、前台不接」为主导失效模式（快捷修改 13 动作/五步录入/交付 manifest/失效引擎/拟人化失败文案/事实槽内核等全属此类），且为 07-13 UIUX 差距复核已诊断过的「前后端接线断层」的同构复发——症状当时修了、生产症状的机制没改。谱系上与闲鱼「测试循环陷阱」、装配失守同型：**完成的定义锚在「工件存在＋门通过」，而不是「用户旅程真实走通」**。
- 原因（五个结构性成因，逐条有审计证据）：
  1. 拆票组织物理上重新引入横切（旅程被切成后端票＋前端票两半），而 D-131 属主制度只防了「前端组自闭环」一个方向，未防「后端建满无人消费」的反方向（T24/T33 互指属主即典型）；联调票条款存在但执行中一张未开。
  2. DoD 完成语义＝合同＋测试，「消费者存在」从来不是关票条件；「组件已建未挂载＝未完成」的判定规则只存在于 review 阶段，不在放行阶段。
  3. Agent 并行开发经济学：读懂并消费队友的复杂合同，成本恒高于手写一个简单版关票；没有 agent 对「消费队友产出」有持续激励。
  4. 决策时序与代码时序错位（主容器 07-20 手写早于 D-130 拍板 4 天），「原地换壳」在已成形实现上打补丁放大了它。
  5. 测试假绿盖住断连（五例：e2e 名带 exports 不断言导出/fixture 注入生产产不出的值/promptfoo 扫硬编码/capabilities 字面谎报/token 桥测试删 remap 照绿），M-04 硬门只覆盖三腿主旅程，支线断连不会红。
- 决定（四条，即刻生效，约束 D-139~D-149 三批及以后全部开票）：
  1. **消费者证明关票门**：任何票新增后端能力（命令/查询/投影/内核函数/契约字段），关票必须给出生产调用点 `file:line` 作为验收证据；或显式指名承接它的下游票号、且该下游票验收含消费此项。审查规则「组件已建未挂载/管线已建无入口/投影已建无消费面＝一律未完成」**从 review 阶段前移为关票放行规则**。
  2. **接缝成对开票＋联调票必开**：前后端拆开交付的旅程，联调/旅程收口票为必开项非可选项；D-131 补对称防线——**后端组内部自闭环、交付无消费面＝未完成**（与既有「前端组不接真后端＝假绿」条款对称）。
  3. **假绿治理**：测试名与断言一致（名字承诺的行为必须有对应断言）；fixture 值必须生产可产出（禁止注入生产代码产不出的值让断言恒绿）；接缝合同测试两端各有消费断言（防 T31/T45 型「双真相」）。存量五例随对应批清理，不单开票。
  4. **支线旅程硬门扩面**：M-04 三腿之外，本轮交付的支线旅程（快捷修改一次/录入一件资产/记录一次结果信号等）入 e2e 硬门；此后每批新交付旅程必须有对应硬门腿，无硬门腿的旅程不得宣布收口。
- 影响：票面模板新增两个必填栏——「**消费者证明**（生产调用点或下游承接票号）」与「**接缝对端**（对端票号/联调票号，纯自包含票填 N/A）」；「前台接线批/HeroUI 扩容批/Harness 接删批」三批票面按此模板产出；评审/收口报告按四条逐项核销。
- 证据边界：本条为流程决策，不改产品合同；成效检验＝下一轮审计中「建了没接」类缺口数量与假绿例数应趋零。
- Supersedes：无——D-131 的对称补强、D-110「修复即建设」完成语义的细化；与 D-122「流程恒前进」不冲突（硬门作用于关票/收口，不作用于运行时用户流程）。

## D-157 反向复核进标准流程：下游证明关票门、双向必跑、对抗复核取反驳立场（流程决策）

- 日期：2026-07-28
- 状态：`accepted`（用户拍板）
- 背景：07-28 反向扫描（`docs/reviews/noop-ui-reverse-sweep-2026-07-28.md`）在 `main@82af2d1a` 上查出 **15 条**「UI 为不存在的能力背书」，**07-27 差距审计一条都没记录**。原因是结构性的：那份审计的视角是「产品承诺 → 用户实际可得」，**从承诺侧往下读，天然看不见「前台建了、下游没有」**——界面在那儿、点得动、有反馈，读起来就是已落地。
- 谱系：本条是同一条对称线的第三步。**D-131** 防了「前端组内部自闭环、不接真后端」；**D-150** 补了反方向「后端建满、无人消费」；**本条**补最后一边——「**前台动作、无下游**」。三者合起来才封闭接缝的两端两向。
- 决定（四条，即刻生效，约束此后全部开票与全部 review）：
  1. **下游证明关票门**（D-150 决定 1 的镜像）：任何票新增或改动**前台用户动作**（按钮／开关／输入提交／快捷键条目／自动触发的交互），关票必须给出该动作的下游 `file:line` 三段——①发出的 core 命令 ②core 侧处理分支 ③写入或产出；或显式说明它是**纯本地状态**（展示态切换、草稿暂存）并说明界面为何不会让商家误以为有更多发生。**无下游且界面暗示有下游＝未完成。** 票面模板据此新增第三个必填栏「**下游证明**」，与 D-150 的「消费者证明」「接缝对端」并列。
  2. **review 双向必跑**：批次收口 review 不得只跑正向（承诺→实现）。**反向（前台动作→下游）为必跑项**，判据三问——有没有发出命令／core 有没有处理分支／有没有写入或产出，任一环断掉即为发现。正向漏掉的与反向漏掉的是**互不重叠的两类缺陷**，只跑一向即结构性盲区。
  3. **对抗复核取反驳立场**：发现项须由独立复核方逐条核，且**复核方的默认假设是 finder 错了**，其任务是尽力找出 finder 没找到的下游。必须在复核提示词中列明五条「看着断了其实没断」的路径：①命令名与前台标识符不同名 ②动作先攒进 draft 最后批量提交（**延迟提交不是空操作**）③走通用信封／泛化命令 ④经 BFF 路由中转 ⑤通过 SSE／轮询回流生效。**宁可漏报不可误报**——一条错误的「这是空操作」会导致删除正在工作的代码。
  4. **假绿清单新增类目**：既有类目是「测试为未完成功能背书」；新增「**UI 为不存在的能力背书**」。后者更隐蔽——它不需要任何人被骗，测试与代码都可以是诚实的，断链只在「这个动作有没有下游」这一问上。
- 原因（决定 3 的实证）：本次 15 条发现的复核结果是 **12 CONFIRMED／3 DOWNGRADED／0 REFUTED**。价值恰恰在那 3 条 DOWNGRADED——它们阻止了三次错误动手：①五步录入「确认，全部记下」下游其实完整接通（`finalize_store_intake` 真写 fact + profile），真实缺陷是`部分确认`子状态缺一道门；②人设 `allowedPlatforms/allowedScenes` 并非无人读取，两个数组整包序列化进生成模型提示词（`production-context-port.ts:686-717` → `production-stage-ports.ts:390-395` → `structured-nodes.ts:486-505`），真实缺陷是**有软消费者无硬门**；③图片操作三选一是 T08 签名字段、进报价身份摘要与 core 准入指纹（`composer-live.ts:167-173`、`server-quote-authority.ts:182-184`），按 finder 口径删字段会打断真实报价与冻结绑定链。**若复核方取「协助确认」立场而非「反驳」立场，这三条会被照单执行。**
- 影响：票面模板三栏（消费者证明／接缝对端／下游证明）；批次收口 review 报告须含反向一节，无反向节不得宣布收口；`.scratch/LIVE-RUNBOOK.md` 派工纪律同步。反向扫描为**只读审计**，不占并发额度——不起服务、不用 e2e 端口、不跑 `locale:compile`、不碰 PG，与开发 lane 并行无冲突。
- 证据边界：本条为流程决策，不改产品合同；成效检验＝下一轮反向扫描中新增的「UI 无下游」条数应趋零，且 DOWNGRADED 比例可作为 finder 口径质量的观测量。
- Supersedes：无——D-150 的对称补强，D-131 的第三边。

## 附录：已确认参考项目索引（实施 Agent 参考入口）

> 各决策条目引用参考项目时以此表为材料权威落点；实施时先读对应调研文档，再按「借什么/不借什么」边界执行。表内路径均相对项目根。

| 参考项目 | 角色与借用边界 | 材料落点 | 关联决策 |
|---|---|---|---|
| **讯飞绘文（turbodesk.xfyun.cn）** | 图文生成第一对标：管线七阶段/确认卡超时放行/跨模型双版/人设向导/拟人化交付语言可借；无 durable/事后合规/styleId 无 revision/失败扣点为反面教材 | 本会话走查 `references/analysis/huiwen-tuwen-walkthrough-2026-07-24/`（REPORT.md 十一节+persona-deep-dive.md）；Codex 深度走查 `references/analysis/turbodesk-article-creation-flow-2026-07-24/`（README+01 端到端+02 API/SSE 合同+03 复刻蓝图+04 成品合规审计+05 人设专文+observed-contracts.json+persona-observed-contracts.json+截图） | D-116、D-117（另供 Track C 定价参照：02 号文档 §14 积分目录） |
| **AgentKit（字节 ad_video_gen 双版）** | 视频线组合模式参照（借组合不引 runtime）：轻量版=最小链路基准、seq 版=生产形态参照；抽卡评估为其运营选择不照搬 | GitHub `https://github.com/bytedance/agentkit-samples/tree/main/python/02-use-cases/ad_video_gen`；火山控制台模板 `tpl-yebi04ewhsntl8dni7oe`（opencli 实读，无本地镜像）；截图 scratchpad `agentkit-seq-arch.png`/`agentkit-seq-case.png`（会话产物） | D-113、D-104/D-105 |
| **Kickart** | 定制视频创作控制节点参照（管控节点可借：来源进入/故事板/参考意图编排/质量检查/局部修订/交付回执）；不做订阅套壳 | `references/benchmark/ai-native-journey-study-2026-07-08/`（kickart-findings.md+03-合成-架构与产品化取舍-KickArt双路评估.md+sources/ 官方文档快照） | D-104、D-113 |
| **mkfast-app** | 支付/兑换码/流水接缝对照 | `references/repos/mkfast-app`（本地镜像，已 gitignore） | D-045 |
| **小云雀（xyq.jianying.com）** | 「门店橱窗」视觉范式参照（氛围层+磨砂玻璃+Composer 主轴） | `references/benchmark/interaction-study-2026-07-07/`（interaction-patterns-xyq-creatok.md+sources/xyq-2026-07-17-live.md）；权威落点 root `DESIGN.md` | 视觉系统定稿（2026-07-16） |
| **CreatOK** | 登录态实探佐证（全栈单体+Skills 形态）；Skills 一等参照 | `references/repos/creatok-skills`＋`references/benchmark/interaction-study-2026-07-07/` | D-108、框架拍板 B |
| **vozeb** | 上游运行时 parity 对标（Pro Studio 收编基线） | `references/repos/vozeb`＋`references/analysis/vozeb-borrowing-report-2026-07-15.md`（含 xcheck） | Pro Studio 收编（挂 D-103 后） |
| **Harness 生态研究（17 仓）** | DBOS/AI SDK/Langfuse/promptfoo/Mastra/Dify 等选型证据 | `references/repos/harness-2026-07-17`＋`references/analysis/harness-research-2026-07-17/`（10 报告+xcheck） | D-034～D-038、D-041 |
| **MinerU（mineru.net）** | 解析段可插拔 Provider：多页 PDF/office/复杂表格/HTML 网页→Markdown+JSON；格式归一器非语义理解器；当前免费、Token 90 天轮换、50 文件/分钟；自部署（开源需 GPU）为规模化迁移路径 | `references/analysis/mineru-api-notes-2026-07-24.md`（API 全参数/限流/登录态实核/路由表/复用环节）；官方文档 `https://mineru.net/apiManage/docs`；开源仓 `https://github.com/opendatalab/MinerU` | D-120、D-119 |

## 当前待验证

（范围说明，2026-07-20：本章仅为 2026-07-17 合并评审识别的 20 条遗留待验证集及其状态投影；D-047 起各决策的待验证项以其条内 inline 记录为权威，不重复收录于此。）

1. 美甲、美睫、美发、生活美容/皮肤管理各自的高频宣发任务、流量机会、最小素材槽、常用表达身份、平台成品形态和 CTA。
2. 真实任务由谁发起、用什么设备、素材在哪里、成品如何发布或导出；只用于定位摩擦和权限边界，不预设岗位流水线。
3. 主页、价目表、团购链接或截图、历史帖子和相册中，哪种冷启动输入最省力且事实准确。
4. 平台热点、同城话题、节日/季节和门店空档等流量或经营信号的可得性、时效、可解释性与错误借势风险。
5. 用户能否正确理解“仅本次、当前场景、当前 IP、当前平台、门店默认”的差异，系统推荐的最窄作用域是否符合预期。
6. “3 个独立任务出现同语义修正”是否是合适的记忆提议阈值；误提议、错学和撤回率是否可接受。
7. 哪些内容结构和素材组合值得资产化，资产预览是否能阻止临时价格、顾客信息和过期活动被带入未来任务。
8. 个性化是否真正提高“像本店”、直接采用或小改率，并减少第二次同场景制作的操作数。
9. 发布授权失效规则是否能做到外部误发为零，同时不制造重复确认负担。
10. 日常项目/服务曝光、热点借势、品牌/个人 IP 运营和宣传物料制作的真实频次、时间压力、可复用程度和成品质量门槛。
11. 顾客案例、评价、前后对比等受限素材在真实宣发任务中的占比，以及必要时授权粒度是否需细化到单张素材、匿名化派生版本或平台用途。
12. 现有 `creation-assistant` 接受/编辑动作从 local-only 闭环到服务端版本后，是否减少重复操作且不破坏用户已手改内容。
13. （2026-07-17 深夜改写：Langfuse 已由 D-036 转正，移出本条）LangMem/Mem0 三池记忆选型属二阶段议题（Harness 调研问题 C，遵守 D-016 不引入第二套记忆运行时）；assistant-ui 隔离 spike 降级为可选项，由薄壳 Composer 回放结果决定是否触发。
14. 咨询、加微、预约、团购券、核销和到店信号中，哪些可以合法、低负担地回流，并用于评估宣发任务而不伪造精确归因。
15. 品牌 IP、老板/主理人 IP、技师/专业人 IP 在不同平台与宣发任务中的表达边界、账号权属、稳定识别特征和离职处置。
16. DBOS PoC 六题（D-034）：五段骨架/崩溃恢复/挂起 48h 审批/进度 SSE/连接池共存/部署链约束；任一硬伤转 Inngest 评估（前置 SSPL 法务口径）。（2026-07-18 已完成：七面全过，DBOS 锁定 → D-041，证据 `.scratch/dbos-poc-2026-07-18/REPORT.md`。）
17. ①③节点在目标中国模型上的基线首过校验率（50-100 条真实输入 eval 集），据此标定 BAML 迁移阈值（D-035）；BAML 许可冲突上游状态。（eval 集采集口径属运营期议题，2026-07-17 拍板暂闭：开发/产品化研究阶段不展开，WOZ 运营启动时再定。）
18. Langfuse trace 与自建 PG 审计表的双写一致性方案；火山/豆包经 openai 兼容覆盖接 promptfoo 的真机验证（D-036）。（时点已定 2026-07-17：双写方案并入 DBOS PoC 六题同批设计——审计写入点与幂等键依赖 step 边界与重放语义，先纸面设计必返工，PoC 顺带跑双写等于免费第七验证面；权威=审计表，先写审计表（与业务同 PG 可同事务）后发 Langfuse，审计写失败=任务硬停、Langfuse 写失败=降级容忍+补偿重发，task_id/workflow_id/span_id 三方对齐。promptfoo×火山为半天级独立 spike，PoC 批次期间并行随做，不单独立项。）（2026-07-18 双写半边已随 T04 PoC 实测通过：审计先行同事务/硬停/降级补偿/重放幂等全部验证 → D-041；promptfoo×火山 spike 仍开放。）
19. admin-config 扩展的 Harness artifact schema 设计与 candidate→eval→approve→publish 门的实现形态（D-037）。（2026-07-24 用户已明确提出“非技术运营可局部调优和受控重新编排，只有架构变化才需开发”，触发原挂账并确认方案 B → D-101：固定五阶段、阶段内受控积木、Recipe compiler 和测试发布面正式进入产品合同。问题由“是否建设”改为“首批 StageType/config Schema、编译合同、原子发布与运营可用性如何实现”，继续禁止用缺少真实字段证据的通用 DSL 先行。）
20. 触发式挂账：Mastra 四项 spike gate（运营编辑体验超出自有受控 Recipe Studio 时）、Dify 子流引擎双前置（受控编辑器仍无法表达真实复杂分支且画布成为硬需求时）、AI Gateway 火山真机 spike（可选）（D-036/D-037/D-101）。D-101 已满足当前运营可视化需求，不因本轮确认自动引入上述第二运行时或网关。

## D-151 W01 建档链接线四裁决（介入点②拍板）
- 日期：2026-07-27
- 状态：accepted
- 决定：①接线形态＝**一次确认事件、两个投影**——三个录入口（流内补录卡/门店页/W02 五步向导）统一汇入批次级 `finalize_store_intake` 薄协调命令，内部复用 `AssetIntakeService.confirmFact` 写 StoreFact 账本（内容事实唯一权威）＋patch-merge 更新 `ProductState.store`（身份/兼容投影），不新增第三个 ledger writer；②投影范围＝**项目/价格＋可引用档案**（service/price/group_buy/discount/fulfillment＋明确 key 的店名/地域档案入账本；账号绑定、brandVoice、合规策略留 ProductState）；③历史迁移＝**生成待确认候选**（稳定档案按真实审计事件以 source=import 入账；历史价格/团购生成待确认 candidate，商家下次流内/门店页操作时一次确认，无证据数字不静默升格为当前事实）；④门店页旧手工表单＝**W01 立即删**（用户裁定，未采纳方案推荐的「W02 硬门绿后删」——接受门店页轨短真空，流内补录轨收编后保留为过渡入口，W02 五步向导落地后补齐门店页入口）。
- 原因：P0-1 复核实证（main@6f155a83）：`confirm_store` 两轨仍只写 `ProductState.store`（product-service.ts:1966 整对象替换），T43 只打通 StoreFact 入账后的后半链；项目/价格双真相与 ProgressiveFactCard 空 draft 整体替换抹数据风险须结构性消除。
- 影响：S1-W01 按接线方案实施（`.scratch/tickets-gap-closure-2026-07-27/w01-wiring-plan-2026-07-27.md`，7 条验收硬门含防抹数据门与静态守卫）；W02 第⑤步挂批次级 finalize_store_intake（禁止浏览器直调 store_fact_append/循环写 facts/另建五步专用 API）；ProgressiveFactCard 改当前 profile 预填＋server patch-merge＋按 ID upsert＋显式 clear 才删；`store_fact_append` 收窄为内核纠正/撤销入口。
- 证据边界：方案由主控代跑 codex exec（read-only）产出，全部断点 file:line 实证；#235 留痕。
- 待验证：批次 finalize 幂等/OCC 语义（同 idempotency key 重放不增 revision、并发旧 revision 409）由 W01 验收硬门⑤核销。

## D-152 对外统一品牌名＝丽客美页 LIKEPAGE，产品端后续跟改；对外物料不写价格
- 日期：2026-07-28
- 状态：accepted
- 决定：①**对外一律以品牌名「丽客美页 LIKEPAGE」为准**——Landing、论坛/线下分享物料、对同行与商家的一切自我介绍统一此名，解除 `landing-copy-2026-07-21.md` 第 28 行挂了一周的「⚠️ 待哥拍：产品端是否跟改」；②**产品端 UI 现名「美业内容簿」（`built_with_brand`）后续跟改为品牌名**，不再维持「Landing=对外品牌、产品内=功能名」的两名并存解释，改名动作本身待开票（归 D-128「产品命名挂 R 门前专项」）；③**对外物料一律不落价格数字**——Growth ¥399/月 仍是试点期意向价且线上支付未开放，纸质/可转发物料写价即成裸承诺，价格问题现场口头答，物料只写「试点期额度由兑换码开通」。
- 原因：论坛同行分享要产出一页式简介，两名并存会让加微信进来的同行看到另一个名字产生落差；价格写进可转发物料后无法附带「支付未开放/意向价」的澄清语境，重蹈 landing 2.10 角标「靠脚注承担澄清责任」的结构。
- 影响：`docs/design/one-pager-forum-2026-07-28.md`（论坛一页式简介，邀请制 CTA、无价格）按此口径产出并生效；`landing-copy-2026-07-21.md` §0 待拍项关闭；产品端 `built_with_brand` 及其余内部露出名的替换待开票，改名票落地前产品内两名并存属**已知过渡态**而非新决策空间；Landing §2.10 的价格脚注约束不变，一页纸不写价格不构成对 Landing 定价区的修改。
- 证据边界：一页纸的能力口径按仓内运行时真实文案取值，非按 Landing 早期定稿——「发布尚未完成真实验证，请导出后在手机端发布，再在此记录结果」与「当前以导出、复制和辅助交接交付」均实证自 `mkfast-template-main/project.inlang/messages/zh.json`；不承诺播放量/涨粉/到店量沿 Confirmed Premises 第 8 条；试点期兑换码口径沿 D-128。
- 待验证：改名票的替换范围（`built_with_brand` 之外的产品内露出、邮件模板、导出物料水印、后台标题）需一次性扫描盘点，未盘点前不宣称改名完成。

## D-153 hold 卡到期＝取消任务并退款（推翻 S6「hold 无界」，reservation sweeper 随之退役）

- 日期：2026-07-28
- 状态：accepted（用户拍板）
- 决定：**等商家确认的 hold 卡到期后取消任务本身**——48 小时（`harness.confirmation_card.hold_timeout_seconds`，后台可调，下限 1h／上限 30 天）无回应即走 durable `submitCoreHoldExpired` 落库 → 退回额度 → 白话告知「超时未选择，本次任务已取消，额度已退回」。**推翻 S6 轮 4「hold 卡诚实等商家、hold 无界」的裁决**，该旋钮从「S6 摘除的失效旋钮」恢复为真接通旋钮（含 `/admin/plans` 高级配置入口与生产控制器断言）。
- 原因：两条 lane 并行实施时暴露出票面自身矛盾——`#240` H05 写「48h 决策持有期入 admin-config」（前提是 hold 有界），同票 sweeper 项写「**无界** hold 的预留额度回收器」（前提是 hold 无界），两者记账于不同时间点。主控倾向保留 S6 的无界语义（把「等待商家」与「占用额度」解耦），**用户拍板取相反一侧**：任务不应永久悬挂，账面与运营看板的干净优先。
- 影响：
  - **reservation sweeper 整块退役**——它的存在前提（无界 hold 的额度被永久占用）随本决策消失，C1 的超时链路自带退款。`#240` 名下「reservation sweeper」项据此关闭，不再是欠账。
  - `codex/c2-sweeper-repair` 分支（已过一轮 Opus review 并修净全部 P0/P1）**不整体合入**，只提取其中与 hold 语义无关的 **D-035 repair 指标真实测量**（见 `codex/c2-repair-only`）。
  - 已知代价并接受：商家周五挂起、周日晚即到期，周一回店时任务已取消需重新发起。审查者曾据美业作息建议把释放期限放到 7 天（sweeper 语义下），本决策不采纳该时长，48h 默认按票面原文保留、由后台调节承接。
- 证据边界：C1 的超时链路已合入 main（`1a2c8ec6`），带真实业务库+DBOS 库的 persistence +180d 门 2356 pass／0 fail，独立 DBOS smoke 11/11；「旋钮真生效」由 config 桩给 1 秒的构造性用例证明（未生效则落回 48h 默认而挂死）。
- 待验证：sweeper 退役后，是否仍存在**非 hold 路径**的孤儿预留（如 successor 创建失败、取消链路中断）。`codex/c2-repair-only` 的属主需就「resume reconciler fence 在 C1 语义下是否仍有独立价值」给出判断与证据，未判定前不宣称额度账面已无悬挂。

## D-154 发布链拆两半：护栏随功能建，流程与文档绑触发点置后

- 日期：2026-07-28
- 状态：accepted（用户拍板，方案 C）
- 决定：**「发布链」不是一件事，按 D-040 边界澄清切成两半分别排期**。
  - **随功能建（不置后）**＝判断新功能有没有做对的尺子：promptfoo 正/负控核销、**H02 事实满足度判断位 golden 集（8-12 例）**、p1-f2 两类基线红、硬门⑥负门、17 spec 首屏复验。这批进护栏批（Wave7），排在任何新功能开发之前。
  - **绑触发点置后**＝流程与文档执行：Core/DBOS 宿主部署工作流（E 门欠条 `E-H06-D038-CORE-DEPLOY`）、E 门台账机制、发布 SOP 排空演练、E 门业务三项（支付真实通道／删除匿名化／取消退款补单，触发点＝真实收款，D-124 口径不变）。
- 决定：**产品讨论与开发并行推进，不排先后**——讨论争的是用户与主控的注意力，开发争的是 lane，两者不争同一种资源；且新功能形态会反过来决定哪些发布链构件值得现在建（如 golden 集的例子集需按新输出类型设计），先建后改即返工。
- 原因：D-040 已拍板「置后的是流程与文档执行，不是结构性护栏——七项硬失败门、条件权利门、provenance、审计双写、promptfoo 红线 CI 门、能力门三态属功能正确性组成部分，随功能同批建」，本条只是把该边界应用到发布链的逐项归类，不新增口径。反面代价已评估：整块先做发布链，Core 部署链会随 Harness 迭代重建、E 门业务三项触发点未到，正是 D-040 所指「随功能迭代反复作废」。
- 影响：`#242` 的「D-038⑤ 发布 SOP＋CI 检查项＋部署 workflow 挂 E 门登记」与「E 门台账同步」两项**转为触发点欠条**，不在当前批交付；其余四项进 Wave7。当前批剩余量由 20 件重新分类，不改总数。
- 证据边界（2026-07-28 主控实测）：机械质量门八 job 已建成且含红线正负控（`core-quality.yml` 的 `redline-evals` 含「Prove promptfoo assertions reject false output」步）；E-01 发布工程门主体已建成（`deploy.yml`：同 SHA release manifest→真实 CF 资源→**PG 迁移先于 Workers 代码**→deploy）；**但 `deploy.yml` 只部署 Web Worker**，`docs/ops/harness-release-sop.md` 首行自陈 Core/DBOS 宿主部署登记为 E 门未实施；`E-H06-D038-CORE-DEPLOY` 全仓仅出现 1 次（就在该 SOP 内），即**E 门台账实际不存在**，所谓「台账同步」真相是台账尚未建立。
- 待验证：护栏批四件完成后，新功能批是否还需要补充别的「尺子」——按新功能实际触碰的判断位再评估，不预先扩围。

## D-156 公开页价格正名：展示数字＝文案资产，计费价＝治理资产（不设豁免期）

- 日期：2026-07-28
- 状态：`accepted`（用户拍板）
- 决定：把当前混为一体的「公开页价格」**按类型拆成两件东西**，分别适用不同规则：
  - **展示数字**（Landing 与 `/pricing` 上给内部人员与内部测试客户看的示意价）＝**文案资产**。与 landing copy 同源，改它等同改标题，**零治理期待、零连锁反应**。
  - **计费价**（真实会扣款的那个）＝**治理资产**。**当前不存在**（D-124 试点期支付零开发）。E 门开真实收款时**新建**：进 admin-config，与 `plan.allowances.*` 同一套治理链（draft→publish→CAS→审计→回滚），并把 `server-catalog-validation.ts` 的三方对账从一次性加油包**扩到订阅**。
- 决定：**不设豁免期。** 豁免需要一个「将来记得解除」的动作，而 D-138 刚刚实证同构机制失效——「随下一张碰 checklist 的票捎带」的那张票从未出现，指引至今未落地。价格豁免忘了解除的代价远高于一句文案：真实收款时显示价与扣款价不一致即商业纠纷。**新建靠票面，解除豁免靠记忆**，本条选前者。
- 决定：**不现在打通全部治理链。** 计费价尚不存在，其形态（订阅周期／试用转正／按量叠加／加油包组合）未定，此时建的链条到时多半要重建；且会给纯文案调整上治理税，与 D-040「开发期不为想象功能修路」一致。
- 原因（起因与证据，2026-07-28 主控实测）：commit `918007da`「feat: refine landing page experience」把 `VITE_GROWTH_MONTHLY_AMOUNT_CENTS` 由 `39900` 改为 `199900`（¥399→¥1999，5 倍），**而同一 commit 记录的 D-152 仍写「Growth ¥399/月 仍是试点期意向价」**——决策记录与代码在同一次提交里朝相反方向走。两条佐证表明这**不是一次定价决策**：
  1. **只动月价，年价与终身价未动**——年价 `399000`＝¥3990、终身 `699000`＝¥6990 保持原值。¥399/月 时年付＝10 个月（标准 SaaS 年折）、终身＝17.5 个月，阶梯自洽；改成 ¥1999/月 后年付＝1.99 个月（年付比月付便宜 83%）、终身＝3.5 个月，**商业上不成立**。
  2. **注释错误归属**——`env/client.ts:18` 与 `.env.example` 改成「D-123 中级 ¥1999/月」，而 D-123 原文写的是 ¥399。代码现在引用 D-123 作为 ¥1999 的权威，D-123 并无此数。
  
  根因是**机制而非纪律**：档位月价存在构建期 `import.meta.env`（`env/client.ts:58`），运营在 `/admin/plans` 改不到，跑着的服务也改不了，因此「调整 landing 展示价」**只能**以改代码＋重新部署发生。当时无第二条路可走。
- 影响（现在执行的四件）：
  1. `VITE_GROWTH_*_AMOUNT_CENTS` 迁出构建期 env，落到 landing 文案同源处；D-143「两页同源」保持不变，只是源头换位置。
  2. `docs/ops/provisioning-manifest.md` C-1b 改口径：由「档位月价」改为「**计费价——E 门时供给**」，并注明 Landing 展示数字属文案资产及其源头位置。
  3. 修掉错误归属：`env/client.ts:18` 与 `.env.example` 中「D-123 中级 ¥1999/月」两句必须撤下，否则下一个读者会以为这是拍过板的。
  4. **新增价格阶梯算术断言**（纯防呆，与治理无关）：不论展示数字取何值，年付不得贵于月付、终身不得便宜于年付。本次错误**任何一条此类断言都能挡住**。
  5. **当前展示值恢复为 ¥399**——理由不是治理文档写它，而是只有 ¥399 能让未被改动的年价与终身价重新自洽。正名后该值属文案，改动成本一行；若日后取 ¥1999 或其他值，须**同批重定年价与终身价**，那才构成一次真正的定价决策。
- 证据边界：以上均为只读实测（`git show 918007da`、`env/client.ts:18-35`、`.env.example:58`、`plan-catalog.ts:1-14`、`policy-revision.ts`、`server-catalog-validation.ts:22-52`）。加油包一次性购买**已有**真实三方对账（provider 侧 `unit_amount` 与 canonical add-on 不符即抛）；两个断言均硬性要求 `type === 'one_time'` 且 `recurring === null`，故**订阅路径走不到该校验**——显示价与扣款价当前零对账，未爆仅因线上支付未开放。
- 待验证：展示数字的具体取值属产品沟通判断（对内部测试客户的预期锚定），正名解决治理负担、不解决锚定选择；E 门新建计费价时须一并确定与 D-123 成本核算（价格↔额度↔毛利三元组）的绑定形式——当前该三元组只存在于文档，代码中无对应关系。

## D-159 竞争定位与 Skill 采纳基准（战略四条）

- 日期：2026-07-29
- 状态：`accepted`（用户拍板「全部建议采纳」）
- 依据调研：`references/analysis/agent-architecture-research-2026-07-28/`（13 篇主报告 ＋ 4 篇对抗复核）。总判断见该目录 **`00-SYNTHESIS.md`**，本条对应其 **§10.2／§10.4／§10.5／§10.3**
- 引用约定：下文 **`[R{n}§x]`** 指该目录第 n 号报告的 x 节（如 `[R13§6.1]` ＝ `13-orchestration-landscape-supplement.md` §6.1）；**`[S§x]`** 指 `00-SYNTHESIS.md` 的 x 节；**`[X-{a}§x]`** 指对抗复核（`xcheck-a1` / `xcheck-a2` / `xcheck-b`）
- 前轮对照：`references/analysis/harness-research-2026-07-17/`（Dify 源码级专篇 `08`、轻量可视化与自建 `09`、决策简报 `11`）。**本条不推翻其任何结论**，但按新判据重估了其 (b) 定位，见 `[R12§C]`
- 关联：为 **D-158**（场景知识从代码转为渐进披露的数据，原标注「待调研裁定」）提供裁定依据；其形态由本条 ④ 与 `[S§14A]` 确定

### 决定 ① 夹缝的定义 ＝「通用 agent 拿不到她的经营事实与结果反馈」，**不是**「她克服不了安装门槛」

两种定义寿命不同，导出的产品也不同：

| 定义 | 寿命 | 导出的工作 |
|---|---|---|
| 她克服不了安装门槛 | **会消失**（门槛在快速下降） | 把 agent 包装得够简单——**在跟一条正在下降的曲线赛跑** |
| **通用 agent 拿不到她的经营事实与结果反馈** | **不会消失**（时序资产只能靠跑同样的时间攒） | **成为她经营事实的唯一沉淀处** |

**推论：零安装不是护城河，是入场券。** 它让商家愿意开始；护城河是她开始之后攒下的东西。

### 决定 ② 认可「策展人真空」，`propose_* → confirm_*` 管线是**战略必需**而非记忆功能的一部分

Claude Code 里是**开发者**在当策展人（挑选／组合／维护／纠错）。本产品的用户（品牌负责人、美业老板娘，多数工作中不使用电脑）**无人能扮演该角色**——不会逛 skill 市场、不读 `SKILL.md`、不会在 skill 未触发时去调 `description`。

故只有两条路：**我们手写**（静态、可抄 ⇒ 退回阶段三）或**系统策展**（从证据沉淀，商家只做确认 ⇒ 阶段四）。选后者。

**这重新定性了现有的十个零调用工具**（`record_preference_signal`／`propose_preference`／`confirm_preference`／`revoke_preference`／`preference_view`／`propose_reusable_asset`／`confirm_reusable_asset`／`create_reuse_task`／`reusable_asset_view`／`series_suggestions`，均在 `apps/core/src/p1/operations/asset-memory-foundation-module.ts`，行号见 `[R11 Q1 全表 #118–#134]`）：它们不是「记忆功能没接完」，**是「系统扮演策展人」这件事的全部实现，且 propose/confirm 的分野正好等于「模型可调／仅商家可调」**——HITL 边界当初就画在了正确的地方，只是从未通电。

> **实测状态**：十条**100% 落 E 档（零生产派发）**，无一例外 `[R11§Q3.3]`。判据是「是否有生产代码把该 action 字符串送进模块派发口」——派发口仅两条：web `queryP1/commandP1`（`mkfast-template-main/src/p1/client.ts:65,153`）、core `executeIdempotentModuleCommand`（唯一实例 `apps/core/src/p1/result-delivery/operations-visual-adoption.ts:67`）`[R11 证据形态]`。
> **⚠️ 已废弃的旧论据**：早前用于对比的「`store_fact` harness 143／web 91」**不成立**——那些命中是判别式 `objectKind: 'store_fact'` 与类型名 `StoreFact`，非工具调用；真正的命令 `store_fact_append` 同样零派发 `[R11§Q3.3 口径更正]`。**不得再用该对比论证「其他工具是活的」。**

### 决定 ③ 三条架构硬约束

1. **零安装是硬约束不是偏好**（Web ＋ 手机）。这是第二次否掉「服务器上跑 coding agent」——第一次理由是计费／多租户／DBOS，本次是**任何需要用户侧 runtime 的方案，交付成本会吃掉全部利润**。
2. **交互形态只能是「确认式」，不能是「配置式」。** 商家不会配置任何东西，只会点是或否。**故 `propose → confirm` 是唯一可行的沉淀交互，不是可选项之一。**
3. **优势必须建立在「通用 agent 拿不到的输入」上，不能建立在「我们写得更好的提示词」上。** 提示词会被模型能力提升碾压，输入不会。那些输入是：她自己都没整理过的经营事实／六个月的内容—结果对应／品类平台规则的实测／她否掉过什么。

> 约束 2 最易被日后「给运营加个高级配置面」悄悄破坏，**明确写死**。

### 决定 ④ 跟 Skills/Plugin 范式的**格式与机制**，**不跟其分发模式**

| 范式组成 | 前提 | 对我们 |
|---|---|---|
| **格式与机制**：声明式知识包、渐进披露、`description` 路由、工具授权、`allowedTools` 边界 | 无前提，纯技术 | **完全适用，必须跟** |
| **分发模式**：marketplace、用户浏览挑选、`pi install`、自写 `SKILL.md`、misfire 自己调 | **建立在「用户是开发者」上** | **完全不适用** |

**推论：采纳 Agent Skills 标准的真实理由是「让装配层↔能力层的边界能随模型变强而移动」，不是「导入第三方生态」。**

- 原因（逐条给出处；标注 `[官方文档]`／`[实测]`／`[推断]`）：
  1. **外部最强佐证** `[官方文档，抓取 2026-07-29]` `[R13§6.1]`：`https://developers.openai.com/api/docs/guides/agent-builder` 原文——*"OpenAI is **deprecating Agent Builder**. Existing users can continue using it during the transition window, and the product is **scheduled to shut down on November 30, 2026**."* 官方迁移路径为**回到代码（Agents SDK 自部署）或 ChatKit（仅 UI 层）**。**可视化 DAG canvas 的最大推手，在发布不到一年后自己关停它。**
  2. **同向信号** `[R13§4.2, §6.4]`：BiSheng 毕昇（`dataelement`）为对齐 Agent Skills 标准而**下线其自研学习回路**（该报告标为 N1／N2）。与第 1 条指向同一判断——**行业正在把「编排」交还给模型，同时把「积累」整个丢掉**；策展人真空是该范式的结构性空缺，**先做出「系统当策展人」的人，拿到的是别人主动腾出来的位置**。
  3. **第三方生态对本品类近乎为零** `[实测]` `[R07§7.1]`：最大聚合库 **1,993 个去重 skill** 中 content/marketing 仅 **5.8%（115 个）**且多为英文 SEO／西方社媒；**中文平台相关 3 个，美业/医美 0 个**。
     另一口径的补充 `[X-b§1.3]`：`anthropics/skills` 官方 **17 个**（真正 coding 向仅 5 个）；**`coreyhaines31/marketingskills` 49 个 MIT skill**（42,094★，2026-07-27 仍在推），其中 `copywriting`／`content-strategy`／`ad-creative`／`cro`／`ab-testing` 等与本项目同域。
     **两组数字不矛盾**：英文通用营销方法论可直接取一批；中文平台规则与美业品类先验一个都没有。**故「导入第三方」的即期收益必须打折——这正是 ④ 把采纳理由改为「边界可移动」的原因。**
  4. **九维竞争拆解** `[S§10.1]` `[推断]`：技术门槛即使归零，通用 agent 在「文案方法论质量／灵活性／模型能力利用」三维赢，但在「单一账单的多供应商生图生视频／平台规则实测知识／合规烧录与七条红线／结果回流／跨月累积／手机可用」六维**没有输入**。阶段四的定义正是把竞争从第一维挪到后几维。
  5. **「换编排器不解决问题」由三份独立报告收敛** `[R02§风险③]` `[R05§4]` `[R06§8.2]`，其上位描述见 `[S§11.6]`：**缺的不是编排，是反馈**——模型看不见自己的结果（`validateHarnessPolicy` 无一调用点在 `apps/core/src/p1/harness/` 生成链内，`HarnessPolicyPhase` 的 `'execution'` 档从未接上）／系统看不见（记忆无使用遥测）／运营看不见（改完 skill 不知效果）。**每个候选框架提供的都是编排，没有一个提供反馈——因为反馈回路是领域特有的。**
- 影响：
  1. **§14 路线按「离护城河多近」排序，而非依赖序**：①证据层（记忆属主＋承接闭环）②充分性判定 ③上下文可计算 ④Skill 标准＋边界标记 ⑤工具循环。
  2. **对外话术与产品叙事**据 ① 重写：不强调「简单易用」（那是入场券），强调「你的经营记录只在这里长出来」。
  3. **十个零调用工具的通电从「补记忆功能」升级为「战略必需项」**，排期权重相应提高。
  4. **任何「让商家配置」的功能提案，按 ③-2 直接判否**，除非同时给出确认式替代形态。
  5. **排除口径统一**：不引 Dify／RAGFlow／BiSheng／FastGPT 的理由是**第二运行时（Go／Python，`apps/core` 是 Node）＋ D-041 ＋ 许可**，**不得写「范式不同」**。
     该理由已被本轮**两份报告独立证伪** `[R13§6.2]` `[R12§B]`：头部三家 2026 上半年均已长出 Agent Skills 原语子系统——RAGFlow 加了 ContextEngine 原语文件系统 ＋ SKILL.md hub；BiSheng PRD 已评审「自研内核下线，迁 deepagents ＋ SKILL.md」；FastGPT 加了 workspace skill ＋ SKILL.md ＋ 版本包 ＋ Sandbox。**Dify Agent 侧更有 7 个 deferred 工具**（`dify.shell`／`dify.drive`／`dify.knowledge_base`／`dify.ask_human`／`dify.config`／`dify.output`／`dify.execution_context`）**＋ Anthropic `SKILL.md` 标准原样采用** `[R12§B]`。
     **⚠️ 与之配套的自纠**：主控原判「这类产品＝DAG，范式与我们相反」**错误**。「一个场景＝一条 workflow」对经典 Workflow 成立、**对 Dify Agent 不成立**。站得住的只剩：**可视化 DAG 焊死决策边界，其逃逸口（`agent_v2` 节点转交 shell agent）需人重画画布，不是「架构允许边界移动」** `[R12§B′]`。
- 证据边界：
  - ①②③④ 的**框架判断属产品决策，不可实测**。
  - 支撑它们的**外部事实全部一手核过**：OpenAI 停运公告原文 `[R13§6.1]`；Dify LICENSE 与 07-17 本地镜像（`references/repos/harness-2026-07-17/dify/`，git HEAD `5c6372d2`）**逐字节 `diff` 无输出**，文件最后改动 2025-03-28，条款 2 的单方收紧权未被行使 `[R12§A.2]`；skill 计数经 `gh api` 拉取 `[R07§7.1]` `[X-b§1.3]`。
  - **九维拆解中「通用 agent 没有输入」的六维是结构性推断，未做对照实验** `[S§10.1]`。
  - 第三方 skill 计数**含 fork 与重复**，只作量级参考 `[R07§7.1]`。
  - **本轮对抗复核共查出 ❌ 9 条／⚠️ 25 条，其中包含主控自身五条 grep 依据里的三条错误**（`getPreferences`/`PreferenceView` 属 `p1/model-supply/` 且生产中是活的；`reuse_memory` 搜蛇形而仓内是驼峰；`preference_memory` 该标识符不存在）`[X-b§4.2]`。**引用本条时不得回溯使用那三条已废论据。**
- 待验证：
  - ① 的「时序资产不可复制」在**商家实际留存**上尚无数据——试点期需观察续费与切换行为；若商家轻易迁走，说明沉淀量未达形成切换成本的阈值。**与 `[S§12.6]` 可携带性立场联动**（该项待拍板）。
  - ② 的 `propose → confirm` **提议触发时机与频率未定** `[S§15.0 P1]`，且**「该出而未出的提议」当前无观测手段** `[S§15.0 P2]`。二者不解决，策展质量不可测。
  - ③-1 的「零安装」与 `[S§10.9]` 沙箱否决互为支撑；沙箱重启条件见该节，**其中一条依赖 `[R09]` 的执行面 skill 占比**（营销域实测 `marketingskills` **49/49 零 `scripts/`**，对照 `anthropics/skills` 8/17 有）。
  - ④ 的「边界可移动」是**期权**，价值仅在模型能力提升到可自主选 skill 时兑现；期间成本是「`description` 今天不读也要按标准写」`[S§12.4]`。
  - **Skill 路径整体目前是「论证充分，验证为零」** `[S§14A.8]`：一条 skill 未端到端跑过；`skill_define`／`skill_accept`／`skill_bind`／`skill_rollback`／`skill_deployment` 五命令 web 调用数为 0（`apps/core/src/p1/skills/foundation-module.ts:64,102,108,127,134`）；八张首发配方卡 `skillRevisionRefs` 全为 `[]`。**且 `apps/core/src/p1/skills/production-wiring.test.ts` 是源码文本正则匹配测试，非行为测试，不足以证明管道可跑** `[S§14A.7 机制①附证]`。**本条决定不依赖该验证结果，但 `[S§14A]` 的工作量估计依赖它。**

## D-160 架构层四条：护城河定位收窄、五步路线全启动、商家 Skill 不可导出（无可携带形态）、及格线信号＝输出末尾评价按钮

- 日期：2026-07-29
- 状态：`accepted`（用户拍板）
- 引用约定同 D-159：`[R{n}§x]`＝`references/analysis/agent-architecture-research-2026-07-28/` 第 n 号报告；`[S§x]`＝该目录 `00-SYNTHESIS.md`；`[X-{a}§x]`＝对抗复核
- 前置：**D-159**（战略四条）。本条是其架构层落地

### 决定 ⓪ 护城河定位：**排他性长在门店层，以及由门店层归纳出的那部分行业层**

D-159① 确立「排他性只可能长在门店层的时序证据上」。**本条按主控建议收窄该二分法**：

> **行业层若来自门店层的向上归纳（`[S§10.8]`），它就部分继承了门店层的不可复制性——因为归纳的输入是我方独有的证据。**
> **故行业层不是天然不可防守；它的可防守性取决于它是「归纳来的」还是「写出来的」。**

**三层的护城河成色（据 `[S§10.8]` 三来源）：**

| 层 | 来源 | 护城河成色 |
|---|---|---|
| **平台层** | 收割转译 | **无**——市面公开范本，人人可取。定位＝**必要成本，非资产**，投入按「够用就行」算 |
| **行业层** | 手写（冷启动）／**归纳**（成熟期） | **取决于来源**：手写的可被抄；**归纳来的不可复制** |
| **门店层** | 单店证据沉淀（`propose → confirm`） | **完全不可复制** |

**推论一（指标语义升级）**：`[S§10.8]` 那条「行业层中有第二来源交叉验证的条目占比」原本只是**防退化度量**（防 (a′) 收割悄悄变回 (a) 手写）；按本条，**它同时是行业层护城河成色的度量**。同一个数字，两重含义。

**推论二（投入分配）**：平台层按必要成本投；**行业层的冷启动手写投入应刻意克制**——它是要被归纳产物逐步顶替的那部分（但**不是临时脚手架**，见 `[S§10.8]` 用户对该措辞的修正：手写是三个并列的一等输入通道之一，风险在「过度依赖」不在「有手写」）。

### 决定 ① 五步路线顺序采纳，且**全部在本轮启动**

路线（`[S§14]`，按「离护城河多近」排序，非依赖序）：

1. **证据层**（记忆属主 ＋ 承接闭环）
2. **充分性判定**
3. **上下文可计算**（预算 ＋ 选择 ＋ 遥测反馈）
4. **Skill 标准 ＋ 边界标记**
5. **工具循环**

**用户裁定：五步全部本轮启动**（推翻主控「只开第一刀 ＋ 第①步子集」的建议）。

**与已批第一刀不冲突**：第一刀（`copywriting`，D-159 关联项）属第 ④ 步的**平台层**，而 `[S§14A.0]` 已定「平台层／行业层无前置可立刻开工，门店层才等证据层」。

**执行口径（主控补充，非新决定）**：并发上限 3 条的约束只作用于**需要 dev server 或测试基础设施**的工作面——`locale:compile` 前缀在 `typecheck`／`test`／`test:interaction`／`e2e` 四条命令上，会重写共享 paraglide 产物并掀掉在跑的 `pnpm dev`。**设计、schema 定义、skill 转译、文档不占该额度。** 故「五步全启动」在物理上可行，约束落在「同时处于『需测试基础设施』状态的工作面 ≤ 3」。

### 决定 ② 商家沉淀的 Skill：**可编辑、可删除、不可导出**——因为它没有可携带形态

- **可用**：商家使用平台提供的现成 Skill
- **可编辑**：商家可通过自己的组合，做简单的 Skill 编辑与二次修改，便于后续独自复用
- **可删除**：保留删除功能作**冗余兜底**（他得能清掉自己不想要的）
- **不提供下载**

**理由（用户原话要点）**：**下载没有意义，因为它的载体不是标准的 Skill，它必须在我们的 Runtime 上才能运行。**

> **⚠️ 本条推翻主控在 `[S§12.6]` 的「可携带性应作为特性并定为公开立场」。**
> 主控原推理链：商家经验以标准格式沉淀 ⇒ 技术上可导出 ⇒ 开放导出可换取信任。
> **错在第一环**：商家经由 `propose → confirm` 沉淀的产物**引用的是我们的租户资产、确定性触发条件表、六原语与平台层 skill**，不是自包含的 `SKILL.md`。导出物在任何其他 runtime 上都跑不起来。
> **故正确表述不是「我们允许/不允许导出」，而是「该产物不存在可携带形态」**——承诺一个技术上无意义的能力本身就不诚实。
>
> **可携带的是平台层**（我们转译进来的标准格式 Skill），**不是门店层**。`[S§12.6]` 混淆了这两层，须据本条修订。

### 决定 ③ 及格线信号 ＝ **流式输出末尾的结果评价按钮**，不做平台外归因

**用户裁定**：不过度兜底，**不做本平台外的功能延伸**；借鉴 coding agent 对输出结果判断的通用做法——**在流式输出结果的最后给出一个输出结果评价按钮即可。这是行业通用惯例，不做发散。**

> **⚠️ 本条收窄主控在 `[S§10.6]` 的「及格线必须锚经营结果，商家感知仅作短期代理并逐步让位」。**
> 主控原方案要为「到店/咨询转化」建归因管线。**问题**：该事件发生在平台外，我们观测不到；信号慢、噪声大（门店位置、天气、竞品活动均干扰）；且违反用户既有原则「验证期用最快栈，护栏须成本≈0」。
> **收窄后**：及格线的即时信号就是输出末尾的评价按钮。**不建归因系统。**

**与既有能力的关系（主控补充）**：`record_content_package_manual_result`（`apps/core/src/p1/operations/foundation-module.ts:643`）＝**商家自报发布结果记账**，**已有生产调用方** `mkfast-template-main/src/routes/dashboard/results_/$workId.tsx:1476`，`[R11 Q1 全表 #20]` 判为 `record` 原语实现。它同样是**商家自报、不需要平台外观测**，与本条不冲突，构成延迟信号；评价按钮构成即时信号。**两者都不是归因管线。**

#### 补充（2026-07-29，D-163 竞品补测后追加）：评价事件合同的**最低携带字段是硬要求**

**决定**：评价按钮产生的事件**必须携带 `skillId ＋ skillVersion ＋ 场景标识`**（场景＝objective 或配方卡分组，D-139），与被评价的产物一并落库；**不得只记「哪条消息被赞/踩」**。

**原因（`[静态]`，Codex 补测 Q3，`references/analysis/miora-teardown-2026-07-29/03-CODEX-FINDINGS.md §3`）**：Miora 已上线评价按钮，但其三套遥测互不关联且全都不含配方标识——

| 通道 | 载荷 |
|---|---|
| 腾讯 Beacon `chat_message_feedback` | `{ver1:"like\|dislike", ver2:"<assistantMessage.id>"}` |
| AiSee `POST /feedbacks` | `{message, levelId, fbType}`（4＝赞，6＝踩） |
| `POST /api/ai/skills/track-usage` | `{skillName}` ——**只有名字，无版本** |

Codex 判定：当前数据最多分别回答「哪条助手消息被赞/踩」与「哪个 Skill 名被成功调用」，**回答不了「哪个配方版本在哪类需求上表现差」**。

**这构成一条对本项目的直接警示**：评价按钮**不会自动产生可用信号**。D-160⓪ 的「由门店层归纳出行业层」依赖的正是配方粒度的质量信号；**事件合同若不在第一版就带全字段，拿到的是无法归纳的废数据，且补录不可能（历史事件缺字段无法回填）**。

**影响**：本条升为 D-162⑤ 遥测设计的**入口约束**——评价按钮与使用埋点**必须共用同一事件合同**，不得像 Miora 那样分成互不关联的两套。

**待验证（同批补，Codex 补测 §9.9）**：评价事件的**去重、撤回与重复评价**口径未定——同一产物被反复赞踩、商家撤回评价时，信号如何计入归纳。Codex 对 Miora 的对应项亦为 `[未证实]`（AiSee 保留期/去重/撤回规则未知），**无外部参照可抄，须自定**。另 `[静态]` 提示一个反面教训：Miora 的 `track-usage` 为 fire-and-forget、失败静默吞掉，**其埋点自身存在系统性漏计**；本项目的评价与使用埋点**不得采用静默失败**，须可观测其投递成功率——否则「配方表现差」与「埋点没送到」不可区分。

- 影响：
  1. `[S§12.6]` 与 `[S§10.6]` 两节须按 ②③ 修订，**且标注被本条 supersede**。
  2. **导出功能不排期**；转而排「商家侧 Skill 编辑 ＋ 删除」，其治理沿用 `[S§14A.8]` 的第八能力域形态（草稿→评测→发布→回滚），但商家侧界面须遵 D-116（人话、零工程词汇）与 D-159③-2（**确认式交互，不得做成配置面**）。
  3. 评价按钮的信号进 `record` 原语，成为 `[S§2]` 缺件 (b)「读取侧使用遥测」的**第一个真实数据源**。
  4. 五步全启动 ⇒ 需与在飞工程线协调：Wave7 四件护栏票尚未派出；`#240` 6 件、`#242` 约 10 件、`#245` 待裁；17 个老 issue 需关票复核。**协调方式按「同时处于需测试基础设施状态的工作面 ≤ 3」执行。**
- 证据边界：
  - ①②③ 均为**用户产品裁定**，不可实测。
  - 支撑 ② 的技术判断（沉淀产物引用租户内资产与平台层标识符，非自包含）**属结构性推断**，未做导出可行性实验——**但该推断可证伪**：若将来沉淀产物被改造成自包含格式，本条前提即失效。
  - `record_content_package_manual_result` 的生产调用方为实测 `[R11 Q1 全表 #20]`。**注**：反向扫描曾把 `mkfast-template-main/src/product/results/delivery-panel.tsx:363` 的「记录发布结果」判为空操作（`docs/reviews/noop-ui-reverse-sweep-2026-07-28.md` #5）——**两者可能是不同入口，一个接通一个未接，待一并核。**
- 待验证：
  - ⓪ 的「归纳出的行业层不可复制」**在归纳机制建成前无法验证**——归纳需门店层样本量，而门店层依赖 ① 第 1 步（证据层）通电。**归纳门槛（多少家店出现同一模式才算行业规律）刻意留空**，`[S§15.0 P4]` 建议等真实数据后再定，不提前设计。
  - ③ 的评价按钮**形态未定**（二元赞踩／多档／可选原因标签）。**建议随第一刀一并定，且遵行业惯例不发散。**
  - ② 的「商家可编辑组合」**边界未定**：能改什么（正文？触发条件？绑定关系？）、改坏了怎么办。**须与 D-159③-2「确认式而非配置式」对齐**——编辑能力过强会直接破坏该约束。

## D-161 落地层五条：治理走 sidecar、`description` 按标准写、代码量三层口径（E 档可立即清）、商家自报记账在冻结面外、pi manifest 形状存档

- 日期：2026-07-29
- 状态：`accepted`（用户拍板「按建议」×5）
- 引用约定同 D-159：`[R{n}§x]`＝`references/analysis/agent-architecture-research-2026-07-28/` 第 n 号报告；`[S§x]`＝该目录 `00-SYNTHESIS.md`；`[X-{a}§x]`＝对抗复核
- 前置：**D-159**（战略四条）、**D-160**（架构四条）

### 决定 ① 治理字段走 sidecar，不塞进 frontmatter

**约束事实** `[R07§7.2]`：Agent Skills frontmatter 共 6 字段（必填 2）；官方参考校验器 `skills-ref` 的 `ALLOWED_FIELDS` 是**闭集，未知顶层字段报错**。合法扩展仅两条——`metadata:`（值被强制 `str()`，**装不下嵌套对象**）与 **sidecar 文件**（OpenAI 用 `agents/openai.yaml` 走的正是这条）。

**决定**：我方治理字段（`budget`／`sideEffectClass`／租户／计费等）**一律走 sidecar**。

> **⚠️ 落地形态澄清（避免望文生义）**：我方 skill 存 PG 不存文件系统 `[R03§1.5：规范原文 "does not mandate where skill directories live"]`，故「sidecar 文件」物理上落成**一个独立的列或表**。
> **要抄的概念不是「文件」，是「把治理数据挡在标准 frontmatter 之外」**——如此 `SKILL.md` 那部分始终能过官方校验、能被标准工具链处理、第三方 skill 进出无需双向转换。

**同批退掉的三个自造枚举** `[R07§6.5, §6.6]`：`SKILL_STAGES`（**纯自创**，标准无「绑定编排阶段」概念，其职能改由确定性触发条件表承担）／`SkillDeploymentArtifactType`（标准目录约定的换名，**且我方漏了 `assets/`**）／`evalSuiteRef`（标准已用 `evals/evals.json` 目录约定覆盖）。均见 `apps/core/src/p1/skills/types.ts`。

### 决定 ② `description` 现在就按标准写好

**决定**：`description` 按 Agent Skills 规范认真写（含字符预算），**即使当前无代码读取它**（我方走确定性显式注入，不用模型自主选 `[R03§2.4]`）。

> **⚠️ 主控自纠**：先前称此为「纯期权」（`[S§12.4]`）**不准确**。它**今天就有两项实际用途**：
> 1. **运营目录页需显示它** `[S§14A.8 五]`——运营靠它认出该 skill 做什么
> 2. **转译时需靠它判断**第三方 skill 该挂到哪个触发条件 `[S§10.7]`
>
> **故它不是「为将来付的成本」，是「顺便兑现了将来」。** 期权价值（模型强到可自选时切换成本为零）是附带收益，不是唯一理由。

### 决定 ③ 代码量口径分三层；**E 档 94 条可立即清理，不依赖任何架构决策**

| 层次 | 数量 | 性质 |
|---|---|---|
| **立即可删** | **E 档 94 条（142 条中占 66.2%）** | **零迁移成本；是既有债务，不是原语化的功劳；不需等五步路线任何一步** |
| **原语化收益** | **B 档 25 条（17.6%，其中 5 条活）** | 有迁移成本——语义搬进 skill 文本，代码删掉但行为要保住 |
| **结构性减法** | —— | 把替模型做的决策还给模型（长期）`[S§11.5]` |

数据来源 `[R11§Q3.1]`。**⚠️ 明确纠正一个会高估 3.8 倍的算法**：不得把「E ∪ B活 ＝ 99 条」当作原语化收益汇报——**真正因原语化省下的只有 B 的 25 条**。

**执行决定**：**「E 档清理」单独开票，逐条复核后再删，禁止盲删。**

> **反例已知** `[R11§Q3.3]`：`confirm_asset_intake_fact`（#114）被 `finalize_store_intake`（#115）顶替**是设计意图（D-151），不是遗漏**——该条不该删。E 档中至少存在此类条目。

保留面参考 `[R11§Q3.1]`：A 52 条归并为 6 个原语工具；C 23 条归并为商家确认面（不进模型工具面）；D 42 条留在 admin 与画布编辑器。**模型工具面自 142 收敛到 A＋C ＝ 75 条语义，再经原语归并落到 6–7 个工具。**

### 决定 ④ 商家自报发布结果的记账：**确认在 D-155 冻结面之外**

**决定**：`record_content_package_manual_result` 等「商家自报发布结果」类记账**不属 D-155 冻结面**。冻结面为 `publish:*` 三目标 ＋ `p1/integrations/` 代发链 ＋ 平台账号绑定；**商家自报是承接闭环的组成部分，不是平台代发能力。**

**支撑证据（本轮新增）**：
1. `record_content_package_manual_result`（`apps/core/src/p1/operations/foundation-module.ts:643`）**有生产调用方** `mkfast-template-main/src/routes/dashboard/results_/$workId.tsx:1476`，`[R11 Q1 全表 #20]` 判为 `record` 原语实现——**它是活的**。
2. **D-160③ 已使其成为承接闭环的延迟信号来源**（评价按钮＝即时信号，本项＝延迟信号；**两者皆商家自报，均不需平台外观测，都不是归因管线**）。

> **⚠️ 附带待核（不阻塞本决定）**：反向扫描把 `mkfast-template-main/src/product/results/delivery-panel.tsx:363` 的「标记待人工发布／记录发布结果」判为**空操作**（`docs/reviews/noop-ui-reverse-sweep-2026-07-28.md` §3 #5，判 CONFIRMED），而 `$workId.tsx:1476` 是通的。**疑为两个入口一通一断，须核实并修复断的那个。**

### 决定 ⑤ pi 的 `package.json` `pi` 键式资源清单形状，**存档为将来「行业话术包分发」的参考**

形状：用 `package.json` 的 `pi` 字段声明 `skills/`／`prompts/`／`themes/` 目录，走 npm 分发（`pi install npm:<pkg>` 解出 `skills/` 目录）。约 5 行 manifest 约定 ＋ 一个目录扫描器 `[R08§3.3, §7]`。

> **⚠️ 存档必须连同反面教材一起记：审核必须自建。**
> pi 官方文档原文 `[R08§安全]`：*"Pi packages run with full system access. Extensions execute arbitrary code, and skills can instruct the model to perform any action including running executables."* 且 `pi install npm:<pkg>` **会真跑 `npm install`**（三方 install 脚本在用户机器执行）。
> 实测 `[R08]`：**零审核**；5,405 条目中头部样本 **4.7% 无许可证、2.7% 传染性许可**；**至少 33% 装了不注册任何东西**。

### 关于原「落地层第 7 项」

「标准的真实采纳理由是**边界可移动**，而非导入第三方生态」＋「载体可以借，内容得自己长」——**已由 D-159 决定④ 的推论段覆盖，本条不重复立项。**

- 影响：
  1. `apps/core/src/p1/skills/types.ts` 的 `SkillRevisionManifest` 须按 ① 重构：标准 6 字段留 frontmatter 侧，其余迁 sidecar；同批退掉三个自造枚举并补 `assets/`。
  2. **新增「E 档清理」票**（③），与五步路线并行，**不占其排期**；执行方式为逐条复核。
  3. `description` 写作规范进入转译规程（②），并作为运营目录页的必显字段。
  4. ④ 解除了 `[S§14]` 第一步（证据层／承接闭环）的一处归属疑虑。
  5. ⑤ 仅存档，不排期。
- 证据边界：
  - ①②⑤ 的外部事实一手核过（`ALLOWED_FIELDS` 闭集见官方校验器；OpenAI sidecar 先例；pi 文档原文与 npm 实测）。
  - ③ 的 142／94／25 三个数来自 `[R11]`，其判据为「是否有生产代码把该 action 字符串送进模块派发口」，派发口仅两条（web `queryP1/commandP1`，`mkfast-template-main/src/p1/client.ts:65,153`；core `executeIdempotentModuleCommand`，`apps/core/src/p1/result-delivery/operations-visual-adoption.ts:67`）。**该报告已排除朴素 grep 的双向假阳性**（web 侧 `task`/`inbox`/`search` 命中全为 `kind:'task'` 判别式；core 侧 `confirm_reusable_asset` 等命中全为 `fingerprint({action:…})` 幂等指纹串）。
  - ④ 的两处调用点为实测；**`delivery-panel.tsx:363` 与 `$workId.tsx:1476` 的关系未核**。
- 待验证：
  - ① 的 sidecar **schema 未定**（字段集、与 frontmatter 的引用方式、PG 落地形态）。须随第一刀（`copywriting`）一并定，且遵「第一条通路自带开荒步骤」`[R09§0.6]`。
  - ③ 的 E 档清理**须逐条复核**，复核标准未定；**至少需能区分「遗漏」与「设计意图顶替」**（D-151 型）。
  - ④ 的 `delivery-panel.tsx:363` 断链待核实与修复。

## D-162 实施架构：知识载体四层分工、六基质原语、有界自纠环、七门前移、Skill 维护面

- 日期：2026-07-29
- 状态：`accepted`（用户拍板，含逐条修正）
- 引用约定同 D-159。前置：**D-159**（战略四条）／**D-160**（架构四条）／**D-161**（落地五条）
- 本条为上述三条的**实施级落地**，并回答 **D-158 待验证②**

### 决定 ① 知识载体四层分工 —— **回答 D-158「避免第三套知识载体」**

| 载体 | 装什么 | 谁改 | 边界 |
|---|---|---|---|
| **Langfuse**（D-149） | **各 LLM 位点的基础提示词模板**（14 位点，请求时钉扎版本） | 开发／运营经 Langfuse | **不装场景知识** |
| **PG Skill 目录**（D-159④／D-161①） | **场景配方、行业先验、平台规则、口吻模板**——注入到基础提示词**之上** | 运营经第八能力域（决定⑤） | **不装基础提示词** |
| **门禁判据（代码）** | **七条硬红线**——属**门禁层，非知识层** | 开发 ＋ 合规评审 | **不可被任何 Skill 覆盖** |
| **评测集**（`evals/evals.json` ＋ promptfoo） | 正负控、golden 集、各 Skill 自带评测 | 运营／开发 | —— |

> **⚠️ 一处实存冲突及其处置**：Skill 版本契约当前带有**内联提示词字段**（`apps/core/src/p1/skills/foundation-module.ts:98` 将 `value.prompt` 作为 `HarnessFrozenPrompt` 传入草稿），**这正是 D-158 警告的「第三套知识载体」**——同一份提示词可同时存在于 Langfuse 与 Skill 记录里，两处真相源。
> **裁定：Skill 不承载基础提示词。** 该字段语义收窄为**引用 Langfuse 位点**（存引用，不存内容）；**禁止内联提示词正文**。
> **理由**：D-149 已定「14 位点全量入 Langfuse ＋ 请求时钉扎版本」；若 Skill 可内联提示词，则位点覆盖率与版本钉扎双双失效，D-149 的收口无法成立。

### 决定 ② 六基质原语（净新建）

能力面由六个原语构成；**领域覆盖来自 Skill 在原语之上组合，不来自新增领域命令**。

| 原语 | 语义 | 副作用等级 |
|---|---|---|
| `read_context(scope, query?)` | 查——商家档案／素材／历史结果／偏好／平台规则／品类先验 | `read` |
| `generate(kind, brief)` | 造——文案／图／视频／**抽取解析** | **计费** |
| `revise(target_ref, instruction)` | 改——有界修改，走 OCC revision 围栏 | **计费** |
| `record(kind, payload, provenance)` | 记——**仅提议级写入** | `bounded_write` |
| `check(target_ref, rulesets?)` | 校验——红线／平台规则／结构；**确定性、不调模型** | `none` |
| `ask_merchant(question, options?)` | **人是逃生舱**——异步、不阻塞 | `none` |

- `generate` 的 `kind` **须含「抽取／解析」**——素材解析是调外部供给且计费的生成类动作，塞不进文案／图／视频 `[R11§Q3.2]`。
- 签名对照现成实现校准 `[R13§6.3]`：`read` 取 `(offset, limit)` 分页语义；`revise` 对照 `edit(old_string,new_string,replace_all)`；`record` 的记忆分层对照 `users/{profile,preferences,entities}`。
- **`revise` 与 `ask_merchant` 在现有 142 条命令中零实现；`check` 名义 2 条但均为死代码且不含红线**——三者按**净新建**对待 `[R11§Q3.2]`。

**安全不变量（不可协商）**：
1. **租户与计费身份环境注入**，永不作为模型可传参数
2. **`confirm_*` 商家专属**，模型只能 `propose_*`（与 D-117／D-122 HITL 总纲、D-159② 一致）
3. **`check` 是附加的，不替代既有确定性门禁**
4. **`generate`／`revise` 次数计入报价预算，超限失败关闭、不静默计费**

**工具注册表**：建立注册表，六原语首批注册；`allowedTools` 校验从「比对调用方自报字符串」改为**对注册表校验**——该前置闸已存在且在子调用执行前生效，缺的是锚 `[R07§5.2]`。

### 决定 ③ 有界工具循环：交付前自纠环

- **循环关在单个 workflow step 内部**，DBOS 步骤边界不变；崩溃重放＝重跑该 step，循环从头再来。**幂等由 step 保证，不由循环保证**——据此不破 D-041。
- step 内核保持 `(input, tools) → output` 纯函数，**工具经参数注入、不从环境抓取**——据此不破 D-038①。
- 环形：`generate → check →（有违规则 revise，硬性封顶 N 次）→ 再 check → 仍不过则门禁拦下 ＋ ask_merchant`。
- `check` 确定性、不调模型、成本可忽略 ⇒ 循环内可自由调用；`revise` 计费 ⇒ 次数硬封顶，预算属 quote 一部分 ⇒ 与「生成前签 `quoteId`」不冲突。

> **「有界」的准确含义不是「最多转 N 次」，而是「循环被关在 step 里，step 的边界就是循环的边界」。**

### 决定 ④ 七门红线前移至 `'execution'` 相位

**实测事实**：`validateHarnessPolicy` 全部调用点在 operations 层（审批、可见文案）与评测侧，**无一在 `apps/core/src/p1/harness/` 生成链内**；而 `HarnessPolicyPhase` 类型**已含 `'execution'` 档，从未接上**。

**后果**：模型生成完永远不知道自己有没有踩线，商家看到的是一次失败或一次被下游拦下的交付。

**决定**：`check` 原语**收编同一份判据实现**，在 `'execution'` 相位调用。**禁止复制第二份红线逻辑。** 门禁层行为不变——`check` 只让模型更早自纠，**拦不拦仍由门决定**。

**并入既有票**：`#242` 的硬门负向用例与本条同属硬门体系，**须协调，不得各做一半**。

#### 补充（2026-07-29，D-163 竞品补测后追加）：「不可被 Skill 覆盖」的**实现机制＝协议层隐藏回包 ＋ 绑定 tool_call_id**

D-162① 定了「门禁判据是代码，不可被 Skill 覆盖」，但**未给实现机制**。Codex 补测在 Miora 的 v2 媒体工具契约中找到一个可直接采用的形态 `[静态]`：

> **确认绑定具体 `tool_call_id`；隐藏回包携带 `params_confirmed_by_user: true` 后，在当前回合重调工具。**

**决定**：本项目的执行确认与门禁放行**采用同一形态**——
- 放行凭证在**协议层**产生并回传，**不经过 Skill 正文、不经过模型输出**；Skill 无论怎么写都无法自行伪造确认。
- 凭证**绑定具体 `tool_call_id`**，不是一个全局或会话级的「已确认」标志位。**防串轮**：历史确认、其它工具的确认、模型自己声称的确认，一律不能放行本次调用。

**原因**：若放行凭证走模型可见/可写的通道（如工具入参、消息文本），Skill 只要写「调用时带上 confirmed=true」即可绕过——D-162① 的「不可覆盖」就只是一句声明而非机制。

**对照事实（说明为何只抄这一条，不抄其风控）**：Miora 的 `POST /api/ai/cloud-agent/risk/pre-report` `[静态]` 与 prompt **并发发出、800ms 超时、失败不阻塞**（`sendMessage` 起一个不被 await 的 async IIFE，外层立即执行 `session.prompts.send`）。Codex 判定其**不构成 execution 前同步 fail-closed 门**，只是追踪/处置线索。**本项目七条硬红线的放行条件仍须 fail-closed，不得降至 Miora 水平。** 可借鉴的只有上述协议边界形态，以及其「安全判定（`safetyStatus` 0–5）与发布生命周期（`publishStatus` 1–5）分轴」的状态模型。

**证据边界补充（2026-07-29，Codex 补测 §9.5）**：`[未证实]` `risk/pre-report` 的正常响应 schema、allow/block/review 语义，以及**后端能否异步终止已发送的 runtime/session** 均未查清。故上文「不构成同步 fail-closed 门」的判断**仅针对前端发送路径**（该路径不阻塞为 `[静态]` 实证），**不排除后端存在异步处置能力**。本项目的裁定不依赖这一点——无论 Miora 后端如何，我方红线仍须 fail-closed。

#### 方法论提醒（同批，Codex 补测 §9.6）：门禁「存在」≠ 门禁「覆盖」

Codex 对 Miora 的一条未闭合项：`[未证实]`「未加载所有延迟页面 chunk，**不能证明每个安装/使用/编辑入口都调用了组合阻断判据**」。

**这正是本项目已被实证过的老毛病**——`[S§14A.7]` 的「零生产调用方占比 66.2%」、`production-wiring.test.ts` 以**源码正则匹配**冒充行为验证（`[R11]` 实锤）、以及 D-162④ 本身的成因（`HarnessPolicyPhase` 已含 `'execution'` 档却从未接上）。

**决定**：七门前移的验收**不得只验证「门存在」，必须逐入口证明「门被调用」**。验收口径并入 D-162 影响项②的「票面收口须给生产派发证明」（见 `[S§14A.7]` 机制①），**不接受「新增了 check 调用」这类不指名入口的收口描述**。

### 决定 ⑤ LLM 介入环节定档

| 环节 | 判 |
|---|---|
| 意图理解 | LLM，保持 |
| 上下文装配 | 确定性，保持——**但这是将来边界移动的位置**（D-159④） |
| **充分性判定** | LLM。**既有模块已存在且已接通生产、已绑 Langfuse**——**先读它做了什么再决定补什么，不按新建对待** |
| brief 编译 | LLM，保持 |
| 执行选择 | N→1，保持 |
| 内容生成 | 供应商模型，保持 |
| **交付前自纠** | **当前缺口，由决定③④补** |
| 提议沉淀 | 后台异步、单次调用，**不需要循环** |

**永不交给模型**：租户与计费身份、红线最终裁决权、`confirm_*` 级动作。

### 决定 ⑥ Skill 维护 ＝ 既有控制台的第八能力域

- 沿用 **D-037**（扩展存量 admin-config）与 **D-048**（一体化控制台，不另建管理运行时）。**不新建管理运行时。**
- 界面是既有五个生命周期命令（定义／接受／绑定／回滚／部署）的**面**；**建这个界面即为这五个命令提供第一个生产派发点**（它们至今 web 调用数为 0）。
- 治理沿用 admin-config 的 CAS ＋ 审计 ＋ 回滚；**在途任务绑定不可变 revision 快照，运营改 skill ＝ 建新 revision，动不了正在跑的任务**（与 D-038④ OCC 围栏同源）。
- **目录页「来源」列（收割／手写／归纳）为硬要求**——D-160⓪ 的「行业层第二来源交叉验证占比」指标由该列算出，**指标要能算，字段就得先存**。
- **使用面板的价值上限由使用遥测决定**；无遥测则退化为 CRUD，运营等于盲改。
- 商家侧：**可用、可编辑、可删除、不提供下载**（D-160②），界面遵 D-116（人话、零工程词汇）与 D-159③-2（**确认式而非配置式**）。
- 权限边界：运营改内容与绑定；开发改原语签名、门禁判据、工具注册表、循环预算；**红线走合规评审，运营不可改**。

### 决定 ⑦ 第一刀：`copywriting`，行业层先空着

- 取 `coreyhaines31/marketingskills`（MIT）中与本域同源的通用文案方法论一条，走完整通路：**转译（人工蒸馏细目）→ 存 PG 归平台层 → 挂确定性触发条件 → 挂商家可见入口 → 真实生成中注入 → 对比有无它的输出差异**。
- **商家可见入口须挂在 D-139 已改约的输出类型轴上**（文案／图文／视频 lens ＋ **配方卡目录**）——配方卡目录即 Skill 的前台挂载点。
- **行业层第一批先空着**（选 A）：先证明管子通，再灌水。
- **验收：输出确实改变且改善可测**（走 promptfoo）。**两种结果都推进判断**——有效则收割转译通道被证明；无效则平台层价值低于预期，行业层与门店层权重上调。
- **前置**：`#242` 的评测正负控核销 ＋ golden 集**排在其前**（验收依赖）。
- **开工第一步不是写 skill，是验证那五个生命周期命令能否跑通**——从未执行过的代码路径不得假定正确；且既有「生产装配」测试是**源码文本正则匹配**，不足以证明管道可跑。

- 影响：
  1. `apps/core/src/p1/skills/` 的版本契约按 ① 收窄提示词字段语义；按 D-161① 重构 sidecar 并退三个自造枚举。
  2. `#240` 的提示词位点登记与本条 ① 同属一个承载体系，**须协调，避免重复建**。
  3. `#242` 的硬门负向用例与本条 ④ 须协调；创作循环 spec 族翻新与商家侧 Skill 编辑界面、评价按钮**同动前台创作面，须串行**。
  4. **D-126 的依赖关系澄清**：首页推荐当前依据是最近交付回放而非「确定性规则＋行业先验」，根因是无结果信号可用；**D-160③ 的评价按钮与本条 ⑤ 的遥测是其燃料**，D-126 的口径不改，但其兑现依赖本轮。
  5. 反向扫描的两条真缺口由本条覆盖：三级信号「已验证」层恒空 → D-160③ 评价按钮为其生产者；人设授权范围有软消费者无硬门 → 由 ④ 的 `check` 收编覆盖（该字段正是七门之一的判据对象）。
- 证据边界：
  - ①的冲突为**实测**（`foundation-module.ts:98` 与 `draftRevision` 入参）；分工表为**产品裁定**。
  - ②的「三者净新建」来自 `[R11§Q3.2]` 全量清点；派发口判据见 D-161 证据边界。
  - ④的「无一调用点在生成链内」为**实测**（`validateHarnessPolicy` 全量调用点枚举）。
  - ⑤的「充分性判定模块已接通」为**实测**；**但其内部行为未读，故只判「需重估」不判「已够用」**。
- 待验证：
  - ① 收窄后，Skill 如何引用 Langfuse 位点的**具体形式未定**（位点 ID ＋ 版本？还是仅位点 ID 由运行时钉扎？）须与 D-149 的钉扎机制对齐。
  - ③ 的循环次数与预算上限**具体取值未定**，须与报价模型一并定。
  - ⑤ 的充分性判定模块**内部行为未读**。
  - ⑦ 的转译规程**无先例可循**，第一条通路须自带开荒步骤。
  - 提议触发时机与频率、「该出而未出的提议」的观测手段，**主控明确未想清**，须在实施中定。

### 待验证状态汇总（2026-07-19）

本表只是对上面 20 个原问题的状态投影，**不改写、不合并、不重编号原问题**；逐条问题文本仍是语义权威。规范化状态含义：`open_operational`＝需要真实运营样本才能回答；`rewritten_deferred`＝原问题已改写并主动延期；`completed`＝已有可复核证据闭合；`deferred_operational`＝问题仍成立，但触发点明确在运营期；`partial_open`＝一部分已闭合、剩余部分继续开放；`trigger_deferred`＝当前不立项，仅在显式触发条件成立时恢复。

| 编号 | 规范化状态 | 现有证据 / 裁决 | 下一触发点 |
| --- | --- | --- | --- |
| 1–12、14–15 | `open_operational` | 原问题均要求真实门店任务、设备、素材、平台、授权、采用率或身份边界样本；D-040 已把研究期与运营期的证据边界分开，当前代码通过不构成这些问题的答案。 | Wave 1/WOZ 启动并取得脱敏的真实任务回放、采用/小改、撤回、授权与发布数据后，按原编号逐项回答。 |
| 13 | `rewritten_deferred` | D-036 已将 Langfuse 转正；当前文本已把剩余范围收窄为二阶段 LangMem/Mem0 与可选 assistant-ui 隔离 spike，并继续受 D-016 的单一记忆运行时约束。 | Composer 回放证明薄壳不能满足交互，或二阶段出现明确的跨任务记忆选择需求时。 |
| 16 | `completed` | D-041 已锁定 DBOS Transact；七面验证报告在 `.scratch/dbos-poc-2026-07-18/REPORT.md`，覆盖原六题并增加审计双写验证。 | 无；若 DBOS 版本、部署拓扑或硬约束发生实质变化，另开兼容性复核，不回写本题为未完成。 |
| 17 | `deferred_operational` | D-035 保留 ①③节点 eval 与 BAML 阈值；2026-07-17 已裁决研究期不构造 50–100 条伪“真实输入”。 | WOZ/运营获得目标中国模型上的真实输入集后，测首过校验率并标定迁移阈值；BAML 上游许可口径变化时同步复核。 |
| 18 | `partial_open` | D-041 已实测闭合“Langfuse trace + 自建 PG 审计”中的审计先行、硬停、降级补偿、重放幂等半边；`promptfoo × 火山/豆包` 真机兼容 spike 尚未执行。 | 获得允许使用的火山/豆包真实 key 与隔离环境，或产品明确要求把该可选 spike 升为发布门时。 |
| 19 | `partial_open` | 用户已明确提出并确认非技术运营安全调优/重组需求；D-101 已锁定固定五阶段、受控积木、Recipe compiler 和测试发布方向。产品边界已闭合，首批 StageType/config Schema、原子发布、执行快照和可用性仍未实现/验证。 | 以 5～10 个首发能力包定义最小积木与 Schema，完成一次局部调优、一次阶段内重组、内部发布和回滚的运营可用性原型。 |
| 20 | `trigger_deferred` | D-101 已确认自有受控 Recipe Studio，Mastra、Dify、AI Gateway 仍非当前默认栈，不因运营可视化需求自动立项。 | Mastra 仅在自有 Studio 调优/观测体验实证不足时；Dify 仅在受控语法无法表达真实复杂分支且双前置满足时；AI Gateway 仅在火山真机路由验证有明确收益时。 |

## D-163 竞品实证校准（Miora）：维持六原语但去枚举化、记忆补被动管道产出提议、门店沉淀本轮不外发

- 状态：`accepted`
- 时间：2026-07-29
- 触发：`https://miora.design`（腾讯设计出品）登录态实测。**该产品与本项目 D-159~D-162 的架构高度同构，构成一次独立实现的对照组。** 完整拆解见 `references/analysis/miora-teardown-2026-07-29/00-TEARDOWN.md`（功能与静态分析）与 `01-RUNTIME-OBSERVED.md`（一次真实任务的 ACP 全链路实录），原始证据在同目录 `evidence/`。

### ① 维持 D-162② 的六基质原语，但**领域枚举一律不进原语签名**

- **裁定**：`generate(kind, brief)` 的 `kind`、以及任何输出类型 / 模型 / 尺寸类枚举，**由 Skill 或能力查询在运行时提供，不写死在原语签名或代码枚举里**。新增一个输出类型必须做到零代码改动。
- **对照事实**：Miora 的原语面是**通用 coding agent 工具面**（`read_file`/`write_to_file`/`execute_command`/`lsp_tool`/子 agent/MCP），领域性 100% 由 Skill 承载，连模型选择都不硬编码——实测工具时序里 agent 先调 `mcp__image_tools__list_model_caps` **查能力**，再调 `text_to_image` 生成。`[实测]`
- **为何仍不改用通用工具面**：D-162④ 的七条硬红线门禁必须**不可被 Skill 覆盖**。若原语面是 `write_file`/`execute_command`，红线门禁**没有可挂载的语义位点**，只能退化为事后扫描。Miora 能用通用工具面，是因为其合规压力是 `safetyStatus` 式内容审核，而非本项目的强监管品类准入（见 `[feedback-regulated-market-access-gating]`）。**约束不同，结论不同，非取舍失误。**
  - **补强（2026-07-29，Codex 补测 §9.7）**：该风险在 Miora 身上**已被独立识别为未闭合项**——`[未证实]` 「未验证 `execute_command`、文件工具或直接 MCP 是否可绕开受控媒体入口」。Codex 按安全约束未做绕路测试，**故这是一个悬而未决的攻击面，不是一个已被证明安全的设计**。
  - **本项目的六原语面根本不含 `execute_command` 与文件工具，从结构上消除了这一整类绕路可能**——这不是「我们更保守」，是**两种原语面在可验证性上的本质差异**：通用工具面的门禁覆盖必须靠穷举验证每条绕路，语义原语面则由原语集合本身封闭。
- 附带采纳：`ask_merchant` 签名补三项——**整组可跳过**、**每项带「暂未确定」兜底选项**、**一次性问全而非逐条追问**。三者均为 Miora 实测形态。

### ② 记忆写入补**被动管道**，但产出 `propose_*` 而非直接入库；新增 `correction` 作为一等 kind

- **裁定 A**：D-162② 的 `record` 之外，增设一条**被动沉淀管道**——每轮对话自动投递，后端异步抽取。但其产出**一律为 `propose_*` 候选，不直接入库**，须经商家在维护面确认。
- **理由**：D-159② 的策展人真空与 `propose_* → confirm_*` 边界要求沉淀必须经确认；且 D-160② 要求门店层沉淀「可用、可编辑、可删除」，直接入库会让商家面对大量未经确认的条目。
- **推论（重要）**：这为 **D-162⑥「Skill 维护 = 第八能力域」填上了真实内容**——该界面的主要工作**不是让商家写 Skill，而是确认系统提议的沉淀**。原设计中该能力域内容偏空，此条闭合。
- **裁定 B**：`record(kind, …)` 的 kind 增设 **`correction`（纠正）**，且其权重高于普通偏好。
- **对照事实**：Miora 的记忆是**一级导航**，分四域——用户 / 工作流 / 项目 / **纠正**，各自独立计数；写入有三条路径（被动管道 / 用户口头说「记住」/ 模型主动 tool），记忆以**图结构**呈现（有思维导图视图与缩放）。`[实测]` 被动管道报文形态：`POST /api/ai/v1/memories/user/chat {text, assistant_reply(前500字), workflow_id, has_asset_ref, asset_file_keys, message_id}`，fire-and-forget。`[静态]`
- **为何 `correction` 值得单列**：用户改过的地方直接暴露系统的错误先验，是信噪比最高的一类记忆；混在普通偏好里会被稀释。

#### 裁定 C（2026-07-29 追加）：「从既有对话沉淀成 Skill」**实现为一条平台层 Skill，不建任何专用管线**

- **决定**：该能力**不做**「一键沉淀」按钮的后端管线、不建 message/session→Skill 的绑定字段、不新增原语。**实现形态＝一条平台层配方**（美业版 skill-creator），触发词用商家的话（如「以后都这么做」「记住这个流程」），跑起来就是一次普通 agent 会话。
- **决定**：该配方的步骤直接采用 Anthropic 官方 `skill-creator` 的 `Capture Intent` 四项抽取——**所用工具 / 步骤序列 / 用户做过的纠正 / 观察到的输入输出格式**；缺口由 `ask_merchant` 补，产出经 `propose_* → confirm_*` 确认后落库。
- **原语映射（零新增）**：

  | 官方步骤 | 本项目原语 |
  |---|---|
  | extract from conversation history | `read_context` |
  | the user may need to fill the gaps | `ask_merchant`（含 D-163① 补的「一次性问全／整组可跳过／每项带兜底」） |
  | should confirm before proceeding | `propose_* → confirm_*`（D-159②） |
  | 产出 SKILL.md | `record` |

- **证据（`[官方文档]`＋`[包内]`）**：Anthropic 官方 `skill-creator`（`anthropics/skills` 主干，Apache-2.0；本机副本 `~/.claude/skills/skill-creator/`，与线上一致）`Capture Intent` 节原文：

  > Start by understanding the user's intent. **The current conversation might already contain a workflow the user wants to capture** (e.g., they say "turn this into a skill"). If so, **extract answers from the conversation history first — the tools used, the sequence of steps, corrections the user made, input/output formats observed.** The user may need to fill the gaps, **and should confirm before proceeding to the next step.**

  **关键：`skill-creator` 本身就是一个 Skill。** 它没有专用 API、没有后端「沉淀」能力——它只是一份 `SKILL.md`。故本能力**落在 D-163① 「少量原语 × 大量 Skill」的正中间：沉淀能力是一条配方，不是一个平台功能。**

- **顺带证实两条既有裁定**：
  - **D-161 的 `metadata` 合法扩展位**——官方校验脚本 `scripts/quick_validate.py` 的闭集为 `{name, description, license, allowed-tools, metadata, compatibility}`，`metadata` 确在其中，仅 `name`/`description` 必填。打包产物为 `.skill`（zip），与 Miora 的 zip 形态一致。
  - **本条裁定 B 的 `correction` kind**——官方四项抽取的第三项即 `corrections the user made`。**「纠正」被 Anthropic 与 Miora 双向独立确认为一等信号。**

- **⚠️ 纠正 Codex 补测的一条结论**：`03-CODEX-FINDINGS.md §4/§9.8` 记「未发现把任意既有对话一键沉淀为 Skill 的入口、API 或 message/session 绑定字段」——**观察属实，但不足以支持「该能力缺失或难以实现」的推论**。正确解读：**这件事本就不需要 API**；Miora 的「新开专用对话让 Agent 写 Skill 文件」与官方 skill-creator 是同一模式，差别仅在**未在既有对话里挂触发入口**，属产品决策而非技术限制。**本条不得被引用为「工程复杂度高、建议砍掉」的依据。**

- **影响**：D-162⑥ 维护面的职责进一步收窄——**不是编辑器，也不是沉淀管线的前台，而是「确认系统提议的沉淀」**，与本条裁定 A 的推论一致。

### ③ 门店沉淀**本轮不做**对外发布/共享；D-160② 的「不可导出」口径理由改写

- **裁定 A**：门店沉淀出的配方**本轮不提供发布给其他门店的通路**。D-160⓪ 的行业层仍走**平台侧归纳**路径。
- **理由**：实测 Miora 市场一个与本项目赛道重叠的 Skill（`xhs-content-generator`）`downloadCount: 221` / `callCount: 27`——**装机与实际使用相差一个数量级（12%）**。`[实测]` 市场机制的实际产出可能远低于预期，而其带来的竞对可见性、经营事实夹带、与 D-142 授权字段 merchant-only 的冲突是**即时**的。留待有真实沉淀数据后重评。
  - **⚠️ 证据强度降级（2026-07-29，Codex 补测 §9.10）**：`[未证实]` **`callCount` 的数据来源未确认**——Codex 未能证明 `POST /api/ai/skills/track-usage`（body 仅 `{skillName}`）是市场 `callCount` 的唯一数据源。故「27」的口径不确定（可能漏计、可能含其它通道）。
  - **本条结论仍成立**，但依据改为**量级差本身**而非精确比例：`downloadCount` 与 `callCount` 差一个数量级这一事实不依赖计数口径的精确性。**「12%」这个数字不得再作为精确论据引用**，只作量级示意。
  - 另需注意：`track-usage` 为 fire-and-forget（失败静默吞掉），**其自身即存在系统性漏计**——这一点反而支持「真实使用率不高于观测值」的方向。
- **裁定 B**：D-160② 的「不可导出」**结论不变，理由改写**为「**官方配方不可见 ＋ 门店沉淀无可携带形态**」。
- **对照事实**：Miora 并非一刀切——官方 Skill 调 `/export` 返回 `"官方 Skill 不支持导出"` 且 `/files` 返回空 `tree`（**配方正文对用户完全不可见**），个人 Skill 则可导出。`[实测]` 其分野更细，但其用户是设计师（有能力也有意愿写 Skill，导出是社区激励的一部分）；本项目用户是美业商家，**导出物引用租户资产与平台层标识符，在别处跑不起来**。前半句（官方配方不可见）直接采纳，后半句为本项目特有。

### 连带确认（不新增决策，仅记录实证结果）

- **D-160③ 被证实**：Miora 交付末尾确有评价条（`复制`/`点赞`/`点踩`/`更多`，纯图标无文字标签），**且同时有后续动作 chip**（如「换背景为深色版」）。两者服务不同对象——评价条给系统质量信号，chip 给用户即时价值。**本项目两者都做**，评价信号价值更高（须喂 D-160⓪ 的行业层归纳）。
- **D-161 三条落地口径全部命中**：Miora 真实 SKILL.md 用 `metadata:` 承载 version/logo，用同包 `_meta.json` 作 sidecar 承载 locales/params，`description` 塞满触发词承担路由。`[包内]` 三者与 D-161 裁定逐条一致，**该决策可从「待验证」视为已获外部实证**。
- **D-159③「交互只能确认式不能配置式」获得工程解法**：Skill 在 sidecar 声明少量枚举参数（`type: single|number` + `options` + `default`），前台渲染为 pill；关键机制是**pill 选中即抑制对话追问**（Miora 的 `references/params.md` 原文：「如果用户通过选择器选定，则不再在对话中重复询问」）。`[包内]`
- **执行前二次确认（参数卡片）为本项目缺件**：Miora 在真正花钱执行前另有一道确认，展示模型/比例/分辨率/数量并给 `[拒绝][确认]`，且用商家语言解释技术参数（「3:4 竖版，适合朋友圈/展架双用」）。本项目目前只设计了意图层确认（D-162② 的 `ask_merchant`），**执行层确认待补**，记入下方待验证。
- **不跟进项**：Miora 输入框自建语音按钮。与 `[feedback-nontech-audience-product-taste]` 第①条（系统层已解决的不造轮子，语音交给输入法）冲突；且其面向设计师桌面场景，本项目面向商家手机场景，手机输入法语音能力远比桌面成熟。**不跟。**

### 证据边界

- ①的 `list_model_caps` 时序、②的四记忆域与被动管道报文、③的 221/27 计数与导出拒绝文案，均为 `[实测]`（登录态实际调用/解包，非推断）。
- Miora 底座为 `claude-sonnet-4.6` + ACP（Agent Client Protocol，JSON-RPC 2.0）+ E2B 沙箱（腾讯 CloudStudio），为 `[实测]` 报文所证。**本条不构成本项目采用同栈的依据**——仅用于解释其原语面为何是通用工具面。
- 本次仅观察**一次**任务的一种交付形态，样本量为 1。凡「Miora 如何如何」的表述均受此限制。
- 首版拆解曾据无障碍快照误判「交付末尾无评价按钮」，经用户指出后核对 DOM 订正，结论反向。已在 `01-RUNTIME-OBSERVED.md §2.3` 留订正记录。

### 待验证

- ① 的「能力查询」在本项目的具体形态未定（是否需要一个 `list_capabilities` 类原语，还是由 Skill 目录直接投影）。
- ② 的被动管道**触发时机与抽取阈值未定**；与 D-162 原有的「提议触发时机与频率」为同一待决项，合并处理。
- 执行层二次确认的**触发条件未定**（是否所有生成都拦，还是仅超过成本阈值时拦）。须与报价模型一并定，与 D-162③ 的循环预算上限合并处理。
- Miora 点赞/点踩的**落库形态未查**（是否与 `track-usage` 打通、是否绑定到具体 Skill 版本）。这决定评价信号能否反哺到**具体配方**而非全局满意度，对 D-160⓪ 的归纳路径有直接参考价值。

## D-164 Dashboard 工作台结构定案：单路由三段、执行确认卡扩容、记忆升一级、动作 chip 与余额常驻

- 日期：2026-07-29
- 状态：`accepted`（五条用户逐条拍板；对齐稿 `references/analysis/miora-teardown-2026-07-29/04-HOMEPAGE-ALIGNMENT.md`）
- 背景：D-163 竞品实证后，用户提出 Dashboard 应参照 Miora 的功能排布与交互形式。本条把 **D-114**（对话流三层／七类卡）、**D-126**（轻推荐＋Day-0 示例店）、**D-139**（输出类型轴＋配方卡目录）三条既有决策合成一张完整的 Dashboard，并逐项裁定采纳/不采纳 Miora。
- **术语前置**：**Landing Page**（未登录，品牌形象入口）归 **D-125**，**本条完全不涉及、一字不改**；本条全部内容指 **Dashboard 工作台**（登录后主界面，D-126 范围）。此界 D-126 原文已划定。

### ① Dashboard **单路由三段**，不再分「工作台首页」与「创作页」

- **决定**：登录后只有**一个** Dashboard 路由，自上而下三段：
  1. **提议位**（D-126）——热态＝一天一个主推荐（三要素：为什么今天适合发＋用了本店什么＋希望顾客做什么），点击＝预填充 Composer 不自动执行；冷态＝Day-0 示例店预览。
  2. **创作面**（D-139＋D-114①）——输出类型轴三 lens（文案／图文／视频）＋大输入框＋执行模式开关＋配方卡 pill（五类宣发任务作分组维度）。
  3. **继续上次工作**（D-126）——最近会话／未完成任务。
- **决定**：Miora 的「项目列表」层**下沉为第 3 段一条，不占主屏、不单独占路由**。
- 原因：Miora 登录后分 `/workbench`（项目列表）与 `/`（创作）两个路由，是为并行项目多的设计师设计；美业商家单店、任务少、Day-0 零资产，多一路由即多一跳，违反 D-043 ≤2 击。D-126 原文「首页构成＝热态轻推荐卡＋继续上次工作／冷态示例店预览」本就未给项目列表留主位。

### ② 一级 tab **维持输出类型轴，不跟领域轴**；但采纳其第二层 Skill pill 形态

- **决定**：D-139 的输出类型轴（文案／图文／视频）**不改**。**不采纳** Miora 的领域轴一级 tab（品牌设计／影视创意／电商广告／互动游戏／网页应用）。
- 原因：领域轴对 Miora 成立因其跨行业服务；**本项目只服务美业单一领域，领域轴退化为只有一个值的空轴**。商家真实分叉点是输出物形态。
- **决定**：**采纳** Miora 第二层的 Skill 快捷 pill 形态——其 pill 按**成品类型**命名（品牌视觉全案／IP 设计全案／TikTok 带货短视频），与本项目「配方卡目录＋五类宣发任务分组」（D-139）**结构同形**，可直接参照排布。

### ③ **额度预览卡扩容为「执行确认卡」；D-013 七类一类不动**

- **决定**：D-114① 单列的**额度预览卡（UserDebitPreview）扩容**，同时承载「用什么参数跑」（模型／比例／分辨率／数量）与「这一跑花多少」。**不新增卡类型，不合并 D-013 任何节点。**
- **决定**：**D-013 七类 HITL 节点全部维持原义，一类不动。** 经逐类核对，七类中**无一类**位于「生成前确认执行参数」这个位置：①明确不要求确认；②限定「一次只问一个阻塞问题」，塞入额度即破约束；③在生成后且明确「不再二次确认是否采用」；④「就地」语义排斥确认卡；⑤非阻塞；⑥保存时；⑦见下。
- **决定**：**不将执行确认归入第 7 类「外部行动批准」。** 第 7 类原文虽含「公开或付费副作用」字面覆盖扣额度，但其语义重量是**外部不可逆**（例：「确认发布到抖音账号 A」）；生成扣额度是**内部可重来**行为。套用发布批准的重量级审阅面板会使每次生成都成为一次发布决定，违反 D-043 ≤2 击与 D-117/D-122「介入位是修正点非审批墙」。**第 7 类维持原义，只管外部发布。**
- **决定**：执行确认卡的参数说明**用商家语言解释技术参数**——如「3:4 竖版，适合朋友圈/展架双用」而非「aspect ratio 3:4」。此为 D-116 在参数面的执行口径。
- **决定（2026-07-29 补，Codex 补测 Q1）**：执行确认卡**只读，不得在卡内配置参数**；参数修改只发生在更早阶段（意图层或输入框）。卡上只有「拒绝／确认」两个动作。
  - 原因：`[静态]` Miora v2 媒体工具契约明写「参数卡只展示，不在卡内配置」，Codex 判为**设计缺陷修复候选**（即其早期版本很可能允许卡内改，后改掉）。且卡内可配置即等于把执行确认变成配置面，**直接违反 D-159③「交互只能确认式不能配置式」**。
  - 放行凭证形态见 D-162④ 补充（协议层隐藏回包 ＋ 绑定 `tool_call_id`）。
- 依据：D-126 原文已定「**额度在『生成』处才消耗**」，确认时刻本就锚在生成点，与该卡位置一致。

### ④ **「记忆」升为一级导航**

- **决定**：门店层沉淀（记忆）**升为一级导航可见项**，不再仅存在于 D-162⑥ 的维护面内部。
- 原因：D-160⓪ 定护城河＝门店层＋由门店层归纳出的行业层。**护城河必须是商家一眼看得见的东西**——否则她感知不到「这个产品越用越懂我」，即失去留存理由；这也是本项目区别于通用 agent 的唯一**可感**证据（D-159①）。
- **决定**：记忆分域结构参照 Miora 四域映射：**门店主体偏好**（对应 MarketingIdentity，D-142）／**项目**（一次营销活动）／**工作流**（一条产线或配方）／**纠正**（`correction`，D-163② 已立为一等 kind）。
- 关联：D-162⑥「Skill 维护＝第八能力域」与 D-163②「维护面主职责＝确认系统提议的沉淀」在此获得前台入口。

### ⑤ 成品预览卡**补「后续动作 chip」**

- **决定**：D-114① 的成品预览卡在评价条（D-160③ 已定：复制／点赞／点踩／更多）之外，**增补 2–3 个后续动作 chip**（形如「换背景为深色版」「加上开业日期」「生成横版尺寸」）。
- **决定**：两者视觉层级分明——**评价条纯图标、无文字标签、紧贴文案末尾、轻到可忽略**；**动作 chip 在其下方独立成组、是明确可点的下一步**。
- 原因：两者服务不同对象——评价条给系统质量信号（喂 D-160⓪ 归纳路径），chip 给商家即时价值。chip 把「调整」这一高频动作从「商家自己想怎么描述」降级为「点一下」，对不擅表达的商家价值高于评价按钮。

### ⑥ 就地纠偏的额度可见性：**按「确定性编辑／再次调用生成」两分**，机制＝**本次动作成本即时反馈**（2026-07-29 依实证重写）

> ⚠️ **本条原方案（「维持不加确认卡 ＋ 创作面常驻余额位」）已被实证推翻，此处为重写后口径。** 原方案见 `references/analysis/miora-teardown-2026-07-29/04-HOMEPAGE-ALIGNMENT.md §5.4`，**不得再引用**。

- **决定 A ── 两分处置**：D-013 第 4 类「就地纠偏」按其**是否再次调用生成**拆两类，**分别处置**：
  - **确定性编辑**（直接编辑文本、undo、恢复 AI 版等不调模型的动作）——**维持 D-013 原义，不加任何确认，不做成本提示**。
  - **生成型改版**（自然语言改写、快捷动作触发重新出图等**会再次调用模型**的动作）——**走 D-164③ 的执行确认卡**，与首次生成同一形态。
- **决定 B ── 成本反馈机制＝「本次动作成本」，不是「总余额」**：生成型动作结束后（含**被拒绝**的情形），必须**就地、即时**反馈本次实际消耗；**总余额常驻位不足以承担此职责，不得作为唯一手段**。
- **决定 C ── 规划阶段的消耗必须计入并反馈**：确认卡弹出前的规划推理**已经产生真实成本**，即便商家在卡上点「拒绝」也已发生。此成本**必须计入并明示**，不得让商家以为「拒绝＝没花钱」。

**原因（`[实测]`，Codex 补测 Q3/Q4，`03-CODEX-FINDINGS.md §3 §4 §10`）**——原方案「常驻余额位」的失效在 Miora 身上已被完整观测到：

| 观测点 | 顶栏余额显示 | 实际 |
|---|---|---|
| 提交改版前 | `709.00` | `709.00` |
| 参数确认卡弹出时 | `709.00` | 规划成本**已在扣** |
| **点「拒绝」** ＋ 重载工作台 | `629.35` | **已扣 `79.65`** |

**商家点了「拒绝」，以为没花钱，实际扣了 79.65。** 且参数卡**不显示本次预计消耗**，会话内余额**不即时刷新**。

同一失效模式在创建 Skill 上复现：`797.47 → 709.00`，**扣 88.47，会话内不刷新，删除 Skill 与项目均不返还**。

> **这条同时推翻了 Codex 任务书自身的一个预估**（「创建 Skill 应当免费」）——**创建对象免费 ≠ 创建过程不计费**。凡经由 agent 规划完成的动作，规划本身即成本。

**与 D-117/D-122 的关系**：本条**不违反**「介入位是修正点非审批墙」。生成型动作本就是一次新的执行，走执行确认卡与首次生成同构；确定性编辑仍然零阻塞。**变的不是阻塞与否，而是成本的可见时点与粒度。**

#### 补充裁定（2026-07-29，用户拍板）：决定 C 与 D-109 的冲突消解

- **冲突**（#261 开工门检查发现）：决定 C 依 Miora 实证要求「规划成本必须计入并明示，不得让商家以为拒绝＝没花钱」；而 D-109 规定内部 Planner/LLM/评估/重试成本**全部进 ProviderCost 台账、不重复扣用户、供应细节永不进前台**——本产品计费模型下，商家点拒绝时**真实消耗就是 0**。照决定 C 字面实现「金额＝真实消耗」等于凭空造一笔不存在的扣费，反噬 D-109 的可信度基础。
- **裁定**：**D-109 不动**。决定 C 在本产品的执行口径＝①规划成本**计入 ProviderCost 台账**（D-109 既有合同，永不进商家额度桶）；②商家侧**明示＝拒绝后就地显示「本次未消耗额度」**——成本可见性的诚实兑现：真实数字是 0 就明示 0，不造扣费、也不留「是不是偷偷扣了」的猜疑空间。
- **证据前提限定**：Miora 失效模式的病根是「规划扣用户余额却不告知」，在本产品结构上不成立；决定 C 的实证依据据此限定为「成本可见性必须明示」，不携带其计费模型。
- **边界**：若未来要把规划成本计入商家桶＝修订 D-109，须另立决策，不得在实施票内让步。执行落点＝#261（发现 lane），决定 B 的「本次动作成本即时反馈」在拒绝分支按本裁定取值。

### 影响

- **D-114**：三层结构与七类卡**不推翻**（Miora 实测形态与之几乎逐项对应，构成外部交叉验证）；两处增补——额度预览卡扩容为执行确认卡（本条③）、成品预览卡补动作 chip（本条⑤）。引导补问卡按 D-163① 已补的三项（整组可跳过／每项带「暂未确定」兜底／一次性问全）实施。
- **D-126**：推荐内容合同（三要素、一天一个、点击预填不自动执行、走 D-116 拟人化）、v1 推荐依据（确定性规则＋行业先验，admin-config 受控）、Day-0 示例店（三行业、`platform_sample` 永不进真实 ContextBundle、示例任务真实执行扣试用额）**全部一字不改**；本条只补其在整页中的位置与上下段关系。其兑现仍依赖 D-162⑤ 遥测与 D-160③ 评价信号（D-162 影响 4 已记）。
- **D-139**：输出类型轴**不改**；配方卡目录的前台排布形态按本条②参照 Miora 第二层 pill。
- **D-125**：Landing Page 不受本条影响。
- **D-103**：自由创作线（模型直调＋参数面板＋生成）不受本条影响，维持 D-114「自由创作不换」。
- 实施：Dashboard 挂载归 D-127 的 **NEW 桶**（「Dashboard 首页挂载」已在其列）；`TodayRecommendationCard`／`ExampleStorePreview` 按 D-126 转正口径挂载。

### 证据边界

- Miora 的排布与交互形态为 `[实测]`（两轮：`01-RUNTIME-OBSERVED.md` 主控实测 ＋ `03-CODEX-FINDINGS.md` Codex 补测）。
- D-013 七类的逐类核对为**原文核对**，非推断。
- 「领域轴对本项目是空轴」「商家单店任务少」为**产品判断**，未经真实商家验证。
- 本条①②④⑤ 为**产品裁定**，无实现证据；③的「只读不可配置」与⑥的全部内容有 Codex 补测实证支撑（见各条内引用）。
- **⚠️ 本条①的立论前提已被补测削弱**：`[静态]` 当前 `/workbench` 与 `/v2/workbench` 渲染**同一个组件**（`Fce`，chunk 明写 `workbench-v2-page`），`/file/:id` 与 `/v2/file/:id` 亦然。**「线上两代页面并存」不成立**——路由并存属实，但不是两套当前实现。故**本条①「Miora 分两个路由」的表述应理解为「存在两个 URL」，不构成「他们认为该分两页」的设计证据**。①的裁定依据回落到 D-043 ≤2 击与 D-126 原文，**该依据独立成立，结论不变**。
- **Miora 当前 v2 一级导航实测为「创作／灵感／技能／记忆」**，Skill 与记忆**均仍是一级入口**——本条④的方向获同向佐证，但 Codex 明确「不能宣称这是经历史改版验证的收益」。

### 待验证

- 三段结构在移动端的信息密度与滚动体验（与 D-114 待验证「移动端对话流＋卡片可用性」合并）。
- 执行确认卡的**触发条件未定**：是否所有生成都拦，还是仅超过成本阈值时拦。与 D-162③ 循环预算上限、D-163 待验证同项合并处理。
- 记忆一级导航的**内容密度**：门店层沉淀在早期近乎为空，一级入口点进去是空页的冷启动问题未解（与 D-126 冷态机制可能需要同构处理）。
- 动作 chip 的**生成方式未定**（由模型即时生成，还是配方声明的固定集合）。`[实测]` Miora 的 chip 点击**只填充输入框、不自动发送**——与 D-126「点击＝预填充 Composer，非自动执行」同构，**该交互口径获实证支持**，但 chip 内容的产生方式仍未查清。
- ⑥ 决定 B 的**反馈形态未定**（成品卡角标／消息尾行／Toast），商家语言表述归 D-124 R 门⑤一并出。
- ⑥ 决定 C 的**规划成本口径未定**：是全额计入还是折算，以及被拒绝时是否提示「本次规划已消耗」。**须与报价模型一并定**。

#### 补测已答掉的旧待验证项（2026-07-29，留痕）

- ~~就地纠偏是否存在行业惯例上的二次确认~~ ── **已答**：`[实测]` Miora 在「询问执行」模式下，动作 chip 触发的生成型改版**会二次确认**（Image Buddy 接手后再弹参数卡，字段 `Pic2 high / 3:4 / 2K / 1张`，操作为拒绝／确认）。**与本条⑥原方案冲突，⑥已按实证重写。**
- ~~常驻余额位的商家语言表述~~ ── **该方案已废**，见⑥重写说明。

### Supersedes

无。本条为 D-114／D-126／D-139 在 Dashboard 整页层面的合成落位，不改写三者任一决定；D-013 七类明确维持原义。

## D-165 模型装配路径：Skill 声明能力不指定模型、三轴钉扎、位点级能力路由

- 日期：2026-07-29
- 状态：`accepted`
- 背景：D-149（提示词入 Langfuse）、D-044/D-129（模型 catalog 为运营参数）、D-162⑥（Skill revision 快照）三条**各自成熟但从未合并定义**。本条回答「Skill 的生成/调用/复用全流程中，LLM 如何装配、如何调整」，**不新建任何模块**，只定三条既有版本轴的合并规则。

### 现状盘点（`[实测]`，读码所得）

三条版本轴均已存在：

| 轴 | 实现 | 现状 |
|---|---|---|
| **提示词** | `HARNESS_LANGFUSE_PROMPT_NAMES` 14 位点（`harness/intent-naming`、`brief-copy`、`brief-image`、`brief-video`、`fact-satisfaction`、`fact-criticality`、`copy-candidate`、`note-plan`、`note-text-block`、`note-consistency`、`destination-mapping`、`copy-generation`、`platform-adaptation`、`text-response`） | 钉扎结构已含 `name/version/content/contentHash/label/source/isFallback/fallbackReason` |
| **模型** | `CatalogRevisionRegistry` 五类 revision（Capability/Price/Route/ProviderProfile/ExecutionChannel），阶段 `draft→enabled→published→retired`，带 `ActivationEvidence`；`supply-registry/hot-assembly.ts` 已有 `AssembleCapabilityRequest→AssembledCapabilityBinding`、`RuntimeCapabilityMatchInput`、通道生命周期 | 成熟 |
| **配方** | Skill revision ＋ 冻结快照 ＋ OCC 围栏（D-038④） | 成熟 |

**关键事实**：`apps/core/src/p1/skills/foundation-module.ts` 的 Skill 版本契约要求 `instruction ＋ manifest ＋ prompt` 三者齐备，**不含任何模型字段**；且 `prompt` 的类型已是 `HarnessFrozenPrompt`（源自 `harness/langfuse-prompts.ts`），**天生即「Langfuse 位点的钉扎结构」而非自由文本**。

### 决定 ① Skill **只声明能力需求，不指定模型**

- **决定**：Skill 的 `manifest` 声明**能力需求**（声明式，如「需中文精确文字渲染」「需视觉输入」「需长上下文」）；**禁止**出现模型 ID、部署名、供应商名等命令式指定。具体部署由 `hot-assembly` 在执行时匹配 `RuntimeCapabilityMatchInput → AssembledCapabilityBinding` 得出。
- **原因三条**：
  1. **保住运营可调性**：D-129 已定默认模型为 catalog 运营参数（后台可换）。Skill 一旦写死模型 ID，运营换默认模型时所有 Skill 需同步改，D-044 机制失效。
  2. **商家不该决定模型**：D-160② 允许商家编辑门店层 Skill。模型选择牵涉计费、合规、能力边界，**不属于商家决策面**；若字段存在，商家侧编辑面就必须要么暴露它（违反 D-116/D-159③）要么特判屏蔽（增加分支）。
  3. **但能力需求必须可表达**：D-115 的 `exactText` 需要「中文文字渲染」作为选模型硬能力轴，该需求由配方产生、必须能向下传递。声明式恰好承载它。
- **同源原则**：此即 D-163① 「kind 去枚举化」的同一条原则在模型面的应用，也是 Miora `mcp__image_tools__list_model_caps`（agent 先查能力再决策）的服务端形态——**配方问「要什么能力」，系统答「用哪个部署」**。

### 决定 ② 一次执行 ＝ **三轴版本的一次钉扎组合**

- **决定**：任务启动时将 **`skillRevision` ＋ `promptName@promptVersion` ＋ `catalogRevision`** 三者一并钉扎进 Task 快照，绑定 DBOS `workflowID`（D-041）。**在途任务不受后续任何一轴变更影响**（与 D-038④ OCC 围栏、D-162⑥「运营改 skill ＝ 建新 revision」同源）。
- **决定**：三轴中任一轴发生降级或回退（如 Langfuse 取不到走 builtin），**必须在快照中留痕**——现有 `isFallback` / `fallbackReason` 字段即为此用，不得静默。

### 决定 ③ 装配时序（六步，不新建模块）

```
Skill 选中
 ① 解析 Skill.manifest        → 能力需求（非模型 ID）
 ② hot-assembly 匹配           → AssembledCapabilityBinding（catalog published revision）
 ③ 解析 Skill.prompt 的位点引用 → Langfuse 取 → HarnessFrozenPrompt（取不到走 builtin 并留痕）
 ④ 三轴钉扎进 Task 快照        → 绑 DBOS workflowID
 ⑤ 执行                        → 七门 check 在 'execution' 相位（D-162④）
 ⑥ 事件落库                    → 三轴版本号 ＋ 评价信号，同一条记录
```

### 决定 ④ 事件合同**扩为三轴全带**（D-160③ 补充的再扩展）

- **决定**：D-160③ 已要求评价事件携带 `skillId ＋ skillVersion ＋ 场景`；本条**扩为三轴全带**——再加 `promptName@promptVersion` ＋ `catalogRevision`。
- **原因（这是本条最实际的收益）**：**失败归因**。生成结果差，是提示词问题、模型问题、还是配方问题？**三者版本号不记在同一条事件里就分不开。** 且运营调完提示词后，历史评价数据若无提示词版本对照，**全部失去可比性**。
- **同 D-160③ 的补录不可能性**：历史事件缺字段无法回填，**必须第一版就带全**。

### 决定 ⑤ `prompt` 字段落地形态：**引用为真相源，全文副本降级为只读兜底**

- **背景**：D-162① 已裁「Skill 不承载基础提示词，字段语义收窄为引用 Langfuse 位点，禁止内联正文」。但现状 `HarnessFrozenPrompt.content` 存的是**提示词全文快照**——这正是 D-158 警告的「第三套知识载体」的实际形态。
- **决定**：**`name ＋ version ＋ contentHash` 为权威引用**，重放与审计以此为准；`content` 全文**保留但降级为只读兜底副本**，仅在 Langfuse 不可达时使用，且使用时必须置 `isFallback=true` ＋ `fallbackReason`。
- **禁止**：任何写入路径把 `content` 当作可编辑字段；Skill 编辑面（含商家侧）**不得暴露 content**。运营改提示词**只在 Langfuse 改**。
- **取舍说明**：纯引用方案（不存 content）会让 Langfuse 不可达时无法重放；纯快照方案会产生第二真相源。**取「引用为真相、副本为兜底」二者兼得**，代价是存储冗余——可接受，且 `contentHash` 可检出副本与上游漂移。

### 决定 ⑥ 14 位点的模型路由 ＝ **位点声明能力需求，同一套 hot-assembly 匹配**

- **背景**：D-129 定了「文案与内部编排判断位默认 DeepSeek（`deepseek-v4-pro`）；**需视觉输入的判断位除外**」，但**「除外」如何路由未给机制**。
- **决定**：14 个位点**各自声明能力需求**（如 `fact-satisfaction` 若需读图则声明「需视觉输入」），走**与决定①完全相同的** `hot-assembly` 匹配路径。**不建第二套位点级模型配置表。**
- **影响**：D-129 的「除外」由此闭合——不是列一张例外清单，而是让位点像 Skill 一样声明需求，由同一个匹配器解析。

### 影响

- **D-149**：14 位点口径不变；本条补其与模型轴、配方轴的合并规则，并把 `content` 的真相源地位交还 Langfuse。
- **D-162①**：四层知识载体分工的**落地形态**由本条决定⑤给出，该条「待验证①」（Skill 如何引用 Langfuse 位点的具体形式）**就此结项**——答案是 `name + version + contentHash` 三元组，版本钉扎发生在决定②。
- **D-160③**：事件合同由三字段扩为五字段（本条决定④）。
- **D-129**：「视觉输入位点除外」的路由机制由本条决定⑥闭合。
- **D-115**：`exactText` 的「中文文字渲染」硬能力轴，经由 Skill manifest 的能力声明向下传递至 hot-assembly，**路径就此打通**。
- **D-163②裁定C**：美业版 skill-creator 作为平台层配方，其自身运行同样走本条六步时序——**沉淀配方的那次会话，本身也是一次三轴钉扎的执行**。
- 实施：本条**不新建模块**，全部落在既有 `model-supply` / `supply-registry` / `harness` / `skills` 四个模块的接线上，归 D-127 的 **KEEP+EXTEND** 桶。

### 证据边界

- 三条版本轴的现状为 `[实测]`（读码：`harness/langfuse-prompts.ts`、`model-supply/catalog.ts`、`supply-registry/hot-assembly.ts`、`skills/foundation-module.ts`）。
- 「Skill 契约不含模型字段」为 `[实测]`（`foundation-module.ts:89` 校验只要求 `instruction/manifest/prompt`）。
- 决定①～⑥ 均为**产品与架构裁定**，无实现证据。
- **`hot-assembly` 的内部匹配行为未读**——只读了导出面。故「复用它即可承载能力声明匹配」为 `[推断]`，须在实施首票中先读其匹配逻辑再确认，**不得按已具备对待**（此为 D-162⑤ 对 `fact-satisfaction` 同类处理的一致纪律）。
- Miora 的 `list_model_caps` / `update-subagent-models` 为 `[实测]`，但仅作**形态参照**，其内部机制未查清。

### 待验证

- `hot-assembly` 现有匹配输入是否足以表达 D-115 的能力轴（中文文字渲染等），不足则需扩 `RuntimeCapabilityMatchInput`。
- 能力声明的**词汇表未定**：能力需求用什么枚举/标签体系表达，与 `CapabilityRevision` 现有字段如何对齐。**这是本条落地的第一个具体设计项。**
- 三轴钉扎在 Task 快照中的**存储形态未定**（独立字段 vs 一个 `assemblyRef` 结构体）。
- 决定⑤ 的 `contentHash` 漂移检出**何时触发、检出后如何处置**未定（告警？拒绝执行？降级？）。
- 14 位点中**哪些实际需要视觉输入**尚未逐位点确认（D-129 只给了原则）。
- 子 agent／多位点若将来出现**同一任务内不同模型**的需求，钉扎粒度是否需要下沉到位点级（当前按任务级钉扎）。

### Supersedes

无。本条为 D-149／D-044／D-129／D-162①⑥ 在模型装配面的合并落位，不改写其任一决定；D-162① 的「待验证①」由本条决定⑤结项。

## D-166 通电工程定案：Skill 层契约接线、判定类 fail-closed、验收以行为为证

- 日期：2026-07-29
- 状态：`accepted`
- 背景：Mastra 文档双信源调研（654 页镜像 × Claude 15 切片 × Codex 15 切片，汇总 `references/analysis/mastra-docs-2026-07-29/02-SYNTHESIS.md`）+ 我方代码七条假设逐条核实（`01-REPO-VERIFICATION.md`，3 确认 / 3 推翻 / 1 改写）。诊断：**「建好不通电」不是普遍病，是聚在 Skill 层的局部病**——红线门禁（`validateHarnessPolicy` 生产真调用 3 处）、幂等核心面（`putOnce`）、SDK 落库解耦均健康。另一条结构性结论：**「注册即通电」对我方不成立**（`@meiye/core` 是被 `@meiye/web` import 的库形态；Mastra 语料明证约定发现与自动暴露在库消费下失效）——正确路线 = **未通电可被构建/启动期检测并硬失败**。

### 决定 ① Skill schema 契约接线（不删除）

- `inputSchemaRef` / `outputSchemaRef` 的 ref 名解析到 `packages/contracts` 的 schema 注册表：新建 ref→zod 索引（命名空间 `skill-input.*` / `skill-output.*`，带 `@版本`）；`validateManifest` 从「只查非空」升级为「**必须可解析，解析失败即受理拒绝**」；invoke 路径在回执落库前用解析出的校验器验 `output.value`。
- `SkillOutputValidator` 提供注册表背书的生产实现，结束全仓零实现状态。
- 依据 `[实测]`：contracts 已有 293 个 zod schema，缺的只是「ref → schema」索引；此前五层全空（ref 名虚构 / 受理只查非空 / 运行期只比字符串 / 校验器零实现 / invoke 零生产调用方）。
- 断言前置纪律（抄 Mastra `requestContextSchema`）：校验发生在**任何昂贵动作（LLM 调用）之前**；失败行为按位置分档——能抛的抛，工具位返回错误对象，不统一吞掉。

### 决定 ② 删除 planner 握手

- `plannerSelectedSkillRefs` 全链路删除（`skills/types.ts` / `service.ts` / `runtime.ts` / `harness/production-stage-ports.ts` / `workflow-core.ts`）；allowlist 归 system message 渲染；**`selected` 改为执行后事实**——从工具调用记录读出，不是执行前申报。
- 依据：全仓零生产写入方 `[实测]`；Mastra Skill 三工具 stateless 设计（「no activation state to track」）同向佐证。`planner_selected` 绑定模式随之退役，按 D-127 RETIRE 真删。

### 决定 ③ D-162④ 补充：`check` 三档 strategy + `onViolation` 恒触发

- `check()` 支持 `block` / `warn` / `detect` 三档；`onViolation` 与档位无关**恒触发**；warn 档进同一条 violation 事件流（携带 D-160③/D-165④ 事件合同字段）——**软提示必须留痕**，否则无法回答「软提示到底有没有用」（D-117 软提示闭环的数据基础）。
- 七门红线恒为 `block` 档且不可配置、不可采样（采样率恒 1.0），在类型层与可配置的质量指标分开。
- 变换类处理（改写/脱敏）不依赖 `onViolation` 作审计源，须显式发事件（Mastra 语料对 detect/translate 无明文触发保证的教训）。

### 决定 ④ 判定类能力 fail-closed

- **判定类能力（校验 / 评分 / 门禁）缺配置必须 fail-closed，禁止 no-op 返回通过**；no-op 降级只许用于观测/增强类能力。反面样本：Mastra rubric 缺配置 no-op 返回满分 1；我方同构 = Langfuse 运行期 warn+兜底照跑。
- Langfuse 三动作：（a）非 pilot 环境缺 `LANGFUSE_*` **拒绝启动**（pilot 保留 warn+兜底，符合 D-039 验证期口径）；（b）D-165② 冻结比对**纳入 prompt 轴**（现只比 Skill 三元组）；（c）`isFallback` 降级信号写入我方 Postgres 审计事件——**故障信号不再只发回故障源**（现状唯一消费方是 langfuse-sender）。
- 「上线完成」的定义含「**在 Langfuse 里看到这次调用**」，不是代码合入即完成。

### 决定 ⑤ 验收以行为为证

- 六格验收链固化为验收模板：`defined → registered → bound → exposed/reachable → invoked → persisted/traced`；新建能力全格取证、改动补受影响格；**前一态不得冒充后一态**（`plannerSelectedSkillRefs` = 卡在 bound/invoked 之间被当成完成的标本）。
- `production-wiring.test.ts` 重写：注册表快照断言（实际 vs 期望清单）+ **一次真实入口调用的行为断言**；删除全部 `readFile` + 正则断言。「门被调用」的举证标准 = 真实执行 trace 里出现该调用，而非源码里有这行。枚举盲区（闭包内动态部分）显式记账，不假装覆盖。
- 负向回归五件套（命名取 Mastra 自认的五类漏接）：`available-but-unbound` / `dynamic-not-in-inventory` / `inventory-blind-to-closure` / `invalid-shape-silently-inert` / `duplicate-authority-key`。
- 同名族 API 统一失败语义（必需依赖抛错 / 可选依赖显式 optional），不复制 Mastra 的混态（getAgent 抛错 / getMCPServer 静默 undefined / addGateway 静默跳过）。

### 决定 ⑥ 六原语落地吸收既有词汇

- D-162② 六原语落地**先做映射表**：`operation: 'generate' | 'edit'`（operations/types.ts）、`mode: 'revise'`（role-action-compiler）等既有词汇 → 原语的对应关系；旧枚举按 D-127 处置；**禁止并行新建第二套**。
- 依据：六原语生产出现为 0 `[实测]`（未落地非缺陷）；Mastra 15 条「第二套机制死亡清单」+ `AgentNetwork` 三次收缩归零——专用机制死因不是没人用，是通用机制长到覆盖它；同 `feedback-partition-by-semantics-not-files` 教训同源。

### 影响

- **D-161**：受理链新增「schema ref 可解析」硬条件。
- **D-162②**：六原语落地路径补「吸收既有词汇」前置（决定⑥）。
- **D-162④**：strategy 三档与七门恒 block 由决定③落地。
- **D-165②**：冻结比对扩 prompt 轴（决定④b）。
- **D-127**：planner 握手按 RETIRE 真删（决定②）。
- **D-149**：「全量入 Langfuse」的完成定义收紧（决定④c 及序列化截断核对，见工程票）。

### 证据边界

- 五层全空 / 零写入方 / 正则测试 / 红线通电为 `[实测]`（`01-REPO-VERIFICATION.md`，主控直接读码）。
- 接线方案与验收链为裁定，无实现证据；`hot-assembly` 匹配逻辑仍未读（沿 D-165 证据边界纪律）。
- Mastra 侧证据为 `[官方文档]`，行号见 `_digest-A/B/D.md`。

### 待验证

- ref→schema 索引的版本演进规则（schema 改版时 ref 的兼容窗口与迁移路径）。
- 快照断言的枚举盲区记账形态（哪些注册面天然枚举不到）。
- 「结构合规」独立命令是否立项（须先确认不触发 `locale:compile` 并发撞车——见工程票 P2 项）。

### Supersedes

无。D-162④ 由决定③补充；D-162③ 由 D-167 收口；其余为新增。

## D-167 有界执行定案：三闸门落数、触顶为可续挂起态、重试单一口径

- 日期：2026-07-29
- 状态：`accepted`
- 背景：D-162③ 已裁「有界自纠环」但**次数与预算上限未定**。双源调研证据：Mastra `.network()` 的隐式迭代上限被显式 `maxSteps` 取代（官方逐字「This replaces the implicit iteration limit」）——**边界存在、数值未定，会落回隐式默认，成本/延迟/终止行为全部漂移**；goals 预算耗尽转 `paused` 而非 failed（默认 50 轮，抬预算可续）为正例；`dountil` 官方示例在 condition 里抛错终止为反例（违反流程恒前进）。

### 决定 ① 边界落成数字进执行钉扎

- 一次执行的钉扎快照（依 D-165② 三轴之侧）新增有界字段：`maxIterations` / `maxCostCents` / `maxWallClockMs` / `maxDelegations` + `stopReason` + 超限降级行为。**全部为可审计事实，禁止隐式默认**。

### 决定 ② 触顶 = 可续挂起态，不是失败

- 撞任一闸门 → 产出「**当前最好结果 + 未达标说明 + 可继续**」，转 HITL 介入点（D-117 介入位=修正点非审批墙）；留痕记录**哪个闸门**触发。
- 自纠判定器缺配置时：自纠环显式关闭并留痕（不乱判、不假绿），主链路照常——与 D-166④ fail-closed 一致（自纠是增强类，关闭合法；判定是判定类，乱判非法）。
- 数值：首版 `maxIterations` 参考 50（Mastra goals 默认），其余以实测票定，**禁止拍脑袋数字直接进代码**。

### 决定 ③ 重试预算单一口径

- 默认关；模型 SDK / 自纠环 / 工具层三层重试**不得叠乘**（显式对齐为单一口径）；未知错误默认不重试；权限类失败（七门中 `subject_asset_rights` / `external_action_approval` 命中）**不进自纠环**，直接硬拦 + `ask_merchant`。

### 决定 ④ 失败姿态按爆炸半径分档

- 挂起路径 fail-open（校验失败也要让 run 挂起，否则 run 失联）；恢复路径 fail-closed（坏数据绝不推进）。**恒前进的边界是「不让坏数据前进」，不是「不让流程停」**——D-117/D-122 的精确化，写进 harness 合同。

### 决定 ⑤ 谓词位无副作用

- D-101 受控积木的条件/判断位（分支谓词、循环 condition）**禁止副作用**（写状态/发事件/挂起）——否则 durable 重放时谓词自身改状态，重放语义崩塌（Mastra 已把 `suspend()`/`setState()` 从条件函数参数里赶出去）。进积木合同与 review 清单。

### 影响

- **D-162③**：收口（形状与触顶处置定案，数值留实测）。
- **D-117/D-122**：恒前进边界精确化（决定④）。
- **D-101**：积木合同新增谓词纯函数约束（决定⑤）。
- **D-013**：额度预览卡（执行确认卡）与 `maxCostCents` 口径须统一（见待验证）。

### 证据边界

- Mastra 侧全部 `[官方文档]`（`_digest-A` C4/D11、`_digest-B` 07-D1，行号在 digest 内）。
- 我方三层重试现状未逐位点盘点，属工程票实施首步；闸门计数器落点未定。

### 待验证

- 三闸门计数器落点（DBOS step 内 vs harness 外层）。
- `maxCostCents` 与 D-013 执行确认卡的口径统一（预估 vs 硬顶）。
- 墙钟闸门与 DBOS durable 长挂起的交互（挂起时长是否计入）。

### Supersedes

无。D-162③ 的「未定」由本条收口。

- **变更记录（2026-07-29 当日，交叉审修正）**：口径统一为**四上限**——`maxDelegations` 为第四个独立上限（委派次数非迭代派生计数），标题「三闸门」为历史名；数值未标定的上限必须显式 `unset` 且拒绝进入要求该上限的生产路径（标定票 #255 为 #247 关票依赖）；与 D-166④ 的接缝显式化：「决定是否放行」的判定类缺配置 fail-closed，「仅决定是否启动增强自纠」的判定器缺配置=显式关闭留痕、主链照常。

## D-168 双源调研批复：P1/P2 决策包处置、记忆删除语义、D-013 扩类

- 日期：2026-07-29
- 状态：`accepted`
- 决定：
  1. **九个决策包按 P0→P1→P2 开工**：P0 两包（通电工程 D-166、有界执行 D-167）先行开工程票；P1 六包（HITL 深化 / 事件观测 / 记忆沉淀 / 模型供给 / 渠道发布 / 授权安全）候选留存于 `references/analysis/mastra-docs-2026-07-29/02-SYNTHESIS.md` §6-§10/§12，**随对应决策落地时并入，不预开票**；P2（运营控制台）记录待触发。
  2. **记忆删除语义采选项③（分离删除）**：删源对话不级联删记忆条目，记忆维护面显式标注「来源已删除」；记忆条目 / DecisionEvent / ApprovalReceipt / provenance 属不同删除策略，细则在包⑤落地时分列。依据：Mastra 对派生级联完全沉默——自动维护范式下删除无解，唯候选制让删除有明确落点（有身份、有属主、有状态的实体）。
  3. **D-013 扩两类节点**：（a）运行时缺参索取（无默认答案，与流内事实确认的本质区别）；（b）资源闸门触发的介入（预算耗尽问「要不要继续」，与 D-167② 挂接）。外部事件注入（世界→agent）**待外部通路真实存在再扩**（`feedback-nontech-audience-product-taste` 警惕无载体的想象功能）；现有七类补「载体」维度（对话内 / 任务卡 / 通知），不作为新节点类型。
- 原因：78 条候选一次性全开会稀释 P0 焦点；删除语义与 D-013 扩类证据已足无需再议，先定案防上下文丢失。
- 影响：D-013（扩类 + 载体维度）；D-163②（删除语义为候选制配套）；`02-SYNTHESIS.md` 为 P1/P2 候选池权威文档。
- 证据边界：全部为裁定；依据 `_digest-A` C4/C5/D15/D19、`_digest-D` 主控速览。
- 待验证：无。
- Supersedes：无。
- **变更记录（2026-07-29 当日）**：决定① 的「P1 不预开票」经用户同日指令调整——九包全量进 spec 并拆票；设计面采纳细目见 D-169。「随对应决策落地时并入」的原意（不让 P1 稀释 P0 焦点）由波次编排承接（P0 在飞、P1 分 W1-W3、P2 W4），非由「不开票」承接。

## D-169 九包设计面采纳定案：P1 六包与 P2 一包对既有决策的修订细目

- 日期：2026-07-29
- 状态：`accepted`
- 背景：D-168① 变更记录（用户令九包全量进 spec 并拆票）。本条把 P1/P2 七个决策包中**属于设计层**的主张正式并入各既有决策，形成权威修订记录；工程细目在 spec v2（`.scratch/spec-agent-substrate-2026-07-29.md` 决策 17-23）与 W1-W4 工程票。候选池权威仍为 `02-SYNTHESIS.md`；本条只列「修订了哪条既有决策、改成什么」，证据行号不重复（见各 digest）。

### ① 包③ HITL 深化 → 修订 D-162⑤ / D-163① / D-013 / D-114③ / **D-116（超时模型统一）** / **D-012（回执语义）**

- **D-163① `ask_merchant` 签名再补七项**（原三项之外）：出站 question schema 与入站 answer schema **两张契约**分离；回答服务端校验**默认强制**、失败转新追问；「跳过」为 answer schema 显式枚举值（`answer | {deferred} | {skipped}`）；无人值守路径返回可读 fallback 文本而非报错；**`options` 缺省 = 自由文本回答；`label` 是回传模型的值；`description` 只给人看不回模型**。原三规则（一次性问全/整组可跳过/每项带兜底）落地为行为验收项。
- **新增「条件性确认」承载位**：`confirm_*` 保持无条件形态，另立「执行体内按运行时条件挂起」路径（金额超阈值/首发平台等）；条件判断不交模型。`propose_*→confirm_*` 边界不变。
- **超时语义定案（统一 D-116 超时模型）**：超时 = 语义层默认回答（系统代答，仅限无对外副作用的问题），留痕区分商家答/代答；**不做载体层挂起过期**（过期→失败/取消是被禁形态）。D-116 的「确认卡超时自动继续（采用默认值）」判定为同一原则的既有实例，**不冲突、并入统一约束**：超时的唯一合法结果 = 采用默认答案继续；其两条安全边界（编辑中暂停倒计时／涉外部副作用或超额度不自动继续）推广到 `ask_merchant` 全域。
- **待确认状态持久化为硬要求**：重启/换设备后有效；渲染不了确认卡的渠道**挂起而非放行**（发布收口属硬门）。
- **拒绝三态**：approved / rejected+feedback / rejected 无 feedback → 停下等商家下一条消息（不让模型瞎猜）。
- **D-114③ 时间桥补 resume 合同**：resume 三元组 = `runId + step + resumeData`。

### ② 包④ 事件观测 → 修订 D-165④ / D-160③ / D-164⑥ / D-149

- **D-165④ 落地形态**：三轴为**三个扁平顶层键**（禁嵌套 `version` 对象、禁导出时回查补齐）；写入走「执行入口一次 + 声明式抽取」，子 span 显式传递。
- **D-160③「不得静默失败」的可执行形态**：埋点丢弃 = 一等事件（count/reason/来源，reason 分永久配置错 vs 临时故障），走与被丢信号**不同通道**；另设 last-success/queue-age 与业务事件↔trace 定期对账（drop event 只能证明已知丢弃）。
- **D-164⑥ 口径落地**：「本次消耗」从执行返回值直取（被拒也当场反馈），观测存储只做累计视图——两数据源分离，预算门不依赖异步指标。
- **D-149 完成定义收紧**：核对 Langfuse SDK 序列化截断（默认 1024 字符 ≈ 1024 汉字），「全量入 Langfuse」以配置证据为准。
- 观测设计交付「信号 × 目的地」矩阵为设计产物标配。

### ③ 包⑤ 记忆沉淀 → 修订 D-163② / D-160② / D-014

- **D-163② 裁定 A 的落地形状定案**：「抽而不存 + 持久化前拦截」；拦截点**四态**（放行/改写/作废/转候选待确认）；抽取失败逐条隔离。
- **候选证据链为硬要求**：每条 `propose_*` 带来源会话 ID + 源消息区间指针，确认界面可见出处——无证据链的 `confirm_*` 视为走过场。
- **沉淀入库为第二收口位**：红线校验在候选生成后、落库前（第一收口位=发布，D-117 不变）。
- **D-160②「不可导出」补齐落点**：查询 API 强制分页上限、禁全量语义——不能只靠 UI 不放按钮。**商家侧始终无导出端点**（D-160② 绝对条款不动摇）；将来若有合规/监管要求的平台侧导出，另行定义受权 actor、最小字段、目的与审计合同，不以「导出单独审计」名义稀释本条。
- **D-014 稳定结构补边界**：工作态日志（长会话续接用）与资产记忆两张表两套生命周期，前者不得流入后者。
- 删除语义已由 D-168② 定案（分离删除+标注），本条不重复。

### ④ 包⑥ 模型供给 → 修订 D-165①（词表）/ **D-058（渠道分层）** / D-129（D-044 仅涉套餐分档处衔接）

- **D-165 待验证「能力词汇表」定案**：A2A 三分骨架（协议能力=布尔／模态=MIME／业务标签=开放字符串）+ 第四类**模态限定子能力**（`image/*` 下 `cjk-text-render` 承载 D-115）；**词表带版本号**；不做通用能力代数——位点所需组合直接注册成轴；允许「未知」且未知走最保守恒可用分支；显式覆写凌驾能力推断。
- **`PriceRevision` 修订（归 D-058 多渠道分层，非 D-044）**：主键含 `ExecutionChannel`；价格分档（缓存命中/批量/长上下文/包量——套餐档与 D-044 试用套餐衔接）；容灾切换既是可用性事件也是计费事件。
- **D-129 容灾语义拆分**：「同模型换通道」（能力不变）与「换模型」（每档声明降级面）两类分开表达；**通道绑定能力一旦使用即禁跨 provider 容灾**或要求等价替代——防「中文渲染保证」被静默降级击穿。
- **catalog 治理两规则**：发现流只写 draft、生产冻结、永不自动 publish；租户级 allowlist（空=不限制；default 违规丢弃并告警，不拒启）。
- `AssembledCapabilityBinding` 留角色化绑定余地（primary/structuring）。

### ⑤ 包⑦ 渠道发布 → 修订 D-126 / D-114③ / D-116 / D-132；**衔接 D-128 注册通道口径**（按 D-155 冻结面切分）

**非冻结（本轮生效）**：
- **D-126 与 D-114③ 同源收敛**：通用「到期项扫描器」（到期时间+类型+载荷一张表）同时承载定时触发与拉回投递；任务保留期由商家离场时长分布倒推（成品保留不短于失败记录）；定时 Task ID 编入业务日天然幂等；触发前短路判断不产生空 run；定时推荐进会话带协议层结构化来源标注。
- **D-116 前台呈现补三条**：中文流式按词发射（`Intl.Segmenter`）；前台错误五态（业务失败/执行异常/传输中断/用户取消/门禁终止），门禁终止的 reason 为可操作建议；未知 chunk 类型透传+遥测+安全降级，**批准类事件绝不静默丢弃**。
- **流合同**：流绑 thread 不绑 HTTP 请求；reattach 与 replay 为两个分别验收的能力。
**冻结（设计约束记录，解冻时生效）**：
- **渠道层形态预定（D-155 解冻时生效，与 D-128 注册通道口径衔接而非修订）**：Provider（授权供给）/ Adapter（内容投递）两层分离，各自独立生命周期与错误路径——**授权层失败（过期/撤权→引导重新授权）与投递层失败（内容不合规→引导改内容）走不同前台引导**；`connect()` 返回 discriminated result，`installation` 一等持久实体（含 pending 态）。
- **D-132 凭证面补充**：缺租户标识 fail-closed，禁回落共享桶；**凭证按 workspace 分桶、逐类登记 scope 归属**；浏览器发布每商家账号隔离上下文。
- MCP 动态挂载与边缘无状态互斥记为 D-163① 落地约束；R2 链路按未通电对待直至有 round-trip 证据。

### ⑥ 包⑧ 运营控制台（P2）→ 修订 D-162⑥ / D-101 远期形态

- 积木代码侧声明可运营改的槽位，前端只读 + **服务端剥离越权字段**；每条「不可覆盖」必须有服务端行为测试（文档声明必然漂移——Mastra 同一快照三向自相矛盾为证）。
- 删除/退役前反向依赖查询；跨租户引用只给 `hiddenCount` 不给身份（D-127 RETIRE 执行前提）。
- 运营改动建模为有 `runId` 的可挂起执行（DBOS 承载）；结果体分离 `success`/`applied` + `validationResults` 一等字段。
- lifecycle status 与 traffic target 两轴分离，不引入双 Published。
- 待触发：configSchema 渲染表单（第 3 类积木出现时）。

### ⑦ 包⑨ 授权安全 → 修订 D-162④ / D-160② / **D-012**

- **D-162④ 补实现约束**：七门判据挂**所有调用必经的平台层**，禁止挂在任何可被 per-call 参数替换的载体上；门禁下沉到动作定义处 + 启动/CI 期覆盖度断言（缺门禁元数据即失败、默认拒绝）。
- **门禁入口清单覆盖续跑面**：DBOS resume/replay、**signal/queue 注入**、审批回注、订阅读取、clone/delete 与首调用同门——不只登记六原语首调用；signal/queue 是外部世界写入运行中流程的通道，漏登记即旁路。
- **HITL resume 重建执行主体**：`ApprovalReceipt` 一次性 + 有效期，不因持久化而继续可信。
- **D-160② 半可信主体补授权键**：工具授权 `(调用方, 工具)` 复合键，商家自建 Skill 不自动继承平台 Skill 的工具授权。
- claim ≠ grant；权限语法用 `propose_*`/`confirm_*` 前缀式分类，不用纯 allow first-match。
- fail 方向逐处决定（丢数据 vs 泄数据/越权）：allowlist 缺省 fail-closed；`check()` 红线判定禁用 fallback 档。

### 影响

- 被修订决策：**D-012** / D-013 / D-014 / **D-058** / D-101 / D-114③ / D-116 / D-126 / D-129 / D-132 / D-149 / D-160②③ / D-162④⑤⑥ / D-163①② / D-164⑥ / D-165①④——各条正文不改写，以本条为修订记录权威（同 D-165 对 D-149 的处理方式）。D-044 / D-128 为**衔接**非修订（交叉审修正：PriceRevision 渠道轴归 D-058，ApprovalReceipt 归 D-012，D-128 仅注册通道口径衔接）。
- spec v2 决策 17-23 为工程细目；W1-W4 工程票为执行载体。

### 证据边界

- 全部主张的语料证据在 `_digest-A/B/C/D.md`（行号实测）；涉及我方现状的均带「前提 + 核对动作」，未核实的不作事实断言。
- 包⑦ 冻结切分依据 D-155；包⑧ P2 定位依据 D-168①。

### 待验证

- 各包核对动作（七门入口唯一性 / 发布失败路径分支数 / 确认态落库位置 / SSE 重订阅能力 / provenance 列形态 / 记忆查询全量参数）在对应工程票首步执行。

### Supersedes

无。本条为九包对既有决策的合并修订记录。

## D-170 Pro Studio 全量退役（RETIRE）

- 日期：2026-08-01
- 状态：`accepted`（2026-08-01）
- 决定：将 Pro Studio（无限画布加购 / 专业工作区 / Canvas 服务 / 节点生成与画布 Agent）从产品与代码中 **全量退役（RETIRE）**。无新开发、新售卖、新入口、新 adoption；不设计无限画布或专业工作区接替。主线仅保留 **定制创作 + 自由创作（模型原生薄路径）**。历史已 adoption 的 ContentPackage **只读**；高用量 Pro / 套餐档位名 **≠** 已退役的 Pro Studio 加购。实施唯一入口：`docs/specs/pro-studio-retirement-spec-2026-08-01.md`（P0 文档 → P1–P6 代码/编排/数据 STOP-IO）。
- 原因：D-127 FREEZE「只留入口」与半死运行时造成引用漂移；parity/升单并行表述与当前双主线产品合同冲突；消除 Canvas readiness、加购、launch 与专属写路径的持续维护成本。
- 影响：文档五层同步（本条 + CONTEXT 墓碑 + 现行 spec rewrite + 旧文 supersede banner + AGENTS/DESIGN）；代码按退役规格真删 `apps/canvas`、core `pro-studio*` 树（先迁 KEEP 的 audio 能力）、Web 入口/加购/e2e、脚本门禁；专属表 STOP-WRITE / 产品面 STOP-READ，**不强制 DROP**。
- 证据边界：Wayfinder 地图 `.wayfinder/map-pro-studio-retirement.md`；审计 `references/analysis/pro-studio-retirement-2026-08-01/`；历史 evidence/旧 D 正文 leave-historical 不 bulk 改写。
- 待验证：无（产品决策已闭合；实施按规格阶段验收）。
- Supersedes（产品权威，**不改写旧 D 正文**）：
  - **D-127 FREEZE**（Pro Studio 行：全冻结仅留入口）→ RETIRE / 真删
  - **D-110 ③**（移出首发投入焦点但仍 keep 代码与付费墙）
  - **D-103** 中「若继续保留＝自由创作高级画布」的画布保留假设（定制+自由薄路径双主线 **不废**）
  - **D-099** 及 parity / K1–K7 现行执行入口
  - **D-075 / D-077 / D-084 / D-092** 入口族（Pro Studio 为必达/次级入口/专业横条等现行声张）
  - **ADR-0012** 作为现行法（升单并行工程 / FREEZE 只留入口）
- 不废：定制创作 + 自由创作薄路径、ContentPackage 唯一写口、Product Core、历史 evidence 事实、D-103 非画布部分。

## D-171 XHS 图文专项融合与全局 Agent 化工作台升级定案

- 日期：2026-08-01
- 状态：`accepted`（2026-08-01，wayfinder 地图九票全关后用户终审闭合）
- 决定：以 `docs/specs/xhs-vertical-integration-spec-2026-08-01.md` 为唯一实施入口，定案七件：① **工作台四态**（Idle/Active/Waiting/Delivered）＋文档时间线对话（AgentFrame 六族作实现注册表）＋宽度合同（对话 800／媒体 1240）＋轻胶囊建议行＋Activity Shelf；② **ContentPackage kind 产品口径三枚举** `media|copy|note`（note=图文复合：页组+封面+正文；旧 `image_text→note`、`video→media` 为兼容别名，起步不做破坏性迁移）；③ **执行确认判定升级为「操作是否触发付费媒体执行」**（D-164③ 实现口径由「仅 media 工作流路径」升级；纯 copy 免确认继承 D-043 不动；**note 过卡在 P1 激活**，与流内 AG-UI interrupt 呈现、e2e 合同同步一体落地）；④ **xhswork 九功能 8 adopt/adapt＋1 reject**（扫码发布出范围）；对象工作区三件套＝Tiptap 编辑器（选区 AI＋违禁词内联替换）＋手机样式嵌套笔记预览＋小红书 App 瀑布流样式嵌套封面预览；违禁词＝美业自建词库与红线门合流共库；⑤ **爆款复刻取材双轨**＝OpenCLI 用户自有登录态（live 门：真实账号跑通 note+download 一次核销）＋手动粘贴兜底；红线＝匿名抓取／逆向签名／账号池；⑥ **UI 实现基线**＝HeroUI Pro AI showcase 模板族＋assistant-ui 示例；零新 agent runtime（AG-UI/assistant-ui/CopilotKit 只抄协议与模式，DBOS/Task/ContentPackage 真相链不换）；Tiptap 只进对象工作区；⑦ **记忆产品化三步走**：P0 去 JSON＋待确认优先＋诚实空态、P1 三层页、P2 改名「经验」＋任务内三处露出。分期 P0/P1/P2 与验收门以规格 §8 为准。
- 原因：#281 有意遗留的 copy/note 集中开票、两版 codex UI/UX 方案对撞合流、xhswork 参考项目九功能盘点三线在同一张 wayfinder 地图收敛；两版方案对现状的事实断言经代码逐条核对基本属实（推荐卡硬编码 copy／双滚动／交付重复等）。
- 影响：P0 实施票 **#286/#287/#288** 已开（工作台收敛／记忆诚实／kind 合同起步）；文档同步＝本条＋CONTEXT 批次段＋PRODUCT/DESIGN 指针＋merge-ledger #281 行备注＋两版方案 supersede banner＋双 AGENTS 约束块；prompt 资产（xhswork 六文件美业化改写）挂 Langfuse 版本化（D-036）。
- 证据边界：wayfinder `.wayfinder/map-xhs-vertical-integration.md`（九票＋两丢弃式原型）；研究报告 `references/analysis/xhswork-integration-2026-08-01/01~03-*.md`；两版方案原稿 `docs/legacy-web-repo-UI-UX-Agent化调整建议0801.md`、`docs/美业宣发经营Agent-UI-UX调整建议-完整版0801.md`（已挂 banner）。
- 待验证：OpenCLI live 门已于 2026-08-01 核销（见下方实施核销补记）；其余 P0-P2 各期验收门（规格 §8.1-8.3）、kind 全链读侧收敛与 image_text 迁移方式仍按实施证据核销。
- Supersedes（产品权威，**不改写旧 D 正文**）：
  - **D-164③ 实现口径**「确认卡只挂 media 工作流路径」（#281 主控收窄裁决）→ 按分期升级为付费媒体执行操作判定（copy 免确认结论继承不废）
  - 两版 codex 方案 md 作为独立建议文档的地位 → 对撞结论以规格＋「锁定工作台 Agent 化目标形态」票为准
- 不废：D-043 copy 主路径免确认、D-164 单路由三段与执行确认卡统一边界、D-113 一主候选、D-114 对话流主容器、D-130 HeroUI Pro 供给基准（本条将其 AI 模板族升格为工作台 UI 基线）、D-126 首页轻推荐（形态演进为胶囊建议行）。

### 实施核销补记（2026-08-02）

- OpenCLI live 门于 `2026-08-01T20:53:37Z` 使用用户自有登录态完成一次真实笔记读取与一次下载，外部写入为 `0`；全程未做发布、评论、点赞或账号状态变更。
- live 门与 device bridge readiness 相互独立且均 fail-closed；fixture 浏览器证据只证明 bridge 合同、手动粘贴兜底与页面接线，不等同于上述 live 读取，也不证明已部署 companion 或 live provider。
- 本补记只保留脱敏计数、时间和行为结论；详细的脱敏完整性摘要仍见 #328 handoff，但任何文档都不登记账号、笔记 URL、token、原始内容或内部对象标识。

## D-172 积分制计费定案：周期积分＋加油包、模型积分定价、Waffo 支付切换

- 日期：2026-08-01
- 状态：`accepted`（2026-08-01，wayfinder 地图 #289 十二项开图决策＋六票全关后闭合；经 Codex CLI 对抗复核 11 findings 全数吸收出终稿）
- 决定：计费体系从「文案/图片/视频三桶条数额度＋三类条数加油包」（D-123 计量框架）整体切换为**积分制**，唯一实施入口＝`docs/specs/credit-billing-spec-2026-08-01.md`（实施票 #298–#302 五 lane，开发纪律与提交合并规范见其 §11）。要点：① 记账＝订阅周期积分（每月发放当月全额、月末清零；包年＝一次付 12 个月费用仍按月发分）＋加油包批次（购买即到账、按 SKU 效期、种子统一 7 天运营可改）；FEFO 统一扣序不分池；发分与支付事件解耦＝内部周期发分调度器单一真相（幂等键 `grant:sub:<subscriptionId>:<cycleIndex>`＋日对账告警）。② 套餐变更＝**升级当日清零重开**（加油包批次存活）、降级下周期生效、退订后加油包效期内照用其余权益回落默认态；同档换周期档下周期生效；续费失败 past_due 宽限 7 天。③ 运营定价＝CatalogModel×操作 8 种全覆盖（视频 15/30/60s 三档各一价）＋**模型级失败退还开关**（随上游是否支持失败退费而设，投影到前台报价提示）＋付费周期三档折扣系数（单月 1.00/连续包月 0.90/包年 0.75，后台可改）＋加油包 SKU 三要素全可配＋价格页参考数字面板（建议值/已发布值/偏离提示，前台只读已发布列）。④ 前台＝价格页方向 A 积分卡阵（积分大数字主角＋卡底「约可生成」小账仅供参考＋周期切换条＋加油包底部横条）、工作台三露脸位（余额徽章/报价 chip/不足拦截双出口）、积分明细页（批次+流水，失败退回与过期未入账行必须可见）。⑤ 支付通道＝**Waffo Pancake 切换、Creem 退役**（RSA-SHA256 平台公钥验签、加油包＝一次性商品走独立结算场景、trial 不建 Waffo 产品、退款本轮只落账+告警人工处置）。⑥ 账本＝复用 grant-lot/ProductUsageLedger 三件套 schema 级收敛为单资源 credits，**P1 唯一生产写入者、P0 product-service 整体退役只读**；余额检查+预扣+FEFO 扣减同一事务＋workspace 积分锁防并发超扣。数字种子（全部后台可改）：文案 1/音频 2/图 5/视频 50-90-160 分；trial 100 免费/starter 500 ¥199/growth 1300 ¥499/pro 2800 ¥899；加油包 100 分¥49/300 分¥139/1000 分¥429。
- 原因：三桶条数让多模型异构成本无法统一售卖与调价；积分制细刻度（文案 1 分＝全系统底线）给运营对照上游报价保留向上校准空间；支付通道切 Waffo 为用户拍板。
- 影响：文档同步＝本条＋CONTEXT 批次段＋PRODUCT 售卖口径指针＋DESIGN 价格页 UI 口径段＋归桶矩阵/landing 文案/供给清单 C-1/ADR-0016 supersede banner＋双 AGENTS 约束块；实施五 lane（L1 账本合同 → L2 运营面/L3 Waffo → L4 前台 → L5 退役验收），验收＝spec §9 十一门行为为证。
- 证据边界：wayfinder 图 #289（六票资解＝各决策唯一详情出处：#290 Waffo 调研/#291 条数触点盘点/#292 降级退订语义/#293 数值口径/#294 价格页原型/#295 Codex 复核）；价格页三方向丢弃式原型（方向 A 胜出）；Waffo 文档全量镜像＋SDK 实探＋CB-0（#297）五项人工前置已核销；Waffo 测试凭据＝`docs/_private/waffo.env`（gitignored，明文永不入库）。
- 待验证：Waffo 生产环境开通复验（§5.4 生产侧）；§9 十一门随 lane 交付逐门核销。
- Supersedes（产品权威，**不改写旧 D 正文**）：
  - **D-123 计量单位口径**（三桶条数额度＋三类条数加油包）→ 积分制；其机制精神全部保留（数字＝运营参数后台手填、缺额提醒超额报停、内部成本基准＋大冗余定价、视频＝高成本亮点＋代运营引流钩子）
  - **D-044 试用条数口径** → trial 积分一次性发放（`grant:trial:<workspaceId>` 每 workspace 终身一次）
  - **D-045 条数流水语义** → 计量单位改积分；FEFO/失败退额/兑换码/幂等骨架机制保留
  - **ADR-0016 计量口径**（三桶）→ 单资源 credits（账本分层结构与双真相原则不废）
  - 既有文档一切 **Creem 口径** → Waffo Pancake（D-124 R/E 门槛归属结构不变，E 门支付 provider 改判）
  - **C-1 三桶种子数字**（D-143 引 D-123 原文）→ credit spec §7 积分种子（admin-config 键 `plan.allowances.*` → `plan.credits.*`）
- 不废：D-061 双真相（上游 token/成本永不暴露商家面）、D-121 工作区套餐维度（1/2/3 个工作区）、D-124 门槛归属结构、D-156 公开页展示价＝文案资产、产出规格文言（条/张描述≠计费单位）。

## D-173 Day-0 首 token 显式操作预算 2→3（胶囊化 Composer 转正）

- 日期：2026-08-05
- 状态：`accepted`（2026-08-05 用户拍板「正式改预算为 3，落决策文档」；源起 #339 决策票——#337 胶囊迁移逐文件真跑发现 intent-routing 激活预算红，Expected 2 / Received 3）
- 决定：Composer 创作对口（lens）单选、Recipe 卡、destination chips 收进胶囊 popover（关闭时整体 unmount，实现批次内部标号 L3-2）后，纯自由文本路径 Day-0 首 token 的显式操作序＝「开输出类型胶囊(1) → 选对口(2) → 开始创作(3)」，**预算由 ≤2 击正式改为 ≤3 击**。Recipe 卡一击两用口径保留（卡在胶囊内：开胶囊(1)＋卡点击(2)＋开始创作(3)，同受 ≤3 击约束）。image_text／video 路径的更高预算（各 5 击：另含图文方向／Brief 确认与 D-164③ 确认执行）为既有合同不变（`tests/e2e/fixtures/ui-journey.ts` JOURNEY_CONTRACTS 已录）。
- 原因：胶囊化收纳换来首屏信息密度与移动端可用性收益；「lens 面常驻／默认展开以保 2 击」的产品改法被放弃（用户 2026-08-05 二选一拍板）。已 push 的 journey 合同表（`3cef0ee9`，copy=3／image_text=5／video=5）与本条一致——该表当时未落决策记录，属实现层静默追认，本条补齐产品权威记录。
- 影响：e2e 断言基线按 3 重立（intent-routing-http-sse 由诚实红转绿）；`tests/e2e/fixtures/user-activation.ts` 预算注释同步；D-043「最少显式操作出活」的原则与五节点纪律不变，Day-0 首 token 数字合同以本条为准。
- Supersedes（产品权威，**不改写旧 D 正文**）：
  - **D-043／D-074「普通路径到首个 token 不超过 2 次显式操作」数字口径** → ≤3 击（原则不变，仅预算数字）
  - **D-081 C6 对账句「纯自由文本路径『选对口(1)+开始创作(2)』恰 2 击合规」** → 恰 3 击（开胶囊+选对口+开始创作）
  - **D-098 C6 Day-0 点击计数口径（恰 2 次显式操作、零余量）** → 按本条 ≤3 击

## D-174 今日推荐行业层换源：行业＝门店档案属性，从档案读取

- 日期：2026-08-05
- 状态：`accepted`（2026-08-05 用户拍板「读取面换源」；源起 #330 AC3 消费者证明——lane-330b 代码考古＋实跑实证行业层 whyNow 对任何商家旅程端到端不可达）
- 决定：今日推荐 whyNow 的**行业层数据源改为门店档案的行业字段**：①封闭事实身份词表（`assertStoreFactMappings`）新增 `store-profile:industry` 身份（key `store.profile.industry`，值＝已发布行业 slug 或读时别名表可解析的中文标签，未映射值不炸、照常落 platform/weekday 兜底）；②today-recommendation 行业读取改为**档案优先**（既有 intent context 链保留为后备，d14f58ba 读时别名归一逻辑复用不动）；③门店档案/五步录入面补行业字段（可跳过——D-119 录入永不前置不变）。
- 原因：whyNow 行业文案（「结合本店护发与头皮护理…」）描述的是门店属性而非某次任务属性，档案是其语义正源。旧路径「问题卡→intent context」经实证结构性不可达：商家可写事实的封闭词表无行业身份（实跑 `STORE_FACT_MAPPING_INVALID`）→ D-111 恒吞卡；即使补供给，门店级单条事实命中「恰 1 条→系统代答」分支卡仍不呈现；流内作答不落库（task_requests 写一次）、补答 successor 以卡挂起为前提。三种复活旧路径的修法（门店级事实／项目级事实＋全局别名／流内落库）要么自相矛盾、要么建模错位、要么爆炸半径过大，全部弃用。
- 影响：#330 AC3 验收口径重述＝「档案填行业→首页行业层 whyNow」，实施票 #342；d14f58ba 在 `foundation-module.ts` 的过强可达性注释（"production actually writes…"）随实施订正。
- Supersedes：不改写旧 D 正文；仅订正 d14f58ba 实现注释的可达性断言。

## D-175 自由创作 grounding 分层：Core 放行门店/项目，保留权利/资质

- 日期：2026-08-06
- 状态：`accepted`（2026-08-05 深夜用户拍板「待拍板项都按你的建议选项继续」＝方向 A；源起 #345 反向复核——lane-343 实证 Core grounding 校验不认识 creationMode，每次提交都要求 `confirmed_store`/`confirmed_project`，Day-0 商家走自由创作一次也提交不了，D-111 的「先出活」承诺落空）
- 决定：Core 的创作 grounding 校验**按创作模式分层**——自由创作（free）只强制**素材权利与受监管资质**（`real_authorized_asset`/`confirmed_qualification`，合规底线无差别），**放行** `confirmed_store`/`confirmed_project`；定制创作（customized）维持全量 grounding 不变。前台两模式按下前行为不变（定制态事实卡前置、自由态零经营上下文），#345 已修复的 409 具名映射（store gap → 'store' blocker）保留为防御层。
- 原因：D-111 把自由创作定位为「引导不满足→自然路由过去先出活」的零经营上下文逃生口，D-103「自由创作不读取隐含门店」、D-119「录入永不前置」同向；Core 无差别强制门店/项目使逃生口名存实亡。方向 B（修订 D-111/D-103、前置门店门）被弃用——两个入口被同一道门挡死，逃生口失去意义。
- 影响：实施票 #348（Core `p1-model-policy`/grounding 解析面按 mode 分层＋消费者证明「Day-0 零门店商家自由创作提交→出活」）；合规红线（素材权利/资质）一字不动。
- **事实前提订正（2026-08-06，lane-348 实施穷举实证）**：`CREATIVE_GROUNDING_INCOMPLETE` 全仓唯一产出点在 `prepareCreativeJob`，可达路径只有 Result Center **重试**（`retryCreativeJob`）与 **result_adjust 遗留分支**；composer 主提交链（admission→冻结 snapshot→startHarness）**从不调用 groundingResolver**（postgres 契约测试 :1558 钉死「freezes the admission root without Product grounding」）。故「Day-0 商家自由创作一次也提交不了」表述过强——首次提交不撞门，撞门的是重试与调整链路。（同日补全：composer 首提经 `PostgresCreationSubmissionPersistence.reserve()` 的裸 INSERT 落 `p1_creative_works` 行，「基于此再创作」的派生第二轮经 result_adjust 遗留分支→`prepareCreativeJob` **真实撞门**——准确读法＝Day-0 free 商家**出得了第一活、续不上第二轮**。）**决策方向不变**：门在哪生效就在哪分层；消费者证明相应落在真实撞门的链路上（free 第二轮出活），不在恒绿旅程上伪造。
- Supersedes：不改写旧 D 正文；D-111/D-103/D-119 按原文兑现。

## D-176 失败创作的原地重跑能力：试点前不建设，诚实文案先行

- 日期：2026-08-06
- 状态：`accepted`（用户拍板「#354 按主控建议执行」；源起 #350——结果中心对 composer 失败作品渲染跑不动的重试按钮，拆除后失败旅程的唯一出口＝返回工作台重新发起）
- 决定：**试点期不建设「原地重放一次 harness 运行」能力**。失败 composer 创作的官方出口维持 #350/#353 合同：主动作「返回工作台」＋提示「请返回工作台重新发起本次创作」。同面板两处与此矛盾的软承诺（`stageSummaryFor`「生成失败，可恢复」／`costSummaryFor`「重新生成前会再次确认」，无 job 作品上均为空头支票）按 #353 的 jobStatus 分流范式改为诚实文案（实施票 #358）。
- 原因：①原地重跑是**新执行语义**不是缺陷修复——retry 管线（Operations Job）与 harness 作品互不相通（#348/#350 考古钉死），要做就牵动重放语义（同快照？新准入？）、计费口径（按次计费的重做）与 D-117/D-122 HITL 编排对齐，属设计票量级；②验证期优先最快栈，能力绑定触发点：试点中「失败后重发起」的真实频次与商家抱怨才是建设信号；③现有出口旅程完整且诚实，不是残缺 MVP——缺的是优化，不是闭环。
- 触发点：试点运营窗（#240）复盘时若失败重发起成为高频痛点，重开 #354 走 spec 流程。
- 影响：#354 关闭（记录触发点）；#358 实施两处诚实文案；#353 已修的 `recoveryHintFor` 为改写范式。

## D-177 standalone 工具支线清零：首批三工具剔除、Recipe Studio 页收敛、admin 整备波指针

- 日期：2026-08-06
- 状态：`accepted`（用户拍板于 wayfinder 地图「工具区编排的产品形态」#398→#418：「多尺寸适配 / 批量去背景 / 字幕擦除这三个都是无效功能，应该剔除，属于原历史决策没有及时更新」）
- 决定①（三工具剔除）：**D-092 首批 standalone 工具组合中的多平台尺寸重排与导出、批量去背景、视频字幕擦除与修复三项废止剔除**，连同整条 `toolEntryRefs` 链路（Core 静态种子/校验、Surface 契约字段、前台工具页签/路由/handoff、编排覆盖投影）一并拆除，实施票 #419。由于 Pro Studio 已由 D-170 全量退役，**D-077 双入口架构的 `standalone_tool` 支线成员归零**——该 kind 分类随 #419 从契约中清理，`composer_recipe`（含配方卡/能力包）成为创作工具的唯一入口形态。剔除依据：#402 资解证明该链四段建成但从未通电（`LAUNCH_TOOL_ENTRY_REFS` 恒空、全仓无非空生产者、前台覆盖分支从未执行、三工具 `capabilityPublished:false` 从未上线）。若相关能力将来回归，归宿是技能/配方轴而非独立工具区（小云雀实测佐证：同名三能力在其产品中均为 Composer 技能形态，#413）。
- 决定②（Recipe Studio 页收敛）：`/admin/recipe-studio` 独立页下线（admin 审计 §六 D3-b，实施票 #375），**D-101 的五阶段受控积木编译链（compile/validate/评测门）不变**，其 Web 唯一入口收敛为 Templates 页创作体验控件＋`RecipeStudioCompileInputAdapter`（#372）。D-127「NEW 桶：Recipe Studio」自本条起指编译链与 Templates 内入口，不得据其重建独立 Studio 页面。评测/内测门的证据服务签发见 `docs/specs/admin-backlog-2026-08-06/spec-i-recipe-evaluation-evidence.md`（⚠️ #374 禁用评测门期间 `switchProduction` 不可达，D+I 必须同批）。
- 决定③（admin 整备波指针，均与本文档既有决策对齐、无冲突）：admin 后台整备波产出的权威文件——审计与 D1–D9 决议（`docs/reviews/admin-config-audit-2026-08-06.md`，其 D1–D9 为审计局部编号，与本文档 D-xxx 序列无关）、九份实施 spec（`docs/specs/admin-backlog-2026-08-06/`，实施票 #359–#397）、评测证据生命周期规格（`docs/specs/evaluation-evidence-lifecycle-spec-2026-08-06.md`：到期自动重评＋失败才告警＋不自动下架）、商家技能选择零持久化规格（`docs/specs/merchant-skill-persistence-spec-2026-08-06.md`：与本文档「所有普通修改默认只影响当前任务」原则同构）。对齐核验：Spec A 单一 admin 角色 ↔ D-057「首期单一管理路径、复杂角色管理延期」与 D-101「不因此新增复杂 RBAC」；Spec A 封禁 impersonate 端点 ↔ 「未拍板前不实现 impersonation」；三条误报更正（p1 无商家 fail-open／Skill 白名单静默在运行时／presentationPolicy 绑定期已消费）为代码事实澄清，不触动任何 D 条目。
- 影响：D-092 部分废止（Pro Studio 项已先由 D-170 处置）；D-078 的「预设尺寸重排与导出」首页切片废止；D-093 的工具目录分类/排序/搜索范围收窄至 `composer_recipe` 一族；D-084 移动端目录容器合同不受影响（模板页签保留）。读 D-077/D-078/D-092/D-093 规划新工具能力者，须以本条为准。
- 证据边界：#402/#413/#418 资解（票内留档）；本条不动 D-101/D-107/D-108 的编译链、能力清单与 Skill 合同。
- Supersedes：D-092 三工具项与上下文 handoff 中对应三工具的引用；D-078 standalone 切片；收窄 D-093 范围。

## D-178 Agent-native 工作台 V3.1 权威规划：双 Harness、plan-as-data 与九张实施 spec

- 日期：2026-08-08
- 状态：`accepted`（用户全程拍板：V2/V3 交叉复核合并→V3.1 三轮评审修订→codex 交叉复核 U1–U12 逐项过→九 spec codex 复核轮 U13–U14；完整决策记录＝V3.1 附录 B 共 14 项）
- 决定：产品升级为 **agent-native 工作台**，实施与架构权威＝`docs/design/0808规划/meiye-agent-v3.1-authoritative-plan-2026-08-08.md`（V3.1，44 节＋附录 A 19 条硬约束＋附录 B 14 项决策＋附录 C 框架引用来源）。骨架：**双 Harness**——Agent Session Harness（AI SDK streamText 工具环，只读优先，Intent/Plan/Steering 低延迟层）＋ Production Make Harness（DBOS durable，付费副作用），两者以冻结 `ExecutionPlanSnapshot` 为唯一交接物（`approvalBasis: merchant_confirmed | policy_exempt_copy`，纯 copy 免确认不免冻结，U1/U9）；**plan-as-data**（`CompiledExecutionPlan`＝typed unit 列表＋依赖分组＋有界重试默认关，控制流留在 DBOS TS 代码，无 grammar 解释器）；AgentThread/AgentRun 一等对象＋三层语义事件（canonical/semantic/ephemeral）＋Thread-root Workstream 文档时间线前台；Progressive Plan Level 0–3；确认拆「待决请求（创建事务内先 reserve，U8）＋不可变决定」；Memory 平台五层认知分类＋authority 双通道＋生命周期分置存储（U5）；HarnessRelease 三对象（immutable Artifact/Lifecycle/Rollout，U10/U11）＋ L0/L0.5/L1 eval（U3/U12）；三 runner 收敛为单 executor，五阶段只留 trace taxonomy（V3.1 §22.4，与 D-036 口径一致）。
- 实施：九张 spec 编号 **#1–#9**（A 地基→B 会话与方案→C 确认与执行→D 交付→E Memory/Evidence 并行 lane→F Goal/Proactive→G Release/Eval→H 运营控制面单列→I Legacy 退役）；**票面真相＝本地快照** `docs/specs/v3.1-agent-specs-2026-08-08/`＋`docs/specs/v3.1-spec-{H,I}-*.md`。GitHub 侧：历史远程仓（legacy-origin-a／legacy-origin-b）已停用，一切以本地文件为准；原 legacy 仓 #429–#435 废弃不再引用。评审链：`docs/reviews/0808-agentic-plan-cross-review-2026-08-08.md`、`docs/reviews/v3.1-product-first-framework-benchmark-review-2026-08-08.md`、`docs/reviews/v3.1-codex-xcheck-2026-08-08.md`、`docs/reviews/v3.1-specs-codex-xcheck-2026-08-08.md`（27 findings 双反驳复核处置＋§6 裁决记录）。
- 影响：**ADR-0020** 为本条实施投影；ADR-0007（增补 Session Harness 层）、ADR-0013（五段执行拓扑由 §22.4 收敛路线接管）、ADR-0014（主容器升级为 Thread-root Workstream）头部已加 2026-08-08 修订注。过渡期新旧链共存，退役门＝V3.1 §35 批次 6（前置条件全满足才开工，U14 归档条件门）。
- 证据边界：Mastra 1.57 / pi.dev 0.84 / LangChain-LangGraph 1.x 三路实查引用逐条登记于 V3.1 附录 C（A1–C4→框架＋URL 对照）；借鉴均为实践参照，零新 agent runtime 引入。
- Supersedes：D-088（`/dashboard/recent` 收编为 Thread 列表投影）；D-016 部分（登记见 V3.1 §0.4）；ADR-0013 五段状态机的长期执行拓扑地位（trace taxonomy 化）。不改写旧 D 正文。

### D-178 权威勘误（2026-08-19）

- 状态：`accepted correction`；本节只修正 D-178 的决策引用，不改写上方历史 accepted 正文。
- 错误留痕：D-178 的 `Supersedes` 行与 V3.1 §0.4 曾把 D-088 写成「不新增 message/thread 实体」的决策。该引用错位；D-088 的真实标题与决定是「视频局部重生与完整视频重生均为新的用户生成任务」。
- 正确裁决：Thread 禁令的正确 supersede 目标为 D-046（其原决策明确 `thread-as-primary` 不做、不新增消息持久化实体）；V3.1 以 `AgentThread` 为一等对象时，只 supersede D-046 中这一 Thread/消息实体禁令，不改变 D-046 的 result 阶段自由调整与 derived revision 能力。
- Recent 入口：Recent 投影另引用 D-097；`/dashboard/recent` 收编为 Thread 列表投影是对 D-097 最近创作/可行动状态入口的承接，不是对 D-088 的处置。
- 保留语义：D-088 视频重生新任务与一次用户用量语义继续有效；用户主动提交局部/完整视频重生仍创建新 derived Task/GenerationJob，一个用户主动任务只产生一次幂等的用户用量预占/结算，系统内部重试不重复扣减。
- 同步落点：V3.1 顶部勘误 banner 与 §0.4 已按 D-046（Thread）/D-097（Recent）修正；D-088 不再出现在 V3.1 的 supersede 清单中。

## D-179 derived_revision 一律走报价工作流计费

- 日期：2026-08-25
- 状态：`accepted`（V31-45 收口；与 D-061、现行商家文案、现行实现对齐）
- 决定：Make Steering 的 `derived_revision` **SHALL 计费**。唯一生产路径是 `SteeringDerivedWorkflowCoordinator.launchDerivedRevision`：先 `quoteAuthority.resolve`，再 `billing.buildQuote`，再把该 quote 交给 Composer 准入／预留。禁止绕过报价的 ContentPackage 直写捷径（含 `derivedRevisionAuthority` replay、无 workId 静默 `'completed'`）。无 quoted consumer → `QUEUE_NOT_READY` 503；无 admitted `workId` → 409。
- 原因：商家 feeNote 已承诺「按正常生成一样算积分」（`projectSteeringImpact` 对 `derived_revision` 恒 `rebilled=true`）。一边承诺计积分、一边直写不计费会同时破坏 D-061 积分口径与配额扣减。方向 2（删捷径、统一走 workflow）已由 Task 8 血统落地于 main；本条把它钉成决策，而不是再开一条并行写入口。
- 影响：`consumeDerivedRevision` 只绑定 `actionConsumers.derivedWorkflow`。`derivedRevisionAuthority` 不得再出现在生产 TypeScript。V31-107 可以把 feeNote 从「按正常生成一样算积分」细化到按页数字，但不得把 `derived_revision` 改回不计费，也不得另算一套积分。
- 证据边界：`docs/ops/master-handoff-bug-batch-2026-08-25.md` §1；实现锚点 `apps/core/src/p1/agent-session/steering-service.ts` `consumeDerivedRevision`、`apps/core/src/p1/agent-session/steering-derived-workflow.ts` `billing.buildQuote` + `quoteAuthority.resolve`。
- 待验证：无（计费义务已定；按页数字文案由 V31-107 承接）。
- Supersedes：无。钉死 Task 8 方向 2 已合入 main 的事实，不改写 D-061／D-088／D-178。

已拍板转正：视频成片首发地位 → D-027；文案/成片两层交付 → D-028；Day-0 零资产首屏 → D-029；定位边界 → D-030；前台无槽位填表、结构化输入融入对话流 → D-031；Agent Workflow 编排总纲（收编原「阻塞作用域」「沉淀检测」两项为推论一/二）→ D-032；Task 统一交互单元与 Harness 五段式 → D-033（均 2026-07-17）；Harness 实现选型四题 + 工程约束（11 号简报全案采纳，提示词承载 = Langfuse 先行）→ D-034~D-038（2026-07-17 深夜，证据 = 10 份调研 + 9 路 Codex 对抗交叉验证（r08 三次容量失败未产出，Dify 在 D-037 中仅为战术搁置项，见其证据边界与 08 号报告头部横幅），`references/analysis/harness-research-2026-07-17/`）；09 合规章义务清单去向（2026-07-18 一致性复核 escalate 项）= 并入 Week 0 预登记文档 → D-039（2026-07-18）。

2026-07-17 合并评审识别的待拍板项已全部转正（即上列清单）；本行不作全局声明——2026-07-20 起新决策产生的「待继续拍板」项以各决策条内记录为权威（见 D-072 起各决策条内记录，现至 D-151；D-079 为跳号未使用）。

## 后续记录格式

```text
## D-XXX 决策标题
- 日期：
- 状态：proposed / accepted / superseded
- 决定：
- 原因：
- 影响：
- 证据边界：
- 待验证：
- Supersedes：
```
