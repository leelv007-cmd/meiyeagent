# Real run 0001

Status: incomplete and not counted.

The `provider-probe/` directory contains redacted, independently verified live
provider artifacts. A continuous merchant journey has not been accepted.

To resume, use real merchant facts plus an authorized real merchant photo,
confirm that a real reference-image provider channel is available, and rerun
the entire browser journey from registration through ContentPackage adoption,
three platform variants, and the content library. Do not splice the diagnostic
browser attempts into the final evidence.
