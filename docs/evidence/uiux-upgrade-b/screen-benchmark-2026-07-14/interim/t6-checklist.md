# T6 中期读图核查清单（controller 亲读 9 屏，2026-07-14 14:12 截图）

> 截图: `docs/evidence/uiux-upgrade-b/screen-benchmark-2026-07-14/interim/`（commit = T4 之后）
> 总观感：六主题方向全部成立——术语人话、去描边、温暖空态、单主钮、种子图入位。以下为收尾必修项（P=优先级）。

## P0（刺眼/可用性）
1. **S7 模型卡同图重复 ×4**：同类型四张供应商卡各渲染一张相同的大预览图（类型级映射 model-copy-planning.webp），视觉怪异且每卡 ~590px 高。改法：预览图移出供应商卡——类型说明带（"文案 LLM…"那条）右侧放一张小图（或卡内改 64-88px 小缩略图），供应商卡压缩为紧凑行（radio + 名称 + 徽标 + 两个默认按钮），radio/命令语义不动。
2. **S6 无内容态主次反转**：可用的「先去创作内容」是灰色次级钮，必然禁用的「记录私信线索」却穿主色。无可关联内容（现有 !contentId 条件）时：空态 CTA 升为该屏唯一 primary，表单提交钮弱化并强化 disabled 视觉。只换 variant/类名，不动 disabled 逻辑与 handler。
3. **S8 首屏底部动作块被固定底栏遮挡**：「拍摄素材」主钮在首屏折叠线处被底栏压住。移动内容容器补底部 padding（底栏高度 + safe-area），保证主 CTA 完整可见可点。

## P1（层级/一致性）
4. **S1+S8 场景卡无可见标签**：SceneVisualButton 的图上看不到场景名（引流·美甲等 chip 文案已有 i18n key）。图上叠底部渐变+白字，或图下加一行标签；对比度要够。
5. **S1 精选预设行无标题**：「选择创作方式」三张图浮在中间无名称。每张补预设名标签，整行与上下文左对齐。
6. **S3/S9 采集双入口主次不明**：两个入口视觉等权。按 T4 裁决给主项轻强调（桌面「上传图片或短视频」为主、移动「拍摄门店素材」为主）：主项 surface-2/图标主色/字重↑，次项 ghost 化。不引入第二个绿主钮。
7. **S2 标题重复**：「任务收件箱」Tab 选中态下方紧跟同名区块标题。去掉重复的区块标题行，保留描述句。

## P2（细节）
8. **资产空态示例图**：32px 太小且无可见示例标注。提到 ~48px 并叠小号「示例」角标（i18n key），消除"这是我的资产"歧义。
9. **S4 配额条语义**：「0 条内容」徽标与「内容 30/30」并存易误读。先查 quota-meter 的数据语义：若是"剩余/总量"，在标签文案加"剩余"（只改 key 值）；若是"已用/总量"则为 fixture 数据现象，不改，笔记说明。

## 观察但不动（记录）
- 全屏为暗色主题（与基线同配方），浅色 surface 阶差观察项继续留在 MAP，不在 T6 处理。
- 桌面任务面板展开遮挡（T4 遗留核查）：fixture 空闲态触发器隐藏，无法截到展开态；触发器已移侧栏 footer，面板从侧栏右侧展开，与 S5/S6 主表单区重叠概率低。留 MAP，真实数据阶段复查。
- S6 复盘笔记/保存复盘笔记（disabled）区块正常。
- brief 本体的三级跳/8pt/触区≥48px 全局项照常执行（见票 brief）。
