# V3.1 全量修复交接 Handoff（2026-08-09）

| Field | Value |
| --- | --- |
| 交接原因 | 用户要求暂停继续扩功能，先提交现有修改并交接后续 |
| 主仓 | `/Users/bin/Desktop/开发/内容无人区/美业内容2` |
| main 业务代码快照（handoff 提交前） | `151b184e9e0d6076723e314650ef84b5fa8a6a7e` |
| 总体状态 | **PAUSED / NOT MERGE-READY** |
| 权威审查 | `docs/reviews/v3.1-current-head-actual-completion-deep-review-2026-08-09.md` |
| 实施计划 | `docs/superpowers/plans/2026-08-09-v31-full-remediation.md` |
| 票面目录 | `docs/tickets/v3.1/` |

> 本文的首要约束：不得把“分支已提交”、“定向单测绿”或“Playwright spec 存在”等同于批次完成。付费、PostgreSQL、DBOS 恢复和浏览器主旅程必须在**最终合并态**重跑。

---

## 1. main 已落地内容

main 相对 `93cb4c55a` 新增：

```text
151b184e9 test(credits): freeze entitlement projection time
e952bb1bb test(ci): align release candidate trigger contract
a1170e284 style(web): satisfy required Biome gate
205851a0b test(v31): require browser acceptance in CI
232eca196 docs(v31): add remediation review and implementation plan
```

说明：

- Web Biome 存量基线已清零，root required 已纳入 Web check 和 V3.1 browser job 骨架。
- 修复了 `credit-entitlement-policy.test.ts` 把 `2026-08-08` 写死为过期日导致隔天必红的测试时钟问题；未改生产过期规则。
- **main 尚未合入下表任何高风险业务分支。**

---

## 2. 正式 worktree / branch 快照

用户要求暂停时，下列正式 worktree 已全部提交且 clean。

| 域 | Branch | HEAD | Worktree | 状态 |
| --- | --- | --- | --- | --- |
| Campaign U7 | `codex/v31-campaign-u7` | `2594754d7f72e6afc3af03d8037f77a8b2427542` | `美业内容2-v31-campaign-u7` | checkpoint；全链 Chromium 未绿 |
| Confirmation 历史线 | `codex/v31-fix-confirmation` | `b439ce99332392cecf31fd0f6e4d098f6d929abd` | `美业内容2-v31-fix-01` | 已被 S0 combined 线取代，不单独合 |
| Paid combined | `codex/v31-fix-paid-chain` | `16492f3232e85ff37d5f20d1f02367b227ea6be8` | `美业内容2-v31-fix-02` | 含 S0 `798707f82` + Campaign checkpoint；缺后续 WIP/Task3/Task7 |
| Interrupt / Fence | `codex/v31-fix-interrupts` | `293614ea8e2967113a0d5a5bb6d6199eef184980` | `美业内容2-v31-fix-03` | H 强旅程绿；D/E/F 仍未闭环 |
| Memory / Outcome | `codex/v31-fix-memory-outcome` | `beb1e3d5ef7cef6159ed646b9e4d8042bd6933e2` | `美业内容2-v31-fix-04` | 独立复审 P0=0/P1=0 |
| Artifact / SSE | `codex/v31-fix-artifacts` | `cf012a91ab5d7ade3648604d213baf8d58d8fcbf` | `美业内容2-v31-fix-05` | 独立复审 P0=0/P1=0 |
| Release / Ops J | `codex/v31-fix-release` | `d8dfbb4f09b8d6678c78f73f08b2c8a885479437` | `美业内容2-v31-fix-06` | **WIP checkpoint**；J 未绿 |
| Session / Plan | `codex/v31-fix-session-plan` | `096191cd77184d2f316cf01169405ec07790d5aa` | `美业内容2-v31-fix-07` | **WIP checkpoint**；最新 dirty 已保存，未重跑终态门 |
| Steering G | `codex/v31-fix-steering` | `354d5f3ff5725fdf65efe40486c4b6cc8879bb97` | `美业内容2-v31-fix-08` | Core/PG 绿；Playwright G 被上游初始任务阻塞 |
| Runner / Legacy | `codex/v31-fix-runner` | `fa4012c30c61840b4fecdf417b280656a2a85ead` | `美业内容2-v31-fix-09` | 独立复审 P0=0/P1=0 |
| CI 历史线 | `codex/v31-fix-ci` | `d412e1c91333307eebbf00f9c649d7bf1473460c` | `美业内容2-v31-fix-10` | 对应变更已以重放 SHA 进 main，不整枝再合 |
| S0 live follow-up | `codex/v31-s0-live` | `42f05cf2120e70537c05507da8b96b8d0d7bfac1` | `美业内容2-v31-s0-live` | **WIP checkpoint**；保存最后 start-receipt/crash-recovery 改动 |

另有 `codex/v31-date-gate` `61da95368c594c3735c2470e7ccbcf2f346942e8`，已以 `151b184e9` 进 main，不再合。

`/private/tmp/v31-format-base.wFRxvG/{base2,repo}` 是 detached 旧格式化/对照 scratch，仍有 dirty files；它们不是正式交付源，内容已被 Session/Plan 正式分支取代。**不要 merge，也不要在未确认归属时删除。**

---

## 3. 已经独立复审清零的域

### 3.1 Memory / Outcome（`beb1e3d5e`）

- `AsyncLocalStorage` 隔离并发 turn。
- server-owned confirmed-memory retrieval 在 Plan 前必定执行，不依赖模型自愿 tool call。
- memory statement 不原文泄露到对客成品；使用结构化 style constraints。
- receipt + outbox 同 PG transaction；commit-response-lost + clock advance 重试返回首次回执。
- Outcome signal 绑 exact ContentPackage revision，无法证明的历史记为 `unknown` quarantine。
- 真实 B2 Playwright 已进 required journey。

终审：P0=0 / P1=0。

### 3.2 Artifact / SSE（`cf012a91a`）

- authoritative AgentThread / Plan / Artifact lineage 可崩溃恢复。
- note/video successor 继承 artifactId/parentRevision。
- 非连续页 subset regeneration 使用真实 source-page 映射。
- SSE reject 与 clean EOF 都会 offline→backoff→replay→按 cursor 重连。
- 真实 PG/Core/Worker/Web/Chromium 旅程不 reload，无 `route.fulfill`。

终审：P0=0 / P1=0。

### 3.3 Runner / Shadow / Legacy（`fa4012c30`）

- 六原语 typed executor，不再由 record 回退执行三条旧 runner。
- 独立 frozen legacy module，不与 compiled business program 共真相。
- production legacy observation 由 durable writer 写入，shadow 只读，不二次调模型/付费 provider。
- 真 DBOS SIGKILL/restart 证明 business receipt exactly once。
- force-legacy note/media 无 nested durable step。
- no-history install ledger 与 admission 共享事务锁，防 false-zero。

终审：P0=0 / P1=0。

---

## 4. S0 / Plan / Confirmation 当前真实状态

### 4.1 已绿基线（`798707f82`）

已完成并有 fresh PG 证据：

- quote/fact/rights missing fail closed。
- live drift 追加真实 append-only MarketingPlan revision，重试不多追一版。
- successor confirmation 替换旧 hold，不重复全额扣分。
- different-successor 并发仅允许一个 predecessor lineage。
- ProductUsage / credit ledger / settlement 使用 successor operation。
- durable confirmation outbox 保存 authority digest requestId。
- 确认从未 dispatch 的 orphan hold 在同 PG transaction 中终态化 ProductUsage/Quote/credits。

该点曾有 Node 72/72、fresh PG 18/18、root contracts/core/web typecheck/build 绿。

### 4.2 `798707f82` 后的复审发现

不可直接合入 `798707f82`，复审发现：

1. reprice successor 后 DBOS interaction/settlement/shadow/sampling 仍有路径读旧 request/quote。
2. `quoteMissing` 在 mid-run fence 被记录但可能最终 `continue`。
3. pre-run snapshot verification 曾位于 billing compensation `try/catch` 外，可遗留 reservation。
4. `harness.start` 成功后、`completeHarnessStart` 前响应丢失，跨 deadline 恢复可能把已运行 DBOS 退款成 failed。

### 4.3 最新 WIP（`42f05cf21`）

`wip(harness): checkpoint repriced execution recovery` 已保存：

- effective successor quote mapper。
- success/failure/cancel/shadow/sampling 使用 effective request。
- `quoteMissing` mid/post-run safe stop。
- pre-run verify 移入 billing compensation 边界。
- `markHarnessStartDispatched` durable seam：外部 start 前先将 outbox 从 pending 标记 dispatched，expiry 只扫真 pending。

已知证据：

- effective quote mapper RED(r1)→GREEN(r2)。
- 旧 quote 拒绝 + successor 新 quote refund/commit 的 PG 定向绿。
- quoteMissing 域测试绿。
- start-dispatch Memory RED→GREEN。

**中断点：**真 PG “cross-deadline + start-response-lost → recovery exact authority”回归正在运行时用户要求暂停，未得到终态结果。`42f05cf21` 必须视为 WIP，不是 fix 完成证据。

### 4.4 Session / Plan WIP（`096191cd7`）

最后已知绿点是前一提交 `7fac92b42`：Core typecheck、focused 159/159、fresh PG 49/49。

`096191cd7` 另外保存了：

- prepare pending 后持久化 exact authority ID。
- start 前 durable dispatched marker。
- starter 返回 ID 必须与已持久 ID 一致。

**`096191cd7` 提交后未重跑终态门，必须在合入 S0 final 后重测。**

---

## 5. Interrupt / D-E-F-H 状态

### 5.1 已闭环

`496f3786c` 与 `293614ea8`：

- exact paid execution-confirmation decision target。
- pending interrupt 对 Composer input/submit/programmatic submit 硬门。
- interrupts 空列表时 schema/load error 仍可见。
- interrupt CAS 与 DBOS resume 之间使用 durable PG outbox/recovery，不再有 resolve 已提交但 workflow 永久挂起的窗口。
- rights live head 不回显 frozen ref，policy fingerprint 改变可 stale/reconfirm。
- H 删除直改 `p1_agent_interrupts` fixture；fixture 只推进 injected clock 并调真 sweeper。
- H 真 PG/DBOS/Playwright 已验：ProductUsage/Credit 全额退回，`core_hold_expired`，typed interrupt resolved，DBOS cancelled，白话卡，resume 409。

已知 H 证据：unit 5/5；real PG 5/5；DBOS 1/1；Playwright expiry 1/1（19.4s）；同轮 schema/stale/exact/duplicate/workspace list 2/2。

### 5.2 仍未闭环

- **D**：当前旧旅程是三页 `image_text` persistent conflict，不是票面要求的真实 video 时长/分镜/页级失败/积分/字幕或封面 assisted fallback。
- **E**：旧旅程未证明 stale 旧确认拒绝→真实持久新 Plan revision/diff→第二次 exact confirmation。
- **F**：旧旅程只证明 revoke safe-stop 文案，未证明换素材恢复和 ProductUsage/Credit ledger 无重复扣费。

必须在 S0 + Task7 + Runner 最终合并态重写并实跑 D/E/F；不得将旧绿测继续计为 §37.4 验收。

---

## 6. Campaign U7 状态

`2594754d7`（独立分支）/`16492f323`（paid combined 中的重放提交）已有：

- production Core route/assembly。
- durable PG lifecycle + claimed lease crash recovery。
- 可见 Composer Campaign panel/BFF/client。
- plan-only 确认前不创建 Work1，plan-only 0 reserve / 0 扣分。
- Work1 canonical delivered 后才允许 producer 生成同 `campaignPlanRef` 的 Work2。
- required CI/catalog/spec 已在该分支补入。

已知绿点：Core 4/4；real PG 1/1；Web 4/4；Core/Web typecheck；CI contract 10/10。

Chromium 实际结果：

- 可见 UI 创建 Campaign。
- plan-only confirm 成功，权威余额保持 100，reserve=0。
- Work1 真 task/work ID 已创建，Work2 未提前出现。
- 随后卡在 `chooseImageTextDirection` / `awaiting_answer`。
- 同一 HEAD 的普通 required `xhs-image-text-main-journey.spec.ts` 也在同点超时，因此判定为上游 Harness/renderer 问题，不在 Campaign 分支继续越界修。

**尚未在浏览器中观测到 Work1 delivered→Work2 出现→第二次 single_work 独立确认，所以 U7 仍不能判完成。**

复现命令：

```bash
PORT=3098 PLAYWRIGHT_CORE_PORT=4198 PLAYWRIGHT_PROVIDER_FREE=true \
MODEL_EXECUTION_MODE=fixture \
pnpm --filter @meiye/web exec playwright test \
  tests/e2e/specs/campaign-paid-work-confirmation.spec.ts \
  --project=chromium --workers=1
```

---

## 7. Steering G / Release J 状态

### 7.1 Steering G（`354d5f3ff`）

已有：

- server-owned task/thread/snapshot/progress binding。
- derived command/outbox 先 pending，result-adjust + quote + sole writer 成功后才 accepted。
- plan-change 只走 prepared successor + explicit confirmation，不改原 task freeze。
- source run 在 next-unit typed interrupt boundary 持久暂停。
- reject/expire 释放 successor 并恢复 source。
- 1:1 aspect ratio 转发到 quote authority。

已知定向：Core 180 passed + 1 PG skip；fresh PG 17/17；Core/Web typecheck 绿。

未闭环：Playwright G 在 steering 前就卡在旧上游 initial creation（submission reserved，freeze/request 空）。必须基于 S0/Task7 final 重放，不得用当前定向测试宣称 G 完成。

### 7.2 Release J（`d8dfbb4f0`）

`c48a7a4f3` 之前的生产逻辑已有：

- 真实 release-bound evaluator/UI，fixture eval command 已移除。
- concurrent rollback completion 幂等 + PG crash recovery。
- active `inFlightRunIds` 写入 rollback audit。
- required CI contract。

已知基线：Core 15/15；PG 2/2；CI 10/10；Core typecheck；Web Biome。

`d8dfbb4f0` 是未绿的 Playwright WIP，尝试从可见 UI 验证 publish→canary/non-canary→eval→trial→promote→in-flight rollback→new task pin。中断前最后产品阻塞是 durable task request 缺 `executionPlanSnapshot/harnessReleaseId`，必须在 S0/Task7 最终态重跑。

**`d8dfbb4f0` 未经提交后终态测试，只是 checkpoint。**

---

## 8. 推荐后续执行顺序

### Phase 1 — 冻结 S0 唯一真相

1. 从 `codex/v31-s0-live` `42f05cf21` 继续。
2. 完成并实跑：
   - cross-deadline + start-response-lost → deterministic recovery。
   - 已 dispatch run 不被 orphan expiry 误退款。
   - start 重放返回 exact authority ID，`completeHarnessStart` 幂等。
3. 重跑 effective successor quote success/refund/cancel + shadow/sampling。
4. 对新 HEAD 再做 Standards/Spec 两轴独立复审，P0/P1 必须清零。

建议命令（必须提供新鲜业务 PG）：

```bash
TEST_DATABASE_URL="$TEST_DATABASE_URL" \
pnpm --filter @meiye/core exec tsx --test \
  src/p1/agent-session/postgres-execution-confirmation.postgres.test.ts \
  src/p1/execution-spine/postgres-creation-submission-store.postgres.test.ts

pnpm --filter @meiye/core exec tsx --test \
  src/p1/harness/context-fence.test.ts \
  src/p1/harness/dbos-workflow.test.ts

pnpm --filter @meiye/core typecheck
git diff --check
```

### Phase 2 — 以 S0 final 重放 Session / Plan

1. 在 `codex/v31-fix-session-plan` 合入 S0 final，保留：
   - Plan append→freeze crash 从已提交 exact revision 幂等重建。
   - clarification resolved + quote/reservation/freeze/outbox 同事务。
   - r1→r2→r3 exact `creditUsageOperationId` lineage。
   - confirmed decision durable 在 start/dispatch 之前。
2. 重跑 Core focused + fresh PG 组合；`096191cd7` 必须被覆盖验证。
3. 运行一条真实 initial paid image-text journey，先消除 `awaiting_answer` renderer/start 上游阻塞。

### Phase 3 — 组装最终运行时后再验用户旅程

不建议盲目把所有 tip 依次 cherry-pick 到 main。Task3/Task7/S0 的旧历史有重复付费准入实现，必须由域主在新 integration branch 上解冲突。

推荐：

1. 从当前 main 建新 integration branch（不删任何现有 worktree）。
2. 先合 S0 final + Session/Plan final + Campaign `2594754d7`。
3. 再合已独审清零的 Memory/Outcome `beb1e3d5e`、Artifact/SSE `cf012a91a`、Runner/Legacy `fa4012c30`。
4. 把 Interrupt 分支迁到该最终 runtime；保留 `496f3786c`/`293614ea8` 的 outbox/H 逻辑，并重写、实跑 D/E/F。
5. 合 Steering，实跑 G；合 Release，实跑 J。
6. 最后更新 required A–K 清单/catalog，跑全门。

### Phase 4 — 最终门禁

至少要求：

```bash
pnpm typecheck
pnpm --filter @meiye/web check
node --test scripts/ci/*.test.mjs
pnpm --filter @meiye/core test
pnpm --filter @meiye/web test
```

以及在独立 PostgreSQL + DBOS system DB 上：

- core persistence suites，禁止 PG skip 充当验收。
- D/E/F/G/H/J 真实 Playwright。
- Campaign Work1 delivered→Work2→第二次 single_work confirmation。
- required V3.1 A–K 显式清单，禁止 optional assertion / `route.fulfill` / API-only 主旅程。

---

## 9. 不要重复踩的坑

1. **不要在旧基线反复跑下游 Chromium。** Campaign/G/J 已证明可被同一 initial paid Harness 阻塞；先冻结 S0/Task7。
2. **不要接受“文案里没有重复扣费”作财务证据。** 必须查 ProductUsage + credit ledger + exact operation。
3. **不要直接改 interrupt 表伪造 expiry。** H 已改为 injected clock + real sweeper。
4. **不要把三页图文失败冒充视频 D 旅程。**
5. **不要把 Campaign 两次普通 Composer submit 冒充 U7。** 必须同 campaignPlanRef/workOrdinal 且 Work2 由 producer 派生。
6. **不要用 `--list` 宣称 Playwright 绿。**
7. **不要整枝重合 `codex/v31-fix-ci`/`codex/v31-date-gate`。** 对应变更已进 main。
8. **不要删除任何现有 worktree。** 本次选择是保留现场交接，不是合并/清理。

---

## 10. 外部阻塞

V31-26b 真实商家 pilot / 历史 replay 观测窗口仍是外部运营阻塞，不得伪造为已完成。代码侧的 archive gate / no-history proof / rollback adapter 已有较强证据，但删除 legacy 必须等 pilot 与窗口真实满足。

---

## 11. 交接结论

- 现有正式 worktree 改动已全部提交，没有未保存的正式分支改动。
- main 保持低风险基线；高风险业务分支保留于独立 worktree。
- 可继续的唯一正确起点是：`codex/v31-s0-live` `42f05cf21` 完成真 PG/DBOS start-receipt 恢复，然后重放 Session/Plan，再组装下游旅程。
- 当前不存在可诚实宣称“V3.1 全部修复完成”的合并态。
