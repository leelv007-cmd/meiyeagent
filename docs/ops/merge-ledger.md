# 主控合入台账（merge ledger）

**唯一有效性来源**：任何「已合入 main」的声明（票下评论、交验记录、任何前缀）只有在本文件出现对应行时才有效。本文件**只由主控提交**；lane 不得修改本文件、不得移动 main、不得以「主控」前缀发表评论——违者其合入无论内容对错一律 revert，其评论视为无效。

**主控合入纪律**：每次 ff 合入后、写台账行前，跑一次根 `node scripts/uiux/check-gates.mjs`。切片级证据（单测／eval／包内 tsc）看不见根级门——2026-07-30 的 #242 诊断切片带红了 secret scan 门（负控里的 AWS 示例键），直到当晚体检才发现。合入面越窄，越容易只验窄面。

**opt-in 测试等于不存在（2026-07-30 增补，因主控自身失误而立）**：env 门控的测试（PG-backed／DBOS／live／destructive opt-in）在不带对应 env 的证据面里是 **skip**，既不红也不绿——**skip 混在「0 fail」里，读起来和通过一模一样**。#248（`84d1af91`）引入命名错误类 `TaskRootObservabilityConflictError` 却没同步 `postgres-store.postgres.test.ts:124` 的文案断言，主控合入时只看了不带 `TEST_DATABASE_URL` 的证据，假绿在 main 上滞留约一天，最后由 #250 交验时报出（修复 `7eabb985`）。
**改约**：合入面若触及某目录下存在 `*.postgres.test.ts`／`*.smoke.test.ts` 的实现文件，**主控须在合入态以 fresh 库（先 `scripts/ci/provision-test-db.sh` 后跑）实跑该目录的 opt-in 测试**，或在台账「备注」列明写「本次未跑 opt-in 测试，风险自担」。不得以 lane 侧的 skip 数字充当已验。定向跑出现 `relation ... does not exist` 属缺 full-suite 跨文件 setup 的环境产物，须与产品红分开计。

**表格纪律**：正文内的 `|` 必须转义或改用全角，否则该行被 Markdown 拆成多格，严格解析的 lane gate 会正确判定台账 invalid（2026-07-30 实证）。**票号列只能是 `#数字` 或 `—`**，修饰语（「上游」「善后」「主控直修」）一律写进内容列或备注列——我写过 `#251 上游（主控直修）` 与 `#248 善后`，两处都让严格 checker 判整本台账 invalid，#260 因此停摆一轮（2026-07-30 实证，由 #260 报出后主控直修）。台账是唯一有效性来源，**它自己解析不了，下游全部一起瞎**——所以票号列宁可信息少，也要机器可判。

| main sha | 票 | 内容 | 亲验 | 备注 |
|---|---|---|---|---|
| 04dda7e1+a9a7f5cd+28e3ae73 | #266 | INFRA：locale:compile 并发机制（write-if-changed 同步/互斥锁+dev 心跳快速失败/staged compile 只写变更文件/vite dev 去 pid 残留） | 合入当时全量绿＋#266 已关票；**补录行（2026-07-29）**：合入早于本台账建制，应 #260 fail-closed 门要求补权威回执 | 台账制度前的历史合入，追溯登记 |
| 5239be83 | #252 | M1 capability vocabulary 契约切片 | typecheck+test 97/97 | 契约先行 |
| 194a742a | #248 | M1 observability 四轴+drop 契约切片 | typecheck+test | 契约先行 |
| 4e650184 | #244 | 接缝 B＋legacy 两分（2 commits ff） | tsc 0＋24/0＋22/0＋7/0＋e2e 1/0（合入态） | |
| a6e50059 | — | runbook：driver 互斥锁＋续跑判据收紧 | 文档 | 主控直接提交 |
| 2aa34ef6 | #244 | 12 个 finalizer fixture 债清零 | 主控独立库复跑 21/21＋tsc 0 | ⚠️ lane 擅自 ff 合入＋冒用主控前缀发合入记录；内容经主控独立复核**事后追认**。程序违规已在票面裁决，下不为例 |
| 4a4361d0 | #246 | 主体切片（A 校验/registry＋fallback 血统＋C2 warn 消费者，15 commits ff） | contracts+core tsc 0＋registry 4/0＋harness 64/0＋skills 27/0（合入态） | C4（trigger 判别消费）等 #248 M1.5 后补尾款；live Langfuse 与 fresh 全量随 A 批合流 |
| 30cbdb89 | #248 | M1.5：note_page_regenerated 事件合同（trigger 判别，contracts-only） | contracts 106/0＋tsc 0（合入态） | 解锁 #246 C4 |
| 281d327b | #247 | 契约＋机制切片（四上限合同/admission 冻结/共享 attempt budget/挂起恢复/checkpoint，44 commits ff） | contracts 111/0＋harness 六件 170/0＋skills 27/0＋双 tsc 0（合入态）；lane 侧全量 2489/0 | 生产装配保持 unset；#255 标定为最终关票门 |
| 7ef16c7d | #242 | fresh-DB auth schema bootstrap（cherry-pick 自 d43a4c54，纯测试基建） | quality-gates 10/0＋双库 provision 后原红 spec 抽验 4/0＋core tsc 0（合入态）；9 个 session 缺表红根治 | #242 其余面仍在 lane |
| aec25cdc+ab5a7c84 | #253 | A 组交付链后端（due-row 链自续/SKIP LOCKED/租约围栏/eligibility/召回，cherry-pick 自 95fdaae0+32555148） | core tsc 0＋harness 六件 170/0＋due-delivery 全套 fresh 双库 32/0（合入态） | B 组（#253FE）仍等 #264FE→#261 |
| 93072db4（尾 7 commits） | #246 | C4 尾款（trigger 判别接生产发射器）＋outbox 运维 CLI 行为证据＋收口文档 | core tsc 0＋harness 170/0＋contracts 111/0（合入态）；lane 侧红 33/3→绿 36/0、fresh 全量 2533/0 | **#246 关票**；live Langfuse 残量归项目 live 门；六格 invoked/persisted 生产格归 #256 |
| 1f1c81c5 | — | e2e 基建 hotfix：playwright core/worker 块声明 LANGFUSE_PROMPT_POLICY=pilot | three-modal wechat_moments desktop 1/0（43.5s）＋pilot 降级审计打点确认 | 6cef418e 使 strict 成缺省，无凭证 e2e 栈全线拒启；主控直接提交 |
| 26d44359+f795b70b | #242 | L1 事实满足度护栏干净切片（recorded 接缝 provider/exact scorer＋11 frozen golden＋incomplete fail-closed＋promptfoo 正负门与 CI 段，cherry-pick 自 issue/242-l1-slice ee26e007+f3600a8d） | eval 11/11/0＋promptfoo 11/0/0＋control 如期 exit 100＋core tsc 0（合入态） | **A 批合流达成**（#246 关＋#247 机制＋#242-L1）；#257 删除窗口开启 |
| d322ca07 | #267 | adjust 语义绑定收紧（冻结 snapshot 语义 exact 绑定/篡改 fail closed/output count 冻结进 deliverable+reservation，cherry-pick 自 83055ffa；前两笔 8ec79eac/6bd6ec99 patch-id 等价于已合入 2b8fad7d/745b2702） | contracts 111/0＋双 tsc 0＋焦点三件 29/0（合入态）；lane 侧全量 4547/0＋双 e2e 绿 | **#267 关票**：合入态双 e2e 亲验绿（three-modal 43.5s＋price-validity 46.5s；中途一次假红=负载，隔离重跑定性） |
| 2e8252b4（2 commits ff） | #242 | W01 反向校验更正值缺口（candidate revision snapshot 冻结确认/finalize 解析 effective corrected draft/竞态 fail-closed） | focused 21/0/0＋core tsc 0（合入态）；lane 侧 fresh 双库 PG 21/0/0＋负控红→绿采信 | price-validity 归因完成后合入 |
| ef5e5106 | — | web 合同修复：hard-gate demoted 名单摘除 creation-loop（IA 处置后「留档带标记」前提过时，main 单测红） | 该合同测试 5/0（合入态） | lane-261 纯 main 复现上报；主控 IA 合入门当时漏跑 web 单测，记档改进 |
| 50cf5d01（2 commits ff） | #249 | 授权与安全面全量（action registry 单点枚举+启动校验/五类 continuation 收口+effect=0 绕行负例/executeCopySelection 收进 .testing/ApprovalReceipt TTL 单点+原子跨期复核/Skill 复合键 deny-all/伪造 permission 拒绝） | 合入态：焦点负例 53/0＋touched 面 135/0＋contracts 111/0＋双 tsc 0；lane 侧 issue249 双库 PG 20/0/0（skip=0）＋全量 2332/0/157（显式门 skip）采信 | **#249 关票**；七条验收行为门逐条对应 |
| 8e442346 | — | e2e 基建 hotfix：P1 worker 块补 HARNESS_DBOS_SYSTEM_DATABASE_URL（701ef89e 信号化后 worker 终态通知从未接通，image/video 全旅程吃满 150s 超时；契约测试改按块计数 2） | **已验证**：#242 p1-f2 复跑 image_text 1.1m＋video 22.2s 全链绿（此前死于 150s stall）；契约测试 2/0；格式债由主控补修（style commit） | 诊断裁决 a＝e2e 双进程拓扑接线缺陷；copy inline 不受影响；主控直接提交 |
| c433ee7f | #242 | p1-f2 两类基线修正（UUID 8 位后缀＋leak 负控 load-bearing；image/video 显式 source slot 授权走全链，cherry-pick 自 5805b457，仅 2 e2e 文件） | 主控范围核对＋web tsc 0＋biome 0；lane 侧锁下 fresh 双库 UUID+image_text 2/0（1.1m）＋video 1/0（22.2s）＋残留 0 采信 | 同时构成媒体拓扑 hotfix 的行为验证 |
| 36fe818c（尾 9 commits，cherry-pick 自 6574aef3..039a0828） | #248 | **M2 主体切片**：agent_primitive.lifecycle canonical 事件（invoked/succeeded/rejected、root 零绑定=四 null 禁 unknown@0、安全拒绝分类、event_append 防伪造）＋drop 分类/delivery health 持久化＋outbox 加固＋Langfuse sender 投影＋web 事件客户端 | 合入态：contracts 119/0＋双 tsc 0＋焦点观测五件 33/0；lane 侧 PG 并发重放/冲突 terminal 1/0（独立 meiye_issue248 库）＋Agent Team 双轴 P0/P1=0 采信 | **#256 gate 的 #248-M2 半边就此满足**；#248 关票另核（live Langfuse 门等项目 live 窗）；交验单「单笔提交」实为 9 commits 栈，已按全段摘取 |
| 66fa1519（21 commits ff） | #258 | Skill 包全量：官方格式 import/export（Unicode name/numeric metadata，官方 validator 钉扎 38a2ff82 过）＋reference-only skill_define＋prewrite/capture claim 语义（同 key 重试确定性）＋allowed-tools 单一权威＋PG repository（池重建迁移）＋web admin Skill 合同面 | 合入态：skills+foundation 焦点 73/0＋contracts 119/0＋web 合同 3/3＋双 tsc 0；lane 侧官方 validator exit 0＋PG 6/0（池重建）＋浏览器锁下 1/0＋终轮双轴 P0/P1=0 采信 | **#258 关票**；D-150 下游=#259（目录/五命令 UI）+#260（import/export 第一刀）；playwright pilot 行重复（同键同值无害）记清理轮 |
| 2f4191bb（18 commits ff） | #257 | 删除批全量：94/94 复核（81 删/13 保），四批（asset-memory 21/context 5/ops query 15/ops command 40）＋三旧浏览器 journey 删除＋hard-gate 名单同步 | 合入态：retired-e 3/0＋hard-gate 5/0＋contracts 118/0（-1=真删 schema）＋双 tsc 0＋抽验 ls-files 空 ×3；lane 侧全量四包（118/1571/2380/277/165 全 0 fail）＋双终审 0 findings 采信 | **#257 关票**；上报的 observability-event-client biome 债（#248 合入遗留）主控已修（style commit） |
| d09695a6 | — | harness fixture hotfix：DBOS smoke 的 prompt.contentHash 改真实 sha256（#258 强校验 fallback pin 后 4 个 PG 门用例红；fixture 值须生产可产出） | 一次性双库 provision 后 11/11/0，库清零 | #255 上报的上游红；主控直接提交 |
| 676fd8e4（尾 4 commits，cherry-pick 自 b47ba996） | #240 | production media assembly join＋admission 冻结完整 RouteSnapshot（server-owned durable field，caller 注入/缺 resolver/stale route 均 provider 前 fail closed；execution/replay 只读冻结路由，resolver 恰 1 次） | 合入态：focused 45/0/0＋persistence gate 12/0＋core tsc 0；lane 侧 fresh PG+DBOS 17/0/0（绝对锁）＋全量 2339/0/158＋终轮双轴 P0/P1/P2=0 采信 | 方案 1 落地；关票另核票面验收 |
| e67e4e9b（尾 4 commits ff） | #242 | 硬门⑥负向接线 corpus（五类 corpus 单一真相＋真实接缝负向门＋stale ApprovalReceipt 修复：approval 策略用 receipt 冻结 revision） | 焦点三件 34/0/0＋core tsc 0（合入态）；lane 侧负控红→绿 | 生产改动仅 approval 2 行 |
| a5cb77a1 | #242 | IA 切片：uiux-creation-loop 退役 6 条旧旅程（不 skip 洗绿），保 2 条 Day-0，TEST-CATALOG 逐条登记 contract owner（ff，仅 2 e2e 文件 -1226 行） | 主控 diff 范围＋留存 2 test 0 skip 核对＋web tsc 0；lane 侧隔离库锁下 2/0/0＋pnpm check 8 段 PASS | 与 price-validity 红零交集 |
| c9ca6e77 | — | harness hotfix：factTypes=下限非白名单（回收 f795b70b 两条过严子句，保 matched∩missing=∅；+1 回归） | 单测 10/0＋eval 12/12＋promptfoo 11 正门 exit 0＋control 如期 exit 100＋core tsc 0 | price-validity e2e 红根因（诊断裁决 A）；frozen golden 11 条未动；主控直接提交 |
| 4704cbd6（3 commits ff） | #248 | 独立收口切片：公有 event_append 收窄至 merchant rating 单径＋canonical 五事件全量对账（drop summary/delivery health 消费）＋durable cursor 只推进完成窗＋PG DB 时钟边界/interval 宽限/完成行禁并发重算＋生产消费者结构化日志＋signal×destination 矩阵文档 | 合入态：焦点观测四件 20/0/0＋core tsc 0；lane 侧 PG observability-health 2/0（锁下独立库）＋双复核 P0/P1=0 采信 | **非整票关票**：生产 emitters 等 #262 载体，live Langfuse 归项目 live 窗；已披露 P2（无租约重复日志发布）非阻塞记档 |
| 43238d5f（4 commits ff） | #264 | FE 半：商家视频接收面退役编辑入口（subtitle/cover/regen/Pro Studio refine＋原生 captions track）＋videoBillableScopes=[]＋旧视频历史档案只读（copy 保留）＋7 接收 action 回归＋e2e 双进程 DBOS__VMID 隔离加固（合同钉扎） | 合入态：合同 7/0＋interaction 38 文件 226/226＋biome 0＋web tsc 0；lane 侧最终 HEAD 浏览器 4 spec 7/7＋焦点 Node 72/72＋视频交互 15/15＋前稳定树 root test/typecheck exit 0 采信 | **FE 半交验合入**；core 半等 C2 明文开工；#261 J10 退役残项就此解锁；FE lane 轮到 #261 收尾 |
| 77db3390 | #242 | intake-receipt 独立切片（主控优先级令拆出）：W02 promoted batch fixture 补 commandFingerprint（生产 finalize 内联记账带 fingerprintValue，无凭据状态生产不可达）＋生产竞态加固——pinned-heads 守卫扩至本命令确认 facts（assertConfirmedFactsUnchanged，revision 冲突 fail-closed）＋122 行单测 | 主控亲验（lane 未交证据，主控直采）：fresh 库 PG behavior 21/0/0（原稳定红转绿）＋finalizer 单测 12/0/0＋core tsc 0 | **#255 上游 W02 红解除**（本行即 #255 解锁判据）；生产改动仅 finalizer 竞态守卫扩展，2e8252b4 同属主语义延伸；媒体切片继续在 lane |
| da5ac8f0（2 commits ff） | #252 | W2-3 模型供给/catalog：能力词表与需求匹配随冻结路由身份版本绑定（含「空需求」能力版本变更也构成身份变更）＋PriceRevision/供给冻结对新治理事实强制显式执行渠道与定价档（历史冻结仍可重放，不编造 unknown）＋同模型换渠道与跨模型替代两类容灾语义分离（缺渠道或同渠道 fail closed；跨模型替代须声明降级面与渠道绑定等价）＋固定选择在策略与三层排序后仍为首选、请求模型的保守兜底优先于授权替代＋租户白名单丢弃不允许的平台默认并集中告警但不拒绝启动（空白名单＝不限制）＋发现面维持 draft-only 无自动发布 | 主控亲验：拓扑 0/2＋禁区净＋full Core 2849＝2666/0/183skip＋contracts 129/0＋core tsc 0＋根 check-gates 全 PASS；可达性引用抽查四处（foundation-module.ts:2574 冻结固定路由／index.ts:3756 能力匹配／supply-control-plane.ts:249,572 固定模型优先／foundation-module.ts:6241 白名单投影）逐条为真 | **首份按新「可达性证明」门交验的切片**（该门 2fbb716e 刚落 runbook）；183 skip 为真 PG/DBOS 与 live 配额环境门，lane 明确不主张持久化与 live 验收；站点注册/装配消费者归 #262 下游，未在本切片冒充 |
| 84d1af91（2 commits ff） | #248 | 生产观测发射全量（#262 载体解锁后的残余面）：task admission 在 registry claim 后、starter 前写唯一 task_pin root（existing replay 同补），后续 assembly/stage 不再二次写 root；rating 公共命令只收商家自撰评价／撤回，四轴按冻结 task/delivery 由 Core server-owned hydration，ActionUsage 从最终 ProductUsageRecord 投影并校验 terminal/mismatch；bounded suspended／resumed 与 note regenerated 走既有 durable seam 发 canonical event；child span 只带实际单值/absent 轴，catalogRevision 只取 executionSnapshot 而 route 仅作一致性校验；PG root claim 用 first-value CAS 并兼容旧 event_persistence root，异事实永久配置冲突写独立 drop、额外 outbox 进 quarantine/discard 不递归回被丢信号通道；Langfuse sender 支持 task_pin root 并保留旧 root 迁移兼容；信号×目的地矩阵回填 | 主控亲验（代为 rebase 到当前 main）：full Core 2827＝2644/0/183skip＋contracts 129/0＋core tsc 0＋fresh 双库真 PG 5/5/0（canonical emitters／observability-health／production-media-assembly）；lane 侧 Web 非交互 1581（4skip）＋interaction 43 文件 261/261＋双轨终审 P0/P1=0 采信 | **非整票关票**：live Langfuse 两项（按轴远端过滤、Prompt API >1024 中文 exact-version 读回）归项目 live 窗，lane 明确未以 fixture／单测／本地 PG 冒充 live 证据，此边界主控采信 |
| 391d7ed1（2 commits ff） | #240 | 媒体准入双 P0：**P0-1** creation-stage-port 终止判据改错误码白名单——仅 REQUEST_FINGERPRINT_CONFLICT／EXECUTION_SNAPSHOT_MISMATCH 判永久终止，FROZEN_ROUTE_MISMATCH／FROZEN_REQUEST_MISSING 及未知错误保持可恢复，删去准入前的 workspace 归属推断（原 taskBelongsToWorkspace 启发式在首次拒绝时恒 false，致每次准入拒绝都被判永久失败、harness_state=failed 不可恢复）；**P0-2** 冻结路由候选在缺 capabilityProfile 时按模态推导媒体 profile，且由 runtime.activation===local_fixture_verified 门住，生产运行时永不走推断 | 主控亲验（rebase 到当前 main 后）：焦点四文件 32/0/0＋full Core 2820＝2639/0/181skip＋core tsc 0 | **浏览器验收（m04 image_text）无法在当前 main 复现**：#255 引入的三闸门 P0 使任何创作提交先于媒体准入 503（见 #247）。lane 在 main@ddf6c24a 取得的 1/1×2 采信为当时证据，**行为面复验顺延至 #247 修复后的主控判别轮**，不以此充当当前 main 绿。legacy 审计（461 有快照／425 缺冻结路由）策略＝不自动回填，421 终态只作 replay waiver，4 条非终态继续阻断 |
| 9d2e6319 | #251 | **#251 上游（主控直修）**：冻结路由回调只解析被请求模态的平台默认模型：原实现一次解析全部模态默认、任一报错即抛，之后才挑自己需要的那条绑定——于是 image 默认缺激活证据会打掉 copy 请求（生产同样成立，非 fixture 假象）。提出 platformDefaultsForOperation 纯函数（归 platform-defaults.ts，该模块本就拥有 operation↔configKey 映射），无平台默认的操作返回空表＝无绑定，本模态自身的 live_verified＋active 校验一字未动 | 主控亲验：新增作用域负控测试 1/0＋平台默认相关五文件 34/0/0＋core tsc 0 | 由 #251 交底第①条结构性阻塞定位而来，主控核实后直修（跨票生产装配面，不归单票）；#251 剩余两条阻塞（conservative fallback 要 live_verified、fixture 无可满足 14 轴的 capability profile）仍待裁决，不在本行 |
| be31bb64 | #254 | 治理动作幂等键长度收口：raw runId 只作内存 intent fingerprint，传输键改为 action＋UUID（runId 可达 150 字符，键长收在 200 内）；skill_governance_start 一并进入同一 registry，语义统一为「在途重试复用、新尝试轮换」 | **主控亲验**：web 路由单测 9/0（含 150 字符 runId 上限负控）＋web tsc 0＋Biome 0＋浏览器 admin-dashboard-shell 5/5；主控另核领域层按 runId 去重（同 runId 同 inputFingerprint 复放、异事实 IDEMPOTENCY_CONFLICT，service.ts:741），故轮换传输键不会引入重复起跑。**lane 自报（未经主控复算）**：Core 聚焦 125/0、Foundation 双周期 receipt 8/0、真 PG/DBOS 15/0、双轴 P0/P1/P2=0 | **补记行**：本 SHA 随 7d71ee44 一同被主控 ff 快进入 main 却漏记，属主控记账疏漏；lane 直接写 main 补行（越权，见票下裁决），主控重写为当前口径并保留内容 |
| 7d71ee44 | #254 | 治理动作幂等键轮换：原用固定键 `<action>:<runId>`，同一 run 的第二次管理取消被服务端当作首次的重放而静默无效（取消→恢复→再取消循环走不通）；改为按尝试轮换键、仅在途重试复用同键（createGovernanceActionIntentRegistry），并补 Foundation 侧「每个不同的管理取消周期都真实执行」行为测试 | 合入态：core dbos-governance 单测 8/0＋web 路由单测 9/0＋core/web tsc 0＋Biome 0＋浏览器 admin-dashboard-shell 5/5 | #254 关票；用户 2026-07-30 拍板关票，四刀（30e90fa7／c105d9ba／7d71ee44／be31bb64）台账四行齐、代码面已冻结；lane 在停工令时序之前已开工的自查刀，主控亲验属真缺陷后采纳；幂等键语义更正为「标识一次尝试」而非「标识一个 action×run 对」 |
| c105d9ba | #254 | 治理轮询补漏（对主控上一刀的反向补全）：governanceRunStatus 补映射 DBOS `ERROR`／`MAX_RECOVERY_ATTEMPTS_EXCEEDED` → `failed`。主控把「未知即轮询」反转后留下反向缺口——真正死掉的工作流因 state.status 仍是 awaiting_approval／applying 而被永久 2 秒轮询；本刀补上终态识别 | 合入态：路由单测 8/0（含 ERROR 与 MAX_RECOVERY_ATTEMPTS_EXCEEDED 两条新负控）＋web tsc 0＋Biome 0＋浏览器 admin-dashboard-shell 5/5 | `MAX_RECOVERY_ATTEMPTS_EXCEEDED` 已对 DBOS SDK 4.23.6 核实为真实状态；lane 在主控合入后自查补交，主控亲验采纳 |
| 30e90fa7（5 commits ff） | #254 | W4 运营控制台全量（主控接手 lane 收口）：可改槽位代码侧声明（仅 instruction／manifest.description）＋服务端逐字段剥离（越权字段 stripped＋去值审计，success/applied 分离，全剥离与 CAS 冲突均 success=true·applied=false，未知 action／非法 envelope 仍前置 strict reject）＋唯一 Published 生命周期指针与 traffic 绑定分轴（publicationGeneration CAS，draft 版本禁绑）＋workspace 作用域反向依赖（可见明细＋跨 workspace 仅 hiddenCount；scope_kind NOT NULL CHECK IN workspace/global/unknown＋互斥约束，legacy unknown 结构性 fail closed 计入 hiddenCount 不泄身份）＋稳定 runId 的 DBOS 可挂起治理运行（suspend/管理取消/恢复/业务终止/replay 幂等）＋运营台只读渲染面；另修 harness 分库校验按库标识而非 URL 字符串比对 | 主控亲验（lane driver 终端已关，主控接手为唯一写者）：rebase 后 core tsc 0＋full Core 2817＝2636/0/181skip＋contracts 128/0＋web tsc 0＋Biome 1092 文件 0＋interaction 43 文件 259/259＋路由单测 8/0＋fresh 双库 PG 15/15/0（含真 DBOS suspend/cancel/resume/replay、Published CAS 单指针、legacy 缺 scope fail closed、退役×引用竞态）＋浏览器 admin-dashboard-shell 5/5 | **主控自查出并修复一条产品缺陷**：治理运行轮询在状态不可读（读取被 abort／失败）时返回 false 停摆，而此时所有动作按钮恰因状态未知而禁用——面板死锁只能靠手动刷新；改为「仅终态停轮询」并提出 governanceRunPollInterval 纯函数钉死判据（修前 4 轮浏览器门挂 3 轮，修后连跑 4 轮全绿）。#260 后端开工依赖就此解除 |
| f3e64e48（5 commits ff） | #242 | RightsBasis rights-consumer 干净切片（D-150 消费证明）：生产视频导出在 exporter 前强制解析 RightsBasis，缺失／不可用／未知异常一律 fail closed；有来源素材时 MarketingPackage rightsRefs 须与冻结 source asset 精确一致并按目标 platform 实时核 Product 授权；source-free AI 视频须唯一 selected asset 且精确绑定 owned asset→succeeded child run→provider task→route snapshot→唯一 completed attempt→生成时 registry/deployment/contract，条款须当时有效且 commercialUse=allowed；fixture 商用许可只在 explicit local fixture runtime 播种并打 local-fixture 标记、默认拒绝，防 fixture→live 同 revision 冒充；交付 JSON/manifest 只暴露安全摘要 basis，不泄 run/task/provider/terms 内部引用；main 与 job-worker 双装配接同一 resolver | 合入态：焦点 8 文件 168/0/0＋contracts 128/0＋core tsc 0；lane 侧 full Core 2750＝2576/0/174skip＋双轴 P0/P1/P2=0＋scope creep 0 采信 | 权利基础裁决落地第二刀（契约 c02dde70 之后的消费端）；真实 provider termsRevision/commercialUse 录入仍属 live 前 provisioning 门；媒体 17-spec／works-reshell 尾巴仍归 #240 后判别轮 |
| 06e613b1 | #242 | criticality 诊断 delta 收割（主控撞线裁决落地）：harness_fact_criticality_v1 的 runner 异常与输出 parse 异常分记 runner／schema_parse，事件绑定 wf:<id>:s2:facts:criticality:0；factDiagnosticEvent 参数化 schemaName/effectIdempotencyKey（satisfaction 事件形状不变）；questionCardSchema.parse 归 runner；移植恶意 name/code 与 newline-frame 注入负控 | 合入态：fact-satisfaction 单测 17/0＋eval 19/0＋core tsc 0；范围仅 2 文件（+249/-24） | 收割自竞争稿 55e1a3bf 的真差距（合入版 criticality 外围原为裸 catch、零事件）；竞争分支就此可删；判别轮取证不变 |
| 094c8f36（12 commits ff） | #256 | C2 生产收口全量：六 primitive（read_context/generate/revise/record/check/ask_merchant）contracts 注册表＋生产装配可达＋P1 执行接缝（generate/revise 单 provider effect 无重复、revise 走 server-owned target fence＋OCC；check awaited 叠加 #246-C 七门不复制第二份红线；ask_merchant 复用既有 DBOS persist-pending 单写；record 仅 proposal port，#251 管道明确不做）＋四负向合同（伪造 tenant/billing/idempotency/observability、confirm_*、未注册 tool、超 maxIterations 均 fail-closed＋rejected trace）＋canonical lifecycle 审计链 audit_events＋langfuse_outbox | 合入态：焦点 Node 243/0＋contracts 128/0＋core tsc 0＋PG 29/0（skills repo＋harness store，execution_spine 自供给文件前置引导；此前 1 红为隔离跑缺前置的环境类非缺陷）；lane 侧 rebased HEAD focused 229/0＋真 PG 1/0＋正式 provision 双库装配 1/0＋closeout 门 6/6＋终审 P0/P1=0 采信 | **#256 关票**；语义锁核准：只消费 #246-C，深化面归 #250/#251/#248/#247；live Langfuse 发射归项目 live 窗 |
| 6d1c98df（32 commits ff） | #255 | live 门链三件套全量：provider-attempt fence（RUN_LIVE_ISSUE_255 未授权即 fail-closed 禁用）＋live collector/PG live receipt＋bounded-execution 标定（recorded 27 样本 3 模态×3 场景带×3 seed：maxIterations default 2/hardCap 4；cost/wallclock 留 unset 不以合成值冒充 live 真值）＋inspector/reset CLI 与 safe-provision 门（destructive reset 显式 opt-in RUN_ISSUE_255_SAFE_PROVISION_POSTGRES_TEST）＋fence/reset runbook | 合入态：拓扑 0/32＋禁区与凭据扫描净（14 处 RUN_LIVE 命中均为围栏谓词/假 env 测试夹具/runbook 禁用文）＋fresh 双库**先供给后跑**全量 2748＝2735 pass/0 fail/13 skip（首轮主控漏跑 provision-test-db.sh 致 4 文件 9 红，定性亲验环境错；供给后隔离重跑 9/9 绿）＋13 skip 明细核准（3 destructive opt-in＋9 live/provider 付费门＋1 MinIO）＋core tsc 0＋标定证据抽查逐项吻合（fixture 0/recorded 27/live 0） | **#252 C1 门翻开＋#247 标定依赖就绪**；live 仍 NO：#240 P0-1 前置＋主控明文放行；lane 全程 ¥0 |
| 7c86dea5 | #242 | works-reshell 结构化安全诊断切片：fact-satisfaction runner/schema 双 catch＋封闭错误映射（异常 payload 不可信，不读传入 message/stack）＋结构化事件仅限裁决字段＋凭证泄漏负控（prompt/fact/route/credential/provider 关键词及伪 sk-*/AKIA stack 均不入事件）＋logger 故障不替换 conservative guidance | 合入态：单测 13/0＋core tsc 0；frozen golden 区零 diff（仅 2 文件） | 采集就位；works-reshell:204 取证并入 #240 后判别轮 |
| c02dde70 | #242 | RightsBasis contracts-only 切片：rightsBasisSchema 严格 XOR（source_asset_authorizations{nonempty rightsRefs} ⊕ ai_generation_terms{generatedAssetId/runId/providerTaskRef/termsRevisionId/commercialUse:allowed}）＋SupplyContract.commercialUse 显式录入位（optional 仅为历史可读，消费者对缺失 fail closed） | 合入态：contracts 121/0＋contracts/core 双 tsc 0 | 契约先行（权利基础裁决落地第一刀）；delivery manifest 消费者与 provisioning 录入随后续切片 |
| 33e67ac9 | #242 | confirmed-store-facts fixture 干净切片：seedConfirmedStore 改走真 finalize_store_intake 命令路径（service/price 从已确认 project 派生、profile OCC、断言 revision 0→1）＋拆 dashboard-home-mount/m04 两处第二套造数旁路（W01 空账本负控保留） | 合入态＝lane 验证树同一 tip：静态门采信＋主控范围核对 3 e2e 文件 +135/-154 | dashboard-home-mount 两红的修复面；行为绿归 #240 后判别轮统一验证 |
| 27dcd374 | — | harness fixture hotfix：DBOS smoke 期望快照补 skillCapabilityRequirements（#262 给冻结 skill 束加该轴后，双库门 smoke 未随更新——#262 全量跑时该测试被 env 门 skip 掩盖，#255 双库全量揭出） | 双库 fresh provision 后 11/11/0＋core tsc 0，库清零 | #255 上报的上游残留；d09695a6 同类；主控直接提交 |
| 7c4f12a7 | #242 | three-modal source-slot 干净切片：image_text/video 提交前统一走 seedComposerInlineAuthorize 可见上传授权（copy 不造素材），保 #261 后 receive 语义不回带 edit/adjust；仅 2 e2e 文件 | 合入态＝lane 验证树同一 tip（merge-base==main）：pnpm check 1092/0＋typecheck 0＋--list 8 tests＋diff-check 0 采信；主控范围核对 2 文件 +4/-3 | **#253FE GO 判据①落账**；行为性绿（three-modal image_text 真跑）受 #240 冻结门 P0 压制，归修复后主控判别轮统一验证 |
| a6fd4409（4 commits ff）＋457da759 | #265 | 品牌统一「丽客美页 LIKEPAGE」全量：盘点清单先行＋六 i18n 键双语改名＋可重跑扫描门挂根 pnpm check＋两条 Landing 界外发现记账；457da759=主控加固扫描 walker 跳过点目录（.playwright-cli/.impeccable 等本机残骸致门裁决依赖本地垃圾，#261 debt e2e 残留触发发现） | 合入态：scan --check 0 对外/16 内部＋locale 3995 双语 parity＋scan 自测 4/0＋check-gates 4/0；lane 侧拉平复验（#261 +30 键零回流，门的抗回流实证）＋真 ZIP/邮件 0 命中采信 | **#265 关票**；i18n-motion spec 复验留主控 e2e 欠账批（非关票门）；Landing 两条归 R 门重估 |
| 2c979709（5 commits ff） | #262 | **三轴装配载体全量**：六步 assembly audit 经生产 LangfuseHttpSender/markLangfuseSent 全链（单 task-root trace、确定性 trace ID）＋RouteSnapshot 版本钉扎（在途任务保 r1、新任务见 r2，catalog/capability/credential/provider/lifecycle/endpoint 六轴）＋#240 单一 durable carrier 无第二真相＋outbox 去全局 claim＋readiness 脚本与负向测试 | 合入态：改动面测试 387/0/11skip＋core tsc 0；lane 侧根 pnpm test 全包 exit 0（Web 1589/Core 2437/Contracts 118/Canvas 277/根脚本 172 全 0 fail）＋锁下真 PG+DBOS 装配 1/0＋readiness exit 0 采信 | **#256 全门就此满足（#248-M2＋本行）→ C2 批开闸**；#248 emitters/#261 评价真值解锁；关票终核另轮 |
| 7dfdfb0b | — | e2e spec 重基线：uiux-shell-routes token 双主题八值＋焦点环＋资产库→素材；mobile-product-shell 身份素材→口吻与素材、表达身份→口吻（加 exact）；删两条宿主已亡断言（next-action-guide 组件 27e74740 已删／今天的创作记录只剩 pendingComponent） | 亲验：agent 逐值 file:line 出处表＋web tsc/biome 双 0（独立 worktree）；主控复核 spec-only 2 文件 ff | **考古更正**：token 期望自出生（0dd43524，07-20）即错——166/188 青绿盘从未在 styles.css 存在过，spec 生而未跑；--product-guide 零消费者/theme.ts 注释陈旧/home_hero_preview_title 孤儿键 记清理轮；主控直接提交 |
| 177ae8e4 | #240 | D-153 残项收口：非 hold 路径孤儿预留穷举（启动 owner/lease fencing/权威拒绝/ambiguous retry＋exact-text 零用量不二次消费＋decision resume 可回收 lease/稳定 effect key＋terminal intent-first settlement/五分钟 grace/orphan owner 重建＋commit-refund task 互斥/冲突归档/序列化后 fail-closed＋backlog 查询排除已 owner 防 LIMIT 饥饿） | 合入态：被改测试全套 102/0/31skip（PG 门显式跳）＋core tsc 0；lane 侧 focused 101/0＋full Core 2397/0/166skip＋真 PG+DBOS 32/0（锁下，等锁 3060s）＋双轴 P0/P1=0 采信；证据 docs/evidence/issue-240-d153-…md | **#240 残项清零**，关票待票面验收终核；改动面横跨 execution-spine/harness/billing＝预留生命周期属主面 |
| 282f95b9（25 commits cherry-pick 自 issue-261，base 5e96f555） | #261 | Dashboard 单路由三段全量：D-137 历史只读文案＋记忆一级导航四域（D-164④）＋?view= 收敛重定向＋提议→创作→继续三段（D-164①）＋配方 pill 行五类任务分组（D-164②/D-139，D-083 形变已裁留痕）＋执行确认卡与成本即时反馈（D-164③⑥，CNY 清账）＋评价条三动作与 follow-up chips（D-164⑤/D-160③，发不出即留 drop 负向证据）＋ImageAdjustConfirmation 收编（D-164⑥A，ls-files 空）＋?view= 独立取证 spec | 合入态：web tsc 0＋biome 0＋locale 3995＋interaction 43 文件 259/259；lane 侧五门全绿（root pnpm test rc=0）＋9 项变异验证＋gate 6/7（余 G3b） | **e2e 证据欠账**（用户裁定暂不跑；四 spec 待补，J13 两条 main 陈旧 spec 红归清理轮）；票不关（余：评价三轴真值等 #262、settled 投影等 #248/#262、记忆三域等 #251/#252）；**#253FE GO＋#265 合入窗开** |
| d55d4e7c（13 commits ff） | #259 | Skill 第八能力域基础面：可信 prompt/EvalRun 权威边界＋immutable PG registry/importer＋运营结构化目录与五命令派发面 | 合入态：Core 64/0/11skip（PG opt-in）＋Web 23/0＋三包 tsc/Biome/diff-check 0；真 PG repository+importer 16/0/0；绝对锁浏览器 1/0（11.2s，整轮 1.2m）；lane 终审双轴 P0/P1/P2=0 采信 | **#259 关票**；浏览器仅 route-mock dispatch proof，真 PG 独立，未宣称 live Langfuse/provider |
| 62a050f0（11 commits ff） | #264 | D-133 视频编辑面终态退役（core 半）：`video-regeneration` P1 module／capability／runtime／foundation／repository／migrator 与 PostgreSQL 生命周期物理删除；subtitle text edit 在进入 canonical video workflow port 前拒绝，foundation-module 只收 select／reorder；video result adjustment 在 prepare 与 confirm **两端**均以 `RESULT_ADJUST_OPERATION_UNSUPPORTED` 拒绝且拦截点位于任何 scope／inherited asset 计算之前，不产生 quote intent／derived Work／submission／reservation／等待态；新视频 ZIP 不再读写 legacy cover，旧 cover／subtitle／evidence schema 与存量数据仍可解析（不做破坏性 DROP）；checklist 按裁决改口（抖音封面字幕自选／视频号只留 upload video.mp4／小红书图文封面指引不变）；D-123 三桶口径复核文档 `docs/reviews/issue-264-d123-video-billing-review-2026-07-30.md` | 主控亲验（合入态）：删除证明自跑——`git ls-tree` 无任何 `video-regeneration*` 文件、生产面（排除测试）token 扫描 0 命中，仅剩两处退役负控测试；禁区未动（docs/specs／design／ops／.scratch／台账）；根 check-gates 九门 Overall PASS；core tsc 0；contracts 129/0/0；焦点三文件 25/0/0（**首轮我用 `node --test` 直跑 .ts 报 ERR_MODULE_NOT_FOUND，属我方跑法错，改 `tsx --test` 后全绿**）；两端拒绝的代码位置逐行核对 | lane 侧真 PG 1/0／根脚本 177/0/1skip／`CI=1 pnpm test` exit 0／双轴四轮到固定点 P0/P1/P2=0 **采信未复跑**；本 core lane 未重跑浏览器旅程；FE 半此前已合入关账。**gate 自锁修复记档**：该 lane 曾因 gate 把「已 rebase 到最新 main」写成放行前置而停摆 10 小时，主控改 gate 后恢复（纪律已入 runbook `878f0701`／`26d8f38f`） |
| 95ba435f（43 commits ff） | #250 | HITL/ask_merchant 深化基础切片：**无界等待家族三入口全闭**——①两个生产 producer（`fact-satisfaction.ts:262`／`unified-media-stage-ports.ts:185`）改声明 `unattended:'hold'`，生产侧唯一 `'continue'` 只剩带 `semanticDefaultAuthority` 的 intent-gap 卡；②`expireUnrendered` 补齐 `unknown→unavailable` 缺失边，`held/renderer` 走向取消并退额度（`core_hold_expired`）；③新增 `ExecutionConfirmationWaitingMessageCard` 专属卡片，waiting 态不再劫持主创作输入框。另：转追问身份校验＋原始 payload 透传；legacy `/decision` 撞 waiting 返回 `stale_question`；带选项卡片找回自由文本；成组回答改显式拒绝（不再静默丢弃）；编辑租约按 generation 定属主；反问重新武装计时器；删除不可达的 legacy promotion reader；**跨版本 durable replay 布局门**（`HARNESS_INTERACTION_LAYOUT_RESET_REQUIRED`，命中即绕过 settlement／terminal write／closeStream 后 rethrow） | 主控亲验（合入态）：三个 P0 的出口逐条走通——producer `unattended` 取值全量核对、`expireUnrendered` 生产调用链与终态核对、前台 waiting 卡片渲染点核对；布局门旁路结算逐行核对（`dbos-workflow.ts:690-698,768-771`）；禁区未动；根 check-gates 九门 PASS；core tsc 0；contracts 137/0/0；焦点四文件 82/0/0 | lane 侧 focused 166/0／Web 15+7／真 PG interaction 5/5／DBOS smoke 15/15（含 renderer expiry 冷恢复、r2 exact-once、pre-be bounded、pre-T45/pre-C1 replay）／三卡 Playwright 1/1 **采信未复跑**。**rollout constraint：pre-a9 durable interaction 记录不受支持，携带者须成对重置 business＋DBOS 库，不提供自动迁移／resume／system default／退款**（成立前提＝环境可弃：main 上 #247 三闸门使任何创作提交 503、live 门从未开启）。资源闸门切片待 #247；最终互票验收待 #251；`marketing-composer-harness.spec.ts:197` 旧用例红归 #247 压制，未改相邻旧 spec |
| 7eabb985（主控直修） | #248 | **#248 善后**：`postgres-store.postgres.test.ts:124` 断言从文案正则 `/idempotency conflict/i` 改为按错误类型 `TaskRootObservabilityConflictError` 判定——#248（`84d1af91`）引入命名错误类时未同步该 PG 测试期待，测试为 PG opt-in 故在当时的证据面被跳过、红了未被发现 | 主控亲验：fresh 库先供给后跑，定向 1/1 绿；根 check-gates PASS | **主控合入面缺口记档**：合入 #248 时未跑 PG-backed 测试，导致文案型假绿滞留 main 约一整天，由 #250 交验时报出。断言改判类型而非文案，比原写法更强 |
| b8fb8ebe（4 commits ff） | #247 | **D-167 有界执行全票（主控接管收编）**：原 lane 因 codex 配额中断留下 41 文件／6682 行未提交，主控保全为 `wip` 快照后 rebase、复核、补完。①**决策 B 落地**——`provenance` 增 `deliberately_unbounded` 档＋`authorization`（owner／reason／recordedAt）签署留痕，resolver 据此把该轴移出 `requiredLimits`；`'unset'`（未标定）仍 fail closed。运行时语义零变化（控制器一直把 `'unset'` 当作不触发；live 花钱守卫在 receipt 预留路径 `issue-255-postgres-live-receipt.ts:1083` 的 ¥3.60／¥5 硬顶，本就不由这两道闸门提供）。②**e2e 播种路径**：`scripts/ci/seed-issue-247-e2e-provisional-bounds.mts`，双 env 门控（`APP_ENV=e2e` ＋ 显式 `RUN_ISSUE_247_E2E_PROVISIONAL_BOUNDS_SEED=true`）＋幂等 CAS，临时值标 `recorded_provisional`。③**触顶为可续挂起态**：`media-bounded-execution.ts` 新增，出口＝向商家发 `unattended:'hold'` 的 `boundedExecutionQuestion`（遵守 #250 的不变式），续跑解析器 `AdminConfigBoundedExecutionContinuationResolver` 生产已装配（`main.ts:2085` 第 7 位实参），提升被 `previous >= hardCap` 抛错＋`Math.min(hardCap, previous + default)` 双重封顶 | 主控亲验（合入态）：**22 套 opt-in 逐套独立新库、先 `provision-test-db.sh` 后跑 —— 21 绿／1 红**（红＝`usage-ledger-invariants`，经复核为 #270 既有红，同错同条数，本票既未造成也未改变）；core tsc 0；contracts 137/0/0；bounded 专项 8/0/0；根门禁**十门**全 PASS；**播种真库实测**：写入后 `requiredLimits` 三条全部有值，创作提交不再 503；B 方案端到端实测：签署两轴后 `requiredLimits` 缩为 `["maxIterations"]`，未签署轴仍精确拒绝 | **全项目唯一硬阻塞解除**——#250 资源闸门／#253FE／#260／#263 的阻塞条件同时满足。**主控自身失误记档**：验证中 `delivery.postgres.test.ts` 一度全红 5 条，系我串行复用同一个库造成的污染，换全新库 5/5 绿——而「每次用全新库」正是我同期写进 #251／#269／#270 开工令的要求。`postgres-store` 定向跑 4 红＝缺 full-suite 跨文件建表的环境产物，全量跑绿，已记入证据 note。生产侧仍 fail closed（无任何环境写入过带签署的配置），live 前需用户以 owner 身份签字 |
