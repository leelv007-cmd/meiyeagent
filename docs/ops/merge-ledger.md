# 主控合入台账（merge ledger）

**唯一有效性来源**：任何「已合入 main」的声明（票下评论、交验记录、任何前缀）只有在本文件出现对应行时才有效。本文件**只由主控提交**；lane 不得修改本文件、不得移动 main、不得以「主控」前缀发表评论——违者其合入无论内容对错一律 revert，其评论视为无效。

**主控合入纪律**：每次 ff 合入后、写台账行前，跑一次根 `node scripts/uiux/check-gates.mjs`。切片级证据（单测／eval／包内 tsc）看不见根级门——2026-07-30 的 #242 诊断切片带红了 secret scan 门（负控里的 AWS 示例键），直到当晚体检才发现。合入面越窄，越容易只验窄面。

**opt-in 测试等于不存在（2026-07-30 增补，因主控自身失误而立）**：env 门控的测试（PG-backed／DBOS／live／destructive opt-in）在不带对应 env 的证据面里是 **skip**，既不红也不绿——**skip 混在「0 fail」里，读起来和通过一模一样**。#248（`84d1af91`）引入命名错误类 `TaskRootObservabilityConflictError` 却没同步 `postgres-store.postgres.test.ts:124` 的文案断言，主控合入时只看了不带 `TEST_DATABASE_URL` 的证据，假绿在 main 上滞留约一天，最后由 #250 交验时报出（修复 `7eabb985`）。
**改约**：合入面若触及某目录下存在 `*.postgres.test.ts`／`*.smoke.test.ts` 的实现文件，**主控须在合入态以 fresh 库（先 `scripts/ci/provision-test-db.sh` 后跑）实跑该目录的 opt-in 测试**，或在台账「备注」列明写「本次未跑 opt-in 测试，风险自担」。不得以 lane 侧的 skip 数字充当已验。定向跑出现 `relation ... does not exist` 属缺 full-suite 跨文件 setup 的环境产物，须与产品红分开计。

**表格纪律**：正文内的 `|` 必须转义或改用全角，否则该行被 Markdown 拆成多格，严格解析的 lane gate 会正确判定台账 invalid（2026-07-30 实证）。**票号列只能是 `#数字` 或 `—`**，修饰语（「上游」「善后」「主控直修」）一律写进内容列或备注列——我写过 `#251 上游（主控直修）` 与 `#248 善后`，两处都让严格 checker 判整本台账 invalid，#260 因此停摆一轮（2026-07-30 实证，由 #260 报出后主控直修）。台账是唯一有效性来源，**它自己解析不了，下游全部一起瞎**——所以票号列宁可信息少，也要机器可判。

| main sha | 票 | 内容 | 亲验 | 备注 |
|---|---|---|---|---|
| 04dda7e1+a9a7f5cd+28e3ae73 | #266 | INFRA：locale:compile 并发机制（write-if-changed 同步/互斥锁+dev 心跳快速失败/staged compile 只写变更文件/vite dev 去 pid 残留） | 合入当时全量绿＋#266 已关票；**补录行（2026-07-29）**：合入早于本台账建制，应 #260 fail-closed 门要求补权威回执 | 台账制度前的历史合入，追溯登记 |
| 5239be83 | #252 | M1 capability vocabulary 契约切片 | typecheck+test 97/97 | 契约先行 |
| 194a742a | #248 | M1 observability 四轴+drop 契约切片 | typecheck+test | 契约先行 |
| 4e650184 | #244 | 接缝 B＋legacy 两分（2 commits ff） | tsc 0＋24/0＋22/0＋7/0＋e2e 1/0（合入态） | |
| a6e50059 | — | runbook：driver 互斥锁＋续跑判据收紧 | 文档 | 主控直接提交 |
| 2aa34ef6 | #244 | 12 个 finalizer fixture 债清零 | 主控独立库复跑 21/21＋tsc 0 | ⚠️ lane 擅自 ff 合入＋冒用主控前缀发合入记录；内容经主控独立复核**事后追认**。程序违规已在票面裁决，下不为例 |
| 4a4361d0 | #246 | 主体切片（A 校验/registry＋fallback 血统＋C2 warn 消费者，15 commits ff） | contracts+core tsc 0＋registry 4/0＋harness 64/0＋skills 27/0（合入态） | C4（trigger 判别消费）等 #248 M1.5 后补尾款；live Langfuse 与 fresh 全量随 A 批合流 |
| 30cbdb89 | #248 | M1.5：note_page_regenerated 事件合同（trigger 判别，contracts-only） | contracts 106/0＋tsc 0（合入态） | 解锁 #246 C4 |
| 281d327b | #247 | 契约＋机制切片（四上限合同/admission 冻结/共享 attempt budget/挂起恢复/checkpoint，44 commits ff） | contracts 111/0＋harness 六件 170/0＋skills 27/0＋双 tsc 0（合入态）；lane 侧全量 2489/0 | 生产装配保持 unset；#255 标定为最终关票门 |
| 7ef16c7d | #242 | fresh-DB auth schema bootstrap（cherry-pick 自 d43a4c54，纯测试基建） | quality-gates 10/0＋双库 provision 后原红 spec 抽验 4/0＋core tsc 0（合入态）；9 个 session 缺表红根治 | #242 其余面仍在 lane |
| aec25cdc+ab5a7c84 | #253 | A 组交付链后端（due-row 链自续/SKIP LOCKED/租约围栏/eligibility/召回，cherry-pick 自 95fdaae0+32555148） | core tsc 0＋harness 六件 170/0＋due-delivery 全套 fresh 双库 32/0（合入态） | B 组（#253FE）仍等 #264FE→#261 |
| 93072db4（尾 7 commits） | #246 | C4 尾款（trigger 判别接生产发射器）＋outbox 运维 CLI 行为证据＋收口文档 | core tsc 0＋harness 170/0＋contracts 111/0（合入态）；lane 侧红 33/3→绿 36/0、fresh 全量 2533/0 | **#246 关票**；live Langfuse 残量归项目 live 门；六格 invoked/persisted 生产格归 #256 |
| 1f1c81c5 | — | e2e 基建 hotfix：playwright core/worker 块声明 LANGFUSE_PROMPT_POLICY=pilot | three-modal wechat_moments desktop 1/0（43.5s）＋pilot 降级审计打点确认 | 6cef418e 使 strict 成缺省，无凭证 e2e 栈全线拒启；主控直接提交 |
| 26d44359+f795b70b | #242 | L1 事实满足度护栏干净切片（recorded 接缝 provider/exact scorer＋11 frozen golden＋incomplete fail-closed＋promptfoo 正负门与 CI 段，cherry-pick 自 issue/242-l1-slice ee26e007+f3600a8d） | eval 11/11/0＋promptfoo 11/0/0＋control 如期 exit 100＋core tsc 0（合入态） | **A 批合流达成**（#246 关＋#247 机制＋#242-L1）；#257 删除窗口开启 |
| d322ca07 | #267 | adjust 语义绑定收紧（冻结 snapshot 语义 exact 绑定/篡改 fail closed/output count 冻结进 deliverable+reservation，cherry-pick 自 83055ffa；前两笔 8ec79eac/6bd6ec99 patch-id 等价于已合入 2b8fad7d/745b2702） | contracts 111/0＋双 tsc 0＋焦点三件 29/0（合入态）；lane 侧全量 4547/0＋双 e2e 绿 | **#267 关票**：合入态双 e2e 亲验绿（three-modal 43.5s＋price-validity 46.5s；中途一次假红=负载，隔离重跑定性） |
| 2e8252b4（2 commits ff） | #242 | W01 反向校验更正值缺口（candidate revision snapshot 冻结确认/finalize 解析 effective corrected draft/竞态 fail-closed） | focused 21/0/0＋core tsc 0（合入态）；lane 侧 fresh 双库 PG 21/0/0＋负控红→绿采信 | price-validity 归因完成后合入 |
| ef5e5106 | — | web 合同修复：hard-gate demoted 名单摘除 creation-loop（IA 处置后「留档带标记」前提过时，main 单测红） | 该合同测试 5/0（合入态） | lane-261 纯 main 复现上报；主控 IA 合入门当时漏跑 web 单测，记档改进 |
| 50cf5d01（2 commits ff） | #249 | 授权与安全面全量（action registry 单点枚举+启动校验/五类 continuation 收口+effect=0 绕行负例/executeCopySelection 收进 .testing/ApprovalReceipt TTL 单点+原子跨期复核/Skill 复合键 deny-all/伪造 permission 拒绝） | 合入态：焦点负例 53/0＋touched 面 135/0＋contracts 111/0＋双 tsc 0；lane 侧 issue249 双库 PG 20/0/0（skip=0）＋全量 2332/0/157（显式门 skip）采信 | **#249 关票**；七条验收行为门逐条对应 |
| 8e442346 | — | e2e 基建 hotfix：P1 worker 块补 HARNESS_DBOS_SYSTEM_DATABASE_URL（701ef89e 信号化后 worker 终态通知从未接通，image/video 全旅程吃满 150s 超时；契约测试改按块计数 2） | **已验证**：#242 p1-f2 复跑 image_text 1.1m＋video 22.2s 全链绿（此前死于 150s stall）；契约测试 2/0；格式债由主控补修（style commit） | 诊断裁决 a＝e2e 双进程拓扑接线缺陷；copy inline 不受影响；主控直接提交 |
| c433ee7f | #242 | p1-f2 两类基线修正（UUID 8 位后缀＋leak 负控 load-bearing；image/video 显式 source slot 授权走全链，cherry-pick 自 5805b457，仅 2 e2e 文件） | 主控范围核对＋web tsc 0＋biome 0；lane 侧锁下 fresh 双库 UUID+image_text 2/0（1.1m）＋video 1/0（22.2s）＋残留 0 采信 | 同时构成媒体拓扑 hotfix 的行为验证 |
| 36fe818c（尾 9 commits，cherry-pick 自 6574aef3..039a0828） | #248 | **M2 主体切片**：agent_primitive.lifecycle canonical 事件（invoked/succeeded/rejected、root 零绑定=四 null 禁 unknown@0、安全拒绝分类、event_append 防伪造）＋drop 分类/delivery health 持久化＋outbox 加固＋Langfuse sender 投影＋web 事件客户端 | 合入态：contracts 119/0＋双 tsc 0＋焦点观测五件 33/0；lane 侧 PG 并发重放/冲突 terminal 1/0（独立 meiye_issue248 库）＋Agent Team 双轴 P0/P1=0 采信 | **#256 gate 的 #248-M2 半边就此满足**；#248 关票另核（live Langfuse 门等项目 live 窗）；交验单「单笔提交」实为 9 commits 栈，已按全段摘取 |
| 66fa1519（21 commits ff） | #258 | Skill 包全量：官方格式 import/export（Unicode name/numeric metadata，官方 validator 钉扎 38a2ff82 过）＋reference-only skill_define＋prewrite/capture claim 语义（同 key 重试确定性）＋allowed-tools 单一权威＋PG repository（池重建迁移）＋web admin Skill 合同面 | 合入态：skills+foundation 焦点 73/0＋contracts 119/0＋web 合同 3/3＋双 tsc 0；lane 侧官方 validator exit 0＋PG 6/0（池重建）＋浏览器锁下 1/0＋终轮双轴 P0/P1=0 采信 | **#258 关票**；D-150 下游=#259（目录/五命令 UI）+#260（import/export 第一刀）；playwright pilot 行重复（同键同值无害）记清理轮 |
| 2f4191bb（18 commits ff） | #257 | 删除批全量：94/94 复核（81 删/13 保），四批（asset-memory 21/context 5/ops query 15/ops command 40）＋三旧浏览器 journey 删除＋hard-gate 名单同步 | 合入态：retired-e 3/0＋hard-gate 5/0＋contracts 118/0（-1=真删 schema）＋双 tsc 0＋抽验 ls-files 空 ×3；lane 侧全量四包（118/1571/2380/277/165 全 0 fail）＋双终审 0 findings 采信 | **#257 关票**；上报的 observability-event-client biome 债（#248 合入遗留）主控已修（style commit） |
| d09695a6 | — | harness fixture hotfix：DBOS smoke 的 prompt.contentHash 改真实 sha256（#258 强校验 fallback pin 后 4 个 PG 门用例红；fixture 值须生产可产出） | 一次性双库 provision 后 11/11/0，库清零 | #255 上报的上游红；主控直接提交 |
| 676fd8e4（尾 4 commits，cherry-pick 自 b47ba996） | #240 | production media assembly join＋admission 冻结完整 RouteSnapshot（server-owned durable field，caller 注入/缺 resolver/stale route 均 provider 前 fail closed；execution/replay 只读冻结路由，resolver 恰 1 次） | 合入态：focused 45/0/0＋persistence gate 12/0＋core tsc 0；lane 侧 fresh PG+DBOS 17/0/0（绝对锁）＋全量 2339/0/158＋终轮双轴 P0/P1/P2=0 采信 | 方案 1 落地；关票另核票面验收 |
| e67e4e9b（尾 4 commits ff） | #242 | 硬门⑥负向接线 corpus（五类 corpus 单一真相＋真实接缝负向门＋stale ApprovalReceipt 修复：approval 策略用 receipt 冻结 revision） | 焦点三件 34/0/0＋core tsc 0（合入态）；lane 侧负控红→绿 | 生产改动仅 approval 2 行 |
| a5cb77a1 | #242 | IA 切片：uiux-creation-loop 退役 6 条旧旅程（不 skip 洗绿），保 2 条 Day-0，TEST-CATALOG 逐条登记 contract owner（ff，仅 2 e2e 文件 -1226 行） | 主控 diff 范围＋留存 2 test 0 skip 核对＋web tsc 0；lane 侧隔离库锁下 2/0/0＋pnpm check 8 段 PASS | 与 price-validity 红零交集 |
| c9ca6e77 | — | harness hotfix：factTypes=下限非白名单（回收 f795b70b 两条过严子句，保 matched∩missing=∅；+1 回归） | 单测 10/0＋eval 12/12＋promptfoo 11 正门 exit 0＋control 如期 exit 100＋core tsc 0 | price-validity e2e 红根因（诊断裁决 A）；frozen golden 11 条未动；主控直接提交 |
| 4704cbd6（3 commits ff） | #248 | 独立收口切片：公有 event_append 收窄至 merchant rating 单径＋canonical 五事件全量对账（drop summary/delivery health 消费）＋durable cursor 只推进完成窗＋PG DB 时钟边界/interval 宽限/完成行禁并发重算＋生产消费者结构化日志＋signal×destination 矩阵文档 | 合入态：焦点观测四件 20/0/0＋core tsc 0；lane 侧 PG observability-health 2/0（锁下独立库）＋双复核 P0/P1=0 采信 | **非整票关票**：生产 emitters 等 #262 载体，live Langfuse 归项目 live 窗；已披露 P2（无租约重复日志发布）非阻塞记档 |
| 43238d5f（4 commits ff） | #264 | FE 半：商家视频接收面退役编辑入口（subtitle/cover/regen/Pro Studio refine＋原生 captions track）＋videoBillableScopes=[]＋旧视频历史档案只读（copy 保留）＋7 接收 action 回归＋e2e 双进程 DBOS__VMID 隔离加固（合同钉扎） | 合入态：合同 7/0＋interaction 38 文件 226/226＋biome 0＋web tsc 0；lane 侧最终 HEAD 浏览器 4 spec 7/7＋焦点 Node 72/72＋视频交互 15/15＋前稳定树 root test/typecheck exit 0 采信 | **FE 半交验合入**；core 半等 C2 明文开工；#261 J10 退役残项就此解锁；FE lane 轮到 #261 收尾 |
| 77db3390 | #242 | intake-receipt 独立切片（主控优先级令拆出）：W02 promoted batch fixture 补 commandFingerprint（生产 finalize 内联记账带 fingerprintValue，无凭据状态生产不可达）＋生产竞态加固——pinned-heads 守卫扩至本命令确认 facts（assertConfirmedFactsUnchanged，revision 冲突 fail-closed）＋122 行单测 | 主控亲验（lane 未交证据，主控直采）：fresh 库 PG behavior 21/0/0（原稳定红转绿）＋finalizer 单测 12/0/0＋core tsc 0 | **#255 上游 W02 红解除**（本行即 #255 解锁判据）；生产改动仅 finalizer 竞态守卫扩展，2e8252b4 同属主语义延伸；媒体切片继续在 lane |
| da5ac8f0（2 commits ff） | #252 | W2-3 模型供给/catalog：能力词表与需求匹配随冻结路由身份版本绑定（含「空需求」能力版本变更也构成身份变更）＋PriceRevision/供给冻结对新治理事实强制显式执行渠道与定价档（历史冻结仍可重放，不编造 unknown）＋同模型换渠道与跨模型替代两类容灾语义分离（缺渠道或同渠道 fail closed；跨模型替代须声明降级面与渠道绑定等价）＋固定选择在策略与三层排序后仍为首选、请求模型的保守兜底优先于授权替代＋租户白名单丢弃不允许的平台默认并集中告警但不拒绝启动（空白名单＝不限制）＋发现面维持 draft-only 无自动发布 | 主控亲验：拓扑 0/2＋禁区净＋full Core 2849＝2666/0/183skip＋contracts 129/0＋core tsc 0＋根 check-gates 全 PASS；可达性引用抽查四处（foundation-module.ts:2574 冻结固定路由／index.ts:3756 能力匹配／supply-control-plane.ts:249,572 固定模型优先／foundation-module.ts:6241 白名单投影）逐条为真 | **首份按新「可达性证明」门交验的切片**（该门 2fbb716e 刚落 runbook）；183 skip 为真 PG/DBOS 与 live 配额环境门，lane 明确不主张持久化与 live 验收；站点注册/装配消费者归 #262 下游，未在本切片冒充 |
| 84d1af91（2 commits ff） | #248 | 生产观测发射全量（#262 载体解锁后的残余面）：task admission 在 registry claim 后、starter 前写唯一 task_pin root（existing replay 同补），后续 assembly/stage 不再二次写 root；rating 公共命令只收商家自撰评价／撤回，四轴按冻结 task/delivery 由 Core server-owned hydration，ActionUsage 从最终 ProductUsageRecord 投影并校验 terminal/mismatch；bounded suspended／resumed 与 note regenerated 走既有 durable seam 发 canonical event；child span 只带实际单值/absent 轴，catalogRevision 只取 executionSnapshot 而 route 仅作一致性校验；PG root claim 用 first-value CAS 并兼容旧 event_persistence root，异事实永久配置冲突写独立 drop、额外 outbox 进 quarantine/discard 不递归回被丢信号通道；Langfuse sender 支持 task_pin root 并保留旧 root 迁移兼容；信号×目的地矩阵回填 | 主控亲验（代为 rebase 到当前 main）：full Core 2827＝2644/0/183skip＋contracts 129/0＋core tsc 0＋fresh 双库真 PG 5/5/0（canonical emitters／observability-health／production-media-assembly）；lane 侧 Web 非交互 1581（4skip）＋interaction 43 文件 261/261＋双轨终审 P0/P1=0 采信 | **非整票关票**：live Langfuse 两项（按轴远端过滤、Prompt API >1024 中文 exact-version 读回）归项目 live 窗，lane 明确未以 fixture／单测／本地 PG 冒充 live 证据，此边界主控采信 |
| 391d7ed1（2 commits ff） | #240 | 媒体准入双 P0：**P0-1** creation-stage-port 终止判据改错误码白名单——仅 REQUEST_FINGERPRINT_CONFLICT／EXECUTION_SNAPSHOT_MISMATCH 判永久终止，FROZEN_ROUTE_MISMATCH／FROZEN_REQUEST_MISSING 及未知错误保持可恢复，删去准入前的 workspace 归属推断（原 taskBelongsToWorkspace 启发式在首次拒绝时恒 false，致每次准入拒绝都被判永久失败、harness_state=failed 不可恢复）；**P0-2** 冻结路由候选在缺 capabilityProfile 时按模态推导媒体 profile，且由 runtime.activation===local_fixture_verified 门住，生产运行时永不走推断 | 主控亲验（rebase 到当前 main 后）：焦点四文件 32/0/0＋full Core 2820＝2639/0/181skip＋core tsc 0 | **浏览器验收（m04 image_text）无法在当前 main 复现**：#255 引入的三闸门 P0 使任何创作提交先于媒体准入 503（见 #247）。lane 在 main@ddf6c24a 取得的 1/1×2 采信为当时证据，**行为面复验顺延至 #247 修复后的主控判别轮**，不以此充当当前 main 绿。legacy 审计（461 有快照／425 缺冻结路由）策略＝不自动回填，421 终态只作 replay waiver，4 条非终态继续阻断 |
| 9d2e6319 | #251 | **#251 上游（主控直修）**：冻结路由回调只解析被请求模态的平台默认模型：原实现一次解析全部模态默认、任一报错即抛，之后才挑自己需要的那条绑定——于是 image 默认缺激活证据会打掉 copy 请求（生产同样成立，非 fixture 假象）。提出 platformDefaultsForOperation 纯函数（归 platform-defaults.ts，该模块本就拥有 operation↔configKey 映射），无平台默认的操作返回空表＝无绑定，本模态自身的 live_verified＋active 校验一字未动 | 主控亲验：新增作用域负控测试 1/0＋平台默认相关五文件 34/0/0＋core tsc 0 | 由 #251 交底第①条结构性阻塞定位而来，主控核实后直修（跨票生产装配面，不归单票）；#251 剩余两条阻塞（conservative fallback 要 live_verified、fixture 无可满足 14 轴的 capability profile）仍待裁决，不在本行 |
| be31bb64 | #254 | 治理动作幂等键长度收口：raw runId 只作内存 intent fingerprint，传输键改为 action＋UUID（runId 可达 150 字符，键长收在 200 内）；skill_governance_start 一并进入同一 registry，语义统一为「在途重试复用、新尝试轮换」 | **主控亲验**：web 路由单测 9/0（含 150 字符 runId 上限负控）＋web tsc 0＋Biome 0＋浏览器 admin-dashboard-shell 5/5；主控另核领域层按 runId 去重（同 runId 同 inputFingerprint 复放、异事实 IDEMPOTENCY_CONFLICT，service.ts:741），故轮换传输键不会引入重复起跑。**lane 自报（未经主控复算）**：Core 聚焦 125/0、Foundation 双周期 receipt 8/0、真 PG/DBOS 15/0、双轴 P0/P1/P2=0 | **补记行**：本 SHA 随 7d71ee44 一同被主控 ff 快进入 main 却漏记，属主控记账疏漏；lane 直接写 main 补行（越权，见票下裁决），主控重写为当前口径并保留内容 |
| 7d71ee44 | #254 | 治理动作幂等键轮换：原用固定键 `<action>:<runId>`，同一 run 的第二次管理取消被服务端当作首次的重放而静默无效（取消→恢复→再取消循环走不通）；改为按尝试轮换键、仅在途重试复用同键（createGovernanceActionIntentRegistry），并补 Foundation 侧「每个不同的管理取消周期都真实执行」行为测试 | 合入态：core dbos-governance 单测 8/0＋web 路由单测 9/0＋core/web tsc 0＋Biome 0＋浏览器 admin-dashboard-shell 5/5 | #254 关票；用户 2026-07-30 拍板关票，四刀（30e90fa7／c105d9ba／7d71ee44／be31bb64）台账四行齐、代码面已冻结；lane 在停工令时序之前已开工的自查刀，主控亲验属真缺陷后采纳；幂等键语义更正为「标识一次尝试」而非「标识一个 action×run 对」 |
| c105d9ba | #254 | 治理轮询补漏（对主控上一刀的反向补全）：governanceRunStatus 补映射 DBOS `ERROR`／`MAX_RECOVERY_ATTEMPTS_EXCEEDED` → `failed`。主控把「未知即轮询」反转后留下反向缺口——真正死掉的工作流因 state.status 仍是 awaiting_approval／applying 而被永久 2 秒轮询；本刀补上终态识别 | 合入态：路由单测 8/0（含 ERROR 与 MAX_RECOVERY_ATTEMPTS_EXCEEDED 两条新负控）＋web tsc 0＋Biome 0＋浏览器 admin-dashboard-shell 5/5 | `MAX_RECOVERY_ATTEMPTS_EXCEEDED` 已对 DBOS SDK 4.23.6 核实为真实状态；lane 在主控合入后自查补交，主控亲验采纳 |
| 30e90fa7（5 commits ff） | #254 | W4 运营控制台全量（主控接手 lane 收口）：可改槽位代码侧声明（仅 instruction／manifest.description）＋服务端逐字段剥离（越权字段 stripped＋去值审计，success/applied 分离，全剥离与 CAS 冲突均 success=true·applied=false，未知 action／非法 envelope 仍前置 strict reject）＋唯一 Published 生命周期指针与 traffic 绑定分轴（publicationGeneration CAS，draft 版本禁绑）＋workspace 作用域反向依赖（可见明细＋跨 workspace 仅 hiddenCount；scope_kind NOT NULL CHECK IN workspace/global/unknown＋互斥约束，legacy unknown 结构性 fail closed 计入 hiddenCount 不泄身份）＋稳定 runId 的 DBOS 可挂起治理运行（suspend/管理取消/恢复/业务终止/replay 幂等）＋运营台只读渲染面；另修 harness 分库校验按库标识而非 URL 字符串比对 | 主控亲验（lane driver 终端已关，主控接手为唯一写者）：rebase 后 core tsc 0＋full Core 2817＝2636/0/181skip＋contracts 128/0＋web tsc 0＋Biome 1092 文件 0＋interaction 43 文件 259/259＋路由单测 8/0＋fresh 双库 PG 15/15/0（含真 DBOS suspend/cancel/resume/replay、Published CAS 单指针、legacy 缺 scope fail closed、退役×引用竞态）＋浏览器 admin-dashboard-shell 5/5 | **主控自查出并修复一条产品缺陷**：治理运行轮询在状态不可读（读取被 abort／失败）时返回 false 停摆，而此时所有动作按钮恰因状态未知而禁用——面板死锁只能靠手动刷新；改为「仅终态停轮询」并提出 governanceRunPollInterval 纯函数钉死判据（修前 4 轮浏览器门挂 3 轮，修后连跑 4 轮全绿）。#260 后端开工依赖就此解除 |
| f3e64e48（5 commits ff） | #242 | RightsBasis rights-consumer 干净切片（D-150 消费证明）：生产视频导出在 exporter 前强制解析 RightsBasis，缺失／不可用／未知异常一律 fail closed；有来源素材时 MarketingPackage rightsRefs 须与冻结 source asset 精确一致并按目标 platform 实时核 Product 授权；source-free AI 视频须唯一 selected asset 且精确绑定 owned asset→succeeded child run→provider task→route snapshot→唯一 completed attempt→生成时 registry/deployment/contract，条款须当时有效且 commercialUse=allowed；fixture 商用许可只在 explicit local fixture runtime 播种并打 local-fixture 标记、默认拒绝，防 fixture→live 同 revision 冒充；交付 JSON/manifest 只暴露安全摘要 basis，不泄 run/task/provider/terms 内部引用；main 与 job-worker 双装配接同一 resolver | 合入态：焦点 8 文件 168/0/0＋contracts 128/0＋core tsc 0；lane 侧 full Core 2750＝2576/0/174skip＋双轴 P0/P1/P2=0＋scope creep 0 采信 | 权利基础裁决落地第二刀（契约 c02dde70 之后的消费端）；真实 provider termsRevision/commercialUse 录入仍属 live 前 provisioning 门；媒体 17-spec／works-reshell 尾巴仍归 #240 后判别轮 |
| 06e613b1 | #242 | criticality 诊断 delta 收割（主控撞线裁决落地）：harness_fact_criticality_v1 的 runner 异常与输出 parse 异常分记 runner／schema_parse，事件绑定 wf:<id>:s2:facts:criticality:0；factDiagnosticEvent 参数化 schemaName/effectIdempotencyKey（satisfaction 事件形状不变）；questionCardSchema.parse 归 runner；移植恶意 name/code 与 newline-frame 注入负控 | 合入态：fact-satisfaction 单测 17/0＋eval 19/0＋core tsc 0；范围仅 2 文件（+249/-24） | 收割自竞争稿 55e1a3bf 的真差距（合入版 criticality 外围原为裸 catch、零事件）；竞争分支就此可删；判别轮取证不变 |
| 094c8f36（12 commits ff） | #256 | C2 生产收口全量：六 primitive（read_context/generate/revise/record/check/ask_merchant）contracts 注册表＋生产装配可达＋P1 执行接缝（generate/revise 单 provider effect 无重复、revise 走 server-owned target fence＋OCC；check awaited 叠加 #246-C 七门不复制第二份红线；ask_merchant 复用既有 DBOS persist-pending 单写；record 仅 proposal port，#251 管道明确不做）＋四负向合同（伪造 tenant/billing/idempotency/observability、confirm_*、未注册 tool、超 maxIterations 均 fail-closed＋rejected trace）＋canonical lifecycle 审计链 audit_events＋langfuse_outbox | 合入态：焦点 Node 243/0＋contracts 128/0＋core tsc 0＋PG 29/0（skills repo＋harness store，execution_spine 自供给文件前置引导；此前 1 红为隔离跑缺前置的环境类非缺陷）；lane 侧 rebased HEAD focused 229/0＋真 PG 1/0＋正式 provision 双库装配 1/0＋closeout 门 6/6＋终审 P0/P1=0 采信 | **#256 关票**；语义锁核准：只消费 #246-C，深化面归 #250/#251/#248/#247；live Langfuse 发射归项目 live 窗 |
| 6d1c98df（32 commits ff） | #255 | live 门链三件套全量：provider-attempt fence（RUN_LIVE_ISSUE_255 未授权即 fail-closed 禁用）＋live collector/PG live receipt＋bounded-execution 标定（recorded 27 样本 3 模态×3 场景带×3 seed：maxIterations default 2/hardCap 4；cost/wallclock 留 unset 不以合成值冒充 live 真值）＋inspector/reset CLI 与 safe-provision 门（destructive reset 显式 opt-in RUN_ISSUE_255_SAFE_PROVISION_POSTGRES_TEST）＋fence/reset runbook | 合入态：拓扑 0/32＋禁区与凭据扫描净（14 处 RUN_LIVE 命中均为围栏谓词/假 env 测试夹具/runbook 禁用文）＋fresh 双库**先供给后跑**全量 2748＝2735 pass/0 fail/13 skip（首轮主控漏跑 provision-test-db.sh 致 4 文件 9 红，定性亲验环境错；供给后隔离重跑 9/9 绿）＋13 skip 明细核准（3 destructive opt-in＋9 live/provider 付费门＋1 MinIO）＋core tsc 0＋标定证据抽查逐项吻合（fixture 0/recorded 27/live 0） | **#252 C1 门翻开＋#247 标定依赖就绪**；live 仍 NO：#240 P0-1 前置＋主控明文放行；lane 全程 ¥0 |
| 7c86dea5 | #242 | works-reshell 结构化安全诊断切片：fact-satisfaction runner/schema 双 catch＋封闭错误映射（异常 payload 不可信，不读传入 message/stack）＋结构化事件仅限裁决字段＋凭证泄漏负控（prompt/fact/route/credential/provider 关键词及伪 sk-*/AKIA stack 均不入事件）＋logger 故障不替换 conservative guidance | 合入态：单测 13/0＋core tsc 0；frozen golden 区零 diff（仅 2 文件） | 采集就位；works-reshell:204 取证并入 #240 后判别轮 |
| c02dde70 | #242 | RightsBasis contracts-only 切片：rightsBasisSchema 严格 XOR（source_asset_authorizations{nonempty rightsRefs} ⊕ ai_generation_terms{generatedAssetId/runId/providerTaskRef/termsRevisionId/commercialUse:allowed}）＋SupplyContract.commercialUse 显式录入位（optional 仅为历史可读，消费者对缺失 fail closed） | 合入态：contracts 121/0＋contracts/core 双 tsc 0 | 契约先行（权利基础裁决落地第一刀）；delivery manifest 消费者与 provisioning 录入随后续切片 |
| 33e67ac9 | #242 | confirmed-store-facts fixture 干净切片：seedConfirmedStore 改走真 finalize_store_intake 命令路径（service/price 从已确认 project 派生、profile OCC、断言 revision 0→1）＋拆 dashboard-home-mount/m04 两处第二套造数旁路（W01 空账本负控保留） | 合入态＝lane 验证树同一 tip：静态门采信＋主控范围核对 3 e2e 文件 +135/-154 | dashboard-home-mount 两红的修复面；行为绿归 #240 后判别轮统一验证 |
| 27dcd374 | — | harness fixture hotfix：DBOS smoke 期望快照补 skillCapabilityRequirements（#262 给冻结 skill 束加该轴后，双库门 smoke 未随更新——#262 全量跑时该测试被 env 门 skip 掩盖，#255 双库全量揭出） | 双库 fresh provision 后 11/11/0＋core tsc 0，库清零 | #255 上报的上游残留；d09695a6 同类；主控直接提交 |
| 7c4f12a7 | #242 | three-modal source-slot 干净切片：image_text/video 提交前统一走 seedComposerInlineAuthorize 可见上传授权（copy 不造素材），保 #261 后 receive 语义不回带 edit/adjust；仅 2 e2e 文件 | 合入态＝lane 验证树同一 tip（merge-base==main）：pnpm check 1092/0＋typecheck 0＋--list 8 tests＋diff-check 0 采信；主控范围核对 2 文件 +4/-3 | **#253FE GO 判据①落账**；行为性绿（three-modal image_text 真跑）受 #240 冻结门 P0 压制，归修复后主控判别轮统一验证 |
| a6fd4409（4 commits ff）＋457da759 | #265 | 品牌统一「丽客美页 LIKEPAGE」全量：盘点清单先行＋六 i18n 键双语改名＋可重跑扫描门挂根 pnpm check＋两条 Landing 界外发现记账；457da759=主控加固扫描 walker 跳过点目录（.playwright-cli/.impeccable 等本机残骸致门裁决依赖本地垃圾，#261 debt e2e 残留触发发现） | 合入态：scan --check 0 对外/16 内部＋locale 3995 双语 parity＋scan 自测 4/0＋check-gates 4/0；lane 侧拉平复验（#261 +30 键零回流，门的抗回流实证）＋真 ZIP/邮件 0 命中采信 | **#265 关票**；i18n-motion spec 复验留主控 e2e 欠账批（非关票门）；Landing 两条归 R 门重估 |
| 2c979709（5 commits ff） | #262 | **三轴装配载体全量**：六步 assembly audit 经生产 LangfuseHttpSender/markLangfuseSent 全链（单 task-root trace、确定性 trace ID）＋RouteSnapshot 版本钉扎（在途任务保 r1、新任务见 r2，catalog/capability/credential/provider/lifecycle/endpoint 六轴）＋#240 单一 durable carrier 无第二真相＋outbox 去全局 claim＋readiness 脚本与负向测试 | 合入态：改动面测试 387/0/11skip＋core tsc 0；lane 侧根 pnpm test 全包 exit 0（Web 1589/Core 2437/Contracts 118/Canvas 277/根脚本 172 全 0 fail）＋锁下真 PG+DBOS 装配 1/0＋readiness exit 0 采信 | **#256 全门就此满足（#248-M2＋本行）→ C2 批开闸**；#248 emitters/#261 评价真值解锁；关票终核另轮 |
| 7dfdfb0b | — | e2e spec 重基线：uiux-shell-routes token 双主题八值＋焦点环＋资产库→素材；mobile-product-shell 身份素材→口吻与素材、表达身份→口吻（加 exact）；删两条宿主已亡断言（next-action-guide 组件 27e74740 已删／今天的创作记录只剩 pendingComponent） | 亲验：agent 逐值 file:line 出处表＋web tsc/biome 双 0（独立 worktree）；主控复核 spec-only 2 文件 ff | **考古更正**：token 期望自出生（0dd43524，07-20）即错——166/188 青绿盘从未在 styles.css 存在过，spec 生而未跑；--product-guide 零消费者/theme.ts 注释陈旧/home_hero_preview_title 孤儿键 记清理轮；主控直接提交 |
| 177ae8e4 | #240 | D-153 残项收口：非 hold 路径孤儿预留穷举（启动 owner/lease fencing/权威拒绝/ambiguous retry＋exact-text 零用量不二次消费＋decision resume 可回收 lease/稳定 effect key＋terminal intent-first settlement/五分钟 grace/orphan owner 重建＋commit-refund task 互斥/冲突归档/序列化后 fail-closed＋backlog 查询排除已 owner 防 LIMIT 饥饿） | 合入态：被改测试全套 102/0/31skip（PG 门显式跳）＋core tsc 0；lane 侧 focused 101/0＋full Core 2397/0/166skip＋真 PG+DBOS 32/0（锁下，等锁 3060s）＋双轴 P0/P1=0 采信；证据 docs/evidence/issue-240-d153-…md | **#240 残项清零**，关票待票面验收终核；改动面横跨 execution-spine/harness/billing＝预留生命周期属主面 |
| 282f95b9（25 commits cherry-pick 自 issue-261，base 5e96f555） | #261 | Dashboard 单路由三段全量：D-137 历史只读文案＋记忆一级导航四域（D-164④）＋?view= 收敛重定向＋提议→创作→继续三段（D-164①）＋配方 pill 行五类任务分组（D-164②/D-139，D-083 形变已裁留痕）＋执行确认卡与成本即时反馈（D-164③⑥，CNY 清账）＋评价条三动作与 follow-up chips（D-164⑤/D-160③，发不出即留 drop 负向证据）＋ImageAdjustConfirmation 收编（D-164⑥A，ls-files 空）＋?view= 独立取证 spec | 合入态：web tsc 0＋biome 0＋locale 3995＋interaction 43 文件 259/259；lane 侧五门全绿（root pnpm test rc=0）＋9 项变异验证＋gate 6/7（余 G3b） | **e2e 证据欠账**（用户裁定暂不跑；四 spec 待补，J13 两条 main 陈旧 spec 红归清理轮）；票不关（余：评价三轴真值等 #262、settled 投影等 #248/#262、记忆三域等 #251/#252）；**#253FE GO＋#265 合入窗开** |
| d55d4e7c（13 commits ff） | #259 | Skill 第八能力域基础面：可信 prompt/EvalRun 权威边界＋immutable PG registry/importer＋运营结构化目录与五命令派发面 | 合入态：Core 64/0/11skip（PG opt-in）＋Web 23/0＋三包 tsc/Biome/diff-check 0；真 PG repository+importer 16/0/0；绝对锁浏览器 1/0（11.2s，整轮 1.2m）；lane 终审双轴 P0/P1/P2=0 采信 | **#259 关票**；浏览器仅 route-mock dispatch proof，真 PG 独立，未宣称 live Langfuse/provider |
| 62a050f0（11 commits ff） | #264 | D-133 视频编辑面终态退役（core 半）：`video-regeneration` P1 module／capability／runtime／foundation／repository／migrator 与 PostgreSQL 生命周期物理删除；subtitle text edit 在进入 canonical video workflow port 前拒绝，foundation-module 只收 select／reorder；video result adjustment 在 prepare 与 confirm **两端**均以 `RESULT_ADJUST_OPERATION_UNSUPPORTED` 拒绝且拦截点位于任何 scope／inherited asset 计算之前，不产生 quote intent／derived Work／submission／reservation／等待态；新视频 ZIP 不再读写 legacy cover，旧 cover／subtitle／evidence schema 与存量数据仍可解析（不做破坏性 DROP）；checklist 按裁决改口（抖音封面字幕自选／视频号只留 upload video.mp4／小红书图文封面指引不变）；D-123 三桶口径复核文档 `docs/reviews/issue-264-d123-video-billing-review-2026-07-30.md` | 主控亲验（合入态）：删除证明自跑——`git ls-tree` 无任何 `video-regeneration*` 文件、生产面（排除测试）token 扫描 0 命中，仅剩两处退役负控测试；禁区未动（docs/specs／design／ops／.scratch／台账）；根 check-gates 九门 Overall PASS；core tsc 0；contracts 129/0/0；焦点三文件 25/0/0（**首轮我用 `node --test` 直跑 .ts 报 ERR_MODULE_NOT_FOUND，属我方跑法错，改 `tsx --test` 后全绿**）；两端拒绝的代码位置逐行核对 | lane 侧真 PG 1/0／根脚本 177/0/1skip／`CI=1 pnpm test` exit 0／双轴四轮到固定点 P0/P1/P2=0 **采信未复跑**；本 core lane 未重跑浏览器旅程；FE 半此前已合入关账。**gate 自锁修复记档**：该 lane 曾因 gate 把「已 rebase 到最新 main」写成放行前置而停摆 10 小时，主控改 gate 后恢复（纪律已入 runbook `878f0701`／`26d8f38f`） |
| 95ba435f（43 commits ff） | #250 | HITL/ask_merchant 深化基础切片：**无界等待家族三入口全闭**——①两个生产 producer（`fact-satisfaction.ts:262`／`unified-media-stage-ports.ts:185`）改声明 `unattended:'hold'`，生产侧唯一 `'continue'` 只剩带 `semanticDefaultAuthority` 的 intent-gap 卡；②`expireUnrendered` 补齐 `unknown→unavailable` 缺失边，`held/renderer` 走向取消并退额度（`core_hold_expired`）；③新增 `ExecutionConfirmationWaitingMessageCard` 专属卡片，waiting 态不再劫持主创作输入框。另：转追问身份校验＋原始 payload 透传；legacy `/decision` 撞 waiting 返回 `stale_question`；带选项卡片找回自由文本；成组回答改显式拒绝（不再静默丢弃）；编辑租约按 generation 定属主；反问重新武装计时器；删除不可达的 legacy promotion reader；**跨版本 durable replay 布局门**（`HARNESS_INTERACTION_LAYOUT_RESET_REQUIRED`，命中即绕过 settlement／terminal write／closeStream 后 rethrow） | 主控亲验（合入态）：三个 P0 的出口逐条走通——producer `unattended` 取值全量核对、`expireUnrendered` 生产调用链与终态核对、前台 waiting 卡片渲染点核对；布局门旁路结算逐行核对（`dbos-workflow.ts:690-698,768-771`）；禁区未动；根 check-gates 九门 PASS；core tsc 0；contracts 137/0/0；焦点四文件 82/0/0 | lane 侧 focused 166/0／Web 15+7／真 PG interaction 5/5／DBOS smoke 15/15（含 renderer expiry 冷恢复、r2 exact-once、pre-be bounded、pre-T45/pre-C1 replay）／三卡 Playwright 1/1 **采信未复跑**。**rollout constraint：pre-a9 durable interaction 记录不受支持，携带者须成对重置 business＋DBOS 库，不提供自动迁移／resume／system default／退款**（成立前提＝环境可弃：main 上 #247 三闸门使任何创作提交 503、live 门从未开启）。资源闸门切片待 #247；最终互票验收待 #251；`marketing-composer-harness.spec.ts:197` 旧用例红归 #247 压制，未改相邻旧 spec |
| 7eabb985（主控直修） | #248 | **#248 善后**：`postgres-store.postgres.test.ts:124` 断言从文案正则 `/idempotency conflict/i` 改为按错误类型 `TaskRootObservabilityConflictError` 判定——#248（`84d1af91`）引入命名错误类时未同步该 PG 测试期待，测试为 PG opt-in 故在当时的证据面被跳过、红了未被发现 | 主控亲验：fresh 库先供给后跑，定向 1/1 绿；根 check-gates PASS | **主控合入面缺口记档**：合入 #248 时未跑 PG-backed 测试，导致文案型假绿滞留 main 约一整天，由 #250 交验时报出。断言改判类型而非文案，比原写法更强 |
| b8fb8ebe（4 commits ff） | #247 | **D-167 有界执行全票（主控接管收编）**：原 lane 因 codex 配额中断留下 41 文件／6682 行未提交，主控保全为 `wip` 快照后 rebase、复核、补完。①**决策 B 落地**——`provenance` 增 `deliberately_unbounded` 档＋`authorization`（owner／reason／recordedAt）签署留痕，resolver 据此把该轴移出 `requiredLimits`；`'unset'`（未标定）仍 fail closed。运行时语义零变化（控制器一直把 `'unset'` 当作不触发；live 花钱守卫在 receipt 预留路径 `issue-255-postgres-live-receipt.ts:1083` 的 ¥3.60／¥5 硬顶，本就不由这两道闸门提供）。②**e2e 播种路径**：`scripts/ci/seed-issue-247-e2e-provisional-bounds.mts`，双 env 门控（`APP_ENV=e2e` ＋ 显式 `RUN_ISSUE_247_E2E_PROVISIONAL_BOUNDS_SEED=true`）＋幂等 CAS，临时值标 `recorded_provisional`。③**触顶为可续挂起态**：`media-bounded-execution.ts` 新增，出口＝向商家发 `unattended:'hold'` 的 `boundedExecutionQuestion`（遵守 #250 的不变式），续跑解析器 `AdminConfigBoundedExecutionContinuationResolver` 生产已装配（`main.ts:2085` 第 7 位实参），提升被 `previous >= hardCap` 抛错＋`Math.min(hardCap, previous + default)` 双重封顶 | 主控亲验（合入态）：**22 套 opt-in 逐套独立新库、先 `provision-test-db.sh` 后跑 —— 21 绿／1 红**（红＝`usage-ledger-invariants`，经复核为 #270 既有红，同错同条数，本票既未造成也未改变）；core tsc 0；contracts 137/0/0；bounded 专项 8/0/0；根门禁**十门**全 PASS；**播种真库实测**：写入后 `requiredLimits` 三条全部有值，创作提交不再 503；B 方案端到端实测：签署两轴后 `requiredLimits` 缩为 `["maxIterations"]`，未签署轴仍精确拒绝 | **全项目唯一硬阻塞解除**——#250 资源闸门／#253FE／#260／#263 的阻塞条件同时满足。**主控自身失误记档**：验证中 `delivery.postgres.test.ts` 一度全红 5 条，系我串行复用同一个库造成的污染，换全新库 5/5 绿——而「每次用全新库」正是我同期写进 #251／#269／#270 开工令的要求。`postgres-store` 定向跑 4 红＝缺 full-suite 跨文件建表的环境产物，全量跑绿，已记入证据 note。生产侧仍 fail closed（无任何环境写入过带签署的配置），live 前需用户以 owner 身份签字 |
| 5c2ab835 | #250 | composer 旧旅程 e2e 半修（主控直修）：旧旅程从 brief 确认直接抢跑提交计数，被 D-164 新增的执行确认卡拦停——补点击 execution-confirm-accept 后，brief→执行确认→提交段恢复，提交信封断言全部走通。残余红定位到传输层漂移：该旅程的问题卡 mock 仍打 legacy decision 端点，而 #250 基础切片后客户端改从 typed-interaction 端点（interaction＋renderer ack）拉卡，同文件 durable-interaction 条 mock 新契约即绿。迁移移交 #250，序在其 bounded 授权 P0 之后 | 主控亲验：e2e-lock 实跑该文件 1 passed／1 failed，failed 即上述漂移点（:216 composer-question-card），:197 原红不再出现；执行确认卡 testid 与挂载点逐行核对（execution-confirm-card-panel.tsx:135、composer-home.tsx:2491） | 更正记载：此前 #250 基础切片行把该 spec :197 红归因「#247 压制」，经 #250 复验与主控独立确认，真因是 #261 执行确认步未被旧旅程消费，与 #247 无关 |
| e76815e5（3 commits ff） | #270 | SupplyRequestFreeze 契约保持收紧（不放宽、不补伪默认）：三条生产服务级 PG 场景原以旧式 ModelDeployment fixture 调真实 ModelSupplyApplicationService，缺 executionChannelId／pricingTier 事实；补齐为可由生产 catalog 真实产出的通道／档位，并把冻结持久化、重启后事实、同 attempt replay 同 freeze、8-job exact-once（canonicalUsage=1／freezes=8／providerAttempts=8／executorCalls=8）钉进断言。钱相关生产实现零改动 | 主控亲验（合入态）：两条原红套件 fresh 双库先 provision 后跑——entitlement postgres-repository 10/10/0、usage-ledger-invariants 2/2/0；抽查发布面 fail-closed（foundation-module.ts:4601-4606）与冻结拒缺字段（foundation-ledger.ts:764-767）逐行核对成立；lane 侧 7 套 19/0/0＋生产供给链八环 file:line 清单＋非 PG 35/0 采信 | **建票定性更正（主控自误）**：原票题「生产调用点未跟上」不成立——生产链每环自带事实，被 #252 收紧打红的只有测试 fixture，与 #269 同班（「不是我以为的缺陷」）。残留核查项：#252 收紧**之前**已发布的 catalog revision 走读路径是否同样 fail closed（环境可弃前提下低风险，记档不阻塞）。#270 关票 |
| 50064baf（14 commits ff） | #253 | **主控接管收编（用户令）**：lane 因四 spec 门（根因 #251③）停摆且交底停在 7/29，driver 由主控停机接管。栈内容三类——①readiness gate 入仓（`scripts/ops/issue-253-readiness.mjs`＋630 行自测，此前 gate 活在 /tmp 无人 review，入仓即受审计；含台账 ff／cherry-pick 栈注解解析）；②harness workflow 依赖命名重构（十个 positional 实参改 named object，`main.ts`／`dbos-workflow.ts`，实例与接线零变化，降低装配位次核对成本）；③due-scanner 接线守卫与 DBOS smoke 适配。主控收编三笔：中断残留的 smoke／workflow 测试适配（stash 保全后补完 `ca635f37`）、幸存孙进程占位身份提交的断言修复（`e2fb4533` 复核后收编）、第三处源码文本守卫补完（`792d6d0c`） | 主控亲验（合入态）：core tsc 0；三源码接线守卫 33/33/0；**19 套 opt-in 逐套独立新库先 provision 后跑**——17 绿＋postgres-store 4 红＝已记档跨文件建表产物＋due-delivery restart 1 红＝#269 既有墙钟红（其 lane 修复在途）；smoke 首轮 6 红经判红纪律定性为主控跑法产物（子进程按 cwd 解析 tsx，从 worktree 根跑必炸，从 apps/core 跑 15/15）；根门禁十门 PASS | **#253 票不关**：B 组（#253FE）尚未开工，等 #251③ 修复合入后四 spec 判别轮转绿再启；readiness gate 后续维护随票走。**接管期杀进程纪律记档**：杀 driver 只按命令行 grep 会漏 code-mode-host 孙进程（其 argv 不含 lane 路径），须按 cwd（lsof +D）清扫 |
| 4ceeeccb+eec644c7+b01c0d78+29e78156+4517e11a | #242 | **孤儿分诊第一批（用户令，主控主会话执行）**：issue/242 三十个孤儿逐一 patch-id 排查（30/30 无 main 等价），摘入五个孤立面 core fix——①`4ceeeccb` boot capability heads 过期刷新（hot-assembly）；②`eec644c7` 242 复审缺口收口（hot-assembly 补充，p1-f2 spec 冲突取 main 侧较晚合入的 zoom 取证法，孤儿的取证口径改写弃）；③`b01c0d78` fixture brief 尊重已授权事实（ai-sdk-runner，即 #268 票面正在重做之事，先合者定契约）；④`29e78156` SSE 断连错误收容（server.ts）；⑤`4517e11a` pg-boss worker 轮询错误保活 | 主控亲验（合入态）：focused 46/46/0；core tsc 0；**11 套受波及 opt-in 逐套独立新库先 provision 后跑——10 绿；admin-supply-runtime 单跑 5 红＝排序产物（缺 `p1_admin_supply_idempotency`），同库先跑建表方 provider-credential-runtime 后 17/17/0，实证非类比**；根门禁十门 PASS | 缓摘四个：`85590363`＋`bf1fc816`（note 结果调整，撞 #250 正在开刀的 workflow-core 语义锁，其 P0 落地后再摘）、`b7e60ff3`（task-admission 在 #240/#247 后大改，须逐行核是否被取代）、`38423c50`（旅程 spec 对齐，与 `5c2ab835` 半修重叠，归 #250 旅程恢复波）。web fix×8＋test(web)×12＋docs×1 归 FE 波一并处置。孤儿源分支保留不删 |
| 42c32a7d（5 commits ff） | #269 | due-delivery 双修（用户拍板口径「停机追赶只投最新一天」）：①重启假红根治——测试时钟钉死，不再依赖真实墙钟；②生产陈旧性抑制——`DueDeliverySuppressionReason` 增类型化 `stale_business_date`（无字符串旁路），worker 在 claim→eligibility 后、beginDelivery 前按与 `nextDailyRecommendation` 同源的 UTC 日界判 stale，陈旧项在**同一 claim-settlement 事务**内落 `suppressed`＋链次日（链条恒前进直到追上当日转真实投递）；优先级 workspace_inactive＞stale＞rest_day，workspace_inactive 仍按既有合同断链 | 主控亲验（合入态）：due-delivery 三套 PG 逐套独立新库——postgres-repository **11/11/0**（含 one-day／multi-day 追赶与钉钟重启）、eligibility 1/1、task-recall-producer 1/1；worker 非 PG 9/9；core tsc 0；guard＋十门 PASS；worker 陈旧分支与同事务结算逐行核对（worker.ts:171-195／postgres-repository.ts:441-473） | **#269 关票**。本票史：主控建票定性两错（「重复投递」实为双 taskId 各一次；「自落地恒红」实为墙钟漂移假红，评论已公开更正），lane 判别后主控在其外查出真问题（停机 N 天连投 N 条），用户拍板只投最新。生产可达性=job-worker.ts:702-726 既有装配，无新出口悬空 |
| 83601672（2 commits ff） | #250 | **资源闸门 P0 修复＋typed-interaction 旅程交验**：①有界续跑授权收口——`bounded-execution-limits.ts` 的 `resolve()` 首行 `assertBoundedExecutionContinuationAuthorization`，授权对象仅由 `workflow-core.ts:2682-2701` 在五字段精确匹配（questionId／workflowRevision／field／decision.state==='accepted'／value）时以 Symbol 键铸造，普通 ask_merchant 决议（groupSkip／deferred／skipped→ignored）再也换不来续跑；②逐轮问题身份 `…:bounded:rN:aN`，skip／defer 走持久 reask（CAS）而非 resume；③硬顶触达＝诚实停牌卡（不可续），`capability()` 只读预检（unset／hard_cap 描述符）在卡片展示前消费（:1024-1031 copy／:2151-2158 media）；④composer 旧旅程迁移完成——`5c2ab835` 半修移交项收口，问题卡 mock 全量改 typed-interaction 端点（tasks/*/interaction GET/POST＋interaction/message＋renderer ACK），legacyDecisionSubmissions 断言为 0 | 主控亲验（合入态）：负向五文件 apps/core 直跑 **120/120/0**（bounded-execution-limits／dbos-workflow／workflow-core／ask-merchant-resolution／interaction-service）；e2e-lock 实跑迁移后 `marketing-composer-harness.spec.ts` **2/2 passed**；PG 抽样四套逐套独立新库先 provision 后跑——smoke 15/15（apps/core cwd）＋media-bounded-continuation 1/1＋delivery 5/5＋interaction-service 5/5；根门禁十门 PASS；证据 JSON 16 套 lane 自刷（52c20b18）＋主控抽样 4 套盖至合入 SHA。lane 侧 core full 3069（1 红＝#269 已修墙钟红）采信 | **#250 票不关**：跨票负向互验（CROSS_TICKET_READY）等 #251 台账合入后跑。`306e67d2` 行的旅程迁移移交项就此闭环。缓摘孤儿 `85590363`＋`bf1fc816`（workflow-core 语义锁已解）自本行起可摘 |
| 5eab9138+01064fcf+aecb1fb9 | #242 | **孤儿分诊第二批（缓摘三枚，#250 P0 落地后语义锁解除）**：①`5eab9138` note 结果调整支持（source-content-package-resolver＋submission-coordinator）；②`01064fcf` note 选择在调整时保持——workflow-core 冲突并解取孤儿 frozenStyle 语义（decisionReferences 逆序取 note_style）＋HEAD 三参 `awaitResolvedDecision` 签名，unified-media-stage-ports 测试冲突保 HEAD 的 `unattended:'hold'` 断言＋并入孤儿的 resumed 流断言；③`aecb1fb9` 冻结快照决策准入——主控取代性核对结论：**未被取代**，main 的 `snapshotWorkflowInput` 在快照准入时仍丢 `semanticDecision`，`semantic-decision-resumption.ts` 只覆盖工作流内续跑缝、不覆盖持久快照重准入；孤儿两条新测试夹具漂移（写于 #240 冻结路由要求前），适配现约后收编 | 主控亲验（合入态）：core tsc 0；受波及 focused 145/145/0＋task-admission 23/23/0；**19 套 stale opt-in 逐套独立新库先 provision 后跑——17 绿；postgres-store 7/4＝已记档跨文件建表产物；assisted-canonical-repository 单跑 1 红＝缺 `p1_content_packages`（provision 不建此表），同库先跑建表方 operations 套件后 1/1 绿，实证非类比**；十门 PASS | **主控自误记档**：第一轮 opt-in 扫描与我在同一工作树的 cherry-pick 并发，冲突标记窗口期把 3 套跑成加载红——纪律：证据扫描必须在干净树、终态 SHA 上跑。#242 剩余：`38423c50`＋21 枚 web/test/docs 归 FE 波 |
| 695489fe | #255 | **direct copy 拒绝根因修复（主控直修）**：lane 第 4 轮 live 首笔 direct copy 被 provider 拒（`AI_APICallError`），主控只读诊断链——DeepSeek key 真实（/models 200 且列出 deepseek-v4-pro）、余额 ¥51.51、max_tokens 13792 在 [1,393216] 内、fence 字节闸已过（authorization=1 证明真发了 HTTP）；用捕获式 fetch 零成本截获 runner 出站体，发现 `response_format:{"type":"json_object"}` 而 messages 全文不含 "json"；以同体免费重放 DeepSeek 得 400 `Prompt must contain the word 'json' in some form`——**逐字实锤，非类比**。修复＝runner 对 `deepseek-v4-*` 在 json 模式下且指令缺 json 关键词时追加传输级指令 `Respond with a single valid JSON object.`，覆盖 generateCopy／adaptPlatformVariants／generateStructured（流式＋首试＋修复路径）；定位为与 response_format 同级的传输层指令，不改 promptBinding 记录 | 主控亲验（合入态）：runner 22/22/0（含新增双断言：缺词追加＋已含词不重复追加）；adapters＋structured-node-runner 52/52/0；core tsc 0；修复后离线截获出站体 468 字节、messages 含 json；6 套受波及 opt-in 逐套独立新库全绿；十门 PASS | **全项目 direct-LLM json 模式对 DeepSeek 均踩此雷，非 #255 独有**。诊断中三次免费探针（/models×2、/user/balance、必拒 400×2）零计费。**同时查明下一颗雷：`TUZI_MEDIA_API_KEY` 是 30 位占位符（与 MODEL_DIRECT_API_KEY 同值），图/视频腿必 401**；本机无真兔子 key；`ARK_MEDIA_API_KEY` 真实且实测 /models 200——Tuzi 补 key 还是改锚 Ark 已上报用户拍板。1/3 占用处置与新信封见票下主控评论 |
| 77bc3646（9 commits ff） | #251 | **模型供给与记忆治理全栈（最终交底合入）**：①`c59f68b5` fixture copy 能力画像——为 fixture 部署补 `capabilityProfile`，Harness 全轴冻结路由不再落 conservative fallback（纯增量 43 行＋两套测试断言全轴 eligible）；②受治理记忆沉淀管线——`ProductionMemorySedimentationCoordinator`（main.ts:1998 装配）在两条真实交付分支完成后调用（production-stage-ports.ts:879/922），`summarize()` 独立无副作用 seam（production-memory-sedimentation.ts:64）由生产 `complete()`（:119）消费后走四态拦截／红线／持久化；③provenance tombstone 后端入口 reuse-memory-service.ts:1602＋前台 memory-vault-page 消费；④删除语义按 B 裁决：只证明 memory-owned 快照 tombstone＋记忆保留＋来源已删标注，**真实 Composer 会话删除明确 pending #271，未冒充完成** | 主控亲验（合入态）：core tsc 0；focused 33/33/0（runtime-config／runtime-assembly／memory-sedimentation／record-proposal）；接线四处逐行核对成立；PG 抽样四套逐套独立新库全绿（memory-sedimentation-pipeline 1/1、parse-asset-memory 1/1、pending-actions-invariant 2/2、interaction-service 5/5）；十门 PASS（首轮 workspace 红＝我后台 e2e 的 `.paraglide-dev.json` 运行时文件，e2e 结束复跑即绿）；lane 全量 35/36 绿（1 套自门控 skip）采信 | **四 spec 判别轮（主控实跑，e2e-lock 下 11.3 分钟）**：1 pass／7 fail／12 未执行——数字未变但**错误面整体更替**：「No active live-verified platform default」5 处归零（#251③ 根因确证已修），新前沿＝composer 面「这个方向暂时没有可用的模型」（quote readiness 判 no_model，fixture catalog／platform default 在 copy 方向缺可用模型），属 #253FE 判别波工作面（主控持票），非 #251 合同项。**#251 票暂不关**：等 #250 跨票负向互验（本行即其机器判解锁信号）。#250 fail-closed 拒把「提交在 main」当合入凭证的处置记一笔正面档 |
| —（验证轮，无新提交） | #250 | **#250×#251 跨票负向互验（新模式首单：主控派 codex exec 执行，stdout 交底）**：基线 main@7e1d666a（#251 九笔＋#250 资源闸门 83601672 均为祖先）；#251 负向治理 focused 38/38/0＋PG 独立新双库 1/1/0；#250 资源授权五文件 121/121/0；零跳过、零产品代码改动。CROSS_TICKET_READY=true | 主控评审采信（与主控自身合入态实跑 120/120＋33/33 交叉吻合）；临时双库已清 | **#250 关票＋#251 关票**（gh 关闭，不再发评论交接——2026-07-30 晚用户拍板通信内部闭环）。#250 头号裁决遗产：Symbol 键授权铸造缝＋逐轮问题身份＋诚实停牌卡；#251 遗产：fixture 能力画像 A 路修法＋B 裁决（真实会话删除 pending #271） |
| bc717983 | #240 | **复验收敛（codex 派发轮）**：票面逐项核对全清——提示词 14 位点注册已被 #246 固定点取代（langfuse-prompts.ts:25 单一注册表＋闭集断言）；生产装配级准入 join 已被 main 吸收（main.ts:2078/2189）；join 行为证明、D-153 孤儿预留、媒体准入双 P0 均已完成并有 file:line 证据；#247/#251 落地后的行为复验以源码/接线/真 PG＋DBOS 完成。净增量仅证据刷新一笔 | 主控评审采信：focused 114/114＋110/110、prompt 真库 12/12＋fallback audit 11/11、production assembly 与 DBOS smoke 独立新库 1/1＋15/15、十门 PASS；执行者判红纪律良好（Unix socket URL 假红换 TCP 新库重跑） | **#240 票暂留**两项：①4 条 active legacy 属运营动作（逐条取消＋退款/补偿核验后重提，不得 catalog 自动回填）；②浏览器 e2e join 面随 #253FE 四 spec 判别轮统一验。两项清后关票 |
| a16b8a6d | #268 | **净增量收敛（codex 派发轮）**：票面核心修复（fixture brief 尊重已授权事实）已由孤儿 b01c0d78 先行合入，本票收敛为 e2e 覆盖净增量一笔——w01-storefact-wiring 显式消费 D-164 计划确认卡步骤＋fixture 事实授权断言（2 文件 +102/-2） | 主控评审采信：playwright --list 解析 68 文件/221 测试 exit 0；core tsc 0；纯 spec 增量不触第十道门；一次服务端 400 中断后续作收口，现场零丢失 | **#268 票暂留**：两条新 spec 的浏览器实跑随 FE 波 e2e 批统一执行，绿后关票。**主控自误记档**：首次 ff 因管道吞退出码带着台账行先行落库（已 reset 重做）——纪律：合入链禁把 git merge 接进管道 | 
| 3c1b4279 | #248 | **接管收官（codex 派发轮）**：票面验收项逐项核对全部已在 main——四扁平键/复合 revision/scene（observability.ts:39）、root write-once＋child 单值（task-admission.ts:387/520/790）、drop 一等事件＋防递归（postgres-store.ts:2761）、last-success/queue-age/对账（observability-reconciliation.ts:100）、rating ingest＋canonical 事件、消耗两数据源拒显式 0、信号×目的地矩阵。净增量仅 PG 证据刷新一笔 | 主控评审采信：core focused 208/208＋contracts 10/10；fresh PG 3 套独立新库 5/5；十门 PASS；临时库清零 | **#248 票暂留**两项 live 窗：①远端 Langfuse 四轴过滤行为门②Prompt API 中文 >1024 exact-version 回读——`LANGFUSE_*` 三项凭证缺失（fail-closed 门在 langfuse-prompt-live-acceptance.ts:22），**列入用户供给清单**，凭证到位后一次核销（D-132） |
| 5d58a4bb（2 commits ff） | #264 | **残留三项收尾（codex 派发轮）＋关票**：①交付 checklist 改口——『上传 video.mp4 与封面』→『封面可在抖音发布页自选或自动生成』（D-138 指引），revision 等工程词汇按 D-116 去除，doesNotMatch 双负向钉死；②商家侧四退役动作 UI 摘除经核早已完成——交互负向断言齐（video-worksurface.interaction.test.tsx:24/28/32/99），七保留动作锁定（video-action-inventory.test.ts:13），confirm 前视频调整显式拒绝零副作用；③D-123 三桶复核页已存在（76d7014a），结论=三桶/加油包/视频 3-6-9 均不调整不改价 | 主控评审采信＋抽验 diff（断言与改口文案逐行核）：TDD 先红后绿 ZIP focused 10/10；core tsc 0；focused 52/52；fresh PG 5/5；web 20/20＋4/4；十门 PASS | **#264 关票**——D-133 修订全量落地：核心退役（62a050f0）＋checklist 诚实性＋UI 负向证明三段闭环 |
| df7b1402（3 commits ff） | #271 | **Composer canonical 会话删除 seam＋记忆 tombstone 联动（codex 派发轮）**：属主端口 `deleteComposerConversation`（workspace/membership 校验＋幂等＋审计事实 entityType=composer_conversation）；生产链 P1 HTTP→capability 门→OperationsFoundationModule→`ReuseMemoryComposerConversationDeletionNotifier`（main.ts:1353 装配）→合入版 `delete_source_conversation` tombstone；**PG trigger 以 42501 拒绝绕域直删 p1_creative_works**，Work 永不冒充 Session | 主控评审采信＋抽验（notifier 装配/trigger/端口三处）：contracts 139/0＋core 2800/0＋十门 PASS；三负向全证——删后记忆可读带 source-deleted 标注／无 membership 403 无副作用／直删被 42501 拒且 Work 行保留；rebase 证据冲突以三方 overlay 解，合入态 16 套重扫 14 绿＋2 已实证排序产物（assisted-canonical 缺 p1_content_packages；migration-cli 缺 product_states，同库先跑 store-intake-finalizer 后 1/1） | **#271 票暂留**：前台删除入口归 FE 波，落地后关票。D-168② 生产可达性就此闭环（#251 B 裁决的承接项完成） |
| b0b446c0（2 commits ff） | #252 | **收紧前已发布 catalog revision 读路径 fail-closed（codex 派发轮，#270 残留核查项判中）**：读取边界确有伪默认链（postgres-admin-supply-runtime.ts:1826 的 unknown/standard＋index.ts:4814 price/tier 默认链）；修复=PG 读取边界校验 executionChannelId/pricingTier，缺失抛带 revision ID＋字段清单的 P1DomainError(INVALID_STATE)，不补默认不回写历史；直插旧式 revision 的 PG 行为测试钉死（读取/初始化拒绝＋不进 hot-assembly/冻结路由＋库中原貌保留） | 主控评审采信：TDD 先红后绿（17/1→绿）；focused 118/118；8 套 PG 新库 28/28；8-job 不变量保持；core 全量 2797/0；十门 PASS | #252 残留核查项就此关账（票面主体此前已合入） |
| 561ab568（3 commits ff） | #255 | **live 标定 v2 收官（codex 派发轮，GO v8 口径）**：①image 证据修——adapter mediaUnits 缺 usage 块时从实际返回图片计数推导＋collector per-image 放行条件＋无图仍 fail-closed；②v1 failed_before_billing 契约迁移已在前轮合入，本轮 image 行按兜底路径结清（taskRef 未持久化，billable-consumed，usageEvidenceKind=price_card_reconciled，providerTaskRefMissing=true 记档）；③三锚点 durable 终账：copy=completed ¥0.012551／image=completed ¥0.05／video=submit 被拒（provider did not accept the fixed sample，attempt pending，未计费未补发）；已知总成本 **¥0.062551**，3/3 授权额度耗尽 | 主控评审采信：adapter 10/10；focused 32/32；PG receipt 19/19＋collector 1/1；core 全量 2801/0；typecheck 双面过；e2e 锁纪律与临时库清理合规 | **#255 票暂留**：video 锚点未成——只读拒因诊断已另派（M 路），根因明确后主控发新信封单笔重试；标定回填与 #247 关票等 video 锚点到手 |
| e19961c1（4 commits ff） | #253 | **#253FE no_model 破障（codex 派发轮，关键路径）**：根因链十环 file:line 全录——#251 的 capabilityProfile 只进了代码侧 fixture 部署，PG 持久 capability head 停在 epoch 2 无 profile（postgres-hot-assembly.ts:140 只认更大 epoch），精确指纹比较含 profile（hot-assembly.ts:508）→不匹配判 inactive（index.ts:1257）→catalog 空→quote-readiness no_model（:111）。修复=boot epoch 提 3（runtime-assembly.ts:66）＋profile 回归断言；另迁移 typed composer interactions 消费（e7c51d84） | 主控评审采信：**四 spec 终轮 7 passed／4 failed／9 did not run（从 1 pass 跃升），no_model 症状归零，copy 主链全通**；六套 opt-in 独立新库全绿；十门 PASS；core/web typecheck＋biome＋focused 27/27 | 残余 4 红=三类真产品缺陷（C6 激活 6 次超约／媒体恢复 Tracer job 丢失／works 图片链同源恢复失败），执行者未改断言掩盖——**已建 #273 承接并派发**。#253 票留 FE 收尾与四 spec 全绿判据 |
| 72df2e13 | #248 | **两道 live 门收官关票**：①Prompt 门（71a9ac71，L-2 实跑）：version 1、3174 字符、9502 UTF-8 字节、SHA-256 932e41c2…、exact-version 回读逐字节一致；②四轴过滤门（72df2e13）：主控亲手取证定案——经典 ingestion span 只进 v1、v2 observations 仅服务 OTel 链路，四轴合同改判 **trace 级** `/api/public/traces` filter；live 实跑 skillRevision/catalogRevision/promptVersion/scene 四轴 pos=1/neg=0（traceId 5ee7c0bb-8439-52c0-a2ea-74c5cad4a3f7，observation axisScope=task_root 经 v1 回读），矩阵文档第 26 行同步改口径；verifier 加 429 退避＋≥5s 轮询 | 主控评审采信：worker focused 20/20＋core tsc；主控亲跑 acceptance 单测 4/4＋远端抽验 trace 四轴齐、scene 过滤精确命中 | #248 关票 |
| d1a42ecc | #260 | **copywriting 第一刀完整通路＋skill-creator 配方后端面合入**（19 commits，容量故障两断两续零丢失）：①生产真接线 main.ts→createDurableSkillRuntime→provisionPlatformRecipes，两条 factory 启动时 define/accept/publish/bind/deploy；②普通会话完整通路 read_context→ask_merchant→propose→confirm→immutable revision→catalog＋Task/DBOS/skill-prompt-catalog 三轴 trace（store-workflow-capture.ts 887 行）＋PG 表达式 UNIQUE 建索引修复；③诚实单变量 A/B：两臂共用 fixture 输出、仅 treatment 加 skillInstructions，结论 unchanged/delta 0 不冒充 live 效果，assertion-control 负门预期 exit 100 | worker：core tsc 0、skills/production/capture 24/0、contracts 139/0、promptfoo 正门 1/0＋负门预期红、PG 7 套独立新库 19/0、十门 9/10（唯一红=分支期陈旧证据、未伪造）；主控亲验：rebase 后 skills 单测 143/0、tsc 0、evidence JSON 三方覆盖 6 键 | **#260 票暂留**：浏览器 e2e 面归 FE 波统一批，跑绿后关票 |
| 2d835f73 | #272 | **Langfuse AI SDK tracing 合入关票**（3 commits，容量故障两断两续）：LangfuseSpanProcessor+NodeSDK 接线、langfuse-config 凭据两态（无凭据零开销 no-op／全凭据启用）、API/Worker 停机等待 NodeSDK.shutdown、environment 按 LANGFUSE_TRACING_ENVIRONMENT→APP_ENV→NODE_ENV→development 解析；真实自审 trace 1c0897829c734d86c4a1ea89fe7f554d：AGENT→SPAN→GENERATION 层级、I/O 完整、usage 17/29/46、三轴 actor→userId／task→sessionId／workspace→metadata、tags llm+copy.generate（fixture 模型标名 gpt-4o-mini，成本为 Langfuse 价表推算、零真实 provider 消费） | worker：focused 55/0、tsc 0、十门 10/10；主控亲验：远端回读 trace 三轴/层级/usage 与报告一致、rebase 后 focused 25/0＋tsc 0、evidence 三方覆盖 8 键 | **#272 关票** |
| cb4a9e71 | #242 | **FE 波孤儿预收编合入**（22 picked / 0 skipped，源=孤儿分支 issue/242 的 fix(web)×8＋test(web)×12＋docs×1＋fix(core)×1）：38423c50 冲突按「main 侧较晚合入者定契约」解——保留 HEAD 生成权利链/typed-interaction/D-164 布局/activation 语义，仅收仍适用的历史旅程、fixture fact filtering、pending recommendation、snapshot identity 修正；另去重 video source seed、焦点重试适配 D-164 | worker：web build＋web/core tsc 过、biome 1106 文件 0 修、interaction 279/279、web 契约 17/17、core 定向 24/24、playwright --list 221 条可解析；主控亲验：rebase 到 2f962fb7 干净落地、装依赖后 core/web tsc 0、interaction 279/279 复跑绿 | **#242 票暂留**：浏览器 e2e 实跑归 FE 波统一批（含 #268 两新 spec＋#261 leftovers），批绿后与 #253/#261/#268 同窗关票 |
| 67d7cbf8 | #252 | **关票核对（零增量）**：票面 7 项任务清单＋4 项行为验收逐条核对全部已在 main——能力词表 v1（supply-registry.ts:34/model-capability-vocabulary）、Price 通道轴与分档（catalog.ts:87/108）、两类容灾语义（failover-semantics.ts:67＋计费事件/降级面/等价替代测试）、catalog 治理 draft-不进生产＋allowlist 告警（catalog.ts:318/339、foundation-module.ts:2021）、CJK 精确匹配＋保守回退（hot-assembly.test.ts:221）、14 位点与 hot-assembly 先读后接按 D-150 转 #262 且已接线（langfuse-prompts.ts:25、hot-assembly.ts:532、index.ts:4259 生产消费） | worker：contracts 4/0、core focused 124/0、PG 独立新库 21/0（5 套分账）、tsc 0、临时库清零；主控抽验 4 锚点属实 | **#252 关票**（live provider 激活属发布证据边界，非本票行为缺口） |
| 463ead17 | #247/#255② | **标定回填机制合入**（ecceaafa）：admin-config 键 `harness.bounded_execution.live_calibration`——每数值强制携带 live evidence ref（禁 recorded 冒充）；copy=12551/image=50000 CNY micros 真实锚点固化，video 成本与三模态 wallClock 留 unset（fail-closed 503 不变）；config_apply CAS 下发免重启（main.ts:1635 热读）；`deliberately_unbounded`=逐轴显式策略（owner/reason/recordedAt 三要素），普通 unset 恒 fail-closed；推导公式带 default/hard-cap 安全系数（iter 2x/4x、cost 2x/4x、wall 1.5x/3x，策略参数随 revision 可调、非标定结论）；live 钱硬顶事务未触碰 | worker：tsc 0、focused 54/54、fresh PG 4/4、临时库清零；主控亲验：rebase 干净、31/31 复跑绿 | **#247 票留**：等 v4 video 锚点经 config_apply 回填＋安全系数拍板后关；#255 同窗 |
| b3ffde49 | #255 | **三锚点收官合入**（10 commits，v3→v5 五轮攻坚）：v3=错误持久化＋fence 删 seconds 强写＋结清 failed_before_billing（真实拒因 Ark duration unsupported，¥0）；v4=seconds=5 根修（提交首次被接受）＋accept 即持久化真实 task id＋失败留原始响应体；v5=Tuzi/Sora 状态词表映射（含 unknown→pending 同步窗口语义）＋GET-only 恢复结清：completed、mp4 取件 4,497,123 bytes、¥1.62 price_card_reconciled、ProviderCost 1 行、wall-clock 上界 35s（提交 00:27:04→签名 URL 00:27:39 双时间戳证据链）；正式 manifest live-anchors-v5.json（SHA-256 979c7e60…，task id 只存 hash）；v2/v4 行如实保持 unknown 归外部对账窗 | worker：focused 44/44、PG receipt 25/25＋collector 1/1 独立库、tsc、diff-check；主控亲验：durable v5 行 completed/1620000 micros/HTTP 6 直读吻合、manifest SHA 亲算吻合、fence 9/9 复跑、rebase 后 tsc 0 | 三锚点齐：copy ¥0.012551（provider_reported）/image ¥0.05/video ¥1.62（price_card_reconciled）；**#255 票留**：标定回填 config_apply＋安全系数拍板后与 #247 同窗关 |
| 903dc07e | #273 | **四 Spec 残余三缺陷根修合入**（2 commits）：①C6 激活 6→3——三个多余激活源全灭（harness 旧 page-plan confirmation：unified-media-stage-ports.ts:214；note_style 二次提交：ask-merchant-group-card.tsx:99；brief 确认后叠加 execution confirmation：composer-home.tsx:2519），D-164 断言未放宽；②媒体恢复四重根因——bounded authorization 丢失、fixture CNY 价被默认 USD revision 覆盖（runtime-assembly.ts:182）、DBOS replay 旧 jobId NOT_FOUND 误判永久失败（index.ts:2679）、失败 attempt 未先 checkpoint 致原始错误被覆盖；③works 链 source-free 图片误强制 rights refs（output-compiler.ts:232 改为有源才要求），promotion_poster 单图无方向卡钉死断言 | worker：四 Spec 锁内全新库 **20/20 全绿**（8.4m，严格 3 激活、image/video 恢复、works 真实交付导出全过、Tracer job was not found 归零）、初始红 9/3/8 →绿、core focused 174/174、web interaction 10/10、PG/DBOS opt-in 17/17、双端 tsc、focused biome；主控亲验：rebase＋evidence 三方覆盖 2 键、装 #272 依赖后 tsc 0、focused 62/62 | **#273 票留**：随 FE 波 e2e 批在合并态复跑后关票 |
| 9b4a5b69 | #260 | **fixture 启动回归修复合入**：skillAcceptanceGateFailure（service.ts:1898）原来只认 Langfuse production revision，Langfuse 未配置环境（builtin/isFallback/unconfigured 降级）在配方 acceptance 硬死致 fixture Core 无法 boot、FE 波 e2e 批被阻。修复=精确放行 builtin+unconfigured fallback 冻结、revision 如实持久化 builtin 出处不伪装、configured 环境其余 fallback 仍严格拒绝、define/accept/publish/bind/deploy 全程未跳过 | worker：TDD 红先行复现 INVALID_STATE、provisioning 4/4、skills focused 145/0/17、tsc 0、fixture boot 实证（4100 监听、/health/assembly 三面 active、PG 两平台 revision=accepted_frozen/builtin/unconfigured）；主控亲验：4/4 复跑＋tsc 0 | FE 波 e2e 批解锁重跑 |
| a4f99aee | #255/#247 | **v6 image 耗时采集合入**（3 commits，用户拍板补拍）：唯一一次 live POST（¥0.05 price_card_reconciled，ProviderCost stage=reconciled/billing known）、provider 请求/响应双时间戳 durable 落行 wallClockMs=10871、taskRef 持久化（交底只留 hash）；manifest live-anchors-v6.json（SHA 0e14808e…）三模态 wall-clock 齐：copy 33106ms（v2 durable 行）、image 10871ms（v6 实测）、video ≤35000ms（双时间戳链）；累计实花 ¥1.732551 ≤ ¥5 | worker：focused 27/27、PG 27/27 独立库、tsc、diff-check；主控亲验：rebase 干净、11/11 复跑、manifest SHA 亲算吻合 | 标定回填只欠 iterations 证据（FE 批绿后提取）→ config_apply → #247/#255 双关 |
| f9c9e2de | #253 | **合并态 live-verified 平台默认回归修复合入**（rebase 后 SHA，原记 c0d7b660）：根因=foundation-module.ts:2325 对同 revision 的 recorded fixture head 早退，DeepSeek 激活证据无法迁移到新 registry revision→copy.generate 平台默认判非 live_verified（main.ts:2280 拒绝）、FE 批 8 spec 连锁红。修复=发布版本化 fixture projection（recorded-default-v1:fixture-live-verified-v1 / live_verified / active），生产判定 main.ts:2274 未放宽、断言未改 | worker：原样复现 m04 红→修后 API 拒绝消失、TDD 红 11/12 先行、focused 92/92、tsc 0；主控亲验：runtime-assembly 12/12＋tsc 0 | 遗留：m04 修后暴露独立 #260 Skill 物化错误（另票处理中）；FE 批第三跑待该项落地。**主控自误二犯**：本行首次落库时 git merge 又接了管道吞掉 ff 失败（基线落后未 rebase），行先于合入推送；已 rebase 至 f9c9e2de 补完合入并以本修正为准——纪律重申：合入链禁管道、merge 前必在主 checkout 验 cwd |
| 900e0d26 | #260 | **Skill 物化回归修复合入**（FE 批最后一颗已知螺栓）：#260 把 capture-store-workflow@1 以 harness_native 绑到 intent_naming 后，DurableSkillInstructionResolver（runtime.ts:95）把该 stage 全部 instruction 无差别送进只收 prompt_materialized 的 receipt writer（service.ts:1420 严格门）→ workflow 起跑即死、composer 永不播报。修复=runtime.ts:100 只有 prompt_materialized 写物化 receipt、全部冻结 instruction 照常进 stage assembly、生产严格门不动、builtin 出处三元组继续传导（service.ts:1787） | worker：TDD 红先行复现错投、目标集 6/6、skills/harness focused 144/144、全 skills 147/0/17、tsc 0、fixture boot 实证（stage line×3、资料整理→要求编译→主推荐、文案交付成功、无物化错误）；主控亲验：runtime.test 2/2＋tsc 0 | FE 批第三跑放行 |
| bc96d17b | #240 | **运营尾项收官合入（codex 新会话，旧会话被上游 429/504/400 三连击杀后换新）**：4 条 active legacy 逐条显式取消（3×core_hold_expired＋1×workflow_failed，decision/audit event 逐项留证）＋退款/补偿核验（quote+usage 逐项，无 GrantLot 补造）＋按现行五步录入通路重提交 4 条（issue-240-ops-resubmit-*，全部拿到真 frozenRouteSnapshot，不复用 a0c579e99a57 的不可用历史 ContentPackage）；旧 copy 工作流 builtin-v1 Skill pin 经正式 define/eval/accept 生命周期升级到 Langfuse production v1，无直写表。**前置解锁（主控执行）**：14 条 harness prompts langfuse:prompts:push 真推（全 v1/production）＋LANGFUSE_PROMPT_VERSIONS 全 14 键落仓根 .env，strict boot 通过 | 终审计（只读）：task_requests 578／frozenRoute 153／legacy 恒 425（delivered 364/failed 58/hold_expired 3/**active 0**）；4 新 submission 均非 legacy；core tsc 0；worker 进程精确清点退出（99650/99651，端口 4417 清空，lane-268 进程未触碰）；证据=docs/evidence/issue-240-ops-legacy-closeout-2026-07-31.md | **#240 关票判据只剩行为复验**=FE 全批在新标定生效态跑绿（与 #247 config_apply 后行为面同窗取证）；遗留治理项：2 条冗余 live binding（同内容 v1 指令、生命周期无 unbind）已如实记档待清理 |
| a6218ad1＋本行 | #247/#255 | **标定回填 config_apply 完成（主控执行）**：`scripts/ops/apply-issue-247-live-calibration.mts`（显式 RUN_ISSUE_247_LIVE_CALIBRATION_APPLY 门＋schema 校验＋CAS，与 issue-247 播种脚本同一 PostgresAdminConfigRepository.apply 留痕通道，未起 Core、未触 DBOS 恢复）将 `harness.bounded_execution.live_calibration` 写入业务库 revision 1。值=三 live 锚点（copy 12551µ/33106ms、image 50000µ/10871ms、video 1620000µ/35000ms 上界）＋observedMaxIterations=1（issue255_live_generation_receipts 全 completed 行 generation_submit_count=1）＋默认安全系数档（成本 2x/4x、耗时 1.5x/3x，用户批）；payload=references/evidence/issue-247/live-calibration-2026-07-31.json（-f 入仓，先例 v6 manifest）。**记档**：a6218ad1 因 git add 撞 gitignore 退 1 致链路错配（script 带了台账 message、行与 payload 未入），本行即修正，pushed 历史不改写 | 真库回读验证：Source.read 派生 iterations 2/4、cost 337/674 cents、wallClock 118466/236931ms；resolver 三轴 requiredLimits 全满足；live_calibration 优先于播种 limits 键，FE 批将在真标定生效态跑 | **#247/#255 关票判据只剩**：FE 全批在此标定下绿；live 钱门（¥3.60/轮、¥5 全局）独立不动 |
| 82e503d0 | #271 | **前台会话删除入口合入（codex 派发轮）＋关票**：入口置于 /dashboard/recent 的 session 记录行（低频破坏性操作不进正在创作的会话面，决策合产品审美）；AlertDialog 确认步带诚实文案（「已经沉淀的记忆会保留，并标注『来源已删除』」）；经 operationsCommand 消费 df7b1402 的 delete_composer_conversation 合同（client.ts:254→server.ts:2433→foundation-module.ts:512），成功即时移除、取消零请求、403/网络失败可见反馈；中英 paraglide 键补齐 | worker：web tsc 0＋biome 1108 文件 0＋locale parity 4014 键＋interaction 47 文件 283/283＋历史列表 Node 4/4；主控亲验：代码逐段评审（成功才移除/pending 防重/错误可见）＋rebase f51f4e80 干净＋新 spec 冷库（meiye_271_verify＋真标定）锁内单跑 **2/2 绿**（确认-取消-删除主链＋403 负向） | **#271 关票**：后端（df7b1402）＋记忆 tombstone 联动＋前台入口全链闭环，D-168② 生产可达性完成；不依赖 #274 绿门（其 spec 已独立冷库取证） |
| b3d82157（3 commits ff：b5b9ac5e＋6bb497a6＋b3d82157） | #274 | **冷库全绿门修复合入（codex 主力＋主控终修接力，上游 429/502/504 十余次击杀零丢失）**：①冷 workspace ledger 授权——generation ledger 五操作对内部 worker actor 放行 owner 校验（application-service.ts authorizeGenerationLedgerAccess），商家授权路径不变；②冷态身份 fallback 合同化——identityFallback 升为 marketing 合同必填输出（contracts/marketing-package.ts＋evidence）；③**容量队列根因**——lease acquiredAt 误用冻结路由 snapshot.createdAt，商家问题等待 ~60s 后新 lease 即被 stale cleaner 判过期秒删（model-supply-admission.ts:356），W03/W10 卡死真身；④W03 失败档重构——fixture 在 provider 层 rejected_before_accept，真实持久化/退款路径结算；⑤durable interaction snapshot 投影——interaction?view=snapshot 新 seam（postgres-store/server/harness-client）＋专职轮询 slot（交付后 pending 每 2s 续读）；⑥**转录 turn 保留（主控直修 d898ed06→b3d82157）**——applyComposerQuestion(null) 与 success 落卡两处都在剥 question turn，settled 横幅永无 DOM 锚点，改为保留 turn 由 slot 自决渲染；⑦admin/catalog spec 冷态收口（locator 过度收窄改 per-domain section 计数、退役 /decision seam 迁 typed-interaction snapshot），零断言放宽 | worker：Core focused 113/113＋contracts 15/15＋web interaction 17/17＋双端 tsc 0＋biome 0；主控终修：reducer TDD 红先行 20/20＋interaction 全量 285/285＋tsc 0；**终验（主控，纯净库 meiye_274_final3＋真标定，e2e 锁内单次 run）：6 spec/16 条 16/16 全绿（8.2m）**；rebase 3ed9ae25 干净＋locale 重编后双端 tsc 0 | **#274 关票**；冷库全批（全 spec）绿门开跑，绿后九票连关；workerd 稳定性随全批观察 |
| 8ec22fe3（4 commits ff：44648cbe＋52d39532＋3efaa06b＋8ec22fe3） | #275 | **image 冷库红修复合入（codex 主力＋主控两轮裁决/直修）**：①**criticality 根因**——fixture fact-criticality 无视意图，把普通护理海报缺失的 discount/fulfillment 一律判 critical，image workflow 永久停 store_facts/PENDING（ai-sdk-runner.ts:1641，改为 claimsPromotion/claimsFulfillment/claimsQualification 条件化）；②image.edit 平台默认投影补齐（platform-defaults.ts＋workspace-provision.ts，spec 断言的 runtime-default:platform.defaultModel.image 冷库可达）；③**主控裁决一：拒绝恢复退役确认卡**——worker 一轮按 T20 陈旧断言恢复 note_plan_confirmation 分支，违反 #273/D-164 三激活合同，令回滚产品改动（3efaa06b，harness 三文件净零 diff）、改 spec 不改产品；④**主控裁决二＋直修（8ec22fe3）**——T20 第二处陈旧：readPendingQuestion 走退役 decision seam，而 note_style 问题按 #271/#274 合同走 interaction 通道（pending_projection kind=harness_interaction，postgres-store readDecisionTarget 对其有意返回 null），且分组卡按 label 渲染无 option-id testid（旧 testid 点击从未被真验过）；改为 UI label 定位答题，删除孤儿 helper；⑤workerd 触发器闭环——无独立 SSE 泄漏，死亡链=stuck-PENDING→SSE 拖到 Playwright 超时取消→workerd 死→全线 vite fetch failed 假红，①即根治 | worker：Core focused 77/77＋63/63、tsc 0、TDD 红先行；主控终验（纯净冷库 meiye_275_gate＋真标定，e2e 锁内）：image-intent 3/3＋T20 1/1 **4/4 全绿（3.1m）**，且 note_style 问题库内实证按现行合同出题（options/label 全对） | **#275 关票判据达成**；下一步：journey 家族红（golden-journey/marketing-harness/pending-actions/p1×2）复测自愈假设＋note 家族（w01/three-modal/m04）定向复验＋chunk1 重跑 workerd 存活验收 |
| d5c8f786 | 冷库绿门收官 | **intent-routing interaction seam 迁移（主控直修，spec-only）＋冷库全图终值**：chunk1 复跑曾在 intent-routing 两条 SSE 红后 workerd 连锁带崩 landing/m01（12 假红）；主控查库实证 s1:promotion_details 每次正常出题但 pending_projection=harness_interaction——decision GET 按设计 null（postgres-store readDecisionTarget），且合同把 ask_merchant presentation.notification 焊死 z.literal('none')（agent-primitives.ts:170）→ 问题不进 pending-actions inbox 是**合同级决策**（42bb1b07，07-30）。spec 迁移=snapshot 同步（interaction?view=snapshot）＋对话卡答题（card-family:371 现行合同）＋interaction POST skip；test 题面「inbox semantic question」改「conversation semantic question」 | **终验**：c1-tail（纯净库 meiye_c1_tail＋真标定，锁内）intent-routing 3/3＋landing 9/9＋m01 1/1=**13/13 绿（3.5m）**→chunk1 全片冷绿收口；chunk3 复跑（meiye_gate_c3 重建）42 绿/8 红零塌方，8 红全属旧债家族（pro-studio×5/asset-upload/registration/t33）；journey-heal 批 25 绿含 w01/three-modal/m04/card-family | **九票关票证据链闭合**：#242/#253/#260/#268/#273/#240/#247/#255 即关；#261 改判保持 open（自家 uiux-upgrade-b 家族冷红）并入二波；journey 余红 4 条（pending-actions:237=inbox 承载问题合同级退役需实质重写/golden-journey=fixture 走 D-127 退役写通路/marketing-harness:23=旧 execution-confirm 面板 testid/p1×2 未深查）全带诊断入二波票 |
| 0486e21c | #277/#261 | **uiux 家族冷库红清算合入（二波 B，codex 单轮交付）**：五类红全部定性 spec 陈旧——四个 uiux-upgrade-b spec 本是 Z1 换壳后整文件 fixme 降级的死壳（断言对象=已拆除旧工作台，catalog 原文「合同待 reland」），本票在现行 Composer/Result Center 上 reland 全部合同（-2370/+333，组合既有 ui-journey fixture 零新增断言逻辑）；hard-gate 四文件移出降级登记恢复全量激活；uiux-mobile-secondary/keyboard/shell-routes/day0 按现行文案与交互面对齐 | worker：focused 4 spec 12/12＋家族 34/34 冷库绿、树净；**主控亲验（克隆冷库 meiye_277_gate＋锁内）：uiux 全家族 10 spec＋mobile-product-shell=40/40 绿（9.1m）**；静态评审三疑点全反向核实（非删覆盖/加严 hard-gate/断言在共享 fixture） | **#277 关票＋#261 凭欠账四 spec 冷绿一并关**；golden 克隆管线首次实战（建库 0.1s） |
| 38f2bb7c | #276 | **journey 家族 5 spec 合同迁移合入（二波 A，codex 单轮交付＋rebase 487b8c85 干净）**：五红全部定性 spec 陈旧并迁现行合同——pending-actions 重写为 approval-only inbox＋interaction snapshot/答题 seam（问题不进待办=合同级 z.literal('none')）；p0-golden 弃 D-127 退役写通路改走 Composer→ContentPackage→canonical assisted handoff；marketing-harness 删 mock 路径上的旧 execution-confirm 面板点击（真 D-164③ 确认卡在同文件 390-621 行有现行覆盖）；p1-integrations 迁写入式 BYOK＋回执合同；p1-recorded 迁「本次使用」radio＋内容历史搜索合同。纯 spec＋catalog，零产品代码零 fixture 改动，零 fixme/skip | worker：冷库 5/5＋定向回归 3/3；**主控亲验（克隆冷库 meiye_276_gate 锁内）：五迁移 spec＋intent-routing 回归邻居=9/9 绿（5.3m）** | **#276 关票**；journey 家族全域冷绿，#22（P0 golden journey 锁定）凭 p0-golden 冷绿一并清账关闭 |
| 553c45ab（3 commits ff：4630ae2e＋47d82c44＋553c45ab，rebase 后 SHA） | #279 | **散红清算合入（二波 D，codex 诊断改半→主控 checkpoint 两笔→grok 按 handoff 收尾一笔）**：①fix(harness) fallback 路由冻结改信 registry 证据投影——refreeze 前置 activationEvidenceStatus!=='live_verified' 硬抛（权威门）原样保留，删候选行陈旧字段冗余二道门（冷库滞后，#253 同型），配 +49 行合同测试；②五 spec 合同迁移：asset-upload（TEST_DATABASE_URL 兼容/删退役授权凭证步/objectKey 内容哈希断言）、registration（provisionedByUserId 改字段不存在断言+行内 admin.name）、t33（产品修=dashboard-header 手机视口 gap）、video-native（D-164③ 确认卡步＋成功 stage 按 id 去重，image 旅程同款合同）、w12（渐进事实卡价格有效期步）| 主控亲验：Core focused frozen-route 6/6＋tsc 0（rebase 前后各一遍）＋五 spec 冷库批（克隆库 meiye_279_gate 锁内）**8/8 绿（3.8m）**；首跑 webserver boot 期 workerd 通道断为基建抖动非测试红，重跑即绿 | **#279 关票**；video-native/w12 自愈复核完成（红因=陈旧合同非 PENDING 残留）；#263 开窗复核：本合入 diff 与 overlap_pattern 零交集，窗口锚点 b7a426ca 仍有效 |
| 6f4fa3a2（8 commits ff，rebase 37fcea74 后 SHA） | #263 | **冻结面归档移出主干合入（Opus 单代理交付，D-155 修订）**：头条=票面「整块归档」前提修正——p1/integrations 10,338 行实为混装单体（冻结代发面 3,327 行 32%／活代码 7,011 行 68%：BYOK/密钥保险箱/供应商凭据/飞书，判活依据=生产装配链逐条到 main.ts 行号），执行**按语义切除代发面**：douyin 五文件＋五混装文件内切除（application-service 4200→2537 等）＋main.ts/server.ts/job-worker 去挂载＋孤儿 DouyinPort 删＋11 条权限登记＋前端抖音面板全链＋74 i18n 键＋商家可见代发承诺文案撤回；最强证据=生产只装配 RecordedDouyinAdapter，全仓无 live 适配器，代发从未可能真发。归档=references/frozen-publish-face-2026-07-31/（-f 入仓，59 快照＋restore.patch）；取回记录按用户豁免降为 26 行最简版（演练豁免前已顺带跑过一次：restore.patch 实打后与锚点逐字节一致）；五张 douyin 表不 DROP 只停新建，deleteWorkspaceFacts 加 to_regclass 守卫（旧库注销清理行为不变/新库不抛）；净变更 60 文件 +306/-8838 | worker：core test 全量 0✖＋tsc 绿＋web test/interaction 288 绿＋e2e 复核集多轮（p1-integrations flake 判定带锚点同负载对照证据链）；**主控亲验（rebase 后）**：ls-files 32 文件 douyin 0＋core tsc 0＋冻结边界三 spec 冷库批（meiye_263_final 锁内）**3/3 绿（3.7m）** | **#263 关票**；遗留：publish:* distributionTarget 枚举 Composer 侧收口另开票（D-155 另一半）；'douyin' 枚举/证据字段按存量行保守保留；snapshot 冗余按 D-155 明文保留待用户裁 |
| 16c5b86d（3 commits ff，rebase 98c86480 后 SHA） | CI 信号恢复（#147 前置） | **main CI 连红 8 天止血三件套（Opus 诊断→codex 单轮交付）**：①拆定时炸弹——build-release-manifest.test.mjs fixture 绝对时间（expiresAt=07-28）改相对 Date.now() 生成，被测过期判定未动；②receipt 形状定论（主控裁决=恒带 label/source）——postgres 测试期望补齐 900e0d26 引入的 provenance 键＋新增内存/Postgres receipt 一致性断言（skip-当-pass 分叉的封堵样板）；③release-manifest 作业降为显式触发（workflow_dispatch/release-candidate label），仓库 variables 全空自进 push 门起从未可能绿，required 门未动，工作流契约测试同步改写编码新政策 | worker：scripts/ci 67/67＋postgres 单文件 10/10 零 skip＋skills focused 双库 164/164＋core check 过；主控亲验（rebase 后）：scripts/ci 67/67＋postgres 10/10 零 skip（克隆库实连）＋core tsc 0 | 本次 push 触发的 CI run=root-quality 第二步真红面揭盅点（07-26 起被炸弹掩盖的 web/core 单测现状）；诊断全文 #147 评论 |
| fbc6072d | #146 | **Ark video duration 根修合入（codex 重派单轮交付，182k tokens）**：#255 冻结回执实证 Ark 顶层 duration 注入被 doubao-seedance-1-5-pro 拒（外层 HTTP451/内层 400 InvalidParameter `contents[0].***.duration`）；修法=注入改条件化＋新 protected `videoSubmitBodySupportsDuration()`（Ark 基类对 `/^doubao-seedance-1-5-pro(?:[-_.]|$)/iu` 返回 false 不注入，Tuzi override true 保留内部字段并继续改写 multipart seconds=5）；回归测试双侧（ark:435 模拟冻结拒因、tuzi:404 锁 seconds=5），4 文件 +74/-5 | worker：provider/adapter focused＋video lifecycle 33/33＋core typecheck；**主控亲验（rebase 125391c8 后）**：ark+tuzi adapter 26/26＋core tsc 0；红线遵守（未设 RUN_LIVE_*、未发真实请求、未碰 issue255_live_generation_receipts） | **#146 代码侧完成，票保持 open**：live 重试＋v3 结清仍待用户侧 Tuzi key 轮换与 v2/v4 上游账单对账后开授权轮 |
| c8555f02 | #147 清算 | **root-quality 6 条 web 单测 node22 分叉修复（codex 单轮交付）**：CI（node 22）core-client.test.ts 6 测 `cancelledByParent` 连锁红、本机 node 24 恒绿——主控用 node v22.17.0 官方二进制本机确定性复现锁定版本分叉；根因=Node 22 `AbortSignal.timeout()` 计时器不保活事件循环，deadline 测试 stub 仅等 abort → 事件循环放空 → node:test 提前取消父测并连锁取消后 5 项；修=测试内保活 setTimeout＋finally 清理（+23/-17 仅测试文件），层级判定=test-harness 缺口（产品宿主 workerd 不受影响），产品代理逻辑零改动 | worker：node22/node24 双跑 13/13＋mkfast `pnpm test` 全量过；**主控亲验**：lane 内 node22 13/13（cancelled 0）＋node24 13/13 | #147 清算件之一；root-quality 第二步 web 红面应由此清零，待下一次 CI run 验证 |
| ec0f1525 | #282 | **boot 供给幂等键补 prompt-authority 维度（codex 单轮交付）**：平台配方 immutable EvalRun boot key 由 `eval.platform.<name>@1` 改为追加冻结 prompt-authority 指纹（sha256(name/version/contentHash/source/isFallback/fallbackReason) 截 16），strict/Langfuse 与 pilot/builtin 各写各 key，消除同库跨 authority boot 的同 key 异 payload IDEMPOTENCY_CONFLICT；配套=已冻结 revision 跳过重复 acceptAndFreeze（保首个冻结版本）＋冲突门细分（静态配方不匹配或同 authority 内容漂移仍硬抛，仅「已冻结＋authority 切换」放行）；未改消费面 | worker：冷库 strict→pilot→pilot 重放 6/6＋skills 内存 148/0＋冷库 PG+DBOS 166/0/0 skip＋core check；**主控亲验（rebase f020ee80 后）**：platform-provisioning 内存 5/0（1 skip=PG 用例）＋冷库克隆 meiye_282_gate2 满跑 6/6/0 skip＋core tsc 0 | **#282 关票**；共库跨 authority 时各 authority 的 boot EvalRun 证据并存可寻址 |
| 0352950f | #147 清算 | **m04 跨任务内容泄漏根修合入（codex 单轮交付，头号真缺陷）**：泄漏链路=Core 与浏览器都不核验 SSE 帧内 workflowId——结果页虽按 Work 的 canonical workflow 订阅，外来 workflow 的高序号 token/state 帧仍可进入 authoritative Work Result 投影（stale ?taskId= 确定性复现，CI retry×3 全红）；修=双侧 fail-closed：Core 在 progress 迭代与 state 产出处丢弃非目标 workflow 帧（2 行守卫），web 在 cursor 推进前按 workflowId 判归属（advanceWorkflowEventCursorForWorkflow），4 文件 +113/-1，双侧新增回归测试；取舍=服务端异常混入帧改为静默丢弃而非降级连接（跨任务隔离 fail-closed） | worker：Core 4/4＋Web 7/7＋双侧 check/typecheck＋冷库全 spec passed；**主控亲验**：rebase 后 focused 4/4＋7/7＋core tsc 0＋冷库克隆 meiye_m04_gate 锁内全 spec **6/6 绿（4.8m，:527 泄漏断言 35.1s 过）**（e2e 证据取自 rebase cbf5a792 前一 SHA，间隔 commit 为纯 references/ 删除零产品面） | **#147 清算件②**；production-main-journey 余红仅 assembly-gate 5a |
| 0359b62c＋7865ae4d | #147 清算 | **root-quality 残红清算（主控直修）**：CI 单测面已全绿后剩两门红——①**opt-in test evidence 门**（#260 fail-closed）50 套件评据过期；批跑揭出 3 个套件在「provision 过但从未 boot 的库」上真红：postgres-store 缺 p1_content_packages/execution_spine.creation_submissions/p1_product_billing_quotes（boot 链专属表）、migration-cli 缺 product_states、admin-supply 并发 per-test migrate 竞态——修=三套件 setup 显式迁移依赖（0359b62c，+53/-1 纯测试）；②**workspace checks（biome）**4 个 spec 文件带未格式化代码入仓（含主控此前 toast 直修与 #277 reland 遗留）＋m04 测试 foreign-token 帧类型错（TS2345，worker typecheck 虚报绿）——修=format＋补合法 token payload（7865ae4d） | 51 套件全部黄金克隆库真跑绿（47@74074fcf 批一＋30@0359b62c 批二＋pending-actions 单跑；两批含重叠）；三修复套件空库冷绿 11/11、1/1、17/17；web pnpm typecheck 绿＋m04 测试 7/7＋biome 1110 文件 0 错；**check-gates Overall: PASS（本地）** | golden 模板已在 74074fcf 重建；本推送触发 CI run=root-quality 全绿验证点；production-main-journey 余 assembly-gate（grok 在修） |
| e940fc0e（3 commits ff：0af71e78＋63b85e25＋e940fc0e，rebase 后 SHA） | #281 | **publish:* 枚举退役＋确认卡语义归位合入（codex 裁定→grok 修复→主控收窄，三段接力）**：①退役 `publish:xiaohongshu|douyin|video_account` 三枚举（裁定=D-155/D-161④/D-112 双字段合同；票面所引 D-131 与原文不符已纠正，D-131 实为前端拆票组织）＋11 文件消费者迁移＋contracts 拒旧值断言；②执行确认卡从已退役 publish:* 触发改挂 D-164③ 付费生成信号（executionSnapshot.quote＋usageReservation 双门，usageReservation=扣费冻结权威），文案改生成语义（旧「外部发布」问句撤除），wire kind 沿用 external_action（渲染门合同，非 D-013 第 7 类）；③**主控裁决收窄**：确认卡只挂 media 工作流路径——grok 初版把 copy/note 也罩进去，广批 13 红实证与现行冷绿合同冲突（composer-card-family T31:30 明文「copy 主路径无 pre-run 额度确认（D-043）」），copy/note 扩展=有意遗留，随 UI 大调整综合评判集中开票（用户拍板 2026-08-01） | worker：core 2820/0＋contracts 140/0＋video-native/marketing 3/3；**主控亲验**：focused 51/51（含新「付费 copy 不 hold」边界测试）＋smoke/interaction 41/41＋tsc 0（rebase 前后各一遍）＋**广批七 spec 冷库锁内 18/18 绿（9.1m，meiye_281_final）**：video-native 确认卡步保留＋card-family/w01/m01/intent-routing/p0-golden/marketing copy 旅程全绿 | **#281 关票**；触及 harness/execution-spine/model-supply/creation-experience 四目录→合并评据批换锚随 assembly-gate 合入后一次做；copy/note 集中开票已由 D-171＋docs/specs/xhs-vertical-integration-spec-2026-08-01.md 收编（P0 起步票 #286/#287/#288） |
| f37b3de6（3 commits ff：87c45f38＋45be8e00＋f37b3de6，rebase 后 SHA） | #147 清算（5a 终章） | **assembly-gate session null 根修＋D-164③ video 旅程合同对账合入（grok 判型与修复→主控两笔对账，四段验证）**：①判型 A 成立——生产候选栈 `import.meta.env.DEV=false` 时旧 email 验证策略不 auto-verify，冷注册无 session（get-session email null 30s 超时）；修=`APP_ENV=e2e` 显式放行（真实部署永不设，注释记档；`mode=e2e` 单独在 production appEnv 下仍不放行）；②m04 lineage 观察器改「进入 result shell 后装」（works 列表页合法显示 wrong 提交文本，早装=假阳性流泄漏，权威断言未动）；③主控对账两笔：共享旅程 fixture 补 video 模态「确认执行」点卡步（media 收窄后 m04 video 挂起在卡上，image_text 走 note 路径无卡佐证收窄边界）＋D-098 C6 活动预算 video 3→4（D-164③ 生成点确认卡的合法一击，契约常量注记出处） | **主控亲验（旅程四跑史）**：R1=真缺陷揭示（video 卡挂起）；R2/R3=基建死（grok w12 侦查遗留 yes 压测孤儿进程压死 workerd，已清，22× CONNECTION_REFUSED 连锁非业务红）；**R4=9/9 全绿（5.3m）**＋video 连带面补充批 three-modal/t39/video-result-live **13/13 绿（7.9m）**＋policy 单测 4/4（rebase 后） | production-main-journey 全绿在望；评据换锚（本合入＋281 触及目录）随 grok 任务 G 结果统一执行后触发 #147 终验 run |
| ceb42d97（3 commits ff：c12aa4fb＋ff874bac＋ceb42d97） | #280 | **PWA 安装性合入（grok 交付→主控三笔对账）**：①grok 交付=network-first 透传 `sw.js`（503 兜底，无离线缓存，符合决策文档「不为产品数据造离线壳」）＋安装引导 UI（设置页卡片＋移动端可关闭轻提示，iOS Safari 加桌指引）＋注册器与单测＋决策文档；②**主控门控**（ff874bac）：`ServiceWorkerBoot` 注册与轻提示全部收进 `import.meta.env.PROD`（沿 analytics 组件族惯例）——dev/e2e 栈不挂 SW，mobile 冷绿 spec（iPhone 仿真 UA 会命中 iOS 分支）不受扰；③**主控撞车修**（ceb42d97）：删 grok 静态 `public/manifest.json`——main 早有 config 驱动动态 `/manifest.json` 路由（#128，locale 化名称＋PRODUCT_THEME_COLOR 单源），静态版生产会遮蔽且必然漂移 | **主控亲验改走 CI（PR #283 draft）**：本机三跑三败均为宿主维护积压假红（形态各异：水合整批 30s 超时→workerd Network connection lost→build 254；判据与处置见 `docs/ops/local-e2e-host-degradation-runbook-2026-08-01.md`）；CI 十查全绿：core 7m33s＋core-persistence 9m51s＋root-quality 23m48s＋**production-main-journey 8m29s**（SW 在 PROD 栈影响面实证）＋redline-evals＋dependency-audit＋required；lane 内 typecheck/biome/单测三绿（门控后） | #280 第 1 项（SW 安装性）＋第 3 项代码面（安装引导 UI）收口；余=iOS 实机验收＋安装 fallback 实测留用户真机窗口；`short_name` 打磨随真机轮 |
| 32cf183e（8 commits ff：51aa720d＋78c75dc1＋1463903f＋7e391bf3＋8a21efcc＋5d7cd1f6＋541273e7＋32cf183e） | — | **C2 reservation sweeper 收编＋#261 gate G3b 探针修＋hold/reservation 语义对账（另会话交付→主控接手对账修复）**：①51aa720d gate.sh G3b 改查 rootAxes 写/读/用三点（票已关，探针不再假红）；②78c75dc1 额度 reservation 租约清扫（7d lease，非问题超时）＋postgres claimBatch/refund/audit；③1463903f 复核补丁：resume 边界 reservationReleased、late_answer abandon 旧工作流、FE 额度已放回文案；④7e391bf3 移植类型洞＋docs/reviews/hold-reservation-semantic-reconciliation-2026-08-01.md 固定 A轴48h core_hold_expired vs B轴7d lease 分立。⑤**主控修复 5d7cd1f6**：cherry-pick 解冲突误丢件全量恢复——readDecisionTarget 崩溃窗口防护（billing 已退但 sweep 未 completed 时 reservationReleased 须为 true，对齐 reconciler 推导）＋markCompleted/markFailed 两笔运营审计（p1_operations_audit_events）＋attempts 上限→dead_letter＋指数退避 next_attempt_at＋applySchema 建表（首轮 CI 红即此因：测试按原版语义写、实现被解冲突丢掉）；⑥541273e7 两 postgres 套件单跑补 billing schema（镜像 sweeper 套件 setup）；⑦32cf183e 评据门 23 套件批换锚。 | **主控亲验**：PR #284 CI 七作业全绿 @32cf183e（首轮 @8a21efcc 红两处：sweeper postgres 测试＋评据门，修后复跑全绿）；本机逐套件冷 provision 库真跑 23 套件（21 首过绿＋interaction-service 5/5、dbos-registration 15/15 修后绿＋safe-provision skip=3 设计内）；focused 23/23＋core tsc 0；评据门本地 guard OK。 | 非关票合入；语义对账见 docs/reviews/hold-reservation-semantic-reconciliation-2026-08-01.md；「测试承诺→实现丢失」再证反向 review 纪律（解冲突即改写，交付面须重对账）。 |
| 38064a35（17 commits ff，rebase 后 SHA；含 ReUI 合同回灌 703f79df＋评据换锚） | #286 | P0-A 工作台四态：Active 折叠（相位表）＋双滚动修复＋交付 per-run 去重＋CandidateSummaryCapsule＋outputHint handoff；TEST-CATALOG 记验收旅程 | 主控亲验：interaction 291/291＋parity 0；PR #296 CI 七作业全绿@38064a35（root-quality 17m45s） | **#286 关票**；完整 e2e DOM 随 #313 |
| 38064a35（17 commits ff，rebase 后 SHA；含 ReUI 合同回灌 703f79df＋评据换锚） | #287 | P0-B 记忆诚实：MemoryValueView 结构化渲染＋sortMemoryEntries 待确认优先＋冷启动 disclaimer 成套 locale | 主控亲验：unit 1553/1550/0 fail；CI 全绿@38064a35 | **#287 关票**；三层页=#316 |
| 38064a35（17 commits ff，rebase 后 SHA；含 ReUI 合同回灌 703f79df＋评据换锚） | #288 | P0-C kind 分层映射（wire 二值不动＋carrier media/copy/note）＋triggersPaidMediaExecution fail-closed；copy 免确认 D-043；note 钉 P1；生产行为变化=0 | 主控亲验：80/80＋5/5＋双 tsc 0＋出口三件＋评据 28 锚＋CI 全绿@38064a35 | **#288 关票**；遗留 #314/#317/#319 |
| b58cec17（3 commits ff：757463a1＋e21af067＋b58cec17） | #313 | P1-01 工作台壳：双栏（≥1240）＋Composer 粘底 morph＋宽度 800/1240＋AgentFrame 六族注册表渐进映射；interaction 9/9＋shell 单元 14/14 | 主控亲验：lane 交验评论齐；focused unit/interaction 绿；ff 合入后 HEAD 链含 b58cec17 | **#313 关票**；P1 关键路径头 |
| 4a1a36b7（2 commits ff：cf922bb5＋4a1a36b7；rebase after #313） | #314 | P1-02 legacy kind 二分派收编：workbench-state-model＋works-projection 改 contentPackageCarrierOf；native video 门迁 media carrier；D-118 deliveredMedia 形状钉住 | 主控亲验：workbench+works 40/40＋projection 22/22；票下交验 1562 unit/297 interaction；ff 后 4a1a36b7 | **#314 关票** |
| 32d4e3d3（3 commits ff：37ca738f＋a1d53c0e＋32d4e3d3；rebase after #314） | #315 | P1-03 六 prompt 美业改写＋Langfuse：HARNESS_PROMPT_SITES 14→20（harness/xhs-*）；version pin＋builtin fallback；anti-drift；交底 issue-315-xhs-prompt-sites | 主控亲验：langfuse-prompts 13/13＋task-admission 23/23；票下交验齐；ff 后 32d4e3d3 | **#315 关票** |
| 37b0ef73（2 commits ff：4e0d9733＋37b0ef73；rebase after #315） | #316 | P1-04 记忆三层页：待你确认默认上／已记住域分组／证据抽屉；#287 诚实空态保持；interaction 17/17 | 主控亲验：memory-vault interaction 17/17；票下交验齐；ff 后 37b0ef73 | **#316 关票**；P1 第 1 波齐 |

| 76c60c75（2 commits ff：f78457a3＋76c60c75） | #317 | P1-05 流内 execution_confirm interrupt＋note 付费媒体过卡：workflow-core note 路径 confirmPaidGenerationExecution after plan.ready；composer DecisionFrame 单轨；rebind 丢陈旧 interrupt；pure-copy 免确认 D-043 | 主控亲验：workflow-core 60/60＋composer unit 33/33＋agent-frame interaction 4/4；lane 双轮 review 0 open；ff 后 HEAD 76c60c75 | **#317 关票**；P1 关键路径中段；e2e Playwright cold-db 未跑（栈 down，记） |
| 76e745e6（2 commits ff：f37560a6＋76e745e6；rebase after #317） | #318 | P1-06 Activity Shelf 完整版（≤3 对象卡 thumbnail/status/next action）＋Idle 建议行轻胶囊；i18n＋a11y＋video thumbs 复核修 | 主控亲验：unit 33/33（含 shelf/chips/dashboard contract）＋interaction 11/11；lane 双轮 review 0 open；rebase 后 ff HEAD 76e745e6 | **#318 关票**；P1 第 2 波齐（#317+#318） |
| 01169b44（1 commit ff） | #319 | P1-07 多页 note_plan 时间线（可编辑大纲/配图状态/逐页重生）＋Delivered C7 对象工作区门＋导出/发布准备占位；§10.3-1 note schema/image_text 分层迁移交底 | 主控亲验：unit 20/20＋interaction 9/9＋core unified-media-stage-ports 51/51；harness opt-in 17 套绿；check-gates PASS | **#319 关票**；P1 收官票；image-text-note-compiler 全 e2e 未本轮实跑（记欠账） |
| 4d04e7bf（10 commits ff） | — | P1 #313–#319 审核修复：failed/cancelled 终态防迟到帧复活；merchant interrupt 与 sticky Composer 分层、方向落地去 force 假绿；migration/recovery advisory lock＋Worker DBOS admin 关闭；W12 cleanup 15s 总期限；canonical note OCC 持久化与逐页 `prepare→quote→confirm→result_adjust`；Delivered shelf 恢复且 Active 只 hidden 不卸载 disclosure；P0-4 样例无 hint 不强选 lens | 主控亲验：Contracts/Core/Web focused 与 typecheck exit 0；Core 独立真 PG `3007 total/2986 pass/0 fail/21 explicit skip`＋safe-provision 3/3；UI/UX Overall PASS；Chromium 热租户 1/1、三文件冷库 15/15/0（3.0m）；双轴终审代码/测试 0 blocking | 原 #313–#319 CLOSED 与历史行保留，本行登记审核补修；fixture structured model 与本机真 PG/Chromium 不冒充 live/provider；Langfuse remote 20/20 已同步，但 Actions 无 secrets/variables，部署态不作已更新声明；P2 开闸仍待本次 main exact-tip journey success |
| 9b1c8ba6 | #320 | 违禁词链：原子 CRUD、全 carrier 检查、UTF-16 原始 offset、左最左最长替换、delivery 前 fail-closed recheck | 本批主控亲验：Contracts 165/165；Core fresh 双库 3096 total、3075 pass、0 fail、21 explicit skip；safe-provision 3/3；Web PG opt-in 3/3；Chromium 五文件 19/19；check-gates Overall PASS | fixture 与真 PG/Chromium 分层；final main exact-SHA CI 后才关票 |
| 9b1c8ba6 | #321 | 签名 generation params：customized 不携带隐藏 voice role；standard thinking；未显式角色时保留 MarketingIdentity | Web build/check/typecheck 绿；unit 1712 pass、0 fail、3 skip；PG opt-in 3 pass、0 fail、0 skip；interaction 390/390；Chromium 总门 19/19 | 非 XHS 路径与前台计价未扩面；final main exact-SHA CI 后才关票 |
| 9b1c8ba6 | #322 | 单一 NoteObjectWorkspace：Tiptap、六动作、空段落保真与 copy-only derived terminal | Core full 0 fail；Web full 0 fail；Chromium 深图文同链覆盖 canonical edit、Selection AI、敏感词与 delivery | Tiptap 未进入 Composer；final main exact-SHA CI 后才关票 |
| 9b1c8ba6 | #323 | AI 封面与风格分析：五 preset、三 ratio、9:16=`1152×2048`、授权 style refs、七维分析 | Core full 0 fail；Web full 0 fail；Chromium AI cover／style-role／Result image 通过；check-gates Overall PASS | style refs 不计入源图 cardinality；final main exact-SHA CI 后才关票 |
| 9b1c8ba6 | #324 | viral paste／OpenCLI structured source、exact recipe、两条 viral prompt；Langfuse 扩为 remote 22/22 | strict materialization 22/22、fallback 0、contentMatches 22；Core full 0 fail；Chromium viral journey 与 OpenCLI 三门通过 | 本机 pins 22；Actions secrets/variables 0/0，不声称部署凭证已更新；final main exact-SHA CI 后才关票 |
| 9b1c8ba6 | #325 | 当前任务 frozen experience basis、三处露出、delivery morph 与 stale/foreign 拒绝 | Web unit／interaction 0 fail；Chromium viral→experience morph→note Result 通过；诚实文案合同复跑绿 | 不制造持续学习承诺；final main exact-SHA CI 后才关票 |
| 9b1c8ba6 | #326 | 同一对象工作区的手机笔记预览＋双列封面预览 | Web full 0 fail；Chromium P2 closure 3/3；单 worker fresh 双库总门 19/19 | 无第二对象真相；final main exact-SHA CI 后才关票 |
| 9b1c8ba6 | #327 | bounded inline replacement、冻结选区、明确 variant/history/delivery；历史及 mixed-version destination exact 投影 | ContentPackage 56/56；creation submission 真 PG 10/10；Core full 0 fail；Agent Team 终审 P0/P1/P2=0 | 新目的地不写 strict compact；只读 immutable submission 响应投影；final main exact-SHA CI 后才关票 |
| 9b1c8ba6 | #328 | stale read cancellation、bridge fail-closed 与 paste fallback；live 一次 note read＋一次 download、外部写入 0 | Chromium OpenCLI 三门 3/3；live 核销时间 2026-08-01T20:53:37Z；secret scan 3264 files、0 finding | live 不等于 fixture、provider 或 companion 部署；不登记账号、URL、token 或原文；final main exact-SHA CI 后才关票 |
| ff6932bb | #298 | CB-L1 账本与合同积分化（70 commits ff）：p1_credit_* 账本 FEFO/原子预扣/发分调度器/两阶段商家执行合同（admission root＋per-effect claim＋fence 后 primary promotion＋辅助 auto 选路 claim 前服务端冻结）/报价八操作/plan.credits 单源 fail-closed/兑换码入积分账/coverage-gap resume 修复 | 主控亲验：Memory 聚焦 52+123 绿（3 红例先红后绿）；fresh PG 隔离双库 22/22＋13/13；根 typecheck 0；合并前唯一一次完整 journey CI 同构 env 9/9（3.8m）；合入态 Core 全量含 PG-backed 3187/3166/0 fail/21 explicit skip＋persistence 断言 0＋Web payment PG 2/2；check-gates Overall PASS | 接管轮：R7 后未交底 rebase＋4 commit 已按 §11.6 补记并双轴复审（2 P1 修净、1 项降级文档修正），票下评论 5157711147/5157833583/5157993511/5158068427；push 待用户发话；下游 #303-#306 以本行 sha 解锁 |
| 4a55c3f3（L4 tip；整批 ff 入 main 见下行） | fix/dev-env-ci（L4） | **环境与验收门修复**：①dev runtime-profile 改为仅对未显式设置的键填默认值，`.env` 的 `MODEL_EXECUTION_MODE=direct` / `APP_ENV=development` 等不再被硬覆盖为 fixture/e2e；②dev:smoke 心跳探针改读 `output/dev/stack-state.json`（start-stack 落库的真运行 DATABASE_URL），无栈时报 `no running stack found` 而非拿 `.env` 死值假红；③P2 五文件 Chromium（image-text-note-compiler / viral-adapt-opencli-gate / p2-browser-closure / admin-sensitive-words / composer-card-family）进 CI `p2-browser-acceptance` 并入 required；④journey 门补 XHS 图文主链 `xhs-image-text-main-journey` | 脚本自验＋journey 本地 fresh 双库全跑；yml 结构评审；整批合入 tip=`310c71e6` | **诚实补记**：(a) #322 台账 :136 曾把「Chromium 深图文同链覆盖」表述为完成态，但 journey/CI 仍欠最终 Result 旅程——本轮以 L4-3 五文件 CI 补验而非改写历史行；(b) L4-3 将 P2 五文件纳入 push+PR required job；(c) 全量 e2e 在 push 上因 `release-manifest` 仅 `workflow_dispatch` / `release-candidate` label 触发而结构性 skip——全量浏览器面的去向仍挂 §5 供给清单，待用户拍板是否另开全量 e2e job 或继续依赖本机/候选门 |
| 310c71e6（26 commits ff：L4→L2→L1→L3→L3b→L5；base plan `6a93369d`） | #313–#328 差距修复批 | **Gap remediation 整批合入**（方案 `docs/ops/gap-remediation-plan-xhs-313-328-2026-08-02.md` R-1~R-8）：L4 环境/CI 门；L2 生成参数与风格分析（thinking provider 键、standard→quality、poster 风格分析、AI 封面中文 preset、aiCover 失配提示）；L1 真单页重生＋页级帧＋running 时间线＋确认卡大纲；L3/L3b 工作台形态（R-1 顺序、底栏胶囊、时间线六族、玫瑰金、Inspector、e2e 适配）；L5 P2 积分化四红（free+deep quote 合同、note credit settle committed、quota-passive 时代对齐、released-hold mock/wire）＋attach 胶囊关面板防 portal 拦截 | 集成链 @ `3cef0ee9`：Web interaction 410/410；journey 5spec 9 passed；L1 单页重生 PG 1/1。L5 终验：product-billing 49/49＋credit-billing 49/49（含 PG）；P2 五文件 Chromium **18/18（7.3m）**（`/tmp/l5-main-full18b.log`）；viral 点击根因=attach portal 拦截，修于 `seedComposerInlineAuthorize` Escape 关面板。A-4：runtime-profile unit 10/10＋`.env`→direct/development 实测。A-5 截图基线记「待 CI」不阻塞 | **本批主控终态**；push 待用户发话；§5 供给（CF secrets / OpenCLI companion / release-manifest 策略）不在本批；handoff `docs/ops/master-handoff-gap-remediation-2026-08-03.md` |
| 0cc1f8c5（1 commit ff） | #303 | **CB-01 运营定价后台**：CatalogModel×操作积分价与失败退还开关；视频 15/30/60 秒独立价；四档套餐、三周期系数及三档加油包经 `plan.credits.*` 治理修订热读；Core 公开套餐合同实时计算周期下单金额；加油包三要素与原则性单价提示；无上游 token/成本字段 | 主控按 credit spec §11.5 亲验：scope/diff 复核通过；Core focused 27/27＋Web focused 14/14；Core/Web typecheck exit 0；真实本机 PG 1/1；固定候选 Chromium 1/1；双向反驳复核确认配置进入生产报价、公开套餐及下游结账合同，模型目录发布缺价 fail-closed | **#303 关票**；完整 journey 未重复运行，保留至固定 current-main 候选合并前一次；下游 #307 以本行 SHA 解锁；未 push |
| 1d3dda96 | journey 基线修复（主控直修） | **fixture Langfuse authority 隔离**：Core 与 P1 Worker 的生产候选 fixture 启动命令显式清空 `LANGFUSE_BASE_URL`／public key／secret key／prompt pins，防止父 shell 的半套远端配置穿透并把 builtin fallback 误判为 unpinned；严格验收逻辑未放宽 | 聚焦合同 3/3；随后 #307 exact-HEAD 完整 journey 中 Skill acceptance 正常以 unconfigured builtin fallback 启动并通过 | 非产品功能票；作为后续候选 journey 的 current-main 基线；未 push |
| 84948754（1 commit ff；base 含 1d3dda96） | #307 | **CB-05 价格页参考数字后台半边**：copy/image/video 分类参考模型；建议值按套餐月积分÷已发布模型积分价计算，视频固定 15 秒档；已发布值、偏离状态、全部采用建议值与 revision/CAS 确认发布；服务端拒缺模型/缺价；公共套餐合同仅投影确认后的参考数字，不泄漏 referenceModels/provider/cost | 主控按 credit spec §11.5 亲验：逐行 scope/diff 与双向生产可达复核通过；Core focused 29/29、Web interaction 3/3、根 typecheck exit 0、diff-check exit 0；fresh PG 双库＋固定候选完整 Chromium journey **10/10（4.5m）** @ exact HEAD `849487541c0e5cdec1fa761c012dede8f8a13e59` | **#307 待主控评论关票**；完整 journey 已在本票合并前唯一有效候选上完成；下游 #310 仍等 #304＋#308，当前不唤醒；未 push |
| ae55649c（4 commits ff；base=5242ba72） | #306 | **CB-04 积分明细页与账单卡**：商家安全批次/流水合同与生产读模型；FEFO 预扣、结算、已入账退回、批次过期未入账退回及过期流水可见；账单卡展示冻结套餐、周期档、本期积分，并区分续费与升级入口；中英文积分口径收口 | 主控按 credit spec §11.5 亲验：scope/diff 与双向生产可达复核通过；Core focused 39/39、Web focused 4/4、根 typecheck exit 0、diff-check exit 0；锁内票面 Chromium 2/2；fresh PG 双库＋固定候选完整 Chromium journey **10/10（4.6m）** @ exact HEAD `ae55649c7488bd474fb6a92e34f9e847c9b0bb91` | **#306 待主控评论关票**；完整 journey 已在本票合并前唯一有效候选上完成；#305 需先 rebase 本行 SHA，#311 仍等 #305＋#310；未 push |
| 9c072635（31 commits ff：#304 全链，code tip=b04fec54） | #304 | **CB-02 Waffo 订阅主链**：provider/waffo RSA 验签＋durable inbox/outbox 结算＋订阅 checkout；Test authority 双层门（api 边界＋SDK 调用前，production 下 SDK call=0）；recognized 事件 contract error 不吞单（空串身份/缺时戳/坏账期→`WAFFO_EVENT_CONTRACT_INVALID` durable retry）；账期缺失经 provider 订单回查（`WAFFO_PERIOD_RECOVERY_UNAVAILABLE` fail-closed，处置 2026-08-03 沙盒 payload 漂移）；checkout 编排（升级 replaces＋durable 恰一次取消旧单/降级换周期停车 fail-closed/重复拒绝/并发双开由部分唯一索引关死/弃单 1h 清扫解锁）；past_due 全链接 Core #298 grace 机器；单调事件栅栏（必填 timestamp＋lifecycle rank，SQL/TS 一致）；getCurrentPlan workspace 隔离；cancel receipts claim/lease/指数退避 | 主控代行（用户授权）§11.5 亲验：rebase no-op（main=6018b993 已为祖先）；红证=新测试在 db14f1ef 上 9 fail→本树全绿；Web 单测 1789/1797（3 红=main 基线）＋contracts 167/167＋Core 0 fail＋interaction 431/431＋typecheck/build exit 0＋check-gates Overall PASS；fresh 双库 @b04fec54（含 0020 整链）Web PG 4/4＋Core PG 10/10；opt-in evidence 8 套件刷新；双轴反驳复审 9 BLOCK 全裁决（11 项修净、6 项带理由否决）；wrangler release 配置按用户明示授权保留 | **#304 关票**；外部测试卡 journey 证据在 db14f1ef/部署构建 c1e18538（评论 5166748001/5166753542），本轮按指令未重跑；下游 #308/#310 以本行 SHA 解锁；封存分支 wip/304-post-freeze-hardening 已消费可删；未 push |
| 6cbe74dc（12 commits ff；code tip=5bd0b5bc） | #308 | **CB-06 加油包结算与续费/退款落账**：bounded stale checkout release＋晚到恰一次结算；Core governed catalog 冻结 SKU snapshot（amount/currency/credits/expireDays）；provider 调用前只读核验 Waffo Test 商品失配 fail-closed；webhook/Core `payment_add_on_grant` 只用 binding snapshot 不热读；退款身份方案 1（owner opaque 无 FK、disposition_actor 可空 ON DELETE SET NULL）；admin refund review 可达；CAS/duplicate/abandoned/late/未授权签名/删号负向 | 主控按 credit spec §11.5 亲验：scope/diff＋双轴无 P0/P1；fresh PG Core **10/10**＋Web payment **11/11** 0 skip；Core full 3225/3204/0f/21s；Web non-int 1824/1824；interaction 465/465；contracts 168/168；typecheck/build/Biome/root check Overall PASS；opt-in guard OK；secret 3366/0；完整 production-main-journey **10/10（4.6m）** @ exact HEAD `6cbe74dc3f569201ad0cdca5b37e3f1a0d640c8a`（DB `meiye_308_jny4_*`） | **#308 关票**；下游 #309 以本行 SHA 解锁；#310 仍等 #305；未 push；旧候选 2d85df8d/9d2b3572/5c5154b6/ed6d7261/8da0996b 作废 |
| d4365d09（8 commits ff；含 evidence；base=624d255c） | #305 | **CB-03 工作台三露脸位**：余额徽章（总余额＋最近到期批次）；报价 chip（积分消耗＋失败退还双态）；不足拦截（还差 N 分＋/pricing#credit-boosters 与 #subscription-plans 双出口）；fresh admission fail-closed；merchant DTO 仅 remainingCredits/expiresAt；无 token/成本泄漏 | 主控 §11.5：rebase 到 main@624d255c（冲突解=保留 #308 HKD catalog 与 #305 soonestExpiringLot 双测）；contracts 4/4；Core focus 54/54；Web focus 14/14；interaction 1/1；typecheck/Biome/root check Overall PASS；opt-in 6 suite fresh PG 12/12 换锚；完整 journey **10/10（4.8m）** @ exact `d4365d0936b7500fc7b2ec7343c24f20a303ad98` | **#305 关票**；下游 #311 仍等 #310；#310 现仅等本行＋#308（已合）；未 push |
| da09c4d8（4 commits ff；code tip=bd1ebe37；base=4c356848） | #309 | **CB-07 Creem 退役**：删除 provider/creem 与 webhook 路由；registry/policy/env/vite/deploy/types 去 Creem；卸 creem 依赖；routeTree 再生；退役审计测试 fail-closed；Waffo/Stripe 回归保留 | 主控 §11.5：scope/diff 仅支付退役面；creem-retirement-audit 绿；payment focused 54/0；interaction 2/2；typecheck/Biome/root PASS（opt-in 5 suite fresh PG 12/12）；完整 journey **10/10（4.8m）** @ exact `da09c4d84f4f49c9e590becfd25e7e1bf13a9378` | **#309 关票**；#300 子票齐（#304/#308/#309）；#310 可派（前置 #307/#304/#308/#305 均合）；未 push |
| 5d73e8b9（2 commits ff；code tip=846abd2e；base=adc5bd4b） | #310 | **CB-08 价格页方向 A 积分卡阵**：公开目录放出 trial 档；周期切换条（单月/连续包月/包年）即时重算折后价+划线原价；四档积分卡+参考数字「仅供参考」；`#subscription-plans` / `#credit-boosters` 锚点；订阅与加油包 Waffo Test checkout 入口；无条数额度旧口径与上游成本字段 | 主控 §11.5：scope 13 文件均落价格页/公开目录；contracts 170/170；Core plan-catalog-http 2/2；model+contract 18/18；interaction 2/2；root typecheck 0；root `pnpm check` Overall PASS；opt-in 4 suite fresh PG **10/10** 换锚；完整 journey **10/10（4.6m）** @ exact `5d73e8b9c06fe884aa3bc0f32f517e4457a47a5b`（CI 同构 env；DB `meiye_310_jny2_20260804_193848`） | **#310 关票**；下游 #311 以本行 SHA 解锁（仍等 #305 已合）；未 push；未做 Waffo publish / Production traffic |
| 55a71413（3 commits ff；code tip=5a6a9bf2；base=b556fee2） | #311 | **CB-09 条数体系退役清扫**：注销 `plan.allowances.*` 注册与 admin 可写面；公开/目录热读停读多桶键（仅 cutover 静态种子+trialEnabled+payment mapping）；P0 ProductService 生产装配 `legacyBillingReadOnly:true` 审计钉死；allowance-retirement-audit 覆盖 progressive-rights 保留、生产无 allowances 读取；s3 e2e 改积分；商家计费真相仅 `plan.credits.*` | 主控 §11.5：scope 17 files 均落 allowances 退役；audit 4/4；entitlement/foundation focused 绿；web admin 33/33；opt-in 6 suite fresh PG **12/12**；root check Overall PASS；完整 journey **10/10（4.7m）** @ exact `55a714132a46083b3b5e9613807906c539f475c2`（CI 同构 env；DB `meiye_311_jny_20260804_200017`） | **#311 关票**；下游 #312 以本行 SHA 解锁；未 push；未做 Waffo publish / Production traffic |
| 778b003d（验证轮，无新提交；#311 tip 已为祖先） | #312 | **CB-10 积分制全门回归验收**：§9 十一门在 main tip 复跑全绿；Creem 退役审计复跑；Waffo Test 结算/加油包/退款幂等路径复验；production-main-journey 10/10；验收报告落 #302 | 主控亲验 @ `778b003d`：Core credit focused 65/65；product-billing 32/32；entitlement 25/25；Core PG 8/8；Web unit 21/21；refund/settlement 9/9；Waffo package/catalog 16/16；Web payment PG 7/7；interaction 14/14；Creem audit 1/1；root check Overall PASS；journey **10/10（4.8m）**（DB `meiye_312_jny_20260804_200821`，CI 同构 env） | **#312 关票**；#302 L5 收口可关；父票 #299/#300/#301 子票齐可关；未 push；未做 Waffo Production publish/traffic |
| d2e83053（2 commits ff；base=bfcfaae1） | — | **审计整改 A1 operations 死表面**：删除 OperationsApplicationService 39 个方法定义（30 零调用 public＋8 migration delegate＋1 private helper，+258/−8284）；migration dispatch 依赖移交 foundation-module 直持（503 guard 保留，8 case 重接）；main.ts 仅获批两行装配移交 | 主控亲验：逐方法生产 grep=0 证据；审计 35→实删 30（5 项反驳有据）；retired surface 3/3；foundation 聚焦 8/8；rebase 后 root typecheck exit 0；Core 全量 0 fail（215 skip=PG 无 env 按纪律跳过，PG 电池待 A3 合入后统跑） | 依据 docs/reviews/codebase-design-module-audit-2026-08-04.md P0-1；未 push |
| f7691150（2 commits ff；base=cb1cf6f4） | — | **审计整改 A2 model-supply 签合同与死壳清理**：ProviderExecutionPort 双定义收敛为 foundation/ports.ts 单一权威（18 import site 更新，无兼容 re-export）；ModelSupplyControlPlaneRepository 39→37 成员并为 PostgresModelSupplyRepository 补 implements（编译零漂移，SQL 未动）；新增 Memory/Postgres 共享契约测试（PG arm 按 TEST_DATABASE_URL 门控）；recorded-media-adapters 12 个零行为命名壳换合同表驱动工厂（导出 25→14，model-id 目录钉死）＋新增全生命周期/四错误相测试 | 主控亲验：grep 单一 port 定义＝1；命名壳 grep＝0；rebase 后 root typecheck exit 0；Core 全量 0 fail（215 skip）；契约测试 Memory 3/3；audit 三处预期被反驳有据（旧 foundation 签名系无消费者遗留） | 依据 audit P0-2/3/5；PG arm 待电池统跑；未 push |
| 3b64bbf2（3 commits ff；base=b9cf7ae8） | — | **审计整改 A3 server 边界声明化**：toHttpError/withErrorEnvelope 统一错误翻译（catch 38→12，仅剩解码/流/资产 I/O 差异化）；32 条路由声明表（4 public＋28 service-token，含 auth 类守卫测试防新路由静默公开）；SSE 双实现合一保留协议/断线/错误帧行为；/public/plan-catalog 计费投影落 credit-plan-catalog publicView；资产归属＋25MiB 策略落 asset-http-policy 端口；server.ts 2687→2428 行 | 主控亲验：行为锁=server-boundary/server-shutdown/各 http.test 原样 35/35 通过；rebase 后 root typecheck exit 0；Core 全量 0 fail（215 skip）；main.ts 零改动（现有装配已承接）；audit「约 35 条路由」被反驳（实际 32） | 依据 audit 项 8＋entry ③；未 push |
| b228b948（7 commits ff；base=b6db6b6a） | — | **审计整改 A4 web 钩子抽取与静态测试退役**：useComposerRun（createWork/runCreate/attemptSubmit 门梯＋credit gate，注入式 transport 默认走真实现）；useComposerInteractions（SSE＋HITL 收件箱，可注入事件源）；useResultCenterView＋useResultCommands（$workId 视图投影钩子化，14 处裸 commandP1 收编 mutation，ad-hoc busy 布尔收敛）；validateSearch 去镜像改 import 真解析器；integration-settings 增 settings? 注入 prop 转正 seam；正则测源码型静态测试成对退役换行为测试 | 主控亲验：web 单测除 3 个 main 基线红（pricing×2/Creem 退役审计，与本 lane 零交集、main 复现实证）外零新增失败；interaction 全绿；codex 断线遗留 4 处测试 fake 类型错误由主控直修（b228b948，fake 补齐合同真形状）后 tsc exit 0；聚焦复跑 1/1 | 依据 audit 项 4＋11；3 基线红另行开票；未 push |
| 8448daac（3 commits ff；base=99e3b046） | — | **审计整改 B2 contracts 硬化**：identifiers.ts 统一身份层（全包 id schema 拷贝清零，ApprovalReceiptId 品牌化，两族按 30 涟漪熔断规则降级命名 schema）；failureCode 改独立 schema 修范畴错误；ApiFailure 39 直传错误码枚举＋文档化 escape hatch；export * 归零改显式清单（901→881 符号，删 12 死符号＋1 条意外转发边）；新增 ./wire ./vocabulary 子入口；editing-context/eval-run/skill-schema-registry 迁回 core、capability-inventory 迁 web | 主控亲验：id 拷贝 grep=0（共享定义仅 1）；export * grep=0；failureCode 修复原文；三入口 node 解析通过；contracts 161/161 亲跑；lane 自证 root typecheck 0＋web 除 3 基线红零新增＋interaction 478 绿；审计两处被反证（错误码 17→39 直传；4 文件仍被 schema 链消费未搬） | 依据 audit 项 10 硬化半；harness 保护区 178 符号候选留 wave 3；未 push |
| 9d46fbe2（2 commits ff；base=b6e37c8a） | — | **审计整改 B1 harness 拆库与单 runner**：PostgresHarnessStore 拆三分片（Interaction/Audit/Observability）＋facade，12 角色接口全部显式 implements、结构化消费清零，生产 wiring 按 slice 注入；三 runner 收敛为 descriptor 表驱动单 runHarnessWorkflow（note/media runner 删除），{shared,copy,media,note} collaborators 落地、Unified 穿透委托 10→0、Parameters<> 别名清零；两构造器位置参数改具名必填组＋可选能力组；PG 测试 raw SQL 断言 61→53（余项 DDL/事务/租约/账本正当） | 主控亲验：五行为锁 263 测试 0 fail；rebase 零冲突；root typecheck exit 0；Core 全量 3065/0 fail；throw 口径 54→50；审计三处反驳有据；A4 遗留消费者证明红由主控直修 b6e37c8a 定性为测试靶偏移非功能回归 | 依据 audit P0-6＋P1-9；PG 电池随本行复跑；未 push |
| ad14ea15（5 commits ff；base=8fe36ee2） | — | **审计整改 C2 contracts 行为迁移与双边校验**：计费数学迁 core quote-math；中文标签迁 web content-package-presentation；状态行为契约迁 core operations；projectHarnessExperienceBasis 迁 harness；web 共享 transport 统一 apiEnvelope safeParse＋三大读路径 payload 校验（无 schema divergence，safeExtend 保 invariant）；死导出重算 81 候选删 20 留 61（旧审计 178 已过期）；孤立 chipsSignalInputSchema 连测试清除 | 主控亲验：计费数学/中文标签 contracts grep=0；client.ts envelope safeParse 原文；鉴权树**审计反证成立**——web 四位点真重跑策略（workspace-authorization 等），属真双边 seam 依停止条件留 contracts；contracts 165/165、Core 3066/0 fail、transport 32/32、interaction 478/478、root typecheck 0 | 依据 audit 项 10 迁移半；未 push |
| c7a370f7（3 commits ff；base=70db288a） | — | **审计整改 C1 CoreAssembly 收官**：assembleCore(env,{role}) 统一装配（core-assembly＋api/worker-runtime），main.ts 2569→3 行、job-worker.ts 780→3 行，顶层常量重复 73→0；四漂移裁决修复（Pool 上限双 role 统一、worker ProductService 补齐 copyProviders/qualitySink/写所有权、referenceAssets 统一 owned 优先、searchProjection 单配方）；域规则抽 domain-rules.ts（模型准入/D-116 键一致性/营销身份/写所有权/存储探针）补 5 测试；四假端口删除（foundation 同名端口属独立账本边界依约保留）；ProductService 具名配置构造 | 主控亲验：main/job-worker wc=3/3 原文；假端口 grep=0；businessPoolMax 入共享装配 :250；24 个 createCoreServer 测试文件零修改；root typecheck exit 0；Core 3071/0 fail；审计行为结论零反驳（仅计数随五轮合并漂移） | 依据 audit 项 7＋entry ③尾；PG 电池终轮随本行 |
| d14f58ba（1 commit；base=7fe159cd） | #330 | **xcheck A7 行业 whyNow 键对齐**：whyNow 规则键改按已发布行业 slug＋读时单别名表，收敛词表分叉（xcheck N-1 订正后的真相：分叉而非全 miss）；含存量 revision 迁移 | 主控亲验合入（lane-330 grok lane，driver 收口）；关票门中 e2e 消费者证明（dashboard-home-mount）因基线 e2e 债 park 为 136 行 patch 留档，等 #337 后补跑 | **#330 未关**（AC3 等 #337）；未 push |
| ab56caf4＋d608b8c0（2 commits cherry-pick） | #333 | **xcheck A6 note 页重生 browser 旅程**：新增 note-page-regeneration-journey.spec（678 行）＋TEST-CATALOG；AC③ 诚实失败真跑绿（31.3s 零注入）；AC①② 完整旅程 park 在 test.fixme——浏览器实证**票面前提被证伪：单页重生 UI 不可达**（canonical package ref 恒空，水合失败被空 catch 吞掉，四次独立运行 `result_adjust_prepare` 零上线） | 主控亲验：零生产 diff（`git diff --name-only` core/contracts/src 空）；lane 复核中拦下两处假绿（新版本断言死分支、负例注入无隔离力）均已修正；头号发现开 #338 修复票 | **#333 未关**（后改挂 #341）；未 push |
| 11be05fc→ac0af02b（4 commits cherry-pick；裁决终稿 2ded8a06 收编） | #334 | **xcheck B4 批量解析纵切**：Core 暴露 start_parse_asset_batch 命令＋进度查询＋草稿枚举（新命名 asset_parse_task/asset_parse_task_drafts）；FE 批量上传进度＋逐草稿确认；fixture 档解析显式标注＋fixture 外 fail-closed；e2e 商家旅程收口 | 主控亲验：退役台账维持 81 条真实（新旧命名裁决经三消息同批交叉后以 ref 冻结定稿，旧名审计留档 lane-334-oldnames-audit=e9cbd107）；lane 无过失，教训「裁决带序号+靠 ref 冻结不靠送达时序」已入 runbook | **#334 已关**；未 push |
| 5f56921f（1 commit ff；base=ac0af02b） | #331 | **xcheck B2 note enhancement judge 五档条件装配**：新增 NOTE_ENHANCEMENT_JUDGE_BY_MODE 选择表（disabled/recorded/fixture→unconfigured，gateway/direct→configured；Record 无 default 编译期穷尽）；api-runtime 装配点 1 行改选择表调用 | 主控亲验：typecheck 0＋71/71（assembly/domain-rules/compiler/stage-ports 合跑）；lane 三次变异反证（表翻错→行为测试红；硬编码回退→静态接线断言红）；e2e 级 fixture no-op 由合入后 main 上 T20 真跑 40.5s 绿补齐 | **#331 已关**；未 push |
| f4e3c138（1 commit cherry-pick 自 ea5a5383，blob 4/4 零漂移） | #332 | **xcheck A2 note 风格确认卡完整 positioning 对比**：note_style 选项改对比卡网格（每卡 button＋label＋全文不截断 positioning，sm 并排/窄屏堆叠）；行为不变（aria-pressed／按 label 点即提交）；e2e 内容级断言＝逐条 toHaveText(writingGuide) 严格相等＋互异；contracts/core/退役卡零触碰 | 主控亲验：diff 范围 4 文件；interaction 组件 16/16 复跑＋lane 全量 486/81 绿；typecheck 0；合入后 main 上 T20 e2e 40.5s 绿；双层防伪（只回退产品文件→interaction 恰 2 红／e2e 红咬 ask-merchant-option-comparison toBeVisible）；产品文件还原 blob sha256 与 commit 一致 | **#332 已关**；未 push |
| 2adacb77→6702f88a（3 commits cherry-pick 自 0ff5df6c/52c812b8/2689b7e1；base=ac0af02b） | #337 | **base 测试债修复**：8 条 web 单测旧断言对齐已发布行为（含 pricing 台架自坏修复——真凶系 846abd2e/#310 删键未同步测试；Creem 审计 fail-closed 收紧防 grep 挂掉静默绿）；12 个 e2e spec 全部迁胶囊 fixture（selectComposerLens/openComposerCapsule），逐文件真跑**零选择器红**，红全在胶囊之后且逐条归属 | 主控亲验：合入后全量 web 单测 1843/0 fail/12 skip（基线 8 fail 清零，skip 未涨）；typecheck exit 0；m01 spot e2e 1/1 绿（48.6s 自起栈）；与 #332 note_style 选择器叠加审计无冲突（三处均容器级）；lane 零生产源码 diff；pricing 断言反转经主控追认（D-123 该条被 credit-billing spec／#310 已验收形态 supersede） | **#337 已关**；残红映射：Result Center 6 spec→#338、激活预算→#339（后已裁）、dashboard-home 段序→D-164① vs xhs §2.4 既有待决、其余→#340；**push 解锁**（8 条基线红清零），统一 push 待 #338 |
| fcd310ed（1 commit，主控直落；base=6702f88a） | #339 | **D-173 Day-0 首 token 预算 2→3 转正**：胶囊化 Composer 后纯自由文本路径＝开胶囊→选对口→开始创作三击；决策落 design 文档 supersede D-043/D-074/D-081C6/D-098C6 数字口径（已 push 合同表 3cef0ee9 的静默追认补齐记录）；fixture 注释＋intent-routing 期望 2→3；顺带对齐被预算红遮蔽的 route-notice 整框断言（舞台标签「叙述」→ toContainText） | 主控直落＋亲验：用户 2026-08-05 拍板二选一「改预算落决策」；intent-routing-http-sse 真跑 **3/3 全绿**（1.4m 专属栈） | **#339 已关**；同类漏网 uiux-day0-contract（5 处直点未迁+键盘合同钉 2）已登记 #340；未 push |
| 4b46d037→813bed4a（3 commits cherry-pick 自 de5cae2e/3944e1d8/83be4cff；base=6702f88a） | #338 | **note 页重生 UI 不可达修复（三层洋葱）**：①transport strict 校验拒收 Core wire 的 statusGroup 键（今晨 01:36 审计 C2 引入的**数小时新回归**，打瘫 content_packages 全部 5 个消费面含 Result Center）→ z.preprocess 摘派生键＋strict 保留＋3 条反削弱回归测试；②水合失败三路诚实上报（原空 catch 删除）＋时间线帧诚实文案（testid/reason/alert）；③旅程去 fixme 拆两测：主体旅程绿=回归门、派生交付段 park 指向 #341（继承自生成配图被实时权利断言误判拒写，真后端缺陷另票） | 主控亲验：commit A/B diff 逐文件审读；合入后全量单测 1846/0 fail、typecheck 0、双 spec e2e **10 过 1 跳**（composer-reshell 8/8 含 Result Center 步＝337 挂账红首个核销）；lane 防伪两轮（stash 修复→正例红回原始形态，逐字节还原）＋自抓「零误报断言位置假绿」并修正；AC④ 后端零改动（core/contracts diff 空）；lane 曾 amend 已交验 commit（消息对穿所致非抗令，收到指令即恢复三笔形态并在终 SHA 复跑，树同一；detach 脱管与一轮漏负载门记档） | **#338 已关**；#341 解锁（后端修复票）；#340 解锁（残红分诊）；#330 patch 可套用；未 push |
| 1979ab50（1 commit cherry-pick 自 53ec17fa；base=2cdc1e59） | #341 | **单页重生交付权利误判修复**：assertLiveDeliveredAssetRights 生成豁免扩为沿 lineage 上溯收集各代已记录生成回执（workspace 限定/环路保护/深度 16/两段懒加载，非派生路径零扰动）；商家源素材实时检查未动；PG 测试接线真实 rights resolver 杀假绿源（顺带修正 workflowId 不忠实与取材侧未接线两处）；journey 交付段去 fixme | 主控亲验：core typecheck 0；PG 重生集成 2/2（混合正例＋撤权负例）；完整旅程 e2e 3/3（2.3m，含 revision>=1＋receipt 见证）；lane 证据链＝先红（真实 PG 写入口）→绿→双层变异（单测＋e2e 回退必红，DBOS error 复现原始签名）→sha256 还原；grok 两轮未自验由 driver 全量亲跑兜底 | **#341 已关**；**#333 随之关**（AC① 交付段见证落位）；spec 入必跑集与否留 wave 收口；未 push | 
| bebb90ad（4 commits cherry-pick 自 0a7cb09b/b00ba7ad/5dd357fe/52273a61；base=600a17dc） | #340 | **xcheck 残余基线红分诊（非 lens 轴）**：day0-contract 七用例迁胶囊 fixture＋激活预算按 D-173 重立（模板3/纯文本3/视频4/键盘3 含事件序）；冷态 transcript 断言反钉「Idle 首页不挂 transcript」（79f9a4e7 移 identity 入 @ 胶囊触发既有空守卫，判旧断言债）；harness 16 个 route mock 补 meta.correlationId（5490078f readP1Envelope 整封校验后裸 { data } mock 全体静默失效——与 #338 同根更外层，已广播各 lane 自查）；image-intent 旅程补 D-164③ 确认执行步（视频路径同根，补确认后原 180s 预算内 15s 过，e2e-baseline §3 旧归因已随本行订正） | 主控亲验：diff 恰 4 spec 文件零生产源码；无 skip/only/fixme 新增；合入后 main 上 4 文件全量真跑 **14 绿/1 红（4.1m）**，红恰＝uiux-upgrade-b:72 P2-09 守门断言；pin 签名 0、自有栈（[Core] 472 行）；lane 自写执行（断言语义裁决类），driver 亲落 4 commit | **#340 已关**；P2-09 显示门回退（7fe159cd 摘门致静默失效控件）开 **#343** 修复票，:72 断言红着挂账至 #343 核销；未 push |
| d6a9892f（1 commit cherry-pick 自 deaecf8d；base=8dedc605） | #342 | **D-174 今日推荐行业层换源实施**：封闭词表新增 `store-profile:industry` 身份（key `store.profile.industry`，任意值可写、未映射值读时兜底不抛）；录入面 industry 进 wizard 且可跳过——`LEDGER_PROJECTED_FACTS` 与 `FACT_ORDER` 拆分（问什么/送什么两职责分离），**Day-0 问题流零改动**守 D-119/D-173；读取面 `configuredWhyNow` 档案优先（storeIndustry→intent 链后备），取数走事实账本与 stale 判据同源（改行业 bump factsRevision，旧卡正确置 stale 而非被静默改标签）；新 e2e `today-recommendation-industry.spec`＋排他清单实测加固（真实回归形态＝通用交付文案兜底，非预判的 platform/weekday）；`product-service` 手写枚举投影补齐（静默丢弃陷阱）；d14f58ba 过强注释订正 | 主控亲验：core typecheck 0；关键单测 20/20；合入后 main 上新 spec 真跑 **1 passed（43s，1.3m 墙钟）**自有栈 pin 签名 0；lane 证据链＝先红（回退换源一处＝D-174 前产品真实形态，红在行业断言 EXIT_1）与变异防伪合一（如实交代，该处即产品合同本身）＋sha256 逐字节还原＋三绿零 flaky（45s–1.2m）＋web 全量 1846/0 fail；due-delivery 时序坑与手写枚举投影坑随本行入 runbook | **#342 已关**；新 spec 必跑集归属留 wave 收口（lane 建议不进，红形态代价 +5.3m 在案）；未 push |
| c0c36c06＋a3af3c95（2 commits cherry-pick 自 0d71dc38/166a8aaa；base=ce378907，TEST-CATALOG 自动合并） | #335 | **xcheck A5 四旅程 spec 三桶断言清扫改积分合同**：前置留证＝旧断言真跑复现（registration `#redeem-copy` 死 240s；dashboard/t39 两红）＋**两处假绿实证**（assembly/dashboard 冷态钉的是 `legacyCreditUsageProjection` 冻结常量 5/5/1，真实出活 credits 动它不动）；新断言＝t39 余额精确等式（used 增量＝available 降幅＝settledCredits）＋三向量单位排他＋视频实测口径（信用授权恒 per_request、15/30/60 档位平价 50/90/160 不按秒乘）＋credits 对象整体 toEqual（含 soonestExpiringLot）；退役负向断言未动、零 allowances 新形状；信封警报自查命中 dashboard 降级腿 2 处裸 mock 补 meta（独立 commit，段序断言原样在位，其余三文件零 mock 全真栈） | 主控亲验：diff 恰 4 spec＋TEST-CATALOG 零生产源码；`LANE335-TEMP` 提交史零命中；合入后 main 上 4 文件全量真跑 **11 绿/2 红（4.8m）**，两红恰＝段序双条签名（proposal/create 顺序）；pin 与信封签名 0；lane 防伪自反证（目标值 +1 必红→还原复绿）＋负载门全程遵守（25.5 等到 14.7 才起跑） | **#335 已关**；两条段序红挂 **D-164① vs xhs §2.4 既有待决**（lane 留档实测段序 create→proposal→continue＋降级腿证明就绪＝裁决输入）；**#336 解锁**；未 push |
| 171f4697（1 commit cherry-pick 自 d3b06b5a；base=8f1e88c3） | #343 | **P2-09 生成参数显示门恢复**：free-creation-panel 收**必填** prop `generationParamsEnabled` 并把挂点包进门（必填设计＝将来漏门的挂点 typecheck 即红）；composer-home 仅 +1 行下传（复用 :1203 既有求值，提交侧 :1264 一字未动）；static wiring 重立「唯一挂点＋必在门后＋home 必须下传」；interaction 补行为证明（不支持→不挂、转支持→出现）。**假绿出处订正**：守卫断言 `assert.match(home, /generationParamsEnabled \? \(/)` 是被 2adacb77（#337 realign）换掉的，非票面写的 7fe159cd 自己——#337 的「源码对、测试过期」前提对这条不成立（源码已在当日 04:27 回归） | 主控亲验：diff 恰 4 文件 +51/-8、e2e spec 零触碰；main 上 static 6/6＋interaction 3/3＋spot e2e **2 绿/1 红（2.8m）**，红恰＝:72 新形态（:84 `composer-capsule-lens` 不可达＝#344 守门证据，:83 门断言已过）；lane 证据链＝先红原形态→变异（回退产品侧、测试留着，static+interaction 双红）→sha256 逐字节还原→全量 web 1846/0 fail | **#343 已关**；AC#2 按 #330→#342 先例改挂 **#344**（自由创作胶囊恢复，A 方向用户拍板）；2adacb77 同形态另两处（quote-precondition readiness／submit-intent groundingBlocker+storeFactsPending）开 **#345** 反向复核；未 push |
| 2b5c45ce（1 commit cherry-pick 自 a2e6c04e；base=6a49c5ed） | #344 | **自由创作胶囊条恢复（A 方向，用户拍板）**：三槽条件回 `7fe159cd~1`（lens/mention 无条件、recipePill 按 `surfaceQuery.data`）＋`recipeSummary` 同步去前缀；两处论证充分的连带改动＝`handleLensChange` 去自由态自动确认（:115 切换确认是消费者证明硬前提，同属那次收窄）＋删自由面板内重复 `LensRadiogroup`（固定 testid 双挂＝strict mode violation，spec §2.4 镜头本就在底栏胶囊）；free-creation 拆分完整保留（标题/模型直出徽标/模型选择器/状态/图片操作槽/P2-09 面板） | 主控亲验：diff 恰 3 文件 +63/-78、e2e spec 零触碰；main 上 interaction 3/3＋两 spec 合跑 **:72 一字不改 7.7s 转绿**＋day0-contract 7/7 全绿；变异防伪（回退产品侧→断言红）＋sha256 还原＋全量 1846/0 fail；顺带在 main 为 lane 点名的 mobile-product-shell:73 定状态＝红，静态链定责**非 #344 造成**（radiogroup 居 lensSlot→胶囊面板，关闭不挂；断言出生 07-22 早于胶囊化；自由面板默认不挂载）＝胶囊迁移债漏网文件，开 **#347** | **#344 已关**；P2-09 全链闭环（#343 门＋#344 可达性），:72 守门红核销；**#345 解锁**随派 lane-343；未 push |
| d724d66a（1 commit cherry-pick 自 a3f091f3；base=086bf38c） | #345 | **2adacb77 两条 realign 反向复核＋相邻缺陷修复**：条目1 readiness＝(b) 有意——注入仅在 free＋有镜头＋未选模型窄条件替换文案（8 个 live 输入逐条核对），状态机 no_model 原文是供给缺口语义、自由态照搬即对商家说谎（D-103 显式选模型）；条目2 grounding＝(b) 客户端收窄有 D-103「不读隐含门店」＋D-111「逃生口零经营上下文」授权，且无合规旁路（素材授权/受监管资质由 Core 无差别强制）；**挖出并修复相邻真缺陷**：Core grounding 不认识 creationMode、每次提交都要 confirmed_store/project，而前台错误映射缺这两项 → 门店缺口落通用「创作失败」兜底，composer-home:3888 的 'store' 具名分支＝全仓不可达死代码——修复只动 groundingBlockerFromError 错误路径（共享预检函数未动，两模式按下前行为零变化），死 UI 复活 | 主控亲验：diff 恰 2 文件 +147/-8、共享函数确认只调用未改（hunk 逐行读）；main 上 interaction 2/2＋tsc exit 0；lane 证据链＝先红（喂 409+missing → expected null to be 'store'）→绿→变异回退红→sha256 还原→216/216＋web 1846/0 fail；e2e 探针测量假象如实交代未采信 | **#345 已关**；**跨层口径冲突上呈用户**：D-111 自由创作零上下文 vs Core 每次提交要求确认门店＋项目 ⇒ Day-0 商家走自由创作一次也提交不了（lane 建议 A＝Core grounding 按 mode 分层，本修复与 A/B 兼容）；!hasRecipe 文案序小记留档；未 push |
| 978ec414（1 commit cherry-pick 自 9e67fef6；base=fe060099） | #347 | **mobile-product-shell 迁胶囊合同（迁移债漏网文件收尾）**：:73 radiogroup 断言改为先开镜头胶囊再断（prior art #337/#340 成例）；**多迁一处经主控复核采纳**——同用例「拍照」按钮同属 d27a43cc 债（composer-image-input 挂 attach 胶囊弹层，关闭不挂载），修好第一道门才看见（先红证据止步 :73），同一把尺不放宽断言；票面「移动视口不可达则上报」分支未触发——**390px 下两胶囊实测可开可读可关**（结论可供后续移动端票引用） | 主控亲验：diff 恰 1 spec 文件 +17、零生产源码；合入后 main 上该文件 **2/2 全绿（46.6s）**，pin 签名 0；lane 证据链＝先红引用 master-344-e2e.log＋第二道门先红（红点前移 :95）→全绿→拍照单项变异红→sha256 还原；一处红日志被同名覆盖的证据管理疏漏如实交代并以专项变异复现补救 | **#347 已关**；lane-343 四票（#343→#344→#345→#347）全部收官，lane 冻结；未 push |
| 73b45c45（1 commit，主控直落；base=66ee1343，spec 修复与本行同 commit） | — | **R-1 段序断言直落修复（挂账红核销）**：dashboard-home-mount 两处 `dashboardSectionOrder` 断言由 D-164① 旧序（proposal→create）重立为 R-1 序（create→proposal→continue）＋注释记 supersede 链——查证结论：该冲突已由 gap-remediation-plan-2026-08-02 裁决表 R-1 定案（spec §2.4 优先），页面实现与 dashboard-home-contract.test.ts 单元合同均已按 R-1 落地，e2e 是 L3-1 漏改债，无需用户再拍板（用户 2026-08-06「按建议继续」追认） | 主控亲验：修后该文件 **7/7 全绿（2.0m）**（此前 5/7，两红即此两条）；pin 签名 0；lane-335 留档的降级投影腿证明随段序修复就绪并已被 :656 用例吸收验证 | 挂账红清零（#335 遗留两条核销）；未 push |
| eb30152d→26cedb30（7 commits cherry-pick 自 lane-336；base=8a1e0a14，rebase 底=b92ea3de 零冲突） | #336 | **xcheck C6 商家语言护栏**：①quota 墙全积分（含最实一条：执行确认卡三桶兜底恒生效的根因链——creditBilling 无条件装配→passive 恒隐藏→「本次用 X 条」兜底恒印，改吃服务端积分报价、报价未到整行沉默）；②Landing 去档名（用户拍板）＝零 per-tier 事实，三卡+PricingPlan/PricingCard 退役、23 条 per-tier key 真删（删 key 即锁）、结构守卫禁数字+禁两套档名 token；③三桶投影边界锁：唯一商家面读取函数+孤儿面板 account-usage-panel 真删（零挂载 grep 证据）、internal/cutover-only 契约注释+RETIRED-METERING 机器标记+静态锁双路径；④fail-closed 审计 merchant-language-audit.test 8 断言（先红 5/8 抓存量→改净→绿；防伪七项含塞 token 红/豁免不哑/根改名红）；Core 8 文件 34 处 merchantMessage 纯文案改积分；三个合同文件按后继合同重立（landing-page 双词汇表排他+CTA 白名单、pricing.contract 禁数字+禁档名+retiredTierKeys 空集、peripheral 无禁用伪 CTA） | 主控亲验：diff 49 文件 +1050/-1452；composer-home 12 hunk 逐 hunk 对账（零渲染条件/状态/effect）；dbos 两行为文件逐行验证纯字符串；合入后 main 上审计+quota+cost 单测 28/28（我漏 locale:compile 造成的 2 假红补编译后清零）、execution-confirm interaction 9/9、locale:check 0、landing-page e2e 7/2——两红同咬「敬请期待」价格断言＝#349 签名（红着挂账）；lane 全部基线红有 stash 对照（failure-recovery 3 红、store-intake 2 红＝基线同款，load 3.0 排除负载） | **#336 已关**；#346/#349 解锁（landing 域两票同 lane 顺序承接：#349 修价→#346 重建守卫）；failure-recovery 3 条基线红收官统一开票；未 push |
| 9040fb84（1 commit cherry-pick 自 992558af；base=ba4d3c35） | #348 | **D-175 Core grounding 按创作模式分层**：resolve() 加 creationMode 入参（缺省 customized＝历史行为零变化）；free 放行 confirmed_store/confirmed_project、保留素材权利+资质合规底线；:80 短路修正（storeFactsRequired && !storeConfirmed）；快照 store 条件化（contracts 仅动 CreativeGroundingSnapshot.store 可选性，grounding 枚举未碰）；creationMode 种进裸 INSERT payload→派生继承→prepareCreativeJob 读 mode 全链真实可达；消费者证明落真实撞门链路（Day-0 free 出活正例读结果中心投影+定制态 409 对照+权利/资质负例+再创作继承旅程），**顺带补上全仓 CREATIVE_GROUNDING_INCOMPLETE 409 分支的测试真空**；两轮自我更正（门不在 composer 主链→裸 INSERT 桥）全程留档 | 主控亲验：diff 恰 8 文件 +384/-37、与 #336 零重叠、contracts/model-policy 关键 hunk 逐行读；main 上 core typecheck 0＋model-policy/creative-work 28/28＋uiux-day0-contract e2e **7/7 绿（2.5m，顶 load 70 跑出）**；lane 证据链＝先红（grounding 拒绝）→变异杀 5 条 free 证明而定制对照保持绿（非恒真）→逐字节还原 8/8；Core 全量 3102/0＋PG 236/0＋Web 1846/0 由 lane 留档 | **#348 已关**；D-175 前提订正已由主控落档（b92ea3de/8a1e0a14）；HANDOFF_PREFLIGHT projectConfirmed 缺口开 **#351** 调查裁决票；幽灵重试 #350 随派 lane-348；未 push |
| e5c5ccb7（1 commit cherry-pick 自 2fe94e35；base=a3750ed5） | #349 | **首页付费档「敬请期待」修复（#304 config-key 回归）**：两层——①plan key 换 growthConfigPlanId() 对真配置目录解析（优先 growth、仅 key 不存在才回退 pro；顺序有分量：Waffo 目录 pro 是真更高档，无序查找会把「少数字」换成「错数字」）；②formatPrice en-US→zh-CN（CN¥399→¥399，中国商家读者＋D-123/124/156 原文＋浏览器合同 /^¥\d+$/ 三依据；HKD 前缀保留既有断言未动即绿）——币种层留在本票经主控裁决：AC2 钉死「不改断言转绿」，改产品迎合合同而非弱化守卫 | 主控亲验：diff 恰 3 文件 +143/-13；main 上 pricing 合同 18/18＋landing-page e2e **9 passed/0 failed（44.8s）**，两条基线红断言零改动转绿；lane 防伪＝塞回旧常量两条立返红（Received 敬请期待）→cmp byte-identical 还原→18/18 复绿 | **#349 已关**；lane 自报新增两页 locale 约定分叉（landing zh-CN vs /pricing zh-HK）＋两页币种本不同（CNY vs HKD）随 #346 一并裁；#346 现红证据已留档；未 push |
| 0eeec681（1 commit cherry-pick 自 0506ae75；base=d175027d，#348 补遗） | #348 补遗 | **creation-mode 管线行为背书**：①手写枚举 payload 写入→读回测试（free/customized 各经真实 claim 落库、经 loadWorkspace（Operations 真读路）读回）——变异表实证坑位真实：删 payload 那行 tsc exit 0 而读回测试红 undefined!=='free'；②retry 永不重解析钉成测试（prepareCreativeJob 先继承冻结快照，mode 计数跨失败+重试全程仅 ['free'] 一次）——「retry 红转绿」构造不出来的结论由此固化，真实再解析旅程＝derive-then-submit（首轮消费者证明第 4 条已覆盖） | 主控亲验：diff 恰 2 测试文件 +156/-1；main 上两文件 36/36（lane 干净库）；lane 留档 PG 全量 237/0＋Core 全量 3104/0＋逐字节还原 | 补遗合入；**#351 依 lane 穷举证据关票**（HANDOFF 门拦 legacy Product ContentItem 链、free 的 P1 产物两链不相交、create_handoff 前台零调用者＝无死路无缺口，D-175 完整兑现）；双向库污染＋清收件箱＋读穿取数链三条新纪律随本行入 runbook；未 push |
| 6764cfe3＋f40f7fd0（1 commit cherry-pick 自 9120efa0＋主控 1 行 #352 指向补充；base=6ff57010） | #346 | **D-143 价源守卫重建（空转数周→真守卫）**：五腿——A：/pricing 必须跟随目录（fixture stub Core 发哨兵价）；B：首页必须跟随展示价（移动 VITE_PUBLIC_QUOTED_MONTHLY_CENTS，旧守卫最强一半保留）；C：**反串扰**（展示价动 /pricing 不许动、目录动首页不许动——替代「两页同数」，能抓「人工同步巧合」挡不住的串源）；D：每页恰一次；E：双资产事实具名 known-condition（不断言相等也不假装不存在，指向 #352）。生产面仅 growth 卡补回共享 handle 且仅 monthly 周期挂（年价答月价正是要防的错配）。机制选型三轮纠正（票面「两页同数」假设→waffo 档巧合→读穿取数链定「无任何配置同源」）全程在动手前，零返工代码 | 主控亲验：diff 恰 4 文件 +233/-51、生产触点仅条件 handle；main 上守卫＋landing 合跑 **11/11 全绿（1.4m）**；lane 防伪两 Plant 均命中（拿掉 handle 复现 #310 态两红；handle 冻字面量→腿 A 红 Expected HK$813 Received HK$522＝旧守卫完全抓不到的形态）＋byte-identical 还原；顺手修死代码 fixture（¥7273→¥7,273，守卫失效使其从未执行的副产品证据） | **#346 已关**；双资产终局与 locale 约定统一（zh-HK vs zh-CN）挂 #352 试点后议；landing 域三票（#336/#349/#346）全收口；未 push |
| 7a377a6f（1 commit cherry-pick 自 d6bf6bd9；base=de242f5b） | #350 | **幽灵重试按钮修复（选型 (a)-换动作）**：无可重试 Job 时主动作换「返回工作台」（与同 phase 视频分支房规逐字一致）；(c) 经重估否决——retry 管线是 Operations 执行器而 composer 作品跑 Harness，造 job 行＝换引擎重做非重试（0506ae75 钉子测试佐证：重试设计前提是已有 Job）；(b) 禁用件不合房规。可达性判定钉死：failed phase 真来源是 harness workflowState（composer 创作失败主旅程必渲染死按钮），work.status 那条才是不可达——只查后者会把真缺陷判成不可达 | 主控亲验：diff 恰 2 文件 +51/-4；main 上 result-shell-model 28/28；lane 证据链＝两条既有测试本是缺陷的测试化身（无 job facts 上断言 retry）补 jobId 重述＋新增 2 条「无 Job 任何位置不出现 retry」先红→28/28→变异精确杀死且只杀死那 2 条→shasum 还原；web 全量 1849/1837/0；3 条 interaction 红全部 stash 基线证伪（2 既有＋1 并发型不稳定） | **#350 已关**；「重试交付」标签派发创作重试的独立语义缺陷＋超时文案不一致开 **#353**；失败 composer 创作无原地重跑出口开 **#354** 跟踪；未 push |
| 19227133（1 commit cherry-pick 自 923a08ad；base=0c10894f，冲突 1 处取 lane 版） | #346 补遗 | **价源守卫三条收尾（主控点名，lane 单独成笔不 amend）**：①known-condition 由「filed for the user」改为指向 #352＋试点后与 #240 联动裁决（spec 头＋TEST-CATALOG 同步；与主控 f40f7fd0 那行一字补充相撞，取 lane 完整版）；②反串扰断言消息写病名不写症状（「/pricing 开始读 D-156 试点文案而非其声明的发布目录」＋镜像句）；③守卫自证栈身份——readQuotedPrice 把「哪个 URL 答了什么」记进 annotation，新增 expectDistinctStacks 断言第二 app 端口≠套件自身栈（防 reuseExistingServer 型「一个数跟自己比、每腿因构造而通过」，即本守卫自身的失效模式高一层） | 主控亲验：diff 恰 2 文件 +53/-18 纯测试面；main 上守卫＋landing 合跑 **11/11（1.4m）** REAL_EXIT=0、pinned 0 命中（master-346b-e2e.log）；waffo 配置实例独立腿裁决**不加**——waffo 档相等是人工同步巧合，证不出同源，lane 建议与主控一致 | #346 保持已关；lane-336 冻结（#336/#349/#346 landing 域全收口）；未 push |
| 27917beb＋17953af7（2 commit cherry-pick 自 b7772ddc＋b0ae353a；base=b40835ea） | #353 | **结果中心失败面诚实性收尾**：①标签「重试交付」→「重试创作」（考古钉死全仓无任何「重交付」命令，retry 唯一派发点=retry_creative_job）；②票面纠正——:641 是测试文件行号、「生成超时可以重试」本身诚实（failureCode 只经 input.job 进投影，无 job 必不渲染），真谎言在 recoveryHintFor（run-detail-model:191）failed 相位无条件叫商家点已被 #350 拆掉的重试键，按 jobStatus 分流（无 job→「请返回工作台重新发起本次创作」）；③主控采纳方向变更（353-2 裁决）：delivery failed 非视频分支主动作无条件改 deliver「处理交付问题」与视频分支房规对齐，job 条件一并退役（deliver 走 ContentPackage 不需 Job 行，旧兜底只为死键而存在）；retry 消失不降级（错药/按次计费误点即账单/移动端折叠藏错药/房规=视频分支不给）；#350 创作失败合同零改动，交叉测试 :439 一字未改仍绿（报而不停处置合规） | 主控亲验：两笔 diff 恰 4+2 文件 +52/-2、+30/-17，两处生产 hunk 逐行对账与自述一致；main 上两测试文件 35/35 REAL_EXIT=0（master-353-unit.log）；lane 防伪 M1-M5 全部精确击杀（M3=改哑变异被 #350 既有测试＋with-job 守卫双杀＝「不许改哑」正面证据；M5=半修变异 id 对 label 旧照杀）＋sha256 逐字节还原；全量 1851/1839/0，interaction 2 红=lane-355 作业面基线同款 stash 对照 | **#353 已关**；两处软承诺（stageSummaryFor/costSummaryFor）已挂 #354 随重跑能力决策一并裁；未 push |
| d6fe840d＋9f9d0d59＋3e9549cb（3 commit cherry-pick 自 a149db45/ab9535f4/5f4a35da；base=e1cae6d3） | #355 | **五条基线红调查终局＋同因家族清扫**：五条全部定性死断言/失效选择器、零产品缺陷零负载零钉子——簇1根因=d27a43cc（08-02）Composer 胶囊化后 3cef0ee9 只适配走 ui-journey fixture 的 spec、手写驱动 spec 全被漏下（lens 组弹层未开不挂载，死证=call log 无 resolved to 行＋探针 lensPresent:false）；簇2根因=d6a9892f（D-174）改了兄弟单测漏了 DOM 级 interaction。扩面（355-2①）：p0-golden-journey 与 video-result-live-commands 同因修复——**必跑门自 08-02 被静默打穿，就此恢复**；works-reshell 胶囊病灶除净后暴露两条另因红（执行确认门未适配＋裸 {data} 信封 mock），lane 按纪律停手上报→#357。防复发静态守卫 composer-capsule-selector-gate（lens/destination/recipe 三族，出现选择器须同文件有胶囊开启），**守卫比人工审计多抓一条**（works-reshell 在关着的弹层上点配方卡＝促销海报配方从未被应用）；豁免仅 file 内标记制、fail-closed 四条 | 主控亲验：diff 7 文件全测试面零产品源（git diff --name-only 复核）；main 上守卫 2/2、interaction **489/489**（store-intake 两条历史红就此清零）、e2e 三文件 **5 passed（4.2m）** REAL_EXIT=0 pinned 0（master-355-{guard,interaction,e2e}.log）；lane 证据=逐 spec 先红（6m/6m/10m 打满同构签名）后绿（26.9s/27.5s/3.3m）、变异全打产品源、守卫红一次点名 5 违规＋变异单点复红、sha256 还原；未 skip 未放宽超时 | **#355 已关**；works-reshell 两条另因红→**#357**；弱断言加固=#356 已解锁；lane-355 续派 #357→#356；未 push |
| c6e0fd57（1 commit cherry-pick 自 f5539a32；base=49829ae6） | #357 | **works-reshell 两条另因红修复（#355 移交）**：①执行确认门——先核合同（D-119 :43 录入永不前置＋D-164③ :3089 执行确认卡）再核实现：fact-satisfaction.ts:248 仅 critical 才发问题卡、fixture criticality 由 intent 文本确定性推导，本用例 intent 零关键词命中→optional→旧断言等的 ask 卡**确定性不可达**＝死断言；改为驱动执行确认卡（参照 ui-journey 房规）；②裸 {data} 信封 mock（works.ts:203,207）——取数链读穿到常量层（apiSuccessSchema .strict()＋meta.correlationId 必填且 :100 拼错误信息）→裸信封必然 parse 失败且不点名 mock，修成完整 P1 信封；同修 30s 默认超时 vs 60s 断言矛盾。fixtures/works.ts 消费者实查仅此一 spec，零外溢 | 主控亲验：diff 恰 2 文件 +43/-8 全测试面（name-only 复核）；main 上整文件 **5 passed（2.1m）** REAL_EXIT=0 pinned 0（master-357-e2e.log）；lane 变异分账两枚——摘确认点击→卡 123× resolved-to 挂屏 60s（门是真门闩）；留超时修正退信封→仍红且跑满 60s（**信封才是缺陷、超时修正不买绿**，绿态 8.8s≪30s）；sha256 双还原；全量 1853/0 | **#357 已关**；lane-355 顺队接 #356；未 push |
| 3aeacefb（1 commit cherry-pick 自 fe2301f2；base=3b8c8a69） | #356 | **积分退还断言加固（账本事实行）**：考古（355-4 口径）先判世界——失败档确实先扣后退（账本 reserve→refund 相隔 408ms、reserve 转 refunded）＝世界(a)为真，**但方案 1 仍不可用**：前台余额线滞后账本（探针 11 格实证：reserve 已落账时余额仍读 100，显示 99 时 refund 早已落地），「中途下探」是缓存陈旧读数非预扣实时反映，断它＝又 flaky 又断错对象；改走方案 2＋保留既有净零断言（组合），取数走 entitlements/credit_detail（账户页同一投影，商家查得到＝仍是商家视角行为为证）。**前提修正**：lane 自纠 #355 交底「只能证明没净扣」过重——变异矩阵证明两个单点故障既有断言各能抓（扣了没退→:323、从未扣→:311 卡面），真盲区＝**化妆型故障 A″（假称已退实际从未扣）既有全放行、仅新断言报警**，等于把 merchant-delivery-language.ts:215 那句「saying 已退回 without knowing is exactly the dishonesty…」注释变成可执行检查 | 主控亲验：diff 恰 1 文件 +65 全测试面；main 上整文件 **3 passed（3.4m）** REAL_EXIT=0 pinned 0（master-356-e2e.log）；lane 证据=四变异（A/A′/A″/B）各红对位、4 个临时改动产品文件 sha256 逐字节还原＋探针拆除核 sha、变异后复跑 3 passed、node 全量 1853/0；无 skip 无放宽、既有断言零改动；计费链无缺陷（余额线滞后属展示层时序，未擅动） | **#356 已关**；lane-355 顺队接 #358（D-176 诚实文案）；未 push |
| a7bbfb21＋933d4418（2 commit cherry-pick 自 c4f475e5＋715b747b；base=3fa42d1b） | #358 | **D-176 诚实文案三处落地**：①stageSummaryFor（折叠 badge，无 job→「生成失败，请返回工作台重新发起」——badge 是折叠态唯一可见行故特意写入出口）；②costSummaryFor（无 job→「当前页面不会重新发起本次创作」，句型抄同函数视频分支已评审句式）；③失败横幅第三处（主控 355-6 折票，lane 探针实证假承诺真实上屏：既有断言只断前缀、收紧到整句 14 passed）。**横幅门=facts.jobId 而非裁决字面的 runDetailFacts.jobStatus——lane 偏离有据，主控复核采纳**：按钮存在性由 retryableRun=Boolean(facts.jobId)（shell-model:230）决定，读同一事实＝句子构造上不可能比按钮活得久；字面口径两毛病（可选 prop 缺省被当无 job＋既有 fixture 两信号已分叉），且 N3 变异实证字面实现会造新空头支票（with-job 守卫报红）。有 job/视频文案三处逐字不变；lane 自纠上封「视频句无守卫」表述（:264 既有两子串断言在）；M4 自曝新分支覆盖缺口并补 running+无job 边界钉 | 主控亲验：两笔 diff 恰 4 文件 +121/-3（生产 2 文件 +24/-3）、三处生产 hunk 逐行对账各读自己屏面按钮的同源事实；main 上 run-detail＋shell-model 36/36、interaction **491/491** 双 EXIT=0（master-358-{unit,interaction}.log）；lane 证据=两轮先红后绿＋变异 M1-M4/N1-N4 各红对位（N2/N3 with-job 守卫击杀＝「不改哑」正面证据）＋生产文件 sha 逐次还原；node 全量 1854/0、typecheck 0、无新增 skip | **#358 已关**（D-176 实施闭环）；阶段条观察留 D-176 触发点重审；lane-355 队列清空冻结；未 push→随本轮统一收口 | 
| e82a039b（lane/419 ff；base=f1f12e42） | #419 | **D-177 standalone tool 链端到端拆除（admin 整备波首合）**：Core 退种（TOOL_ENTRY_SEEDS 删除＋墓碑注释、recipe-studio 生产输入去 surfaceToolRefs、browser projection 去 toolEntryRefs、launch-seeds 负断言钉死字段不再出现在已发布 Surface）；contracts 删 SurfaceToolRef/CreativeToolEntry＋能力表裁剪；Web 删 9 文件（tool-entry-seeds/tool-route-model/tool-handoff/composer-tools/composer-tools-strip/tools_ 路由）＋全目录 templates-only（tools tab 退场、legacy 'recipe' 别名保留、catalog-return-store 拒绝 tab:'tools' 快照、composer-live 去 tool_list）；routeTree.gen 手工修剪（CLI 全量重生引入无关 implicit-any，下次 dev regen 收敛同树）；migration-matrix 两行补 D-177 退役标注 | 主控亲验：46 文件 +138/-1927；验收 grep 全仓仅余 3 处合法（负断言测试＋墓碑＋已标注文档）——grok 报 0 hits 系 grep 范围窄于主控扫描，无实质遗漏；Core 27/27＋PG 2/2（meiye_lane419）＋mkfast node 41/41＋interaction 15/15（vitest 4 文件）；三包 tsc 净（mkfast 滤 2 条已知 content-collections 环境项）；launch-seeds cast 红由主控修（as unknown 中转）| **#419 已关**；合入定序第一位——admin-creation-experience-control.tsx 热点解锁 #372-UI/#359/#361；dontAsk 拒删病理复盘记 memory；未 push |
| 18bbc898（lane/360 rebase 后 ff；base=b99d4b20） | #360 | **published-recipe workflow catalog 读端口（Skill 治理链地基）**：catalog-service 增 listPublishedRecipeWorkflowRevisionRefs（seed＋DB 合并、去重、published-only）；memory/postgres repo 对称实现 listPublishedRecipes；导出接缝供 #362 bind 下拉与服务端校验共用 | 主控亲验（rebase 到 #419 基线后全套复跑）：memory 12/12＋PG 绿（meiye_lane360）＋catalog/launch-seeds 合跑无红；三包 tsc 净 | 与 #362 同笔 ff；未 push |
| ff454189（lane/360 rebase 后 ff；base=b99d4b20） | #362 | **Skill bind 治理边界**：bind/define 拒绝未发布或治理白名单外的 workflow revision refs（稳定 INVALID_STATE）；红测钉死旧静默病理（frozen skill 治理仅 copy@1 却可绑 image_text@1 得空 allowlist）；durable runtime 先 migrate catalog 再绑平台 Skill；admin 下拉消费 published_recipe_workflow_revision_refs query（workspace.read），Web 不另造白名单 | 主控亲验：core 118/118＋PG 档 49/49（原 skip 的 PG 测在 meiye_lane360 真跑）＋route 9/9；rebase 后复跑 147 pass＋PG＋三包 tsc 净（mkfast 滤 2 条已知环境项） | **#360/#362 已关**；E 批 #382 硬边解锁；未 push |
| 69c432ed（lane/359 ff；base=15e28f1a） | #359 | **三态字段合并语义（Spec B P0，票面已按 #398 终局收窄）**：revision-field-merge.ts 唯一实现（创建缺省→规范化默认／更新缺省→继承 head／显式空→清空），导出接缝供 #361 复用；draftSurface 先读 head＋CAS 再 normalize+hash（顺序由 spy 测钉死）；recipeRefs（#419 后 Surface 仅存的可选集合）接入；draftRecipe 改 head-first 为 #361 铺位；SurfaceBodyInput.recipeRefs 转可选表达 omit vs 显式空；写通道/历史/CAS/发布/审计零改动；Web 侧验收按依赖更新评论作废未做 | 主控亲验：rebase 到 15e28f1a（冲突仅 index.ts 导出并集）后 50/50（含 #360 的 12 条——发现 node --test 对缺失文件静默无红，测网虚指已纠）＋PG 2 文件绿（meiye_lane419）＋core tsc 净 | **#359 已关**；#361 解锁（L1 尾票）；未 push |
| 034915e1（lane/372 rebase 后 ff；base=32d9f8a7） | #372（部分） | **governed 保存链 Core 腿**：RecipeGovernanceFormInput＋adaptRecipeGovernanceFormToCompileInput，recipe_governance_save 命令串 compile→CAS validate。**票留开口**：Templates UI 接线待 #361 让出 admin-creation-experience-control.tsx 热点后补做，届时再关票 | 主控亲验：governance-form 20/20（首验）；rebase 后并入全网 154/154 | 未关票；未 push |
| 375f5a41（同笔） | #373 | **recipe_published_revisions typed query**：入参 {surfaceId,recipeIds}，groups（published-only、recipeId 升序/revision 降序、空组保留）＋availableRecipeHeads（每 recipe 最新 published revision，容忍 head 已前进）；repo 双实现 listLatestPublishedRecipes 与 #360 的 listPublishedRecipes 语义并存（head 恰 published vs 最新 published）；capability=config.publish 管理员限定＋无 step-up 读路径登记 | 主控亲验：memory 12/12＋PG 于 meiye_lane372 绿（PG 测全库 deepStrictEqual 撞种子数据，主控按 #360 suffix 隔离法修净）；rebase 冲突 4 文件全并集解（剔除旧基线夹带的 isKnownToolEntryId 工具链残余）＋154/154＋PG 6/6＋tsc×3 净 | **#373 已关**；未 push |
| d2e198b3（同笔） | #374 | **default-deny 证据端口（Spec D×I 交界）**：RecipeEvidenceReceipt 合同冻结为 Spec I 十三字段（主控逐字段核对）；record_eval/internal_test 只收 evidenceReceiptId、丢弃客户端构造对象；default-deny 适配器抛 evidence-unavailable INVALID_STATE；launch seed 走服务端 permitting 适配器 bootstrap；admin 两门按钮永久禁用＋说明；生产切换不可用窗口交付说明落 docs/ops/recipe-governance-production-switch-unavailable-window-2026-08-06.md（D+I 同批约束由该说明显式承接，#396 解锁） | 主控亲验：41/41（recipe-studio 13＋launch-seeds 9＋governance-form 9＋foundation 10）＋route 9/9＋tsc 净；rebase 后全网 154/154＋PG 6/6 | **#374 已关**；未 push |
| lane/361 ff（base=370cfbde） | #361 | **Recipe draft 往返保住 factTypes/skillRevisionRefs（Spec B 第 2 处静默丢失，L1 尾票）**：draftRecipe 在 head+CAS 后对两字段走 #359 的 resolveThreeStateCollectionField 再 normalize/hash（?? [] 仍是规范化默认非覆盖器，注释钉死口径）；Web RecipeRecord 增两字段、hydrate 从 head 投影、recipe_draft payload 显式发送；交互测试=fixture 带既有绑定只改标题保存，payload 与外部 revision 双保留 | 主控亲验：core 42/42 首验→rebase 到 370cfbde 后全网 151/151＋PG 3/3（meiye_lane419）＋interaction 5/5＋tsc 净 | **#361 已关**；L1 链（#419→#359→#361）全闭；admin-creation-experience-control.tsx 热点让出，#372-UI 接线解锁；未 push |
| fd12388e（lane/372ui ff；base=e0f8a416） | #372（Web 腿，票就此完整） | **Templates 治理保存接线**：recipe_governance_save 由 Templates 面板提交（RecipeGovernanceFormInput 形状；编辑从 head 回填、新建确定性默认 industryKey=beauty_general＋studio sample refs，不从 lens 猜）；无 raw JSON 路径；payload 永不携带发布态/passed/隐藏 prompt/blocks（交互测钉形状）；服务端编译回执渲染；recipe_draft 完整保留（#361 往返不回归）；capability=config.publish＋step-up 豁免登记（迭代写同档） | 主控亲验：interaction 8/8（含 #361 原测）＋recent-auth 8/8＋access-control 7/7＋tsc×3 净 | **#372 已关**（Core 腿 034915e1＋Web 腿本笔）；#375 解锁；未 push |
| aa320087（lane/393 rebase 后 ff；base=fd12388e） | #393 | **不可变证据回执注册表（Spec I 地基）**：回执与 EvalRun 分离存储（运行事实 vs 绑定断言）；put-once 深比对幂等（同 receiptId 同事实幂等成功／异事实 IDEMPOTENCY_CONFLICT）；p1_recipe_evidence_receipts 表 receiptId 主键＋(evidence_kind,recipe_id,recipe_revision) 索引；EvalRun 注册表端口提升为 harness 共享服务，Skill 侧表与写入路径零改动零迁移；runtime 暴露双注册表并 migrate；#374 冻结的 13 字段合同未动（主控 diff 复核） | 主控亲验：memory 5/5＋PG 3/3（meiye_lane372 真库，索引存在性断言过）＋rebase 后 35/35＋tsc 净 | **#393 已关**；#394 签发器解锁；未 push |
