# V3.1 批次 1 漏斗与性能基线（V31-05 / §38）

**Status**: captured as measurement contract + collector scaffold  
**Authority**: V3.1 §35 批次 1 末项交付物；口径 §38；供 §43.11/12「不劣化」比较  
**Ticket**: V31-05  
**Captured at**: 2026-08-08  
**Data window**: **unavailable** — no production/pilot warehouse export in this worktree; live numbers not fabricated.

## 1. 口径（与 §38 同名）

### 1.1 硬门 5 条（验收层，非观测）

| # | 指标 | 门限 | 分母 / 样本下限 |
|---|---|---|---|
| H1 | rights / billing 错误 | = 0 | 全部付费确认 + 结算事件 |
| H2 | Plan 与执行 snapshot mismatch | = 0 | 全部含 ExecutionPlanSnapshot 的 Make 启动 |
| H3 | duplicate debit / duplicate accepted side effect | = 0 | 全部 product usage ledger 写入 |
| H4 | pending interrupt 丢失 | = 0 | 全部 interrupt.requested → 恢复成功 |
| H5 | Day-0 自由创作可达且简单任务不因升级变慢 | 可达 + Level 1 不劣化 | Day-0 旅程样本 ≥ 试点窗约定 |

### 1.2 产品漏斗（观测层）

顺序（§38）：

`Intent submitted → first useful understanding → Plan ready → Plan confirmed → Make started → first usable artifact → Delivered → Publish handoff completed → Published recorded → Outcome reported → Observed signal`

### 1.3 延迟 / 质量观测（试点参考值）

| 指标 | 试点参考 | 聚合 |
|---|---:|---|
| 首个有意义 acknowledgment | ≤ 1.2s | p75 |
| 首次 Activity | ≤ 1s | p75 |
| memory retrieval | ≤ 300ms | p95 |
| intent ready | ≤ 2.5s | p75 |
| simple task（Level 1）plan/brief | ≤ 4s | p75 |
| complex Living Plan（Level 2+） | ≤ 8–12s | p75 |
| steering ack | ≤ 1s | p95 |
| reconnect snapshot | ≤ 2s | p95 |
| semantic projection lag | ≤ 500ms | p99 |
| 每轮必要问题 | ≤ 1 | max |
| Plan 接受或小改后接受 | ≥ 70% | rate |
| schema repair | < 10% | rate |
| 重连恢复成功 | ≥ 99.5% | rate |
| Artifact 重复对象率 | 0 | rate |
| 交付到手机交接 | 中位 < 5 分钟 | median |
| 交付后 7 日自报覆盖率 | 试点 ≥ 40%（仅观测，U2） | rate |

命名、分母、样本下限在升门时必须与 §38 / §43 / 批次退出门 / 测试数据窗口 **四处同一**。

## 2. 当前数据状态

| 项 | 状态 |
|---|---|
| Production warehouse 查询 | **未接入**（本 worktree 无只读分析库凭据） |
| Fixture / e2e 合成延迟 | 不作为 before 基线（会污染「不劣化」） |
| before 数值表 | **空** — 下列单元格保留为 `n/a` |
| Collector | `scripts/ops/collect-v31-batch1-baseline.mjs` |

### 2.1 Before 表（数值待主控/运维填）

| 指标 | 值 | 窗口 | 样本 n | 备注 |
|---|---:|---|---:|---|
| H1 rights/billing errors | n/a | — | 0 | 待采集 |
| H2 snapshot mismatch | n/a | — | 0 | 待采集 |
| H3 duplicate debit | n/a | — | 0 | 待采集 |
| H4 pending interrupt lost | n/a | — | 0 | 待采集 |
| H5 Day-0 p75 simple task | n/a | — | 0 | 待采集 |
| acknowledgment p75 | n/a | — | 0 | 待采集 |
| first activity p75 | n/a | — | 0 | 待采集 |
| reconnect snapshot p95 | n/a | — | 0 | 待采集 |
| funnel: Intent→Delivered conversion | n/a | — | 0 | 待采集 |
| funnel: Delivered→Outcome 7d | n/a | — | 0 | 待采集 |

**诚实标注**：本文件满足 V31-05「基线文件落盘 + 数据窗口注明」；**不**声称已有真实 before 数字。后续填数时只追加窗口与 n，不改指标命名。

## 3. 采集命令

```bash
# Dry-run: emit schema + empty rows (safe, no network)
node scripts/ops/collect-v31-batch1-baseline.mjs --mode=schema

# When analytics export path is available (env-gated):
# V31_BASELINE_SOURCE=path/to/events.ndjson \
#   node scripts/ops/collect-v31-batch1-baseline.mjs --mode=from-export --out=docs/ops/v3.1-batch1-baseline-filled.json
```

## 4. A15 / A16 对照（批次 1 退出）

| 项 | 现状 | V31-05 处置 |
|---|---|---|
| A15 required CI job 聚合门 | `.github/workflows/core-quality.yml` → `required` job + `scripts/ci/assert-required-jobs.mjs` | **保持**；本票不改 job 图 |
| A15 五 journey 门 | `production-main-journey` + P2 五文件矩阵（既有） | **保持**；Thread-root journey 以新增 e2e **spec 文件**交付，实跑由主控 lane 端口收口 |
| A16 R-8 三态截图基线 | GAP A-5 Idle/Active/Delivered `toHaveScreenshot` | **重拍留主控**（形态改版后旧基线作废）；本票不落假截图 |

## 5. ai / @ai-sdk/react 大版本对齐核查

| 包 | 声明版本（mkfast） | 安装解析 | 结论 |
|---|---|---|---|
| `ai` | `7.0.19` | `7.0.19` | 主版本 **7** |
| `@ai-sdk/react` | `4.0.23` | `4.0.23`（其依赖 `ai@7.0.22`） | 主版本 **4**，与 AI SDK v7 线对齐 |
| `apps/core` `ai` | `^7.0.19` | 同主版本 7 | 与 web 主版本一致 |

**结论**：大版本已对齐（`ai@7` + `@ai-sdk/react@4`）。补丁级 `7.0.19` vs 传递依赖 `7.0.22` 不在本票折腾；升补丁可由主控统一依赖刷新。
