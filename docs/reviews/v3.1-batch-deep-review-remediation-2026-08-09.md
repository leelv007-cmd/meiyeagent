# V3.1 批次 Deep Review 修复完成报告（2026-08-09）

> **执行方式**：按 `docs/reviews/v3.1-batch-deep-review-2026-08-09.md` 报告 S1–S5 分级,以 `docs/tickets/v3.1/` 票面为唯一执行依据,agent team 分 5 波次并行修复 + 主控装配合流与交叉验证。未 commit(仓库纪律:不 push、不关票,合入由主控亲验)。

---

## 一、修复完成总览

| 波次 | 任务 | 判定 | 验证 |
|---|---|---|---|
| 1 | V31-05 CI 红测试(同步真实产品契约) | ✅ | web wiring 静态测试 10/10 |
| 1 | V31-06 中间件未注册 policy fail-closed(U11 一致性) | ✅ | 146/128+18skip, 新增回归测试 |
| 1 | V31-24 kill switch 客户端 config 覆盖封堵 | ✅ | goal-proactive 24/24 |
| 1 | V31-15 冷 delta 无限 resync 死循环(客户端冷 delta 自举) | ✅ | contracts 210/0 + SSE roundtrip + reducer 80/0 |
| 2 | V31-12 ExecutionPlanSnapshot 产源(compile-finalize 冻结钩子) | ✅ | 1022/0 (core harness+spine+session) |
| 2 | V31-11 savePending 并入创建事务(A3 原子性) | ✅ | 4 PG 测试(无孤儿行/无超扣)+ 8 内存 |
| 3 | V31-14 中断闭环(DBOS 镜像+resume 回注桥+§23.4 接线) | ✅ | 156/156, harness 全目录 848/0 |
| 3 | V31-15 视频 producer 接线+图文骨架/文案三阶段 | ✅ | workflow-core 65/65 + emitter 8/8 + frame 5/5 |
| 3 | V31-21 bootstrap release+resolveForRun 生产接入 | ✅ | harness-release 13/13 |
| 3 | V31-23 L0.5 生产抽样触发点 | ✅ | dbos-workflow 40/40 |
| 3 | V31-20 boot 级 production release 可解析校验 | ✅ | prompt-packs 覆盖测试 |
| 3 | V31-13 shadow 采样随产源可达 | ✅ | shadow-reconciliation 30/30 |
| 3 | V31-11 确认 HTTP 路由(create/decide/expire/list) | ✅ | execution-confirmation-http 4/4 |
| 3(主控) | V31-14 装配三处补线(interrupts port/resume bridge/fence live-facts) | ✅ | 装配 53/53, tsc 0 |
| 4a | V31-11 确认门挂 createRequest+双扣合流+web decide 接线 | ✅ | confirmation-gate-merge 4/4 + web 15/15 |
| 4b | V31-18 注入清单生产记录+P1 query+web UI+撤销 | ✅ | core 539/0 + web interaction 56/0 |
| 4c | V31-17 §37.4-K 空转断言升级、V31-22 ops-console release journey、V31-07 Day-0 journey | ✅ | 5 e2e 可解析(list 通过), testid 全核对 |
| 4d | V31-25 收敛前后等价对照(冻结 pre-convergence 基线) | ✅ | runner-convergence 84/84, 负向验证有效 |
| 5(主控) | 全局验证 | ✅ | 见下 |

## 二、全局验证结果

| 门 | 结果 |
|---|---|
| contracts typecheck | ✅ 0 |
| core typecheck | ✅ 0 |
| web typecheck | ✅ 0 |
| contracts 全量测试 | ✅ 210 pass / 0 fail |
| core 全量测试 | ✅ 3456 pass / **1 fail(既有,非本批引入)** |
| web node 测试 | 3 fail(全部既有:merchant-language-audit×2 + recent-admin-session,均未改动文件) |
| web interaction 全量 | ✅ 594 pass / 0 fail |
| e2e spec 解析 | ✅ 5/5 (v31-day0 / ops-console-release / publish-handoff×3) |
| 改动面 | 64 文件 +5548/-227, 全部对应票面修复项 |

**既有失败确认**:core 的 `credit-entitlement-policy.test.ts`(booster-only credits)与 web 的 3 个失败均在干净 HEAD(6a1d01d2c)独立 worktree 复现,与本次修复无关。

## 三、按票面验收的修复对照

### 批次 1
- **V31-05**(原 PARTIAL→现恢复 CI 绿):`agent-workbench-wiring.static.test.ts` 断言同步为 `explicitThreadId={activeAgentThreadId}` + 来源表达式断言(不弱化,仍验证 Thread-root 绑定)。
- V31-05 遗留(A16 重拍/基线真数)仍按票面自注挂主控——需浏览器与 analytics 导出,本批不改。

### 批次 3(本批最大修复面)
- **V31-12**:compile-finalize 冻结载体(`ExecutionPlanCompileFreeze`)→ `assembleExecutionPlanSnapshot` → task-admission 一次性写(`admitSnapshot` sole writer)→ DBOS 复验;纯 copy 走 `policy_exempt_copy`(U9);fingerprint 排除 snapshot(重放幂等)。
- **V31-11**:`savePendingWithClient` 并入创建事务;确认门经 `createExecutionConfirmationRequest` 挂 `createRequest`(requestId=`confirmation:<workflowId>` 幂等);**双扣合流**=同一 `creditUsageOperationId(taskId)`(U8=A 确认前 reserve,执行期结算不再扣,退款释放恰一次);web decide 先走确认路由,失败回退 interaction-only(不劣化)。
- **V31-14**:DBOS `awaitDecision` 经 `HarnessInterruptProtocolPort` 镜像进 `p1_agent_interrupts`(8 个决议出口全同步生命周期);resume 经 `createHarnessInterruptResumeBridge` 用 DBOS.send 第 4 参 exactly-once 回注;`fenceContext` 先跑 `evaluateMidExecutionContextFence`(safe_stop 不重复扣费/pause_prompt 带提示停止)再落 recompile 兜底。
- **V31-13**:随 V31-12 产源落地,shadow 采样在生产路径可达(无需额外接线)。

### 批次 4
- **V31-15**:冷 delta(baseRevision=0)客户端自举(消除无限 resync);视频 4 发射点(分镜/关键帧/重编译/重选)接真实 runner;图文 skeleton→copy→image 三阶段,artifactId 不变、revision 单调递增。

### 批次 5
- **V31-21**:`ensureBootstrapProductionRelease()`(无 production 自动发布,沿用 `composer-plan-surface-v1`);session resolveRelease 改走 `resolveForRun`(frozen pin 恒赢、无 pin 回退 production,canary/rollback 对生产流量生效);执行链 releaseId 经 compile-freeze 链真实填充。
- **V31-23**:Make complete 边界 `production-l0-5-sample` runStep(sample→verdict→release 绑定→Langfuse outbox,失败吞掉不毁 run,resultId 确定性幂等)。
- **V31-20**:boot 调 `assertProductionReleasePromptResolvable`(无 production no-op,有则 fail-closed)。
- **V31-22**:新 e2e `v31-ops-console-release-journey.spec.ts`(发布→缺 pin 拒发→canary→试跑→放量→回滚→演练→审计留痕,真实 P1 路由)。

### E lane
- **V31-18**:`memory.injection_receipt` P1 query(workspace 鉴权);`read_confirmed_experience` 注入真实点记录 receipt(观测侧通道不打断 turn);web 任务详情注入清单面板(可撤销);`memory.revoke_memory` 最小命令。

### 批次 6
- **V31-25**:冻结 `fixtures/pre-convergence-equivalence-baselines.ts`(真实收敛前 commit `64bdaded8^` 独立执行产物)→ 当前 executor 逐 fixture 对照交付物/结算/effect 键/进度;kill/restart 双跑 effect 多重集==基线;负向验证(篡改 packageId 即红)。

### 收尾
- **V31-27/28**:无新问题(上轮已 COMPLETE)。V31-28 引入的红测试已随 V31-05 修复闭环。

## 四、剩余缺口(诚实清单)

1. **e2e 浏览器实跑**:5 个 journey spec 已写成可执行契约并过 `--list` 解析,实跑仍归 merge controller(仓库纪律),本环境无完整栈。
2. **V31-14 pause_prompt 可续挂起**:当前为「带提示停止」(terminal failure 带 merchantMessage),非 in-flight 可续挂起——需 workflow-core run 循环内加 awaitDecision 门,建议下票处理。
3. **V31-12 rights head 表**:生产解析器仍把冻结 rights 报为 current(撤权检测以 delivery/fence 端口兜底 fail-closed),属既有文档化缺口。
4. **V31-05 A16 三态截图重拍 + 基线真数**:仍挂主控(需浏览器与 analytics 导出)。
5. **V31-25 P1-a 六原语内部重写**:本批以等价对照证明「收敛未改变行为」,runner 内部六原语化(workflow-core 直调 executeAndSelect/compile*Brief)未重写——与票面叙事仍有差异,建议显式修订票面或另票。
6. **V31-26b**:仍挂试点门(触发点=真实商家试点结论),26a 已闭。

## 五、既有失败(非本批引入,供参考)

- core: `credit-entitlement-policy.test.ts` "booster-only credits never restore paid non-credit entitlements"(干净 HEAD 复现)
- web: `merchant-language-audit.test.ts` ×2、"default session getter dynamically imports auth under Node 24"(`recent-admin-session.test.ts`,干净 HEAD 复现)
