# #261 复核（正向）· 承诺 → 实现：票面与决策原文逐条找落点

> 方向：**承诺侧有没有落空**。反向（实现建了但没人要）见 `06-xcheck-reverse.md`。
> 复核对象：`01-ia-three-sections.md`／`02-confirm-card-and-cost.md`／`03-rating-memory-events.md`／`04-events-memory-nav.md`（三个互不可见的 agent 写成，从未交叉核对）。
> 复核时 `07-implementation-plan.md` 已出现（16:39），凡它补上的落点单独标注 —— **它是执行清单不是设计稿**，只给步骤不给文案/键名/断言。
> 承诺来源：`gh issue view 261 --comments`／`docs/design/beauty-marketing-agent-product-design-2026-07-17.md`（D-164 全段 `:3070-3175`、D-160③ `:2704-2739`、D-163② `:3005-3033`）／`docs/specs/agent-substrate-dev-spec-2026-07-29.md`。
> 判定口径从硬：**语义不符与「只在别处顺带提过一句」一律不记已覆盖**。

---

## 一、承诺 → 落点对照表

### 1.1 票面「任务」六条

| # | 承诺原文 | 设计稿落点 | 判定 | 证据 |
|---|---|---|---|---|
| T1 | 路由收敛：单 Dashboard 路由三段，不再分「工作台首页/创作页」 | `01 §2.1` 分叉收敛表＋`§2.2` 三段渲染＋`§6` 收敛清单 9 行 | **已覆盖** | `?view=` 改 `beforeLoad` 重定向；四个 redirect 壳逐个断行为 |
| T2 | 第二层 Skill pill 按 D-139 配方卡目录排布（输出类型轴不改） | `01 §4.1-4.5` 方案 A ＋ 分组映射 ＋ lens 联动 ＋ D-083/084 承接 | **部分覆盖** | 五类宣发任务只出 3 组（热点借势/品牌 IP 零配方，`§4.2`），空组不渲染。缺口已识别并留痕，但「按 D-139 五类排布」这条承诺首版只兑现 3/5 |
| T3 | 执行确认卡（UserDebitPreview 扩容）：只读、拒绝/确认两动作、商家语言参数说明 | `02 §1.2` 命名裁定／`§2.2` props 契约＋编译期只读门／`§3` 商家语言映射表 | **已覆盖** | 「扩容」措辞已由主控裁定作废＝净新建，`02 §1.1` 已核 |
| T4 | 记忆一级导航四域；冷启动空态与 D-126 冷态机制同构 | `04 §四` 全节（路由/导航/四域 tab/四态机/文案/31 键） | **已覆盖**（但见 L7） | `§4.4` 逐行照抄 `todayRecommendationView` 形状，复用同一 `workspaceHasWork` 判据 |
| T5 | 成品卡评价条＋动作 chip；chip 点击＝预填充不自动执行 | `03 §一`（评价条）＋`§二`（chip）＋`§2.5` prefill 链 | **部分覆盖** | 评价条第四动作「更多」只有图标与 aria-label，**全稿无菜单内容、无落点**（见漏项 M6） |
| T6 | 本次动作成本即时反馈：生成型动作结束（含被拒）就地反馈实际消耗；规划成本计入；**总余额常驻位不得作为唯一手段** | `02 §5.2-5.3` 三候选淘汰＋消息尾行；`§6.2` 规划成本裁定 | **部分覆盖** | 「总余额常驻位」在四稿中**零字提及**（grep `总余额`/`常驻余额` = 0）。实质由尾行满足，但既未确认常驻位保留、也未对照该禁令留痕 |

### 1.2 票面「验收（行为为证）」四条

| # | 承诺原文 | 落点 | 判定 | 证据 |
|---|---|---|---|---|
| A1 | 评价事件带满五字段（消费 #248 合同）；投递失败可观测、不静默（负向：断投递后可见失败计数） | `04 §3.3` 适配层／`§3.7` 验收 1-3／`§7` #248 回填 | **语义不符** | `§7.3` 裁定「发 `ObservabilityDropEvent`，**不自造计数器**」，但 `§3.3` 仍导出 `ratingEventDropCount()`／`ratingEventDropCountByReason()`，`§3.7` 验收 2 仍断言 `deliver_failed`。**同一份稿里两套互斥的可观测方案**（漏项 M4） |
| A2 | 点「拒绝」后就地出现本次规划消耗反馈，金额＝真实消耗 | `02 §5.3` 文案表第 1 行／`§6.2` 冲突裁定／`§4.3` 单位口径 | **已覆盖** | 口径已由用户拍板改判为「本次未消耗额度」＋条数单位，设计稿与裁定一致 |
| A3 | 确认卡内无任何可编辑参数控件（负向断言） | `02 §8.1` 断言 A（结构穷举 ＋ 打字不变值双条）＋`§2.2` `AssertNoEditableControls` | **已覆盖** | 结构断言 ＋ 行为断言 ＋ 编译期门三重，强于票面要求 |
| A4 | 旧双路由入口收敛（移除或重定向），三段结构上屏 | `01 §6` 九行清单／`§7.2` e2e 草案 | **已覆盖** | 含「反向证明 `/dashboard/recent` 不是第二张工作台」的负向断言 |

### 1.3 票面「边界与协调」

| # | 承诺原文 | 落点 | 判定 | 证据 |
|---|---|---|---|---|
| B1 | 三项待验证未定项按最小形态实现并留痕，形态争议交用户拍板 | `DECISIONS.md` D1-D5 ＋ `02 §7.3` 三处留痕方案 | **部分覆盖** | 用户已在票下**全部五项拍板**（D5 一条 ＋ D1/D2/D3/D4 一条），`DECISIONS.md` 仍全文 `PENDING`，02/03/04 多处仍写「建议/待拍板」（漏项 M8） |
| B2 | D-126 推荐卡合同**一字不改** | `01 §2.2` 声明「四态机 `:77` 与 D-126 内容合同一字不改」 | **语义存疑** | 同表同行给 `TodayRecommendationCard` 加 `compact`，compact 下「只渲染标题＋『用这条』，三格分区折叠」。三格分区＝D-164① 段① 热态定义的**三要素**。移动端三要素不可见（漏项 M5） |
| B3 | D-013 七类一类不动 | `02 §5.5` 不改 `BriefSurfacePhase`／`§9.4` 红线 | **已覆盖**（但与 SC1 冲突，见三） | —— |
| B4 | 自由创作线（D-103）不受影响 | **无** | **未覆盖** | 四稿 grep `D-103`／`自由创作` 均 0 命中（漏项 M3） |
| B5 | 事件合同属主＝#248（本票只消费） | `04 §3.1` 可以/绝对不能两列表 | **已覆盖** | 边界写得比票面细 |
| B6 | **确认卡超时语义与 #250 的语义层代答协调** | **无** | **未覆盖** | 02 全文 grep `超时` = 0；`staleNotice` 只处理报价过期不是超时。spec `:399-400` 的「超时＝语义层默认回答」「待确认状态持久化（重启/换设备后有效）」在四稿零落点（漏项 M2） |
| B7 | 前端 lane 与 #253 注意 locale:compile 并发纪律 | `01 §7.2` 跑法纪律／`04 §3.7` ⚠️ | **已覆盖** | —— |
| B8 | 依赖：#248 先行或同批联调 | `04 §七` 回填／`02 §9.1` X1-X7 | **已覆盖** | 主控已修订为 #248＋#262 双依赖，`04 §6.1 B4` 已记 |

### 1.4 票面评论「补充任务」（D-137）

| 承诺原文 | 落点 | 判定 | 证据 |
|---|---|---|---|
| 两页各补一句只读历史定位文案 ＋ i18n 双语键 ＋ 不改动任何行为（落点经主控修正为 `legacy_projection_canvas_job_*` 在用键组 ＋ `jobs_/$jobId`） | 四份设计稿**零落点**；`00-blockers.md 三·3` 只作为「票面事实偏差」记了落点更正；`07 Step 1` 有一步 | **部分覆盖** | 无文案定稿、无键名、无 en 译文、无验收断言。四稿分工时这一条**没有归属任何一个 agent**（漏项 M1） |

### 1.5 D-164 六条子决定

| 条 | 承诺要点 | 落点 | 判定 |
|---|---|---|---|
| ① | 单路由三段（提议位／创作面／继续上次工作）；项目列表下沉第 3 段一条、不占主屏不占路由 | `01 §2.2` 三段＋`§3.1` 澄清「项目」＝session＋`§3.2` 两行一出口 | **已覆盖** |
| ② | 输出类型轴不改；不采纳领域轴；采纳第二层 Skill pill | `01 §4` 全节 | **部分覆盖**（同 T2，3/5 组） |
| ③ | 额度预览卡扩容承载参数＋花费；不新增卡类型不合并 D-013；不归第 7 类；商家语言解释参数；**只读，卡上只有拒绝/确认** | `02 §1-§3`、`§8.1` | **已覆盖** |
| ③-附 | **不将执行确认归入第 7 类「外部行动批准」**（第 7 类只管外部发布） | `02` 全稿把确认卡建为独立组件，未挂任何 D-013 类别；`§5.5` 不改 Brief 状态机 | **已覆盖** |
| ④ | 记忆升一级导航，四域＝门店主体偏好／项目／工作流／纠正 | `04 §四` | **语义不符（稿间）** | 
| ⑤ | 评价条（纯图标、无文字标签、紧贴文案末尾、轻到可忽略）＋下方 2–3 个动作 chip，视觉层级分明 | `03 §1.4` 墨量/命中区分离表、`§1.7` 卡内四段、`§2.6` chip 视觉 | **已覆盖** |
| ⑥ | 两分（确定性编辑零阻塞／生成型走确认卡）；本次动作成本即时反馈含被拒；规划成本计入并明示 | `02 §5.4` 两分表、`§5.3` 反馈、`§6.2` ＋用户补充裁定 | **已覆盖** |

补两条挂在 ④/证据边界下、容易被当作「显然」跳过的：

| 承诺原文 | 落点 | 判定 | 证据 |
|---|---|---|---|
| ④ 关联段：D-162⑥「Skill 维护＝第八能力域」与 D-163②「维护面主职责＝**确认系统提议的沉淀**」**在此获得前台入口** | `04 §4.1` 属主表：确认动作 ❌ 等 #251，Skill 目录查询 ❌ 等 #259；`§4.3` 条目卡「不做编辑、不做删除」 | **部分覆盖（已切分）** | 入口（导航项＋页面）本票建，入口背后是只读四域。「获得前台入口」只兑现「看得见」，`propose_*→confirm_*` 的确认动作书面切给 #251，属主明确，不记漏 |
| 证据边界：本条①②④⑤ 为产品裁定、**无实现证据**；①的立论前提已被补测削弱 | `01 §8③` 风险坦白 ＋「上线后用真实商家观察首屏行为再决定是否回退」 | **已覆盖** | 02/03/04 未复述，但该风险只落在 ① 的段序上，01 承接即可 |

> ④ 判「语义不符」的原因是**稿间口径相反**，不是内容缺失：`01` 文件头第 5 行写「**④ 明确不做**——`navigation.ts:10` 四项与 `mobile-nav.tsx:55` 的 `grid-cols-4` 本票一律不动」，`01 §8 遗留观察`末条重申「本票不动这三处」；而 `04 §4.2` 正是要改这三处，主控裁决也已判「nav 四项合同→五项，裁定生效」。（漏项 M7）

### 1.6 D-164「影响」段

| 承诺原文 | 落点 | 判定 | 证据 |
|---|---|---|---|
| D-114 三层与七类卡不推翻；两处增补（③⑤） | 02/03 均按增补写 | **已覆盖** | —— |
| **引导补问卡按 D-163① 已补的三项实施**（整组可跳过／每项带「暂未确定」兜底／一次性问全） | **无** | **未覆盖** | 四稿零处。该条挂在 D-164 影响段下但实现面属 `ask_merchant`（#250，spec `:398`）——**须书面切给 #250，不能默认蒸发**（漏项 M9） |
| D-126 推荐内容合同全部一字不改 | 见 B2 | **语义存疑** | —— |
| D-139 输出类型轴不改；配方卡目录按 pill 排布 | `01 §4` | **部分覆盖** | 同 T2 |
| D-125 Landing Page 不受影响 | 四稿均不碰 | **已覆盖**（不涉） | —— |
| D-103 自由创作线不受影响 | **无** | **未覆盖** | 同 B4 |
| **实施：Dashboard 挂载归 D-127 的 NEW 桶；`TodayRecommendationCard`／`ExampleStorePreview` 按 D-126 转正口径挂载** | `01 §2.2` 处理了组件位置 | **部分覆盖** | 四稿零处提「D-127 NEW 桶」与「转正口径」（02 提 D-127 是另一件事：`§5.4`「真删默认」）。另：稿中实名为 `ExampleStoreShowcase`，与决策文档写的 `ExampleStorePreview` 不同名，未做对位说明（漏项 M10） |

### 1.7 D-164「待验证」段

| 待验证项 | 落点 | 判定 |
|---|---|---|
| 三段结构在移动端的信息密度与滚动体验 | `01 §2.3` 密度差异表（顺序不反转、只压密度） | **已覆盖** |
| 执行确认卡触发条件 | `02 §7` ＋ D2 用户拍板「零新增拦截点＋单常量开关」 | **已覆盖** |
| 记忆一级导航的内容密度／冷启动 | `04 §4.4`「未接上游用 pending 不用 cold」 | **已覆盖** |
| 动作 chip 生成方式 | `03 §2.1` ＋ D4 用户拍板「配方声明的固定集合」 | **已覆盖** |
| ⑥ 决定 B 反馈形态 | `02 §5` ＋ D3 用户拍板「消息尾行、渲染层隔离」 | **已覆盖** |
| ⑥ 决定 C 规划成本口径 | `02 §6.2` ＋ 用户补充裁定（D-109 不动） | **已覆盖** |

### 1.8 D-160③（含 07-29 补充）与 D-163②

| 承诺原文 | 落点 | 判定 | 证据 |
|---|---|---|---|
| **及格线即时信号＝「流式输出结果的最后」给出评价按钮；不做平台外归因** | `03 §1.1` 落位裁定：挂在 `ComposerDeliveryCard` 整卡 `</button>` 之后、现有动作行之前 | **语义存疑** | 交付卡 ≠ 流式输出末尾。流式面正在被 **#253FE** 改造（D lane 串行 `#261→#253FE`），本票把评价条钉在交付卡上，#253 的流式末尾可能是另一个 DOM 位置（漏项 M13）。「不做归因」一侧四稿均守住 |
| 评价条四动作＝复制／点赞／点踩／更多，纯图标无文字标签 | `03 §1.2` `DELIVERY_RATING_ORDER`／`§1.3` 四图标来源 | **部分覆盖** | 「更多」无下游（漏项 M6） |
| 事件必须携带 `skillId＋skillVersion＋场景`，与被评价产物一并落库，不得只记「哪条消息被赞/踩」 | `04 §3.2` 五字段↔三轴映射／`§3.3` payload 含 `packageId/versionId/revision` | **已覆盖** | 场景已由 #248 定为第四顶层键（`04 §7.2`） |
| **评价按钮与使用埋点必须共用同一事件合同，不得分成互不关联的两套** | `04 §3.5` 论证了「不选 C 双写」 | **部分覆盖** | 只证明了本票内不双写；**与 D-162⑤ 使用埋点是否共用同一合同**全稿未对照（漏项 M11） |
| 不得静默失败，须可观测投递成功率 | `04 §3.6` ＋ `§7.3` | **语义不符** | 同 A1，两套方案并存 |
| 去重/撤回/重复评价口径须自定 | `03 §1.6` ＋ `DECISIONS.md`「已自定项」 | **已覆盖** | 「一次点击一条事件、不做去重」，理由充分 |
| **影响 3：评价按钮的信号进 `record` 原语，成为读取侧使用遥测的第一个真实数据源** | **无** | **未覆盖** | 四稿 grep `record 原语` = 0。`04 §3.5` 选定通道 A′＝前端 `emitTelemetry`，与「进 `record` 原语」是两条不同路径，未做对位或切分说明（漏项 M12） |
| D-163② `correction` 立为一等 kind | `04 §五` 全节：判归 #251、本轮 pending 空态、禁在前端落 `'correction'` 字面量 | **已覆盖** | 三条撞点均可复现，论证扎实 |

### 1.9 spec 相关条款

| spec 位置 | 承诺 | 落点 | 判定 |
|---|---|---|---|
| `:507` 验收表「评价按钮｜主｜信号落库并进入『已验证』层」 | 落库＋进已验证层 | `04 §3.5` ⚠️ 诚实标注「#261 单独不可兑现」 | **已按裁决切分**（不算漏，见四） |
| `:400`「待确认状态持久化（重启/换设备后有效）；渲染不了确认卡的渠道挂起而非放行」 | 确认态持久化 | **无** | **未覆盖**（`ExecutionConfirmState` 是纯前端 useState，刷新即丢；漏项 M2） |
| `:49`「一进来就是工作台（单路由三段式）；确认卡只读、改动就地纠偏」 | —— | 01＋02 | **已覆盖** |
| `:172-173` 故事 80/81（确认卡一次说清、系统替我定好不用配置） | —— | `02 §2.2`＋`§3` | **已覆盖** |
| `:580` 事件合同唯一属主＝#248，下游只消费 | —— | `04 §3.1` | **已覆盖** |
| `:596/:601` D lane 串行 ＋ 前台创作面语义锁 | —— | `01 §4.1`／`04 §3.1`／`07 §一` | **已覆盖** |
| `:86` 故事 21「评价过之后下一条产出往我喜欢的方向变」 | —— | 无 | **不属本票**（归纳侧 D-160⓪／#251），但没有一稿写明这条切给谁 |

---

## 二、漏项清单（逐条展开）

**M1 · D-137 历史定位文案在四稿中无属主（部分覆盖）**
漏的是：两句文案定稿、`legacy_projection_*` 前缀下的**具体键名**、en 译文、「不改行为」的验收方式。四稿分工按 D-164①②/③⑥/④⑤ 切，D-137 落在切分缝里。`07 Step 1` 只给了改哪两个文件和一句示例。
后果：这是票面评论列的**即时交付项**，且是 D-137 唯一未执行项，最容易在关票时被漏。
应补在：新起一节或并入 `01`（同属 dashboard 路由面），给出两条键名＋zh/en 文案＋「`git diff` 零行为改动」的验收写法。

**M2 · 确认卡超时语义与状态持久化全无设计（未覆盖）**
漏的是：票面边界明列的「确认卡超时语义与 #250 的语义层代答协调」，以及 spec `:400`「待确认状态持久化（重启/换设备后有效）」「渲染不了确认卡的渠道挂起而非放行」。
后果：`02 §2.3` 的 `ExecutionConfirmState` 是组件内状态，商家刷新页面后卡消失、规划成本已花却无任何留痕——正是 D-164⑥ 要消灭的失效模式的另一个入口。且「超时自动继续」与「拒绝＝零消耗」两条语义未对齐（超时算确认还是拒绝？）。
应补在：`02` 新增一节「确认卡的生命周期边界」，至少写明本票取哪一种（建议：v1 不做持久化、不做超时，显式记为切给 #250 并在票下留痕），不能沉默。

**M3 · D-103 自由创作线零处交代（未覆盖）**
漏的是：票面边界「自由创作线（D-103）不受影响」的一句核对。
后果：段② 把整个 Composer 集群包进 `<section>`、pill 行进 PromptBar、确认卡插进提交路径——三处都可能碰到自由创作线（模型直调＋参数面板＋生成）。不核就等于赌它不在这条路径上。
应补在：`01 §2.2` 段② 表下加一行、`02 §7.2` 触发位表加一行（自由创作是否走确认卡）。

**M4 · 04 稿内部两套「投递可观测」方案并存（语义不符）**
`§7.3` 明写「上游已给出丢弃事件一等合同，本票应**消费它而不是自造**」，但 `§3.3` 的接口草案、`§3.6` 的计数器形态、`§3.7` 的验收 2 全部仍是自造计数器口径，`§7` 只在文末追加没有回改前文。
后果：实施者按 `§3.3` 写就是自造，按 `§7.3` 写就是消费上游——票面验收「断投递后可见失败计数」两种写法断言完全不同，会做出一半。
应补在：`04 §3.3/§3.6/§3.7` 就地改写为 `ObservabilityDropEvent` 口径（`{signal:'feedback', reason, count, source}`），验收 2 改为断言 drop 事件而非计数函数。

**M5 · 移动端 compact 折叠掉 D-164① 段① 的三要素（部分覆盖／语义存疑）**
`01 §2.2` 一边声明「D-126 内容合同一字不改」，一边让 compact 态「只渲染标题＋『用这条』，三格分区（`:322-361`）折叠」。三格分区承载的正是 D-164① 段① 定义的三要素（为什么今天适合发＋用了本店什么＋希望顾客做什么）。
后果：手机上商家看到的推荐卡不含三要素，等于在主力设备上把 D-126 的内容合同实质改了，却记为「一字不改」。
应补在：`01 §2.3`，或改为「compact 保留三要素但单行化」，或显式承认这是对 D-126 呈现面的形变并交用户拍板（与 `§8②` 的 D-083 形变同规格处理）。

**M6 ·「更多」按钮无下游（部分覆盖）**
`03 §1.2` 把 `'more'` 写进四动作枚举与 `DELIVERY_RATING_ORDER`，`§1.5` 给了 aria-label，但全稿无菜单内容、无落点、无 `onRate('more')` 的宿主处置；`§1.7` 只在备选里提「若读混则把现有三动作收进『更多』」（本轮不做）。
后果：渲染一个点了没东西的按钮，撞 `PRODUCT.md`「警惕无载体的想象功能」与 spec `:509`「前台承诺撤回｜断言不再渲染无下游的可点元素」——本票会自己制造一个新的空承诺。
应补在：`03 §1.2`，要么给出菜单三项（如复制链接/重新生成/反馈原因），要么首版**只出三动作**并留痕说明「更多」等后续票。

**M7 · 记忆一级导航在 01 与 04 口径相反（语义不符）**
`01` 文件头第 5 行与 `§8 遗留观察`末条声明本票不动 `navigation.ts`／`sidebar-config.ts`／`mobile-nav.tsx`；`04 §4.2` 逐个改这三处；主控裁决判定「nav 四项合同→五项，裁定生效」。
后果：合并两稿时按 01 读会把票面任务 4 整块漏掉。这正是仓内 C1/C2 事故的同形——文件面不冲突（两稿写在不同文件里），语义面互斥。
应补在：以 `04 §4.2` 为准，`01` 那两处声明作废（本轮我不改现有稿，请主控在合稿时删）。

**M8 · DECISIONS.md 五项仍全 PENDING（部分覆盖）**
用户已在票下两条评论中把 D5 与 D1/D2/D3/D4 全部拍板，G6 已过门；`DECISIONS.md` 全文仍 `PENDING`，`02 §9.2`、`03 §2.1`、`04 §6.2 S1` 仍写「待拍板/建议」。
后果：G6 判据是「全文不含 PENDING」，脚本判定与事实不符；实施者读到的是「chip 生成方式还没定」。
应补在：`DECISIONS.md` 五项翻 DECIDED 并填裁定/日期；D1 需追加用户加的一条——`image-adjust-confirmation` 的 CNY 泄漏作为既有违规**一并修正**（`02 §4.5` 已有，需标为已授权而非自行扩张）。

**M9 · 引导补问卡三项（D-163①）无落点也无切分（未覆盖）**
D-164 影响段明写「引导补问卡按 D-163① 已补的三项实施」。四稿零处。
后果：该条随 D-164 进本票范围，若不书面切给 #250 就是蒸发。
应补在：`02` 阻塞/属主表加一行「引导补问卡三项＝#250（spec `:398`），本票不实施」。

**M10 · D-127 NEW 桶挂载与组件名对位缺失（部分覆盖）**
D-164 影响段的「实施」句要求 Dashboard 挂载归 D-127 NEW 桶、两个组件按 D-126 转正口径挂载。四稿只处理了位置，未提桶归属；决策文档写 `ExampleStorePreview`，仓内实名 `ExampleStoreShowcase`（`example-store-showcase.tsx:19`），无对位说明。
后果：D-127 的桶登记是删/留的账本，漏登会让本票的新挂载不在任何桶里。
应补在：`01 §2.2` 表下一行。

**M11 · 与使用埋点共用同一事件合同未对照（部分覆盖）**
D-160③ 补充把「评价按钮与使用埋点必须共用同一事件合同」升为 D-162⑤ 遥测设计的**入口约束**。`04 §3.5` 只论证了本票内不双写。
后果：本票选 `emitTelemetry` allowlist 通道，若 D-162⑤ 的使用埋点走另一条，Miora 被点名的失效模式在本产品原样复现。
应补在：`04 §3.5`，加一行「使用埋点属 #251/#248，须共用 `observabilityAxesSchema`，本票 payload 形状即为共用凭据」。

**M12 · 评价信号「进 `record` 原语」这条路径未对位（未覆盖）**
D-160③ 影响 3 明写信号进 `record` 原语，是读取侧使用遥测的第一个真实数据源。四稿零处，选定的是前端遥测通道。
后果：两条路径都成立但服务不同承诺——`record` 那条是记忆/遥测缺件的燃料，遥测通道那条只是可观测。不写清就等于悄悄把 D-160③ 影响 3 换了实现。
应补在：`04 §3.5` 通道对照表加一列「与 D-160③ 影响 3 的关系」，明确 `record` 落点属 #256/#251，本票只做前台发射。

**M13 · 评价条钉在交付卡上，与 D-160③「流式输出结果的最后」错位（语义存疑）**
D-160③ 的原文位置是「**流式输出结果的最后**给出一个输出结果评价按钮」。`03 §1.1` 出于「禁止按钮嵌套」的正确工程理由，把它放在 `ComposerDeliveryCard` 整卡 button 之后——但那是**交付卡**，不是流式消息末尾。而流式面正是紧随本票的 **#253FE**（chunk／中文流式／错误五态）的改造对象。
后果：#253 落地后若流式输出末尾另有一个自然的评价位，本票的评价条就成了错位遗留；反之若 #253 改动交付卡的渲染时机，评价条的挂载点会被动摇。两票在 D lane 内串行相邻，现在讲清成本最低。
应补在：`03 §1.1`，加一句与 #253FE 的接缝声明——本票取交付卡位的理由、以及 #253 若引入流式末尾位时谁负责迁移。

---

## 三、过度承诺清单（票面与决策都没要求的，建议砍或降级）

| # | 设计稿承诺 | 出处 | 为什么算 creep | 处置建议 |
|---|---|---|---|---|
| **SC1** | 视频计费只读区从 Brief 迁入执行确认卡，**「Brief 里那段删掉」** | `02 §7.2` 表第 2 行、`§1.3` | 票面与 D-164③ 都没要求搬迁 Brief 内容；Brief 是 D-013 七类的安全确认位，删它一段与同稿 `§9.4`「不得改 `brief-surface`」自相矛盾 | **砍**。确认卡自己渲染计费行即可，Brief 原样保留；两处并存不是重复，是两类语义（安全 vs 花费） |
| **SC2** | `ImageAdjustConfirmation` 吸收**退役**：`git rm` 组件＋两个测试文件，改 `result-route-live-wiring.static.test.ts:47` 断言 | `02 §1.3`、`§5.4` | D-164⑥A 只要求「同一形态」，退役是推论；且它整块卡在 L4（G3 未过），会把一个可先行的改造绑成不可交付 | **降级**：本轮只做「结果页改用 `ExecutionConfirmCard`」，退役与删测独立成步、票下单独留痕 |
| **SC3** | 记忆页面加入 `check-locale-keys.ts` 的 `PRODUCT_SHELL_SOURCES`（把页面内中文全走 i18n 变 CI 强制） | `04 §4.2⑥` | 票面无此要求，是给别人加 CI 约束 | **保留但标可选**（已登记 S4），由主控口径定 |
| **SC4** | 「门店主体偏好」域双数据源（`marketing-identity` ＋ `preference_view`，同域两块不合并） | `04 §4.3` | D-164④ 只写「对应 MarketingIdentity」；多接一路是扩张（虽有 D-159② 依据） | **保留**，但在票下留痕「多接 `preference_view` 属本票判断，非决策原文」 |
| **SC5** | pill 行末尾新增「看全部做法」出口（`composer_recipe_pill_view_all`） | `01 §5.2` 末行 | `ComposerToolsStrip:66` 已有「查看全部」出口，两个出口＝两处入口 | **保留**（稿中已自标「若重复则不新增，实现时先核对」），实现时按该注核对 |
| **SC6** | 文案人称口径两稿不一：`04 §4.5` 全部第一人称「我」，`01 §5.2` 全部无人称 | 01/04 | 非 creep 但是稿间不一致，同一张 Dashboard 会说两种话 | 交 D-124 R 门⑤统一，本轮留痕即可 |

> 未判为 creep 的两条，记此免争议：`02 §2.2` 的 `AssertNoEditableControls` 编译期门（成本≈0、直接服务票面负向验收，符合「护栏须成本≈0」）；`02 §4.5` 的 CNY 泄漏修正（用户 D1 裁定已明确授权「一并修正」）。

---

## 四、验收条款可达性（设计稿到位后能否兑现）

| 条款 | 设计到位后可兑现？ | 卡在谁 | 说明 |
|---|---|---|---|
| **验收 1** 事件带满五字段 | **否（半）** | **#262**（G3b 三轴值）＋**#248**（落库目的地矩阵未裁） | 可兑现：payload 组装、过 `observabilityAxesSchema`、缺轴拒发、drop 事件发出。不可兑现：生产路径上发出一条**带真值**的事件（`04 §3.4`：main 上三轴取不到）。前提是先修 M4，否则「失败计数」这条负向断言两套写法 |
| **验收 1 后半** 投递失败可观测、不静默 | **是**（修 M4 后） | —— | 断投递 → 发 `ObservabilityDropEvent` 可断言，零上游依赖 |
| **验收 2** 拒绝后就地反馈、金额＝真实消耗 | **是（拒绝分支）／否（成功分支）** | 成功分支等 **#248 B1**（执行返回体带 `settledUnits`） | 拒绝分支＝「本次未消耗额度」，产品口径推出、零后端依赖（`02 §6.5` 降级三）；成功分支的「数字来自结算回执」断言（`02 §8.2` 断言 E）在 B1 前无法变绿 |
| **验收 3** 卡内无可编辑控件 | **是** | —— | `02 §8.1` 今天就能全绿；但组件本体所属的 L4 仍卡 G3，负向断言可先随组件落地 |
| **验收 4** 旧双路由收敛 ＋ 三段上屏 | **是** | **#264FE**（G4 串行门，非设计缺口） | `01 §6/§7.2` 断言完备；含 Day-0 两段 ＋ 热态三段两条 |
| **spec `:507`** 信号落库并进「已验证」层 | **否** | **#248**（ingest ＋ 字符串 meta 落库）＋**#251**（已验证层写入） | 主控已书面切分；关票不得以「已埋点」充数 |
| **spec `:400`** 待确认状态持久化／渠道挂起 | **否，且无设计** | 未定（建议切 **#250**） | 见漏项 M2——这条目前既不在本票设计里，也没被切出去 |
| **票面 T6** 总余额常驻位不作为唯一手段 | **是（实质）** | —— | 消息尾行独立于常驻位即满足；但需补一行留痕确认常驻位保留（M1 类文档缺口） |

**整票可达性结论**：按当前门状态（G1/G2/G5/G6 过，G3/G3b/G4 未过），四稿设计**全部到位也只能兑现验收 3、验收 4、验收 2 的拒绝分支，以及验收 1 的组装与可观测半边**。这与 `07 §一` 的 L0-L3 判断一致，关票时须原样如实报。

---

## 五、给合稿的最短修复顺序

1. **M7**（01↔04 记忆导航口径相反）——不修会整块漏掉票面任务 4，优先级最高。
2. **M4**（04 内部两套可观测方案）——不修会做出一半的验收 1。
3. **M1**（D-137 无属主）＋ **M6**（「更多」无下游）——两条都是「会被漏掉/会新造空承诺」的即时项。
3b. **M13**（评价条位置 vs #253FE 流式末尾）——同 lane 相邻两票的接缝，现在讲清成本最低。
4. **M2**（超时与持久化）＋ **M9**（引导补问卡）——要么补设计要么书面切给 #250，不能沉默。
5. **M8**（DECISIONS.md 翻 DECIDED）、**M5**（compact 与 D-126）、**M3/M10/M11/M12**（各一行核对）——文档级，合稿时一并。
6. **SC1 砍、SC2 降级**——两条都在动别人的语义面。
