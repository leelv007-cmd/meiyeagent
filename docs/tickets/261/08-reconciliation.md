# #261 合稿裁定：两份复核的 22 条逐条定死

> 依据：`05-xcheck-forward.md`（承诺→实现，13 条漏项 M1-M13）＋`06-xcheck-reverse.md`（实现→现状，8 个交叉点 C1-C8 ＋ 9 条过时判断 O1-O9）。
> **实施以本文件为准，不以 01-04 原稿为准**——原稿锚点错误率 29%（44/153），已由 `333c921a` 校准，但结论层的冲突要靠本文件裁。
> 锚点基点：`main@ab5a7c84`。

---

## 一、跨稿冲突裁定（C 组）

| # | 冲突 | 裁定 | 依据 |
|---|---|---|---|
| **C1** | 记忆升一级导航在不在本票范围：01 说「一律不动」，04 全改 | **04 胜，01 那两处声明作废**（01 头部第 5 行 ＋ §8 遗留观察末条，合稿时删） | 用户 2026-07-29 票下裁决：「**nav 四项合同→五项，裁定生效**」；D-164④（07-29）晚于且具体于 D-136（07-27），后者在此点被前者修订 |
| **C2** | `composer-home.tsx` 那 8 行注释谁改：01 说「须重写」，03 说「本票唯一允许修改的既有注释」 | **注释属主＝01**（它做布局重排、被推翻的正是那段的产品判断）。**03 删掉「唯一」那句**，只在 01 重写后的文本里追加「三处 CTA」措辞 | 两种改法落在同一个 git hunk，不裁会冲突 |
| **C3** | 三段验收断言 `getAllByRole('region')` 计数＝3 | **改用专用 testid 断 DOM 顺序**，禁用全局角色计数。三段各挂 `data-testid="dashboard-section-{proposal,create,continue}"` | main 上已另有两个带可访问名的 `<section>`＝region：`example-store-showcase.tsx`（冷态渲染，而 01 自己规定冷态段① 走示例店）与 `brief-surface-panel.tsx`（02 让确认卡与它并列）。**冷态用例按 01 自己的设计就会红**——这是两份稿里唯一一条「照现稿写必红」的缺陷 |
| **C4** | 冷态 vs pending：01 段③「空即整段不渲染」，04「未接上游用 pending 不用 cold、不因为空就藏」 | **04 的原则胜**：段③ 同样走 `workbenchHasWork()`——**有产出但无 session ＝ pending 文案（不消失）**，真 Day-0 才不渲染。01 §7.1 第 4 条断言同步改 | 用 04 自己的论据反驳 01：两个面用两套「有没有做过事」的判据，迟早出现推荐卡说 pending、记忆页说 cold 的自相矛盾 |
| **C5** | memory 的「门店主体偏好」tab ↔ main 上已存在的整页 `/dashboard/identity` | ⚠️ **升级用户拍板**（见 §四 E1）。倾向 (a)：memory 的该 tab 只出**只读摘要 + 链到** `/dashboard/identity`，不做第二个可停留工作台 | 两处并存正撞 01 §6 自己立的「第二个可长期停留的工作台入口一律移除或重定向」 |
| **C6** | 配方目录的第三个前台落点：01 收敛到 pill 行＋`/dashboard/catalog`，04 的「工作流」域又渲一遍 | **memory「工作流」域不重渲配方卡**，只出「你常用的做法」计数/名称 ＋ 链到 `/dashboard/catalog` | 同一份列表三处露面与 01 的收敛论调打架 |
| **C7** | 04 的 `SubstrateEventDeliverer` 用 `eventName: string`，`emitTelemetry` 收的是 `TelemetryEventName = keyof typeof fieldAllowlist` | **deliverer 的 `eventName` 收窄为 `TelemetryEventName`**，不用 `as` 断言绕。allowlist 加条目时类型自动放宽 | 04 未处理这处类型不匹配；用断言绕过等于把 allowlist 的编译期保护关掉 |
| **C8** | 确认卡挂点：02 说「渲染在确认卡刚才占据的挂载点」，01 的段③ 插在它与提交按钮之间 | **确认卡与其反馈尾行一并渲染进段②（创作面）尾部**，不留在 return 末尾的覆盖层 | 否则确认卡与提交动作之间隔了整个段③，02「商家的眼睛刚才就在这个位置」的论据不成立 |

---

## 二、漏项裁定（M 组）

| # | 漏项 | 裁定 |
|---|---|---|
| **M1** | D-137 历史文案四稿无属主 | 归 **`07-implementation-plan.md` Step 1**（已在册）。补在 `legacy_projection_canvas_job_*` 在用键组 ＋ `jobs_/$jobId`；`canonical_canvas_job_*` 12 条孤儿键**转 #242 尾批，本票不删**（用户裁决） |
| **M2** | 确认卡超时语义与状态持久化全无设计 | ⚠️ **升级用户拍板**（见 §四 E2）。倾向：**v1 不做持久化、不做超时**，显式切给 #250 并在票下留痕——但不得沉默 |
| **M3** | D-103 自由创作线零处交代 | **实施第一步先核**：段② 包 `<section>`、pill 行进 PromptBar、确认卡插提交路径三处是否碰自由创作线。核完在 `01 §2.2` 与 `02 §7.2` 各补一行结论。**不核就是赌** |
| **M4** | 04 稿内两套「投递可观测」并存 | **统一到 `ObservabilityDropEvent`**（`{signal:'feedback', reason, count, source}`）。`04 §3.3/§3.6/§3.7` 里自造的 `ratingEventDropCount()` 系列作废，验收 2 改为断言 drop 事件 |
| **M5** | 移动端 compact 折叠掉 D-126 推荐三要素 | ⚠️ **升级用户拍板**（见 §四 E3）。倾向：**compact 保留三要素但单行化**——折叠掉等于在主力设备上实质改了 D-126 内容合同，却记作「一字不改」 |
| **M6** | 评价条「更多」按钮无下游 | **首版只出三动作（复制/点赞/点踩），不渲染「更多」**，留痕说明等后续票。渲染一个点了没东西的按钮撞 `PRODUCT.md`「警惕无载体的想象功能」与 spec `:509`「断言不再渲染无下游的可点元素」——本票不自己制造新的空承诺 |
| **M7** | 记忆导航 01↔04 口径相反 | 同 **C1**，已裁 |
| **M8** | DECISIONS 五项仍 PENDING | **已办**（`5bc38d85`）：五项翻 DECIDED；D1 追加了用户授权的 `image-adjust-confirmation` CNY 泄漏一并修正（标为**已授权**，非本票自行扩张） |
| **M9** | 引导补问卡三项（D-163①）无落点也无切分 | **书面切给 #250**（spec `:398`），本票不实施。在 `02` 属主表补一行——不切就是蒸发 |
| **M10** | D-127 NEW 桶挂载与组件名对位缺失 | `01 §2.2` 补一行：本票新挂载登记进 D-127 **NEW 桶**；并注明决策文档写的 `ExampleStorePreview` 在代码中实名为 `ExampleStoreShowcase`（对位说明，不改名） |
| **M11** | 与使用埋点是否共用同一事件合同未对照 | `04 §3.5` 补一行：使用埋点属 #251/#248，**须共用 `observabilityAxesSchema`**，本票 payload 形状即共用凭据。不共用则 Miora 被点名的「三套遥测互不关联」在本产品原样复现 |
| **M12** | 评价信号「进 `record` 原语」路径未对位 | `04 §3.5` 通道表补一列：`record` 落点属 **#256/#251**，本票只做**前台发射**。两条路径都成立但服务不同承诺，不写清等于悄悄换了 D-160③ 影响 3 的实现 |
| **M13** | 评价条钉在交付卡上 ↔ D-160③「流式输出结果的最后」 | `03 §1.1` 补接缝声明：本票取交付卡位（因流式尚未落地），**#253FE 若引入流式末尾位，迁移责任归 #253FE**。两票 D lane 内串行相邻，现在讲清成本最低 |

---

## 三、过时判断处置（O 组）

| # | 处置 |
|---|---|
| **O1-O3** | 02/03/04 里「须用户拍板／PENDING／若用户仍拍板」的措辞全部过时（裁定方向与稿中建议**完全一致**，结论不过时）。合稿时改措辞即可，不改结论 |
| **O4** | ⚠️ **必改**：`04 §7` 的回填只在文末追加，正文 §3.3 仍留 **5 个 `TODO(#248)`** 与自造占位类型（`SubstrateEventAxes`/`SkillRevisionRef`/…）。照 §3.3 实现的人会直接抄占位类型。**§3.3 就地改为 `import type { ObservabilityAxes } from '@contracts/observability'`** |
| **O5** | ⚠️ **未结清，§7 误导**：`observability.ts` 全文只有 `observabilityAxesSchema`（四键）与 `observabilityDropEventSchema`，**没有任何 verdict 枚举**。故 03 §1.6 的撤回值问题与 04 §6.2 **S2 依然有效**。§7 须补一句点明，否则读者以为 verdict 也被上游答掉了 |
| **O6** | 04 §6.1 B1 的「`skillRevision` 全仓 0 处」已被 `observability.ts:10` 证伪，改措辞 |
| **O7** | 四稿基点标注**已办**（`333c921a` 统一为校准时的 main） |
| **O8** | `00-blockers.md` 与 `DECISIONS.md` 对 G6 互相打架——**已办**（`5bc38d85` 修了判据与状态） |
| **O9** | ⚠️ 01 §8③ 只引了「本条①②④⑤为产品裁定」，**漏引紧邻的削弱条**：决策文档写着「⚠️ 本条①的立论前提已被补测削弱：当前 `/workbench` 与 `/v2/workbench` 渲染**同一个组件**」——这直接削弱 01 全稿立论（「Miora 双路由所以我们要收敛」）。**01 §8③ 须补引**。注：D-164① 自己声明「①的裁定依据回落到 D-043 ≤2 击与 D-126 原文，该依据独立成立，结论不变」，故结论不动，但证据链必须如实 |

---

## 四、升级用户拍板（4 条）

| # | 事项 | 我的倾向 |
|---|---|---|
| **E0** | **G4 之前的提前开工窗口**（Step 1 D-137 文案 ＋ Step 5 前半导航壳，与 #264FE 改动面零交集） | 已在票下提请裁定。准了本票现在就能落真 commit |
| **E1** | **C5**：memory 的 identity tab ↔ 已存在的 `/dashboard/identity` 整页，两个可长期停留入口 | (a) memory 该 tab 只出只读摘要 ＋ 链过去 |
| **E2** | **M2**：确认卡超时语义与确认态持久化（spec `:399-400` 要求「超时＝语义层默认回答」「重启/换设备后有效」，现设计是纯前端 `useState`，刷新即丢） | v1 不做，显式切给 #250 并留痕；**但不能沉默** |
| **E3** | **M5**：移动端 compact 是否可折叠掉 D-126 推荐三要素 | 保留三要素但单行化——折叠等于在主力设备上实质改了 D-126 内容合同 |

---

## 五、实施顺序的唯一改动

`07-implementation-plan.md` 的 Step 1-7 顺序**不变**，但每步开工前须先读本文件对应裁定。新增一条**前置动作**：

**Step 0（零源码，可立即做）**：把 C1/C2/C4/C6/C7/C8 与 O4/O5/O9 的裁定就地落进 01-04 四稿，使原稿与裁定不再打架——否则实施者读到的仍是两套互斥说法。本文件是裁定台账，四稿是施工图，两者必须一致。
