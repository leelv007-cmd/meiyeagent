# 本地商家内容创作 Agent · 产品方案合集

> 版本：v1.5（P0 决策定稿）
> 日期：2026-07-07
> 定位：美业到店 + 医美/医疗资质准入制商家 · 云端 Web · 账号原生创作副驾 P0

> **历史记录与最新结论（2026-07-11，已 superseded）**：本文保留 P0 决策过程和当时的证据，不再作为当前 P1/ContentPackage/Pro Studio 实施规范。当前口径以 `CONTEXT.md`、`docs/specs/beauty-content-agent-p1-spec.md`、`docs/specs/contentpackage-productization-spec.md`、ADR-0011/0012 和最新 Vozeb 方案合集为准。后续章节中把“风险感知批量”写成创作门槛、把产品水印/AIGC 标识写成合成阶段不可关闭、或把合规检查写成普通创作 gate 的表述，均仅用于追溯，不得直接复制为当前实现约束。

> **正式退役（2026-07-17 权威版生效，本横幅为审计追加）**：自 2026-07-17 合并权威版落盘起，本文整体退役为历史决策/证据档案，任何章节不再是现行权威。现行权威：`docs/design/beauty-marketing-agent-product-design-2026-07-17.md`（D-001~D-038 合并权威版，产品权威）与 `CONTEXT.md`（一致性规则）为顶层，`docs/adr/0001~0012` 为决策记录层；执行事项中量化指标命名、硬失败清单、进场节奏以已批准的 Wave 1 执行合同与 Week 0 预登记口径为准（见合并权威版头部与 D-026）。上一段 2026-07-11 横幅所列"当前口径"为当时的中间权威层，同样已被上述权威覆盖。阅读本文须知：
> 1. §3 Scope Lock「保 8/缓 4」与「视频成片 = P0 主打」坐标系已由 ADR-0008 的 2026-07-17 修订节废止；现行范围坐标 = 五类入口发布合同 + 能力门三态（已验证/辅助完成/不可用），见权威文档第一部分 + D-023/D-024/D-026/D-027/D-028。
> 2. 修订摘要第 10–13 条中的「场景卡 + 字段表单」与「L0–L4 门槛梯度」容器由 D-031（前台无槽位填表、结构化输入融入对话流）与 D-029（Day-0 与 Day-N 同界面，五类场景入口 = Composer 上下文切换）共同取代；「文案 3 选 1」候选政策被 D-023（一个主推荐 + 按需备选）取代。
> 3. 修订 13 的 D1（五层买建边界）、D2（KickArt 定位）、D5（视频工程件）由 ADR-0008 继续承载有效（其中保8缓4坐标与标识烧录不可关闭两点分别按本须知第 1、5 条已修订）；D3（分页骨架）与 D4（工作台细节）以本文修订 13 为唯一原始记录（ADR-0008 明示不复述），仅作追溯，现行形态以 D-029/D-031/D-032 与 ADR-0010/0011 为准。
> 4. 08 章对 Mastra、组件与框架的状态判断基于 2026-07-07 依赖快照；现行口径见 D-034~D-038 与 `references/analysis/harness-research-2026-07-17/`。
> 5. 09 合规章仍是我方法定义务（17 条义务清单、行为红线、Gate 0 时点表、分层验收指标）的最完整拍板记录；其中「AIGC 标识合成阶段烧录、不可由用户关闭」（09 §3）以及前文各章把合规检查写成普通创作 gate、发布前合规预审卡 blocked 硬停的表述，已由 ADR-0008 2026-07-11 修订节及 `docs/specs/beauty-content-agent-p0-spec.md` §8/§9（创作阶段开关 + provenance 如实记录 + 法务后审）修订；其余义务条目在新的现行合规承载文档落盘前，仍按 CONTEXT.md 权威规则参照使用（去向已拍板 2026-07-18：并入 Wave 1 Week 0 预登记文档并做三处校正，见权威文档 D-039）。

---

## 0. 决策摘要页

**产品定义**：面向本地到店商家（首发美业，含资质准入的医美/医疗）的 AI 内容创作副驾——P0 基于门店档案与真实授权素材，生成小红书 / 抖音可直接发布的文案与**视频成片（主打功能之一，2026-07-08 拍板）**，用内容库、L3 发布包与手工线索台账管理"内容 → 咨询 / 到店"闭环；图文渲染管线与完整账号中心为 Go 后 / P1 扩展；合规义务内建，发布默认 L3 发布包。

**Go 建议**：**继续投入**。方向成立——红海缝隙里的一门重服务生意，楔子（本地商家单轮 + 美业首发 + 创作副驾收敛 + L3 兜底 + 先验证后建设）判断正确；竞争力来自合规服务化 + 门店数据飞轮 + 陪跑关系，而非已被平台免费工具与私域 SaaS 做掉的"跨平台内容工作台"。下一步不是全量 build，而是先跑 0-4 周真实付费验证，Go 门槛达成后再进 P0 build。

**六项已拍板决策（2026-07-07）**：

| # | 决策 | 结论 | 依据 |
|---|---|---|---|
| 1 | 医美/医疗接入 | 资质准入制·轻量版：线下筛资质入档案 + 线上 Preflight 纯提醒 + 行为红线硬停 + before/after 素材授权标记 + 陪跑边界；试点纳 1-2 家认证医美探针 | ADR-0004（refines 0003） |
| 2 | 部署与模型 | 阶段化：验证期 CF-first + 国内外模型混用 + 三轻护栏（adapter 纪律 / 顾客 PII 不进海外 API / 商家 Day-0 网络预检）；落地触发点不预设，备案 1-3 月提前量记录在案 | ADR-0005 |
| 3 | P0 架构 | 架构 A = Workers 壳 + 单 Node 服务 + 单托管 Postgres（含 Better Auth 表）；四服务图 = 演进终态 + 三拆分信号 | ADR-0006（supersedes 0002） |
| 4 | Agent 框架 | 框架 B = Vercel AI SDK 起步 + 自研 step-runner + promptfoo；Mastra 推迟、Runtime Port 预留 | ADR-0007 |
| 5 | L2 浏览器辅助 | P0 砍掉；L3 做厚为替代（扫码转手机 / 分段一键复制 / 图片存相册 / 发布 checklist，列 P0 验收）+ pilot 发布耗时探针；P1 扩展形态优先 | 评审 D4 / 03 §六 |
| 6 | 试点定价 | 挂价 Starter 199 / Growth 499 + onboarding 300-800 单收；0-4 周上付费墙；Go 门槛 = ≥3 家在 ≥399 档付定金 | 评审 D5 / 02 §9 |

**易用性增补拍板（2026-07-07 晚，四采四否）**：采纳——移动端上传入口做重、召回 = 连接桥模式、首页拟人化提醒、界面话术专业口语折中；否决防复活——产品内语音输入、发布包店员转交、口播提词器、周报转发长图。详见 §5 修订摘要第 9 条。

**交互研究三件落 P0（2026-07-08，小云雀×CreatOK 登录态实测支撑）**：①"本周内容"第一步 = 美业场景技能卡 + 领域字段表单（字段值口径已由第 11 条升级为 AI 预填可编辑默认值）②冷启动预置"示例美甲店"（档案 + 素材 + 3 内容卡 + 1 发布包，只读不计用量）③生成按钮旁标注次数消耗。详见 §5 修订摘要第 10 条。

**UI 适配九项落定（2026-07-08 晚，UI 研究四路调研支撑）**：①placeholder 升级为 AI 预填可编辑默认值 ②组件选型定稿（prompt-kit + Streamdown-cjk + AI SDK UI 路线，RSC 禁入生产）③任务四态 + 退款分层（技术失败自动退 / 不满意送免费重试上限 2 次）④合规拦截四段式 ⑤Week-1 spike 扩五题（+PWA-Serwist / Streamdown 中文流式）⑥Day-0 预检 + iOS 实机两项 ⑦中文界面字体 = 系统字体栈 ⑧undo 优先 + revert-to-AI ⑨示例内容质量 = 设计第一优先级（WoZ 优选沉淀）。详见 §5 修订摘要第 11 条。

**UI 交互二轮·生成式平台降门槛（2026-07-08，即梦/可灵/Higgsfield 登录态实测 + 字段范式，10 项回写）**：主入口从"场景卡 + 字段表单"升级为 **L0–L4 四层门槛梯度 + 三设计铁律**（L0 场景卡墙 → L1 少数语义槽位[含 **Hook 独立成槽 = 爆款开场脚本库**] → L0.5 做同款引导式向导 → L4 字段级 AI 预填；铁律 = 字段永不留空默认 Auto / 渐进展开 / Hook 独立成槽；**主入口容器已由第 13 条升级为 Agent 工作台 + 创作流，四层内核与铁律全保留**）；传图优先、品牌音建"像我"信任、按产出量卖定价、AI 标注可解释、采纳即担责留痕。降门槛核心共识 = **取消/绕过提示词**而非帮写。L-1 贴链接入口经实测降为待研究专项（§1）。详见 §5 修订摘要第 12 条。

**AI 原生骨架与分层买建 D1-D5（2026-07-08 晚拍板，KickArt/AgentKit/ad_video_gen 三源调研支撑）**：**D1** 五层买建边界——模型能力买（API）/ 编排薄自建（step-runner）/ 体验自建（=第一眼价值锚定）/ 垂类知识自建（唯一真护城河）/ 成片 = 模型端买 + 薄合成壳自建；**D2** KickArt = 范式参考 + 视频技术路线第三候选，**不订阅套壳**（旗舰 API ¥32.8 万/年数量级 + 合规控制点两笔账）；**D3** 分页骨架从"4 等权页 + 卡墙首屏"切换为 **"1 个 Agent 工作台 + 3 个轻侧栏资产页"**（对话式外壳、结构化内核；L0–L4 换容器保内核）；**D4** 场景货架收缩为 chips 一行 + 可展开，候选策略按媒介分层（文案 3 选 1 + 换一批 / 成片单发 + 免费重试）；**D5** 视频 P0 工程包（durable 异步 worker + 存储/标识烧录链 + 视频额度计价 + spike 六题）。**视频成片 = P0 主打功能之一**（用户修正拍板，视频相关 P1 项并入 P0）。详见 §5 修订摘要第 13 条 + ADR-0008。

**P0 范围**：**保 8 / 缓 4 / 砍 6**（详见 §3 Scope Lock；2026-07-08 视频成片并入后由保 7 / 缓 6 调整）——保门店档案、真实素材库、文案生成（小红书 + 抖音）、**视频成片**、内容库、L3 发布包（做厚）、合规 gate（红线不砍 + 按义务主体重构）、手工线索台账八项核心。

**工期口径**：**里程碑制，不再承诺 4-10 周全量交付**。当前里程碑 = 0-4 周付费验证（10-20 家访谈 + WOZ 陪跑 + 付费墙读数）；Go 判定达成后才进 P0 build，build 排期在 Go 后按保 8 范围另定（视频成片入 P0 后工期压力上升，用户知情拍板）。

---

## 1. 待决问题清单

以下问题仍开放，各自绑定触发条件，不在 v1.5 预定。

| 问题 | 何时决 | 谁决 |
|---|---|---|
| 落地触发点（启动备案 / ICP / 国内云迁移的时点） | 按商业化节奏，pilot Go 后视付费规模与公开上线计划定 | 用户 |
| 小红书 / 抖音医美类目商家侧发布权限真实边界 | Week-0 用真实认证商家账号实测 | pilot 执行 |
| 价格带 199 / 499 是否成立 | pilot 定金数据（≥3 家在 ≥399 档）验证后定 | 数据驱动 |
| Pro 档定价 | P1 再定 | 用户 |
| Mastra 引入时点 | 流水线出现真实复杂度（多分支 / 子流程 / 人在环矩阵）触发 | 技术触发信号 |
| 四服务拆分时点 | 三拆分信号任一触发（渲染阻塞事件循环 / Agent 发版节奏分叉 / DB 真实瓶颈） | 技术触发信号 |
| **连接识别店铺/团单信息**（贴大众点评/美团/抖音团购链接自动录入店铺档案与项目价格）的技术方案 | 专项研究，技术路径验证可行后再评估进产品，不预设时点；P0 用截图 OCR + 粘贴文本降级替代 | 待研究（技术方案） |

---

## 2. 风险登记册

| 风险 | 概率 | 影响 | 缓解 |
|---|---|---|---|
| CF 页面大陆可达性不稳（workers.dev 污染 / 自定义域 ISP 级封锁） | 高 | 高（目标客户 100% 大陆手机访问） | 商家 Day-0 网络预检 + 记录在案，把"打不开"从"不想用"信号中剥离；落地期迁国内云 |
| AIGC 备案 1-3 月周期卡公开收费上线 | 高（确定性法定前置） | 高 | 触发点到即启动 + 材料预研（清单 / 周期确认就绪）；Gate 0 语义 = 公开收费上线的前置门 |
| 素材供给第 3-4 周断供（店主懒得拍） | 高 | 高（留存第一断点） | 低摩擦采集（PWA 桌面快捷入口直达相机、拍完即传、上传自动建议标签 / 历史帖抓取 / 枯竭兜底拍摄清单）+ "素材上传周频"列北极星前置指标 |
| 线索台账没人记（手工反人性，大概率空表） | 高 | 中（承载差异化叙事） | P0 台账极简化；差异化叙事不押因果归因，改内容台账 + 人工标注洞察 |
| 医美平台权限收紧 | 中 | 中 | Week-0 真实认证账号实测边界；资质准入制前置筛选；发布默认 L3，不依赖平台 API |
| 平台原生 AI 免费挤压（抖音来客等，6-12 月窗口） | 高 | 高 | 差异化收敛到合规服务化 + 数据飞轮 + 陪跑，不押"帮你发"跨平台工作台 |
| 单人 / 小团队工期不足 | 中 | 高（视频成片入 P0 后工期压力上升） | 里程碑制 + 保 8 范围 + 成熟组件优先，不自写 framework；视频链路整段抄 ad_video_gen 范式降低自研量 |
| L2 缺席压付费意愿 | 中 | 中 | L3 做厚四件套 + pilot 发布耗时探针，P1 数据驱动再评估扩展形态 |
| pilot 样本小的幸存者偏差 | 中 | 高（商业结论失真） | 访谈扩 15-20 家 + ≥2 家真实扣款续跑 8-12 周 + 纯自助对照组留存 ≥50% |
| PWA 于 TanStack Start 无官方支持（生产构建 SW 不生成，dev 看似正常） | 中 | 中（PWA 快捷入口为已拍板易用性决策） | Week-1 Serwist POC 验证生产构建；失败则降级为"添加到主屏幕"浏览器引导 |
| 视频成片成本 / 时长 / 质量波动（15s 实测 18 分钟量级；直调边际成本未实测） | 高 | 高（P0 主打功能 + 直接冲击单位经济） | Week-1 spike 实测单条成本/时长/可用率后定额度档位；提交即走 + 任务中心 + 连接桥通知；成片单发 + 免费重试上限 2 次；候选按美学/画质/合规评估抽卡控质量 |

---

## 3. P0 Scope Lock

| 保 8（P0 必做） | 缓 4（Go 后 / P1） | 砍 6（P0 不做） |
|---|---|---|
| 门店档案（精简 + 资质档案子模块） | 图文渲染管线（先 Canva / 人工顶） | L1 官方接入（只验证边界不写码） |
| 真实素材库（上传 + 标签 + 授权状态，不做 pgvector） | 能力矩阵四表 | L2 整体（ToS 违约 + 群控 260 万判例 + 托管 cookie；凭据保险库随之消失） |
| 文案生成（小红书 + 抖音两平台） | 周报 | 内容任务收件箱 |
| **视频成片**（主打之一，2026-07-08 并入：AIDA 分镜 → 首帧 → 片段 → 薄壳合成 + AIGC 标识烧录；Seedream/Seedance 或即梦/可灵直调；脚本/分镜为流内可确认过程产物；durable 异步任务随之上提） | 完整用量账本 | Skills 版本系统 |
| 内容库（草稿 / 已发布两态） | — | Sources registry |
| L3 发布包·**做厚**（扫码转手机 / 分段一键复制 / 图片·视频存相册 / 发布 checklist） | — | Permission 三模式（P0 只留"生成自动 / 发布人工"二态） |
| 合规 gate（红线不砍 + 按义务主体重构） | — | — |
| 手工线索台账 | — | — |

**P0 Done 定义**：10-20 家商家访谈完成；≥3 家在 ≥399 档付定金；保 8 功能可真实跑通（档案 → 素材 → 文案 / 视频成片 → 内容库 → L3 发布包 → 线索台账端到端）；视频成片链路可跑通（分镜确认 → 成片 → 标识烧录 → 存相册）且单条成本 / 端到端时长实测在案；AIGC 标识注入率 100%（含导出图片 / 视频文件）；素材授权字段可用且 before/after 走授权门控；可由陪跑人工汇总下一批内容建议（不作为自动周报产品功能）。

---

## 4. 统一术语与契约表

**单一事实源 = `references/product/reports/p0-data-model-api-contract.md`（Codex 数据模型 API 契约）。** 全文各章工具名、状态枚举、发布建模一律以此为准，冲突处以契约为权威：

- **工具命名**：`domain.snake_case`。例：`copy.platform_adapt`（平台改写）/ `content.save_draft`（存草稿）/ `publish.create_package`（生成发布包）。废弃 01 的 camelCase 与 08 的动宾倒置写法。
- **状态枚举**：以契约 §4 为准（消除 `ready_for_review / review / review_required` 三套拼写；`ready_to_publish` 不在权威枚举中，禁用）。
- **发布层级**：L1 / L3 由 REST 端点建模，**不作为并列的 agent tool**（消除"三 tool vs REST 端点"分歧）。
- 本合集各章工具名已按此统一。

---

## 5. v1.5 修订摘要

v1.4 → v1.5 变更：

1. **医美口径**：从 Regulated Content Mode 泛化合规边界改为**资质准入制·轻量版**（线下筛选前置 + 线上纯提醒 + before/after 授权标记 + 陪跑边界，ADR-0004 refines 0003）。
2. **部署与模型**：新增**阶段化策略**（验证期 CF-first + 混用 + 三轻护栏；落地触发点启动备案 / ICP / 国内云迁移，ADR-0005）。
3. **架构**：从"mkfast 仅 App Shell + 四独立服务"改为**架构 A = Workers 壳 + 单 Node 服务 + 单托管 Postgres**（Better Auth 入 PG，双库身份分裂消除；四服务图降为演进终态 + 三拆分信号，ADR-0006 supersede 0002）。
4. **Agent 框架**：从"Mastra 独立 Agent Service"改为**AI SDK 起步 + 自研 step-runner + promptfoo**，Mastra 推迟、Runtime Port 预留（ADR-0007）。
5. **发布**：L2 灰度整体砍掉，**L3 发布包做厚**为正式替代（四件套列 P0 验收）+ pilot 发布耗时探针。
6. **商业**：试点**挂价 199 / 499 + onboarding 300-800**，0-4 周付费墙，Go 门槛 = ≥3 家在 ≥399 档付定金；商业验证升级为 15-20 家 / 8-12 周 / 纯自助对照组留存 ≥50%。
7. **新增 09 合规与资质架构章**：按义务三主体重构（我方生成式 AI 服务提供者义务 + 受托处理者 / 潜在广告经营者义务 + 商家广告主义务；17 条义务清单 + 硬拦截清单 + Gate 0 + 分层验收指标）。
8. **悬空引用修复**：`analysis 13 / 14 / 15` + `adr/0003` 等从旧工作区 `美业内容/` 迁入本工作区，相对路径修正；被本轮推翻 / 修正的结论（13 无中国部署评估、09 海外模型优先）挂批注防复活。
9. **易用性增补（2026-07-07 晚拍板，四采四否）**：①移动端上传入口做重（05 §3 / 03 §2.2：PWA 桌面快捷 + 直达相机拍完即传 + 上传自动建议标签）；②召回通道定形为**连接桥**（05 §4：NotificationBridge port，外接商家在用的飞书/企微等工具——P0 最小 = 群机器人 webhook 挂"本周内容就绪 / 素材补给 / 人工复盘提醒"三钩子，P1 经 MCP 通用化；WoZ 期 = 陪跑群人肉推送 SOP + 24h 打开率探针；个人微信无官方接口由陪跑兜底。不新增保 7 条目，Scope Lock 计数不变）；③首页提醒 = **拟人化一句话**（后台关键字提炼 + 低成本快速模型，明确不做传统 SaaS 待办卡片）；④界面话术 = **专业运营语言与口语的折中**（不暴露技术概念，也不过度口语化——纯口语化损 AI 专业价值感）。**否决记录（防复活）**：产品内语音输入（讯飞/微信键盘等成熟输入法已解决，Web 文本框天然继承，不重复造轮子）、发布包店员转交链接（不过早做团队协作、不穷尽用户场景）、口播提词器模式（低频冗余）、周报可转发长图（无独立 App 载体，Web 形态下不构成分享/拉新渠道）。
10. **交互研究三件落 P0（2026-07-08 拍板）**：依据小云雀(剪映)×CreatOK 登录态实测的交互共性（报告 `references/benchmark/interaction-study-2026-07-07/interaction-patterns-xyq-creatok.md`）——①"本周内容"第一步定形为**美业场景技能卡 + 领域字段表单**（场景卡：新客拓客/节日活动/项目种草/口碑晒单/门店日常；字段用美业行话 + 真实示例值 + "自动匹配"兜底——示例值口径已由第 11 条升级为 AI 预填可编辑默认值；brief 编译对用户不可见、不设"编辑提示词"入口，03 §3.2；**本条"场景卡 + 字段表单"主入口形态已由第 12 条升级为 L0–L4 四层门槛梯度**）；②冷启动**预置"示例美甲店"**（完整档案 + 已授权示例素材 + 3 张内容卡 + 1 个发布包，只读、不计用量、可一键隐藏，03 §3.1）；③**用量透明原则**：扣额度动作在按钮旁标注本次消耗、余量常驻可见（05 §3）。研究同时确认 Agent 角色定位：指引（周计划 + brief 代写，两竞品共同空白）为主、替代（prompt 工程/选题/合规初审）+ 加速（改稿 chips/平台变体/自动打标）为辅——佐证"本周内容"结构化入口第一、副驾对话第二的排序与 ADR-0007 轻框架选型（**该排序表述已由第 13 条改写为"对话式外壳、结构化内核"**）；实测真视频 15s 生成约 18 分钟 → 视频等待交互必须"提交即走 + 连接桥通知"，生成成本进单位经济（**视频已由第 13 条从 P1 并入 P0 主打**）。
11. **UI 适配九项落定（2026-07-08 晚拍板）**：依据 UI 适配四路调研（合成报告 `references/benchmark/ui-adaptation-study-2026-07-08/00-合成-UI适配与组件选型.md` + 4 分报告 + 20 份一手快照；用户定调"架构对内、体验对外，UI/UX = 第一眼价值锚定 = 核心竞争力之一"）——①**placeholder 升级为 AI 预填可编辑默认值 + 常驻说明**（修正第 10 条细节；NN/g：会消失的占位符有害；带 AI 标记 + 一键回到 AI 版本，03 §3.2）；②**组件选型定稿**（05 §3：Base UI 版 shadcn 4.0 底座、prompt-kit + Streamdown/cjk + AI Elements 按需、AI SDK UI 路线 RSC 禁入生产、copy-in 单件纪律、Magic UI/Aceternity 限量点睛、避坑清单）；③**任务四态 + 退款分层**（03 §3.2 + 05 §4 Billing：技术失败自动退留痕 / 成功不满意不退送免费重试上限 2 次）；④**合规拦截四段式文案**（09 §5：不指责 → 原因 → 给替换 → 归还控制权）；⑤**Week-1 spike 三题扩五题**（04 §5：+PWA-Serwist 生产构建、Streamdown 中文流式）；⑥**Day-0 预检 + iOS 实机两项**（02 §3.1：相机选择层、存相册双保险）；⑦**中文界面字体 = 系统字体栈**（P0 不自托管，05 §3）；⑧**undo 优先 + revert-to-AI**（03 §3.2）；⑨**示例内容质量 = 设计第一优先级**（03 §3.1：第一眼五杠杆之首、文案即产品本体，样例来源 = WoZ 优选产出沉淀）。风险登记册同步新增 PWA 行。
12. **UI 交互二轮·生成式平台降门槛（2026-07-08 拍板"回写"）**：依据即梦/可灵/Higgsfield 登录态实测 + 字段级范式（合成 `references/benchmark/ui-adaptation-study-2026-07-08/01-合成-生成式平台降门槛与主入口调和.md` + 4 findings + 快照），10 项落定——① **主入口从"场景卡+字段表单"两层 → L0–L4 四层门槛梯度 + 三设计铁律**（03 §3.2：L0 场景卡墙[按业务目的分类 + 自带"该传什么图"引导] → L1 少数语义槽位[内容类型 × **Hook 钩子独立成槽 = 爆款开场脚本库** × 场景，选中即隐藏提示词框] → L0.5 做同款引导式向导[预置 + 逐槽确认] → L4 字段级 AI 预填可编辑[revert-to-AI，价格字段隔离不预填]；铁律 = 字段永不留空默认 Auto / 渐进展开 / Hook 独立成槽；**主入口容器已由第 13 条升级为 Agent 工作台 + 创作流，四层内核与铁律全保留**）；② AI 预填细化 + 价格字段隔离 + 采纳担责留痕（03/09）；③ 传图/拍照优先打字（03/05）；④ 参数视觉 chip + 收益语 + 自动默认、场景卡按业务目的分类（03/05）；⑤ **品牌音**（学门店旧文案 + 带/不带对照建信任，03 §3.1）；⑥ 做同款病毒回路（P0 本店 / P1 同行 feed）；⑦ 定价按产出量卖、免费档卡商用（05 §1）；⑧ AI 标注 UI 形态 = Carbon AI label 可解释 + "内容由 AI 生成"（09 §3）；⑨ 选预设即隐藏提示词框 + 卡片自带传图引导（03 §3.2）；⑩ **L-1 贴链接零表单入口经实测降为待研究专项**（§1 待决清单；服务端裸抓中国平台反爬不可靠——点评 302 + JS 渲染 + 字体加密 + 无 API，P0 用截图 OCR + 粘贴替代）。降门槛核心共识 = **取消/绕过提示词**而非帮写；核心张力（即梦"纯对话藏字段" vs 字段范式"预填表单"）裁决 = 我们是经营内容须结构化骨架、故四层软化不纯对话。
13. **AI 原生骨架 + 分层买建 + 视频成片入 P0（2026-07-08 晚拍板"D1-D5 全落"）**：依据 KickArt 登录态实探+官方文档 / AgentKit 控制台实探 / `bytedance/agentkit-samples` ad_video_gen 源码三变体深拆（合成 `references/benchmark/ai-native-journey-study-2026-07-08/02-合成-AI原生旅程与排布重构.md` + `03-合成-架构与产品化取舍-KickArt双路评估.md` + 3 findings + 源码留档；触发 = 用户判 W0-W8 线稿"卡片痕迹/传统 SaaS 痕迹明显，不 AI 原生"，并升维到结构取舍）——**D1 五层买建边界**：模型能力买 API / 编排薄自建（抄 ad_video_gen 三件套范式：session.state 黑板 + CallBackAgent 人话进度 + 评估抽卡 N→1，只抄范式不抄 Python 栈）/ 体验自建 / 垂类知识自建 / 成片 = 模型端买 + 薄合成壳自建。**D2 KickArt 定位**：范式参考 + 视频技术路线第三候选（Seedance 直调 → 即梦/可灵 API → KickArt API），不订阅套壳——license 数量级（旗舰 API ¥32.8 万/年 ≈ 46 家 Growth 满配才 cover）+ 合规控制点（标识烧录/审计链必须自控）两笔账；其同厂演进（智能创作云多页 SaaS → KickArt Agent 工作台）为骨架切换最强旁证；企业级定价（基础版 ¥16800/月）证明其页面范式可借鉴、商业模式不可照搬。**D3 分页骨架切换**：从"4 等权导航页 + 卡墙首屏（人找功能）"切换为 **"1 个 Agent 工作台 + 3 个轻侧栏资产页（agent 找人）"**（02 §1 / 00 §3.2 / 04 §3 已重写）；工作台三段 = Agent 开场拟人化问候+今日建议 chips / 中央意图框三喂料同框（打字·拍照传图·粘贴文本，KickArt 范式）/ 创作流对话式时间线（brief 确认卡 → 人话进度旁白 → 候选卡 → 合规预审卡 → 产物卡，全为流中卡片，"流里跑、库里存"）；两处轻确认（brief 卡+候选挑选）= 对官方样例"零确认黑盒"（ad_video_gen prompt.py:35-37 实证）的差异化补位；两条 supersede——"结构化入口第一、副驾对话第二"改写为**"对话式外壳、结构化内核"**（价格/平台/合规硬事实仍由结构化卡片守住）、副驾不设独立浮层（工作台即副驾）；L0–L4 门槛梯度**换容器保内核**（卡墙→场景 chips+可展开货架；L1 槽位+L0.5 逐槽确认+L4 字段预填→合并进 brief 确认卡；三铁律/Hook 脚本库/价格不预填/渐进展开全保留）。**D4 工作台细节**：货架 = chips 一行横滑 + "全部场景▾"展开；候选策略按媒介分层 = 文案/创意一次 3 条 + 换一批（消耗按 1 次计）、视频分镜确认 → 成片单发 + 免费重试（退款分层规则不变）。**D5 视频成片 = P0 主打功能之一（用户修正拍板，视频相关 P1 项并入 P0）**：链路 = AIDA 四段分镜（流内可确认）→ 首帧 → 片段（Seedream/Seedance 或即梦/可灵直调，进 benchmark 可切换）→ 薄合成壳（ffmpeg 拼接 + BGM/字幕 + **AIGC 显式标识烧录+隐式 metadata，合成阶段完成不可后置**）→ 存储 → 存相册/发布包；三工程件上提 = durable 异步任务（原缓做）+ 存储/转码/标识链 + 视频额度计价（按条进"够发 X 条短视频"口径，单条成本 spike 实测后定档）；Week-1 spike 五题扩六题（+视频链路 POC：单条成本/端到端时长/质量可用率/标识烧录四指标）；Scope Lock 保 7/缓 6 → **保 8/缓 4**；质量 = 候选按美学/画质/合规评估抽卡（"一致性"维度替换为医美违禁词过滤）；KickArt 双合规门范式吸收 = 素材提交内嵌确权勾选 + 发布前预审卡。剥离不抄：电商基因（商品 URL 抓取/100 条批量裂变/逐镜专业编辑默认收起）、Python/VeADK 栈、企业级定价。架构 A / 框架 B / 验证期 CF+混用模型不动；视频合成 worker = 架构 A"三拆分信号"首要监测对象；视频流水线 = step-runner 第一条重流水线（Mastra 触发条件按 ADR-0007 原铁律检验）；火山生态记为中国化落地期云候选之一。决策记录 ADR-0008。

**本轮同步依据**：

- `references/analysis/plan-review-2026-07-07/00~05`（综合评审报告 + 五角度分报告）
- `docs/adr/0004~0007`（本轮四决策）
- 迁入的 `references/analysis/00~16` 系列 + `docs/adr/0001~0003`（其中 0002 已 superseded by 0006）
- `references/creatok/reports/creatok-architecture-estimate.md`（同行架构证据：零 agent 框架 + 单部署单元）
- `references/product/reports/p0-data-model-api-contract.md`（术语与契约单一事实源）
- `references/benchmark/ai-native-journey-study-2026-07-08/`（第 13 条依据：02/03 合成 + kickart/agentkit-console/adgen-source 三 findings + 一手快照与 agentkit-samples 源码留档）
- `docs/adr/0008-video-in-p0-and-layered-buy-build.md`（D1/D2/D5 决策记录）

---

## 6. 文档目录

> **前置决策层（本文 §0–§5）优先于正文各章；正文与前置冲突时，以前置为准。**

| 编号 | 文档 | 内容 |
|---|---|---|
| 00 | 产品蓝图 | 产品定位、目标用户、P0/P1/P2 范围、核心闭环 |
| 01 | Agent 架构设计 | 门店 Operating Agent、记忆系统、工具目录、编排逻辑 |
| 02 | 功能规划 | 文案、图片、视频、账号、内容、线索、合规模块规划 |
| 03 | 分发引擎 | L1/L3 发布策略、账号中心、平台能力矩阵 |
| 04 | 技术架构与选型 | 云端 Web 架构、阶段化部署、数据模型、模型路由 |
| 05 | 商业模式与路线图 | 套餐、单位经济、GTM、阶段里程碑 |
| 06 | 核验与修订记录 | 原方案保留 / 修正 / 反驳项、待验证问题 |
| 07 | 美业到店 P0 功能设计 | 首发垂类落地方案、页面、场景包、验收标准 |
| 08 | 开源项目与框架借鉴分析 | Mastra、mkfast-template/TanStarter、Craft Agents 的可复用能力、改造边界与自研清单 |
| 09 | 合规与资质架构 | 义务三主体重构、17 条义务清单、硬拦截清单、Gate 0、分层验收指标 |

---

## 附：v1.4 修订摘要（历史）

**v1.4（2026-07-06）** 把泛化远景收敛为单一可验证产品，仍有效的核心：首发本地商家单轮 + 美业到店（覆盖医美 / 医疗）；头牌能力 = 账号原生创作副驾（自主运营 Agent 降 P1/P2）；P0 闭环 = 档案 → 素材 → 生产 → 内容库 → 账号 → 发布 → 线索 → 下一轮建议；发布默认 L3；归因叙事降级为内容台账 + 线索看板 + 人工标注；调研与试点先行。

**已被 v1.5 修订的**：医美 Regulated Content Mode 泛化合规边界 → 资质准入制·轻量版（ADR-0004）；mkfast 仅 App Shell + 四独立服务边界 → 架构 A 单 Node 服务（ADR-0006 supersede 0002）；Mastra 独立 Agent Service → AI SDK 起步 + Runtime Port 预留（ADR-0007）；L2 no-submit 灰度 → 整体砍掉 + L3 做厚；第一批模型 OpenAI/Gemini/DeepSeek → 阶段化混用（ADR-0005）；WOZ 3-5 家 / 4 周 → 15-20 家 / 8-12 周 + 定金门槛。

---
# 00 · 产品蓝图：本地商家内容创作 Agent

## 0. 一页纸速览

**产品暂名**：门店内容副驾

**一句话定义**：

> 给本地到店商家的 AI 内容创作副驾。P0 基于门店档案、真实素材、项目价目、优惠活动和账号人设，生成小红书与抖音可用文案与**视频成片**，用内容库、做厚的 L3 发布包、合规 gate 和手工线索台账跑通从内容到咨询/到店的最小闭环；点评、公众号、图文渲染、周报和完整账号中心均为 Go 后 / P1 扩展。

**首发对象**：

- 本地到店商家，优先美业到店；覆盖美甲、美发、美睫、SPA、生活美容，以及有正常内容生产需求的医美/医疗、注射、激光、手术、药械相关商家（实行资质准入制：线下接洽核验执业许可与平台认证后开通，见 ADR-0004 与 09 章）。
- 操作人通常是老板、前台或店员，不是专业运营。
- 目标不是“写得更像 AI”，而是“少花时间持续发布真实、有转化意图的平台原生内容”。

**P0 必须跑通的链路**：

```
门店档案
  → 真实素材库
  → 创作副驾生成小红书/抖音文案
  → 内容库管理草稿与已发布内容
  → Publish Compliance Preflight（医美/医疗资质准入轻量版提醒）
  → L3 发布包交接（做厚：扫码转手机/分段一键复制/图片存相册/发布 checklist）；L1 仅账号级 verified 后启用；L2 移出 P0（ADR 决策）
  → 线索台账记录咨询/加微/预约/核销
  → 人工汇总反馈下一批内容建议
```

**P0 必做能力**：

| 能力 | P0 交付 |
|---|---|
| 文案 | 小红书笔记、抖音口播稿、标题/话题/私信引导 |
| 素材 | 真实素材上传、标签、授权状态；before/after 只作为授权素材使用，不做医美预置营销模板 |
| 内容管理 | 草稿、已发布、基础平台变体、合规状态 |
| 发布 | 做厚 L3 发布包；L1 只做账号级 verified 后的边界验证；L2 不进 P0 |
| 线索管理 | 手工记录咨询、加微、预约、团购券领取/核销、内容关联 |
| 合规 | AIGC 标识、广告法敏感词、医美/医疗资质准入轻量版 Preflight、价格/优惠/资质/素材授权校验 |

**P0 不做**：

- 不做全自动无人运营。
- 不做六平台全自动发布承诺。
- 不做复杂因果归因。
- 不做完整 SCRM、POS、收银、会员系统。
- 不做 L2 浏览器辅助发布与任何平台凭据托管（P1 数据驱动再评估，扩展形态优先）。
- 不做完整 Permission 多模式（P0 只保留“生成自动/发布人工”二态）。

---

## 1. 背景与项目判断

本地商家现在面对的是“内容经营碎片化”问题：拍摄在手机里，文案在备忘录里，账号分散在多个平台，团购/预约在另外的后台，发布后的咨询和核销又靠人工记忆。泛 AI 写作工具只能解决“写一段话”，不能解决“这家店本周该发什么、用哪张图、发哪个平台、发完有没有来咨询”。

因此本项目不应被定义为“AI 写作工具”，而应定义为“本地商家的内容运营副驾”。它的关键价值不是单条文案质量，而是把门店素材、平台内容、账号人设、发布状态、线索反馈组织成一套持续运营系统。

### 1.1 为什么先做本地商家，而不是泛内容创作者

原方案保留“自媒体博主 + 本地商家双轮”的远景是合理的，但首发不应双轮并进。原因：

- 两类用户的 GTM、渠道、付费理由、成功指标不同。
- 本地商家的 ROI 路径更短，内容可直接导向咨询、加微、团购券和到店。
- 本地商家的运营水平普遍较低，更需要“给成品”的副驾，而不是“空白聊天框”。
- 单垂类可以沉淀模板、素材标签、合规规则和线索口径，便于产品早期快速迭代。

### 1.2 为什么先做美业到店

美业到店适合作为 P0 样板：

| 判断维度 | 美业到店特点 |
|---|---|
| 内容素材 | 案例图、环境图、before/after、手法视频天然丰富 |
| 平台适配 | 小红书种草、抖音同城、大众点评决策链路清晰 |
| 转化动作 | 私信问价、加微、预约、团购券核销容易被人工登记 |
| 运营痛点 | 老板/店员常拍了图但不会写、不会持续发 |
| 合规边界 | 生活美容低风险先跑；医美/医疗采用资质准入轻量版，发布前展示资质、广告审查、平台规则、AIGC 标识和素材授权提醒 |

### 1.3 竞品判断

本产品真正的竞品不是单一的“讯飞绘文”，而是四类：

| 竞品类型 | 代表 | 我方不应硬碰 | 我方机会 |
|---|---|---|---|
| 平台原生 AI | 剪映、即创、平台后台创作能力 | 不比单平台生成能力 | 做跨平台内容资产、门店知识库、线索台账 |
| 通用 AI 写作 | 豆包、Kimi、秘塔等 | 不比“写一篇更便宜” | 做门店专属语气、真实素材调用、场景模板 |
| 商家 SaaS/代运营 | 有赞、微盟、美团/抖音本地生活服务商 | 不直接承诺 GMV | 做内容生产 + 发布包 + 轻线索台账 |
| RPA/群控/逆向工具 | 私有 API、代理池、签名逆向 | 不碰逆向和对抗式自动化 | 做受控发布、官方能力优先、合规留痕 |

差异化应落在：**跨平台内容工作台 + 门店私有知识库 + 真实素材组织 + 合规创作 + 内容到线索台账**。

竞品完整图谱应为六分类：除上表四类外，补第五类**本地商家垂类 AI 内容工具**（番薯侠、我赢 AI 等）与第六类**私域 SaaS 的 AI 内容层**（微盟 WAI、有赞智能助手），此二类为贴身竞争对手。据此差异化叙事收缩为三点：**合规服务化 + 门店数据飞轮（真实素材授权链）+ 陪跑产品化**；“跨平台工作台/生成质量”不作为主卖点（详见 references/analysis/plan-review-2026-07-07/01）。

---

## 2. 产品原则

### 2.1 门店知识优先

生成内容之前，系统必须先理解这家店：商圈、项目、价目、人设、真实案例、禁忌话术、主推活动。没有门店知识的 AI 写作只是泛模板。

### 2.2 真实素材优先

图片、案例、环境、好评和手法视频优先来自门店真实素材。AI 图适合做封面背景、氛围元素和图文卡片，不应伪装成真实顾客效果。

### 2.3 平台原生，不同稿多发

同一内容内核要生成平台变体：

- 小红书：种草标题、正文、话题、本地定位、图片顺序。
- 抖音：口播脚本、短标题、封面文案、同城标签。
- 大众点评：项目介绍、套餐卖点、价格说明、到店提示。
- 公众号/视频号：私域承接、活动通知、图文排版。

### 2.4 受控发布，不承诺隐形全自动

高风险动作必须可预览、可确认、可审计、可回退。平台官方能力优先；没有官方能力的平台，L3 发布包交接是 P0 常态下限，L2 浏览器辅助移出 P0（P1 数据驱动再评估）。



---

## 3. 核心工作流

### 3.1 冷启动

目标：新店 5 分钟内拿到第一条可发内容。

新工作区预置一个**"示例美甲店"**：完整门店档案 + 已授权示例素材 + 3 张内容卡 + 1 个发布包。用户先看到终态再替换成自己的，比从空表单开始快得多（对标小云雀短剧 Agent 预置示例项目、CreatOK"做同款"）。示例数据只读、不计用量，用户建好自己门店后可一键隐藏。

**示例内容质量 = 设计第一优先级**：3 张内容卡与各场景卡的 AI 预填值须人工打磨到"像头部美业操盘手写的、能直接发"的水准——第一眼五杠杆之首，文案即产品本体（Google 17-50ms 第一印象 / 美学-可用性效应 / Fogg design-quality 三条一手证据，见 UI 研究）。样例来源 = WoZ 陪跑期优选产出沉淀，零额外成本且自带真实感。

输入方式：

- 店名、城市/商圈、门店类型。
- 项目与价目，可手填、表格导入或从公开主页辅助整理。
- 1–2 条历史内容或历史图片。
- 账号语气选择：温柔专业、活泼年轻、高端轻奢、老板娘口吻等。
- **品牌音（学门店旧文案建"像我"信任）**：可上传门店几条已发真实文案，AI 学出门店口吻；生成时给"带门店口吻 vs 通用版"两版对照，让商家确认"这是我的调性"（Jasper Brand Voice 范式），解掉非技术商家"AI 写的不像我、太假"的核心抵触。接账号人设字段；最小实现的有效性随 Week-1 AI SDK spike 顺带验证。

系统输出：

- 门店档案初稿。
- 项目标签和素材标签。
- 3 条首发内容候选。
- 拍摄清单。

### 3.2 本周内容生产

主入口 = **Agent 工作台**（2026-07-08 D3 拍板，AI 原生骨架；三源调研合成见 `references/benchmark/ai-native-journey-study-2026-07-08/02-合成-AI原生旅程与排布重构.md`）。分页骨架从"4 等权导航页 + 卡墙首屏"切换为 **"1 个 Agent 工作台 + 3 个轻侧栏资产页"**（信息架构见 02 §1）；发起方从"人找功能"换成"**agent 找人**"；"结构化入口第一、副驾对话第二"改写为 **"对话式外壳、结构化内核"**——价格 / 平台 / 合规等经营硬事实仍由结构化卡片守住，不因对话式而丢。

**工作台三段**：

1. **Agent 开场**：拟人化问候 + 今日建议 chips（2-3 条，点一下即预填意图开跑；后台关键字提炼 + 低成本快速模型；替代"人逛卡墙"）。
2. **中央意图框**："说说你想发什么"——**三喂料同框**：打字（可选）/ 拍照传图（优先）/ 粘贴文本（价目表 / 旧文案）（KickArt 范式，呼应上传入口做重）。下方**场景 chips 一行横滑**（引流 / 种草 / 促销 / 复购，点选即预填；"全部场景 ▾"才展开完整货架）。
3. **创作流**（对话式时间线）：过程与产物全部是流中卡片，历史向上翻即创作历史；右下任务浮标 = 异步任务中心（"流里跑、库里存"，产物沉淀内容库）。

**创作流五步**（原 L0–L4 门槛梯度**换容器保内核**，叠加 ad_video_gen / KickArt 实证范式）：

```
意图（点建议 chip / 场景 chip / 说一句话 / 传图）
  → brief 确认卡 ＝ 轻确认①（结构化槽位 AI 预填：主推项目 × 内容类型 × Hook × 场景/平台；
    逐槽 picker 可改；价格不预填）
  → 生成推进（人话进度旁白："正在看你传的图…正在写开场钩子…"，禁百分比；
    文案秒级流出，视频分钟级提交即走）
  → 候选卡 ＝ 轻确认②（文案/创意一次 3 条 + 换一批，消耗按 1 次计；
    视频走 分镜确认 → 成片单发 + 免费重试）
  → 合规预审卡（四段式；医美红线场景采纳即担责留痕；blocked 硬停）
  → 产物卡（采用可撤回 / 改稿 chips / 做同款 / 存内容库 / 去发布包）
```

两处轻确认（brief 卡 + 候选挑选）是对官方样例"零确认黑盒"（ad_video_gen 明文"展示后继续不等确认"）的**差异化补位**：过程透明 + 改而非重填，给非技术老板娘掌控感。

**L0–L4 内核到新容器的映射**（内核与铁律全保留，梯度出处见修订摘要第 12 条）：

- 原 L0 场景卡墙（首屏主体）→ **场景 chips + 可展开货架**：每卡仍带成品预览缩略图 + 一句话"它替你做什么" + 使用量背书 + **自带"该传什么图"引导文案**（可灵范式）；按业务目的分类（引流/种草/促销/复购），不按技术能力。
- 原 L1 少数语义槽位 + L0.5 做同款逐槽确认 + L4 字段级 AI 预填 → 合并进 **brief 确认卡**：★ **Hook 独立成槽 = 预写"爆款开场脚本库"**（前 3 秒决定被看还是被划走，老板娘最写不出、最该产品化的一层）；每维可视化 picker（看成品挑 > 读文字）+ 大白话描述 + 客群语言分类；字段进入即带 **AI 预填的可编辑真实默认值**，带 inline AI 小标（点开可解释）、人工修改即接管、可一键 revert-to-AI；示例说明放标签下方常驻，不用会消失的 placeholder（NN/g）；**价格机制字段不 AI 预填、不上 AI 样式**（价格是事实/红线，Carbon 局部 presence 隔离）；医美卖点字段红线校验。做同款 = 预置该内容槽位 + 逐槽确认（引导式向导，非静默填满；P0 本店内容做同款，跨店同行 feed 为 P1 缓做）。
- "选中预设即隐藏提示词框"（可灵/Higgsfield 实测）→ **意图框不是提示词框**（说人话 + 喂料即可）；表单/对话编译成生成 brief 对用户不可见，不设"编辑提示词"入口（我们客群比剪映用户更小白）。
- **三条设计铁律不变**：① 字段永不留空、默认 = Auto/智能预填；② 渐进式展开（专业参数默认收起，想深控才点开）；③ Hook 独立成槽。
- 参数一律视觉 chip 点选 + 收益语（"人像更真/更省"，不暴露模型版本号）+ 自动默认；生成按钮旁标注本次消耗次数（用量透明，见 05 §3）。

> **L-1 贴链接零表单入口**（贴点评/团购链接自动录入店铺/项目）经 2026-07-08 实测降级为**待研究专项**（见 §1 待决清单：服务端裸抓中国平台反爬不可靠）；P0 用冷启动截图 OCR（§3.1）+ 粘贴文本降级替代。

生成任务的呈现与计费规则（2026-07-08 UI 研究定稿）：

- **任务四态**：排队中（显示位次）→ 生成中（白话阶段叙事："正在读你的门店信息 / 正在写初稿 / 正在配标题"，禁用假百分比）→ 完成（显著角标 + 连接桥通知）→ 失败（一键重试）。提交即走、可离开可恢复，创作流时间线为回归入口。**视频成片为分钟级长任务（15s 实测 18 分钟量级），"提交即走 + 任务中心 + 连接桥通知"为生存必需，完成后产物卡回流时间线。**
- **退款分层**：技术失败**自动返还次数并留流水**；生成成功但不满意**不退**，改送免费重试（每条上限 2 次，挂价页小字注明）。
- **undo 优先**：采用/发布默认可撤销（采用后短时间内可撤回），事前确认框仅保留"不可逆 + 高监管后果"动作，防止确认框滥用失效。

每张内容卡包含：

- 选题。
- 推荐平台。
- 标题。
- 正文。
- 话题标签。
- 封面文案。
- 图片/图文建议。
- 视频分镜与成片（P0 主打；口播脚本 / 分镜为流内可确认过程产物）。
- 转化钩子：问价、预约、加微、领券、到店。
- 合规状态。
- 一键操作：改一版、换语气、生成平台变体、存草稿、去发布。

### 3.3 发布与交接

发布不是单一“发布按钮”，而是发布任务中心：

- 能官方 API 发的，走 L1。
- 可用官方分享/发布链路的，优先验证官方能力。
- 无稳定官方能力的，生成完整发布包（做厚），由人复制上传确认；L2 移出 P0，P1 再评估。
- 所有平台都保留 L3 草稿交接。

### 3.4 线索回流

发布后每条内容都可以挂线索：

- 私信咨询数。
- 评论问价数。
- 加微数。
- 预约数。
- 团购券领取/核销数。
- 到店备注。
- 人工标注“这条为什么有效”。

周报示例：

> 上周“渐变猫眼 before/after”带来 3 个咨询、1 个到店；“周年庆优惠”收藏高但咨询少。下周建议继续做真实案例对比，弱化硬广优惠。

---

## 4. 阶段范围

### 4.1 P0：可真实跑起来的最小闭环

P0 做到（保 8）：

1. 门店档案。
2. 真实素材库。
3. 小红书 + 抖音文案生成。
4. **视频成片**（AIDA 分镜流内确认 → 首帧 → 片段 → 薄壳合成 + AIGC 标识烧录；Seedream/Seedance 或即梦/可灵直调）。
5. 内容库（草稿 / 已发布两态起步）。
6. L3 发布包（扫码转手机 / 分段复制 / 图片·视频存相册 / 发布 checklist）。
7. 合规 gate（红线硬停 + 医美/医疗资质准入轻量版 Preflight）。
8. 手工线索台账。

> **注**：图文渲染管线、能力矩阵四表、周报、完整用量账本为缓做（视频脚本与 Durable 异步任务已随视频成片并入 P0，2026-07-08）；L2、凭据保险库、完整 Permission 三模式等为 P0 砍项。

P0 不做：

- 7×24 自动回复。
- 复杂 CRM。
- 自动投放。
- 多门店集团管理。
- 跨垂类模板市场。
- 全平台自动发布承诺。

### 4.2 P1：从创作副驾到运营工作台

增加：

- 内容日历。
- 批量生产。
- 平台变体管理。
- 周报与内容建议。
- 账号 packs。
- 轻量团队协作。
- 抖音/公众号等官方能力深化。
- L2 浏览器辅助：P1 由 pilot 发布耗时数据驱动重启评估，浏览器扩展形态优先（凭据不出商家设备）。

### 4.3 P2：监督自驾和值守 Agent

增加：

- 统一收件箱。
- 评论/私信意图分类。
- 授权范围内自动回复。
- 线索卡自动生成。
- 多门店/代运营工作区。
- 内容表现回流到选题计划。

P2 的前提是平台能力矩阵和账号安全已经跑通，否则不能作为商业主承诺。


---
# 01 · Agent 架构设计：一个门店一个 Operating Agent

## 1. 架构定位

本产品的 Agent 不是一个泛聊天机器人，也不是把几十个功能按钮包一层对话框。它是一个长期存在的门店运营对象：

> **一个门店 workspace = 一个 Store Operating Agent；多个平台账号、人设、素材、内容、线索都挂在这个 Agent 的上下文里。**

P0 的 Agent 形态是“创作副驾”，即人主导、Agent 负责理解门店、调用工具、生成内容、组织发布包和回收线索。P1/P2 再逐步解锁监督自驾和值守。

---

## 2. Store Operating Agent 的五类记忆

原方案中的 persona/knowledge/attribution 需要进一步门店化。P0 建议拆成五类记忆：

| 记忆 | 内容 | 用途 |
|---|---|---|
| 门店记忆 | 店名、城市、商圈、营业时间、项目价目、门店卖点、预约方式 | 保证内容像这家店，而不是通用模板 |
| 账号人设记忆 | 平台账号、昵称、语气、人设、目标受众、禁忌表达 | 保证跨平台变体仍保持账号一致性 |
| 素材记忆 | 案例图、环境图、before/after、好评截图、手法视频、素材标签 | 图片/图文/视频脚本调用真实素材 |
| 内容记忆 | 草稿、已发布、平台变体、版本、合规状态、表现备注 | 避免重复、沉淀本店爆款库 |
| 线索记忆 | 咨询、评论意图、加微、预约、核销、人工标注 | 形成内容台账和下一轮建议 |

### 2.1 为什么“线索记忆”不是因果归因

单店样本小，平台数据不完整，不能把一条内容和到店结果强行解释成因果。线索记忆的定位是：

- 记录内容与线索的相关性。
- 支持人工标注“为什么有效”。
- 形成周报和下一批内容建议。
- 不宣称自动因果优化。

---

## 3. 三种运行模态

### 3.1 ① 创作副驾 Copilot：P0 头牌

触发方式：用户主动点击或对话。

典型任务：

- “帮我出这周 5 条小红书内容。”
- “这张美甲图帮我写一条不那么广告的小红书笔记。”
- “把这条改成抖音 30 秒口播稿。”
- “给我一组大众点评项目介绍图文。”
- “今天店里没素材，告诉我该拍什么。”

Agent 负责：

1. 从门店记忆和素材记忆中补上下文。
2. 判断平台和内容类型。
3. 选择文案与视频成片工具；图文渲染工具为 Go 后 / P1。
4. 生成结构化内容卡。
5. 走合规 gate。
6. 存入内容库或生成发布包。

人在环边界：人始终确认内容和发布，不自动公开发布高风险内容。

### 3.2 ② 监督自驾 Supervised Autonomy：P1/P2 验证后解锁

未来形态：Agent 每周自动生成内容计划，交人批量确认，再生产内容和发布包。

P1 之前必须用 concierge/wizard-of-oz 证伪：

- 10–20 家门店连续 4 周试跑。
- 记录人工审阅分钟数是否低于亲手创作。
- 记录从副驾到监督自驾的转化率。
- 若用户长期只用副驾，不应强推监督自驾。

### 3.3 ③ 值守 Ambient / Daemon：P2 后置

未来形态：评论/私信/咨询分类、自动回复建议、线索捕获。

前置条件：

- 平台可合法稳定读取评论/私信/指标。
- 账号凭据保险库和审计机制完备（P0 无凭据托管，凭据保险库为 P2 值守前置）。
- 自动回复规则可解释、可撤权、可限制风险。

读不到数据的平台不能写入“值守闭环”叙事，只能做提醒和人工登记。

---

## 4. Agent 工具目录

工具设计原则：幂等、可预览、可回退、带成本标注、带合规状态。

| 域 | Tool | 输入 | 输出 | P0/P1 |
|---|---|---|---|---|
| 门店 | `store.profile.read` | workspace/store_id | 门店档案 | P0 |
| 门店 | `store.profile.update` | 结构化字段 | 更新建议/版本 | P0 |
| 素材 | `asset.search` | 项目/风格/平台 | 可用素材列表 | P0 |
| 素材 | `asset.tag` | 图片/视频/截图 | 标签、用途建议 | P0 |
| 选题 | `topic.suggest` | 主推项目、平台、数量 | 选题卡 | P0 |
| 文案 | `copy.compose` | 选题、平台、语气、素材 | 标题、正文、话题 | P0 |
| 文案 | `copy.rewrite` | 文案、改写意图 | 新版本 | P0 |
| 文案 | `copy.platform_adapt` | 内容内核、目标平台 | 平台变体 | P0 |
| 图片 | `image.generate` | 风格/用途/尺寸 | AI 图或背景元素 | P1（P0 用真实素材+模板顶） |
| 图文 | `graphic.compose` | 模板、素材、文案 | 封面/图文卡片 | Go 后 / P1（P0 用 Canva/人工模板顶） |
| 视频 | `video.script` | 选题、时长、平台 | 口播稿/分镜（流内可确认过程产物） | P0（2026-07-08 随视频成片并入） |
| 视频 | `video.generate` | 分镜、首帧图、参数 | 视频片段（Seedream/Seedance 或即梦/可灵直调） | P0（主打；工具名契约条目待补进数据模型契约） |
| 视频 | `video.compose` | 片段、BGM、字幕 | 成片（薄合成壳 ffmpeg，含 AIGC 标识烧录） | P0（主打；同上待补契约） |
| 合规 | `compliance.check` | 文案/图片/脚本 | 风险项、替换建议 | P0 |
| 合规 | `aigc.label_inject` | 文本/图片/视频文件 | 显式/隐式标识 | P0 |
| 内容 | `content.save_draft` | 内容卡 | 草稿/版本 | P0 |
| 内容 | `content.create_variant` | 内容内核、平台 | 平台变体 | P0 |
| 账号 | `account.list` | workspace/store_id | 账号列表/状态 | P0 |
| 账号 | `account.capability_check` | 平台/内容类型 | 可发布/可读取能力 | P0 |
| 发布 | `publish.create_package` | 内容包、平台 | L3 发布包（L1/L3 路由由 Core API REST 端点承载，不作为并列 tool；L2 移出 P0） | P0 |
| 线索 | `lead.create` | 线索来源、备注 | 线索卡 | P0 |
| 线索 | `lead.link_content` | lead_id/content_id | 关联记录 | P0 |
| 报告 | `weekly.report` | 时间范围 | 周报/建议 | Go 后 / P1（P0 人工洞察） |
| 计费 | `usage.reserve` | 任务类型 | 额度预留 | P1（P0 可用简单额度计数） |
| 计费 | `usage.commit` | reservation_id | 扣费确认 | P1 |
| 计费 | `usage.refund` | reservation_id | 退款 | P1 |

> **注**：工具命名单一事实源见前置决策层 §4（domain.snake_case）。

---

## 5. 编排流程

### 5.1 生成“本周内容”

```
用户选择本周主推项目/活动
  → store.profile.read
  → asset.search
  → topic.suggest
  → copy.compose / graphic.compose / video.script
  → compliance.check
  → content.save_draft
  → 返回内容卡片流
```

### 5.2 从一张图生成一条小红书笔记

```
上传图片
  → asset.tag 识别项目/风格/可用性
  → store.profile.read 补门店信息
  → copy.compose 生成标题/正文/话题
  → graphic.compose 生成封面文案或封面图
  → compliance.check
  → content.save_draft
```

### 5.3 生成抖音口播脚本

```
选择内容内核
  → copy.platform_adapt(platform=douyin)
  → video.script(15s/30s/60s)
  → shotlist.create 拍摄清单
  → caption.generate 字幕草稿
  → compliance.check
  → publish.create_package 生成发布包
```

### 5.4 发布后回流

```
内容已发布
  → 人工或自动录入咨询/加微/核销
  → lead.create
  → lead.link_content
  → weekly.report
  → 下一轮 topic.suggest 读取线索记忆
```

---

## 6. 人在环设计

### 6.1 必须人确认的动作

- 首次绑定账号。
- 公开发布。
- 优惠/价格/承诺类内容。
- 医美/医疗/注射/激光/手术/药械等受监管内容触发项。
- 自动回复涉及价格、疗效、退款、承诺、加微。

### 6.2 可自动执行的动作

- 草稿保存。
- 平台变体生成。
- 素材标签。
- 合规初筛。
- 周报生成。
- 发布包打包。

### 6.3 可撤权与审计

所有账号授权、自动化操作、合规提醒、发布结果、用量扣费都写入审计日志。用户可以撤销账号授权、删除素材、导出内容和线索。

---

## 7. Runtime 要求

P0 对 runtime 的要求相对克制：

- 流式输出。
- Tool calling。
- 长任务队列。
- 任务状态可恢复。
- 人在环暂停/继续。
- 成本与用量可记录。
- 失败可回滚。

P0 落地口径（ADR-0007）：Vercel AI SDK（streamText/generateObject + zod tools + provider registry）+ Postgres durable_jobs + 自研薄 step-runner（约 300-500 行）+ promptfoo 评测接 CI。业务代码只依赖 `ContentWorkflowRunner` 接口（Runtime Port 铁律），AI SDK 只出现在 runner 实现与工具包装层（包装层同时写 tool_calls 审计与 usage reserve/commit/refund）。Mastra 推迟引入：触发条件 = 流水线出现真实复杂度（多分支/子流程/人在环矩阵），届时仅替换 runner 实现。业务后端与 runner 同仓分模块，按 ADR-0006 拆分信号演进。

---
# 02 · 功能规划：从内容生产到运营闭环

## 1. 信息架构

P0 的导航要极简，避免把商家扔进复杂运营后台：

```
Agent 工作台   首页＝产品本体（D3，2026-07-08）：Agent 开场拟人化问候 + 今日建议 chips + 中央意图框（三喂料：
              打字/拍照传图/粘贴文本）+ 场景 chips 一行（全部场景▾展开货架）+ 创作流时间线（brief 确认卡 /
              进度旁白 / 候选 3 选 1 / 合规预审 / 产物卡，全为流中卡片）+ 右下异步任务浮标；
              示例美甲店＝预置在时间线里的演示流（只读不计用量，workspace chip 可切换/隐藏）
内容库        轻侧栏资产页：草稿、已发布两态 + 基础平台变体 + 做同款（"流里跑、库里存"）
线索台账      轻侧栏资产页：咨询、加微、预约、核销、内容关联、人工洞察
门店档案      轻侧栏资产页：项目价目、人设+品牌音、真实素材、拍摄清单、资质档案、账号基础记录
```

账号中心和用量/套餐不作为 P0 一级导航。账号相关在 P0 只保留门店档案里的账号基础记录和 account-level verified 边界验证；用量只在顶部 quota meter、生成按钮旁次数提示和账户抽屉中轻量呈现。P1 再独立成“账号中心”和“用量/套餐”一级导航。不设独立副驾浮层——**Agent 工作台即副驾**（D3，2026-07-08）；移动端 bottom nav 5 槽（工作台 / 内容库 / ⊕新建内容 / 线索 / 门店，⊕＝聚焦意图框）。

---

## 2. P0 功能模块

### 2.1 门店档案

作用：让副驾“懂这家店”。

字段：

- 店名、城市、商圈、地址、交通/停车。
- 门店类型：美甲、美发、美睫、SPA、生活美容等。
- 项目价目：项目名、价格、时长、适合人群、注意事项。
- 主推卖点：老师资历、环境、服务、产品、审美风格。
- 预约方式：电话、微信、团购链接、私信引导语。
- 语气人设：温柔专业、活泼年轻、老板娘亲切、高端轻奢。
- 禁忌：不能打折的项目、不能承诺的话术、不能使用的素材。

冷启动：支持从用户上传的历史帖子、价目表截图、公开主页信息生成档案初稿，但必须让用户确认。

### 2.2 真实素材库

素材类型：

- 案例图。
- before/after。
- 环境图。
- 手法视频。
- 顾客好评截图。
- 价目表/活动海报。
- 门头/地理位置图片。

标签体系：

| 标签类型 | 示例 |
|---|---|
| 项目 | 猫眼美甲、法式、烫染、睫毛、肩颈护理 |
| 风格 | 显白、通勤、甜酷、轻奢、自然、韩系 |
| 用途 | 封面、案例、过程、环境、口碑、价格 |
| 平台 | 小红书、抖音、点评、公众号 |
| 合规 | 真实素材、AI 辅助、顾客授权待确认、敏感 |

素材授权状态为一等字段：`已授权 / 待确认 / 不可公开`——before/after 类素材必须为“已授权”方可被内容引用（上传时一次性标记，见 09 章）。素材库视图分组：真实素材 / AI 生成 / 已用于内容 / 回收站（软删）。

素材库不仅存文件，还要服务创作：副驾生成内容时优先搜索真实素材，并在素材不足时生成拍摄清单。

上传入口按移动场景做重：PWA 加桌面快捷方式，移动端入口直达相机、拍完默认上传；上传时自动建议项目/风格标签（人工可改），不依赖商家事后整理——素材供给是留存第一断点，采集摩擦要压到"拍完即完"。

### 2.3 创作副驾

两个入口：

1. **对话式副驾**：用户随时说需求。
2. **成套产出**：一次生成本周 3–5 条内容。

结构化输出：

- 标题。
- 正文。
- 话题标签。
- 封面文案。
- 图片顺序 / 封面建议。
- 拍摄清单 / 口播方向 / 视频分镜（视频成片入 P0 后，分镜为流内可确认过程产物）。
- 转化钩子。
- 合规状态。
- 平台建议。

常用快捷指令：

- 更口语。
- 更专业。
- 弱化广告感。
- 加同城定位。
- 加价格但合规。
- 换成小红书风。
- 换成抖音口播。
- 生成点评项目介绍（Go 后 / P1；P0 可由陪跑人工样例验证）。

### 2.4 文案生产

P0 文案类型：

| 类型 | 说明 |
|---|---|
| 小红书笔记 | 标题、正文、话题、私信引导、图片顺序建议 |
| 抖音口播 | 15/30/60 秒口播、开头钩子、结尾转化 |
| 大众点评项目介绍 | Go 后 / P1；P0 可作为 WoZ/人工样例验证 |
| 公众号/私域短文 | Go 后 / P1；P0 不进自动生成范围 |
| 评论/私信话术 | Go 后 / P1；P0 只在线索台账人工备注中记录 |

文案生成要读取：门店档案、项目价目、素材标签、历史内容、平台模板、合规规则。

### 2.5 图片与图文生产

图文渲染管线为 Go 后 / P1。P0 仅保留真实素材整理和发布包内的封面/图片顺序建议：

1. **真实素材整理**：压缩、排序、封面文案、标签识别。
2. **封面/图文建议**：输出素材顺序、封面文案、Canva/人工渲染说明。
3. **图文卡片生成 / AI 图辅助**：Go 后 / P1；不得冒充真实案例。

Go 后 / P1 图文编辑器候选：

- 模板选择。
- 文案自动排版。
- 品牌色/字体预设。
- 一键替换图片。
- 移动端预览。
- 导出图片并注入 AIGC 标识元数据。

### 2.6 视频生产

**视频成片 = P0 主打功能之一**（2026-07-08 拍板，视频相关 P1 项并入 P0；买建边界与技术路线见 ADR-0008）：

- **链路**：AIDA 四段分镜（注意 → 兴趣 → 欲望 → 行动，ad_video_gen 实证骨架；美业映射 = 探店钩子 → 痛点共鸣 → 项目/手法特写 → 到店引导）→ 首帧图 → 片段生成（Seedream/Seedance 或即梦/可灵直调，进 benchmark 可切换）→ 薄合成壳（ffmpeg 拼接 + BGM/字幕 + **AIGC 显式标识烧录 + 隐式 metadata，合成阶段完成不可后置**）→ TOS/R2 存储 → 存相册 / 发布包。
- **交互**：分镜 = 创作流内可确认过程产物（逐镜可换）；成片单发 + 免费重试（上限 2 次）；分钟级长任务走"提交即走 + 任务中心 + 连接桥通知"。
- **质量**：候选按 美学 / 画质 / 合规 三维评估抽卡择优（"一致性"维度替换为医美违禁词 / 功效承诺过滤）。
- 拍摄清单 / 口播方向仍作为文案附属建议保留。

P2 再评估数字人口播与更重的视频编辑能力。

### 2.7 内容库

内容库按“内容项目”组织，而不是简单文件夹：

| 对象 | 说明 |
|---|---|
| Content Item | 一条内容的母体，如“渐变猫眼新款种草” |
| Platform Variant | P0 为小红书版、抖音版；点评版、公众号版为 Go 后 / P1 |
| Asset Link | 使用了哪些图片/视频/好评截图 |
| Version | 改稿版本 |
| Status | 持久化状态以 `references/product/reports/p0-data-model-api-contract.md` §4 为准；中文状态仅作 UI label，P0 内容库只暴露草稿/已发布两态 |
| Compliance | AIGC 标识、广告法检查、资质检查、人工确认 |
| Lead Link | 关联的咨询/加微/预约/核销 |

视图：

- 本周内容。
- 草稿箱。
- 已发布。
- 排期（P1）。
- 本店爆款库（P1）。

### 2.8 账号管理

P0 账号基础记录：

- 账号昵称、平台、主页链接、授权状态。
- 账号人设：老板娘口吻、专业老师口吻、年轻店员口吻。
- 账号级 verified 边界验证记录。

平台能力矩阵、健康状态、操作审计和独立账号中心均为 Go 后 / P1。

### 2.9 发布任务中心

P0 不需要复杂自动发布，但要有清晰的任务状态：

```
待准备 → 待合规 → 待确认 → 发布中/交接中 → 已发布/待人工/失败 → 已归档
```

以上为 UI 展示文案；持久化状态以 `references/product/reports/p0-data-model-api-contract.md` §4 为准，生成任务卡使用 queued / running / needs_action / completed 四态。

每个发布任务记录：

- 平台。
- 账号。
- 内容变体。
- 发布层级 L1/L3（L2 移出 P0，见 03 章）。
- 发布时间。
- 失败原因。
- 人工操作备注。
- 平台链接。

### 2.10 线索台账

P0 线索台账应允许人工登记，因为很多平台数据读不到。

线索字段：

- 来源内容。
- 平台。
- 时间。
- 类型：私信、评论、加微、预约、团购券、核销、到店。
- 金额/项目（可选）。
- 备注。
- 跟进状态。

人工洞察输出：

- 哪类内容带来咨询。
- 哪类内容收藏高但咨询低。
- 下周建议发什么。
- 素材缺口。

### 2.11 合规 gate

本节为创作侧摘要；义务主体分层、我方自身义务全量清单与验收口径见 09 章。

合规 gate 包含：

- AIGC 显式/隐式标识。
- 广告法敏感词：最、第一、永久、100%、根治、疗效承诺等。
- 医美/医疗资质准入轻量版：水光针、热玛吉、线雕、注射、整形、激光、手术、药械等词触发发布前核验提醒，而不是创作阶段一刀切拒绝。
- 优惠/价格：避免虚构原价、全网最低、限时误导。
- 医疗/医美发布前核验：机构资质、诊疗科目/项目范围、医疗广告审查证明、平台规则、顾客素材授权和人工确认。
- 硬停止项：伪造资质、未授权顾客案例、移除 AIGC 标识、绕过平台审核、疗效/安全性保证、治愈率/有效率承诺。

合规检查应该返回可操作建议，而不是只报错。

---

## 3. P1/P2 功能筛选

### 3.1 P1：运营工作台

- 内容日历。
- 批量平台变体。
-（轻视频生成已并入 P0 视频成片，2026-07-08，见 02 §2.6 / ADR-0008。）
- 账号中心独立页。
- 账号 packs。
- 自动周报。
- 简单团队协作。
- 官方平台能力深化。
- 发布成功/失败分析。
- L2 浏览器辅助：仅在 pilot 发布耗时数据支持后重启评估，浏览器扩展形态优先（凭据不出商家设备）。

### 3.2 P2：值守与自动化

- 评论/私信统一收件箱。
- 意图分类。
- 自动回复建议。
- 授权范围内自动回复。
- 线索卡自动生成。
- 多门店管理。
- 代运营工作台。
- 行业模板市场。

### 3.3 暂不做

- 私有签名逆向。
- 住宅代理池。
- 群控养号。
- 自动投放。
- 完整 CRM/SCRM。
- 未经核验提醒的医疗广告发布、诊疗建议、疗效保证、伪造案例或伪造资质。
- 全自动发布承诺。
- 复杂因果归因。

---

## 4. 验收指标

| 指标 | P0 目标 |
|---|---|
| 激活 | 新店 5 分钟内产出第一条可发内容，占比 ≥70% |
| 内容采用 | 直接采用/小改采用率 ≥60% |
| 持续使用 | 第 2–4 周每周至少产出/发布 3 条，占比 ≥50% |
| 线索闭环 | ≥60% 门店能记录至少 1 条“内容 → 线索”关联 |
| 合规 | AIGC 标识注入率 100%；受监管内容发布前核验提醒覆盖率 100%；资质/广告审查/素材授权/疗效承诺风险有留痕 |
| 商业 | 试点 Go=≥3 家在 ≥399 档付定金；P0 商业验收=≥2 家 8-12 周真实扣款续跑 + 纯自助组 4 周留存 ≥50% |

> **注**：跨阶段验收总表（WoZ/P0/P1 各一列）见前置决策层与 07 §9；发布包指标统一为 L3 使用率 ≥60%（P0）。

---
# 03 · 分发引擎：官方能力优先，发布包常态兜底

## 1. 设计目标

分发引擎不是产品头牌，而是内容闭环的基础设施。目标是让商家少花时间发布，同时不把平台账号置于不可控风险中。

核心原则：

1. **L1 官方能力优先**：能走官方 API、官方 SDK、官方分享链路就不走自动化。
2. **L2 浏览器辅助：P0 移除**。P1 由 pilot 发布耗时探针数据驱动再评估；重启时浏览器扩展形态（凭据不出商家设备）优先于云端 worker；五条件：不托管 cookie、不批量多账号、协议明示自动化性质与封号风险、逐平台法务评估、一键降级 L3。
3. **L3 发布包常态兜底**：任何平台都能生成完整发布包，人工复制/上传/确认。
4. **高风险动作必须留人确认**：尤其价格、优惠、受监管内容、私信承诺、账号授权。

---

## 2. 三层路由

### 2.1 L1 · 官方 API/开放能力

适用：官方支持发布、草稿、分享、数据读取或账号授权的平台。

特点：

- 稳定性高。
- 合规风险最低。
- 可审计。
- 依赖平台审核和权限。

P0 优先验证：微信公众号/视频号相关能力、抖音开放平台内容发布/分享能力、大众点评/美团商家侧可用能力。

### 2.2 L2 · 浏览器辅助发布（P0 移除，P1 数据驱动评估）

P0 移除理由：

- 平台 ToS 违约与群控案判例风险。
- 托管平台凭据为最高危资产。
- 边际价值有限，仅每条省 2-4 分钟。

P1 重启口径：由 pilot 发布耗时探针数据驱动再评估；浏览器扩展形态优先（凭据不出商家设备）；五条件——不托管 cookie、不批量多账号、协议明示自动化性质与封号风险、逐平台法务评估、一键降级 L3。

### 2.3 L3 · 发布包交接

发布包内容：

- 标题。
- 正文。
- 话题标签。
- 图片/视频文件。
- 封面图。
- 发布步骤。
- 定位/团购链接提示。
- 合规提示。

**P0 做厚交付（正式替代 L2，列入 P0 验收）**：扫码把发布包转到手机、分段一键复制、图片打包下载/直接存相册、发布 checklist；目标把单条发布操作压到 1-2 分钟。pilot 记录发布环节真实耗时（探针）。

价值：

- 永远可用。
- 不依赖平台接口。
- 对本地商家已经能节省大部分准备时间。

---

## 3. 平台路由矩阵（P0 初判）

| 平台 | 美业角色 | P0 路由建议 | 备注 |
|---|---|---|---|
| 小红书 | 种草、本地发现 | L3 常态（做厚）；L2 移出 P0 | 不承诺全自动提交 |
| 抖音 | 同城流量、团购引流 | L3 默认；L1 open_api/分享链路只做账号级验证 | 开放平台能力需按应用、账号、权限、内容类型和审核结果验收 |
| 大众点评/美团 | 决策、团购、核销 | L3 为主；官方商家能力逐项核实 | 交易闭环重要，但自动化需谨慎 |
| 公众号 | 私域承接 | L3 默认；L1 草稿箱优先验证 | 草稿和 freepublish 分 feature flag，发布必须人工确认 |
| 视频号 | 私域/同城补充 | L1/L3 | 按内容类型核实 |
| 企微/私域 | 线索承接 | 话术/线索交接为主 | 不做完整 SCRM |

> **注**：医美类目商家侧发布权限以 Week-0 真实认证账号实测为准（见 09 章）。

---

## 4. 平台能力矩阵

每个平台必须拆成四张能力表，不能只看“能不能发”。

| 能力 | 要回答的问题 |
|---|---|
| Publish | 能发什么内容？图文/视频/文章/草稿/分享？权限和审核条件是什么？ |
| Observe | 能读什么？阅读、播放、点赞、评论、私信、收藏、分享？频率和粒度如何？ |
| Engage | 能不能回复评论/私信？能否自动回复？是否需要企业/服务商资质？ |
| Attribution | 能回传什么线索？团购券、表单、企微、POS、预约、核销？ |

硬规则：

- 读不到评论/私信的平台，不进入“值守 Agent”承诺。
- 拿不到硬回传的平台，只做内容台账，不做 ROI 归因。
- 平台能力变化时，发布路由表必须版本化。

---

## 5. 账号中心与登录态

### 5.1 账号模型

一个门店可绑定多个平台账号：

- 平台。
- 昵称。
- 主页 URL。
- 授权方式。
- 授权状态。
- 账号人设。
- 平台能力。
- 健康状态。
- 最近发布。
- 最近异常。

### 5.2 凭据保险库

**P0 不托管任何平台凭据（L2 移除后本节无 P0 落点）**；保留为 P1 若重启云端 L2 的要求基线；扩展形态无需此系统。

如果托管云端浏览器 profile 或 cookie/session，必须设计为高风险凭据系统：

- KMS/信封加密。
- 租户隔离。
- 最小权限。
- 操作审计。
- 用户随时撤权。
- 异常登录告警。
- 泄露响应预案。
- 高风险动作默认人工。

### 5.3 账号健康状态

状态：

- 正常。
- 待登录。
- 授权过期。
- 待验证。
- 平台受限。
- 发布失败高发。
- 暂停自动化。

发布路由应读取健康状态，异常时自动降级到 L3。

---

## 6. 发布任务状态机

```
待准备
  → 待合规
  → 待确认
  → 发布中 / 交接中
  → 已发布 / 待人工 / 失败
  → 已归档
```

失败恢复：

- L1 失败：重试 → 降级 L3。
- （P1 重启后适用）L2 失败：保存状态 → 解释原因 → 降级 L3。
- L3 失败：提示人工步骤、保留素材和文案。

每次发布记录：

- route 层级。
- 账号。
- 内容版本。
- 合规结果。
- 人工确认人。
- 时间。
- 失败原因。
- 平台链接。

---

## 7. 合规姿态

分发引擎承担两类合规：

### 7.1 平台自动化合规

P0 阶段该风险面为零（无自动化操作、无凭据托管）；以下为 L2 若在 P1 重启时的自动化合规基线。

- 不逆向内部 API。
- 不伪造签名。
- 不做代理池对抗。
- 不隐藏风险。
- L2 操作显式告知并留审计（P1 重启后适用）。

### 7.2 内容生成合规

- AIGC 标识。
- 广告法敏感词。
- 价格/优惠校验。
- 医美/医疗资质准入轻量版 Preflight。
- 顾客素材授权提示。

发布前必须看到合规状态，不能让用户发布未知风险内容。

---
# 04 · 技术架构与选型

## 1. 架构原则

1. **云端 Web 优先**：首发不做桌面壳，优先跑通创作、内容、账号、线索和发布包。
2. **业务后端与 Agent runtime 解耦**：两者可同栈，但必须独立故障域。
3. **模型 provider registry**：文案、图片、视频、合规检测分别可路由到不同供应商。
4. **真实素材与内容资产是一等数据**：对象存储、素材标签、版本、合规状态要从 P0 设计。
5. **合规与审计内建**：AIGC 标识、广告法、账号授权、发布动作、扣费用量都可追踪。
6. **阶段化部署**：验证期 CF-first 最快验证，落地触发点启动国内迁移与备案（ADR-0005）；一切 CF 专属依赖走 adapter 保平移。

---

## 2. 总体架构

P0 采用架构 A：Workers 壳 + 单 Node 服务 + 单托管 Postgres（ADR-0006 supersede ADR-0002）。

```
Cloudflare Workers App Shell, mkfast-template fork
  ├─ 官网 / 登录 / Better Auth UI
  ├─ dashboard shell / settings / admin / billing / legal
  ├─ typed BFF adapters only
  ├─ 副驾 chat 端点（可选，进壳）
  └─ R2 upload/proxy mechanics（storage adapter）
        │  Hyperdrive
        ▼
core-service, 单 Node 服务（一个部署单元）
  ├─ core-api 模块：域逻辑
  │     Store Workspace authorization
  │     Store Profile / Services / Prices
  │     Real Asset Library metadata and rights
  │     Content Core / Platform Variants
  │     Compliance Gate / AIGC labels
  │     Publish Package / Publish Jobs / Publish Attempts
  │     Lead Ledger / Weekly Reports
  │     Usage Ledger / Provider Cost Ledger
  │     Provider Registry / Eval Gate / Audit Events
  ├─ ai-runner 模块：AI SDK，Runtime Port 之后
  ├─ renderer 模块：P0 可用 Canva/人工模板顶；satori → resvg-js → sharp 为 Go 后 / P1
  └─ jobs worker 模块：Postgres durable_jobs
        │
        ▼
Managed Postgres（唯一事实源，含 Better Auth 表）
        │
        ▼
R2 binary object storage（storage adapter，落地换 OSS/COS）
  ├─ original assets
  ├─ rendered artifacts
  ├─ publish package exports
  └─ consent evidence files
```

边界规则（模块级）：

- App Shell 壳不拥有 Store Workspace、内容、合规、发布、线索或用量事实。
- ai-runner 模块不直接修改产品事实；所有业务读写通过 core-api tools。
- renderer / jobs worker 模块不判断授权、不扣费、不决定合规，只处理 core-api 创建的 job。
- R2 key 只是 artifact reference，不是权限、授权、发布或合规事实。

### 演进终态与拆分信号

四服务边界是目标形态，不是 P0 交付物：

```
App Shell（Workers）──▶ Core API（Node service）
                          ├─▶ Agent Service（独立 Node）
                          ├─▶ Worker Pool（Node containers）
                          └─▶ R2 object storage
```

从单 Node 服务拆出独立服务的三个信号：

- **渲染阻塞事件循环**：renderer 的 CPU/字体/图片任务开始拖慢 core-api 请求路径 → 拆 Worker Pool。
- **Agent 发版节奏分叉**：ai-runner 的迭代频率与域逻辑不一致，需要独立发布/回滚 → 拆 Agent Service。
- **DB 真实瓶颈**：单托管 Postgres 出现读写瓶颈，需要独立扩容或分库 → 拆存储。

### 阶段化部署策略（ADR-0005）

- **验证期（CF-first）**：Workers 壳最快验证；国内外模型按质量混用（国产模型立即进 benchmark 作平移对照）；封闭邀请制试点；三轻护栏——
  - 可移植纪律：领域逻辑可移植 Node/Postgres，CF 专属（D1/R2/DO/Workers AI）走 adapter；
  - 数据卫生：顾客 PII/人脸不进海外 API，AIGC 标识保留；
  - 商家 Day-0 网络预检。
- **落地期（触发点不预设）**：备案 1-3 月、ICP 2-4 周为已知提前量；落地动作清单——
  - Node 服务 + PG 迁国内云；
  - R2 → OSS/COS（storage adapter 换实现）；
  - 默认路由切国产已备案模型；
  - 接入微信/支付宝；
  - **备案三件套 Gate 0**：公开收费上线的前置门。

---

## 3. 前端选型

建议：

| 模块 | 选型 |
|---|---|
| Web 框架 | `mkfast-template` fork，保留 TanStack Start / React App Shell |
| UI | 沿用模板已有 UI primitives，优先 shadcn/Base UI/Tailwind 风格一致 |
| 状态 | TanStack Query + Zustand |
| 富文本 | TipTap + Novel |
| 图文预览 | 同一份 template schema 生成 SVG/HTML 预览 |
| 图文导出 | P0 用 Canva/人工模板顶；satori → resvg-js → sharp 为 Go 后 / P1 主链路（CJK 自动换行）；Konva/自由画布后置 |
| 文件上传 | App Shell 做上传/proxy 机械能力，Core API 管 metadata 和 rights |
| 移动端 | 响应式 Web，优先保证上传素材、看内容卡、确认发布；PWA 桌面快捷方式 + 上传入口直达相机（拍完即传，自动建议标签） |

**UI 组件选型定稿（2026-07-08 研究，全文与一手证据见 `references/benchmark/ui-adaptation-study-2026-07-08/`）**：

- **底座事实**：mkfast = React 19 + Tailwind 4.1 + **Base UI 版 shadcn 4.0**（54 个 primitives）；新组件一律按 Base UI 组合 API（`render={}` / `nativeButton`）编写，**禁写 Radix `asChild`**。
- **AI 展示层**：prompt-kit copy-in（ResponseStream / Loader / Steps / ChainOfThought / Markdown / PromptInput，全套仅 5 处 asChild 改写、价值中心组件零改写，后端无关可直接吃 server function 流）；中文流式渲染 = **Streamdown + @streamdown/cjk**（防未闭合 markdown 乱码闪烁）；副驾对话（Go 后）按需单件引 AI Elements（Conversation / Tool / Reasoning / Sources）。
- **生成 UI 路线**：AI SDK UI——tool parts 三态（占位卡 → 成品卡就地替换）+ data parts 同 id reconciliation + useObject；**RSC / streamUI 官方判实验性，禁入生产**。
- **专用件**：qrcode.react（发布包二维码，canvas 直出 PNG）；react-dropzone + janglad/shadcn-dropzone（上传，依赖已核实干净）；价目表增删行 = react-hook-form `useFieldArray` 手写；移动弹层 = 模板已装 vaul Drawer（独立库非 Radix，不切官方新版）。
- **采购纪律**：全部 copy-in 手动单件移植，**不跑任何整包安装命令**。
- **视觉点睛（限量）**：Magic UI / Aceternity（均 MIT、primitive-agnostic 安全）仅用于技能卡墙、生成中状态、发布完成 1-2 处，懒加载；**全站铺动效 = AI slop + 掉帧，禁止**。
- **大字号触区**：`html{font-size:18-20px}` + Tailwind 4 `@theme` 间距 token 放大为主干；集中改 Button/Input/Select 默认高度变体（h-9 → h-12 ≈ 48px，WCAG AAA 档）补触区，合计 <10 文件。
- **中文界面字体**：P0 = 系统字体栈（PingFang SC / HarmonyOS Sans / MiSans fallback，零加载成本）；品牌中文字体落地期再议（与渲染链思源黑体是两件事）。
- **避坑（一手核实）**：assistant-ui（核心包焊死 9 个 Radix 依赖）/ CopilotKit（绑 AG-UI 协议 + Runtime）/ AutoForm（无 License）/ Origin UI（AGPL）/ Tremor（放缓）不引入。
- **PWA**：Serwist custom plugin 方案（不牺牲 SSR）；生产构建 SW 验证列 Week-1 POC（见 04 §5 spike 清单）。

核心页面：

- Agent 工作台（首页，D3 2026-07-08）：Agent 开场拟人化提醒 + 今日建议 chips（后台关键字提炼 + 低成本快速模型生成，如"这周烫染还没进新图，拍两张就能出内容"）+ 中央意图框（三喂料）+ 场景 chips + 创作流时间线（brief 确认卡 / 进度旁白 / 候选 3 选 1 / 合规预审 / 产物卡，全为流中卡片）+ 异步任务浮标；不做传统 SaaS 待办卡片，不设独立副驾浮层。
- 内容库：草稿/已发两态 + 基础平台变体；排期和本店爆款为 Go 后 / P1。
- 线索台账：内容关联、咨询、预约、核销、人工洞察；自动周报为 Go 后 / P1。
- 门店档案：项目价目、素材、拍摄清单、人设。
- 账号中心：P0 不做一级页；账号基础记录收在门店档案，能力矩阵/健康状态为 Go 后 / P1。

界面话术原则：以专业运营语言为基调（选题、笔记、口播稿、发布包、线索），不暴露技术概念（模型 / prompt / token / 重试与额度扣减的技术形态）；也不过度口语化——纯口语化会让 AI 的专业价值感打折。系统主动提醒一律走拟人化一句话形态。

用量透明原则：所有会扣减额度的 P0 动作（生成、重新生成、发布包导出）在按钮旁直接标注本次消耗次数，余量在创作台常驻可见；完整 Usage Ledger 前端为 Go 后 / P1。

---

## 4. 后端选型

建议 TypeScript/Node 优先。P0 为单 Node 服务（core-service），内部按模块边界隔离，不把产品事实放进 App Shell 或 ai-runner 模块。

| 层 | 建议 |
|---|---|
| App Shell | `mkfast-template` on Cloudflare Workers（采用方式=一次性取用 + adapter 隔离 CF 专属层） |
| Core API | 单 Node 服务内 core-api 模块（Hono/Fastify 任选），Postgres + Drizzle |
| Agent Runtime | ai-runner 模块：Vercel AI SDK，Runtime Port（ContentWorkflowRunner）隔离；Mastra 推迟（ADR-0007） |
| Renderer / Jobs | P0 保发布包导出 + durable_jobs + **视频合成 worker**（ffmpeg 拼接 / 字幕 / AIGC 标识烧录，2026-07-08 视频成片入 P0）；图文渲染管线 Go 后 / P1；拆分信号触发后独立 |
| DB | 单托管 Postgres（Neon/Supabase 等）＝唯一事实源，含 Better Auth 表；D1 不承载业务数据 |
| Queue | P0 先用 Postgres-backed durable jobs；吞吐瓶颈出现后再评估 Redis Streams/BullMQ/Inngest |
| Object Storage | Cloudflare R2，只存二进制对象和导出文件 |
| Search | Postgres full-text + 结构化标签；pgvector 仅预留，不建 P0 表 |
| Auth/IAM | Better Auth 负责身份/session；Core API 负责 workspace/store authorization |
| Billing | 订阅入口 + Usage Ledger reserve/commit/refund（refund 仅技术失败自动触发并留流水；"成功但不满意"不退、送免费重试每条上限 2 次） |
| Audit | Core API 写 append-only audit_events |
| Notification Bridge | 连接桥模式外接商家在用的工具：P0 最小实现 = 飞书/企微群机器人 webhook（"本周内容就绪 / 素材补给提醒 / 人工复盘提醒"三钩子）；P1 经 MCP 通用化；个人微信无官方接口，未覆盖商家由陪跑群人肉兜底 |

服务边界（P0 为单 Node 服务内的模块边界）：

- core-api 模块负责租户、权限、内容、素材、账号、线索、计费、合规、发布和审计。
- ai-runner 模块负责任务编排、工具调用和模型路由，但不拥有业务事实。
- renderer 模块负责重 CPU、字体和图片任务，结果回写 core-api。
- jobs worker 模块用独立 durable_jobs 队列，不能阻塞 App Shell 或 core-api 请求路径；拆分信号触发后 renderer/jobs 独立为 Worker Pool。**视频合成属重 CPU 长任务（分钟级），率先在 jobs worker 内隔离，是"三拆分信号"的首要监测对象（2026-07-08 视频成片入 P0）。**

---

## 5. Agent Runtime

P0 采用 Vercel AI SDK（ADR-0007），封装在 core-service 的 ai-runner 模块内，不进入 Workers App Shell，也不进入 core-api 域逻辑的类型边界。

能力映射（P0 稳定且可审计所需）：

- 流式生成：`streamText`。
- 工具调用：`tools` + zod schema。
- 结构化输出：`generateObject`。
- 多供应商：provider registry（可配 Cloudflare AI Gateway 做缓存/限流/观测）。
- 任务队列、人在环暂停/继续、失败重试、用量统计、tool_call 审计：由下方流水线与 core-api 承载。

流水线不引重框架：`durable_jobs`（Postgres）+ 自研薄 step-runner（300-500 行）承载编排、暂停恢复与重试。评测用 promptfoo 接入 CI，作为内容质量与回归基线。**视频成片流水线（分镜 → 首帧 → 片段 → 评估抽卡 → 合成）为 step-runner 第一条重流水线（2026-07-08）——实现直接抄 ad_video_gen 三件套范式（session.state 黑板 / CallBackAgent 人话进度 / 评估 N→1），Mastra 触发条件由它首个实测检验（ADR-0007 铁律不变）。**

**Runtime Port 铁律**：业务代码只依赖 `ContentWorkflowRunner` 接口，不 import 任何具体 runtime（AI SDK / Mastra / 其它）实现。第一批 Port 方法：

```text
ContentWorkflowRunner.generateWeeklyContent()
ContentWorkflowRunner.rewriteContent()
ContentWorkflowRunner.createPublishPackage()
ContentWorkflowRunner.cancelRun()
ContentWorkflowRunner.approveRun()
ContentWorkflowRunner.streamRunEvents()
```

第一批 Core API tools：

```text
store.profile.read
asset.search
usage.reserve
usage.commit
usage.refund
copy.compose
copy.platform_adapt
video.script
compliance.check
content.save_draft
publish.create_package
```

**Week-1 证伪 spike（六题，2026-07-08 两轮增补）**：

1. 流式输出经 BFF 透传或直连壳是否稳定（副驾 chat 端点）。
2. `generateObject` 结构化输出在中文内容任务上的稳定性。
3. 暂停/恢复与 `durable_jobs` 的配合（人在环改约束后重规划）。
4. **PWA-Serwist 生产构建验证**：TanStack Start 生产构建下 Service Worker 是否真实生成（已知坑：dev 正常、生产不生成；失败则 PWA 快捷入口降级为"添加到主屏幕"浏览器引导）。
5. **Streamdown + @streamdown/cjk 中文流式渲染验证**：逐字流出无乱码闪烁、未闭合 markdown 正确修复。
6. **视频成片链路 POC（2026-07-08 视频入 P0 新增）**：Seedance/即梦 API 直调 + ffmpeg 合成 + AIGC 标识烧录端到端；验收四指标 = 单条成本 / 端到端时长 / 质量可用率 / 标识烧录可行（成本数据回填 05 视频额度档位）。

验收：

| 验证项 | 通过标准 |
|---|---|
| 多轮 tool calling | 可调用门店档案、素材搜索、合规、内容保存 |
| Runtime 隔离 | 业务代码不 import 具体 runtime，仅依赖 ContentWorkflowRunner |
| Job 幂等 | 生成失败可 refund，重试不重复扣费 |
| 人在环 | 用户改约束后任务能重新规划 |
| 成本记录 | 每次模型/图片/视频调用写入 usage ledger |
| 审计记录 | tool_calls、agent_runs、model_calls 可追踪 correlation id |

**Mastra 推迟**：P0 不引入 Mastra；引入触发条件 = 流水线真实复杂度上升（多分支编排、长时值守、多实例恢复需求）超出薄 step-runner 承载力。届时 Mastra 落在 `ContentWorkflowRunner` 之后替换实现，不改业务代码。P1/P2 若做监督自驾和值守，再验证 durable agent、event source、daemon 调度和多实例恢复。

---

## 6. 模型与供应商路由

统一 provider registry：

| 任务 | 路由策略 |
|---|---|
| 选题/策略 | 高质量 LLM |
| 文案批量生成 | 性价比 LLM |
| 改写/标题/标签 | 低成本快速模型 |
| 合规初筛 | 规则引擎 + 小模型；高风险升级 |
| 图片生成 | 多图片 API，按质量/成本/尺寸路由 |
| 图文合成 | 本地/服务端渲染，不依赖模型 |
| 视频脚本 | LLM |
| 视频成片（P0 主打，2026-07-08） | 首帧 Seedream + 片段 Seedance（或即梦/可灵 API，benchmark 可切换）；单独计费进 usage ledger；KickArt API 仅为第三候选（≥50 家且自建链被证伪时重估，ADR-0008） |

**Provider shortlist（阶段化）**：

- 验证期：按质量混用——海外 GPT / Claude / Gemini + 国产 DeepSeek / 豆包 / 通义 / Kimi / GLM；图片 即梦 / 通义万相 / 可灵 同步进 benchmark 作平移对照。
- 落地期：默认路由切国产已备案模型（文本 DeepSeek/豆包/通义/Kimi/GLM，图片 即梦/通义万相/可灵）。
- 涉顾客 PII/人脸的任务，任何阶段只走国产或脱敏后再调用。
- BYOK 口径保留（见下）。

要求：

- 系统 key 默认。
- BYOK 可选但不作为唯一毛利方案。
- 每次调用记录 provider、模型、token/图片数、成本、失败原因。
- 支持 fallback。

---

## 7. 数据模型草案

| 表 | 作用 |
|---|---|
| `workspaces` | 租户/门店空间 |
| `users` | 用户 |
| `workspace_members` | 成员与角色 |
| `stores` | 门店档案 |
| `store_projects` | 项目/服务/价目 |
| `personas` | 门店/账号语气 |
| `accounts` | 平台账号 |
| `account_capabilities` | 平台能力矩阵 |
| `account_credentials` | 授权引用/凭据引用，不直接明文存储（P0 不落地——无凭据托管） |
| `assets` | 图片、视频、好评截图、文件 |
| `asset_tags` | 素材标签 |
| `content_items` | 内容母体 |
| `content_variants` | 平台变体 |
| `content_versions` | 版本记录 |
| `publishing_tasks` | 发布任务 |
| `publishing_attempts` | 发布尝试与降级记录 |
| `compliance_results` | 合规检查结果 |
| `leads` | 线索 |
| `lead_content_links` | 线索与内容关联 |
| `weekly_reports` | 周报 |
| `usage_ledger` | 用量账本 |
| `subscriptions` | 套餐 |
| `agent_runs` | Agent 执行记录 |
| `tool_calls` | 工具调用记录 |
| `audit_logs` | 审计日志 |

> 权威契约 = `references/product/reports/p0-data-model-api-contract.md`，字段与状态枚举以契约为准。

关键关系：

- 一个 `content_item` 可以有多个 `content_variants`。
- 一个 `asset` 可以被多个内容使用。
- 一个 `lead` 可以人工或自动关联到一个或多个内容。
- 每次发布尝试都保留 route、状态和失败原因。

---

## 8. 合规与安全

### 8.1 AIGC 标识

- 文本、封面、图文卡、视频脚本只要由 AI 生成或实质性改写，就记录 `is_ai_generated=true`。视频成片同样记录，并**在合成阶段完成显式标识烧录 + 隐式 metadata 写入**（薄合成壳职责，不可后置、不可由用户关闭）。
- 发布包和导出文件必须保留 AIGC 状态说明；不得提供“移除 AI 标识”功能。
- 图片/视频如涉及 AI 生成或合成，必须保留显式提示和隐式元数据。
- 内容库记录 AIGC 标识状态、生成时间、内容编号、模型/provider 和导出版本。
- AIGC 标识为强制口径，无“如适用”豁免；标识规则、平台差异与显隐式要求详见 09 章。

### 8.2 广告法与垂类规则

- 规则引擎维护敏感词与替代表述。
- 强监管词触发医美/医疗资质准入轻量版 Preflight。
- 医美、医疗、注射、激光、手术、药械等边界词在 P0 允许创作和发布包准备，但保存、导出、发布包交接或 L1 官方提交前必须展示 Publish Compliance Preflight。
- Publish Compliance Preflight 提醒商家核验医疗机构执业许可、诊疗科目/项目范围、医疗广告审查证明、平台规则、AIGC 标识、顾客素材授权；线上为纯提醒和留痕，缺件强警示但不做逐条门控确认。
- 对伪造资质、未授权顾客案例、移除 AIGC 标识、绕过平台审核、疗效/安全性保证、治愈率/有效率承诺等请求保留硬停止。
- 价格、优惠和效果表达必须来自价目表、活动表或商家确认，不能由模型猜测。

### 8.3 账号安全

- **P0 无平台凭据托管**（L2 移出 P0）；以下为 L2 重启后的账号安全要求。
- 凭据加密。
- 用户撤权。
- 操作审计。
- 异常告警。
- 高风险动作人工确认。

### 8.4 租户隔离

- 所有数据带 `workspace_id`。
- 对象存储路径按租户隔离。
- 向量索引按租户过滤。
- Agent tool 必须校验租户权限。

---

## 9. 可观测与成本控制

必须从 P0 建：

- 任务成功率。
- 首 token 延迟。
- 内容生成耗时。
- 图文生成耗时。
- 合规命中率。
- 用户采用率。
- 发布包使用率。
- 线索登记率。
- 每 workspace 成本。
- 每条内容成本。
- 模型 provider 失败率。

成本控制：

- 内容草稿可先低成本生成，再对高价值内容升级模型润色。
- 图文合成尽量本地/服务端渲染，不把所有需求交给图片模型。
- 视频成片按条单独计费（P0 主打；首帧+片段+合成全链成本进 usage ledger，单条成本 spike 实测后定档）。
- L2 移出 P0，无此项成本；P1 重启评估时再计。

---
# 05 · 商业模式与路线图

## 1. 商业模式

### 1.1 计费原则

不按字数计费。按“门店内容运营闭环价值”计费：

- workspace 订阅。
- 内容额度。
- L3 发布包次数。
- 素材容量。
- 陪跑/onboarding 服务。
- **视频成片额度 = P0 计价权益**（2026-07-08 视频入 P0）：按条进"够发 X 条短视频"口径，单条成本由 Week-1 spike 实测后定档。
- Go 后 / P1+ 再评估：运营账号 packs、图片/图文额度、线索台账容量。

**额度表述原则（2026-07-08 UI 研究，面向非技术客群）**：额度一律用**产出量口径**表述（"够发一个月朋友圈 / X 条短视频 / X 张海报"），**绝不用"积分 / credit / token / 模型数"话术**（可灵"产能换算"是正确样板，Higgsfield 积分制对非技术客群失效）。免费档给"做 + 预览"，付费墙精准卡在**"商用 / 去水印 / 下载高清"**——引流客群的价值兑现点（可灵已验证此闸门有效）。

### 1.2 套餐建议

价格需通过真实付费验证，以下为建议框架：

| 套餐 | 建议对象 | 包含 | 备注 |
|---|---|---|---|
| Starter | 单店试用 | 1 门店、账号基础记录、基础内容额度（含少量视频成片条数）、L3 发布包、手工线索台账 | 试点挂价 ¥199/月；用于降低试用门槛 |
| Growth | 主力单店 | 1 门店、更多内容额度 + **视频成片条数（主打权益）**、本周内容批量、L3 发布包、手工线索台账、首月陪跑复盘 | 试点挂价 ¥499/月（主力验证档）；P0 主推 |
| Pro | 高活跃门店/多员工 | 多账号、轻团队协作、更多额度与视频条数、优先支持 | P1 再定价 |
| Agency | 代运营/连锁 | 多门店、多客户 workspace、角色权限、审计、客户报表 | P2/P3 |
| Add-on | 加购 | 陪跑服务；图片包、**视频加量包**、账号包 | onboarding/陪跑 ¥300-800 单独收费（不含在订阅内）；控制毛利（视频包毛利按 spike 实测成本核） |

**定价锚点与单位经济**

定价锚点链：免费（平台原生）— ¥69/月（讯飞绘文）—【我方 199 / 499】— ¥2000-5000/月（代运营）。

单位经济三条件（同时满足才成立）：

- 获客成本 CAC < 1500。
- 首月陪跑 ≤3 人时/店。
- 留存 ≥8 个月。

以 499 档为例：留存 12 个月毛利约 +239（≈48%）；留存跌至 6 个月即转亏。草表见 `references/analysis/plan-review-2026-07-07/02-客户GTM与商业模型验证.md` §2。

### 1.3 单位经济要重新测

不能直接继承原方案的 ARPA 假设。P0 需要实测：

- 获客成本 CAC。
- 首月 onboarding 成本。
- 客服/陪跑成本。
- 单店月模型成本。
- Go 后 / P1+ 图片/图文成本。
- P0 视频成片单条成本、端到端时长、质量可用率和失败退款率。
- Go 后 / P1+ 重编辑 / 数字人口播成本。
- 发布失败处理成本。
- 账号异常处理成本。
- 月流失率。
- 续费率。

P0 的商业门槛建议：

> 试点 Go 门槛：≥3 家在 ≥399 档支付定金（“续跑”与口头意向不计）；P0 商业验收：15-20 家访谈、5-8 家进入 WOZ、≥2 家延至 8-12 周真实扣款续跑、纯自助对照组 4 周留存 ≥50%、招募全程按渠道记录 CAC。

---

## 2. GTM 策略

### 2.1 首发垂类

首发只选一个垂类：美业到店，并把资质准入的医美/医疗内容商家纳入探针子范围。

原因：

- 内容视觉化强。
- 到店转化路径清晰。
- 老板/店员痛点明确。
- 小红书/抖音/点评平台组合典型。
- 可以沉淀生活美容模板、医美/医疗发布前核验规则和平台合规提醒。

### 2.2 销售话术

不要讲“AI 写作更快”，应讲：

> 给你店配一个懂项目、懂素材、懂平台的内容副驾。P0 每周帮你准备能直接发的小红书/抖音文案、短视频成片和 L3 发布包，发完还能记哪条带来了咨询和到店。

### 2.3 交付形态

商家侧建议：**自助 SaaS 为主 + 首月陪跑为辅**。

首月陪跑内容：

- 门店档案梳理。
- 素材整理。
- 账号基础信息和边界验证。
- 第一周内容共创。
- 线索台账使用培训。
- 人工复盘下一批内容建议。

陪跑三条硬约束（防止产品滑向变相代运营）：

- 首月 ≤3 人时/店。
- 有毕业标准。
- onboarding 单独收费（300-800）。

对医美商家，陪跑不代写医疗声明内容（ADR-0004）。

这不是要变成代运营公司，而是用陪跑降低冷启动门槛，验证自助产品是否能留存。

### 2.4 渠道

候选：

- 美业 SaaS/工具社群。
- 本地商家培训课。
- 小红书/抖音同城运营课程合作。
- 美业品牌/供应链合作。
- 本地服务商/代运营合作。
- 线下商圈试点。

---

## 3. 路线图

### 3.1 0–4 周：调研与原型验证

交付：

- 10–20 家美业门店访谈。
- 收集真实素材、历史内容、价目表、平台账号主页。
- 竞品实测：剪映/即创/小红书/抖音/点评/美团商家工具。
- 平台能力矩阵初版。
- Figma 原型：创作台、内容库、门店档案、线索台账。
- Wizard-of-Oz：人工模拟副驾为门店产内容。
- 付费墙/fake-door（挂价 199/499）上线。
- 招募按渠道记录 CAC。
- 纯自助对照组设计（operator 产出耗时设天花板，Week3-4 切 1-2 家纯原型）。
- 召回 SOP：陪跑群每周人肉推送"本周内容已备好"，记录推送后 24h 打开率（连接桥形态的前身探针）。
- Day-0 预检增补（iOS 实机两项）：相机上传路径（capture 会先弹"拍照/图库/浏览"选择层，引导文案 = "请选择拍照"）；存相册路径（Web Share 优先 + 长按图片兜底双保险是否都通）。
- WoZ 优选产出沉淀为示例工作区样例底稿（操盘手水准，见 03 §3.1 示例内容质量原则）。

成功标准：

- ≥3 家在 ≥399 档付定金。
- 成品采用率 ≥60%（直接采用或小改）。
- 素材持续供给率（每周有新素材的门店占比）≥60%。
- operator 分钟数逐周下降。

注：“5 分钟产出首条 70%”为真产品阶段指标，移至 §3.2。

### 3.2 4–10 周：P0 MVP（进入条件 = 0–4 周 Go）

交付：

- 门店档案。
- 素材库。
- 创作副驾。
- 小红书/抖音文案。
- 内容库。
- 合规 gate。
- L3 发布包。
- L1 官方能力验证（不作为 P0 销售承诺）。
- 手工/半自动线索台账。
- 简单额度计数、生成前次数提示和技术失败退款留痕。

成功标准：

- 10 家真实付费。
- 每店每周至少产出或发布 3 条内容。
- 第 2–4 周留存 ≥50%。
- AIGC 标识注入率 100%。
- 受监管内容发布前核验提醒覆盖率 100%。
- 新店 5 分钟内产出首条可发内容 ≥70%。
- L3 发布包使用率 ≥60%。

### 3.3 10–22 周：P1 付费 beta

交付：

- 内容日历。
- 平台变体批量管理。
- 账号 packs。
- 自动周报。
-（轻视频生成已并入 P0 视频成片，2026-07-08，见 02 §2.6 / ADR-0008。）
- 官方发布能力深化。
- L2 重启评估（仅当 pilot 发布耗时探针数据支持；浏览器扩展形态优先，五条件见 03 章）。
- 简单团队协作。

成功标准：

- 50–100 个付费 workspace。
- 内容采用率 ≥60%。
- 发布包使用率 ≥50%。
- 单店月贡献边际为正。

### 3.4 22 周以后：P2/P3

交付：

- 统一收件箱。
- 意图分类。
- 自动回复建议。
- 线索自动捕获。
- 多店/代运营工作台。
- 行业模板市场。
- 其他垂类复制：餐饮、宠物、健身、教培（合规后再评估）。

---

## 4. 成本结构

核心成本：

| 成本 | 说明 |
|---|---|
| LLM 文案 | 可通过模型路由控制 |
| 图片生成 | 按张计费，需额度控制 |
| 图文渲染 | 服务器或客户端渲染，成本较低 |
| 视频 | 真视频成本高，必须单独计费 |
| 对象存储 | 图片/视频素材占用 |
| L2 浏览器 worker | P0 无此项（移出）；P1 评估时再计 |
| 客服/陪跑 | P0 商家侧重要成本 |
| 合规维护 | 规则、人工审核、法务咨询 |
| 平台接入 | 官方 API 申请、审核、维护 |

---

## 5. Proceed / Do not proceed

Proceed 条件：

- 门店愿意为自助工具 + 首月陪跑付费。
- 真实素材驱动内容的采用率达标。
- 线索台账能让商家感知价值。
- 合规 gate 可控。
- P0 不依赖 L2 全自动发布也能交付价值。
- 纯自助组（无陪跑）4 周留存 ≥50%。

Do not proceed 条件：

- 用户只愿意为“写文案”付 ≤99/月。
- 门店无法持续提供真实素材。
- 内容采用率低于 40%。
- 线索台账无人记录，无法形成闭环感。
- 需要承诺 GMV 或全托管代运营才能付费。
- 受监管内容无法形成可审计的发布前核验，或用户持续要求伪造资质、未授权案例、去 AIGC 标识、平台绕审、疗效/安全保证。
- 定金转化为零（挂价 199/499 无人付定金）。

---
# 06 · 核验与修订记录

## 1. 保留项

原方案中以下判断继续保留：

1. 不做私有签名逆向。
2. 官方能力优先，草稿交接兜底。
3. 私有知识库是核心护城河。
4. 不按字数计费。
5. provider registry 必须早做。
6. 受控发布必须保留人在环。
7. 合规必须前置。
8. 云端优先，桌面壳后置。

## 2. 修正项

### 2.1 从泛内容增长改为本地商家内容副驾

原方案“AI-native 内容增长 Agent”容易过大。修订后，首发聚焦本地商家，尤其美业到店。自媒体博主保留为未来复制方向。

### 2.2 从自主运营改为创作副驾

监督自驾和值守仍是远景，但 P0 头牌必须是用户可理解、可马上用的创作副驾。商家要的不是“我配置目标函数”，而是“这周帮我准备能发的内容”。

### 2.3 从复杂归因改为线索台账

不再把归因作为杀手级护城河。P0 只做内容与咨询/核销/预约的相关性记录。

### 2.4 从批量铺量改为真实素材驱动

平台不缺 AI 内容，缺真实案例和可信素材。批量生成服务于“多变体择优”和“跨平台适配”，不是同质化铺量。

### 2.5 发布能力改为分层路由

全自动发布不是 P0 承诺。L3 发布包是常态能力，L1 官方能力只在账号级 verified 后验证；L2 已移出 P0，P1 仅按发布耗时探针数据重新评估。

### 2.6 抖音能力口径已修正

抖音开放平台已有内容发布接入方案，支持通过 open_api 直接发布视频或图片内容至抖音。原方案中“抖音无官方图文发布能力，只能 L2”的口径已修正（见 03 §3 平台路由矩阵）：口径为优先核实官方能力、权限、内容类型与审核限制。

### 2.7 视频能力分层

**2026-07-08 更新：视频成片并入 P0 主打**——P0 = AIDA 分镜（流内可确认）+ 首帧/片段直调 + 薄壳合成 + AIGC 标识烧录（见 02 §2.6 / ADR-0008），字幕基础版随成片链路提供；P2 再评估数字人口播与重编辑能力。

### 2.8 医美接入改为资质准入制·轻量版

医美/医疗商家按资质准入制·轻量版接入（ADR-0004）：线下先核验执业许可与平台认证并入档案，线上 Preflight 为纯提醒、缺件强警示但不做门控。行为红线（伪造资质、未授权案例、去 AIGC 标识、绕审、疗效保证）硬停止；before/after 素材在上传时标注“顾客已授权”才可用；陪跑只做合规提示，不代写医疗声明。试点纳入 1–2 家认证医美作为探针。

### 2.9 部署与模型改为阶段化

部署与模型选型分阶段（ADR-0005）。验证期 CF-first + 模型混用，加三条轻护栏（adapter 可移植、顾客 PII 不进海外 API、网络预检）；落地触发点不预设。落地期迁国内云 + OSS + 国产模型默认 + 微信/支付宝 + 备案三件套，Gate 0 为公开收费上线前置门。

### 2.10 架构定为 Workers 壳 + 单 Node 服务

架构采用 Workers 壳 + 单 Node 服务（core-api / ai-runner / renderer / jobs 四模块）+ 单托管 Postgres（含 Better Auth 表）（ADR-0006，supersede 01 章 ADR-0002 四服务图）。原四服务图作为演进终态与三拆分信号保留，其边界规则下沉为单服务内的模块规则。

### 2.11 Agent Runtime 以 AI SDK 起步、Mastra 推迟

P0 Agent Runtime 用 Vercel AI SDK + 自研 step-runner，Mastra 推迟（ADR-0007），触发条件为流水线真实复杂度，届时只换 runner 实现。此条消除 01 §7 与 04 §5 之间关于 runtime 选型的自相矛盾。

### 2.12 L2 移出 P0、L3 做厚为正式替代

L2 浏览器自动化移出 P0，L3 发布包做厚（扫码转手机、分段一键复制、图片存相册、发布 checklist）为正式替代；凭据保险库随 L2 一并移出 P0。P1 重启时扩展形态优先。

### 2.13 商业验证前置

商业验证前置为硬门槛：挂价 Starter 199 / Growth 499 + onboarding 300–800 单收；试点 Go = ≥3 家在 ≥399 档付定金；P0 商业验收 = ≥2 家 8–12 周真实扣款 + 纯自助对照组留存 ≥50% + 记录 CAC。WoZ 阶段指标（定金/采用率/素材供给率/operator 分钟数）与真产品阶段指标（如“5 分钟激活”）分列。

## 3. 反驳项

以下方向不建议进入 P0：

- “一个 Agent 自主运营所有账号”。
- “多平台一键全自动发布”。
- “AI 生成大量内容铺量”。
- “归因越用越准”。
- “用 L2 自动化替代平台官方能力”。
- “未经资质准入和 Preflight 设计的医美/医疗/教培强监管内容同时首发”。

## 4. 仍需验证的问题

| 问题 | 验证方式 |
|---|---|
| 美业门店是否愿意付费 | 0-4 周付费墙：≥3 家在 ≥399 档付定金 |
| 内容采用率是否足够高 | 盲测 + 采用埋点 |
| 真实素材是否能持续供给 | 素材上传频率和拍摄清单完成率 |
| 线索台账是否有人记录 | 连续 4 周台账使用率 |
| 发布包是否足够节省时间 | 发布包使用率、用户反馈 |
| 合规 gate 是否可控 | 命中率、误杀率、受监管内容核验提醒覆盖率 |
| 官方平台能力边界 | 平台能力矩阵实测 |
| L2 是否值得做 | 真实账号 2 周压测，失败率/验证码率/异常率 |
| 小红书/抖音医美类目商家侧发布权限 | Week-0 真实认证账号实测 |
| 199/499 价格带真实付费转化 | 0–4 周付费墙定金数据 |
| 发布环节耗时是否构成 L2 需求 | pilot 发布耗时探针 |

---
# 07 · 落地功能设计：美业到店创作副驾（云端 P0）

## 1. 首发范围

**给谁**：美甲、美发、美睫、SPA、生活美容门店，以及有正常内容创作需求的医美/医疗类商家（资质准入制：线下核验执业许可与平台认证后开通，ADR-0004），1–10 人或小型连锁，老板、前台、店员或运营亲自参与。

**解决什么**：

- 不知道今天发什么。
- 拍了图但不会写。
- 不知道小红书/抖音/点评分别怎么发。
- 发完不知道有没有咨询和到店。
- 没有系统管理素材、草稿、账号和线索。

**P0 目标**：让一家美业门店从“每条内容 1–2 小时”降到“5–10 分钟准备一条可发内容”，并能记录内容带来的咨询/加微/核销。

---

## 2. 用户旅程

```
开通
  → 建门店档案
  → 上传真实素材
  → 点“生成本周内容”
  → 获得 3–5 条内容卡
  → 批量改稿/采用
  → 生成发布包/受控发布
  → 记录咨询/加微/核销
  → 周报建议下一批内容
```

关键体验：

- 首条内容 5 分钟内产出。
- 默认给成品，不给空白框。
- 每条内容都带平台建议和转化钩子。
- 真实素材不足时，副驾给拍摄清单。
- 合规问题直接给替代表述。

---

## 3. 页面设计

### 3.1 Agent 工作台：首页

布局：

```
顶部：Agent 拟人化问候 + 今日建议 chips + quota meter
中央：意图框（三喂料：打字 / 拍照传图 / 粘贴文本）+ 场景 chips 一行横滑
流中：brief 确认卡 → 进度旁白 → 文案候选或视频任务 → 合规预审 → 产物卡
浮标：异步任务中心 / 连接桥通知状态
```

内容卡字段：

- 标题。
- 推荐平台。
- 内容类型。
- 转化钩子。
- 正文预览。
- 配图/封面建议。
- 视频成片入口（分镜确认 → 成片，2026-07-08 视频入 P0）。
- 合规状态。
- 操作按钮。

### 3.2 内容库

视图：

- 草稿。
- 待发布。
- 已发布。
- 已归档。
- 本店爆款。

筛选：平台、项目、状态、合规、是否带线索。

### 3.3 线索台账

字段：

- 日期。
- 内容。
- 平台。
- 线索类型。
- 项目。
- 金额/券核销。
- 跟进状态。
- 备注。

支持人工复盘占位；一键生成周报为 Go 后 / P1。

### 3.4 门店档案

模块：

- 基础信息。
- 项目价目。
- 人设语气。
- 真实素材。
- 拍摄清单。
- 账号授权。
- 禁忌话术。

---

## 4. 美业场景包 v1

| 场景 | 内容结构 | 转化钩子 | 平台 |
|---|---|---|---|
| 新款项目种草 | 效果图 + 适合人群 + 过程简述 + 预约引导 | 私信问价/预约 | 小红书/抖音 |
| Before/After | 对比图 + 顾客诉求 + 改善点 + 注意事项 | 信任建立/加微 | 小红书/抖音 |
| 团购优惠 | 套餐内容 + 到店礼 + 使用说明 + 合规价格 | 领券/核销 | 大众点评/抖音 |
| 专业科普 | 常见误区 + 专业解释 + 门店服务方式 | 关注/咨询 | 小红书 |
| 顾客口碑 | 好评截图 + 服务细节 + 门店日常 | 私域/复购 | 全平台 |
| 同城引流 | 商圈定位 + 交通便利 + 本地话题 | 到店导航 | 抖音/小红书/点评 |
| 门店日常 | 老板/店员日常 + 环境 + 小故事 | 拉近距离 | 小红书/公众号 |

语气包：

- 温柔专业。
- 活泼年轻。
- 高端轻奢。
- 老板娘亲切。

---

## 5. 文案样式模板

### 5.1 小红书美甲种草

结构：

1. 标题：效果 + 人群/场景 + 情绪词。
2. 开头：本地小姐姐语气。
3. 正文：效果、适合人群、细节、到店提示。
4. 结尾：私信/预约引导。
5. 话题：项目词 + 风格词 + 同城词。

### 5.2 抖音口播

结构：

1. 0–3 秒：痛点或效果钩子。
2. 3–12 秒：展示过程或案例。
3. 12–25 秒：讲适合谁、为什么好看。
4. 25–30 秒：到店/私信/团购引导。

### 5.3 大众点评项目介绍

结构：

1. 项目是什么。
2. 适合谁。
3. 包含什么。
4. 体验流程。
5. 到店注意事项。
6. 合规价格说明。

---

## 6. 图片/图文模板（Go 后 / P1 候选）

图文渲染管线不进 P0。P0 只保留发布包中的素材顺序、封面文案建议和 Canva/人工渲染说明。以下为 Go 后 / P1 模板候选：

- 小红书封面图。
- 美甲 before/after 对比。
- 项目价格卡。
- 团购套餐说明图。
- 顾客好评卡。
- 门店环境介绍图。
- 拍摄清单卡。

每个模板支持：

- 替换图片。
- 自动填项目名/价格/门店名。
- 修改封面标题。
- 导出多平台尺寸。
- 标记是否 AI 辅助生成。

---

## 7. 视频脚本模板（P0 分镜/脚本起步库）

视频成片并入 P0 后（2026-07-08），以下模板从 Go 后候选转为 **P0 分镜与 Hook 脚本的内容起步库**（喂给 AIDA 分镜与爆款开场脚本库）。

### 7.1 案例展示脚本

| 镜头 | 内容 | 时长 |
|---|---|---|
| 1 | 成品近景，展示效果 | 3s |
| 2 | 顾客原始状态/需求 | 4s |
| 3 | 制作过程 1–2 个细节 | 8s |
| 4 | 成品多角度 | 8s |
| 5 | 预约/私信引导 | 5s |

### 7.2 店员口播脚本

- 开头：一句话钩子。
- 中段：适合人群 + 项目特点。
- 展示：插入真实素材镜头。
- 结尾：私信/团购/预约引导。

### 7.3 拍摄清单

副驾按内容自动生成：

- 成品正面图。
- 成品侧面图。
- 过程视频 5–10 秒。
- 门店环境图。
- 顾客好评截图。
- 门头/定位图。

---

## 8. 美业合规 gate

本节为垂类速查；义务主体分层与全量口径见 09 章。

### 8.1 广告法敏感词

拦截：

- 最、第一、顶级、全网最低。
- 永久、100%、绝对、保证。
- 根治、疗效、修复疾病相关表达。
- 纯天然、无任何副作用。

替换示例：

| 风险表述 | 建议替换 |
|---|---|
| 永久定型 | 更持久的造型效果 |
| 全城最低 | 近期体验价 |
| 100% 不伤甲 | 更注重护理与操作细节 |
| 根治皮肤问题 | 改善日常护理体验 |

### 8.2 医美/医疗资质准入轻量版 Preflight

触发词：

- 水光针。
- 热玛吉。
- 线雕。
- 注射。
- 整形。
- 玻尿酸。
- 肉毒。
- 医疗美容。
- 医疗、注射、激光、手术、药械。

处理：

- 允许生成草稿、改写、拍摄清单、素材整理和发布包准备。
- 保存、导出、发布包交接、L1 官方提交前触发 Publish Compliance Preflight。
- 提醒商家核验医疗机构执业许可、诊疗科目/项目范围、医疗广告审查证明、平台规则、AIGC 标识、顾客素材授权；线上为纯提醒和留痕，缺件强警示但不做逐条门控确认。
- 不替商家判断最终法律合规，不输出“已合法/可放心发布”等保证性结论。
- 对伪造资质、未授权顾客案例、移除 AIGC 标识、绕过平台审核、疗效/安全性保证、治愈率/有效率承诺等请求硬停止。

### 8.3 AIGC 标识

- AI 生成文本/图片/视频脚本记录标识状态。
- 导出图片/视频时注入元数据标识。
- **视频成片（2026-07-08 入 P0）**：显式标识（片头提示/角标）在 ffmpeg 合成阶段烧录进视频文件 + 隐式 metadata 注入；注入率 100% 含导出文件，计入 P0 验收。
- 发布包提示用户平台侧标识要求。

---

## 9. P0 验收

| 类别 | 标准 |
|---|---|
| 激活 | 新店 ≤5 分钟产出第一条可发内容，占比 ≥70% |
| 内容质量 | 直接采用/小改采用率 ≥60% |
| 频率 | 第 2–4 周每店每周至少产出/发布 3 条，占比 ≥50% |
| 线索 | ≥60% 门店记录至少 1 条内容关联线索 |
| 发布 | L3 发布包使用率 ≥60%；L1 官方能力只按 account-level verified 平台启用；L2 不在 P0 |
| 合规 | AIGC 标识注入率 100%，受监管内容发布前核验提醒覆盖率 100% |
| 商业 | 试点 Go=≥3 家在 ≥399 档付定金；P0 商业验收=≥2 家 8–12 周真实扣款续跑 + 纯自助组留存 ≥50% |

---

## 10. P0 迭代优先级

### Sprint 1：基础闭环

- 门店档案（精简版）。
- 素材上传 + 授权状态标记。
- Agent 工作台（含创作流与异步任务浮标）。
- 文案生成（小红书 + 抖音，AI SDK + step-runner 跑通）。
- 内容保存（两态）。
- 视频成片链路 POC：分镜 → 首帧 / 片段 → ffmpeg 合成 → AIGC 标识烧录 → 存储。

### Sprint 2：合规与发布

- 合规 gate（09 章口径：广告法敏感词 + AIGC 标识 + 医美 Preflight 提醒）。
- L3 发布包做厚四件套。
- 视频文件存相册 / 发布包交接。
- 平台变体（轻，人工改写辅助）。
- 图文先用 Canva 或人工模板顶（渲染管线为拆分信号触发项）。

### Sprint 3：线索与商业

- 手工线索台账。
- 内容关联。
- 简单额度计数。
- 视频成片按条额度和技术失败退款留痕。
- 付费墙与定金收取。
- 周报（人工）。

### Sprint 4：账号与打磨

- 账号授权（门店档案子模块）。
- L1 官方能力文档验证（不写接入代码）。
- onboarding 流程。
- 评测集接 promptfoo。

---

## 11. 下一步可继续深化的交付物

1. 低保真线框图。
2. 数据库 ERD。
3. Agent tool schema。
4. Prompt/模板库。
5. 合规规则 JSON。
6. 平台能力矩阵实测表。
7. P0 Sprint backlog。
8. 10 家试点访谈脚本。
9. 09 章合规 SOP 落地件（资质档案字段 + Preflight 提醒文案 + 行为红线词库）。
10. 付费墙页面与定金收取流程。

---

---

# 08 · 开源项目、组件与框架借鉴分析

> 本章用于补充 `04-技术架构与选型.md` 与 `07-功能设计-美业到店副驾-云端P0.md`。  
> 分析对象：Mastra、mkfast-template、open-tanstarter/TanStarter、Craft Agents。  
> 目标：明确哪些能力可以直接借鉴，哪些能力只能作为参考，哪些能力必须补足或自研。  
> 当前产品口径：**美业到店 + 医美/医疗资质准入制商家，云端 Web，自助 SaaS 为主 + 陪跑为辅，创作副驾 P0**。

---

## 0. 总结结论

这三个项目不应被看成三选一的底座，而应该分层吸收：

```text
mkfast-template
  = SaaS App Shell
  = 官网 / 登录 / 支付 / 用户后台 / 设置 / 文件存储 / 营销页 / 基础 Dashboard

open-tanstarter / TanStarter
  = TanStack Start / Better Auth / Postgres-first 形态参考
  = 只作对照，不作为 P0 主壳

Mastra
  = Agent Runtime 候选（P0 推迟，ADR-0007；P0 用 Vercel AI SDK + 自研 step-runner）
  = Agents / Tools / Workflows / Memory / RAG / Guardrails / Observability / Evals

Craft Agents
  = Agent 产品机制参考
  = Conversations / Permission Modes / Skills / Sources / Automations / Browser UX

自研核心域
  = 门店档案 / 真实素材库 / 内容母体 / 平台变体 / 发布包 / 线索台账 / 合规 gate / 用量账本（凭据保险库移出 P0——L2 已砍，为 P1 云端 L2 重启时的条件项）
```

一句话方案：

> **mkfast-template 一次性取用做 SaaS App Shell，Core API/Postgres 管产品事实，AI SDK 在 Runtime Port 后管 P0 编排（Mastra 为触发式升级项），Craft Agents 提供产品机制启发。**

这种分层与现有方案一致：P0 不做“全自动数字员工”，而是先做能让一家美业店真实跑起来的创作副驾、内容库、发布包、线索台账和合规 gate。

---

## 1. 三个项目在本产品中的定位

| 项目 | 最适合承担的层 | 可直接借鉴程度 | 不应承担的层 | 结论 |
|---|---|---:|---|---|
| Mastra | Agent Runtime、工具调用、工作流、RAG、评测与观测 | 高 | SaaS 业务主模型、门店领域对象、平台发布策略、垂类合规 | P0 推迟（ADR-0007）；本章分析保留为引入时的实施基线 |
| mkfast-template | SaaS 外壳、登录、支付、用户后台、R2 上传/proxy、部署、营销页 | 高 | Agent Runtime、重任务、Playwright、视频处理、Postgres 业务域 | P0 一次性取用 + adapter 隔离 CF 专属层，不长期 fork 跟随 |
| open-tanstarter / TanStarter | TanStack Start、Better Auth、Postgres-first 结构参考 | 中 | P0 主壳、产品事实源、Agent Runtime | 只作为参考，不作为主仓 |
| Craft Agents | Agent 产品交互范式、权限模式、会话任务箱、Skills、Sources、Automations、Browser UX | 中高 | 直接作为云端 SaaS 后端、直接复用桌面形态、公开平台 API discovery | 借机制，不借形态；尤其借权限、Skills、Sources 和任务状态 |

本地源码审查后的结论是：`mkfast-template` 可以作为 P0 App Shell 起点，但按 ADR-0006，Better Auth、session、API key、payment、upload metadata 等均随单托管 Postgres 收敛，D1 至多承载营销站轻数据；Store Workspace、素材、内容、合规、发布、线索、用量、成本和审计进入 Core API/Postgres。`open-tanstarter`/TanStarter 仅保留为 Postgres-first 和 Better Auth 参考。

---

## 2. Mastra 深度分析：功能、最佳实践与采用边界

**本节地位（ADR-0007）**：P0 采用 Vercel AI SDK + Postgres durable_jobs + 自研 step-runner + promptfoo；本节全部分析保留为 Mastra 引入时（触发条件=流水线真实复杂度）的实施基线，其中“P0 可直接采用/建立 XxxAgent”类表述以 ADR-0007 为准做时点平移。

Mastra 官方文档把能力分成 Core、Capabilities、Long-running Agents、Production 等大类。核心能力包括 Agents、Tools、Workflows、Memory、Studio、Workspaces、Browser、RAG、Voice，以及 Durable Agents、Background Tasks、Goals、Heartbeats、Signals、Signal Providers、Agent Controller、Server、Observability、Evals、Deployment 等。

### 2.1 Agents：Mastra 引入时的创作副驾形态

Mastra Agent 的定位是“LLM + tools + memory”的任务执行体，适合处理开放式目标，由模型决定调用哪些工具、循环多少轮、何时结束。

**可直接借鉴**

Mastra 引入时可以建立一个 `StoreContentCopilotAgent`；P0 当前实现为 `ContentWorkflowRunner` + AI SDK tools：

```text
StoreContentCopilotAgent
  ├─ 读取门店档案
  ├─ 检索真实素材
  ├─ 选择美业场景包
  ├─ 生成小红书/抖音平台内容
  ├─ 调用封面/图片顺序建议工具
  ├─ 调用合规检查
  └─ 保存草稿或生成发布包
```

**最佳实践**

Agent 不应直接输出一大段聊天文本，而应该输出结构化内容卡：

```ts
ContentCardOutput = {
  contentCoreId: string;
  title: string;
  body: string;
  platform: 'xiaohongshu' | 'douyin';
  hookType: '种草' | '优惠' | '信任' | '同城' | '私域';
  hashtags: string[];
  coverText: string;
  imageSuggestions: string[];
  shootingList?: string[];
  compliance: {
    status: 'pass' | 'warning' | 'blocked';
    issues: string[];
  };
  nextActions: Array<'save_draft' | 'rewrite' | 'make_variant' | 'create_publish_package'>;
}
```

**不应照搬**

不要把一个超级 Agent 设成“自己运营这家店”。P0 的主链路应该是“用户触发 → 固定 Workflow → Agent 在其中完成写作/改写/判断”，而不是不受控的 long-running agent。原因是商家账号、价格优惠、受监管内容、AIGC 标识、发布动作都需要强审计与强闸门。

**必须自研**

Mastra 不提供以下领域对象：

```text
Store / StoreProject / Persona / Asset / ContentCore / ContentVariant
ComplianceResult / PublishPackage / Lead / UsageLedger / PlatformAccount
```

这些对象必须在 Core API 中自研，Mastra 只调用它们。

---

### 2.2 Tools：把业务能力变成 Agent 可调用工具

Mastra 使用 `createTool()` 定义工具，要求明确 `id`、`description`、`inputSchema`、`outputSchema` 和 `execute()`。这非常适合把本产品里的创作、素材、合规、发布、线索全部工具化。

**推荐工具目录**

| 工具域 | Tool | P0 使用场景 | 风险级 |
|---|---|---|---|
| 门店 | `store.profile.read` | 生成内容前读取项目、价目、商圈、人设 | 低 |
| 素材 | `asset.search` | 优先找真实案例图、环境图、好评截图 | 低 |
| 素材 | `asset.suggest_shooting_list` | 真实素材不足时生成拍摄清单 | 低 |
| 文案 | `copy.compose` | 生成小红书笔记、点评介绍、口播稿 | 低 |
| 文案 | `copy.rewrite` | 更口语、弱化广告感、换语气 | 低 |
| 平台 | `copy.platform_adapt` | 一条内容内核生成多平台变体 | 中 |
| 图文 | `graphic.compose` | Go 后 / P1：封面图、价格卡、before/after 卡片 | 中，计费 |
| 图片 | `image.generate` | Go 后 / P1：只用于氛围图/背景/插画，不冒充真实案例 | 中，计费 |
| 视频 | `video.script` | P0：AIDA 分镜、Hook、拍摄/成片脚本，流内可确认 | 中 |
| 视频 | `video.generate` | P0：首帧与片段生成，按条计费 | 中，计费 |
| 视频 | `video.compose` | P0：ffmpeg 薄合成、字幕/BGM、AIGC 标识烧录 | 中 |
| 合规 | `compliance.check` | 广告法、AIGC 标识、医美/医疗资质准入轻量版 Preflight | 高 |
| 内容 | `content.save_draft` | 保存内容母体和平台变体 | 中，写库 |
| 发布 | `publish.create_package` | 生成 L3 发布包 | 中 |
| 发布 | `publish.prepare_browser_step` | P1+ L2 备稿到发布前一步（P0 禁用） | 高 |
| 线索 | `lead.link_content` | 内容与咨询/核销/加微关联 | 中 |
| 计费 | `usage.reserve` / `usage.commit` / `usage.refund` | P0 用于内容/视频/发布包额度和技术失败退款留痕；图像与未来 L2 为 Go 后 / P1+ | 高 |

**最佳实践**

所有工具都要记录：

```text
tool_call_id
agent_run_id
workspace_id
store_id
cost_estimate
risk_level
side_effect_type
requires_approval
input_hash
output_hash
started_at / ended_at
```

其中 `side_effect_type` 至少分为：

```text
read_only
write_draft
consume_quota
external_publish_prepare
external_publish_submit
credential_access
```

**必须自研**

Mastra 的 Tool 框架只解决“怎么定义工具”，不解决“工具内部业务是什么”。合规检查、用量账本、发布包生成、线索台账、账号能力矩阵都必须自研。

---

### 2.3 Workflows：Mastra 引入时的主链路形态

Mastra Workflows 适合步骤明确、多阶段、可暂停恢复、可观测的任务。它比纯 Agent loop 更适合 P0 的“本周内容生成”。

**可直接借鉴**

Mastra 引入时的主工作流（P0 由自研 step-runner 承载同等步骤）：

```text
GenerateWeeklyContentWorkflow
  1. load_store_context
  2. retrieve_real_assets
  3. select_beauty_skills
  4. generate_topics
  5. compose_content_core
  6. adapt_platform_variants
  7. create_visual_plan
  8. create_video_script_if_needed
  9. run_compliance_gate
 10. save_drafts
 11. return_content_cards
```

改稿工作流：

```text
RewriteContentWorkflow
  1. load_content_variant
  2. apply_rewrite_instruction
  3. run_compliance_gate
  4. save_new_version
  5. return_diff
```

发布包工作流：

```text
CreatePublishPackageWorkflow
  1. load_approved_variant
  2. ensure_aigc_label
  3. export_text
  4. export_assets
  5. render_checklist
  6. create_handoff_package
```

**最佳实践**

- 确定性步骤用 Workflow，开放式创作步骤用 Agent。
- 每一步都有 input/output schema，方便失败重试和成本归因。
- 生成内容前先 `usage.reserve`，成功后 `usage.commit`，失败或用户取消则 `usage.refund`。
- 合规 gate 必须在保存草稿和发布包前各跑一次。

**不应照搬**

不要用 Workflows 取代业务状态机。Workflow 是执行态，Content / Publish / Compliance / Usage 仍要有自己的持久状态。

---

### 2.4 Memory：用作会话记忆，不替代门店知识库

Mastra Memory 支持消息历史、观察性记忆、工作记忆、语义召回、多用户线程和 memory processors。

**适合放进 Mastra Memory**

```text
副驾对话历史
本次任务上下文
用户刚刚表达的临时偏好
某次改稿链路中的局部信息
```

**不应放进 Mastra Memory 的内容**

```text
门店档案
项目价目
素材 metadata
内容版本
发布状态
线索台账
用量账本
合规审计
账号凭据
```

这些应在 Core API / Postgres / 对象存储中管理。

**最佳实践：双层记忆**

```text
Mastra Memory
  = 会话级、线程级、短期任务上下文

Core Database + RAG
  = 门店级、租户级、长期资产、审计资产
```

---

### 2.5 RAG：门店知识库可借鉴，但结构化事实必须精确读取

Mastra RAG 提供文档处理、chunking、embedding、向量存储、检索，并支持 pgvector、Pinecone、Qdrant、MongoDB 等向量库。

**适合 RAG 的内容**

```text
历史爆款内容
顾客好评
门店介绍
服务流程
案例说明
老板/店员语气
公开行业模板
```

**不适合 RAG 的内容**

```text
价格
优惠有效期
营业时间
资质状态
禁忌话术
账号能力
套餐额度
```

这些应该结构化读取，不应靠向量检索“猜”。

**推荐实现（P1+；P0 不建 pgvector 表）**

```text
Postgres
  ├─ stores
  ├─ store_projects
  ├─ assets
  ├─ content_cores
  ├─ content_variants
  └─ leads

pgvector
  ├─ asset_embeddings
  ├─ review_embeddings
  ├─ historical_content_embeddings
  └─ template_embeddings
```

---

### 2.6 Guardrails / Processors：只做通用安全层，垂类合规必须自研

Mastra Guardrails / Processors 可做 prompt injection、PII、moderation、system prompt scrub、cost guard 等通用安全能力。

**可以直接借鉴**

```text
PromptInjectionDetector
SystemPromptScrubber
PIIDetector
ModerationProcessor
CostGuardProcessor
BatchPartsProcessor
```

**在本产品中的用法**

| Processor | 用法 |
|---|---|
| PromptInjectionDetector | 防止用户上传素材/文本里的 prompt injection 污染系统提示 |
| SystemPromptScrubber | 防止流式输出泄露内部指令、工具 schema、provider key 提示 |
| PIIDetector | 防止误输出顾客手机号、微信号等敏感个人信息 |
| CostGuardProcessor | 防止一次“本周内容”任务超出该套餐预算 |
| BatchPartsProcessor | 合并流式输出，降低重型 guardrail 的调用次数 |

**必须自研**

```text
广告法绝对化用语拦截
医美/医疗资质准入轻量版触发词库
价格/优惠合规
AIGC 显式标识
AIGC 隐式元数据写入
资质门槛校验
合规结果审计
```

Mastra Guardrails 是通用安全网，不是中国本地广告法和美业合规系统。

---

### 2.7 Studio：研发和运营调试台，不给商家用

Mastra Studio 可用于构建、测试和管理 agents、workflows、tools、processors、MCP tools、request context、eval datasets 等。

**适合内部使用**

```text
调试 CopilotAgent
跑 GenerateWeeklyContentWorkflow
查看 tool call
复现合规提醒和硬停止
比较 prompt 版本
查看 eval dataset
```

**不应暴露给商家**

商家端只应该看到：

```text
创作台
内容库
线索台账
门店档案
账号中心/发布授权
```

不要让商家看到 Agent、Workflow、Trace、Tool、Processor 等工程概念。

---

### 2.8 Workspaces：适合内部执行环境，不等于 SaaS Workspace

Mastra Workspace 提供持久文件环境、命令执行、搜索索引和 Skills。它适合 Agent 读写文件、执行命令、使用 sandbox。

**可以借鉴**

- `requireApproval`、`requireReadBeforeWrite` 这类工具权限机制。
- 多租户场景下的动态 filesystem / sandbox resolver。
- Skills 作为可复用指令包的机制。

**适合本产品的场景**

```text
图文渲染临时工作目录
发布包导出目录
内部模板包构建
批量内容生成的中间文件
测试环境中的 agent sandbox
```

**不应混淆**

Mastra Workspace 不是本产品的 `workspace`。本产品的 `workspace` 是租户/门店/成员/套餐/权限/用量对象，必须自研。

---

### 2.9 Browser：P1+ 重启 L2 时的风险参考

> L2 已移出 P0；P1 重启时浏览器扩展形态优先（凭据不出商家设备）。

Mastra Browser 提供 AgentBrowser、Stagehand、BrowserViewer 等能力，能导航网站、填写表单、点击、提取结构化数据、通过 CDP 或云浏览器服务操作真实浏览器。

**可借鉴**

P1 可用来验证：

```text
打开创作者后台
填标题
填正文
上传图片
选择话题/定位
停在发布前一步
截图回传给用户确认
```

**强约束**

- L2 已移出 P0；P1+ 只有在发布耗时探针支持后才重新评估。
- 小红书/抖音等平台 P0 以“做厚 L3 发布包 + 人工发布”为成功口径。
- 不做签名逆向、代理池、stealth browser、公开平台内部 API discovery。

**必须自研**

```text
PublishRouter
PlatformCapabilityMatrix
PublishTask
PublishAttempt
BrowserProfileVault
AccountHealth
HumanConfirmGate
L3PublishPackage
```

Mastra Browser 只是“浏览器操作能力”，不是“平台发布系统”。

---

### 2.10 Voice：P0 不做，可用于 P1 口播稿试听

Mastra Voice 支持 TTS、STT、STS 的统一接口。对本产品而言：

| 阶段 | 使用建议 |
|---|---|
| P0 | 不引入。只做口播稿、分镜、拍摄清单 |
| P1 | 口播稿试听、店员练习、视频脚本转音频 |
| P2 | 数字人口播、自动字幕、音频素材生成 |

视频真正落地前，语音能力不应进入 P0 承重墙。

---

### 2.11 Long-running Agents：P1/P2 再验证

Mastra 提供 Durable Agents、Background Tasks、Goals、Heartbeats、Signals、Signal Providers、Agent Controller 等长任务能力。其中 Durable Agents 仍标注 beta，适合客户端断线重连、长 agent loop、Inngest 执行等场景。

**采用建议**

| 能力 | 本产品场景 | 阶段 | 建议 |
|---|---|---|---|
| Durable Agents | 页面关闭后内容生成继续、长任务断线重连 | P1 | 先 spike，不做 P0 主依赖 |
| Background Tasks | 图片生成、封面渲染、embedding、发布包导出 | P1 | 可借鉴；P0 默认先用 Core API/Postgres durable jobs |
| Goals | “本周自动完成 5 条草稿” | P2 | 监督自驾验证后再用 |
| Heartbeats | 周报、素材提醒、草稿过期提醒 | P1 | 可先用普通 cron/queue |
| Signals | 用户中途改方向、合规通过后继续 | P1/P2 | 需多实例注入不丢不重的 spike |
| Signal Providers | 外部事件触发 agent | P2 | 平台能力矩阵验证后再做 |
| Agent Controller | 多 Agent、复杂审批、subagents | P2 | 不进 P0 |

**P0 原则**

```text
短任务：普通 Agent + Workflow
异步任务：Core API 创建 Postgres-backed durable jobs
长任务：只做 spike，不当主卖点
```

---

### 2.12 Server、Observability、Evals：Mastra 引入前置条件

Mastra Server 提供 agents / workflows API、middleware、auth、request context、custom routes、OpenAPI/Swagger 等。Observability 记录 agent run、workflow step、tool call、model interaction，并抽取 latency、token、cost 等指标。Evals / Scorers 用于评估非确定性 AI 输出。

**Mastra 引入后的推荐部署方式**

```text
mastra-service
  ├─ /agents/store-content-copilot/stream
  ├─ /workflows/generate-weekly-content/start
  ├─ /workflows/:runId/resume
  ├─ /tools/compliance-check/test
  └─ /healthz
```

**必须观测的指标**

```text
首 token 延迟
工作流完成率
成品直接采用率
小改采用率
合规提醒率
单条内容模型成本
单张图成本
发布包生成成功率
P1+ L2 备稿到发布前一步成功率
线索登记率
人工确认耗时
```

**必须做的评测集**

```text
50 条美甲小红书样本
50 条美发/美睫/SPA 样本
30 条大众点评项目介绍样本
30 条抖音口播样本
30 条违规/受监管内容核验样本
20 条价格优惠合规样本
```

P0 每次 prompt 或模型升级，都应跑 eval，避免“感觉更会写”但合规、转化钩子和平台原生性变差。

---

## 3. mkfast-template 深度分析：SaaS App Shell 可以复用

`mkfast-template` 是 P0 App Shell fork 目标；`open-tanstarter`/TanStarter 作为 TanStack Start、Better Auth、D1/R2、Cloudflare Workers 形态参考。模板类能力包含 TanStack Start、Vite、Drizzle ORM、Better Auth、Shadcn UI、Base UI、Tailwind CSS、Content Collections、Resend、Stripe/Creem、Cloudflare Workers、D1、R2 等。

### 3.1 项目结构：适合作为 App Shell 主仓

模板类目录通常包含：

```text
src/api
src/auth
src/cache
src/components
src/config
src/db
src/env
src/hooks
src/lib
src/mail
src/payment
src/storage
src/routes
src/types
```

其中 `src/routes` 已有 dashboard、settings、admin、blog、api 等路由分区。

**推荐改造**

```text
src/routes/dashboard/
  ├─ index.tsx              # Agent 工作台：意图框 + 创作流 + 任务浮标
  ├─ contents/              # 内容库
  ├─ leads/                 # 线索台账
  ├─ store/                 # 门店档案
  ├─ account-drawer/        # P0 账户抽屉：quota meter / 套餐入口；独立用量页 Go 后
  └─ settings/              # 成员、账号、授权、安全

src/components/content-agent/
  ├─ workbench.tsx
  ├─ intent-box.tsx
  ├─ creation-timeline.tsx
  ├─ content-card.tsx
  ├─ platform-variant-editor.tsx
  ├─ compliance-badge.tsx
  ├─ publish-package-preview.tsx
  ├─ asset-picker.tsx
  └─ lead-link-panel.tsx

src/api/
  ├─ agent.ts               # 转发到 core-service 的 ai-runner（AI SDK）
  ├─ contents.ts            # 转发到 core-api
  ├─ assets.ts
  ├─ leads.ts
  ├─ store.ts
  ├─ usage.ts
  └─ publish.ts
```

**不建议**

不要把 core-service 的重模块（ai-runner 长任务、renderer、FFmpeg/Playwright 类 worker）塞进 Cloudflare Workers。TanStarter 适合 app-shell 和轻 API，不适合重后端和长任务。

---

### 3.2 Better Auth：可直接用作认证底座，但要扩 Workspace

TanStarter 使用 Better Auth，支持 Google OAuth、邮箱密码登录、账号删除、登录/注册/忘记密码/重置密码等路由。

**可直接用**

```text
User
Session
OAuth Account
Email login
Password reset
Account deletion
```

**必须扩展**

```text
Workspace
WorkspaceMember
Store
StoreMemberRole
AgencyWorkspace
ClientWorkspace
```

本产品最小组织单位不是单个用户，而是门店 workspace。未来代运营版还需要 agency workspace 管多个 client workspace。

---

### 3.3 用户管理：改造成商户运营与客户成功后台

TanStarter 用户管理已有用户搜索、过滤、分页、角色、状态、封禁/解封能力。

**可改造为**

| TanStarter 用户后台 | 本产品商户后台 |
|---|---|
| 用户搜索 | workspace / 门店 / 联系人搜索 |
| 用户角色 | owner / operator / reviewer / admin |
| 用户封禁 | 冻结 workspace、暂停发布、禁用高风险动作 |
| 用户状态 | onboarding、活跃、流失风险、合规风险 |
| 用户详情 | 门店档案、套餐、用量、内容数、线索数、合规命中 |

**原因**

P0 是“自助 SaaS + 首月陪跑”。如果没有运营后台，客户成功无法判断哪家店没有完成门店档案、没有上传真实素材、内容采用率低、合规频繁命中或连续两周没有发布。

---

### 3.4 支付 providerRegistry：直接借鉴，但用量账本自研

TanStarter 支持 Stripe 和 Creem，并提供 `PaymentProvider` 接口与 `providerRegistry` 注册机制。

**可直接借鉴**

```text
PaymentProvider interface
providerRegistry
checkout session
customer portal
webhook handler
```

**必须自研**

支付只解决“收钱”，不解决“用量”。本产品 P0 计费先围绕 workspace 订阅、内容额度、视频成片额度、L3 发布包和素材容量；账号 packs、图文额度、自动发布/辅助发布次数、线索量级等均为 Go 后 / P1+ 口径。

境内收款还需自研微信支付/支付宝 provider（模板仅 Stripe/Creem，境内收款必须自建）。

需要自研：

```text
plans
subscriptions
quota_balances
usage_ledger
usage_reservations
usage_commits
usage_refunds
provider_cost_logs
```

**P0 账本原则**

```text
reserve before run
commit after success
refund on cancel/failure
never let tool directly mutate quota without ledger event
```

---

### 3.5 R2 存储：适合作为素材对象存储，但 metadata 自研

TanStarter 使用 Cloudflare R2 做文件存储，支持上传、下载、删除、用户文件列表、文件夹组织、文件大小和类型限制。

**可直接存**

```text
真实案例图
before/after 图片
门店环境图
手法视频
顾客好评截图
生成封面图
生成图文卡片
发布包文件
```

**必须补**

R2 是对象存储，不是素材库。素材库需要：

```text
assets
asset_tags
asset_embeddings
asset_usages
asset_compliance_results
asset_rights
asset_source
asset_platform_reuse
```

P0 副驾必须能回答：

```text
这张图是什么项目？
是否真实门店图？
是否顾客授权？
适合发哪个平台？
是否已用于某条内容？
是否触发过合规或限流？
```

---

### 3.6 D1 + Drizzle：保留模板轻数据，核心业务迁到 Postgres

模板使用 Cloudflare D1 和 Drizzle ORM。按 ADR-0006，D1 不承载业务数据，Better Auth 表入单托管 Postgres。

**D1 适合**

```text
营销站轻数据（可选）
```

**不适合承载唯一核心业务库**

本产品核心域建议用 Postgres：

```text
stores
store_projects
assets
content_cores
content_variants
content_versions
publishing_tasks
leads
lead_content_links
compliance_results
usage_ledger
agent_runs
tool_calls
audit_logs
```

原因：核心域需要复杂 JOIN、P0 最小额度留痕、审计、BI 分析和后续多租户扩展；pgvector 和强一致完整用量账本为 Go 后 / P1+ 能力。

---

### 3.7 AI playground：可作内部 demo，不作核心 AI 层

TanStarter AI 模块支持 Cloudflare Workers AI 和 fal.ai，内置文本摘要、翻译、标语生成、TTS、图像描述、图像生成、图像编辑等卡片。

> 注意：fal.ai 与 Workers AI 面向境内用户不可用，仅作内部研发/评测。

**适合**

```text
内部 AI playground
provider smoke test
fal.ai 图片 API 验证
Workers AI 轻量 demo
素材描述生成 demo
```

**不适合**

直接作为“门店内容副驾”。你的副驾需要门店知识、真实素材、场景包、合规 gate、内容库、发布包和线索台账的编排，这必须通过 core-service（core-api + ai-runner）来做。

---

### 3.8 API Key 管理：可借鉴交互，不等于凭据保险库

TanStarter API key 管理支持创建密钥、完整密钥只展示一次、列表掩码、过期时间、删除，并通过 Better Auth 校验。

**适合**

```text
机构客户 API
外部系统创建内容任务
Webhook 集成
开发者访问
```

**不适合直接存**

```text
小红书/抖音 cookie
浏览器 profile session
模型 BYOK 密钥
支付密钥
平台 OAuth refresh token
```

这些必须进入独立凭据保险库，要求 KMS / 信封加密 / 租户隔离 / 最小权限 / 操作审计 / 用户撤权 / 泄露响应。（P0 无凭据托管——L2 移出后无此类数据；本要求适用于 P1 若重启云端 L2。）

---

### 3.9 Cloudflare Workers 部署：适合壳，不适合重任务

TanStarter 面向 Cloudflare Workers 部署，并通过 GitHub Actions / Wrangler 部署。Workers 的包大小与运行时限制意味着它适合 app-shell 和轻量 API。

**适合放在 Workers**

```text
官网
登录/注册
dashboard shell
用户设置
支付 webhook
文件上传签名
套餐页
营销页
博客
轻量 BFF API
```

**不适合放在 Workers**

```text
Mastra long-running agent
Playwright browser worker
FFmpeg / Remotion
大图批量渲染
复杂 RAG / pgvector 重查询
视频处理
凭据保险库核心逻辑
```

**推荐仓库分层**

架构 A（ADR-0006）：

```text
mkfast app-shell（Workers）
  = SaaS Web 主壳 / 官网 / 登录 / 支付 / dashboard shell / 轻量 BFF

core-service（单 Node 服务）
  = core-api + ai-runner + renderer + jobs 四模块

managed Postgres
  = 产品事实源（含 Better Auth 表）/ 内容 / 合规 / 用量 / 线索 / 审计

R2（adapter）
  = 对象存储，经 adapter 隔离 CF 专属层
```

原四仓图保留为演进终态：

```text
mkfast-template
  = app-shell / SaaS Web 主仓

core-api
  = Postgres 业务域 / 线索 / 内容 / 合规 / 用量 / 账号

mastra-service
  = Agent / Workflows / RAG / Guardrails / Evals / Observability

worker-pool
  = P0：视频薄合成 / 标识烧录；Go 后 / P1+：图文渲染 / Playwright / embedding / 周报 / 重视频编辑
```

---

## 4. Craft Agents 深度分析：借产品机制，不借桌面形态

Craft Agents 的价值不在于直接作为后端框架，而在于它把 Agent 产品机制做得完整：会话收件箱、权限模式、Skills、Sources、Automations、Browser、API Discovery、Labels、Statuses、Messaging 等。

### 4.1 Conversations：改造成“内容任务收件箱”

Craft Agents 把会话组织成类似邮件客户端的多会话 inbox，每个会话可开始、暂停、恢复、按状态组织、搜索、归档。

**借鉴到本产品**

```text
内容任务收件箱
  ├─ 本周内容生成
  ├─ 小红书改稿
  ├─ 抖音口播脚本
  ├─ 大众点评项目介绍
  ├─ 合规待处理
  ├─ 发布包待确认
  ├─ 线索周报
  └─ 素材拍摄清单
```

**推荐状态（UI 标签示例；持久化状态以契约枚举为准）**

```text
todo
in_progress
needs_review
blocked_by_compliance
needs_asset
publish_package_ready
published
done
archived
```

**实现方式**

不要照抄 JSONL 本地存储。云端 SaaS 应用要用：

```text
agent_runs
agent_events
tool_calls
content_tasks
content_versions
audit_logs
```

同时可以借鉴 append-only event log 的思想，方便追溯每次 Agent 生成和人工修改。

---

### 4.2 Permissions：最值得借鉴，改造成受控运营权限

Craft Agents 提供 Explore、Ask to Edit、Execute 三种权限模式。Explore 只读；Ask to Edit 默认审批修改；Execute 允许自主执行。

**改造成本产品权限模式**

| Craft 模式 | 本产品模式 | 允许动作 | 禁止/需确认动作 |
|---|---|---|---|
| Explore | 只读建议 | 分析门店、检索素材、生成建议 | 写草稿、扣费、发布 |
| Ask to Edit | 草稿确认 | 生成草稿、改稿、保存版本 | 高风险合规、价格承诺、发布 |
| Execute | 授权自动 | 低风险批量保存、生成发布包、周报 | 真正发布、自动回复、优惠承诺 |
| 高风险闸门 | 受控发布 | 逐项人工确认 | 医美/医疗行为红线、价格、资质、L1 官方提交 |

**P0 默认策略**

```text
生成内容：自动
保存草稿：自动
扣内容额度：自动但账本留痕
生成发布包：自动
真正提交发布：默认人工
优惠/价格/受监管内容：必须确认；伪造资质、未授权案例、去标识、平台绕审、疗效保证等请求硬停止
自动回复：P0 不做
```

**需要自研**

```text
PermissionPolicy
RiskLevel
ApprovalRequest
ApprovalDecision
AuditLog
ComplianceGate
PublishGate
```

---

### 4.3 Skills：直接借结构，改造成“美业场景包”

Craft Skills 使用 `SKILL.md`，含 YAML frontmatter 和正文指令，可通过 `@skill` 或相关性自动触发，并支持 `globs`、`alwaysAllow`、`requiredSources` 等字段。

**本产品建议结构**

```text
skills/
  beauty-xiaohongshu-note/
    SKILL.md
    examples.json
    templates.json
    compliance.json
  beauty-before-after/
    SKILL.md
    examples.json
  beauty-dianping-project/
    SKILL.md
  beauty-douyin-script/
    SKILL.md
  beauty-shooting-checklist/
    SKILL.md
```

**示例 SKILL.md**

```yaml
---
name: 美甲小红书种草笔记
description: 生成美甲/美睫/美发项目的小红书风格图文内容
platforms:
  - xiaohongshu
requiredSources:
  - store_profile
  - project_price
  - real_assets
compliance:
  - regulated_content_preflight
  - no_absolute_claims
  - aigc_label_required
output:
  - title
  - body
  - hashtags
  - cover_text
  - image_suggestions
---

生成要求：
- 像本地真实顾客分享，不要像硬广。
- 必须优先引用真实案例图。
- 有优惠时只使用合规价格表达。
- 医美/医疗内容必须进入资质准入轻量版 Preflight；不得生成疗效承诺、伪造资质、未授权案例或绕审话术。
```

**为什么重要**

Skills 是你的行业资产沉淀方式。它把 prompt、模板、合规、样例、平台风格和素材要求从代码中抽出来，形成可版本化、可评测、可运营迭代的“美业内容能力包”。

---

### 4.4 Sources：改造成平台/数据源注册体系

Craft Sources 支持 MCP servers、REST APIs、本地文件夹等外部数据连接，并为每个 Source 配置 `config.json`、`guide.md`、`permissions.json`。

**本产品 Source Registry**

```text
source_type:
  store_profile
  asset_library
  xiaohongshu_account
  douyin_account
  dianping_manual
  wechat_official_account
  wecom
  form
  pos
  coupon_redemption
  public_hotspot
```

**每个 source 必须记录**

```text
授权方式
可读能力 Observe
可写能力 Publish/Engage
频率限制
ToS 风险
是否可用于线索台账
是否可用于值守
是否需要人工确认
失效兜底
```

这与当前方案的 Publish / Observe / Engage / Attribution 四表完全一致。

---

### 4.5 Automations：改造成运营提醒和周任务

Craft Automations 支持 app/agent 事件触发 shell command、prompt action、cron scheduled workflow，并通过 JSON 配置。

**本产品可借鉴**

```text
每周一生成本周内容候选
素材不足时提醒拍摄
草稿 3 天未发布提醒
线索 24 小时未处理提醒
套餐额度不足提醒
高风险提醒或硬停止后提醒客户成功
发布失败自动转 L3 发布包
每周日生成内容-线索周报
```

**P0 原则**

自动化只做：

```text
提醒
生成草稿
生成发布包
人工洞察待办
创建待办
```

不做：

```text
自动提交发布
自动回复私信
自动承诺价格
自动修改平台账号设置
```

---

### 4.6 Browser 与 API Discovery：只借受控浏览器 UX，不借公开平台逆向思路

Craft Browser 内置 Chromium，Agent 可导航、填表、点击、提取数据、截图、查看网络请求。其 API Discovery 通过网络请求观察发现内部 API，并直接调用以提升提取速度。

**可以借鉴**

```text
浏览器操作流程
页面快照/元素引用
截图留痕
发布前一步人工确认
远程 Agent + 本地浏览器的安全思路
```

**不建议用于公开内容平台**

API Discovery 不应被用于小红书、抖音、大众点评等公开平台内部 API。原因：这会偏离“不逆向、不爬、不做代理池”的分发原则，并把 L2 从受控浏览器操作推向未授权内部 API 访问。

**允许使用的范围**

```text
自有后台
客户明确授权的内部系统
合作方开放 API 调试
自建 admin / CRM / 表单系统
```

**P0 发布边界**

```text
L3 发布包常态兜底
L1 官方 API 仅账号级 verified 后启用
L2 已移出 P0，P1+ 按发布耗时探针数据重评
禁止把 API Discovery 包装成公开平台发布能力
```

---

## 5. 直接借鉴清单

Mastra 各行“进入阶段”按 ADR-0007 统一平移为“触发式（P1+）”；AI SDK 为 P0 实际采用。

| 来源 | 能力 | 借鉴方式 | 进入阶段 | 原因 |
|---|---|---|---|---|
| mkfast-template | 项目结构 | 作为 Cloudflare Workers App Shell fork | P0 | 已有 dashboard、settings、admin、api、auth、payment、storage |
| mkfast-template | Better Auth | 直接用于身份/session；workspace/member/store 归 Core API | P0 | 登录注册无需重造，但产品授权不能靠 shell role |
| mkfast-template | 用户管理 | 改造成商户/客户成功后台入口 | P0 | 支撑首月陪跑与合规审查 |
| mkfast-template | R2 上传/proxy | 复用机械能力；metadata/rights 归 Core API/Postgres | P0 | P0 存真实素材、授权证明、发布包导出；图文卡片为 Go 后 / P1 |
| mkfast-template | Payment providerRegistry | 作为订阅入口参考；Usage Ledger 另建 | P0/P1 | 收款事实和产品用量事实必须分离 |
| mkfast-template | API key 管理 | 借交互，不存平台 session | P1 | 外部 API/机构版可用 |
| mkfast-template | AI demo/playground | 内部 provider 验证 | P0 | 不作为核心副驾 |
| Mastra | Agent + Tools | 创作副驾核心 | 触发式 | 适合工具调用与流式生成 |
| Mastra | Workflows | 本周内容、改稿、发布包生成 | 触发式 | 步骤明确，便于重试和审计 |
| Mastra | Memory | Agent 内部会话记忆 | 触发式 | 不替代 Store Profile、Content Core、价格、合规、发布和账本事实 |
| Mastra | RAG | 门店知识/历史内容检索 | 触发式 | 支撑“懂这家店” |
| Mastra | Guardrails | 通用安全层 | 触发式 | prompt injection、PII、成本防护 |
| Mastra | Observability | AI trace/token/cost/latency | 触发式 | 成本和质量必须可观测 |
| Mastra | Evals | 美业内容质量评测 | 触发式 | 避免 prompt/model 更新退化 |
| Mastra | Browser | L2 发布前一步 spike | P1 | P0 不作为主承诺 |
| Craft | Permissions | 改造成受控运营权限（P0 简化为“生成自动/发布人工”二态，三模式 P1） | P0 | 与受控发布高度一致 |
| Craft | Conversations | 内容任务收件箱 | P1 | 让 Agent 任务可暂停、恢复、筛选 |
| Craft | Skills | 美业场景包（P0 用静态模板，Skills 版本系统 P1） | P0 | 形成行业资产 |
| Craft | Sources | 平台能力矩阵/数据源注册（P0 用一张能力记录表） | P1 | 决定闭环是否成立 |
| Craft | Automations | 周内容/草稿/线索提醒 | P1 | 先提醒，不自动高风险动作 |
| Craft | Browser UX | 发布前一步确认、截图留痕 | P1 | 借 UX，不借逆向 |

---

## 6. 必须自研或补足的核心模块

这些模块是产品壁垒，不能交给通用框架：

| 模块 | 为什么必须自研 | P0 交付 |
|---|---|---|
| 门店领域模型 | 本产品核心对象是门店，不是普通 user/project | Store、StoreProject、Persona、PriceList、StorePolicy |
| 素材库 | 真实素材优先是美业内容质量基础 | P0：Asset、AssetTag、AssetUsage、AssetCompliance；P1+：AssetEmbedding |
| 内容模型 | 一条内容要有母体、多平台变体、版本、合规状态 | P0：内容母体/变体/版本最小表；P1+：复杂版本与任务收件箱 |
| 平台适配 | 平台风格、限制、发布方式完全不同 | P0：小红书/抖音模板配置；P1+：PlatformPreset、PlatformVariantRule |
| 平台能力矩阵 | 读/写/互动/归因能力决定闭环叙事 | P0：单表能力记录；P1+：Publish / Observe / Engage / Attribution 四表 |
| 发布系统 | L3 发布包是核心工作流 | P0：PublishPackage、verified L1 PublishTask；P1+：L2/复杂 PublishAttempt |
| 线索台账 | 商家买的是咨询/预约/核销可见性 | P0：Lead、LeadContentLink；P1+：WeeklyReport |
| 合规系统 | 广告法、AIGC 标识、医美/医疗资质准入轻量版 Preflight 是 P0 红线 | ComplianceRule、ComplianceResult、AIGCLabel、PublishCompliancePreflight |
| 用量账本 | 模型/图片/视频/L2 发布均有成本 | P0：简单额度计数；P1+：UsageLedger、QuotaBalance、Reservation/Commit/Refund |
| 凭据保险库 | 平台 cookie/session 泄露即账号风险（**P0 不落地**——L2 移出后无凭据可管；P1 云端 L2 重启时的条件项） | SecretRef、KMS、EnvelopeEncryption、CredentialAudit |
| 行业 Skills | 这是长期资产，不是一次 prompt | BeautySkill、TemplateSet、ExampleSet、CompliancePack |

---

## 7. 推荐落地架构

> 本图为**演进终态**（拆分信号触发后的目标形态）；P0 实际形态见 §3.9 架构 A 与 04 章——ai-runner（Vercel AI SDK）、renderer、jobs 为 core-service 进程内模块，Mastra 为触发式升级项（ADR-0007）。

```text
mkfast-template App Shell
  ├─ 官网 / 定价 / 登录 / 注册
  ├─ Dashboard shell
  ├─ 创作台
  ├─ 内容库
  ├─ 线索台账
  ├─ 门店档案
  ├─ 套餐 / 用量
  ├─ Admin / 客户成功后台
  └─ 轻量 BFF API

Core API + Postgres，自研
  ├─ Workspace / Store / Member
  ├─ Asset / Content / Lead
  ├─ PlatformAccount / CapabilityMatrix
  ├─ Compliance / UsageLedger
  ├─ PublishTask / PublishPackage
  └─ AuditLog

AI Runtime（P0 = core-service 内 ai-runner/AI SDK；触发式升级为独立 Mastra Service）
  ├─ StoreContentCopilotAgent
  ├─ GenerateWeeklyContentWorkflow
  ├─ RewriteContentWorkflow
  ├─ CreatePublishPackageWorkflow
  ├─ RAG / Memory
  ├─ Guardrails / Processors
  ├─ Observability / Evals
  └─ Durable / Signals spike later

Worker Pool（P0 = core-service 进程内模块；拆分信号触发后独立）
  ├─ SVG template compiler
  ├─ resvg-js rasterizer
  ├─ sharp image processor
  ├─ FFmpeg thin compose + AIGC label burn-in（P0 视频成片）
  ├─ Embedding worker
  ├─ Remotion / heavy video editor later
  ├─ Playwright QA/fallback screenshots
  └─ Weekly report worker

Craft-inspired Product Layer
  ├─ Content task inbox
  ├─ Permission modes
  ├─ Skills / Scene packages
  ├─ Sources / Platform capabilities
  ├─ Automations
  └─ Browser audit / confirmation flow
```

---

## 8. P0 / P1 / P2 分阶段采用计划

### P0：验证“美业门店内容副驾”

直接采用：

```text
mkfast-template app-shell
Better Auth
R2 上传/proxy 机械能力
用户/商户后台雏形
支付 providerRegistry 雏形
Vercel AI SDK（chat + structured output + tools）
Postgres durable_jobs + 自研 step-runner
promptfoo evals
Craft-style permissions：简化二态
Craft-style skills：静态模板
Craft-style sources：能力记录表
```

自研：

```text
Core API
Postgres 业务主库
内容模型
素材 metadata
素材 rights gate
线索台账
合规 gate
用量账本
发布包
平台能力矩阵
durable jobs
audit events
video compose worker + label burn-in
```

不做：

```text
Durable Agent 主链路
Goals 主链路
AgentController
全自动浏览器发布
API Discovery 用于公开平台
完整统一收件箱
自动回复
重编辑时间线 / 数字人口播 / 专业剪辑器
桌面壳
```

---

### P1：从副驾变成运营工作台

增加：

```text
Mastra 引入评估（触发条件）
Mastra Background Tasks / Durable spike
Inngest / Redis 持久任务
Playwright / Mastra Browser 发布前一步
内容任务收件箱
周报自动化
Remotion / 重视频编辑
更多美业 Skills
商户 admin 与陪跑工作台
凭据保险库（仅当云端 L2 重启）
```

---

### P2：监督自驾和值守再进入

增加：

```text
Signals / Heartbeats / AgentController spike
统一收件箱
自动回复建议
平台数据读取
多门店 / 多账号 agent
半自动运营周期
Sources 扩展到企微/POS/表单/券核销
```

前提：平台能力矩阵证明 Observe / Engage / Attribution 能成立。否则值守 Agent 只能做提醒，不能进入“自动闭环”叙事。

---

## 9. Week-0 / Week-1 技术验证清单

### 9.1 Week-0/1 Runtime spike（AI SDK）

| 验证项 | 成功标准 |
|---|---|
| AI SDK 工具调用 + 流式 | 经 BFF 透传或直连稳定 |
| generateObject 结构化输出 | 结构化内容卡稳定 |
| step-runner + durable_jobs | 暂停/恢复/重试/refund 正确 |
| promptfoo evals | 20 条样本进 CI |
| RAG / Guardrails / Observability / Durable | Mastra 引入时再验 |

Week-0 另加：

- 试点商家网络预检 SOP。
- 备案路径预研（材料清单与周期，触发点到即提交）。

### 9.2 mkfast-template 源码审查

| 审查项 | 要看什么 |
|---|---|
| 路由结构 | dashboard/settings/admin/api 是否易扩展 |
| 鉴权 | Better Auth schema、session、middleware、admin guard |
| 数据库 | 确认 D1 仅承载 shell-local / 营销轻数据，不承载业务事实；业务事实进单托管 Postgres |
| 存储 | R2 上传 API 是否支持 folder、metadata、user scope |
| 支付 | providerRegistry、webhook 幂等、订阅状态同步 |
| API key | 一次展示、掩码、删除、verifyApiKey 逻辑 |
| 部署 | GitHub Actions、wrangler、运行时 secrets、包大小 |
| AI | AI playground 与 core-service 内 ai-runner 模块的边界 |

### 9.3 Craft-inspired 产品原型

| 原型 | 验证点 |
|---|---|
| 内容任务收件箱 | 商家是否理解“待审核/待发布/需素材/已完成” |
| 权限模式 | 只读建议、草稿确认、授权自动、受控发布是否清晰 |
| Skills 场景包 | 运营能否不改代码迭代模板 |
| Sources 能力矩阵 | 每个平台读/写/归因能力是否能产品化展示 |
| Automations | 提醒与自动生成是否不会制造误操作风险 |

---

## 10. 推荐数据模型增补

为了吸收 Mastra / mkfast-template / Craft 的机制，建议在原数据模型上增补。P0 只保留 `agent_runs`、`tool_calls`、必要 audit/durable job；其余为 P1/P2：

```text
agent_runs
  id, workspace_id, store_id, agent_id, workflow_id, status, started_at, ended_at, cost_estimate

tool_calls
  id, agent_run_id, tool_id, input_json, output_json, risk_level, side_effect_type, cost, status

content_tasks  # P1+
  id, store_id, type, status, source, assigned_to, related_content_id, due_at

approval_requests  # P1+
  id, workspace_id, risk_level, action_type, payload_json, status, approved_by, decided_at

skills  # P1+
  id, slug, name, industry, platform, version, status

skill_versions  # P1+
  id, skill_id, body_md, examples_json, compliance_json, eval_score, published_at

sources  # P1+
  id, workspace_id, source_type, platform, status, auth_type, capabilities_json, risk_level

automations  # P2
  id, workspace_id, trigger_type, condition_json, action_json, enabled, created_by

agent_evals  # P1+
  id, agent_id, dataset_id, prompt_version, model, score_json, run_at
```

---

## 11. 对现有文档的修订建议

（v1.5 已执行本节建议，保留作变更依据）

### 11.1 `04-技术架构与选型.md`

新增“开源项目采用边界”小节：

```text
mkfast-template = Cloudflare Workers App Shell，不承载产品事实和重任务
Core API/Postgres = 产品事实源、权限、合规、发布、线索、用量和审计
ai-runner = core-service 内模块，Vercel AI SDK + Runtime Port；Mastra 为触发式升级项
renderer/jobs = core-service 内模块；图文渲染管线 Go 后/P1，拆分信号触发后独立
Craft Agents = 产品机制参考，不复制桌面形态
```

同时把架构图更新为：

```text
Workers App Shell → core-service（core-api / ai-runner / renderer/jobs）→ Postgres/R2
```

### 11.2 `01-agent架构设计.md`

新增：

```text
Agent 运行权限模式：只读建议 / 草稿确认 / 授权自动 / 受控发布
Skills 场景包：美业 SKILL.md 结构
Sources：平台能力矩阵的 source registry 表达
```

### 11.3 `02-功能规划.md`

新增：

```text
内容任务收件箱
美业 Skills 资产管理
运营 Automations：周内容、草稿、线索、素材提醒
```

### 11.4 `07-功能设计-美业到店副驾-云端P0.md`

新增：

```text
P0 页面可加入“任务状态”概念
门店档案中加入 Sources/账号能力矩阵
场景包管理从静态模板升级为 Skill version
```

---

## 12. 最终采用决策

| 决策 | 结论 |
|---|---|
| 是否直接用 Mastra | P0 推迟，AI SDK 起步（ADR-0007）；触发条件=流水线真实复杂度 |
| 是否直接用 mkfast-template | 是，作为 SaaS App Shell fork；不承载产品事实 |
| 是否直接用 open-tanstarter/TanStarter | 不作为主仓；只作 Postgres-first 和 Better Auth 参考 |
| 是否直接用 Craft Agents | 不直接用；借产品机制和交互模型 |
| 是否把核心业务放 mkfast-template D1 | 不；D1 只放 shell-local 数据，核心域放 Core API/Postgres |
| 是否把 AI runtime 放 Cloudflare Worker | 副驾 chat 端点可在 Workers（AI SDK 兼容）；流水线/渲染/jobs 在 core-service（Node）；Mastra 若引入放 Node（ADR-0007） |
| 是否把 API Discovery 用于公开平台 | 不建议；只用于自有或明确授权系统 |
| 是否 P0 做完整自动运营 | 不做；P0 只做创作副驾、内容库、发布包、线索台账、合规 gate |
| P0 部署形态 | 验证期 CF-first，阶段化迁移（ADR-0005） |
| L2 浏览器辅助 | 移出 P0，P1 数据驱动（扩展形态优先） |

---

## 13. 参考来源

- Mastra Docs: https://mastra.ai/docs
- Mastra Agents: https://mastra.ai/docs/agents/overview
- Mastra Workflows: https://mastra.ai/docs/workflows/overview
- Mastra Memory: https://mastra.ai/docs/memory/overview
- Mastra RAG: https://mastra.ai/docs/rag/overview
- Mastra Guardrails: https://mastra.ai/docs/agents/guardrails
- Mastra Browser: https://mastra.ai/docs/browser/overview
- Mastra Workspace: https://mastra.ai/docs/workspace/overview
- Mastra Durable Agents: https://mastra.ai/docs/long-running-agents/durable-agents
- Mastra Observability: https://mastra.ai/docs/observability/overview
- Mastra Evals: https://mastra.ai/docs/evals/overview
- Mastra Server: https://mastra.ai/docs/server/mastra-server
- TanStarter Docs: https://docs.tanstarter.dev/zh/docs
- TanStarter Project Structure: https://docs.tanstarter.dev/zh/docs/structure
- TanStarter Auth: https://docs.tanstarter.dev/zh/docs/auth
- TanStarter Payment: https://docs.tanstarter.dev/zh/docs/payment
- TanStarter Storage: https://docs.tanstarter.dev/zh/docs/storage
- TanStarter AI: https://docs.tanstarter.dev/zh/docs/ai
- TanStarter Deployment: https://docs.tanstarter.dev/zh/docs/deployment
- TanStarter API Keys: https://docs.tanstarter.dev/zh/docs/apikeys-management
- Craft Agents Introduction: https://agents.craft.do/docs/getting-started/introduction
- Craft Agents Conversations: https://agents.craft.do/docs/core-concepts/conversations
- Craft Agents Permissions: https://agents.craft.do/docs/core-concepts/permissions
- Craft Agents Skills: https://agents.craft.do/docs/skills/overview
- Craft Agents Sources: https://agents.craft.do/docs/sources/overview
- Craft Agents Automations: https://agents.craft.do/docs/automations/overview
- Craft Agents Browser: https://agents.craft.do/docs/browser/overview
- Craft Agents API Discovery: https://agents.craft.do/docs/browser/api-discovery

---

# 09 · 合规与资质架构

> 本章为拍板口径。合规不是"提醒商家核验"一件事——有一整层义务的主体是我方，提醒转移不了。本章按义务主体重构，把强制义务定为产品内建的硬能力，把提醒与留痕留给商家自负的部分。证据与来源见 `references/analysis/plan-review-2026-07-07/03`。

## 1. 设计原则：按义务主体分层

把义务错误地统一成"提醒商家核验，不替商家判断"，会在两个法律关系里落空：作为生成合成服务提供者，AIGC 标识是我方的无条件强制义务；作为为商家设计、制作内容的一方，广告经营者查验义务也落在我方。这两层靠"提醒"转移不了。本章按义务主体分三层，各层的产品形态不同：

| 义务主体 | 我方角色 | 义务性质 | 产品形态 |
|---|---|---|---|
| 我方作为生成式 AI 服务提供者 | 服务提供者 | 法定强制、不可转移，产品必须内建 | 硬能力：备案、AIGC 标识、内容审核、滥用拦截 |
| 我方作为受托处理者 / 潜在广告经营者 | 受托处理者 / 广告经营者 | 法定强制，靠 SOP + 协议 + 档案履行 | DPA、资质档案、承接留存、Preflight 提醒 |
| 商家作为广告主 / 内容责任主体 | 工具提供方 | 商家自负，我方提醒 + 留痕 | 提示 + 授权留痕 + 协议约定责任归属 |

三层的分界线就是产品的责任边界：第一层是我方无条件内建的硬能力；第二层以受托处理者/潜在广告经营者身份履行，靠销售环节 SOP、协议与档案落地；第三层才是"提醒 + 留痕 + 协议约定"，最终发布决定由商家自负。医美准入制之所以能走轻量版（见 §5），前提正是第一层的硬能力不打折。

## 2. 我方自身义务清单

下表 17 条为我方自身法定义务，每条附性质、法规依据、生效时点与 P0 落点。生效时点分三档：**验证期**（封闭邀请制试点即须满足）、**公开上线**（面向公众收费上线的 Gate 0 前置，见 §8）、**涉医美时**（承接医美/医疗商家时触发）。

| # | 义务 | 性质 | 法规依据 | 生效时点 | P0 落点 |
|---|---|---|---|---|---|
| 1 | 算法备案 | 上线前置 | 《互联网信息服务算法推荐管理规定》 | 公开上线 | 备案路径预研（材料+周期），触发点提交 |
| 2 | 生成式 AI 服务登记（省级网信办取上线编号） | 上线前置 | 《生成式人工智能服务管理暂行办法》 | 公开上线 | 同上，绑落地触发点启动 |
| 3 | 页面公示所用模型名称及备案号 | 强制 | 网信办公告要求 | 公开上线 | 页面公示位预留 |
| 4 | AIGC 显式标识（含导出文件，无"如适用"） | 强制 | 《标识办法》第 4 条 | 验证期 | 导出管线强制注入显式标识 |
| 5 | AIGC 隐式标识（文件元数据） | 强制 | 《标识办法》第 5 条 | 验证期 | 导出注入元数据：属性/提供者/编号 |
| 6 | 用户协议写明标识方法/样式 + 用户标识义务 | 强制 | 《标识办法》第 8 条 | 验证期 | 用户协议条款 |
| 7 | 违法内容审核 + 停止生成/传输/报告 | 强制 | 《暂行办法》第 14 条 | 验证期 | 内容安全过滤 + 停止/报告机制 |
| 8 | 投诉举报入口 + 流程/时限公示 | 强制 | 《暂行办法》第 15 条 | 验证期 | 举报入口 + 处理时限公示 |
| 9 | 用户滥用拦截（连续 3 次/日累计 5 次）+ 关键词库 | 强制 | 《生成式 AI 服务安全基本要求》 | 验证期 | 滥用计数 + 关键词/测试题库 |
| 10 | 广告经营者义务：查验身份/建档/留存 ≥3 年/审核人员/证明不全不制作 | 若认定广告经营者则强制 | 《互联网广告管理办法》第 14 条 | 涉医美时 | 资质档案 + 承接留存（轻量版） |
| 11 | 医美内容查验《医疗广告审查证明》 | 强制 | 《医疗美容广告执法指南》第 3 条 | 涉医美时 | Preflight 提醒，缺件强警示（见 §5） |
| 12 | 敏感个人信息单独同意 + 告知必要性/影响 + PIA | 强制 | 《个人信息保护法》第 29/30/55 条 | 验证期 | 素材上传单独同意标记 |
| 13 | 受托处理者 DPA 六项 + 监督 + 禁转委托 + 终止删除 | 强制 | 《个人信息保护法》第 21/59 条 | 验证期 | DPA 模板列为签约交付物 |
| 14 | 人脸信息存储/传输限制（原则上不经互联网对外传输） | 强制 | 《人脸识别技术应用安全管理办法》 | 验证期 | 人脸不发海外模型 API |
| 15 | 未成年人个人信息特别保护 | 强制 | 《个人信息保护法》第 31 条 | 验证期 | 未成年人素材不进公开营销 |
| 16 | 数据出境合规（标准合同 + 备案 / 安全评估） | 强制 | 《促进和规范数据跨境流动规定》 | 公开上线 | 验证期以数据卫生护栏规避（PII 不出境） |
| 17 | 用户服务协议 + 免责声明 + 内容责任归属条款 | 强烈建议 | 综合 | 验证期 | 用户协议区分工具/受托与广告主定位 |

第 1–3、16 条绑 Gate 0（见 §8），验证期以封闭邀请制 + 数据卫生护栏规避；第 4–9、12–15、17 条验证期即须满足；第 10–11 条承接医美商家时触发。

## 3. AIGC 标识（强制口径，无"如适用"空间）

这是我方作为生成合成服务提供者的无条件强制义务，不是"提醒商家"事项。落地口径统一如下，并据此删除 07 §8.3 的"如适用"表述：

- **显式标识**：对 AI 生成或实质改写的文本、图片、视频（脚本），添加显式标识；**提供下载、复制、导出功能时，导出文件本身必须含满足要求的显式标识**（《标识办法》第 4 条）。
- **隐式标识**：在文件元数据中写入隐式标识——生成合成内容属性信息、服务提供者名称或编码、内容编号（《标识办法》第 5 条，"应当"级强制；数字水印为鼓励项，非替代）。
- **用户协议写明标识规范**：在用户服务协议中明确标识的方法、样式，并提示用户阅读理解（《标识办法》第 8 条）。
- **不提供去标识功能**：法规第 9 条允许"有条件豁免 + 留存日志不少于 6 个月"，我方采取更严口径，**不开放移除显式标识的功能**；对移除 AIGC 标识的请求归入行为红线硬停止（见 §5）。
- **分内容形态落地**：文本、图片、视频脚本分别记录标识状态并注入标识；来源不确定的走 `suspected_ai_generated` 兜底标记，宁可多标不可漏标。
- **标识的 UI 形态（不减分反增信，2026-07-08 UI 研究）**：字段/内容级用设计系统 AI label（小图标+"AI"字，**同时是可解释入口**——点开就地解释"这条为何这样生成"，IBM Carbon for AI 范式），视觉中性偏正面（光效/微光）、忌警告色；内容级导出/发布带"内容由 AI 生成"固定文案（对齐即梦/可灵登录态实测的合规文案）。坦诚披露 = 增信而非减分（Fogg up-front disclosure），与 AIGC 强制义务同一动作完成。C2PA 内容凭据（CR pin）为落地期溯源升级项预留。
- **视频成片标识（2026-07-08 视频入 P0）**：显式 = 片头提示 / 角标在薄合成壳（ffmpeg）阶段**烧录进视频文件**，不可后置、不可由用户关闭；隐式 = 文件 metadata 注入；存相册 / 发布包 / 扫码转手机全链保持，注入率 100% 计入 P0 验收（吸收 KickArt 双合规门范式：素材提交内嵌确权勾选 + 发布前预审卡，与本方素材授权字段 / Preflight 同构咬合）。

配套强制性国家标准 GB 45438-2025《网络安全技术 人工智能生成合成内容标识方法》与《标识办法》均自 2025-09-01 施行。AIGC 标识始终保留贯穿验证期与公开上线，不因阶段而降档。

## 4. 内容安全与滥用拦截

现方案合规 gate 只覆盖广告法/医美，缺《生成式 AI 暂行办法》第 14/15 条的通用义务。这一层与业务垂类无关，是我方作为服务提供者对所有生成内容的底线审核，P0 必须补齐：

- **违法不良信息审核**：对政治敏感、暴恐、色情、赌毒等违法不良信息，在生成前与生成后两道过滤；命中即**停止生成/传输、消除内容**，并按需向主管部门报告（第 14 条）。
- **停止生成 + 处置留痕**：发现使用者利用服务从事违法活动的，警示、限制功能、暂停或终止服务，保存记录。
- **投诉举报入口**：设便捷的投诉、举报入口，公布处理流程与反馈时限（第 15 条）。
- **滥用拦截（3/5 规则）**：同一使用者连续 3 次、或一天累计 5 次输入违法不良信息（或明显诱导生成违法不良信息）的，暂停提供服务（《生成式 AI 服务安全基本要求》）。
- **关键词库 + 测试题库**：建立关键词库与测试题库，覆盖五类安全风险；测试题库满足最低条目数量要求，纳入上线前评测。

行为红线（伪造资质、疗效/安全性保证等）属另一类硬停止，集中列在 §5；本节是内容层面的通用安全底线，两者并行。

## 5. 医美/医疗：资质准入制 SOP（轻量版）

医美/医疗是正常经营行业、有正常宣传需求，本产品是工具，做规范筛选而非品类回避。采用"线下资质准入 + 线上纯提醒 + 行为红线硬停"的轻量版（细化 ADR-0003，落地口径见 `docs/adr/0004-qualified-access-medical-content.md`）。

**① 线下接洽筛选（闸门在销售环节，不在产品内逐条校验）**

筛选清单：《医疗机构执业许可证》；平台认证状态（小红书专业号 / 抖音医疗认证等）；有投放需求的另收《医疗广告审查证明》。录入 workspace 资质档案，**未过筛不签约**。资质档案字段：机构主体、执业许可证号、诊疗科目/项目范围、平台认证类型与状态、广审证明编号（如有）、有效期、承接登记时间。

**② 线上 Publish Compliance Preflight = 纯提醒**

保存、导出、发布包交接、L1 官方提交前触发 Preflight：展示资质/授权/平台规则提示，**资质档案缺件时强警示**；**不做逐条门控确认**，不拦截创作与导出；系统日志记录"提醒已展示"（零交互证据，满足已尽提示义务的最低留痕）。

**③ 行为红线硬停清单（与准入制并行，命中即硬拦截）**

- 处方药、未经批准的药品/医疗器械宣传；
- 疗效/安全性保证、治愈率/有效率承诺；
- 伪造资质；
- 未授权顾客案例；
- 移除 AIGC 标识；
- 绕过平台审核、诱导规避审核。

**拦截呈现 = 统一四段式文案（2026-07-08 UI 研究定稿，PAIR/NN/g 依据）**：硬停绝不只弹"违规"——①陈述限制、不指责（不用"违法/错误/invalid"字眼）→ ②一句白话原因 → ③直接给 1 条可采用的合规替换写法 → ④"改用这句"按钮归还控制权。非红线风险走内联 warn 可继续（两档分级沿用 07 §8.1"检测 → 提醒 → 建议替换"机制，此处规范其呈现层）。合规做得有分寸是专业可信感的一部分；坦诚披露（AI 标识、"请核对"）= 增信而非减分。

**采纳即担责确认留痕（Amazon 范式，医美红线场景强制）**：商家在"采用/发布"AI 内容的节点，做一次"已审阅、内容由本店负责"的确认并留痕（时间 / 账号 / 内容版本）。这把 §1 分层原则第三层"商家自负"的责任显式落到可追溯的动作上（Amazon"采用即视为已审阅且合规"范式）；医美/医疗内容采纳强制此确认，普通到店内容可轻量化。

**④ before/after 素材授权标记（非发布时门控）**

不做预置营销模板；沿用素材库授权状态字段——素材上传时标记"顾客已授权"方可进入内容（人脸 + 医疗健康双重敏感，单独同意是个保法硬要求）。这是**一次性上传标记**，不是发布时逐条门控确认。

**⑤ 陪跑边界**

医美商家陪跑限于工具使用辅导，陪跑人员**不代写、不改写医疗声明性内容**——守住"工具"定位，避免滑向"受托制作"而被认定为广告经营者。

**⑥ 试点探针与 Week-0 实测**

试点反向纳入 1–2 家已过平台认证的医美机构作探针；Week-0 用真实认证商家账号**实测**小红书/抖音医美类目当前的商家侧发布权限边界，以实测替代二手报道的禁令传闻。

## 6. 素材授权与个人信息

我方替商家处理顾客照片、微信号、手机号，法律定位是**受托处理者**；顾客人脸 = 生物识别 = 敏感个人信息，医美 before/after 叠加医疗健康属性，双重敏感。

- **单独同意**：处理敏感个人信息须取得单独同意，并告知处理必要性及对个人权益的影响（《个人信息保护法》第 29/30 条）。
- **拍摄同意 ≠ 公开宣传同意**：拍摄留档的同意不等于对外公开发布的同意；公开发布需另行单独同意，且不得超出授权的平台/期限/项目/形式范围。
- **素材授权状态字段**：`source_type`（真实/AI 生成）、`rights_owner`、`consent_scope`（internal_reference / publish_package / public_marketing / paid_ads 四级）、`contains_person`、`contains_sensitive_personal_info`、`minor_involved`、`redaction_status`；**撤回授权后进入下架/替换队列**。
- **未成年人**：未成年人素材不进入公开营销（`public_marketing` / `paid_ads`）范围。
- **线索台账最小化**：线索台账存微信号/手机号/备注属个人信息处理，落最小化 + 告知同意 + 留存期限 + 租户隔离（workspace_id）。
- **DPA 受托处理者要点**：与商家签数据处理协议，约定处理目的/期限/方式/种类/保护措施/双方权责六项；接受商家监督、禁止擅自转委托、合同终止返还或删除（《个人信息保护法》第 21/59 条）。
- **人脸存储与传输**：人脸信息原则上不经互联网对外传输（除法律规定或单独同意）；验证期数据卫生护栏——顾客 PII/人脸不发海外模型 API，相关任务路由国产模型或先脱敏（《人脸识别技术应用安全管理办法》，2025-06-01 施行）。

## 7. 广告法与价格表达

- **敏感词 + 替代表述机制**：沿用 07 §8.1 的广告法敏感词表与替代表述——检测到"最/第一/顶级/全网最低/永久/100%/绝对/保证/根治/疗效/纯天然/无副作用"等风险表述，给出可操作的建议替换（如"永久定型 → 更持久的造型效果"）。对纯到店生活美业，"检测 → 提醒 → 建议替换、不一刀切"成立，风险主要是普通违规用语，提醒 + 改写可有效降险。
- **价格/优惠来自价目表，不由模型猜测**：价格、优惠、套餐信息一律取自门店价目表/活动表/商家确认，模型不得自行生成价格数字。价格结构化必填字段：`price_source`、`valid_from`、`valid_until`、`inventory_or_quota`；缺来源的价格不得进入内容。
- **广告可识别性**：以"知识介绍、体验分享、消费测评"等形式推销并附加购物链接等购买方式的（种草笔记），提醒需显著标明"广告"（《互联网广告管理办法》第 9 条）；"三品一械"健康养生科普不与经营者联系方式/购物链接同页出现。

## 8. Gate 0 与阶段化时点表

义务生效点绑定其真实生效时刻，不前置成立项门槛。**Gate 0 = 公开收费上线的前置门**：算法备案 + 大模型登记 + 页面公示三件套完成，方可面向公众收费上线。验证期为封闭邀请制（合同制、非公开注册）试点，不触发 Gate 0 义务（落地策略见 `docs/adr/0005-phase-gated-deployment-and-models.md`）。

| 义务 | 验证期（现在 → 落地触发点） | 公开上线（Gate 0 触发后） |
|---|---|---|
| 算法备案 + 大模型登记 + 页面公示 | 不触发；做备案路径预研（材料清单 + 周期） | **必须完成，未完成不上线** |
| 模型路由 | 国内外混用按效果路由；国产模型同步进 benchmark 作平移对照 | 默认切国产已备案模型；海外模型转内部研发/评测 |
| 数据出境 | 数据卫生护栏规避：顾客 PII/人脸不发海外 API | 若仍有出境，订标准合同 + 备案（敏感个人信息量小也走） |
| 部署 | Cloudflare 优先，最快迭代 | Node 服务 + Postgres 平移国内云，对象存储换 OSS/COS |
| AIGC 标识 / 内容审核 / 滥用拦截 / 个保与 DPA | **验证期即须满足，不因阶段降档** | 维持 |

触发点不预设，按商业化节奏决定（建议 = pilot Go 判定日）。已知提前量记录在案：算法备案 + 大模型登记约 1–3 个月，ICP 约 2–4 周——谁排公开收费上线，谁就得提前这么久启动，否则备案排队会卡住商业化。

## 9. 分层验收指标

把合规验收从"提醒覆盖率 100%"改为分层硬指标：

| 指标 | 口径 | 目标 |
|---|---|---|
| Gate 0 完成 | 备案 + 登记 + 公示三件套（公开上线前置） | 是 / 否 |
| 行为红线硬拦截覆盖率 | 处方药/未批药械、疗效安全性保证/治愈率承诺、伪造资质、未授权顾客案例、移除 AIGC 标识、绕过平台审核、违法不良信息与滥用（3/5） | 100% |
| AIGC 标识注入率 | 含导出文件显式标识 + 隐式元数据 | 100% |
| 素材授权字段留痕率 | 含人物/敏感个人信息的素材均有授权状态与 consent_scope 字段 | 100% |
| Preflight 提醒展示日志留存 | 缺件强警示 + "提醒已展示"日志（零交互证据） | 留存可查 |

行为红线硬拦截对普通用户不可 override；验收以此表为准，替代分散在 02/03/04/07 章的"提醒覆盖率"口径。

---
